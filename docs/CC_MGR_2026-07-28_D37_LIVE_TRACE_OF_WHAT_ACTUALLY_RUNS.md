# CC 管理(MGR) → 設計/監査(CC-α): **★D-37 — Taka の6問。「読んで答える」を禁じる。1件投入して、実際に走った関数を記録して答えること**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **典拠: Taka（2026-07-28・逐語 §1）**

## 1. Taka の問い（逐語・要約しない）
> 「**今どのようなロジックが具体的に走っているか細かく、正しい内容を私に報告して。RRIを叩いた際に4軸７カテゴリの分類が走るか。走った際にRRIは次のアクションの決定を行うことができるか。その上でEGLに書き込む、読み出すなどの処理が行われるか。DWまで動いているならどういう動きをしているか。台帳は勘定科目、サブIDなど自動で割り当てられているか。本来の仕様がそもそも認識されているか。**」

## 2. ★方法の指定（これが本依頼の中心）
**★コードを読んで答えることを禁じる。**
> **理由: 本日、読んで出した結論が1件、丸ごと誤っていた**（「front door から台帳を読む経路が無い」→ **`/api/resolve` は在って動いた**）。
> **∴ 同じ方法で6問に答えれば、同じ精度の答えになる。**

**★指定する方法:**
```
① 依頼文を1件、front door（webui /api/submit）から投入する
② その1回の処理で、★実際に呼ばれた関数を記録する
   （例: sys.setprofile / トレース / 各関数の入口にログを仕込むのではなく観測する など。手段は任せる）
③ 「呼ばれた」「呼ばれなかった」を、関数名つきで表にする
④ 記録に残った ID を front door（/api/resolve）で引き、返った中身を貼る
```
- **★依頼文の内容は Taka の実運用に近いものを1つ。** **短い挨拶や試験用文字列にしない**（分岐が走らない）。
- **★1回でよい。** **1回の観測であることを明記する。**
- **★「走らなかった」も同じ価値の答えである。** **走らせるために依頼文を書き換えない。**

## 3. 答えるべき6問（Taka の順のまま）
| # | 問い | 答え方 |
|---|---|---|
| **Q1** | **RRI を叩いた際に4軸7カテゴリの分類が走るか** | **★「7カテゴリ」と「4軸」を分けて答える。** どちらの関数が呼ばれ、どちらが呼ばれなかったか |
| **Q2** | **RRI は次のアクションの決定を行えるか** | **決定した値と、それを出した関数名。** 決めていないなら「決めていない」 |
| **Q3** | **EGL への書き込み・読み出しが行われるか** | **書いた関数／読んだ関数／書かれた ID。** **その ID を `/api/resolve` で引いて中身を貼る** |
| **Q4** | **DW はどう動いているか** | **状態遷移（どの op が走ったか）と、止まった場所** |
| **Q5** | **台帳は勘定科目・サブ ID などを自動で割り当てているか** | **★実際に割り当てられた値を貼る。** 割り当てられていないなら「されていない」＋その ID の実物 |
| **Q6** | **本来の仕様がそもそも認識されているか** | **★これは「仕様書がどこに在り、実装がそれを参照しているか」である。** **無いなら「無い」** |

**★Q6 について（私の見立て・あなたが確かめること）:**
**本日 D-18 で「4軸→7戦略の決定論セレクタは `WIRED_UNENTERED`、4軸のうち3軸は本番で作られていない」と報告された。**
**∴ Q6 の答えは「仕様は在るが、実装が参照していない箇所が在る」になる可能性が高い。** **★しかしこれは私の予想である。** **実行で確かめること。** **予想に合わせないこと。**

## 4. ★禁止
- **読んだだけで「走る」と書かない**（運用方針 §3-6）。
- **走らせやすい依頼文に書き換えない。**
- **★答えが期待と違っても、答えを曲げない。** **「4軸は走らなかった」なら、そう書く。**
- **新しい台帳を作らない**（★Taka 常設命令 §0-2・本日発効）。**トレース結果は文書に書く。**
- **★台帳に直接手を出さない。** **記録の確認は `/api/resolve` を使う。**

## 5. 期限と扱い
- **★これは Taka への報告になる。** **私が要約して渡す。** **∴ 事実だけを、関数名と値つきで書くこと。**
- **分からなかった項目は「分からなかった」と書く。** **★その場合、§3-7 に従い「いつ誰が確かめるか」を同じ行に書く。**

---
*MGR。D-37。★Taka の6問（逐語保存）=RRI を叩くと4軸7カテゴリの分類が走るか／RRI は次のアクションを決定できるか／EGL への書き込み・読み出しが行われるか／DW はどう動いているか／台帳は勘定科目やサブ ID を自動で割り当てているか／本来の仕様がそもそも認識されているか。★方法を指定する（これが本依頼の中心）=**コードを読んで答えることを禁じる**。理由は本日、読んで出した結論が1件丸ごと誤っていたから（「front door から台帳を読む経路が無い」→`/api/resolve` は在って動いた）∴ 同じ方法で答えれば同じ精度の答えになる。指定する方法=①front door から依頼文を1件投入 ②その1回で実際に呼ばれた関数を記録 ③「呼ばれた／呼ばれなかった」を関数名つきで表に ④記録に残った ID を `/api/resolve` で引いて中身を貼る。依頼文は実運用に近いものを1つ（試験用文字列にしない）、1回でよくその旨を明記、「走らなかった」も同じ価値の答えであり走らせるために依頼文を書き換えない。★6問の答え方=Q1 は「7カテゴリ」と「4軸」を分けて／Q2 は決定した値と出した関数名／Q3 は書いた関数・読んだ関数・書かれた ID とその `/api/resolve` の中身／Q4 は状態遷移と止まった場所／Q5 は実際に割り当てられた値／Q6 は仕様書の所在と実装がそれを参照しているか。Q6 について MGR の見立ては「仕様は在るが実装が参照していない箇所が在る」（D-18 の 4軸→7戦略が `WIRED_UNENTERED`・4軸のうち3軸は本番で未生成）だが、★これは予想であり実行で確かめ、予想に合わせないこと。禁止=読んだだけで「走る」と書かない／走らせやすい依頼文に書き換えない／答えを曲げない／新しい台帳を作らない（Taka 常設命令・本日発効）／台帳に直接手を出さず確認は `/api/resolve` を使う。分からなかった項目は「分からなかった」と書き、§3-7 に従って「いつ誰が確かめるか」を同じ行に書く。*
