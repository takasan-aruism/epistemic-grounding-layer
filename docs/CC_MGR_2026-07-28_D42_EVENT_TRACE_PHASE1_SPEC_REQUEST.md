# CC 管理(MGR) → 設計/監査(CC-α): **★D-42 — Event Trace 第一段階の SPEC を書いてください。★入口ごとの注意で防がない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **正典**: `EVENT_TRACE_PHASE1_WORK_ORDER_v1_0.md`（Taka 逐語・本日受領）

## 1. ★この作業の合否を決める1行（Taka の言葉）
> **「入口ごとの注意事項で防ぐのではなく、★どの入口から入っても同じ記録機構を必ず通る構造に直すべきです。」**

**∴ SPEC の良し悪しは、★「記録しない入口を作れないか」で決まる。**
**∴ 「CLI でも記録するようにした」では足りない。** **★「記録せずに投入する経路が構造上存在しない」でなければならない。**
**∴ 受入に、★「記録を通さずに投入できる経路が無いこと」を示す項目を必ず入れること。**

## 2. ★先に解くこと（3つ。実物で）
| # | 問い | なぜ先か |
|---|---|---|
| **1** | **CLI と webui は本当に同じ関数を通るか。** **他に入口は無いか**（`operator` / `dispatch` / 直接 import など） | **★入口を数え漏らせば、そこが次の抜け道になる。** **「探した範囲に無い」を「無い」と書かない**（§3-7） |
| **2** | **記録先は既存で足りるか。** **DW `events.jsonl` は `task_id` 単位だが、受付・DS登録・RRI開始は `task_id` 前である** | **★私の裁定①は、そのままでは成り立たない**（正典 §2-3）。**足りないなら「足りない。理由は◯◯」と書く** |
| **3** | **`親 event_id` の親子を、誰がどう決めるか** | **★これが無いと「一本の系列」にならない**（D-41 の最重要項目）。**設計で決める。実装に委ねない** |

## 3. ★裁定を仰ぐこと（あなたが決めない）
| # | 論点 | 出し方 |
|---|---|---|
| **A** | **記録失敗時に本処理をどう扱うか**（Taka の完了条件） | **★fail-closed と fail-open の★両方の帰結を書く。** 「RRI 正典 §3 は根拠を保存せずに進めないと言うが、記録の不具合で本番が止まる」——**この対立をそのまま出す。** **★私が裁定する** |
| **B** | **新しい記録先を作る必要が在るか** | **在るなら「既存では足りない。足りないのは◯◯」まで。** **★作ってよいのは共通の Event Trace 記録先1つだけ**（Taka がより新しい指示で開いた範囲）。**ついでに他を作らない** |

## 4. ★禁止
1. **★新しい RRI 判断ロジックを作らない**（Taka 明示）。**4軸・7戦略・EGL 仕様・DW 仕様を書き換えない。**
2. **★既存の判断結果・返り値・保存内容を変えない**（完了条件）。**変わっていないことを★実測で示す。**
3. **★第二段階を先取りしない。** **関数内部の全分岐・「呼ばれなかった処理」は今回やらない。**
4. **★記録先を2つ作らない。** **「CLI 用」「webui 用」を作った時点で、この作業は失敗である。**
5. **★入口に注意書きを足すことを解にしない。** **本日それが効かないことを実証した**（ガイドに書いてあったのに CLI が正規手順として配られていた）。

## 5. 受入（Taka の完了条件＋MGR の追加1件）
```
Taka:
 ① 同一内容を CLI と webui から各1件投入する
 ② 両方とも同じ形式で Event Trace が残る
 ③ DS → RRI → EGL → DW のどこまで進んだかが trace_id で追える
 ④ 既存の判断結果・返り値・保存内容が変わっていない
 ⑤ 記録失敗時に本処理をどう扱うかを明示する
 ⑥ 実データを貼って証明する

MGR 追加:
 ⑦ ★記録を通さずに投入できる経路が無いことを示す（入口の列挙 + 各入口が記録関数を通ることの実測）
```
**★①について**: **Taka は「投入する」と明示した。** **∴ 本件に限り投入を許可する。** **★2件だけ。** **依頼文は実運用に近いものを1つ。**
**★投入する前に、その依頼文を本文書に書いて私に見せること。** **不要な記録を1件も増やさないため。**

## 6. 私（MGR）が本 build でやること
- **A と B を裁定する。** **受領して commit する。**
- **★私は「共通の記録先」の形を指定しない。** **本日、私が形を先に決めて2回外している**（裁定①の前提が崩れた／二択の立て方が誤り）。

---
*MGR。D-42。★合否を決める1行は Taka の「入口ごとの注意事項で防ぐのではなく、どの入口から入っても同じ記録機構を必ず通る構造に直すべき」であり、SPEC の良し悪しは「記録しない入口を作れないか」で決まる ∴「CLI でも記録するようにした」では足りず「記録せずに投入する経路が構造上存在しない」でなければならず、受入に「記録を通さずに投入できる経路が無いこと」を示す項目を必ず入れる。★先に解くこと3つ=①CLI と webui は本当に同じ関数を通るか・他に入口は無いか（入口を数え漏らせばそこが次の抜け道。「探した範囲に無い」を「無い」と書かない）②記録先は既存で足りるか（DW `events.jsonl` は `task_id` 単位だが受付・DS登録・RRI開始は `task_id` 前なので**私の裁定①はそのままでは成り立たない**。足りないなら理由まで書く）③`親 event_id` の親子を誰がどう決めるか（無いと「一本の系列」にならない最重要項目。設計で決め実装に委ねない）。★裁定を仰ぐこと=A 記録失敗時の扱いは fail-closed と fail-open の両方の帰結を書き、「RRI 正典 §3 は根拠を保存せずに進めないと言うが記録の不具合で本番が止まる」という対立をそのまま出す（MGR が裁定）／B 新しい記録先が要るかは「既存では足りない。足りないのは◯◯」まで書き、作ってよいのは共通の Event Trace 記録先1つだけでついでに他を作らない。★禁止=新しい RRI 判断ロジックを作らない・4軸/7戦略/EGL 仕様/DW 仕様を書き換えない／既存の判断結果・返り値・保存内容を変えず変わっていないことを実測で示す／第二段階を先取りしない／★記録先を2つ作らない（「CLI 用」「webui 用」を作った時点で失敗）／★入口に注意書きを足すことを解にしない（本日それが効かないことを実証した）。★受入は Taka の6条件＋MGR 追加の⑦「記録を通さずに投入できる経路が無いことを示す（入口の列挙＋各入口が記録関数を通ることの実測）」。①について Taka が「投入する」と明示したので本件に限り投入を許可するが2件だけで、依頼文は実運用に近いものを1つとし、**投入する前に依頼文を文書に書いて MGR に見せる**（不要な記録を1件も増やさないため）。★MGR は A と B を裁定し受領して commit するが、**共通の記録先の形は指定しない**——本日、私が形を先に決めて2回外している。*
