# 【BUILD SPEC】**★planner に観測の中身を見せる**（`_plan_prompt` 1箇所）

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 03:3x / TYPE=BUILD_SPEC
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-31_HOLD_THE_DIAGRAM_PROVE_PLAN_USES_OBSERVATION.md` ／ **承認**: `CC_MGR_2026-07-31_D153_…` §4
- **★2DER 優先原則の例外**（IMPL が書く）。**★担当工程数の差分は 0 の見込み。★0 なら「0」と書く。**

---

## 0. ★MGR §1 の(c) を確かめた（★写: MGR。★これで(a)(b)(c) が揃った）
```
★対象: 計画の全文（4535字）★打ち切り無し・私自身の走査
   「観測」0 ／「既に」0 ／「取得済」0 ／「ss -ltn」0 ／「プロセス」0   ← ★IMPL の申告と一致
★★さらに強い事実（★私が足す）:
   「OBS-」は★8回 出るが、★全部 `cites_source_ids` と `provenance.egl_source_refs` の中だけである。
   ★objective / scope / steps / completion_criteria / requirement に ★`OBS-` は★0回。
∴ ★引用は「欄」にしか無い。★計画の本文は、★観測を一度も参照していない。
```

---

# 1. ★やること（★1箇所だけ）

> ### **★`twoder/build_planner.py` の `_plan_prompt` に、★引用した観測の★中身を渡す。**

| 条件（★MGR `D-153` §4。★増やさない） | |
|---|---|
| **1** | **★既に在る観測を渡すだけ。★新しい台帳・API・ID 族を作らない** |
| **2** | **★GPU 固有・ポート固有の語を入れない**（★`gpu` / `nvidia` / `port` を diff に0件・大小無視・打ち切り無しで示す） |
| **3** | **★観測を Task の下流へ移さない**（★線は動かさない。★参照と中身を渡すだけ） |
| **4** | **★後方互換**: `BUILD_CAPABILITY`/`MODIFY_EXISTING` 経路の PLAN を壊さない（★§3 の基準値） |
| **5** | **★受入は Taka の4条件のまま。★1文字も緩めない** |
| **6** | **★PLAN が出たら止める。★GENERATE へ進まない** |

## 1-1. ★設計からの補足（★あなたが探し直さなくていいように）
```
★渡す先 : build_planner.py:99-126 `_plan_prompt(goal, provenance)`
          ★現在プロンプトに入るのは ★goal と ★provenance["trace_id"] だけ
★渡す元 : ★provenance["egl_source_refs"]（★D-152 で入った。★OBS- の id）
          ★中身は front door でも引ける（`GET /api/resolve?id=OBS-…`）。★submit の TRACE にも `MEASURED_STATE` が在る
★★どこから中身を取るかは あなたが選ぶ。★狭い方を採り、★選んだ理由を1行 書く
★★プロンプトに入れる文言は「観測」一般の語にする（★`port`/`gpu` を書かない＝条件2）
```

---

# 2. ★確認（★依頼文は★MGR が固定した。★2026-07-31 03:2x・`D-155` §2）

## 2-0. ★依頼文（★これを1文字も変えずに投入する）
```
現在動いているコンテナの状況を確認し、確認できない項目があれば、それを取得できるようにする方法を検討してください。
```
**★`CC_MGR_2026-07-31_D155_THE_TEXT.md` の §2 から機械で抜くこと**（★打ち直さない）。
**★設計が事前に計算した値（★一致を確認してから投入する）**:
```
★字数 57 ／ ★sha1 9a6415be83d4918f1d05a8d3e6626712096fd243
★★予告 task_id: TASK-2DER-9A6415BE   ← ★違う id が出たら「外れた」と書く
```
**★設計の予告（★§4 に追加）**: **★分類は `OBSERVE_CURRENT_STATE` に落ちると予想する。**
**★理由: 前回（ポート）の同型文が `OBSERVE_CURRENT_STATE` だったため。★前回 私はここを外し、しかも理由も誤った ∴ 今回は実測に合わせる。★外れたら「外れた」と書く。**

## 2-1. ★確認の前提（★実証済）

```
★★同じ依頼文では届かない（★実証済・D-148）:
   task_id は依頼文の sha1 ∴ ★同じ文＝同じ task ＝ CREATE 確定済 ＝ ★新しい packet は書かれない
∴ ★確認には★新しい依頼文が要る。★MGR `D-153` §4-6 のとおり ★MGR が固定する。
★★あなたは ①修正 を先に進めてよい。★投入は文が届いてから。
```
## 2-1. 手順（★文が届いたら）
```
① 修正 → webui 再起動（★全件 記録。★run-gate 初期化も）
② ★依頼文を MGR の文書から機械で抜く（★打ち直さない。★字数と sha1 を書く）
③ ★task_id を投入前に自分で計算して予告に書く（`TASK-2DER-` + sha1 先頭8桁 大文字）
④ POST /api/submit ★1回 → ★直後に GET /api/receipt → run_next ★1回
⑤ `GET /api/claude_packet?task_id=…` で計画の全文を引く → ★PLAN が出たら止める
```

---

# 3. ★受入（★1条件に1つの印。★「概ね」と書かない）

| # | 受入（★Taka 逐語） | ★示し方 |
|---|---|---|
| **1** | `cites_source_ids` が空でない | 実値 |
| **2** | 対象 Observation の ID を引用する | **★その task の `egl.source_refs` と一致するか** |
| **3** | **★既に観測済みの能力を、新規作成対象として重複計画しない** | **★`objective`/`files_expected`/`steps` の実文。★「取得するツールを作れ」が出たら不成立** |
| **4** | **証拠から不足が判定された場合だけ改修 Task を作る** | **★計画が「観測の結果、何が取れていないか」を述べているか**（★依頼文の言い換えではなく、★観測に基づくこと） |
| **★補** | **★観測が本文に届いたか（★今回 私が足す観測点）** | **★`objective`/`scope`/`steps`/`completion_criteria`/`requirement` に `OBS-` または観測の値が★1回でも出るか**（★前回は 0回） |
| 補 | 後方互換 | **★基準値11件**（`D-151 SPEC §3-1`）を取り直して1件ずつ |
| 補 | 1箇所・語の禁止 | 変更ファイル／hunk／挿入削除行数 ／ `gpu`/`nvidia`/`port` 0件 |

---

# 4. ★予告（★投入前に固定する。★外れたら「外れた」と書く）
```
★私は「受入3 が立つ」と予告しない。★見せても同じ計画を書くかもしれない。
★★予告するのは1点だけ: ★「補」の観測点（本文に観測が届く）は★立つ。
   ★理由: プロンプトに入れれば、★出力に反映される可能性が高い。★これは能力の話ではなく入力の話である。
★★受入3 が立たなかった場合、★それは「見せても効かない」という結果であり、★失敗ではない。
   ★そのときは「見せても効かなかった」と書く。★言い換えない。
```

---

# 5. ★やってはいけないこと
```
★観測を Task の下流へ移す設計変更  ★GENERATE を押す  ★新しい台帳・API・ID 族の追加
★Ledger（生出力）を触る（★保留中）  ★別の欠陥を見つけたことを理由に止まる
★「概ね引用できた」「概ね使えた」と書く  ★commit しない  ★61本の非回帰は走らせない
★依頼文を自分で決める（★MGR が固定する）  ★:8005 を自分で叩かない
```

# 6. ★止まってよい場所（★狭い）
```
★この1件が実行不能である具体的証拠 ／ データ破壊・既存互換性破壊・ロールバック不能の証拠
★★それ以外は止まらない。★2通りに読めたら狭い方を採って進み、★迷いを1行 書く
```

# 7. ★報告（★正典の6項目）
```
1. 変更した1箇所  2. 受入1〜4＋補 の判定（★1条件に1つの印）  3. Last PASS  4. First FAIL
5. ★2DER 担当工程数の前回差分（★0 なら「0」）  6. 次に直す1件
★予告の当否  ★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---
**決めたこと**: **①MGR §1(c) を私も走査して一致（「観測」「既に」「取得済」「ss -ltn」「プロセス」＝各0）、さらに `OBS-` は欄にしか無く本文には0回＝引用は欄だけで中身を参照していない ②直すのは1箇所＝`_plan_prompt` に観測の中身を渡す（既に在るものを渡すだけ・`gpu`/`nvidia`/`port` を diff に0件・観測を task 下流へ移さない） ③確認には新しい依頼文が要る（同じ文では届かないことは実証済）ので MGR が固定する——IMPL は修正を先に進めてよい。予告は「本文に観測が届く」1点だけで、受入3 が立つとは予告しない。**
