# 設計/監査 → MGR（写: Taka / IMPL）: **D-70 — 155件の状態を★全数 実測した。Taka の4区分のうち★3つは根拠が在り、★1つは無い**

- `BUILD_ROLE: 参照`（**調査のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **典拠**: Taka 逐語（4区分・欠落5件）

## 0. ★結論
> **① 実数は★155件**（画面の「160件」は Taka の目視。**★正確には155**）。
> **② ★状態は既に機械で決まる。** **全数を front door で引けた**（8種類）。
> **③ Taka の4区分のうち、★3つは根拠が在る。★「現在前面化すべきもの」だけ根拠が無い。**
> **④ ★名前を取る関数は既に在る**（`_task_goal`）。**★捨てられているだけである。**

---

## 1. ★全数の実測（★サンプルではない・打ち切り無し）
```
再現: GET /api/tasks → 155件 → 1件ずつ GET /api/resolve?id=TASK-… で state を取得
総件数 155 / 確認 155 / 打ち切り無し / ★読み取りのみ（投入していない）

  67  READY_FOR_AUDIT
  49  READY_FOR_IMPLEMENTATION
  18  JUDGE_REQUIRED
  12  CREATED
   5  READY_FOR_UPPER_REVIEW
   2  BLOCKED
   1  READY_FOR_REGENERATE
   1  DISPOSITION_REQUIRED
 ───
 155
```

## 2. ★Taka の4区分に振り分けられるか（★根拠を1つずつ示す）
| Taka の区分 | **根拠が在るか** | **振り分けの根拠（決定論）** | 件数 |
|---|---|---|---|
| **停止中** | **★在る** | `state == BLOCKED` | **2** |
| **依存待ち**（人の判断待ち） | **★在る** | `next_legal_operation.claude_barrier == True`（`CREATED`→PLAN / `DISPOSITION_REQUIRED` / `READY_FOR_UPPER_REVIEW` / `JUDGE_REQUIRED`）**※§2-1 で実測が要る** | **★未測（下記）** |
| **背景保持** | **★在る** | 上記以外（機械が進められる: `READY_FOR_IMPLEMENTATION` / `READY_FOR_AUDIT` / `READY_FOR_REGENERATE`） | **★117**（49+67+1） |
| **現在前面化すべきもの** | **★無い** | **★「前面化すべき」を決める値が、どこにも無い**（§3） | **★決められない** |

### 2-1. ★私が測っていないもの（先に書く）
> **`claude_barrier` は `/api/state` に在るが、★私は155件について引いていない**（state だけを引いた）。
> **∴ 「依存待ち」の件数は★出していない。** **★出せると言えるが、★出していない。**
> **★推測で 18+12+5+1=36 と書かない。** **`JUDGE_REQUIRED` が barrier かを確かめていないためである。**

## 3. ★「現在前面化すべきもの」に根拠が無い（★最重要）
```
再現（実読）: _active_2der_tasks は COMPLETE/BLOCKED を除いた TASK-2DER* を
             ★「最近順」で返すだけである。優先度・期限・重要度の値を1つも持たない。
再現（実読）: submit.py:378 _rec("DEFERRED_ACTIVE_TASKS", active)
             コメント: 「honest: prior tasks exist but are not dominating」
```
> **★「前面化すべき」を決める値が、2DER のどこにも無い。**
> **∴ この区分を作れば、★我々が優先度を発明することになる。**
> **★Taka の例では「現在前面化すべきもの 0件」だった。** **★0 と書くのは正しいが、★理由が違う**——
> **「今は無い」ではなく「★決める値が無い」である。**

**∴ 出すなら文言はこうである**（★意味だけ。MGR の裁定を仰ぐ）:
> **「前面化すべきものを決める値が 2DER に無いため、この区分は出せません」**

## 4. ★Taka の欠落5件（逐語）に答える
| Taka の指摘 | **出せるか** | 根拠 |
|---|---|---|
| **タスク名なし** | **★出せる** | **`_task_goal(W, tid)` が既に在る**（`submit.py`・実読）。**★捨てられているだけ** |
| **状態なし** | **★出せる** | **§1 で全数 実測済**（8種類）。**`derive_state` が既に計算し、★捨てている** |
| **開始日なし** | **★出せる見込み** | **DW の `CREATE` event に `ts` が在る**（`workcell._append_event`・実読）。**★私は155件について引いていない** |
| **優先度なし** | **★出せない** | **§3**。**値が無い** |
| **なぜ表示されたか不明** | **★出せる** | **★「COMPLETE でも BLOCKED でもないから」である**（`_active_2der_tasks` の実読）。**★1行で書ける** |

> **★5件のうち4件は「既に在るが捨てている」である。** **★1件だけが「無い」である。**
> **★これは本日ずっと出ている型と同じである。**

## 5. ★件数（MGR §4 の求め）
```
出す候補: 停止中 2 / 背景保持 117 / 依存待ち ★未測 / 現在前面化すべき ★決められない
★合計 155 のうち、区分が確定しているのは 119件。残り36件は claude_barrier を引けば確定する。
```
> **★「全件を出さない」は満たせる。** **★停止中2件だけを出せば、★2行になる。**
> **★ただし何件を出すかは★まだ決めていない**（MGR §4-1「何件にするかはあなたが決める」）。
> **★決める前に、★依存待ちの実測が要る。** **∴ ★ここで止めて MGR に返す**（MGR §2-4「件数が出た時点で私に返すこと」）。

## 6. ★私は実装しない
- **★調査のみ。** **コードを1行も変えていない。** **★投入していない。**
- **★4区分を勝手に変えない。** **★増やさない。★言い換えない。**
- **★LLM に分類させない**（MGR 明示）。**★上の振り分けはすべて決定論である。**

## 7. ★未確認
| # | | 誰が・いつ |
|---|---|---|
| 1 | **`claude_barrier` を155件について引いていない**（「依存待ち」の件数） | **★CC-α / MGR が「続けよ」と言えば直ちに** |
| 2 | **`CREATE` event の `ts` を155件について引いていない**（開始日） | 同上 |
| 3 | **画面の「160件」と実測155件の差** | **★Taka の目視である。★私は155と測った。どちらが正しいかは言わない** |
| 4 | **`JUDGE_REQUIRED` が `claude_barrier` かを確かめていない** | **★これが「依存待ち」の件数を左右する** |

---
*CC-α D-70（調査のみ）。★結論=①**実数は155件**（画面の「160件」は Taka の目視）②**状態は既に機械で決まり全数を front door で引けた**（8種類）③**Taka の4区分のうち3つは根拠が在り「現在前面化すべきもの」だけ根拠が無い** ④**名前を取る関数は既に在る**（`_task_goal`）が捨てられている。★全数の実測（サンプルではない・読み取りのみ）=`READY_FOR_AUDIT` 67／`READY_FOR_IMPLEMENTATION` 49／`JUDGE_REQUIRED` 18／`CREATED` 12／`READY_FOR_UPPER_REVIEW` 5／`BLOCKED` 2／`READY_FOR_REGENERATE` 1／`DISPOSITION_REQUIRED` 1＝155。★4区分の振り分け=**停止中**は `state==BLOCKED` で**2件**／**依存待ち**は `claude_barrier==True` で決まるが**155件について引いていないので件数を出していない**（**推測で36と書かない**——`JUDGE_REQUIRED` が barrier かを確かめていないため）／**背景保持**は機械が進められるもので**117件**／**現在前面化すべきものは根拠が無い**。★**最重要=「前面化すべき」を決める値が 2DER のどこにも無い**（`_active_2der_tasks` は COMPLETE/BLOCKED を除いた `TASK-2DER*` を最近順で返すだけで優先度・期限・重要度を1つも持たない）∴ **この区分を作れば我々が優先度を発明することになる**。**Taka の例の「0件」は正しいが理由が違い、「今は無い」ではなく「決める値が無い」である** ∴ 出すなら「前面化すべきものを決める値が 2DER に無いため、この区分は出せません」（意味だけ・MGR 裁定を仰ぐ）。★**Taka の欠落5件のうち4件は「既に在るが捨てている」**=タスク名は `_task_goal` が在る／状態は `derive_state` が計算して捨てている／開始日は DW の `CREATE` event の `ts` に在る見込み（155件については未取得）／**なぜ表示されたかは「COMPLETE でも BLOCKED でもないから」で1行で書ける**——**優先度だけが「無い」**。★件数=区分が確定しているのは119件、残り36件は `claude_barrier` を引けば確定する。**「全件を出さない」は満たせ、停止中2件だけなら2行になるが、何件を出すかはまだ決めておらず、決める前に依存待ちの実測が要る** ∴ **ここで止めて MGR に返す**。★CC-α は実装せず4区分を変えず増やさず言い換えず、**振り分けはすべて決定論**（LLM に分類させていない）。★未確認=`claude_barrier` と `CREATE` の `ts` を155件について引いていない／**画面の「160件」と実測155件の差は Taka の目視であり、どちらが正しいかは言わない**／**`JUDGE_REQUIRED` が `claude_barrier` かを確かめていない（依存待ちの件数を左右する）**。*
