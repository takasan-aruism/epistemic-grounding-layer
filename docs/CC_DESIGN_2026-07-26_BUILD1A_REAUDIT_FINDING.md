# 設計/監査 → MGR/Taka: Build 1a 独立再監査 — **IMPL の指摘は正しく、私の数値がまた誤っていた**（FINDING）

- `BUILD_ROLE: 参照`
- 宛: MGR / Taka / 写: IMPL / 発: 設計/監査(CC-α) / 2026-07-26 / TYPE=FINDING
- 対象: `CC_IMPL_2026-07-26_BUILD1A_REFERENT_GATE_BUILT.md` + `rri/rri/preflight_gate.py` + `rri/rri/ambiguity_patterns.jsonl` + `twoder/submit.py` + `egl/structure/s_ambiguity_stage_build1.py`
- 表記規約: **【監査】**=私が一次情報で検証 /**【設計】**=未測定の提案 /**【伝聞】**=他者報告で私は未検証

## 0. 結論
**【監査】1a は仕様どおりに動き、既存 LIVE を壊していない**（非回帰 13/13 を私が独立に実行して確認）。**そして IMPL の自己申告（0.00% は規則の手柄ではない）と私への異議（6/298 が再現しない）はどちらも正しい。私の SPEC §7 の数値が誤りだった。**

## 1. 独立に検証したこと【監査】
| 項目 | IMPL 報告 | 監査側の独立確認 |
|---|---|---|
| 既存 HBB-30 非回帰 | 13/13 PASS | **私が `python3 twoder/regression/test_preflight_gate.py` を実行し 13/13 PASS・rc=0** ✓ |
| ハーネス `--check` | GREEN | **私が実行し GREEN**（スキーマ/抑制カウンタ 5/5/決定論再現/非回帰7項目/fixture） ✓ |
| `next_legal_operation` quant 逐語保持 | 逐語一致 | `--check` の `next_legal_operation_verbatim: True` を確認 ✓ |
| 非発火時の戻り値の形が不変 | 不変 | `allow_result_keeps_quant_shape: True` を確認 ✓ |
| コーパス ユニーク298件 | 一致 | 独立に再現（478 → 19除外 → 459 → dedup 298） ✓ |

**IMPL の報告作法は今回も模範的**: スライスを混ぜず、dedup を明記し、**自分の成果を自分で割り引き**、**私の数値に異議を出した**。

## 2. ★★私の誤り — SPEC §7 の「指示語の表層一致のみ = 2.0% (6/298)」は誤り
IMPL の異議を受けて自分の測定を検証した。**ヒット6件の内訳:**

| 件 | マッチした語 | 実際の中身 |
|---|---|---|
| 1 | `前の件` | `QwenとCoderの切替3分が完全にボトルネック。前の件を優先して進めて` ← **真** |
| 2 | `前の件`/`その` | `前の件のQwenとCoderの切替タスクをそのまま進めて` ← **真** |
| 3-6 | **`この`** | 長い技術指示文の**内部**に `この` が部分一致しただけ（`この処理`等）← **偽陽性** |

- **6件のうち4件は、裸の `この` を部分一致で拾った artifact。** 意味のある「指示先の無い指示語」は **2件**であり、**IMPL の 2/298 が正しい。**
- **これは同じ分析での2度目の測定誤り**（1度目: 重複で 25.1%→実は 3.7%）。**どちらも私が「率を出したがヒットの中身を見なかった」ことによる。**
- **自分への恒久規律**: **率を出したら必ずヒットを目視で列挙する。**（本 finding では実行した。）

## 3. ★IMPL の設計上の問い（表層集合を広げるか）への裁定 — **広げる**
IMPL の指摘の核心は正しい:
> 3語（あれ/それ/例のやつ）だけなら、**束縛先条件は実データで一度も判定されておらず、fixture 専用の機構**である。

**裁定【設計】: 表層集合を `前の件` / `さっきの件` 型まで広げる。** 根拠:
1. **【監査】我々自身の front door 実コーパスに実在する**（`前の件を優先して進めて` 他 計2件）。SPEC §12 の「証拠のあるパターンのみ」を満たす。
2. **束縛先条件が load-bearing になる**。この2件はまさに「文脈があれば解決でき、無ければ聞き返すべき」ケースであり、**AND 条件が実データで初めて意味を持つ**。
3. **【監査】input-clarity の DB が `前の件`/`さっきの件` を `REFERENT` 型として `clarification_slots` つきで既に持っている**。様式はそのまま使える（データ流用ではなく、**当該2パターンのみ証拠つきで起こす**）。

### ★ただし私の失敗から来る必須制約
- **裸の `この` / `その` / `あの` を部分一致で撃ってはならない。**（§2 の偽陽性4件がまさにそれ。）
- 許すのは **名詞に束縛された形**のみ: `この件` / `その件` / `あの件` / `前の件` / `さっきの件` / `先ほどの件`。
- **単独の `この`・`その`・`あの` はパターンに入れない。**

## 4. 私の SPEC の誤り2件（訂正）
1. **§7 の表**: 「指示語の表層一致のみ 2.0% (6/298)」→ **正しくは 2/298 = 0.67%**（うち意味のある指示語は2件）。SPEC を訂正する。
2. **§11-7**: 「`rri` を触るので rri 単体で commit + push」→ **誤り。実際は `rri` / `twoder` / `egl` の3 repo に跨る**（IMPL の指摘どおり）。**3 repo それぞれ commit + push が要る。**

## 5. IMPL が置いた定数への裁定
| # | IMPL の決定 | 裁定 |
|---|---|---|
| 1 | `AUTO_SUPPRESS_IGNORED_THRESHOLD = 3`・**一度でも採用された警告は抑制しない** | **承認**。「一度でも accepted なら抑制しない」は保守的で正しい |
| 2 | `BIND-RULE-001` の名詞句判定（`DE-\d+`/漢字2字/カタカナ3字/英数3字・ストップワード7語） | **承認**（thin の指示に沿う・DB に文字列で記録済＝反証可能）。**ただし §3 で表層集合を広げると load-bearing になる**ので、拡張時に**この規則自体を測る**こと |
| 3 | ゲート triggered → `return TRACE` で**意図調べに到達しない**（既存挙動の継承） | **承認**（本番として正しい）。**ただし測定上の副作用を明記**: 発火した入力について「意図調べなら何と答えたか」が失われる。**評価 run では両方を走らせられる経路を残すこと**（本番の分岐は変えない） |

## 6. 1a の正味の到達点（過大にも過小にもしない）
- **【監査】できたこと**: 決定論ゲート表化・既存 LIVE 非回帰・IP1/IP2/IP3 を手前で 3/3 捕捉・BV3 非発火・誤発火 0/298・`--check` GREEN。
- **【監査】できていないこと**: **束縛先条件（AND）は実データで一度も検証されていない**（3語が実コーパスに出現しないため）。**fixture 上でのみ検証済**。
- **【監査】未配線**: `twoder/submit.py` は `PG.detect()` に `context` を渡していない。**実 front door では常に「束縛先なし」判定**になる。**「動いた」とは言えない。**
- ＝ **1a は「仕様どおり動く部品ができた」段階であり、「実運用で効く」証拠はまだ無い。**

## 7. 次（1a' として小さく追加してから 1b へ）
1. **表層集合を §3 の制約つきで拡張**（`前の件`/`さっきの件`/`この件` 型・**裸の この/その/あの は禁止**）→ **束縛先条件を実データで初めて測る**（当該2件が context 有無でどう変わるか）。
2. その後 **1b（P3 文脈）** → **1c（P4 存在 + SUPERSEDE）**。
3. `ds_events.jsonl` の `preceding_utterance_ref`【監査・IMPL 発見】は Build 2 の context 配線の材料。**今は使わない**（提案として記録）。

---
*DESIGN CC-α 独立再監査。1a は仕様どおりで非回帰 GREEN。IMPL の自己割引と異議は両方正しく、私の 6/298 が誤りだった(4件は裸の「この」の部分一致)。表層集合は拡張する、ただし裸の指示語は禁止。*
