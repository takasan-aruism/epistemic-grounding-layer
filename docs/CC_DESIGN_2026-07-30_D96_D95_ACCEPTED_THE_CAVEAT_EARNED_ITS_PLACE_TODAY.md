# 設計/監査 → MGR（写: Taka / IMPL）: **D-96 — D-95 受入（15件すべて）。★併記が今日いきなり効いた（2件の先頭30字が同一）／★私は偽の欠陥を1つ報告しかけた**

- `BUILD_ROLE: 参照`（**監査のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_IMPL_2026-07-30_D95v11_OPEN_THE_STOPPED_ONES_BUILT.md`

## 0. ★2DER 優先原則の5点
| ① 入口 | **`GET /command`（配信物）／`GET /api/state`** |
|---|---|
| ② 何を試したか | **止まった2件の先頭30字が同じかどうか／配信物の JS が壊れていないか／集計が動いていないか** |
| ③ **できなかったこと** | **★画面のクリックが実際に働くところは見ていない**（★`openTask` の定義は読んだ。★押していない） |
| ④ 何を実装するか | **★実装しない**（受入確認のみ） |
| ⑤ | **★該当しない** |

## 0-1. ★結論
> **① 受入15件すべて満たされている。★私が自分で測った。**
> **② ★併記「先頭が同じでも別の依頼です」が、★今日いきなり効いた**——**★出した2件の先頭30字が★完全に同一だった。**
> **③ ★私は偽の欠陥を1つ報告しかけた。★自分の正規表現が `async` を切り落としていた。**

---

## 1. ★受入（★15件・★1つずつ）
| # | 条件 | 判定 | 私の確かめ方 |
|---|---|---|---|
| **1** | 止まった2件が開いて読める | **★済** | 配信物に2件の `<a onclick="openTask(…)">` が在る／`openTask` の定義が在る（★押していない・§3） |
| **2** | 終わった依頼を開く経路が在る | **★済／実例0件** | `completed_outside_ids` が在り、`b.concat(c)` で同じ道を通る／**★`completed_outside`＝0**（実測） |
| **3** | `total` が153のまま | **★済** | **★実物の `deferred_summary` を呼んで 153**（★私の変更前の基準と一致） |
| **4** | 和＝合計が変わっていない | **★済** | **117+18+18+0 = 153** |
| **5** | `blocked_outside` が和に入っていない | **★済** | 和が153のまま（`blocked_outside`=2 は外） |
| **6** | 「対象外ですが、開いて読めます」 | **★済** | 「開いて読めます」1件／**旧文言「この一覧の対象外です」★0件** |
| **7** | 内訳が数えた結果と一致（固定文でない） | **★済** | `kinds.push` → `kinds.join("／")`。**★固定文字列が無い** |
| **8** | 0件のものは語ごと出ない | **★済** | `if(c.length)` で囲われている／配信物に**「終わった 0件」★0件** |
| **9** | 先頭30字＋併記が出る | **★済** | 併記1件・`head30` が2件とも30字 |
| **10** | ID がこの2件だけ | **★済** | **★差分で数えた**（追加分の ID 出力は `outsideBlock` の1箇所のみ・153件の一覧は戻っていない） |
| **11** | 色を足していない | **★済** | **★差分で数えて0件**（`pill ok`/`pill bad`/`class='ok'`/`class='bad'`） |
| **12** | `/api/state` の返りを変えていない | **★済** | 差分は `deferred_summary`（`/api/submit` 側）と `PAGE` のみ |
| **13** | 配信物の JS が `node --check` | **★済** | **★私が実行**（配信 HTML から `<script>` を抽出＝18862バイト → 構文 OK） |
| **14** | 非回帰 91 passed / 7 failed | **★済** | **★私が自分の3行で実行**。91/7・**失敗7本の顔ぶれも一致** |
| **15** | 開いたときの所要 | **★済** | **0.047 秒**（★数字だけ。「速い」と書かない） |

## 2. ★併記が今日いきなり効いた（★これが本 build の一番の収穫）
```
再現（★実物の deferred_summary の返り）:
  TASK-2DER-D6346F79  head30 = '既存RRI分類結果からBUILD taskを生成する前に、e'
  TASK-2DER-7089D8FC  head30 = '既存RRI分類結果からBUILD taskを生成する前に、e'
  ★完全に同一である。

再現（★全文で確かめた）:
  TASK-2DER-D6346F79  全文 1092字  sha256=ad7c5a992824
  TASK-2DER-7089D8FC  全文 4089字  sha256=b2dda72ea381
  ★別の依頼である（★長さも中身も違う）。
```
| | |
|---|---|
| **★何が起きたか** | **★画面に初めて出す2件が、★先頭30字では見分けられない** |
| **★併記の位置づけが変わった** | **★D-72 では「42件が同じ先頭を持つ」という★統計だった。★今日は「出した2件が同じ」という★実害である** |
| **★もし併記が無かったら** | **★Taka は「同じ依頼が2回 並んでいる」と読む。★2件しか無いので、★なおさらそう読む** |

> **★私は D-72 で「先頭は区別には使えない。併記を必ず書く」と決めた。** **★あの時は統計だった。**
> **★今日、初めて出した2件で当たった。** **★2件しか出さない画面で当たったのは、★偶然ではないかもしれない**（★確かめていない）。

## 3. ★私は偽の欠陥を1つ報告しかけた（★自己申告）
```
私が見たもの: function openTask(tid){…;await load();}
              → ★「非 async の中に await が在る。★構文エラーだ」と判断しかけた
実際          : ★async function openTask(tid){…}
原因          : ★私の正規表現 `function openTask[^\n]*` が、★行頭の "async " を切り落としていた
```
| | |
|---|---|
| **★どうやって止めたか** | **★その1行だけを取り出して `node --check` にかけた** → **単独では確かに構文エラー**／**★しかし配信物 全体では OK** |
| **★∴ 食い違いが出た** | **★「単独で NG／全体で OK」は、★私の抽出が違うという意味である** |
| **★確かめた** | **`grep -n "function openTask"` → `async function openTask`。★`async` が在った** |

> **★これは本日4回目の私の計器の誤りである**（別経路 4→9件／入口 grep→構文木／「完了0件」／今回）。
> **★今回は「報告する前」に止まった。** **★D-58 のときは、★報告して commit されて5時間 画面が死んだ。**
> **★止まった理由は1つ: ★2つの計器が食い違ったから**（単独 NG ／ 全体 OK）。**★片方だけ見ていたら、偽の欠陥を出していた。**

## 4. ★受入の判定
**【設計:CC-α】★D-95 v1.1 を受け入れる。** **★MGR の commit を推す。**
| ★申告 | |
|---|---|
| **★私が押していない** | **★リンクをクリックしたところは見ていない。** **`openTask` の定義は読んだ。★`await load()` が実際に画面を切り替えるかは★未確認** |
| **★これは Taka にしか出せない** | **★D-95 受入1 は「開いて読める」であり、★私が示せたのは「開く手段が在り、構文が通る」までである** |

## 5. ★残件の現況（★本 build 後）
| # | | status |
|---|---|---|
| ⑤ | 一件を最後まで読める | **★部分のまま**（★(a) 辿れる＝**本 build で満たした**／★(b) 通過した処理＝**待つ**） |
| 他6件 | | **★済** |
```
★済 6 / ★部分 1 / 未 0
```
> **★「Phase 2 完了」と書かない。** **★⑤(b) は Taka が普通に使ったときに満たされるもので、★我々が満たせるものではない。**
> **★「あと1つで完了」とも書かない**（★その1つが我々の手の中に無い）。

## 6. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **リンクを押したら画面が切り替わるか** | **★押していない。★Taka にしか出せない** |
| 2 | **2件の先頭30字が同じなのが偶然か** | **★確かめていない**（★同じ系列の依頼かもしれない） |
| 3 | **`_head30` が `CREATE` phase から取っていること** | **★読んだ。★`CREATE` が無い task で空になる場合を試していない** |
| 4 | **完了が出た日に本当に同じ道で出るか** | **★実例0件 ∴ 確かめられない** |

---
*CC-α D-96（監査のみ）。★結論=①**受入15件すべて満たされており CC-α が自分で測った** ②**併記「先頭が同じでも別の依頼です」が今日いきなり効いた——出した2件の先頭30字が完全に同一だった** ③**CC-α は偽の欠陥を1つ報告しかけ、自分の正規表現が `async` を切り落としていた**。★受入15件=止まった2件の `<a onclick="openTask(…)">` と `openTask` の定義が在る（**押していない**）／終わった依頼は `completed_outside_ids` と `b.concat(c)` で**同じ道を通り実例0件**／**実物の `deferred_summary` を呼んで `total`=153 が変更前の基準と一致**／**和 117+18+18+0=153**／`blocked_outside`=2 は和の外／「開いて読めます」1件・**旧文言0件**／**`kinds.join("／")` で固定文字列が無い**／`if(c.length)` で囲われ**「終わった 0件」は0件**／併記1件・`head30` が2件とも30字／**ID の出力は `outsideBlock` の1箇所のみ（差分で数えた）**／**色は差分で0件**／`/api/state` 不変／**`node --check` は CC-α が配信 HTML から `<script>` を抽出（18862バイト）して実行し構文 OK**／**非回帰は CC-α が自分の3行で 91/7・失敗7本の顔ぶれも一致**／所要 0.047秒（数字だけ）。★**併記が今日いきなり効いた（本 build 一番の収穫）**——`TASK-2DER-D6346F79` と `TASK-2DER-7089D8FC` の `head30` が**完全に同一**（`'既存RRI分類結果からBUILD taskを生成する前に、e'`）だが**全文は 1092字 と 4089字 で sha256 も違う別の依頼**である ∴ **画面に初めて出す2件が先頭30字では見分けられない**——**D-72 では「42件が同じ先頭を持つ」という統計だったが、今日は「出した2件が同じ」という実害であり、併記が無ければ Taka は「同じ依頼が2回 並んでいる」と読む（2件しか無いのでなおさら）**。★**CC-α が偽の欠陥を報告しかけた（自己申告）**=`function openTask(tid){…await load();}` を見て「非 async に `await` が在る＝構文エラー」と判断しかけたが、**実際は `async function openTask` で、CC-α の正規表現 `function openTask[^\n]*` が行頭の "async " を切り落としていた**——**止めた理由は2つの計器が食い違ったこと（その1行だけを `node --check` にかけると NG／配信物 全体では OK）であり、片方だけ見ていたら偽の欠陥を出していた**。**本日4回目の計器の誤りだが、今回は「報告する前」に止まった（D-58 のときは報告して commit され5時間 画面が死んだ）**。★**D-95 v1.1 を受け入れ commit を推す**が、**CC-α はリンクを押しておらず、示せたのは「開く手段が在り構文が通る」までで、「開いて読める」は Taka にしか出せない**。★**残件は 済6／部分1／未0**（⑤は (a) を本 build で満たし (b) は待つ）——**「Phase 2 完了」と書かず「あと1つで完了」とも書かない（その1つが我々の手の中に無い）**。★確かめていないこと=リンクを押したら画面が切り替わるか（押していない・Taka にしか出せない）／**2件の先頭30字が同じなのが偶然か（同じ系列の依頼かもしれない）**／`_head30` が `CREATE` phase から取るので `CREATE` が無い task で空になる場合を試していない／**完了が出た日に本当に同じ道で出るかは実例0件で確かめられない**。*
