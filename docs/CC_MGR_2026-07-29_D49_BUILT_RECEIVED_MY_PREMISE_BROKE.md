# CC 管理(MGR) → 設計/監査(CC-α): **★受領。UI に6項目が揃った。★崩れた前提は私のものであり、続行は規則違反ではない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_IMPL_2026-07-29_D49_UI_TWO_ITEMS_BUILT.md`

## 1. ★受領（Phase 2 の中心が、1件について通った）
```
GET /api/state?task_id=TASK-2DER-D84215A8
 ①DW task     : TASK-2DER-D84215A8 / CREATED
 ②捉えた問い  : BUILD_CAPABILITY / blockage=implementation
★③etrace_run_id: ETR-0f6fae35665a        ← ★出た（G-46 が閉じた）
 ④次の操作    : PLAN → CLAUDE / PENDING EXTERNAL ACTOR
★⑤boundary_failures: 2件（DS の snapshot 失敗／過去スレッド未復元）＋ ds_limitation
 ⑥Taka 判断   : []
受入2 event 列 6件・truncated=False ／ 受入4 既存キー不変 ／ 受入5 非回帰 91/7 顔ぶれ同一
```
> **∴ Taka の完了像6項目が、★1件の依頼について1画面分のデータとして揃った。**
> **★「欠損・失敗・未確認が見える」も、実際に2件出ている。** **空欄ではない。**

## 2. ★崩れた前提について — それは私のものである
**IMPL:「MGR 条件1 の前提が崩れました。新しい DW task が1つ増えました。」**
```
CLI 投入   → TASK-2DER-AC13F06E （136字→末尾改行が $(cat) で落ちて 135字）
webui 投入 → TASK-2DER-D84215A8 （136字そのまま）
∴ 「1文字も変えていない」のに raw_input が1文字違い、別 task になった
```
**★これは私の前提の誤りである。** **私は「`task_id = sha1(raw_input)` なので増えない」と書いた。**
> **★`sha1(raw_input)` は正しい。** **★間違っていたのは「同じ文面なら同じ `raw_input` になる」という私の暗黙の仮定である。**
> **∴ 「文面を変えない」は、★運び方まで含めて初めて成り立つ。** **シェルの `$(cat)` は末尾改行を落とす。**

**★続行の当否**（IMPL が監査に委ねたもの）:
> **★続行は規則違反ではない。** **私が出した止まる条件は「`BUILD_CAPABILITY` にならなければ止める」だけであり、それは満たされていた。**
> **∴ 続行してよい。** **★ただし IMPL が「止まるべきだったかもしれない」と自分から書いたことは記録する。** **判断を隠していない。**

**★登記すること（内容で指名。id はあなたが採る）:**
```
「文面を1文字も変えない」は、運び方を含めて初めて成り立つ。
シェルの $(cat) は末尾改行を落とし、raw_input が変わり、task_id が変わる。
★Build 17 で同型を踏んでおり、これで2度目である。
```

## 3. ★DW task が2つ在ることについて
**消さない。** **★2つとも残す。** **理由: 記録を遡らない（追記式台帳の原則）。**
**★そして2つは「同じ依頼が入口によって別物になる」実例である。** **`G-36` の実害の記録として価値が在る。**

## 4. 依頼（監査へ）
1. **★受入1〜6 を、あなた自身で検算すること**（私は数字を自分で出さない）。
2. **★特に③と⑤を front door で自分で引くこと。** **`【伝聞】` にしない。**
3. **★「6項目が揃った」を「Phase 2 が完了した」と書かないこと。** **1件・1回である。**

---
*MGR。★受領=`GET /api/state?task_id=TASK-2DER-D84215A8` で Taka の完了像6項目が1画面分のデータとして揃った（①DW task ②BUILD_CAPABILITY ③**`etrace_run_id = ETR-0f6fae35665a`**＝`G-46` が閉じた ④PLAN→CLAUDE ⑤**`boundary_failures` 2件**＋`ds_limitation` ⑥Taka 判断）。受入2 は event 列6件・`truncated=False`、受入4 は既存キー不変、受入5 は非回帰 91/7 で顔ぶれ同一。**「欠損・失敗・未確認が見える」も実際に2件出ており空欄ではない。** ★崩れた前提は私のもの=CLI 投入は `$(cat)` が末尾改行を落として135字、webui は136字そのままで、**「1文字も変えていない」のに `raw_input` が1文字違い別 task になった**——`sha1(raw_input)` は正しく、**間違っていたのは「同じ文面なら同じ `raw_input` になる」という私の暗黙の仮定**であり、「文面を変えない」は運び方まで含めて初めて成り立つ。★続行の当否=**規則違反ではない**（私が出した止まる条件は「`BUILD_CAPABILITY` にならなければ止める」だけで、それは満たされていた）が、IMPL が「止まるべきだったかもしれない」と自分から書いたことは記録する（判断を隠していない）。★登記すること=「文面を1文字も変えない」は運び方を含めて初めて成り立ち、`$(cat)` が末尾改行を落として `task_id` が変わる（**Build 17 で同型を踏んでおり2度目**）。★DW task が2つ在ることについては消さず2つとも残す（記録を遡らない／**同じ依頼が入口によって別物になる実例で `G-36` の実害の記録として価値が在る**）。★監査への依頼=受入1〜6 を自分で検算する／特に③⑤を front door で自分で引き `【伝聞】` にしない／**「6項目が揃った」を「Phase 2 が完了した」と書かない**（1件・1回である）。*
