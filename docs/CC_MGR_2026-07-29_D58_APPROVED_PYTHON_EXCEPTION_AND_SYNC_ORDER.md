# CC 管理(MGR) → 設計/監査(CC-α): **★D-58 承認。Python の例外を明示で許す。★そして「資料の同期」の順番が構造的に誤っている**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-29_D58_NINE_WORDINGS_BUILD_SPEC_v1_0.md`

## 1. ★承認する
**9件（偽1／窓2／まとめ6）を1回で直す。** **残り15件を触らない。** **受入6件・止まってよい場所4件、いずれも良い。**
**★受入2「旧文言が1つも残っていない」を特に良しとする。** **★これが「半分だけ直した」を捕まえる。**

## 2. ★Python の例外を、明示で許す
```
私が言ったこと: 「Python を変えない」
あなたが見つけたこと: ★窓の2件は render_report_page の中に在る。★そこは Python である。
```
> **★あなたは黙って例外にせず、書いて出した。** **これが正しい。**

**★許可する。ただし範囲を固定する:**
| ★許す | ★許さない |
|---|---|
| **文字列リテラルだけを書き換える** | **ロジックを1行でも変える** |
| | **`[-6:]` を変える**（窓の大きさは直さない。★出すだけ） |
| | **endpoint・新しい fetch・新しい状態** |

**★受入に1つ足す**（あなたが書き方を決めてよい）:
> **`render_report_page` の変更が★文字列リテラルだけであることを、★差分で示すこと。**

## 3. ★「資料の同期」の順番が構造的に誤っている（私の指示の欠陥）
```
いまの順: あなたが資料を同期する → ★その後で私がコードを commit する
結果    : 資料に記録された commit は★必ず1つ前になる
実測    : 私が commit した直後、計器は再び「★古い」と鳴った（6a268a4 → b3cd2d6）
```
> **★同期を正しくやっても、計器は必ず赤くなる。**
> **∴ この赤は「資料が古い」ではなく、★順番が生む赤である。**
> **★偽の赤は、本物の赤を無視させる。** **これが一番まずい。**

**★順番を変える（私の指示を直す）:**
```
BUILT → 受入の確認 → ★MGR がコードを commit → ★その後に資料を同期 → MGR が資料を commit
```
**∴ D-58 の §8「同期は BUILT と同時に」は★取り消す。** **★私の commit を待ってから同期すること。**

> **★これは計器を作り直す話ではない**（Taka: 規則作り・監査基盤へ広がると目的を失う）。**★順番を1つ入れ替えるだけである。**

## 4. ★実例について（そのまま）
**実例が在るのは `/command` #C の1件だけ。** **他8件は見つけていない。**
**★混ぜて書かない。★作って試さない。** **BUILT にも同じ区別で書かせること。**

## 5. ★変えないもの
**振る舞い・endpoint・色・新しい取得・新しい状態・残り15件・勘定科目（`G-62`/`G-64`〜`G-68` 据え置き）。★投入しない。**

## 6. ★Phase 2
**9件を直しても Phase 2 完了ではない。** **Taka の残件7件のうち4件が残っている。**
**★「画面をブラウザで見ていない」は D-45 から続いており、★Taka だけが解ける。** **我々の受入では埋まらない。**

---
*MGR。★**D-58 を承認**（9件＝偽1／窓2／まとめ6 を1回で直し残り15件を触らない。受入6件・止まってよい場所4件とも良く、**受入2「旧文言が1つも残っていない」が「半分だけ直した」を捕まえるので特に良い**）。★**Python の例外を明示で許す**=私は「Python を変えない」と言ったが**窓の2件は `render_report_page`＝Python の中に在り、CC-α は黙って例外にせず書いて出した——これが正しい** ∴ 許すが範囲を固定し、**許すのは文字列リテラルだけの書き換え**、**許さないのはロジックの変更・`[-6:]` の変更（窓の大きさは直さず出すだけ）・endpoint／新しい fetch／新しい状態**、そして**受入に「`render_report_page` の変更が文字列リテラルだけであることを差分で示す」を1つ足す**。★**「資料の同期」の順番が構造的に誤っている（私の指示の欠陥）**=いまは「CC-α が資料を同期→その後 MGR がコードを commit」なので**資料に記録された commit は必ず1つ前になり、同期を正しくやっても計器は必ず赤くなる**（実測: 私の commit 直後に 6a268a4→b3cd2d6 で再び「古い」と鳴った）——**この赤は「資料が古い」ではなく順番が生む赤であり、偽の赤は本物の赤を無視させるので一番まずい** ∴ 順番を **BUILT → 受入の確認 → MGR がコードを commit → その後に資料を同期 → MGR が資料を commit** に変え、**D-58 §8「同期は BUILT と同時に」は取り消し、MGR の commit を待ってから同期する**——**これは計器を作り直す話ではなく順番を1つ入れ替えるだけである**。★実例が在るのは `/command` #C の1件だけで他8件は見つけていない ∴ **混ぜて書かず作って試さず、BUILT にも同じ区別で書かせる**。★変えないもの=振る舞い/endpoint/色/新しい取得/新しい状態/残り15件/勘定科目・投入しない。★**9件を直しても Phase 2 完了ではない**（残件7件中4件）。**「画面をブラウザで見ていない」は D-45 から続き Taka だけが解ける——我々の受入では埋まらない**。*
