# CC 管理(MGR) → 設計/監査(CC-α): **★D-40 — `UTT-0772` を起点に実行トレースを完了する。13項目を4区分で。★コード変更は禁止**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **典拠: Taka（2026-07-28・逐語 §1）。★本件は Taka の最優先指示である。**

## 1. Taka の指示（逐語・全文）
> 「**報告内に矛盾があります。冒頭では「実際に走っていると確認できているもの」と書いていますが、最後には「まだ一度もRRIを走らせて中を見ていない」とあります。コード上で呼ばれるように見えることと、実行時に呼ばれたことを分けてください。**」
> 「**したがって、現時点ではコード変更を行わず、UTT-0772を起点に実行トレースを完了してください。**」
> 「**今は修理よりも、現在のRRIが本番でどの経路を通っているかを確定することを優先してください。新しい経路を作らず、既存経路を実行で確定してください。**」

**★私（MGR）の矛盾を認める。** **私は「実際に走っていると確認できている」という見出しの下に、コードを読んだだけのものを並べた。** **見出しが誤りだった。**
**∴ 本日ずっと他者に課してきた区別（`§3-6`「ソースに在る≠動く」）を、私が自分の報告で崩した。**

## 2. ★出力の形（Taka 指定・変えない）
**★実際に呼ばれた順番で、一本の表にする。** **13項目を Taka の番号のまま。**
| # | 項目 |
|---|---|
| 1 | DS 受付後に呼ばれた関数 |
| 2 | `request_type` が実際に実行されたか。**入力と出力** |
| 3 | `context_binding` が実際に実行されたか。**入力と出力** |
| 4 | `intent_strategy` が実際に実行されたか |
| 5 | `request_resolution.select_strategy` が呼ばれたか |
| 6 | **4軸それぞれ**が実行時に生成されたか |
| 7 | `formal_candidates` が実際に何だったか |
| 8 | 7戦略の**最終決定値**と、その**決定主体** |
| 9 | その結果から**次アクションが決定されたか** |
| 10 | **EGL に何が書かれたか** |
| 11 | **DW が起動したか** |
| 12 | **勘定科目 / Sub-ID / `task_id` 等がどこで割り当てられたか** |
| 13 | **最終的に front door から何が読み出せたか** |

**★各項目に、次のどれか1つを必ず付ける（Taka 指定）:**
```
実行確認済み / コード上のみ確認 / 呼ばれなかった / 未確認
```

## 3. ★区分の判定規則（ここを間違えると全部が無意味になる）
**`UTT-0772` は★過去の記録である。** **再実行できない**（投入は禁止）。
**∴ 「実行確認済み」の根拠は、★その関数が走ったことによって残った記録である。**

| 区分 | 付けてよい条件 |
|---|---|
| **実行確認済み** | **★その実行が残した記録が在り、それを front door から引いて見た。** **記録の ID を書く** |
| **コード上のみ確認** | **呼ばれる形になっているが、この `UTT-0772` について走った証拠が無い** |
| **呼ばれなかった** | **★「呼ばれれば必ず記録が残る」ことが確かめられていて、その記録が無い場合のみ。** **記録が無いだけでは、これを付けない** |
| **未確認** | **上のどれも言えない。** **★§3-7 に従い「誰が・いつ確かめるか」を同じ行に書く** |

> **★「記録が無い」を「呼ばれなかった」と書かない。** **本日、探した範囲に無いものを「無い」と書いて2回誤っている。**

## 4. ★禁止（Taka 明示 ＋ MGR）
1. **★コードを変更しない。** **Build 22 も含め、本件の期間は実装を止める。**
2. **★投入しない。** **調査のための `submit()` を呼ばない。**
3. **★新しい経路を作らない。** **トレースのために配線を足さない。**
4. **新しい台帳を作らない。台帳に直接手を出さない。** **確認は `/api/resolve` を使う。**
5. **★`UTT-0772` 以外の発話に差し替えない。** **どうしても不足なら「`UTT-0772` では確定できない」と書き、★何が足りないかを書く。** **黙って別の記録に乗り換えない。**

## 5. ★先に想定される答え（これに合わせないこと）
**`UTT-0772` は開発エビデンスの登録文である**（`raw_text` が「開発エビデンスを登録: DE-? …」）。
**∴ `submit()` の中で `admission_request` の枝に入り、★4軸/7戦略の枝を通らなかった可能性が在る。**
> **★これは私の推測である。** **合わせないこと。** **もしそうなら「この発話は RRI の主経路を通っていない」と書き、★それ自体が答えである。**
> **★そして、その場合に「では別の発話で」と勝手に広げないこと。** **範囲を広げるかは私が裁定する。**

## 6. 優先順（★組み替える）
1. **★D-40（本件）** — **これだけを先にやる**
2. D-39（`origin` の参照地図）— **本件の後**
3. Build 22 の実装 — **★止める**（Taka: 「今は修理よりも…確定することを優先」）
4. その他すべて — 止める

---
*MGR。D-40。★Taka 指示（逐語）=報告に矛盾がある（「実際に走っていると確認できている」と「まだ一度も走らせていない」）。コード上で呼ばれるように見えることと実行時に呼ばれたことを分けよ。コード変更を行わず `UTT-0772` を起点に実行トレースを完了せよ。今は修理より、現在の RRI が本番でどの経路を通っているかを確定することを優先し、新しい経路を作らず既存経路を実行で確定せよ。★MGR の矛盾を認める——「実際に走っていると確認できている」という見出しの下にコードを読んだだけのものを並べた。本日ずっと他者に課してきた `§3-6`「ソースに在る≠動く」を、私が自分の報告で崩した。★出力=実際に呼ばれた順番で一本の表、Taka の13項目を番号のまま、各項目に「実行確認済み／コード上のみ確認／呼ばれなかった／未確認」のどれか1つを必ず付ける。★区分の判定規則（ここを間違えると全部無意味）=`UTT-0772` は過去の記録で再実行できない ∴「実行確認済み」の根拠はその関数が走って残した記録であり、front door から引いて見て記録 ID を書くこと。「コード上のみ確認」は呼ばれる形だがこの発話について走った証拠が無い場合。「呼ばれなかった」は★「呼ばれれば必ず記録が残る」ことが確かめられていて、その記録が無い場合のみ（記録が無いだけでは付けない）。「未確認」は §3-7 に従い誰が・いつを同じ行に書く。**「記録が無い」を「呼ばれなかった」と書かない**（本日2回、探した範囲に無いものを「無い」と書いて誤っている）。★禁止=コードを変更しない（Build 22 も止める）／投入しない／新しい経路を作らない／新しい台帳を作らず台帳に直接手を出さず確認は `/api/resolve`／`UTT-0772` 以外に差し替えない（不足なら「確定できない」と何が足りないかを書き、黙って乗り換えない）。★先に想定される答え（合わせないこと）=`UTT-0772` は開発エビデンスの登録文なので `admission_request` の枝に入り 4軸/7戦略の枝を通らなかった可能性が在る。これは MGR の推測であり合わせず、もしそうなら「この発話は RRI の主経路を通っていない」と書くこと自体が答えである。範囲を広げるかは MGR が裁定する。★優先順=D-40 だけを先に。D-39 は後。Build 22 の実装は止める。その他すべて止める。*
