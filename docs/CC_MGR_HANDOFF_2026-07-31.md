# 【MGR 引き継ぎ】次の MGR は★これを最初に読む（2026-07-31 20:3x）

- `BUILD_ROLE: 参照` / **宛: 次の MGR** / 写: Taka / DESIGN / IMPL / TYPE=HANDOFF
- **★昨日の引き継ぎ（`CC_MGR_HANDOFF_2026-07-30.md`）は★状態から書き始めていた。★今日は A から書く。**

---

## 0. ★★最初に見るのは、状況表の「★A:」の行

```
★A: 2DERだけで完了した工程 = 2段 ／ Claude が要る工程 = 7段（最終更新 …前 / …）
```
```
★毎ターン 機械が出す（`~/.claude/hooks/2der_a_metric.sh`）。★2時間 動かないと★名指しで警告する。
★更新は ★工程が実際に進んだ時だけ:  2der_a_metric.sh set <A> <副> "<何が動いたか>"
★★文書を書いても・測定を回しても ★この数字は動かない。★そういう作りにしてある。
★★★移管先: `/api/control` の roadmap 面（★移せたらこのスクリプトは消す）【未確認・設計が確認する】
```

## 1. ★★Taka が成功指標を入れ替えた（★これが最上位）
**正典**: `TAKA_2026-07-31_SUCCESS_METRIC_IS_CLAUDE_DEPENDENCE.md`（逐語）
```
★評価は ★文書数でも prompt の長さでも測定回数でもない。
★★「★Claude の仕事が減り、★2DER の仕事が増えているか」★これだけである。
★毎回 報告する5項目:
   1 2DER だけで完了した工程 ／ 2 Claude が介入しないと止まる工程
   3 Claude の仕事は増減 ／ 4 一時的補助か永久か ／ 5 2DER 主体率
★★★「★移管予定が説明できない作業は、極力増やさないでください」
★最終目標（逐語）: 「1件の依頼について 2DER が Observation / PLAN / Generate / Audit / 修正提案 /
   Ledger 更新 まで自律して実行し、最後に私へ『承認しますか』だけを聞いてくる状態」
```

## 2. ★★今日いちばん大きい発見（★未裁定。★ここから始める）

> ### **★押しても進まない。★PLAN の成功が、次の段の gate を閉じている。**

```
★実測: `run_next` を6回 → ★6回とも refused ／ ★0段 進んだ（`D-190`）
★原因（決定論・逐語）:
   `dispatch.py:79` の `nlo` は★実行前の値 → `:116-118` の成功経路でも ★PLAN/MANAGER のまま
   → `webui.py:717` が ★その actor で次の可否を決める → `runnable=False`
★★gate を開け直す口は `/api/submit` だけ ＝ ★新しい task が立つ
   ∴ ★★同じ task を GENERATE 以降へ進める経路が★存在しない
★★★「★バグである」と書いていない。★意図した安全装置の可能性が残る。
★★★★★次の1手: ★設計が「この gate の意図が記録に在るか」を Gap 台帳・過去文書・git 履歴で調べる。
   ★★記録に在れば Taka の手番を使わずに済む。★無ければ「無い」が結果で、そこで初めて上げる。
```

## 3. ★崩れた前提（★昨日まで信じていたもの）
| 前提 | ★実際 |
|---|---|
| 「GENERATE を押していないから進んでいない」 | **★押しても進まない**（§2） |
| 「Generate は Taka が禁止している」 | **★誤り。**★Taka は機能を否定していない。止めていたのは「★原因が未確定な段階で Generate まで動かして★評価を混ぜること」 |
| 「受入3 で測れている」 | **★その規則は prompt に1件も書かれていなかった。**★書いていない規則で40本 採点していた |
| 「規則1文で打ち切りが4倍」 | **★再現しなかった**（4/10 → 2/10 → 2/10）。★1バッチの揺れだった |
| 「我々の採点は移管予定が無い」 | **★移管先は在る**（`QwenAuditor`）。★通していないだけ |

## 4. ★★今日の運用の失敗（★規律は書いてあった。★使わなかった）
```
① 「file を置いただけでは届いていない」（運用方針 §2-4）→ ★5時間 沈黙に気づかなかった
② 「測定は並列で回す」（`PROCESS-01`）      → ★40本を3時間半 直列で回した
③ 「最低 10 run × ★2 batch」（§4-1）        → ★1バッチの揺れを効果と読んだ
★★★3つとも ★規律の不足ではない。★運用の失敗である。★同じ形を4度 作らない。
```

## 5. ★止めたもの・保留（★触らない）
```
★案C の測定（30本）      … ★MGR が取り下げ（★Claude の測定が増えるだけ・移管予定なし）
★受入3 の採点           … ★保留（★書いていない規則で採点していたため）
★Ledger（生出力への到達）… ★保留のまま（★Taka の元々の1番）
★図の修正               … ★保留（★二経路の確定は Taka 裁定待ち）
★(c) の patch           … ★保存済（`/tmp/.../D160_values_only.patch`）
★`G-96`（物差しが自己申告依存）／`G-98`（案B は取得方法を提示しない 32%）… ★未解決のまま
```

## 6. ★4人目のインスタンス（★Taka 提案。★条件つき保留）
```
★Taka:「タイムキーパー役と現場指揮役を分けてインスタンスを増やすか」
★★MGR の答え: ★今日は増やさない。★時計は §0 の機械へ移した。
★★★増やすなら役は「時計係」ではなく「★降りるのを止める係」。★条件3つを先に固定した:
   ①文書を書かせない（出力は「降りるな/降りてよい」の1行と A の更新だけ）
   ②読ませるのは ★A の行 と ★MGR の直近1本だけ
   ③移管先は ★`UPPER_REVIEW`（`CLAUDE_SENIOR`）＝ ★一時的代役と位置づける
★★判断材料: ★2〜3日。「A が長時間 動いていないのに B を続けた」が実測で出たら増やす
```

## 7. ★MGR の作法（★今日 変えたもの）
```
★往復のたびに文書を書かない。★裁定が要るときだけ書く。★予想の範囲なら commit だけ
★相手に届けるものは★必ず新しい名前で置く。★既存文書を同名で差し替えない（★5時間 沈黙の原因）
★B へ降りるとき「★これは A のどの数字を動かすか」を1行 書けなければ★降りない
★Taka へ上げる前に「★これは調べれば分かることか」を確かめる（★今日2回 破った）
```

## 8. ★状態（★機械が出すものは書き写さない）
```
★5 repo: 未commit 0 / 未push 0 ／ ★資料 gap 98 ／ ★詳細は★状況表を見ること
★★今日: 文書103本・コード87行（2ファイル）・本番投入10回・LLM 実測130本（★全部 2DER の外）
```

---
*MGR 引き継ぎ（2026-07-31）。★**最初に状況表の「A:」の行を見る**（毎ターン機械が出し、2時間 動かないと警告する。更新は工程が進んだ時だけ）。★**Taka が成功指標を入れ替えた**——文書数でも測定回数でもなく「Claude の仕事が減り 2DER の仕事が増えているか」だけで、毎回5項目を報告し、移管予定が説明できない作業を増やさない。★**今日いちばん大きい発見＝押しても進まない**（`run_next` 6回で0段。PLAN の成功が gate を閉じ、同じ task を GENERATE へ進める経路が存在しない）——**バグとは書かず、次は「意図が記録に在るか」を設計が調べる**。★**崩れた前提5件**（押していないから進まない／Generate は禁止されている／受入3 で測れている／規則で打ち切りが4倍／採点に移管予定が無い）。★**運用の失敗3件は規律の不足ではない**（届いていない／並列で回す／10run×2batch——3つとも書いてあって使わなかった）。★**止めたもの**＝案C の測定・受入3 の採点・Ledger・図・patch・`G-96`/`G-98`。★**4人目は条件つき保留**（役は「降りるのを止める係」・文書を書かせない・移管先は `UPPER_REVIEW`）。★**MGR の作法を4つ変えた**（往復ごとに書かない／新しい名前で置く／A の数字を書けなければ降りない／上げる前に調べる）。*
