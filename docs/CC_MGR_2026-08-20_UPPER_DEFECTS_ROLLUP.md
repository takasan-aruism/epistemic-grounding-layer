# 宛: Taka ―― **★上位構造欠陥への 集約 ／ ★何件の 停止を 生んだかの 実数**

**2026-08-20 02:5x ／ ★新しい 実装 0 ／ ★SELF_DEV_TOKEN = ★5/5 ／ ★HEAD = `3dd7d02`（不変）**

---

## 0. 数え方（★先に 宣言する）

```
★母数 = ★今夜 goal から 走らせた task（★私が 投入した もの）
★『停止』= ★その task が ★終端で 止まり ★先へ 進めなかった 回
★1つの task が ★2つの 欠陥で 止まった 場合は ★両方に 数える（★重複を 隠さない）
★★推測で 埋めない ―― ★記録に 出た ものだけ
```

## 1. ★上位構造欠陥（★5件）と ★生んだ 停止の 実数

| # | 上位欠陥 | 停止 件数 | 該当 task |
|---|---|---|---|
| **A** | **★契約が 作られない**（`serves_segment` を 名乗ると 経路表名 以外は 必ず differs） | **★4** | `670E3F6C` `1EB0877C` `C3217123` `AF059FD8` |
| **B** | **★骨格の 作法が 監査の 指摘を 作る**（注記の 挿入 ／ 引数 `a,b,c` 改名） | **★4件の finding ／ ★3 task** | `99CB3F62` `E8AAEA8C` `3361D3E1`(×2) |
| **C** | **★成果が 元の 停止 task へ 還流しない**（作れる ／ 繋がらない） | **★4** | `CBAFD9EC` `6D501FC9` `070D062A` `3361D3E1` |
| **D** | **★DECIDE が 作業に ならない**（`EGL_RESEARCH` へ 回り DW task に ならない） | **★5**（★投げ直し 実測） | 命名裁定1 ／ 一時裁定1 ／ 依存判定2 ／ 初回1 |
| **E** | **★自力で 回り続けられない**（`JUDGE_REQUIRED` ＋ `no_progress_since_last_review`） | **★2** | `7D461717` `92FE6932` |

**★併記（★欠陥では なく ★状態）:**

```
★DISPOSE の 滞留 = ★2件（`E8AAEA8C` `3361D3E1`）★どちらも actor=CLAUDE / barrier=True
★『既存を 調べずに 作る』= ★6件中 ★5件（★6件目 `3361D3E1` で 初めて 段が 出た）
```

## 2. ★★「1つ 直せば 複数 消える」順（★根拠つき）

```
★★① B（骨格の 作法）―― ★直すと ★消える 停止 = ★最大 4件 ＋ ★DISPOSE 滞留 2件
   根拠 = ★4件の finding 中 ★3件が B 由来（★実装由来は 1件のみ）
        ★その findings が ★DISPOSITION_REQUIRED を 生み ★Claude 手番の 滞留に なっている
        ★B は ★毎回 必ず 骨格に 入る ∴ ★今後の 全 task に 効く（★止血効果が 最大）

★★② A（契約が 作られない）―― ★直すと ★消える 停止 = ★4件
   根拠 = ★4件 とも ★runner が 一度も 走っていない（★最も 早い 段で 落ちる）
        ★但し ★A は ★『検査の 意味』そのもの ∴ ★Taka の 一般仕様（★既に 出た）が 前提

★★③ C（還流しない）―― ★直すと ★消える 停止 = ★4件
   根拠 = ★成果は 出来ている ∴ ★繋ぐだけで ★過去の 成果も 生きる
        ★但し ★C を 直す task 自身が ★C で 止まった（★3361D3E1）＝ ★自己参照

★④ D（DECIDE が 作業に ならない）―― ★消える 停止 = ★5
   根拠 = ★私が 動詞を 変えて 迂回できている ∴ ★止血の 緊急度は 低い
        ★但し ★『Taka の 裁定を 機械へ 渡す 経路』が 塞がっている ＝ ★構造的には 重い

★⑤ E（自力で 回り続けられない）―― ★消える 停止 = ★2
   根拠 = ★件数が 最小 ／ ★A・B が 直れば ★そもそも 到達回数が 減る
```

## 3. ★★優先1件（★私の 集計の 結論・★裁定は Taka）

```
★★B ―― 「パイプラインが 機械的に 生成した 骨格・注記・変換結果」と
        「実装成果物 そのもの」を ★監査時に 区別できない。

★理由（★数字だけ）:
   ・★finding の 4件中 3件が B 由来（★75%）
   ・★DISPOSE 滞留 2件 の ★両方に 効いている
   ・★毎 task 必ず 発生する（★3 task 連続で 再現）
   ・★A が 直っても ★B は 残る（★A の 先で 必ず 当たる）
   ・★C を 直しても ★B 由来の finding は ★消えない
```

## 4. ★次に 2DER へ 戻す goal（★1件だけ）

```
★★B のみ。★A・C・D・E は ★今回 投げない。
★渡す = ★3 task の 誤検知の 逐語 ／ ★実装由来の 正当な finding も 在る 事実
        ／ ★『機械生成だから 無条件に 監査対象外に しない』
        ／ ★『誰が・どの段階で 生成したか』と『実装者の 判断として 扱ってよいか』を 区別する
        ／ ★『実装由来の 正当な finding を 消さない』
★★渡さない = ★どの 監査器 ／ ★どの 文字列 ／ ★どの 欄 ／ ★どの file・function
        ／ ★注記と 引数改名の ★出所（`precheck_names` / `contract_from_plan`）
```

## 5. ★数えられなかった もの（★隠さない）

```
★『Claude DESIGN 由来 9件』『古い CREATED 159件』は ★今夜の 自己開発とは 別 ∴ ★数に 入れない
★`834D7DD2` は ★停止 0（★唯一 実 repo まで 届いた）∴ ★どの 欠陥にも 数えない
★D の 5件は ★私の 投げ直し 回数 ＝ ★task が 生まれなかった 回 ∴ ★task 単位では 数えられない
★B の 「4件の finding ／ 3 task」は ★finding 単位と task 単位が 違う ∴ ★両方 併記した
```
