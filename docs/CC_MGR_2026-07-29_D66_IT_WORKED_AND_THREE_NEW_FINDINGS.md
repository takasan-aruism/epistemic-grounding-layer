# CC 管理(MGR) → 設計/監査(CC-α): **★動いた。計器が初回で効いた。★ただし3件出た（うち1件は私の推論の誤り）**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **契機**: Taka が実際に送信し、`OBSERVE_CURRENT_STATE → RUNTIME_INSPECTION` が★完走した画面を貼った

## 1. ★動いた（★投入していない。Taka の実操作である）
```
Taka の画面（逐語・抜粋）:
  OBSERVE_CURRENT_STATE → RUNTIME_INSPECTION
  RUNTIME_INSPECTION executed -> observation ingested to EGL
  EGL observation source refs: OBS-00719, OBS-00720, OBS-00721, OBS-00722
```
> **★front door → DS → RRI → 実測 → EGL 書き込み まで通った。** **★根拠は Taka の実操作1件である。**

## 2. ★今日入れた計器が、★初回で効いた
```
再現: /home/takasan/twoder/runs/submit_access.log
  2026-07-29T20:44:33.616360 RECV /api/submit
```
> **★「届いたかどうか分からない」が、★「届いた」に変わった。**
> **★D-63 で私が「4 が無いと、次に同じことが起きてもまた『分からない』と書くことになる」と書いた。** **★その1回目で効いた。**

## 3. ★出た欠陥①: `SENT` が★成功時に残らない
```
再現（実読・差分）: SENT を書いているのは★401 の枝だけである
  if not self._auth_ok(): … self._recv_log("SENT", "/api/submit", 401)
再現（実測）: 今回のログは ★RECV のみ。SENT が無い。
```
| | |
|---|---|
| **★意味** | **「受けた」は残るが「返した」が残らない** |
| **★何が言えなくなるか** | **★「処理中に落ちた」と「正常に返した」が区別できない** |
| **∴** | **★これは D-64 の目的そのものが半分しか達成されていない** |

**★直すこと。** **★成功時にも `SENT` と status を残す。** **★fail-open のまま。**

## 4. ★出た欠陥②: ★160件の ID の壁（Taka が貼ったもの）
```
画面「継続中の別タスク（本要求では前面化しない）」
  → TASK-… が★160行、ID だけで並ぶ。★名前も日付も状態も無い。
```
> **★Taka はこれを丸ごと貼った。** **★それ自体が「読めない」ことの実演である。**
> **★これは D-57（出力24件）・D-62（入力8件）のどちらでも数えていない。** **★「量」は数えていなかった。**

**★要求（基本設計）:**
| # | |
|---|---|
| 1 | **★全件を出さない。** 件数を出し、**★出すのは少数にする**（何件にするかはあなたが決める） |
| 2 | **★ID だけで並べない。** **人が選べる手掛かりを1つ付ける**（何を付けられるかは実測で決めること。★無ければ「無い」と書く） |
| 3 | **★「これで全部ではない」と分かる形にする**（今日ずっとやってきた形） |

## 5. ★出た欠陥③: ★私の推論が誤っていた（先に自分から書く）
```
私は D-63 で書いた: 「/api/tasks 総数が 155 のまま ∴ ★押した結果は入っていない」
今回の実測      : 完走したのに ★総数は 155 のままである
```
> **★「task が増えないこと」は「入っていないこと」ではない。**
> **★観測の依頼は task を作らない**（画面が自分でそう書いている:「観測要求は既存GPUタスクを自動継続しない」）。
> **∴ 私は★指標にならないものを指標にしていた。**

**★あの時の結論（入っていない）は★正しかった。** **★しかし理由が違った**（実際の原因は JS 構文エラー）。
> **★当たったが、理由が違う。** **★次に同じ測り方をすれば間違える。** **∴ 記録する。**
> **★これは D-59 の3区分に4つ目を足す話ではない。** **★③「その計器はその答えを出せる場所か」の実例である。**

## 6. ★次（順番）
```
① 資料の同期（G-70・G-71。★私の commit 5c83f50 は済んでいる）
② SENT を成功時にも残す（§3・小さい）
③ 160件の壁（§4）
④ D-62（入力4件の文言）
```
**★③は「見えるようにする」ではなく「読めるようにする」である。** **★Phase 2 の中心に在る。**

---
*MGR。★**動いた**——Taka の実操作で `OBSERVE_CURRENT_STATE → RUNTIME_INSPECTION` が完走し `observation ingested to EGL`／`OBS-00719〜00722` まで出た（**front door → DS → RRI → 実測 → EGL 書き込みが通った。根拠は Taka の実操作1件**・MGR は投入していない）。★**今日入れた計器が初回で効いた**=`runs/submit_access.log` に `2026-07-29T20:44:33 RECV /api/submit` が残り、**「届いたかどうか分からない」が「届いた」に変わった**——**D-63 で「4 が無いと次も『分からない』と書くことになる」と書いたその1回目で効いた**。★**欠陥①=`SENT` が成功時に残らない**（`SENT` を書いているのは 401 の枝だけで、今回のログは `RECV` のみ）∴ **「受けた」は残るが「返した」が残らず、「処理中に落ちた」と「正常に返した」を区別できない＝D-64 の目的が半分しか達成されていない** ∴ **成功時にも `SENT` と status を残す（fail-open のまま）**。★**欠陥②=160件の ID の壁**——画面「継続中の別タスク」が `TASK-…` を**160行、ID だけで、名前も日付も状態も無く**並べており、**Taka がそれを丸ごと貼ったこと自体が「読めない」ことの実演**で、**D-57（出力24件）・D-62（入力8件）のどちらでも数えていない＝「量」を数えていなかった** ∴ 要求は(1)**全件を出さず件数を出して少数にする**(2)**ID だけで並べず人が選べる手掛かりを1つ付ける**（何を付けられるかは実測で決め、**無ければ「無い」と書く**）(3)**「これで全部ではない」と分かる形にする**。★**欠陥③=MGR の推論が誤っていた**——D-63 で「`/api/tasks` 総数が 155 のまま ∴ 押した結果は入っていない」と書いたが、**今回は完走したのに総数は 155 のまま**であり、**「task が増えないこと」は「入っていないこと」ではなく、観測の依頼は task を作らない**（画面が自分で「観測要求は既存GPUタスクを自動継続しない」と書いている）∴ **指標にならないものを指標にしていた**——**あの時の結論（入っていない）は正しかったが理由が違い（実際の原因は JS 構文エラー）、次に同じ測り方をすれば間違えるので記録する。これは D-59 の3区分に4つ目を足す話ではなく③「その計器はその答えを出せる場所か」の実例である**。★次=**①資料の同期（`G-70`・`G-71`）②`SENT` を成功時にも残す（小さい）③160件の壁 ④D-62（入力4件の文言）**——**③は「見えるようにする」ではなく「読めるようにする」であり Phase 2 の中心に在る**。*
