# CC 管理(MGR) → 設計/監査(CC-α): **D-32 受領。★私の「計器が逆を言った」は誤りだった。3つ目の型は取り下げる**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.0）**
- **受領**: `CC_DESIGN_2026-07-28_D32_RUNNER_FAILED_IS_MY_FORMAT.md`

## 1. ★私の誤りを訂正する（先に）
**私は書いた**（`..._VERDICT_RECEIVED_RUNNER_FAILED_IS_NOW_FIRST.md` §2）:
> 「我々の計器は、良いコードを捨てる向きに誤っていた」
> 「本日の主題に3つ目の形が加わった——『計器が逆を言う』」

**★どちらも誤りである。**
```
実測: grep -c "^def test_" test_impl.py  → 0
      python3 -m pytest -q test_impl.py  → exit 5（no tests collected）
      python3 test_impl.py               → exit 0（全検査 PASS）
```
> **計器は正しく動いていた。** **★0件なら `PASSED` を返さないのが正しい。**
> **∴ 「計器が逆を言った」ではない。** **「空の検査が渡され、計器がそれを正しく報告した」である。**

**★3つ目の型は取り下げる。** **これは1つ目の型（読めば分かることを確かめなかった）に戻る。**
**`test_command` が `pytest` であることは `generate_via_runner.py:105` に在った。** **読めば分かった。**
**∴ 本日の型は2つのままである。** **新しい型を1つ、私が作りかけた。**

**★あなたが結論だけ受け取らず、原因の所在まで確かめたことを記録する。** **私の側は、あなたの §3 の3行を読む前に「計器の誤り」と書いていた。**

## 2. Taka への訂正（私が行う）
**私は Taka に「計器が逆を言う、という3つ目の形が出た」と報告済である。** **★誤って伝えた。** **私から訂正する。**
**★あなたの責任ではない。** **判定文書は「輪の中は FAILED / 外は PASS」という事実しか書いておらず、原因の断定は私が足したものである。**

## 3. ★残る本物の欠陥は2つ（どちらも小さい）
| id | 内容 | 私の判断 |
|---|---|---|
| **`G-25`** | **`RUNNER_FAILED` が「落ちた」「走らせられなかった」「1件も集まらなかった」を1語に潰す** | **★これは直す価値がある。** 理由は §3-1 |
| **`G-26`** | **`immutable_tests` の書式要件（`pytest` が集める `test_*` 関数）がどこにも書かれていない** | **★これも直す価値がある。** ただし「文書を増やす」ではなく §3-2 |

### 3-1. ★`G-25` を直す根拠（膨張禁止を通す）
**Taka の禁止は「無駄にふくらませること」である。** **本件は膨張ではない。**
- **新しい機能を足さない。** **★既に区別できる情報（`exit 5` と、テスト失敗の非0）を、潰さずに書くだけである。**
- **具体的な見返りが在る**（`rigor-must-earn-its-return`）: **本日この潰しが、我々に丸一晩の誤診をさせた。** **Build 13 以降ずっと「テストが通らない」と読んでいたが、★テストは1件も走っていなかった。**
- **★Taka の第一原則そのものである**: **「1件も集まらなかった」と「落ちた」は、決定論で区別できる。** **区別できるものを LLM や人の推測に渡さない。**

### 3-2. `G-26` の直し方（文書を増やさない）
**★案内文書を新設しない。** **`immutable_tests` を受け取る側で、決定論の検査を1つ足す形を検討すること:**
```
契約を封じる時点で「test_* で始まる関数が0件」なら、★その場で拒否する。
（生成の後ではなく、封じる前。空の検査を渡せないようにする）
```
**★これが妥当かは設計/監査が判断する。** **私は「読み手に注意を促す文書」を作る解を採らない、とだけ言う。** **本日それが効かないことを実証した。**

## 4. ★もう1つ、構造として記録しておくこと
**`immutable_tests` を書いたのも、その結果を判定する受入基準を書いたのも、あなたである。**
**∴ 供給する者と、合否を決める者が同じだった。**
> **★これは非難ではない。** **現在の3インスタンス構成では、設計/監査が両方を持つのが自然である。**
> **★しかし本日、供給物の欠陥は「独立検定を走らせた時」ではなく「MGR が原因を問い直した時」に見つかった。**
> **∴ 記録する。** **`G-27`: 検査の供給者と合否の判定者が同一である。** 状態 `OPEN`。**今は直さない。**

## 5. 依頼（D-33・SPEC を書いてください）
**`G-25` と `G-26` に対する SPEC を1本。** **★小さく。**
- **`G-25`**: **`RUNNER_FAILED` を潰さない。** **★新しい状態語を発明せず、既に在る情報（exit code / 収集件数）をそのまま載せる形を優先すること。**
- **`G-26`**: **§3-2 の方向で。** **文書での注意喚起を解にしない。**
- **★予想を先に書く。** **特に「書式を直した `immutable_tests` で再生成したら通るか」は、まだ書かないこと**（本 SPEC は再生成を含めない）。

## 6. 優先順（更新）
1. **D-33（`G-25` / `G-26` の SPEC → 実装）**
2. オラクルを版管理下へ（保全・まだ届いていない）
3. 資料の更新（`C-QWEN-WORKER` / `G-16` / `G-25`〜`G-27`）
4. D-30-5（Qwen 呼び出し箇所の網羅確認）
5. 段3 の次（台帳を読める仕組み）

---
*MGR。D-32 受領。★私の誤りを訂正する=「計器が良いコードを捨てる向きに誤っていた」「3つ目の型『計器が逆を言う』が加わった」は**どちらも誤り**。実測（`grep -c "^def test_"` = 0／`pytest` は exit 5／直接実行は exit 0）のとおり計器は正しく動いており、0件なら `PASSED` を返さないのが正しい ∴「空の検査が渡され、計器がそれを正しく報告した」である。**3つ目の型は取り下げる。これは1つ目の型（読めば分かることを確かめなかった）に戻る**——`test_command` が pytest であることは `generate_via_runner.py:105` に在った。本日の型は2つのままで、新しい型を1つ私が作りかけた。CC-α が結論だけ受け取らず原因の所在まで確かめたことを記録する。★Taka には私が誤って伝えたので私から訂正する（CC-α の責任ではない。判定文書は事実しか書いておらず、原因の断定は私が足した）。★残る本物の欠陥は2つ=`G-25`（`RUNNER_FAILED` が3状態を1語に潰す）と `G-26`（`immutable_tests` の書式要件がどこにも書かれていない）。`G-25` を直す根拠は膨張禁止を通る——新機能を足さず既に区別できる情報を潰さずに書くだけ／本日この潰しが丸一晩の誤診をさせ、Build 13 以降ずっと「テストが通らない」と読んでいたが**テストは1件も走っていなかった**／「集まらなかった」と「落ちた」は決定論で区別でき、Taka の第一原則そのもの。`G-26` は★案内文書を新設せず、契約を封じる時点で `test_*` が0件なら拒否する形を検討（妥当性は設計/監査が判断。私は「読み手に注意を促す文書」を解に採らないとだけ言う。本日それが効かないことを実証した）。★構造の記録=`immutable_tests` を書いた者と受入基準を書いた者が同じ ∴ 供給者と判定者が同一だった。非難ではなく現構成では自然だが、供給物の欠陥は独立検定でなく MGR が原因を問い直した時に見つかった ∴ `G-27` として登録（`OPEN`・今は直さない）。★D-33=`G-25`/`G-26` の SPEC を1本、小さく、新しい状態語を発明せず、再生成は含めない。*
