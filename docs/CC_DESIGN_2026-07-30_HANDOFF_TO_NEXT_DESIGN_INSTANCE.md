# 設計/監査(CC-α) 引き継ぎ — **★次の設計インスタンスは、まずこれを読む**

- `BUILD_ROLE: 参照` / **宛: 次の DESIGN/AUDIT(CC-α)** / 写: MGR / Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 15:53 / TYPE=HANDOFF

---

# 0. ★最初にやること（★順番どおり）
```
1. ~/.claude/hooks/2der_role.sh set DESIGN        ← ★これをやるまで作業を始めない
2. egl/docs/CC_OPERATING_POLICY.md を読む         ← ★文書冒頭に「運用方針 確認済（版: v2.8）」と書く
3. egl/docs/CC_2DER_USAGE_GUIDE.md を読む         ← ★Taka 常設命令 §0 が入っている
4. 本文書 §2「いまの状態」を読む
5. ★何も始めない。★MGR の指示を待つ（★2026-07-30 15:53 時点で全員 停止中）
```
> **★`cd` は必ず `/home/takasan/egl/docs` に戻す。** **★`cc_register.py` はそこでしか動かない。**
> **★`cd /home/takasan/twoder` を残したまま python を打つと★必ず落ちる**（`operator.py` が stdlib を隠す）。**★本日4回 踏んだ。**

---

# 1. ★あなたは誰か
| | |
|---|---|
| **役割** | **設計/監査(CC-α)**。3インスタンス（MGR / DESIGN / IMPL）の1つ |
| **やる** | BUILD SPEC を書く／BUILT を監査する／`2DER_EXECUTION_ARCHITECTURE.{md,json}` を保つ／Gap を登記する |
| **★やらない** | **★実装しない。★commit しない。★自分の監査を自分で承認しない。★運用方針を自分で書き換えない**（MGR へ上げる） |
| **★宛先** | **★SPEC は IMPL 宛。★裁定を仰ぐものは MGR 宛。** MGR は設計・監査にしか書かない |
| **経路** | **★チャットでなく `egl/docs/CC_*.md` の file 往復**。★書いたら `cc_register.record_doc(...)` で登記する |

---

# 2. ★いまの状態（2026-07-30 15:53）
```
★Phase 2 完了 ／ ★Phase 3 は P-1〜P-5 を終えて ★Taka の評価待ち
★全員 停止中。★あなたも何も始めない。
```
| Taka の Phase 3 終了条件 | |
|---|---|
| 1 受信状態を問い合わせられる | **成立**（★「あの依頼が届いたか」は不可） |
| 2 EGL から証拠を1件 取得 | **成立**（`DE-0567` / `DE-0568`） |
| 3 主体担当を自動取得 | **未** |
| 4 散文依存が1つ減る | **不成立**（6 → 6） |
| 5 人の確認作業を1件 停止 | **成立**（★自己申告／★口のおかげかは区別できない） |
```
★三値: 能力形成 成立 ／ 体制再編 成立（本日 初） ／ ★主体移管 不成立（2DER 担当 0/8）
★Phase 3 で実装したのは ★口1つだけ（GET /api/receipt）。P-2/P-3/P-5 は 0
★C（登記だけで直していないもの）: 21件。★掘らない
```

## 2-1. ★Taka を待っている2件（★急がせない）
| | |
|---|---|
| `CC_TAKA_2026-07-30_REGRESSION_INJECTS.md` | **非回帰が本番へ投入していた件。「止めたまま進む」か「向き先を直す」か** |
| `CC_MGR_2026-07-30_PHASE3_EVALUATION_REPORT.md` | **Phase 3 の評価**（★冒頭 `§0 訂正` を必ず読む） |

---

# 3. ★いま生きている決め事（★破ると事故になる）
## 3-1. Taka 常設命令（★最優先）
```
★架空の依頼文や調査専用データを本番経路へ投入しない
★全案件を台帳経由で管理する ／ ★新規の台帳を作らない ／ ★台帳に直接 手を出さない
★あなたは 2DER の外にいる市民である。★2DER の内部に入らない
★見つけた問題はその場で直さず登記して進む（★本線を実行不能にする欠陥を除く）
★規則・完成語彙・指数・スコアを新しく作らない
```
## 3-2. ★2DER 優先原則（★全文書の冒頭に5点を書く）
```
① 2DER のどの入口を使うか   ★(a) HTTP の入口 か (b) submit() の道 か を書く（資料 §5.3-0）
② 2DER で何を試したか
③ ★2DER では何ができなかったか   ← ★ここが空欄なら、まだ 2DER を使っていない。実装へ進まない
④ その不足を補うために何を実装するか
⑤ 実装したなら書く（★実装していないときだけ省略してよい）
```
## 3-3. ★走らせてはいけないもの
```
★非回帰98本のうち、★「逃がしている」印が無い 61本は走らせない（既定は反転済）
★印が在る 37本も「候補」であって「安全」ではない。★走らせる本だけ1行 読んで確かめる
★禁止（本番へ書く）: test_concurrency_and_run_gate / test_dead_approach_guard / test_rri_formal
★受入に「N passed / M failed」という総数を書かない。★走らせた test の名前と結果を書く
```

---

# 4. ★私の作法（★今日 効いたもの・★真似してよい）
| # | |
|---|---|
| **1** | **★受入は「1条件に1つの印」。★まとめない**（まとめると中の1つが落ちても分からない） |
| **2** | **★「0件」を受入にするとき、★その0が「作った物自身」を含まないか確かめる**（★今日3回 満たせない受入を書いた） |
| **3** | **★変更前の値は、変更前にしか取れない。★先に取る**（★commit 前なら `git show HEAD:…` で後からでも作れる） |
| **4** | **★基準は自分で取る。★相手の基準を使わない**（★出所が違う2つが一致して初めて検算になる） |
| **5** | **★0 が返ったら計器を疑う。★「思ったより少ない」も同じ** |
| **6** | **★語の一致は主題の一致ではない。★当たった件を開いて読む** |
| **7** | **★数を書くときは述語を一緒に書く**（★書かないと後から比べられない。★私は 42/37 で食い違った） |
| **8** | **★「探した範囲」を「無い」と書かない。★「見つからない」と書く** |
| **9** | **★言えない受入を書かない。★書く前に「これは示せるか」を測る** |
| **10** | **★Taka 宛の報告は、出る前に監査する**（★MGR が「宛先が Taka だから」で素通ししていた） |

---

# 5. ★今日 私が踏んだ罠（★同じ所で転ばないため）
| # | 罠 | ★回避 |
|---|---|---|
| 1 | **`cd` が別 repo に残ったまま python/grep** | **★毎回 `egl/docs` に戻る**（★4回 踏んだ） |
| 2 | **ページ全体で文字列を数える** | **★差分で数える**（★3回 助かった） |
| 3 | **`grep -c … \|\| echo "読めない"`** | **★grep は0件で終了1 ∴ 偽の「読めない」が出る** |
| 4 | **正規表現が壊れて全部0** | **★「全部0」を「きれい」と読まない** |
| 5 | **`function X` を正規表現で切り出す** | **★行頭の `async` を落とす。★偽の欠陥を報告しかけた** |
| 6 | **1件だけで測る** | **★状態ごとに測る**（★`CREATED` の task では下流が空。★4件→9件に変わった） |
| 7 | **「テスト」を中身を読まずに走らせる** | **★98本が何をするか1本も読まずに2回 走らせ、★禁止を破った** |

---

# 6. ★資料の保ち方（★あなたの担当）
```
★2DER_EXECUTION_ARCHITECTURE.json と .md を★両方 直す（★gap id の集合が一致していること）
★確認: JSON と MD の G-xx を突き合わせ、★食い違い0 を毎回 出す
★commit は MGR。★同期は「MGR が commit した後」（★順番: 直す→再起動→配信物で検査→設計が受入→MGR commit→設計が同期）
★Gap の採番は設計/監査がする（★MGR は id を指定しない）
★いま G-01〜G-77 ／ component 23 / edge 11 / entrypoint 8
```

---

# 7. ★覚えておくと効く事実（★今日 測ったもの）
```
★front door は2つを指す（資料 §5.3-0）: (a) HTTP の入口 ／ (b) submit() の道
   → ★(b) は RECV に出ない。★G-77 で止めたテストも (b)
★153件の分類は 0.004秒（★D-76 で events を渡せるようにした）
★DE は front door から引ける: GET /api/resolve?id=DE-xxxx
★DE を記録する正規の道は既に在る: egl/structure/de_submit_route.py::record_de（★既定=front door）
★「2DER 自身の操作」の定数は既に在る: principal_attribution.py の _TWODER_SELF
★受信を聞く口: GET /api/receipt（★1件ごとには答えられない。★id が無い）
★DS を返す口は★無い（★手前に何が残ったか聞けない）
```

---

# 8. ★次の人へ（★私からの1行）
> **★今日いちばん効いたのは、★作ったものではなく「★作らなかったこと」だった。**
> **★条件を1つ満たせる場所に立って、★数のために記録を作らないことを選んだ。**
> **★受入が弱くなるのを承知で、★非回帰を止めた。**
> **★「あと1段で動く」と書かなかった。**
>
> **★あなたも、★測れないものを測れたことにしないでほしい。**
> **★MGR も IMPL も、★自分の誤りを自分から出す人たちである。★あなたも出してよい。**

---
*CC-α 引き継ぎ（2026-07-30 15:53）。★**最初に役割を DESIGN に設定し、運用方針と使用ガイドを読み、本文書 §2 を読んでから何も始めない**（★全員 停止中・Taka の評価待ち）。★**cwd は必ず `egl/docs` に戻す**（`cc_register.py` はそこでしか動かず、`twoder` を cwd にすると `operator.py` が stdlib を隠して落ちる。**本日4回 踏んだ**）。★**役割**=SPEC を書く／BUILT を監査する／実行構造の資料を保つ／Gap を登記する。**実装しない・commit しない・自分の監査を自分で承認しない・運用方針を自分で書き換えない**。**SPEC は IMPL 宛、裁定は MGR 宛、経路は `CC_*.md` の file 往復で書いたら `cc_register.record_doc` で登記**。★**いまの状態**=Phase 2 完了・Phase 3 は P-1〜P-5 を終えて Taka の評価待ち。終了条件は**1 成立／2 成立／3 未／4 不成立（6→6）／5 成立**、三値は**能力形成 成立・体制再編 成立（本日 初）・主体移管 不成立（2DER 担当 0/8）**、**Phase 3 で実装したのは口1つだけ**、**C は21件で掘らない**。**Taka を待っているのは2件**（非回帰の投入をどうするか／Phase 3 の評価。**評価報告は冒頭 `§0 訂正` を必ず読む**）。★**生きている決め事**=Taka 常設命令（**架空データを本番へ入れない／新規台帳を作らない／台帳に直接 手を出さない／2DER の内部に入らない／見つけても直さず登記して進む／新しい語彙や指数を作らない**）と**2DER 優先原則の5点**（★③が空欄なら まだ 2DER を使っていない）と**走らせてはいけないもの**（**印が無い61本は走らせない・印が在る37本も「候補」であって安全ではない・禁止3本・受入に総数を書かず走らせた test の名前を書く**）。★**作法10件**（1条件に1つの印／「0件」の受入が自分自身を含まないか／変更前の値を先に取る／基準は自分で取る／0 が返ったら計器を疑う／語の一致は主題の一致ではない／数には述語を添える／「探した範囲」を「無い」と書かない／言えない受入を書かない／**Taka 宛の報告は出る前に監査する**）。★**今日 踏んだ罠7件**（cwd 残り／ページ全体で数える／`grep -c ... || echo` の偽表示／壊れた正規表現の全部0／`async` を落とす切り出し／1件だけで測る／**中身を読まずにテストを走らせて禁止を破った**）。★**資料の保ち方**=JSON と MD を両方 直し G-xx の集合一致を毎回 確認、commit は MGR、同期は commit の後、Gap の採番は設計、いま **G-01〜G-77／component 23／edge 11／entrypoint 8**。★**覚えておくと効く事実**=front door は2つ（(a) HTTP／(b) `submit()` の道。**(b) は RECV に出ない**）／153件の分類は 0.004秒／**DE は `GET /api/resolve?id=DE-xxxx` で引ける**／**DE を記録する正規の道は `de_submit_route.record_de`（既定=front door）**／**`_TWODER_SELF` が既に在る**／**`GET /api/receipt` は1件ごとには答えられない**／**DS を返す口は無い**。★**次の人へ**=**今日いちばん効いたのは作ったものではなく「作らなかったこと」であり、条件を1つ満たせる場所に立って数のために記録を作らないことを選び、受入が弱くなるのを承知で非回帰を止め、「あと1段で動く」と書かなかった。測れないものを測れたことにしないでほしい。MGR も IMPL も自分の誤りを自分から出す人たちである**。*
