# 【D-199】★2DER が ★単発で Claude を呼ぶ（★開いているインスタンスに見せるのは自動化ではない）

> ## ★★本件は ★保留（★着手しない）。★Taka 指示 逐語:「★ちょっとこの話は実際のとこ傍にのけておいて、★本筋に集中しといて」
> ```
> ★消さない。★後回しにしたと書く。★復帰条件: ★S-3 が完走し、★C-1 / C-2 が済んだ後に再判定する。
> ★★本書の内容は ★参考として残す（★環境の実測＝claude CLI 2.1.220 在り・API 鍵 無し は ★事実として有効）。
> ```

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-08-01 00:1x / TYPE=STATUS / **★保留**
- **開発者規律 確認済（版: v1.0）** ／ **この .md がまだ .md である理由**: 台帳へ渡す口が front door に無いため（C-1）

## 1. ★Taka 指示（逐語）
```
★「これはオープンになってるClaudeインスタンスが見ているのでは★意味がない。
   ★ショットでClaudeに実務依頼をできるようにならん？
   ★そうすれば ★インスタンスの立ち上がってるClaudeの仕事を減らせる。★自動化とはそういうもの」
```

## 2. ★★向きが逆転することを、先に固定する（★境界を壊さない）
```
★これまで: ★Claude が 2DER を触る（★外の市民が中へ入る＝★禁止されている形）
★★これから: ★2DER が Claude を呼ぶ（★Claude は ★2DER に使われる★部品になる）
★★★∴ ★境界（Claude は 2DER の外の市民）を ★壊さない。★むしろ ★Taka が言ってきた
   「★2DER を主体にする」の ★実体そのものである。
★★★★呼ばれた Claude は ★渡された packet の範囲だけを見る。★repo を書かない。★判定だけ返す。
```

## 3. ★MGR が確かめた事実（★環境。★2DER の中身ではない）
```
★`claude` CLI が入っている: ★`/home/takasan/.local/bin/claude` ／ ★`2.1.220 (Claude Code)`
★★`ANTHROPIC_API_KEY` は ★設定されていない ∴ ★従量課金の鍵は無い。★CLI の単発実行なら ★追加費用は生じない見込み【未確認】
★★★`twoder` 配下に ★`CLAUDE_SENIOR` / `claude_packet` / `UPPER_REVIEW` を含む .py が ★7件
   （★MGR が数えたのは★件数のみ。★中身は★未確認。★設計が確かめること）
★★★★`claude -p`（単発実行）を呼んでいるコードは ★見当たらなかったが、★MGR の探索範囲は狭い。★断定しない
```

## 4. ★依頼（★1件にまとめる。★`D-198` §4 の「戻す口」調査と★同じ件として扱う＝★増やさない）
```
(a) ★2DER から Claude を ★単発で呼ぶ経路は ★既に在るか → ★在って動く/★在るが未配線/★無い
(b) ★無ければ ★最小で作る。★仕様は次の4点だけ:
    ★入力  : ★`claude_packet`（★既に在る。★作らない）
    ★出力  : ★構造化された判定（★合否 ＋ ★足りない物 ＋ ★根拠）。★散文で返させない
    ★記録  : ★actor = `CLAUDE_SENIOR` として ★台帳に残る（★誰が判定したかが後から分かる）
    ★上限  : ★呼び出し回数の上限を ★先に決める ＋ ★呼んだ回数を ★機械で数える
★★★新しい台帳を作らない ／ ★新しい状態語を作らない ／ ★repo を書かせない（★判定のみ）
```

## 5. ★受入（★これを満たさなければ「できた」と言わない）
```
★いま止まっている `TASK-2DER-E8F8CA7B` について、
   ★★人が開いている Claude インスタンスを ★一切 使わずに、
   ★★★2DER が ★自分で Claude を呼び、★判定を受け取り、★task が ★次の段へ進む
      （★または ★理由つきで ★保留に入る）。
★★★★「呼べた」で終わらせない。★★判定が ★台帳に残り、★front door から読めるところまで。
```

## 6. ★これが循環になる条件（★`D-197` §4 を再掲。★緩めない）
```
★① 上級監査の指摘が ★2DER の仕組み（申請書テンプレ／受入試験）へ★書き戻る
★② 次の同種は ★Qwen だけで通る
★③ ★同じ穴で Claude が呼ばれた回数が ★減る（★機械で数える）
★★満たさない間は「★Claude を呼べるようになった」までしか書かない。
```

## 7. ★順序（★更新）
| # | 件 | 扱い |
|---|---|---|
| ACTIVE | S-3 人間用UIの4つ（＋止まっている理由の表示） | 進行中（IMPL） |
| 次 | (2) 止まった時に理由を書く配線 | `D-198` 裁定 |
| **その次** | **★本件（2DER が単発で Claude を呼ぶ＋答えを戻す口）** | **★`D-198` §4 を吸収。★別件にしない** |
| その次 | (1) 契約 None を task 分岐で止める ／ C-1 進捗の書き込み口 ／ C-2 申請書を 2DER が作る | 予約 |
| 後回し | C-3 ／ `D-191` | 後回しのまま |

## 8. ★Taka へ返す1点（★資源の話なので私は決めない・ただし止めない）
```
★呼ぶ経路は ★`claude` CLI の単発実行を ★既定とする（★API 鍵が無く、★追加費用が生じないため）。
★★これは ★あなたの Claude の利用枠を ★消費する。★∴ ★上限を先に決めて ★機械で数える。
★★★「★API 鍵を使う方がよい」なら ★言ってください。★言われるまでは ★CLI で進めます。
```

---
**決めたこと**: **①Taka 指示——開いているインスタンスが packet を見るのは自動化ではない。2DER が単発で Claude へ実務依頼できるようにする ②向きが逆転する。Claude が 2DER を触るのではなく、2DER が Claude を呼ぶ。境界を壊さず、むしろ「2DER を主体にする」の実体である。呼ばれた Claude は packet の範囲だけを見て判定だけ返し、repo を書かない ③環境の実測——`claude` CLI 2.1.220 が入っており、API 鍵は無い。twoder 配下に CLAUDE_SENIOR/claude_packet/UPPER_REVIEW を含む .py が7件（件数のみ・中身未確認）④依頼は1件にまとめ、D-198 §4 の「戻す口」調査を吸収する。仕様は入力=既存 packet、出力=構造化された判定、記録=actor CLAUDE_SENIOR、上限=回数上限と機械カウントの4点だけ ⑤受入は「開いているインスタンスを一切使わずに、2DER が Claude を呼び、判定が台帳に残り、front door から読め、task が次へ進むか理由つきで保留に入る」こと ⑥循環と呼べるのは、指摘が仕組みへ書き戻り、次は Qwen だけで通り、同じ穴での呼び出しが機械で数えて減った時だけ ⑦呼ぶ経路は CLI の単発実行を既定とする。Taka の利用枠を消費するので上限を先に決める。API 鍵を使うべきなら Taka が言う。**
