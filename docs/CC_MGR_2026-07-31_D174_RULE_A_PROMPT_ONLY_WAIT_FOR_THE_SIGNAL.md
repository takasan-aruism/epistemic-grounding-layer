# 【D-174】裁定は **(a) prompt にだけ足す** — **★ただし着手は Taka の合図の後**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 15:1x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.9）** ／ **★新しい名前で置いた**

## 1. ★裁定（★聞かれたので、その場で答える。★保留しない）

> ### **★(a) prompt にだけ足す。★`STRUCTURED_KEYS` には足さない。**

```
★理由1: ★「一度に一箇所」（Taka）。★(a) は1箇所、★(b) は2箇所である
★理由2: ★(b) は★既存の挙動を変える（★今まで通っていた計画が落ちうる）。
        ★★いま我々は「★何を変えたら何が動いたか」を測っている最中である ∴ ★変数を増やさない
★理由3: ★(a) で planner が書かなければ、★それが結果である（★「書ける器を与えても書かない」）。
        ★★それは ★(b) へ進む根拠になる。★順番が逆だと、この情報が取れない
★★設計の見立て（(a)）と同じである。★私は独立に同じ結論に達した
```

## 2. ★ただし着手しない（★Taka の線）
```
★Taka 逐語: 「★PLANの修正、新規監査、GENERATE実行はその確認後です」
★★確認（`D-173`）は★出した。★しかし ★Taka の「進め」は★まだ来ていない。
★★∴ ★裁定だけ置く。★SPEC を書いてよいが、★IMPL は着手しない。
★★★合図が来たら ★その場で動けるようにしておく（★D-160 の再発を避けるため、★SPEC は合図の後でよい）。
```

## 3. ★合図が来たときの形（★先に固定する。★増やさない）
```
★変更は ★`_plan_prompt` の中の★1箇所。★欄を1つ増やす（★JSON の欄として書けるようにする）
★★`STRUCTURED_KEYS` / `EXECUTABLE_KEYS` は★触らない（★検査も worker も壊さない・★設計が確認済）
★★受入は ★Taka の4条件のまま。★1文字も緩めない
★★確認は ★依頼文1回（★文は私が固定する）→ run_next 1回 → PLAN で停止
★★予想を先に書く: ★受入3 は動く見込み／★受入4 は別（★不足を観測から述べさせる話）
★★測定は並列（v2.9 §4-18）。★同時実行数を記録する
```

## 4. ★設計の7回目（★今回は形が違う。★記録する）
```
★★前6回: ★文書に書いてから訂正した。
★★今回 : ★書きかけて、★確かめて、★文書に入る前に止めた。
   ★内容: 「`DESTRUCTIVE_MARKERS` が大文字を素通りしている」と書こうとしたが、
          ★`_serialized_body` が `.lower()` していたので★誤りだった。
★★★通った本当の理由も突き止めた: ★計画が `authority_requirements` に docker を★宣言していた
   ＝ ★fail-closed が★設計どおり働いている（★これは 2DER についての★良い発見である）
★★★「機械 → 目視」に加えて「★書く前に確かめる」が1つ増えた。★7回目にして形が変わった。
```

## 5. ★やらないこと
```
★着手しない ／ ★prompt を変えない ／ ★GENERATE を押さない ／ ★新規監査を作らない
★★保留はそのまま: Ledger ／ 図の修正 ／ (c) の patch ／ 出力契約を本番へ（★今回の (a) がその一部になる）
```

---
**決めたこと**: **①裁定は (a) prompt にだけ足す——1箇所であること、(b) は既存の挙動を変えて測定の変数を増やすこと、(a) で書かれなければそれ自体が (b) へ進む根拠になること、の3つ ②ただし Taka の「確認後」の合図がまだ来ていないので着手しない。SPEC も合図の後でよい（`D-160` の再発回避） ③合図が来たときの形を先に固定した（`_plan_prompt` 1箇所・`STRUCTURED_KEYS` は触らない・受入は4条件のまま・投入1回・予想を先に書く・測定は並列） ④設計の7回目の誤りは、初めて文書に入る前に止まった——しかも通った本当の理由（`authority_requirements` の宣言で fail-closed が設計どおり働いている）まで突き止めた。**
