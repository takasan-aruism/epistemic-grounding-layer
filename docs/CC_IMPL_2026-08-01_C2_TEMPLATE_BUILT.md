# 【BUILT / C-2 `EVO-0022`】雛形は出た — **★「機構が保証している」は★経路による。★空欄でも弾かれなかった**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: MGR / Taka / 発: 実装(IMPL) / 2026-08-01 14:4x / TYPE=BUILT
- **開発者規律 確認済（版: v1.1 / 2026-08-01）** ／ **実装源**: `CC_DESIGN_2026-08-01_C2_TEMPLATE_THE_FORM_IS_ALREADY_HALF_THERE.md`
- **commit していない** ／ **`:8005` を叩いていない**

---

# 1. ★変更行数（★誰が書いたか）
```
★★Claude（IMPL）が書いた。★2DER の実績に数えない。
   ★新規 `twoder/request_template.py`  ★33行
   ★既存 `twoder/webui.py`             ★+2（★`build_state` に欄1行＋関数内 import 1行）
★★合計 ★35行。★新しい台帳・マーカー・状態語・エンドポイントは作っていない
★★★`contract_seal` の検査は ★1文字も緩めていない
```

---

# 2. ★受入 (a)〜(e)

| # | 受入 | 判定 | 実測 |
|---|---|---|---|
| **(a)** | `/api/state` に `next_request_template` が在り雛形が返る | **★○** | `B37727E3` で **3519字**／`24EF3F6E` でも返る |
| **(b)** | 契約マーカーの枠が在り、中身が空（`def test_` が0行） | **★★task による（×の場合が在る）** | `24EF3F6E`（goal に契約なし）: **`def test_` 0行・SK/IT/END = 1/1/3 → ○**／**`B37727E3`（goal に契約あり）: `def test_` 5行・SK/IT/END = 2/2/4 → ×** |
| **(c)-1** | 空のまま投入 → `ValueError` で弾かれる | **★★×（弾かれなかった）** | `24EF3F6E` の雛形を投入 → **`task_id: "TASK-2DER-670DA828"` が作られた**（`request_type=OBSERVE_CURRENT_STATE`）。★エラーは返っていない |
| **(c)-2** | 埋めて投入すると通る（別 task になる） | **★○** | `task_id: "TASK-2DER-F8BC8053"`（**★予告と一致**）／`request_type=MODIFY_EXISTING` |
| **(d)** | 止まった理由が front door から読める | **★○（再確認）** | `B37727E3` の `work.next_information_need` **7件** |
| **(e)** | 枠は日本語で出る（★中身は英語のまま） | **★半分（★先に書いたとおり）** | 枠は日本語。中身は逐語「Explicit mapping table or function for English-to-Japanese status translation」＝**英語のまま**。**★「日本語で出ました」とは書かない** |

---

# 3. ★★食い違い①: `(b)` は「前回の依頼」に契約が入っていると崩れる

```
★SPEC §3-1 は ★「■ 依頼（★前回の依頼をそのまま置いています）」と指定した。
★★`TASK-2DER-B37727E3` の goal は ★S-3 の依頼文であり、★★契約マーカーを含んでいる（★実測）。
★★★∴ ★雛形の「依頼」欄に ★★前回の契約が丸ごと再掲され、★雛形全体では
   ★`def test_` が ★5行、★マーカーが ★SK 2 / IT 2 / END 4 になる。
★★★★＝ ★★受入(b)「中身が空」と ★SPEC §3-1「前回の依頼をそのまま置く」は
   ★★契約入りの依頼に対しては ★★両立しない。
★★★★★★私は ★指示どおりに作った（★goal を加工していない）。★★どちらを採るかは ★設計の判断である。
```

---

# 4. ★★食い違い②（★こちらが重い）: **「空欄で出すことは機構が保証している」は★経路による**

```
★SPEC §2 の逐語:「★契約の枠だけ在って中身が空の雛形は ★投入時に `ValueError` で弾かれる」
★★★実測: ★★弾かれなかった。★task が1件 作られた（`TASK-2DER-670DA828` / `CREATED`）。

★なぜか（★決定論・逐語で示す）:
  ★① `contract_seal.extract_contract(雛形)` は ★★確かに ValueError を投げる
     逐語: `ValueError: immutable_tests has no pytest-collectable test (^def test_)`
  ★② しかし ★`submit.py` で ★`extract_contract` が呼ばれるのは ★2箇所だけ:
     ★`:237` ← ★★EVO-0028 で ★私が入れた判定。★★try/except で包んでおり ★★例外を握りつぶす
     ★`:506` ← ★DW 分岐の中 ∴ ★`OBSERVE_CURRENT_STATE` に分類された投入は ★通らない
  ★③ ★雛形の投入は ★`OBSERVE_CURRENT_STATE` に分類された ∴ ★★どちらにも当たらず ★素通りした
★★★★★∴ ★「人が埋めるまで進めない」は ★★DW 経路でのみ機構である。★観測経路では ★機構ではない。
```

## 4-1. ★★私の変更が関与している（★自分の分を先に書く）
```
★`:237` の try/except は ★★私が `EVO-0028` で入れた（★「壊れた契約は『契約あり』側に倒す」＝★安全側のつもり）。
★★★結果として ★★壊れた契約の ValueError が ★そこで消える。
★★★★ただし ★★それが無くても ★`OBSERVE` 経路は ★`:506` に到達しない ∴ ★素通りは起きる。
   ＝ ★★私の変更は ★★唯一の原因ではない。★しかし ★弱めた側に働いている。★隠さない。
★★★★★★直すべきかは ★設計の判断である（★`contract_seal` の検査は緩めていないが、★呼ばれない所が在る）。
```

---

# 5. ★予告の当否（★投入前に固定・`c2_pre.txt`）
| 予告 | 結果 |
|---|---|
| 変更35行（33＋2）・Claude が書いた | **★当たり** |
| (a) 雛形が返る | **★当たり** |
| (b) `def test_` が0行 | **★半分 外れ**（★契約入りの goal では 5行になった。★§3） |
| (c) 空なら弾かれる | **★★外れた**（★弾かれず task が1件 増えた。★§4） |
| (c) 埋めれば通る／`TASK-2DER-F8BC8053` | **★当たり**（★id まで一致） |
| (d) 7件 読める | **★当たり** |
| (e) 枠は日本語・中身は英語 | **★当たり**（★半分しか満たせないと先に書いた） |

---

# 6. ★副作用（★隠さない）
```
★★(c) の測定で ★task が ★2件 増えた:
   ★`TASK-2DER-670DA828`（★空のまま投入・`CREATED`）／★`TASK-2DER-F8BC8053`（★埋めて投入・受入(c)-2）
★★★前者は ★★弾かれる予定だったものである ∴ ★★増えたこと自体が ★§4 の証拠である。
★★★★消していない（★append-only）。★畳む口が無いことは ★`EVO-0028` で既に登記済み。
```

# 7. ★戻し方（★可逆）
```
★① `twoder/request_template.py` を消す（★新規ファイル・★他から参照されるのはここだけ）
★② `twoder/webui.py` の `build_state` から ★`"next_request_template": …` と ★関数内 import の2行を消す
★★`git checkout -- webui.py` ＋ `rm request_template.py` で戻る
```

# 8. ★廃止（★SPEC §6・★通った時だけ）
```
★★通っていない（★(b)(c) が ×）∴ ★★①②とも ★廃止しない。★★嘘を書かない。
```

---
*IMPL → 設計/監査（写: MGR / Taka）。C-2。**Claude（IMPL）が 35行**（`twoder/request_template.py` 33行 新規＋`webui.py` +2）。新しい台帳・マーカー・状態語・エンドポイントは作らず、`contract_seal` の検査も緩めていない。**受入は (a)○（雛形3519字が `/api/state` から返る）／(d)○（`next_information_need` 7件）／(c)-2○（埋めて投入すると通り、予告 `TASK-2DER-F8BC8053` と id まで一致）／(e) 半分（枠は日本語・中身は英語のまま——「日本語で出ました」とは書かない）。** **★(b) は task による——goal に契約が無い task では `def test_` 0行で ○ だが、`B37727E3`（goal が S-3 の契約入り依頼文）では雛形の「■ 依頼」に前回の契約が丸ごと再掲され `def test_` 5行になり ×。SPEC §3-1「前回の依頼をそのまま置く」と受入(b)「中身が空」は、契約入りの依頼に対して両立しない。私は指示どおり goal を加工していない。どちらを採るかは設計の判断。** **★★(c)-1 は外れた——「空欄で出すことは機構が保証している」は経路による。`extract_contract(雛形)` は確かに `ValueError: immutable_tests has no pytest-collectable test (^def test_)` を投げるが、`submit.py` で呼ばれるのは `:237`（EVO-0028 で私が入れた判定・try/except で例外を握りつぶす）と `:506`（DW 分岐の中）だけで、`OBSERVE_CURRENT_STATE` に分類された雛形の投入はどちらにも当たらず素通りし、`TASK-2DER-670DA828` が作られた。★私の `EVO-0028` の try/except が弱めた側に働いている（唯一の原因ではないが隠さない）。直すかは設計の判断。** 副作用として測定で task が2件 増えた（前者は弾かれる予定だったもの＝それ自体が証拠）。戻し方は新規ファイルを消して `webui.py` の2行を戻すだけ。**通っていないので SPEC §6 の廃止は行わない（嘘を書かない）。** commit していない。*
