# 設計/監査 → MGR（写: Taka / IMPL）: **Build 15 の修理は、再起動しないと効かない（先回りの1件）**

- `BUILD_ROLE: 参照`（短い注意喚起。**実装源ではない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=FINDING
- **運用方針 確認済（版: v1.9）**

## 0. 用件（1つだけ）
**状況表が既にこう出している:**
```
実行中プロセス: twoder.webui(pid 1594748, 4h前起動: ★ソースが新しい=再起動が要る)
```
**Build 15 は `twoder/authority.py` と `twoder/generate_via_runner.py` を直す。**
**動いている webui はそれを持たない。** **∴ 直後に `GENERATE` を走らせても、修理前のコードが動く。**

> **★これは Build 10 と同じ形である**（修理が disk 上では正しく、実行中の系に入っていない＝本日7回目の「作ったが作用する場所に無い」の新形）。
> **∴ 次の `GENERATE` の前に、webui を再起動する必要がある。**

## 1. なぜ先に出すか
- **Build 15 の BUILT → 監査 → `GENERATE` の SPEC、と進んでから気づくと、1往復無駄になる。**
- **再起動は Taka の live な画面（`100.107.6.119:8770`）を止める操作である。** **∴ MGR が Taka に諮る時間が要る。**
- **★前回（Build 10R）は Taka の直接発話「webui 再起動して受入やり直して」で許可された。** **その許可はあの1回に対するものであり、恒久ではないと私は読んでいる。** **∴ 私は勝手に前提にしない。**

## 2. 併せて確認したいこと（再起動すると何が同時に効くか）
**前回の再起動では、Build 10 の修理だけでなく 8 commit がまとめて効いた**（段3e の配線・token gate の配線など）。**そして token gate が新しい壁になった。**
**∴ 今回も「再起動前に、前回の再起動以降に何が変わったかを確認する」べきである。**
```
前回の再起動: 2026-07-27 18:37:58（pid 1594748）
∴ 確認範囲: twoder / dev-workcell の 18:37 以降の変更
```
**★私はまだ確認していない。** **再起動の裁定が出たら、その前に確認して出す。**

## 3. 私からの提案（裁定は MGR）
1. **Build 15 の BUILT を受けたら、監査と並行して再起動の可否を Taka に諮る。**
2. **再起動の直前に §2 の差分を私が出す。**
3. **`GENERATE` の SPEC は、冒頭に「起動時刻 > ソース mtime」の確認を必須で入れる**（既定どおり）。

---
*CC-α。先回りの1件=Build 15 の修理は `twoder/authority.py` と `generate_via_runner.py` を触るので、動いている webui(pid 1594748・4h 前起動)を再起動しないと効かない——直後に GENERATE を走らせても修理前のコードが動く（Build 10 と同じ形・本日7回目の「作ったが作用する場所に無い」の新形）。先に出す理由=BUILT→監査→SPEC と進んでから気づくと1往復無駄になる／再起動は Taka の live な画面を止めるので MGR が諮る時間が要る／前回の許可は Taka の直接発話によるあの1回に対するものであり恒久ではないと読んでいるので勝手に前提にしない。★併せて、前回の再起動では 8 commit がまとめて効き token gate が新しい壁になったので、今回も「前回の再起動(18:37:58)以降に何が変わったか」を再起動前に確認すべき——私はまだ確認しておらず、裁定が出たら出す。提案=BUILT 受領後に監査と並行して Taka に諮る／再起動直前に差分を出す／GENERATE の SPEC 冒頭に鮮度確認を必須で入れる。*
