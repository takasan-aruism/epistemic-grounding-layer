# CC 管理(MGR) → 設計/監査(CC-α): **★Taka の問い「`HUMAN_RELAYED` にすると実際に何が変わるか」。答え=分岐は1つだけ、保存3項目。判断は1つも変わらない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **典拠: Taka（2026-07-28・逐語 §1）**

## 1. Taka の問い（逐語）
> 「**origin=HUMAN_RELAYED になった結果、実際に どの分岐 / どの判断 / どの保存内容 が変わったのか。変わらなかったものも全部列挙してください。**」

## 2. ★まず事実: Build 22 はまだ作られていない
**SPEC を発注した直後であり、実装されていない。** **∴ 現時点で「変わったもの」はゼロである。**
**∴ 以下は「作った場合に何が変わるか」の調査である。** **★実測は `origin` を読む側の探索であり、動かした結果ではない。**

## 3. ★変わるもの（3つだけ）
**探索範囲（§3-7）**: `ds/ds` `rri/rri` `egl/egl` `dev-workcell/dw` `twoder`（`test*` / `regression/` / `structure/` を除く）。**本番コードで `origin` を含む行は 18 行。**

### 3-1. 分岐（★1つだけ・DS の中）
```
ds/ds/phase0.py:112   if origin not in ORIGINS: raise ValueError(...)
ds/ds/phase0.py:114   if origin == "HUMAN_RELAYED" and not (relayed_by and authored_by):
                          raise ValueError("origin=HUMAN_RELAYED は relayed_by と authored_by の併記を要求する…")
```
> **∴ `HUMAN_RELAYED` にすると、「誰が入れたか」「誰が書いたか」の併記が★強制される。** **書かなければ記録を拒否する。**
> **★これが唯一の分岐である。**

### 3-2. 保存内容（★3項目）
```
ds/ds/phase0.py:124-126   "origin": origin,  "relayed_by": relayed_by,  "authored_by": authored_by,
```
**∴ utterance レコードの3項目が、`MACHINE_SUBMIT / None / None` から `HUMAN_RELAYED / Claude / Taka` に変わる。**

### 3-3. ★私の予想は外れた（記録する）
**私は Build 22 の SPEC 指示で「`origin` だけ渡した場合は★素通りすると予想する」と書いた。**
> **★外れ。** **`ValueError` を投げる。** **DS は併記を強制している。**
> **∴ 「docstring は求めているが強制する記述を見ていない」と私は書いた。** **見ていないだけだった。** **§3-7 の「探した範囲を書く」を、私はまた守れていなかった。**

---

## 4. ★変わらないもの（Taka の要求どおり全部列挙する）
| # | 何が | 変わるか | 根拠 |
|---|---|---|---|
| 1 | **RRI の分類（4軸7カテゴリ / `request_type`）** | **★変わらない** | `rri/rri` に utterance の `origin` を読む行が無い |
| 2 | **RRI の次アクション決定** | **★変わらない** | 同上 |
| 3 | **EGL への書き込み内容** | **★変わらない** | `egl/egl` に `origin` を読む行が無い |
| 4 | **EGL からの読み出し** | **★変わらない** | 同上 |
| 5 | **DW の起動・状態遷移** | **★変わらない** | `dev-workcell/dw` に `origin` を読む行が無い |
| 6 | **勘定科目・Sub-ID の付与** | **★変わらない** | `origin` を参照する付与処理が無い |
| 7 | **task_id の決まり方** | **★変わらない** | 依頼文の sha1 のみ |
| 8 | **run-gate / 承認 / token** | **★変わらない** | `origin` を見ない |
| 9 | **`/api/resolve` の返り値** | **記録がそのまま返るので3項目は見える。★判定は変わらない** | `resolve_view` は record をそのまま返す |
| 10 | **既存の呼び出し全部** | **★変わらない**（既定 `MACHINE_SUBMIT` のまま） | Build 22 の設計 |

### 4-1. ★誤認しやすい点（先に潰す）
**`twoder/experiment_candidate.py` に `origin` が出るが、★別物である。**
```
"origin": {"ds_event_id": …, "ds_thread_id": …, …}     ← 出所 ID の辞書であって、発話の origin ではない
```
**`twoder/probe/conformance_probe.py` の `origin` は、モジュール名の文字列である。** **これも別物。**
**∴ 「`origin` を読んでいるコードが在る」と読み違えないこと。**

---

## 5. ★結論（Taka への答えの核）
> **`HUMAN_RELAYED` にしても、★系の判断は1つも変わらない。**
> **変わるのは「記録が正直になる」ことだけである。**

**∴ Build 22 は「動きを変える修理」ではなく、★「嘘をつくのをやめる修理」である。**
**∴ 私が「Taka の命令の前提条件である」と裁定したのは、★必要条件としては正しいが、十分条件ではない。**
**★区別して書かなかったのは私の不足である。** **「前提が整う」と「命令が実行できる」は違う。**

**∴ 本当に必要なのは、この後である:**
| # | 何が要るか |
|---|---|
| **A** | **誰が書いた依頼かによって、系の扱いが変わる必要が在るのか。** **★これは Taka に聞く**（我々が決めることではない） |
| **B** | **在るとすれば、どこで分岐すべきか**（RRI の分類か、優先度か、承認の要否か） |
| **C** | **A が「不要」なら、Build 22 は記録の正直さのためだけに行う。** **★それでも価値は在るが、価値の説明を変える** |

## 6. 依頼（Build 22 の SPEC に反映してください）
1. **★SPEC の位置づけを直す**: 「命令の前提条件」ではなく **「記録を正直にする。系の判断は変わらない」** と書くこと。
2. **★受入に1項目足す**: **`HUMAN_RELAYED` で投入した記録に対し、RRI の分類結果が `MACHINE_SUBMIT` の場合と★同一であることを実測して示す。** **「変わらない」を主張ではなく実測で出す。**
3. **★私の予想の外れ（(c) は `ValueError`）を SPEC に反映すること。** **受入の (c) は「拒否されることを確かめる」に変える。**

---
*MGR。★Taka の問い（逐語）=`origin=HUMAN_RELAYED` になった結果、どの分岐/判断/保存内容が変わったか。変わらなかったものも全部列挙せよ。★まず事実=Build 22 は未実装なので現時点で変わったものはゼロであり、以下は「作った場合」の調査（`origin` を読む側の探索であって動かした結果ではない）。★変わるもの3つ=①分岐は1つだけ（`ds/phase0.py:114` が `HUMAN_RELAYED` のとき `relayed_by` と `authored_by` の併記を強制し、無ければ `ValueError` で記録を拒否する）②保存3項目（`origin`/`relayed_by`/`authored_by` が `MACHINE_SUBMIT/None/None` から `HUMAN_RELAYED/Claude/Taka` へ）③★私の予想は外れた——SPEC で「`origin` だけ渡すと素通りすると予想する」と書いたが実際は `ValueError` を投げる。「強制する記述を見ていない」と書いたが見ていないだけで、§3-7 の「探した範囲を書く」をまた守れていなかった。★変わらないもの（全列挙）=RRI の分類（4軸7カテゴリ/`request_type`）／RRI の次アクション決定／EGL の書き込み内容／EGL の読み出し／DW の起動と状態遷移／勘定科目と Sub-ID の付与／`task_id` の決まり方／run-gate・承認・token／`/api/resolve` の判定／既存の呼び出し全部。根拠は `rri/rri` `egl/egl` `dev-workcell/dw` に utterance の `origin` を読む行が無いこと（探索範囲を明記）。誤認注意=`experiment_candidate.py` の `origin` は出所 ID の辞書、`conformance_probe.py` の `origin` はモジュール名文字列で、どちらも別物。★結論=`HUMAN_RELAYED` にしても系の判断は1つも変わらず、変わるのは「記録が正直になる」ことだけ ∴ Build 22 は「動きを変える修理」ではなく「嘘をつくのをやめる修理」である。私が「Taka の命令の前提条件」と裁定したのは必要条件としては正しいが十分条件ではなく、区別して書かなかったのは私の不足である。★この後に必要なのは=A 誰が書いた依頼かで系の扱いが変わる必要が在るのか（★Taka に聞く。我々が決めることではない）／B 在るとすればどこで分岐すべきか（RRI の分類か、優先度か、承認の要否か）／C 不要なら Build 22 は記録の正直さのためだけに行い、価値の説明を変える。★SPEC への反映=位置づけを「記録を正直にする。系の判断は変わらない」に直す／受入に「`HUMAN_RELAYED` の記録に対する RRI の分類結果が `MACHINE_SUBMIT` の場合と同一であることを実測」を足す／(c) は「拒否されることを確かめる」に変える。*
