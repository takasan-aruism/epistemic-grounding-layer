# 【D-163】対照試験へ切り替える — **★A/B/C を同じ Qwen に直接 聞く。★3択は取り下げ**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 09:4x / TYPE=HANDOFF
- **正典**: `TAKA_2026-07-31_INPUT_DESIGN_NOT_MODEL_RUN_THE_CONTROLLED_TEST.md`（★逐語・全文）
- **★新しい名前で置いた**（★既存文書を差し替えない）

## 1. ★取り下げるもの（★私の誤りとして）
```
★D-161 の3択（a/b/c）と ★私の推し (a) は ★取り下げる。★Taka が自分の判断を修正した。
★★私の誤り: ★私は「値を見せても効かなかった」と書いた。★これは Qwen を変数として扱った言い方である。
   ★実際には ★入力側に3つの曖昧さが在り、★それを固定しないまま結論の向きを決めていた:
     ①依頼と無関係な観測まで渡していた（メモリの質問に GPU と docker）
     ②値と取得手段が混在（★`ps` が「実行済の手段」か「実装候補」か、モデルから見て曖昧）
     ③出力契約が無い（★既知／未確認／新規実装要 を分けさせる形になっていない）
★★私は D-160 で ★H2（無関係な観測まで見せている）を自分で登記していた。★それを結論に反映しなかった。
   ★登記しただけで、★言い方を直さなかった。★これが今回の私の失敗である。
```

## 2. ★やること（★正典のまま。★増やさない）

> ### **★実際の planner 入力を固定し、★同じ Qwen に直接 聞く。**

**★問い（★Taka 逐語。★1文字も変えない）**
```
この情報だけを使って、
1. 既に確認できていること
2. 未確認のこと
3. 新規実装が本当に必要なこと
を分けてください。
```

**★条件（★Taka 指定の3つ。★増やさない）**
| | 渡すもの |
|---|---|
| **A** | **★現在のプロンプトそのまま** |
| **B** | **★メモリに関係する観測だけ** |
| **C** | **★メモリに関係する値だけ。★取得コマンドは除外** |

## 3. ★MGR が固定する条件（★測る前に決める）
```
★(1) ★A は「実際に投入された prompt」であること。★作り直さない。★`TASK-2DER-5ABBA4A1` の実物を使う
      （★再現できないなら、★同じコードで再生成し「再生成した」と書く）
★(2) ★B・C は ★A から★引くだけで作る。★足さない・言い換えない
      （★選別と除外だけ。★語順や語彙を変えない）
★(3) ★問いは3条件とも★同一。★条件ごとに問いを変えない
★(4) ★各条件 ★3回ずつ（★計9回）。★1回で断定しない（運用方針 §4-13）
      ★同じ条件の中で結果が割れたら、★それ自体が結果である
★(5) ★model / temperature / seed / max_tokens を★3条件で同一にし、★全部 記録する
★(6) ★出力は★全文 保存する（★要約しない・★捨てない）
★(7) ★GPU を使う ∴ ★使うと書く（★黙って使わない）
```

## 4. ★この試験の位置づけ（★はっきりさせる）
```
★これは ★2DER の外での測定である。★production の経路を通らない。
★∴ ★投入しない。★run_next を押さない。★台帳に書かない。★commit するのは記録だけ。
★∴ ★2DER の担当工程には ★1件も数えない（★1/8 は動かない）
★★「モデルは使えた／使えなかった」を測るのではない。
   ★「★どの入力なら、既知と不足を分けられるか」を測る。
```

## 5. ★判定（★測る前に決める。★測ってから動かさない）
| 条件 | ★何をもって「分けられた」とするか |
|---|---|
| **共通** | **★出力が ①既知 ②未確認 ③新規実装要 の★3区分になっている** |
| **共通** | **★①に、★実際に観測された値が入っている**（★一般語ではなく `qwen36_vllm` / `703029` / `30520` 等） |
| **共通** | **★③が空か、★①に無いものだけになっている**（★既に取れているものを「新規実装要」に入れない） |
**★1つでも欠けたら「分けられた」と書かない。**

## 6. ★言い方（★先に固定する）
```
★A で分けられなければ  → 「★現在の入力では分けられない」
★B か C で分けられたら → 「★入力を◯◯にすれば分けられる」（★どちらかを名指しする）
★どれでも分けられなければ → 「★入力側の3条件では分けられない」
★★どの場合も「Qwen が使えない」とは書かない（★今回の3条件は入力の話である）
```

## 7. ★保留のまま
```
★Ledger ／ ★(c) の SPEC と patch ／ ★図の修正 ／ ★GENERATE
★★どれも触らない。★捨てない。
```

## 8. 手番
```
★設計: SPEC を1本（★A/B/C の作り方と保存先。★問いは正典の逐語）→ IMPL
★IMPL: A を固定 → B・C を引き算で作る → 各3回 → 全文保存 → BUILT
★私  : 確定 → Taka へ報告
```

---
**決めたこと**: **①D-161 の3択と私の推し (a) を取り下げる。私は「値を見せても効かなかった」と Qwen を変数にした言い方をしたが、入力側の3つの曖昧さを固定していなかった——H2 を自分で登記しながら結論に反映しなかったのが私の失敗である ②実際の planner 入力を固定し、同じ Qwen に Taka の問いをそのまま聞く（A/B/C の3条件・各3回・設定同一・全文保存） ③これは 2DER の外での測定であり、投入も台帳書き込みもせず、担当工程には数えない ④「分けられた」の判定と言い方を、測る前に固定した。**
