# 【調査】★`no_receive` 10本 ―― ★★**種類は 4** ／ ★★★本物の 計装は **1本だけ**

宛: MGR ／ 写: IMPL ／ 発: DESIGN ／ 2026-08-17 01:0x ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **Taka 逐語**（★『まず 10本を ★受け手の 実体・既存記録・識別方法で 分類し、★『本当に 必要な 計装の 種類数』を 出すこと。★特に 同一受け手関数に 集約される 6本を ★6回 個別実装する 前提を 置かない』）

---

## 0. ★★答え（★先に・★数だけ）

```
★★★種類 = ★★4（★★10 では ない）
★★★実装の 回数 = ★★4（★★10 でも 6 でもない）
★★★『新しい 記録の 行を 作る』必要が 有る 物 = ★★★1本だけ（`S05`）
   ―― ★残り 9本は ★★★既に 記録の 行が 在る ＝ ★★欄を 1つ 足すだけ
```

## 1. ★★分類（★★受け手の 実体 ／ 既存記録 ／ 識別方法 の 3つで）

| ★種類 | ★本数 | ★区間 | ★受け手の 実体 | ★既存記録 | ★★識別方法 | ★実装 |
|---|---|---|---|---|---|---|
| **A** | 2 | `S11` `S15` | `webui.py:1383` ／ `live_worker_runtime.py:186` | ★★在る（`emit` 1つ） | ★固定の 文字列 | ★2回 |
| **B** | **6** | `S09` `S12` `S13` `S16` `S17` `S18` | ★★★全部 `dw/workcell.py:72 _append_event` | ★★在る（`:97` `emit("DW","_append_event",{"phase":phase,"role":role},…)`） | ★★★**`phase` 引数**（★★既に 記録に 載っている） | ★★**1回** |
| **C** | 1 | `S02` | `ds/ds/phase0.py:61` | ★在る（★但し `inputs` が `None`） | ★★受け手の 名前が **動的**（`record_type`） | ★1回＋判断 |
| **D** | 1 | `S05` | `rri/rri/preflight_gate.py::detect` | ★★★**無い**（`emit` 0） | ― | ★1回（★★道具は 当たらない） |

## 2. ★★★B が 要点 ―― ★6回では なく **1回**（★Taka の 名指しへの 答え）

```
★★実物 = ★`_append_event(task_id, ★phase, role, identity, payload, ts, run_id=None)`
★★★既存の 記録は ★既に `phase` を 載せている（★`:97`）
★★経路表の 6本の `phase` 欄 = ★★`CREATE` / `PLAN` / `GENERATE` / `AUDIT` / `DISPOSE` / `COMPLETE`
   ―― ★★★6本 とも 別の 値 ＝ ★★送り手が 一意に 決まる
★★★∴ ★受け手は ★★『★`phase` から `received_from` を 決める』を ★1回 書けば ★6本 賄える
   ―― ★★6回 個別に 実装する 前提は ★★★置かない（★Taka 逐語どおり）
★★★但し ★★今の 道具では できない ―― ★`add_locator` が 書くのは ★★固定の 文字列
   ―― ★★★これが ★『新しい 計装の 種類』の 正体 ＝ ★★『引数から 決まる 値を 書く』1種
```

## 3. ★★C と D は ★計装の 前に **判断** が 要る

```
★★C（`S02`）= ★受け手の 名前が ★★動的（`record.get("record_type") or "DS_WRITE"`）
   ―― ★★経路表は `DS.UTTERANCE` を 期待 ＝ ★★★実装が その名前を 出す 保証が 無い
   ―― ★★∴ ★★計装より 先に ★『どちらを 正とするか』（★`S03` `S04` と 同族の 判断）
   ―― ★★★これは ★計装では ない ＝ ★開いていない 側に 当たらない 見込み（★★裁定は Taka）

★★D（`S05`）= ★`detect` は ★★`def` が 複数行 ／ ★記録の 行が ★0
   ―― ★★★本物の 計装（★行を 作る）＋ ★★道具が 当たらない 形（★既知の 例外①）
   ―― ★★∴ ★10本の うち ★★★ここだけが ★『Taka が 閉じた 計装』に 真正面から 当たる
```

## 4. ★★★数え方を 変えた 効き目（★★Taka の 見立てが 正しかった）

```
★★素朴に 数えると = ★10本 × 1回 = ★★10回
★★受け手で まとめると = ★★5つの 受け手（★但し ★6本を 6回 やる 前提が 残る）
★★★識別方法で まとめると = ★★★4種類 ／ ★4回
   ―― ★★内訳 = ★A 2回（★固定） ／ ★★B 1回（★引数から） ／ ★C 1回（★＋判断） ／ ★D 1回（★行を 作る）
★★★∴ ★Taka の 逐語（★『6回 個別実装する 前提を 置かない』）は ★★実物で 正しい
   ―― ★★★根拠 = ★★区別の 材料（`phase`）が ★★既に 記録に 載っている（★足す 必要が 無い）
```

## 5. ★★開けるか どうか（★★★私は 決めない）

```
★★A（2本）= ★★既存の 記録に 欄を 1つ ＝ ★★★今日 通った `add_locator` と 同じ 形
   ―― ★★『新しい 計装』に 当たるか は ★★Taka の 線の 読み方 次第 ＝ ★★★私は 開けない
★★B（6本）= ★★同じく 欄を 1つ ／ ★★但し ★★値が 引数から 決まる ＝ ★★道具の 形が 1つ 増える
   ―― ★★★『工具の 一般化は しない』に 触れる 可能性 ＝ ★★★上げる（★私の 一存で 進めない）
★★C（1本）= ★★判断が 先（★計装では ない 見込み）
★★D（1本）= ★★★本物の 計装 ＝ ★閉じている
```

## 6. ★★やらないこと

```
★★★10本を 10回 数えない（★★4種類 4回）
★★★6本を 6回 実装する 前提を 置かない（★Taka 逐語）
★★★私が 開けない（★A も B も ★上げる ／ ★D は 閉じている）
★★★『4回で 済む』と 書かない ―― ★★正しくは ★『★種類は 4 ／ ★実装の 回数は ★4 と 数えた ／ ★開けるかは 別』
```
