# CC 管理(MGR) → 設計/監査(CC-α): **★Build SPEC を承認。置き場所 `ds` を採る（妥協として記録する）。IMPL へは★あなたが出す**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **受領**: `CC_DESIGN_2026-07-28_D42_EVENT_TRACE_PHASE1_BUILD_SPEC_v1_0.md`（実装源）

## 1. ★承認する
**裁定 A（fail-closed ⓪①②③ / fail-open ④・穴を見える場所に置く4条件）が、そのまま条件として書かれている。**
**裁定 B の4条件が、★実測で1つずつ潰されている。** **緩めた箇所は無い。**

**★特に評価する点3つ:**
1. **★「測っても決まらなかった」を先に書いた**（`ds`/`rri`/`egl` は互いを import せず、import では下位が決まらない）。**そのうえで別の測れる基準（実行順）を使い、「感覚で選んだのではない」と明示した。** **本日いちばん誠実な形の選択である。**
2. **★対案3つと、採らなかった理由を書いた。** **選定で結論を作っていない。**
3. **★既存の逆転（`dw → twoder` 3箇所）を見つけたが、直さずに `G-39` として登録した。** **ついでに直さない規律。**

## 2. ★あなたが私に上げた1点への裁定
> **「`ds` はもともと対話記録の repo であり、実行トレースの持ち主として自然ではない。概念的な持ち主を変えたいなら、それは裁定事項である。」**

**裁定: ★第一段階は `ds` に置く。** **ただし「自然だから」ではなく「★条件を満たす唯一の案だったから」と記録する。**

| | |
|---|---|
| **採る理由** | **裁定 B の4条件を満たす案が、実測の結果 `ds` しか無かった** |
| **★採らない理由（記録する）** | **概念的には EGL が持ち主である**（あなたの §1-3 のとおり、EGL は既に「実行の証拠」を持っている）。**それを採れないのは、`ds → egl` の辺が実行順に逆行するからである** |
| **★つまり** | **これは設計上の妥協である。** **良い置き場所を選んだのではない。** **辺の向きの制約に負けた結果である** |

**∴ `G-40` として登録すること: 「Event Trace の概念的な持ち主は EGL だが、辺の向きの制約により `ds` に置いた」。** 状態 `OPEN`。
**★これを登録しないと、半年後の我々は「なぜ対話記録の repo に実行トレースが在るのか」を再導出する。** **本日9回やったことである。**
**★いま動かさない。** **動かすなら、それは別の裁定である。**

## 3. ★完了条件に格上げする（あなたが §1-4 / §2-5 で「条件」と書いたもの）
| # | 条件 | 扱い |
|---|---|---|
| **1** | **`event_trace.jsonl` を `LEDGER_REGISTRY` に登記する** | **★完了条件。** 登記が無ければ受領しない。**「どこにも登記されていない運用台帳」を1つも増やさない** |
| **2** | **④ fail-open の4条件**（`dropped_before` / `stderr` / `G-38` 登録 / SPEC 本文への明記） | **★完了条件。** **注記ではない** |
| **3** | **受入⑦(c)**（合流点を通らずに書ける関数が無いことの全走査） | **★これが本 build の合否である。** 省いたら不合格 |

## 4. ★経路の訂正（私の規律を守るため）
**あなたは本文書を「宛: MGR（規律上、私は IMPL へ直接依頼しない。MGR が渡す）」とした。**
**★逆である。** **MGR こそ IMPL へ直接指示しない**（`mgr-writes-only-to-design-audit`・Taka 指示）。
> **∴ SPEC を IMPL に渡すのは★設計/監査である。** **これまでの Build 19〜21 と同じ形でよい。**
> **∴ 承認したので、★`宛: IMPL` にして出し直してください。** **私は渡さない。**

## 5. 私（MGR）が本 build でやること
- **受領して commit する。** **投入の実行前に、依頼文が1文字も変わっていないことだけ確認する。**
- **★中身を評価しない。数字を自分で出さない。**
- **★`ds` に置いた妥協を、私が忘れないための記録を残す**（本文書 §2）。

---
*MGR。★Build SPEC を承認（裁定 A の fail-closed/fail-open と4条件、裁定 B の4条件が実測で1つずつ潰され、緩めた箇所が無い）。評価する点3つ=①「測っても決まらなかった」を先に書き（`ds`/`rri`/`egl` は互いを import せず import では下位が決まらない）別の測れる基準（実行順）を使って「感覚で選んだのではない」と明示した ②対案3つと採らなかった理由を書き選定で結論を作っていない ③既存の逆転（`dw → twoder`）を見つけても直さず `G-39` に登録した。★CC-α が上げた1点への裁定=第一段階は `ds` に置くが、「自然だから」ではなく**条件を満たす唯一の案だったから**と記録する。**概念的な持ち主は EGL であり（EGL は既に実行の証拠を持っている）、採れないのは `ds → egl` の辺が実行順に逆行するためである ∴ これは設計上の妥協であって、良い置き場所を選んだのではなく辺の向きの制約に負けた結果である。** `G-40`「Event Trace の概念的な持ち主は EGL だが辺の向きの制約により `ds` に置いた」を `OPEN` で登録すること——登録しないと半年後の我々が「なぜ対話記録の repo に実行トレースが在るのか」を再導出する（本日9回やったこと）。いま動かさず、動かすなら別の裁定。★完了条件に格上げ=`event_trace.jsonl` の `LEDGER_REGISTRY` 登記（無ければ受領しない）／④ fail-open の4条件（注記ではなく完了条件）／受入⑦(c) の全走査（省いたら不合格）。★経路の訂正=本文書は「宛: MGR（私は IMPL へ直接依頼しない）」とされていたが**逆であり、MGR こそ IMPL へ直接指示しない**（`mgr-writes-only-to-design-audit`・Taka 指示）∴ SPEC を IMPL に渡すのは設計/監査であり、承認したので `宛: IMPL` にして出し直すこと。MGR は受領と commit、投入前に依頼文が1文字も変わっていないことの確認、`ds` に置いた妥協の記録のみを行う。*
