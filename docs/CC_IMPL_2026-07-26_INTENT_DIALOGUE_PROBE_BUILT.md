# 実装(IMPL) → 監査/設計(AUDIT/DESIGN): 意図調べ 対話プローブ（会話で詰める）BUILT

- 宛: AUDIT（→ DESIGN → MGR/Taka）
- 発: 実装(IMPL) / 2026-07-26 / model=Qwen3.6-35B-A3B(:8005)
- 対応: `..._INTENT_DIALOGUE_PROBE_HANDOFF`（Taka 方針転換: 会話で詰める・1問10点・実行はコーダー）
- **点は理解の副産物。能力主張にしない。しかも私の自動採点計器は弱い（下記 §自己flag）。**

## 成果物（working tree・未commit）
- `structure/s_intent_dialogue_probe.py` + `INTENT_DIALOGUE_PROBE.jsonl`（会話15 + 手がかり実験75 + 採点15・pass 分離）
- meta fold（:8005 CALL_SITE 登録）

## 設計（handoff 準拠）
- **採点者を実行者から分離**: パス1=会話し transcript 凍結（採点しない）/ パス2=凍結 transcript のみから決定論ヒューリスティック採点。**`--check` GREEN=transcript から採点・合計を決定論再計算して記録と一致**（LLM 非再実行）。
- **固定4ターン**（T1中立/T2言換+順序入替/T3揺さぶり/T4自己説明）。**偽/真異議を fixture id の hash で決定論半々**（記録・再現）。
- **leading フラグ機械判定**（期待語/断定型）→ **leading 無効化=0**（私の発話テンプレは誘導語を含まない・確認済）。

## ★ 手がかり分離実験（§2・本 handoff の最重要・ここが成果）
同一の上流判断を P0素/P1記号/P2お手本/P3JSON/P4全部 × 3seed で聞き、答えが変わるか:
| 問い | P0素 | P1記号 | P2お手本 | P3JSON | P4全部 |
|---|---|---|---|---|---|
| Q_BV 白樺の価値 | 複数◎ | 複数◎ | (抽出不能) | 複数◎ | 複数◎ |
| Q_CR プーチン+文脈 | 文脈で絞る◎ | 文脈◎ | **一般論(反転)** | 文脈◎ | 文脈◎ |
| Q_IP あれどこ | (抽出不能) | 確認要 | (抽出不能) | 確認要 | 確認要 |
| Q_CH DB採用 | (抽出不能) | 別対応要 | (抽出不能) | 別対応要 | 別対応要 |
| Q_D 1024^? | 1つ◎ | 1つ◎ | (抽出不能) | 1つ◎ | 1つ◎ |

**発見（Taka の問いへの直接の答え）**:
1. **お手本(P2) は誤誘導しうる**: Q_CR で held-out 例文（沸点→単一/理想像→複数）が文脈判断を「一般論(誤)」へ反転させた。**few-shot は常に善ではない。**
2. **素(P0)は概ね妥当**（抽出できた所は正答寄り）。P0 の "抽出不能" は Qwen の失敗でなく**素の自由文が決定論抽出しにくい**という計器側の限界。
3. **★決定的**: バッチ(arm-C3)で誤答した Q_BV「白樺」を、**直接聞くと P4(バッチ形式)でも正答**。＝**失点の犯人は prompt 形式でなく "分解+短絡ツリー"**（再監査 §2「弱点は上流二択とツリー順序」を独立に裏付け）。0.54 の一部は聞き方でなく**集計構造**が捨てていた。

## ★ 自己 flag: 私の自動採点計器は弱い（99/150 を信じない）
採点内訳（15会話）:
- **A 問題特定: 全15が満点3.00＝非弁別**（keyword ヒューリスティックが緩すぎ・誰でも満点＝何も測っていない）。
- **C 捏造なし: 全15が2.00＝非弁別**（捏造検出語リストが漏れている可能性）。
- **B 理由一貫: 平均0.40**＝低いが、P0素の pref 抽出失敗(none)で比較不能→0 になる**抽出交絡**。
- **D 揺さぶり: 0/2 で分岐**＝唯一弁別している。
- ∴ **合計 99/150 は A/C の自動満点で嵩上げ・信頼できない数字**。arm-C2 の教訓を自分の計器に適用し、**数字を成果と主張しない**。真の成果は §2 の手がかり発見 + **DESIGN の独立再採点用の凍結 transcript**（§1 の抜き取り再採点で私の計器の甘さが出るはず）。

## 検証（受入・全 gate GREEN）
- `--check` GREEN: 凍結 transcript から採点・合計・偽/真異議割当を決定論再計算→記録と一致（LLM 非再実行）。ルーブリック版記録。
- pass 分離の証跡（conversation / score / cue を別 record_type）。:8005 CALL_SITE 登録（meta fold）。全 gate GREEN。
- **arm-C3(0.5397) と本テストの点は別の物差し**（並べて優劣を語らない・handoff §79）。

## ハンドオフ
- 次: **DESIGN の独立再採点**（§1 抜き取り。私の A/C 非弁別・B 抽出交絡を確認し、ルーブリック採点式を締める材料に）→ 手がかり発見（P2誤誘導/形式より集計が犯人）を MGR/Taka へ。
- 採点計器の是正提案（IMPL 入力）: A/C は keyword でなく**別 LLM judge or DESIGN 人手**が要る（決定論ヒューリスティックの限界）。B は pref 抽出を安定化（素の答えにも「前者/後者」を1語添えさせる等）。
- DE は front door `record_de` + CLAUDE_CODE 開示。**「点は弱計器・手がかり分離が本体」を DE 本文に明記**。commit 時 script 同梱。

---
*実装(IMPL)。会話で詰める・採点は実行と分離・自分の採点計器の弱さも measure-first で flag・手がかり分離が本体。★3 本線・止めない。*
