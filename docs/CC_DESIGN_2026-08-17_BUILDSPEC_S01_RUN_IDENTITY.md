# 【BUILD SPEC】★S01 の **走行の 番号を 揃える** ―― ★★最小変更（★新部品 0）

宛: MGR ／ 写: IMPL ／ 発: DESIGN ／ 2026-08-17 00:2x ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **Taka 逐語**（★『(イ)だけ 開ける。★S01の split_run 1本のみ 対象。★既存の 両側記録・位置情報は 触らず、★受け渡し時の run identity を 一致させる 最小変更に 限定する。★完了後に 再計測し、そこで 閉じる』）

---

## 0. ★★なぜ 揃っていないか（★★実測・★推測では ない）

```
★★送り側 = ★`twoder/webui.py:1293` ★`_HO01("S01", to_key="SUBMIT.received")`
   ―― ★★`handoff_emit.handoff(segment_id, to_key=None)` は ★★`run_id` を 受け取る 引数が 無い
   ―― ★∴ ★`current_run_id()`（★スレッドの 値）を 使う
★★受け側 = ★`twoder/submit.py:172` ★★`_run_id = ETRACE.open_run(raw_input, ts, entry="submit()")`
   ―― ★★★走行は ★ここで 生まれる（★`open_run` が uuid を 混ぜて 新しい 番号を 発行）
   ―― ★177行 は ★その番号で 記録する（`run_id=_run_id`）
★★★∴ ★送りの 1行は ★★走行が 生まれる **前** に 出ている ＝ ★★番号が 揃いようが ない
   ―― ★★これは 順番の 問題であって ★★★誰かの 書き忘れでは ない
```

## 1. ★★手は 2つ（★★私は 1つを 推すが ★決めない）

**★★どちらも ★新部品 0 ／ ★新台帳 0 ／ ★口 0増。**

```
★★★(あ) ★渡した 直後に 出す（★★推す）
   ―― ★`SUB.submit(...)` の 戻り `tr["ETRACE_RUN_ID"]` を 取る
   ―― ★`ds.etrace.set_run_id(<その番号>)` を 呼ぶ（★★★既に 在る 関数 ／ ★逐語＝
      『別プロセス/別スレッドで続きを記録するとき用。新しい run を始めるのではない』）
   ―― ★その後に `_HO01("S01", to_key="SUBMIT.received")`
   ―― ★★変更 = ★★`webui.py` の ★2行（★1行 移動 ＋ ★1行 追加）／ ★他の file 0

★★(い) ★入口で 走行を 開く
   ―― ★`webui` が 先に `open_run` を 呼び、★`submit()` は それを 引き継ぐ
   ―― ★★変更 = ★`webui.py` ＋ ★`submit.py`（★★★『走行を 開くのは 合流点⓪』という 決めに 触る）
```

## 2. ★★★(あ) の 代償を 先に 書く（★★隠さない）

```
★★★①『★渡す 直前に 1行』が ★『★渡した 直後に 1行』に なる
   ―― ★★これは ★この系が 明文で 持っている 言い方（★`handoff_emit` の docstring 逐語）
   ―― ★★★∴ ★★言い方も 一緒に 直す 必要が 在る（★★黙って 変えない）
   ―― ★★但し ★意味は 変わらない（★渡した 事実は 同じ ／ ★★記録の 位置が 後ろに ずれるだけ）
★★★②`set_run_id` は ★★スレッドの 値を 書き換える
   ―― ★★★その後の 同じ スレッドの 記録が ★その 番号に 乗る
   ―― ★★∴ ★★確かめる 事 = ★★『S01 の 後に ★同じ 要求の 中で 出る 記録が 在るか』
      ―― ★在るなら ★その 記録の 番号が 変わる ＝ ★★★他の 語が 動く（★★出してはいけない 結果）
   ―― ★★★私は これを 引いていない ∴ ★★『問題ない』とは 書かない（★当てる 前に 1回 引く）
```

## 3. ★★受入（★★数で ／ ★★出してはいけない 結果と 対）

```
★★① `split_run` ★1 → ★★0
★★② `linked` ★7 → ★★8（★★★但し ★予想を 固定しない＝★★別の 語に なったら ★その語が 答え）
★★③ 6語の 合計 ＝ ★★18（★数が どこにも 消えない）
★★④ `no_locator` ★0 の まま ／ `no_receive` ★10 の まま（★★★開いていない 側が 動かない）

★★★出してはいけない 結果
   ★(ア)★★S01 以外の 語が 動く（★★★`set_run_id` の 巻き添え）
   ★(イ)★走行が 二重に 開く（★`open_run` が 2回）
   ★(ウ)★既存の 両側記録・位置情報が 変わる（★★Taka 逐語＝★触らない）
   ★(エ)★`identity` が ★★8 より 減る
```

## 4. ★★当てる 前に 1回 引く（★★私の 本日の 反省を 手順に）

```
★★★`set_run_id` を 入れる 前に = ★★『★同じ 要求の 中で ★S01 の 後に 出る 記録』を ★数える
   ―― ★★0件なら ★巻き添えは 起きない ＝ ★★(あ)を そのまま
   ―― ★★1件以上なら ★★その 記録の 番号が 変わる ＝ ★★★(あ)の 前に ★戻す 1行が 要る
      （★★元の 番号を 控えて ★S01 の 後に 戻す）
★★★これを 飛ばすと = ★★『直したのに 別の 語が 動いた』が 出る（★★本日 私が 11回 踏んだ 形）
```

## 5. ★★やらないこと

```
★★★`no_receive` の 10本に 触らない（★★開いていない）
★★★既存の 両側記録・位置情報を 触らない（★Taka 逐語）
★★★`handoff_emit` に 引数を 増やさない（★★(あ)なら 要らない＝★★工具を 太らせない）
★★★『揃った』と 先に 書かない ―― ★★正しくは ★『★`split_run` が ★N』
```
