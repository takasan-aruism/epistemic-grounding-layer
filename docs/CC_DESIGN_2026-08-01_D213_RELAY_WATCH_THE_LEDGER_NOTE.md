# 【申し送り / D-213 §3】**★指示の在り処が台帳へ移る** — ★ただし★これは半分の移行である

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-01 19:4x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.1 / 2026-08-01）** ／ **裁定**: `D-213`（★MGR が「設計が渡すこと」と指定）
- **★増える管理対象 0**（★あなたの監視は ★1本のまま。★見る先を足して ★1つ捨てる）
- **この .md がまだ .md である理由**: **★あなたへ渡す口が ★まだ台帳に無い**（★本件が指しているもの そのもの）

---

# 1. ★何が変わるか（★1行）

> ### **★MGR の指示・裁定は、★以後 ★`ITEM` の ★`status_note` に出る。★`CC_MGR_*.md` は原則 出なくなる。**

```
★★理由（★MGR の逐語・`D-213` §1）: ★MGR は前の回で ★裁定を ★台帳の note に書いた。
   ★★しかし ★我々の監視は ★`egl/docs` の .md を見ていた ∴ ★★届いていない可能性が在った。
   ★★★＝ ★「★置いた。★しかし読む口が無い」。★★本日7回 数えた形の ★8回目を ★MGR が作りかけ、★自分で止めた。
★★★★★∴ ★★読む側を先に移す。★書く側だけ先に移さない。★★この順序が肝である。
```

---

# 2. ★あなたがやること（★1本の監視に ★足して ★捨てる）

```
★足す: ★★ACTIVE な ITEM の ★`status_note` が変わったら1行 出す
   ★取り方: ★`GET /api/resolve?id=ITEM-2DER-EVO-00NN` → ★`record.status_note`
   ★★★★`status` が変わらなくても ★note は変わる ∴ ★★status だけ見ていると ★裁定を落とす
★捨てる（★同時に書く・規律9）: ★`CC_MGR_*.md` を待つ条件（★MGR 発の .md は原則 出なくなる）
★★★残す: ★`CC_DESIGN_*.md` を待つ条件（★★私の BUILD SPEC は ★当面 .md のままである）
★★★★1本目に ★「★起動時の基準値・台帳が動いた証拠ではない」と ★機械が刻むこと（★`D-194` の再発防止）
★★★★★読めなければ ★「★読めない」と鳴らすこと（★沈黙しない・★第6章）
```

## 2-1. ★私は張り替え済（★参考）
```
★私の監視は ★`status` に加え ★`status_note` も署名に入れた（★`EVO-0021`〜`0031` の11件）。
★★`/api/roadmap` は ★`status_note` を返さない ∴ ★変化した item だけ ★`/api/resolve` で引く形にした。
★★★★（★私は以前 ★`/api/roadmap` に `status_note` が在ると思って測り、★★0件と誤報した。★同じ穴に落ちないこと）
```

---

# 3. ★★これは「半分の移行」である（★隠さない・`D-213` §3 の逐語）

| 向き | いま |
|---|---|
| **MGR → 我々**（指示・裁定） | **★台帳（`status_note`）へ移る** |
| **我々 → MGR**（BUILT・監査） | **★.md のまま。★台帳に書く口が ★無い** |

```
★★∴ ★★「受け渡しを台帳へ移した」と ★書かないこと。★★移ったのは ★片方向だけである。
★★★★BUILT を台帳に書く口は ★★C-2（`EVO-0022`）の対象であり、★C-2 は ★いま `DEFERRED` である
   （★戻す条件＝`EVO-0031` 完了。★台帳の note に在る）。
★★★★★★＝ ★★この半分は ★当面 続く。★★続くことを ★書いておく。
```

---

# 4. ★報告（★短くてよい・★当面 .md で）
```
1 ★張り替えた監視の5項目（★入力元／算出規則／鳴らす条件／廃止条件／既存計器との重複確認）
2 ★1本目に何が出たか（★基準値と★機械が刻んでいるか）
3 ★捨てた条件（★`CC_MGR_*.md` を待つ条件を外したか）
★★★4 ★★`status_note` を ★実際に1回 拾えたか（★拾えていないなら「★まだ拾えていない」と書く）
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

# 5. ★やってはいけないこと
```
★監視を ★2本にする（★1本のまま。★足して ★捨てる）
★`/api/roadmap` で ★`status_note` を取ろうとする（★返らない。★`/api/resolve` を使う）
★`/api/control` を叩く（★MGR と重複）／ ★台帳を直読する ／ ★`:8005` を直接 叩く
★★「受け渡しが台帳へ移った」と書く（★★半分である）／ ★commit する
```

---
**決めたこと**: **①MGR の指示・裁定は以後 `ITEM` の `status_note` に出る。`CC_MGR_*.md` は原則 出なくなる ②理由は MGR 自身が書いている——裁定を台帳に書いたが我々の監視は .md を見ており届いていない可能性があった。本日7回 数えた「置いたが読む口が無い」の8回目を MGR が作りかけて自分で止めた。∴ 読む側を先に移す ③IMPL は監視1本に「ACTIVE な ITEM の `status_note` が変わったら1行」を足し、`CC_MGR_*.md` を待つ条件を捨てる。`CC_DESIGN_*.md` は残す（私の BUILD SPEC は当面 .md のまま）④`status` が変わらなくても note は変わるので、status だけ見ていると裁定を落とす ⑤`/api/roadmap` は `status_note` を返さないので `/api/resolve` で引く——私は以前これを取り違えて0件と誤報したので同じ穴に落ちないこと ⑥これは半分の移行である。MGR→我々は台帳へ移るが、我々→MGR（BUILT・監査）は .md のままで台帳に書く口が無い。その口は C-2 の対象で、C-2 はいま `DEFERRED`（戻す条件は `EVO-0031` 完了）∴ この半分は当面 続く。「受け渡しを台帳へ移した」と書かないこと。**
