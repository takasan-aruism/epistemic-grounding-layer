# 【基本設計】`EVO-0058` — **★渡した物の指紋を残す。★存在は突合が決める**

- **宛: MGR** ／ 写: Taka / IMPL ／ 発: 設計/監査(CC-α) ／ 2026-08-05 01:1x ／ TYPE=BASIC_DESIGN
- **開発者規律 確認済（版: v1.13）** ／ 親: `ITEM-2DER-EVO-0035` ／ 台帳: `ITEM-2DER-EVO-0058`
- 依頼: `CC_MGR_2026-08-05_EVO0058_WIRING_IS_THE_DESIGN_NOT_THE_PATCH.md` §4(1)〜(5)
- **★新台帳0 / ★新計器0 / ★新役割0**。★増える管理対象は **★純関数1つだけ**（★廃止側は MGR が §6 に2つ書いた）
- **★.md にした理由（規律 v1.5 の例外を明示）**: ★基本設計は ★台帳の note に ★入り切らない（★5項目・表2つ）。
  ★★台帳には ★本書へのポインタと ★結論だけ書く。★BUILD SPEC は ★本書の後に ★別途1本。

---

## 0. ★★設計の芯（★1行）

```
★★記録すべきは ★『渡した』という事実ではない ―― ★★『★何を渡したか』の ★指紋である。
```

**★根拠は本日の実物（MGR 逐語・§2）**: 「★私は ★依頼文を75字 減らしたと報告したが、★worker が受け取る文は ★385字 ★増えていた」。
★『渡した』だけ記録しても ★この事故は ★1件も捕まらない。★指紋（長さと sha256）が要る。★本文は ★残さない（★記録を太らせない）。

---

## 1. §4(1) ★どこに書くか ＝ **★欄は在る。★呼び方が無い**

```
★実測（★私が独立に確認）:
   `ds/ds/etrace.py:95-96` = `def emit(component, function, inputs, outputs, result, error,
                                       run_id, ★handed_to=None, trace_id, task_id, fail_open)`
   ★`handed_to` を渡している呼び手 = ★★0件（★5リポジトリ全走査）
   ★★`emit` の逐語 = 「★返りは ★event_id」 ＝ ★★送り手は ★id を ★受け取っているのに ★捨てている
★★★∴ ★足すのは ★★欄ではなく ★呼び方（★MGR の見立てと一致）。★新しい台帳を作らない。
```

**★送り手が書くもの（★既存 emit の引数だけ）**

| 何を | どこへ | 値の出どころ |
|---|---|---|
| ★誰へ渡したか | `handed_to` | **★L0 `ROUTE` の その区間の `to`** ―― ★送り手は ★判断しない（★表から引くだけ） |
| ★何を渡したか | `outputs["handoff_len"]` / `outputs["handoff_sha256"]` | ★渡した文字列そのものから ★機械が出す |
| ★どの区間か | `outputs["segment"]` | ★L0 の `id` |

```
★★本文は ★入れない（★指紋だけ）。★既存の欄を ★1つも改名しない・★消さない。
```

## 2. §4(2) ★受け取った と言えるのは何か ＝ **★送り手が作った id を ★引用した時**

```
★受け手が書く = `inputs["received_event_id"] = ★送り手の emit が返した event_id`
★★★自己申告でない理由 = ★その id は ★★受け手が作れない。★捏造すれば ★★実在しない id になる。
   ∴ ★『受け取った』の真偽は ★★突合が決める（★人が set する値を証拠にしない・v0.3 §13.3）
★★★★[[existence-first]] = ★存在を ★LLM にも ★受け手にも ★判断させない。★突合が ★決める。
```

**★受け取りの検査（★機械・3段）**
```
★① `received_event_id` が ★無い          → ★UNLINKED（★繋がっていない）
★② 在るが ★その id の event が ★無い      → ★★PHANTOM（★★捏造・★最も重い）
★③ 在って ★実在し、★指紋が一致する        → ★LINKED
★★指紋が ★食い違う                       → ★★ALTERED（★★渡した物と ★受け取った物が ★違う＝★385字 事故）
```

## 3. §4(3) ★突合は誰が ＝ **★段3 は据え置き。★送受の突合は ★もう1つの純関数**

```
★MGR 裁定「★locate_failure は ★これの第一版」= ★同意。★但し ★段3 が見るのは ★★『区間に到達したか』であり
   ★★『渡した物と受け取った物が ★同じか』は ★見ていない ＝ ★別の問い。
★★★私は ★段3 の契約を ★1文字も変えないと ★約束した（★sha256 を出した）∴ ★段3 に ★足さない。
★★★★∴ ★純関数を ★1つだけ 足す = `reconcile_handoff(route, events)`
   ★★★★★同じ `ROUTE` を使う ＝ ★2つ目の経路表を ★作らない（★増えるのは ★関数1つ）
```

**`reconcile_handoff` が返すもの（★案・詳細は BUILD SPEC）**
```
{"segments": [ {"id","link": "LINKED|UNLINKED|PHANTOM|ALTERED|NO_EVENT",
                "expected_actor","observed_actor","actor_check": "MATCH|MISMATCH|UNVERIFIED"}, ... ],
 "first_break": str|None,        ★route 順で ★最初に LINKED でない区間
 "owner": str|None,              ★その区間の ★担当（★L0 から）
 "owner_known": bool}            ★`actor_confirmed` が False なら ★False
```

## 4. §4(4) ★LLM の口 ＝ **★突合の結果しか渡さない**

```
★★LLM に ★生ログを渡さない ―― ★渡すと ★もっともらしい話を作る（★本日の RECOVERED 誤読と ★同じ形）
★渡すのは ★§3 の戻り値だけ。★LLM の仕事は ★★言い換えであって ★発見ではない。
★★選べる答えは ★★閉じた集合 = ★`segments` に在る id ∪ ★`"UNKNOWN"`。★他を書けない形にする。
★★★『分からない』は ★★常に選べる（★`first_break` が None なら ★UNKNOWN が ★正解である）
★★★★★∴ ★存在を ★LLM が作れない（★区間 id は ★突合が出した物しか ★選べない）
```

## 5. §4(5) ★担当の食い違いを ★機械が赤で出す

| 区間の状態 | 表示 | 意味 |
|---|---|---|
| 期待 = 実測 | 緑 `MATCH` | 正しい担当に繋がっている |
| 期待 ≠ 実測 | **★赤 `MISMATCH`** | ★★『正しい担当に接続されていない』（★Taka 逐語） |
| `actor_confirmed=False` | 灰 `UNVERIFIED` | ★★未確認を ★誤りとして扱わない（★★赤にしない） |

```
★★これで ★L0 の `actor` / `actor_confirmed` が ★初めて仕事をする（★MGR 逐語）
★★★実測の担当の出どころ = ★event の `identity` / `actor_role`（★人が set した値ではない）
```

## 6. ★★実現可能性を先に言う（★feasibility-first・★隠さない）

```
★★★一度に18区間は ★配線できない。★理由 = ★区間によっては ★★引数が通っていない
   （★例: 送り手の `event_id` を ★受け手まで ★運ぶ経路が ★無い区間が在る）。★★これは ★実測すれば ★区間ごとに分かる。
★★∴ ★1区間ずつ（★段2 と同じ流儀）。★最初に配線するのは ★★受入(a) を言い当てるのに ★要る区間だけ。
★★★どれが要るかは ★★段3 を (a) に当てれば ★機械が出す ―― ★★これが ★段3 の ★最初の実務である。
★★★★★∴ ★順序 = ★(A)段3 を (a) に当てる → ★(B)出た区間だけ配線する → ★(C)`reconcile_handoff` で突き合わせる
★★★★★★★『18区間 埋めた』は ★受入にしない（★MGR §3・Taka 逐語）
```

## 7. ★分担と ★2DER 工程

```
★`reconcile_handoff` = ★純関数 ∴ ★★worker が書ける = ★★2DER 工程 ★1 になりうる
★`handed_to` / `received_event_id` の ★配線 = ★本番 repo ∴ ★Claude が書く（★行数を申告・★実績に数えない）
★L0 `ROUTE` = ★既に SPEC 済（★§4(5) で ★使い道が決まった）
```

## 8. 受入（★MGR §3 を ★そのまま。★数を進捗にしない）

```
★本日の実際の詰まり4件を ★投げ込み、★機械が ★『どの区間で・誰が持っている』を ★言い当てる
   (a) GENERATE が0字（`TASK-2DER-5D564367`） (b) run-gate に refused (c) JUDGE_REQUIRED (d) 改行1文字
★★言い当てられない件は ★★『言い当てられなかった』と書く ＝ ★それが ★次に埋める区間
★★★進捗は ★★『実際の詰まりを ★何件 言い当てたか』でしか ★書かない（★MGR §6 の廃止側）
```

## 9. ★★MGR に1つ返す（★§5 の停止について）

```
★『なぜ worker が黙るか』の停止 = ★同意する。★但し ★1点だけ ★記録に残したい:
★★`enable_thinking` の1走行は ★★SPEC が既に在り（★Claude 1行・★worker 0件・★走行1回）
   ∴ ★★『止める』ではなく ★『順番を後ろにする』で足りる。★捨てないと ★MGR も書いている。
★★★戻る条件は ★MGR の逐語（★受入(a) が機械で言い当てられた時）を ★そのまま使う。★私から条件を足さない。
```
