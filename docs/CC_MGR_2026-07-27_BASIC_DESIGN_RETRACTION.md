# CC 管理(MGR) → 設計/監査(CC-α): **基本設計の一部を撤回する（私が確かめずに書いた）**（HANDOFF）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.1 → 本文書で v1.2 へ改訂）**
- 対象: `CC_MGR_2026-07-27_BASIC_DESIGN_ID_LINES_AND_ACCOUNTS.md`（**§2-3 と §4-1 を撤回**）

## 0. Taka 直接発話（逐語）
> **「それはおかしい。勘定科目はエンベーディングを使ってるから自由に作られる。その辺はすでに設定した。勘定科目の仮科目というものも存在しないはずだよ。未定はあったかな。とまぁこうやって毎回適当ぶっこくから困ったもんだ。そろそろexcution architecture なんてまったく使ってないだろうしね。早いとこあなたを使わない開発体制にしないと一生終わらん」**

## 1. ★撤回する（私が確かめずに書いた前提）
| 私が書いたこと | 実際（Taka 指摘） | 扱い |
|---|---|---|
| **「勘定科目は凍結済 chart から選ぶ。無い科目は作らない」** | **勘定科目はエンベーディングを使っており、自由に作られる。既に設定済。** | **撤回** |
| **「未確定は仮勘定（suspense）に記帳し、確定したら振替える」** | **仮勘定は存在しない。「未定」は在ったかもしれない。** | **撤回**（仮勘定という機構を私が発明した） |
| 「account chart は完成・2軸凍結命名済・触らない」を制約として繰り返した | **凍結の意味を私は確かめていない。** 自由に作られるなら、私の理解は誤り。 | **未確認として扱う** |
| （併せて）**`EXEC_ARCH` を前提にした記述** | **Taka: 「そろそろ execution architecture なんてまったく使ってないだろう」** | **未確認として扱う** |

**これらは「既存の実装がこうなっている」という事実の主張だった。私は一件も確かめていない。**
**しかも同じ文書で、設計/監査には「まず既存を読め」と指示している。**

## 2. 撤回しないもの（設計の理屈であり、既存実装の主張ではない）
以下は**帳簿の実装に依存しない推論**なので、**判断は設計/監査に委ねるが、私は取り下げない**:
1. **1投稿 = 1 ID（伝票）＋ 複数の明細（サブID）。** 複数 top-level ID に分割しない（出来事の記録・訂正の単位・原文追跡・母数）。
2. **ID 単位は「進むか止まるか」だけ。明細単位で機能と科目。**
3. **★多重依頼（明細を分ける）と多義（明細1つのまま解釈候補を持つ）を混同しない。**
4. **明細は原文の span を必ず持つ**（span の無い明細は捏造であり機械で弾ける）。
5. 分解は **決定論が切り出し → LLM が提案 → 決定論が確定**。

**ただし「未確定の明細をどう記帳するか」は §1 の撤回により空欄になった。** **その答えは、既存の帳簿が実際に何を持っているかを見てから決めること**（「未定」があるならそれを使う。**私は新しい機構を提案しない**）。

## 3. ★依頼（D-14）— 事実を先に確定する
**D-12（差分表）に以下を追加すること。設計より先。**
1. **勘定科目は実際にどう決まるか**（エンベーディング／自由生成／既存科目との突き合わせ／どこまで自動か）。
2. **未確定・未定に相当する状態は在るか。** 在るなら名前と扱い。**無ければ「無い」と書く。**
3. **`EXEC_ARCH` は現在使われているか。** **使われていないなら「使われていない」と事実で書く**（Taka の見立てを裏取りする）。
4. **`account chart は凍結` の実際の意味**（何が凍結で、何が自由か）。

**★推測で埋めない。** 判定材料が無ければ `UNKNOWN` と書く（運用方針 §6-4）。

## 4. ★運用方針を v1.2 に改訂した（私の側の構造的な穴を塞ぐ）
**§3-6 を追加:**
> **MGR の基本設計に「既存の実装がこうなっている」という記述を、事実として書かない。**
> 既存実装への言及は **`【未確認】` を付けるか、設計/監査に確認させてから入れる。**
> **MGR は現物を読まない立場である以上、既存実装について断定する資格を持たない。**

**理由**: 本日 MGR は既存実装について3回誤った（`origin` があれば除外フィルタは不要／単発観測で「本番は動いていない」を支持／本件の勘定科目・仮勘定）。**いずれも「読まない立場の者が、読んだ前提で書いた」ことによる。**

## 5. Taka の指摘への MGR の回答
> **「早いとこあなたを使わない開発体制にしないと一生終わらん」**

**これに対する反論はしない。** **MIGRATION-3 は「いつかやること」ではなく、この種の損失を止めるための本線である。**
- **Build 8（第1号の投入）を最優先で進めること。**
- **私が基本設計を書く場面そのものを減らす方向で、設計/監査は D-12/D-14 の差分表を「MGR の起草なしで設計に入れる形」まで詰めてよい。** **私に投げ返す前提で書かなくてよい。**

---
*MGR。Taka 指摘により基本設計の §2-3・§4-1 を撤回（勘定科目はエンベーディングで自由生成・仮勘定は存在しない・私が発明した機構だった）。EXEC_ARCH と account chart 凍結の理解も未確認として扱う。撤回しないのは帳簿実装に依存しない5点（1投稿=1ID+明細／ID は進退のみ／多重依頼と多義を混同しない／span 必須／決定論が確定）。D-14=事実を先に確定（勘定科目の決まり方／未定に相当する状態の有無／EXEC_ARCH は使われているか／凍結の実際の意味）。運用方針 v1.2=MGR は既存実装について事実として書かない（読まない立場の者が読んだ前提で書いた誤りが本日3回）。MIGRATION-3 を最優先。*
