# 宛: Taka ―― **上位欠陥 C の 上申: ★連動を どこで 表すか ／ ★最小で 何を 足せば 閉じるか**

**2026-08-20 04:2x ／ ★実装 0 ／ ★SELF_DEV_TOKEN = 5/5 ／ ★実 repo 書き込み 0 ／ ★常駐 停止のまま**

---

## 0. ★判定（★Taka 裁定を そのまま 受ける）

```
★C =「成果は 作れる が 元の 停止へ 還流しない」
★同型 ★6件 ＋ ★C を 直す goal 自身の 再発 ∴ ★2DER 正規経路だけでは 解消できない 構造欠陥
★実測された 不足 = ★TASK の 局所成功 と ★上位 ITEM／元停止の 状態変化 を 結ぶ
   ★正規の 因果表現 と ★再評価経路 が ★存在しない
```

## 1. ★★連動の 4段を 既存構造の どこに 置けるか（★実測に 基づく 対応表）

| 連動の 段 | 既存で 置ける 場所 | 実測 | 判定 |
|---|---|---|---|
| ① 子 TASK の **成果** | `GENERATE/REGENERATE.payload.test_result`（`passed` / `artifact_sha256` / ★`artifact_head`（★今夜 成功時も 載るように なった）） | ★在る | **★表せる** |
| ② その **証拠** | `provenance` 8欄（`dw_task_id` `etrace_run_id` `egl_source_refs` …）／ `AUDIT.findings` ／ `DISPOSE.finding_dispositions` | ★在る | **★表せる** |
| ③ 上位 **ITEM の acceptance** | `ITEM.record.acceptance`（★本文 1本） | ★在る が ★★1本の 自由文 | **★条件に 割れていない** |
| ④ **元停止の 再評価** | `ITEM.status` / `ITEM.status_note` / `ITEM.history`（★121件） | ★在る が ★★粒度が ITEM | **★TASK 単位では 表せない** |

```
★★＝ ①② は ★既に 表せる。★③④ が ★表せない。
★★＝ ★不足は ★『TASK の 成果を 受け止める 側』に 在る（★出す 側では ない）。
```

## 2. ★★実測で 確定した 「表せない」の 中身（★2つ）

```
★★③ `acceptance` が ★1本の 自由文 ∴ ★『どの 条件が 満ちたか』を ★指せない
   実測 = `ITEM-2DER-EVO-0019` の acceptance は
   「CHECKLIST DISCHARGE (… item [10] …) — SKELETON FIXED: [i] … [ii] …」
   ＝ ★★人が 読めば [i][ii] と 番号が 在る が ★★欄では ない ∴ ★機械は 1条件も 指せない。

★★④ ★再評価の 引き金が ★無い
   実測 = ★履歴 121件 の うち ★機械が `status_note` に task id を 書いた 回数 = ★★0
        （★4回は ★すべて 私が 手で 書いた）
   ＝ ★★『子が 通った』が ★ITEM 側に ★1度も 届いていない。
   ＝ ★届ける 口が 無い ∴ ★再評価も 起きない。
```

## 3. ★★最小で 何を 足せば 閉じるか（★私の 設計判断・★裁定は Taka）

```
★★足すのは ★1つだけ ―― ★『ITEM の acceptance を ★条件の 一覧に する』こと。

★理由（★なぜ これ 1つで 閉じる 見込みか）:
  ・★①② は ★既に 在る（★成果も 証拠も 揃っている）
  ・★③ が 一覧に なれば ★『どの 条件に 効くか』を ★機械が 指せる
  ・★④ の 再評価は ★『満ちていない 条件が 0 か』を 数えるだけ に なる
    ＝ ★新しい 判断器を 作らずに ★数で 決まる
  ・★TASK → TASK の 欄は ★要らない（★ITEM が 束ねている ＝ ★実測 37件）
    ＝ ★『TASK を 直接 結ぶ 必要が 本当に あるか』の 答えは ★★現時点では 「無い」

★★増やさない もの:
  ・新台帳 ★0（★`ITEM.acceptance` は ★既存の 欄）
  ・新しい 分類器 ★0（★満/未満 の 2値 ＋ ★数える だけ）
  ・新しい 自由な 権限 ★0（★書き込みは ★既存の 台帳 経路のみ）
  ・新しい 状態語 ★0（★`DISPOSITION_VERDICTS` の 4語も 触らない）
```

**★★但し ―― ★これは ★私の 見立てで あって ★実証では ない（★正直に 書く）:**

```
★★確かめていない こと:
  ・acceptance を 一覧に する 口が ★既存の 台帳 経路に 在るか（★DS-RRI 経由で 書けるか）
  ・条件と 成果を 突き合わせる のが ★決定論で 済むか（★自由文なら LLM が 要る 恐れ）
  ・★条件の 粒度を 誰が 決めるか（★人か ／ ★2DER か）
★★∴ ★これは ★『閉じる 見込みの 最小案』であり ★『閉じると 証明した もの』では ない。
```

## 4. ★★ESDE 的な「階層」について（★実測の 事実だけ）

```
★組織の 階層 = ★ROADMAP → PHASE → ITEM → TASK（★4段・★実在）
★★問題の 階層（TASK / COMPONENT / PIPELINE / SYSTEM）= ★語彙が ★無い
   （★`kind` は ROADMAP / PHASE / ITEM の 3語のみ ／ `FINDING_CATEGORIES` 13語は ★欠陥の 種類）
★★∴ ★『問題が どの 層か』を 表すには ★別の 語彙が 要る ―― ★但し ★C を 閉じる のに
   ★それが 要るかは ★未確認（★§3 の 案は ★階層語彙 なしで 閉じる 見込み）。
★★∴ ★今回は ★階層語彙を ★足さない ことを 勧める（★増やす 物を 1つに 絞る ため）。
```

## 5. ★★Taka に 決めていただきたい 1点

```
★★『ITEM の acceptance を ★条件の 一覧に する』を 採るか。
   ・採る なら ―― ★次は ★『既存の 台帳 経路で 一覧に できるか』を ★2DER に 調べさせる
     （★私は 実装しない ／ ★条件の 中身も 決めない）
   ・採らない なら ―― ★別の 案（★TASK 間の 欄 ／ ★階層語彙 ／ ★別の 形）を ★裁定いただく
★★私は ★実装も 追加も していません。
```

## 6. ★していないこと

```
★実装 0 ／ 新しい sandbox 部品 0 ／ 新台帳 0 ／ 新分類器 0 ／ 新権限 0
★実 repo 書き込み 0（★HEAD = `24c649a` 不変）／ ★常駐 再開 0 ／ ★DISPOSE 代行 0
★`E8AAEA8C` `3361D3E1` の 滞留 2件は ★未接触
★SELF_DEV_TOKEN = ★5/5
★★『閉じる』と 断定していない ―― ★§3 は ★見立てと 明記した
```
