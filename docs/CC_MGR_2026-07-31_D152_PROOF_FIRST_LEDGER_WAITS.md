# 【D-152】順番が変わった — **★PLAN が Observation を使えることの証明が先。★Ledger は待つ**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 03:0x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-31_HOLD_THE_DIAGRAM_PROVE_PLAN_USES_OBSERVATION.md`（★逐語）

## 1. ★止める・切り替える（★いま動いているものが在るなら、そこで手を止める）
```
★Ledger（生出力への到達）: ★中断。★順番が後ろになっただけで、★取り下げではない
★★既に書いたコードが在るなら: ★commit しない・★本番へ入れない・★捨てない。★そのまま置く
★★D-151 §3 の SPEC は★保留。★D-151 §5 の登記（PLAN 証拠接続）は★今回の本題に昇格した
```

## 2. ★今回の1件（★これだけ）

> ### **★「PLAN が、直前の Observation を使って立つ」ことを★証明する。**

**★受入条件（★Taka 逐語。★1条件に1つの印。★緩めない）**
| # | |
|---|---|
| 1 | **`cites_source_ids` が空でない** |
| 2 | **対象 Observation または Ledger 記録の ID を引用する** |
| 3 | **既に観測済みの能力を、新規作成対象として重複計画しない** |
| 4 | **証拠から不足が判定された場合だけ改修 Task を作る** |

**★1〜2 は「引用したか」。★3〜4 は「引用が効いたか」。★分けて判定する。★まとめない。**
**★特に3**: **★前回の PLAN は「`ss` を実行するスクリプトを作れ」だった。★同じものが出たら、★1・2 が立っても不成立である。**

## 3. ★禁止（★正典のまま。★増やさない）
```
★観測を Task の下流へ移す設計変更   ★GENERATE を押すこと
★新しい台帳・API・ID 族の追加        ★Ledger 修正との同時実装
★別の欠陥を見つけたことを理由に止まること
★「概ね引用できた」と書くこと
```

## 4. ★実施の形（★設計が SPEC に落とす。★私は形だけ固定する）
```
★修正は1箇所（★観測の参照を planner の入力に渡す。★新しい ID 族を作らない）
★★これは「観測を Task の下流へ移す」ではない。★既に在る観測の★参照を渡すだけである。★線を越えない
★確認には★新しい依頼文が要る（★同じ文＝同じ task＝CREATE 確定済＝届かない。★D-148 で観測済み）
   → ★依頼文は★私が固定する。★設計が思いつきで書かない。★§5
★投入1回 → run_next 1回 → ★PLAN が出たらそこで止める（★GENERATE へ進まない）
★予告を先に書く。★外れたら「外れた」と書く
```

## 5. ★確認に使う依頼文（★MGR が固定する。★1文字も変えずに投入する）

```
現在の待ち受けポートの状況を確認し、確認できない項目があれば、それを取得できるようにする方法を検討してください。
```

**★この文を選んだ根拠**
```
★(1) ★観測が先に走る形になっている（「確認し」）＝ ★PLAN が引用すべき Observation が★同じ依頼の中で生まれる
★(2) ★「不足があれば」＝ ★受入条件4（証拠から不足が判定された場合だけ改修 Task を作る）を★直接 試す形である
★(3) ★GPU 固有語を含まない（★共通基盤の線を維持）
★(4) ★架空ではない（★実際、前回 使用率とプロセスの GPU 紐付けが取れていない＝★我々が知りたい形そのもの）
★(5) ★裸の指示語を含まない ／ ★1行・改行なし
★(6) ★前回の文と違う ∴ ★新しい task_id になる（★届く）
```
**★予告として先に書いておくこと（★設計が SPEC に入れる）**: **★この文が `MODIFY_EXISTING`/`BUILD_CAPABILITY` に分類されるか、★`OBSERVE_CURRENT_STATE` に落ちるか。★どちらに転んでも「外れた」と書く。**

## 6. ★報告（★正典の6項目のまま。★増やさない）
```
1. 変更した1箇所  2. 受入1〜4 の判定（★1条件に1つの印）  3. Last PASS  4. First FAIL
5. ★2DER 担当工程数の前回差分（★見込み 0。★0 なら 0 と書く）  6. 次に直す1件
```

## 7. 手番
```
★設計: SPEC を1本 → IMPL（★§2 の1件のみ・★§5 の文をそのまま入れる）
★IMPL: 調査 → 修正1箇所 → 再起動（全件記録）→ 投入1回 → run_next 1回 → BUILT
★私  : 確定 → Taka へ6項目
★Taka: ★手番なし（★図をどう直すかは、★この証明の後に決める）
```

---
**決めたこと**: **①Ledger は中断（取り下げではない・書いたコードは commit も投入もせず置く） ②今回の1件は「PLAN が直前の Observation を使って立つ」ことの証明で、受入は Taka の4条件を1つずつ判定（1〜2＝引用したか／3〜4＝引用が効いたか） ③修正は観測の参照を planner の入力に渡す1箇所のみ——これは「観測を Task の下流へ移す」ではない ④確認用の依頼文は MGR が固定した（観測が先に走り、不足があれば改修を検討する形＝受入4 を直接 試す）。**
