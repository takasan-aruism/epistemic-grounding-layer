# 宛: Taka（★裁定が要る） / 設計 / 監査 ―― **配線は契約経路では構造上できない**

**Claude はコードを書いていない。契約を依頼していない（★依頼しても無駄になると分かったため）。**

## 0. 結論

```
★★「DESIGN へ 契約作成を 依頼し 2DER に 実装させて 配線する」は ★★構造上 できない。
★理由 = 契約経路が 置ける 先は ★★『関数名から 決まる 新しい 1ファイル』だけ。
★★既存ファイル（manager_v0.py）の 一部を 書き換える 口が ★無い。
★∴ 依頼すれば ★また 呼び手 0 の 部品が 1つ 増えるだけ（★今夜 3回 起きた 型）。
```

## 1. 根拠（★実物の逐語）

**`domain_dw._place_and_commit()`（★機械が置いて commit する唯一の口）:**

```
逐語「★★置く 先は ★関数名から 決める（★`def X(` の X → ★`twoder/X.py`）
      =★★人が 名前を 決めない。」
実装:
   m = re.search(r"^def\s+([A-Za-z_][A-Za-z0-9_]*)\s*\(", source, re.M)
   name = m.group(1)
   path = os.path.join(PARTS_DIR, name + ".py")      # ★PARTS_DIR = /home/takasan/twoder
   ★twoder/ の 外を 指したら 置かない
   ★構文が 壊れていたら 置かない
   ★中身が 同じなら 置かない
```

**∴ 契約が生む成果物は必ず「1関数＝1ファイル」。**
**★`_last_task` という名前で契約を書けば `twoder/_last_task.py` という★新しい孤児ファイルが生まれるだけで、`manager_v0.py` は1文字も変わらない。**

**★同じ逐語がもう1つ在る**（`receive_finished`）:

```
「★★線（★受領と 配置を 分ける）= `twoder/` へ 置かない ／ commit しない ／ ★★配線しない。」
   （★『置く・commit』は 2026-08-17 Taka 許可で 解禁 ／ ★『配線しない』は そのまま）
```

**★配線は設計として機械の外に置かれている。★欠落ではなく、まだ解禁されていない。**

## 2. ★では誰が配線するのか（★正本の区分）

**`manager_v0.py` の module docstring 逐語:**

```
★足場と判断の境目（★試金石の数え方）
  ★判断 = twoder/manager_decide.py（★2DER が書いた・★1行も書き換えていない）
  ★足場 = ★★このファイル（★★Claude が書いた・★行数は 2DER の実績に数えない）
```

```
★今回 配線する 中身:
   判断 = ★`requeue_decision`（★2DER が 契約経路で 書いた・★置かれ済み・★私は 1行も 書いていない）
   足場 = ★`manager_v0._last_task()` の 呼び出し 1箇所（★このファイルは ★正本上 Claude の 足場）
★∴ ★判断は 既に 2DER の 物。★残るのは ★足場の 接続だけ。
★★同型の 前例 = `senior_review`（★判断は 部品）を ★`webui.py` が import して 配線している
   （★配線は 人が 書いた・★実測 参照 1件）。
```

## 3. ★選択肢（★私は決めない・★Taka 裁定）

```
A. ★足場の 接続を Claude が 書く（★1箇所・`CLAUDE_IMPL=1` / `[Claude実装]` で 分けて commit）
   ＋ 前例と 同じ形（senior_review → webui.py）
   ＋ 判断は 2DER の 物のまま（★実績の 数え方を 汚さない＝正本の 逐語どおり）
   ＋ ★今夜 私が 既に 同じ 種類の 足場変更を 2回 している（0d5a818 / c36b57b）
   − ★今回の 指示「Claude は コードを 書かない」に 触れる ∴ ★Taka の 一言が 要る

B. ★`_place_and_commit` に「既存ファイルを 書き換える」能力を 足す
   − ★★新しい 能力＝★上申条件①④（設計原則の 変更 ／ 高影響）
   − ★機械が 既存ファイルを 上書きできる＝★安全境界が 大きく 動く
   − ★★今夜の 3部品を 生んだ 経路が そのまま 危険物に なる

C. ★配線を 要らない 形にする（★部品側を 呼ばずに 済む 設計へ 変える）
   − ★★それは 今回 作った requeue_decision を 捨てる こと ＝ 手戻り
```

**★私の見立て（★決定ではない）: A。**
**理由: ★判断（部品）は既に 2DER が書き終えており、残るのは正本が「Claude の足場」と明記した1ファイルの接続1箇所。B は安全境界を動かす。**

## 4. ★これが今夜4回目の同じ型

```
「置いてある≠繋がっている」
  ①経路表の 区間が 在る ≠ 通っている
  ②機能表に 口が 在る   ≠ 収穫が 在る
  ③部品が 在り 試験に 通り commit されている ≠ 呼ばれている
  ④★★『機械が 部品を 作る』能力は 在る ≠ ★『機械が 配線する』能力は ★無い（★設計として 保留）
```

**★機械の到達点（★実測）: 作る・試す・置く・commit する ―― ★ここまでは人の手0で回る。**
**★止まっている1点: ★配線。**

## 5. していないこと

```
★コードを 書いていない ／ 契約を 依頼していない（★無駄になると 分かったため）
★queue へ 手で 追加 0 ／ run_next 0 ／ UPPER_REVIEW 代行 0
★tasks_to_enqueue / dispose_decision を 触っていない
★A/B/C を 私が 選んでいない
```
