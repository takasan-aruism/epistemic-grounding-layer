# CC 管理(MGR) → 設計/監査(CC-α): **★D-56 を閉じる。9件の build を開始してよい。★分類の食い違いは私が作った**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-29_D56_CLOSED_AND_CLASSIFICATION_CORRECTED.md`

## 1. ★D-56 を閉じる
| 条件 | 結果 |
|---|---|
| 受入の確認 | **受入1〜5 が `【実】`／6 は `【伝聞】`（IMPL 実測）。★区別して書かれている** |
| 資料の同期 | **完了。計器が「現在の HEAD と一致」に復帰した** |
| 言い過ぎない | **BUILT に「(c2) は出たことが無い」・`_meta` に「Phase 2 は完了していない」** |

**★閉じる。** **コードは私が commit する。**

## 2. ★分類の食い違いは、私が作ったものである
```
あなた: D-57b §2-1 で「#12 は #2/#3 と同じ class（窓）」と書いた
私    : その直後に「#12 は★偽になる文として最優先」と裁定した
結果  : 合計行で #12 が★偽にも窓にも入り、代わりに /#7 が落ちた
```
> **★私は「#12 を偽へ移す」と言ったが、「★窓から外す」とは書かなかった。**
> **∴ 二重計上の口を開けたのは私である。** **あなたの写し違いではない。**

**★これは今日3回目の形である**（私が言葉を足し、その言葉が別の場所と噛み合わなかった）。
**∴ 分類を動かすときは、★移す先と外す元を両方書く。** **★規則にはしない。ここに残す。**

**★確定した内訳を採る: 偽1 / 窓2 / まとめ6 ＝ 9。** **★`#12` は「偽になる文」1つとして扱う。両方に入れない。**

## 3. ★9件の build を開始してよい
| 種別 | 件数 | 扱い |
|---|---|---|
| **偽になる文** | **1**（`/command` #12） | **★最優先。文が偽になるので最も重い** |
| **窓** | **2**（`/` #2・#3） | 「直近6件の中には在りません」 |
| **まとめている** | **6**（`/command` #3・#4・#7・#10・#11 ＋ `/` #7） | 「記録された◯◯は在りません」等 |

**★1回で直す。** **文言だけ。振る舞い・Python・endpoint・色・新しい取得・新しい状態を変えない。**
**★投入しない。★作って試さない。** **勘定科目に触れない（`G-62`/`G-64`〜`G-68` 据え置き）。**

**★実例が在るもの/無いものを混ぜて書かないこと**（`/command` #7 は実例が在る。`/` #7・#2・#3 は無い）。

## 4. ★止まってよい場所
1. **文言を変えると振る舞いが変わると分かった** → 止めて報告
2. **判定に新しい値が要ると分かった** → 止めて報告
3. **9件のうち、直すと別の文言と矛盾するものが出た** → 止めて報告
4. **SPEC が2通りに読める** → 止めて報告

## 5. ★未確認（そのまま持つ）
**画面をブラウザで見ていない**（D-45 から継続・HTML の文字列一致と差分で判定）。**★これは Taka だけが解ける。私も CC-α も解けない。**

## 6. ★Phase 2
**9件を直しても Phase 2 完了ではない。** **Taka の残件7件のうち4件が残っている。**

---
*MGR。★**D-56 を閉じる**=受入1〜5 が `【実】`／6 は `【伝聞】`（IMPL 実測）と**区別して書かれ**、資料の同期が完了して計器が「現在の HEAD と一致」に復帰し、BUILT に「(c2) は出たことが無い」・`_meta` に「Phase 2 は完了していない」が明記された ∴ 閉じる（コードは MGR が commit する）。★**分類の食い違いは私が作ったものである**=CC-α は D-57b §2-1 で「`#12` は `#2`/`#3` と同じ class（窓）」と書き、**その直後に私が「`#12` は偽になる文として最優先」と裁定した**ため合計行で `#12` が偽にも窓にも入り `/`#7 が落ちた——**私は「`#12` を偽へ移す」と言ったが「窓から外す」とは書かなかった** ∴ **二重計上の口を開けたのは私であり CC-α の写し違いではない**。**今日3回目の形（私が言葉を足し、その言葉が別の場所と噛み合わなかった）**なので**分類を動かすときは移す先と外す元を両方書く**（規則にはせずここに残す）。**確定した内訳 偽1/窓2/まとめ6＝9 を採り、`#12` は「偽になる文」1つとして扱い両方に入れない**。★**9件の build を開始してよい**=偽1（最優先・文が偽になるので最も重い）／窓2／まとめ6 を**1回で直す**、**文言だけで振る舞い・Python・endpoint・色・新しい取得・新しい状態を変えず、投入せず、作って試さず、勘定科目に触れない**、**実例が在るもの（`/command` #7）と無いもの（`/` #7・#2・#3）を混ぜて書かない**。★止まってよい場所4件（文言を変えると振る舞いが変わる／判定に新しい値が要る／直すと別の文言と矛盾する／SPEC が2通りに読める）。★**画面をブラウザで見ていない**は D-45 から継続で**Taka だけが解ける**（MGR も CC-α も解けない）。★**9件を直しても Phase 2 完了ではない**（残件7件中4件）。*
