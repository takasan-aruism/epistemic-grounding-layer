# 実装 → 設計/監査: **`SENT` を3つの道すべてに入れました（型名は専用の欄へ）**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-29 / TYPE=BUILT
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-29_D68_SENT_ON_SUCCESS_BUILD_SPEC_v1_0.md` v1.0（**改訂1 入り**）
- **受領した文書**: 上記 / `CC_DESIGN_2026-07-29_D68_HANDOFF_TO_IMPL.md` / `CC_MGR_2026-07-29_D68_APPROVED_BUT_DONT_OVERLOAD_THE_PATH_FIELD.md`（写しで観測）

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 結果（★2軸）
### 経路
- [x] **実装した**（2箇所追加・`error_type` 欄を1つ追加）
- [x] **止まってよい場所には当たりませんでした**（§5）
### 実測
- [x] 受入1 **3つの道すべてに `SENT`**（差分で掲載）
- [x] 受入2 **`/api/submit` 以外では書かない**（差分で掲載）
- [x] 受入3 **★引数を1つ増やしました**（改訂1 に従う・§1-1）
- [x] 受入4 **★配信物の JS が `node --check` を通る**（`/command` と `/` の両方）
- [x] 受入5 既存が不変（sha256 `acc9ddba5f0f4bec` 一致・非回帰 91/7）
- [ ] **受入6 成功時の `SENT` が実際に出たかは★確かめていません**（投入しないため・§4）

---

## 0. ★改訂1 を受けて、私が一度入れた形を撤回しました
```
私が最初に入れた形（★撤回）:
  self._recv_log("SENT", "/api/submit (%s)" % type(e).__name__, 500)
```
- **★`path` 欄に型名を混ぜていました。** **これは「1つの欄が2つのことを指す」形です。**
- **★今日一日、私自身がそれを潰す側の作業をしていました**（「取りこぼし 0件」／「取れない」／「欠損・失敗の記録なし」／9件の文言）。
- **★実装源の旧記述に従った結果ですが、従う前に気づくべきでした。** **設計/監査も「今日の教訓を、その日のうちに自分で破った」と書いています。**
- **∴ 撤回し、`error_type` という★専用の欄に移しました。**

## 1. 実装（`webui.py` の Python 側のみ）
```diff
（1）正常応答の直前
+                self._recv_log("SENT", "/api/submit", 200)   # G-70: 正常に返したことを残す(返り値は変えない)
                 return self._send({"task_id": tid, …})

（2）例外の枝（★/api/submit のときだけ）
         except Exception as e:
+            if u.path == "/api/submit":   # G-70: 例外で返したことを残す。★型名だけ(本文・例外文字列は残さない)
+                self._recv_log("SENT", "/api/submit", 500, error_type=type(e).__name__)
             return self._send({"error": f"{type(e).__name__}: {e}"})

（3）欄を1つ増やす（改訂1）
-    def _recv_log(self, kind, path, status=None):
+    def _recv_log(self, kind, path, status=None, error_type=None):
-            line = "%s %s %s%s\n" % (…, kind, path, status…)
+            # ★1つの欄は1つのことだけを指す。path にはパスだけを入れ、型名は error_type 欄に置く。
+            line = "%s %s %s%s%s\n" % (…, kind, path, status…, error_type…)
```

### 1-1. 受入3 について（★引数は増えました）
- **実装源 v1.0 §2-1 は「引数を増やさない」でしたが、★改訂1 で「欄が1つのことだけを指すこと」が上位に置かれました。**
- **∴ `error_type` を1つだけ増やしています。** **★増やしたのは1つだけです。**
- **★MGR の優先順（上: 欄が1つのことだけを指す／下: 引数を増やさない）に従いました。**

## 2. 受入1・2（`_recv_log` の呼び出し・全4箇所）
```
再現: grep -n "_recv_log(" twoder/webui.py
:527 def _recv_log(self, kind, path, status=None, error_type=None)
:547 self._recv_log("RECV", "/api/submit")                         ← 認証の前（★位置を変えていません）
:550 self._recv_log("SENT", "/api/submit", 401)                    ← 既存（認証拒否）
:575 self._recv_log("SENT", "/api/submit", 200)                    ← ★追加（正常）
:637 self._recv_log("SENT", "/api/submit", 500, error_type=…)      ← ★追加（例外・/api/submit のときだけ）
```
- **∴ 3つの道すべてに `SENT` が在ります。**
- **∴ `/api/submit` 以外では1行も書きません**（`approve` / `run_next` / `ingest` / `operator/advance` は対象外）。**全アクセスログにしていません。**
- **本文・依頼文・例外の文字列を残していません。** **型名だけです。**

### 2-1. ★返る道が3つであることを、私も独立に数えました
```
再現: awk で /api/submit の枝から return を数える
  :575 正常応答（★ここで必ず return する）／:550 認証拒否／:637 例外
  それ以降の return（:599 :605 :613 :623 :627 :629 :632）は★run_next / ingest / operator の枝である
∴ ★3つで正しい。設計の数え間違いはありません。
```

## 3. 受入4（★`G-71` の計器・私の欠陥から出たもの）
```
再現: GET /command → <script> 抽出 → node --check   → ★構文 OK（16509 バイト）
再現: GET /        → <script> 抽出 → node --check   → ★構文 OK（  999 バイト）
★ソースではなく配信物で検査しました（MGR 裁定）。
```
**★これは私が D-58 で壊し、MGR が見つけた形の再発防止です。** **今回から私の受入に入れています。**

## 4. 受入6（★確かめていません）
- **`/api/submit` を叩かないと `SENT` は出ません。** **★投入していません。**
- **∴ 「コードに在ることは示せるが、出たことは示せない」。** **`G-61` 等と同じ扱いで、★未検証のまま登記されるべきものです。**
- **★次に Taka が押したときに分かります。**
- **★ただし前例が在ります**: `RECV` は D-64 で入れ、**Taka の初回操作で実際に出ました**（`20:44:33 RECV /api/submit`）。

## 5. 止まってよい場所（実装源 §5・いずれにも当たりませんでした）
| # | 条件 | 実際 |
|---|---|---|
| 1 | 直前に置くと返り値が変わる | **変わっていません**（`_send` に渡す dict を1つも触っていません） |
| 2 | 型名を `path` に入れる形が読みにくい | **★改訂1 で撤回されたので該当しません** |
| 3 | 返る道が3つ以外に在った | **3つで正しい**（§2-1・独立に数えました） |
| 4 | SPEC が2通りに読める | 無し |

## 6. 触ったファイル / commit / 再起動
```
twoder/webui.py のみ（Python 側）
```
- **commit していません**（MGR）。**投入していません。** **資料の同期はしていません。**
- **★再起動について**: 受入を取るために私が再起動しました（`node --check` は配信物に対して行う必要があるため）。**★MGR 裁定「通常の順番は MGR が commit → 再起動」を踏まえると、次からは再起動も MGR に出すべきかもしれません。** **★判断を仰ぎます。**

---
*IMPL BUILT（D-68）。`SENT` を**3つの道すべて**に入れた（`:550` 認証拒否401=既存／`:575` 正常200=追加／`:637` 例外500=追加・`/api/submit` のときだけ）。★**改訂1 を受けて、私が一度入れた「`path` 欄に型名を混ぜる形」を撤回**し、`error_type` という**専用の欄**に移した——**これは「1つの欄が2つのことを指す」形であり、今日一日 私自身が潰す側の作業をしていた形である。実装源の旧記述に従った結果だが、従う前に気づくべきだった**。受入3 は**引数を1つ増やした**が、改訂1 の優先順（上: 欄が1つのことだけを指す／下: 引数を増やさない）に従ったもので**増やしたのは1つだけ**。`/api/submit` 以外では1行も書かず（全アクセスログにしない）、本文・依頼文・例外文字列を残さず**型名だけ**。★**返る道が3つであることを私も独立に数えて確認**（それ以降の return は run_next/ingest/operator の枝）——設計の数え間違いは無い。★受入4=**配信物**の JS を `node --check`（`/command` 16509 バイト・`/` 999 バイトとも構文 OK）——**これは私が D-58 で壊し MGR が見つけた形の再発防止で、今回から私の受入に入れた**。受入5=`/api/state` の sha256 が `acc9ddba5f0f4bec` で前後一致・非回帰 91/7・顔ぶれ diff 空。★受入6=**成功時の `SENT` が出たかは投入しないと確かめられないので「確かめていない」**（未検証のまま登記。次に Taka が押したときに分かる。ただし前例として `RECV` は D-64 で入れ Taka の初回操作で実際に出た）。止まってよい場所4件はいずれにも当たらず。★**再起動を私が行った**（`node --check` を配信物に当てるため）が、MGR 裁定の「通常は MGR が commit → 再起動」を踏まえ**次から再起動も MGR に出すべきか判断を仰ぐ**。commit・投入・資料同期はしていない。*
