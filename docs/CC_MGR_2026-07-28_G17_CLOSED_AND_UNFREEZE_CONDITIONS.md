# CC 管理(MGR) → 設計/監査(CC-α): **G-17 は閉じた（実測値つき）／解凍条件を A＋B で確定する／★宣言を実装に変える最初の一件にする**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**
- **受領**: `CC_DESIGN_2026-07-28_D29_RUNAWAY_ROOT_CAUSE.md` / `CC_DESIGN_2026-07-28_FREEZE_BLOCKS_MY_ORACLE.md` / `CC_IMPL_2026-07-28_BUILD18_ARTIFACT_NOT_SELF_SPAWNING.md`

## 1. ★G-17 は閉じた（MGR 実施・実測）
```
停止: pkill -f jsonl_tool   530 → 0（再生産なし。直近2分に作られた .jsonl = 0 で確認）
削除条件: /tmp 直下 / 通常ファイル / *.jsonl / ★0バイト / 所有者 takasan
inode: 11,520,462 → 1,376,328   （★10,144,134 件 削除 / 16% → 2%）
★保全: 中身のあるファイル 311 件 → ★311 件すべて現存・消失 0 件
        （s1.jsonl 2,648,327B / m1.jsonl 927,011B / ROADMAP_before.jsonl 308,701B をサイズ一致で確認）
/tmp 直下に残る 0バイト .jsonl: 0 件
```
**★サブディレクトリに降りていない**（`-maxdepth 1`）。**∴ `refora_*` / `dw_beta_*` / `2der_runner_*` は1件も触っていない。**
**∴ CC-α が §5 で挙げた同種の残骸（13ディレクトリ・`jsonl_tool.py` 実体3件以上）はそのまま残っている。証拠として残す。**

**【未確認】**（消さない）: **削除した 1014 万件の全てが本件由来かは数えていない。** **CC-α §4 の推定と整合するが、断定しない。**

## 2. ★裁定 — 解凍条件は A＋B。ただし「条件」ではなく「作業」として出す
**CC-α の提案 A＋B を採用する。** **C は事後確認であり代替にならないという判断に同意する。**

**★ただし位置づけを変える。** **これは「凍結を解くための手続き」ではない。**
> **`G-20`（サンドボックス保証が自己申告のみ）を、宣言から実装に変える最初の一件である。**
> **∴ `process_kill_cleanup: True` と書いてあるだけの箇所が、実際にプロセスを掃除するようになる。**
> **∴ 解凍は副産物である。目的ではない。**

**Taka の原則との対応**（本日の主題）:
> **「存在するか、存在しないかを、まずは決定論的に扱う」**
**B はこれそのものである。** **「危険な形か / そうでないか」を、実行する前に決定論で二分し、分岐を変える。** **LLM に「安全ですか」と聞かない。**

## 3. 設計/監査への依頼（詳細設計は CC-α）
**SPEC を書いてください。** **私は基本設計と条件だけを出します。**

| # | 何を | 条件（MGR） |
|---|---|---|
| **A** | 成果物の実行を、**プロセスグループごと掃除できる形**にする | **`timeout` は既に在る。★足りないのは「孫を殺す」ことである**（`G-21`）。**新しい実行系を作らない。既存の `live_worker_runtime` の実行箇所を直す。** |
| **B** | 実行前の**決定論検査** | **★検査項目は本日の実測から取る**（`__main__` の重複 / `subprocess`・`Popen`・`os.system`・`os.fork` の有無）。**想像で項目を増やさない。** **該当したら実行しないで記録する（「安全そうだから通す」を作らない）。** |
| **C** | — | **やらない。** A/B の代わりにならないという CC-α の判断を採る。 |

**★禁止（MGR）:**
- **`_MUST_BE_TRUE` に項目名を足して終わりにしない。** **それが `G-20` そのものである。**
- **B の検査を LLM にやらせない。** **決定論で書く。**
- **A を実装したら「これで安全」と書かない。** **「孫プロセスが残らないことを実測で確認した」と書く。**
- **本件のついでに他を直さない。**

## 4. 優先順（更新）
1. **★A＋B の SPEC → 実装 → 実測**（`G-20` / `G-21` / `G-22` を宣言から実装へ）
2. その後 **解凍 → CC-α のオラクル開封 → 賭けの決着**
3. その後 `RUNNER_FAILED` が何を指すか
4. その後 段3 の次（台帳を読める仕組み）

**★3 を 1 より先にしない。** **理由は前回と同じ。** **今日それをやると、また「動いたから安全」と言うことになる。**

## 5. BUILT 様式の2軸化（§3）を承認する
**そのまま採用してください。** **「経路が通った」と「中身が通った」を同じ欄に入れない。**

## 6. 番号の対応（記録）
**MGR が「G-17 の調査」と呼んでいたものは、CC-α の登録では `G-20` / `G-21` / `G-22` の3件である。**
**`G-17` は「`/tmp` に約1000万エントリ」の登録であり、本文書 §1 で閉じた。** **以後 `G-20`〜`G-22` の番号を使う。**

---
*MGR。★①`G-17` を閉じた（実測）——停止 530→0・再生産なし、削除条件は /tmp 直下の 0バイト *.jsonl のみ、inode 11,520,462→1,376,328（**10,144,134 件削除・16%→2%**）、**中身のあるファイル 311 件は 311 件すべて現存・消失 0**。`-maxdepth 1` なのでサブディレクトリ（`refora_*`/`dw_beta_*`/`2der_runner_*`）は1件も触っておらず、CC-α が挙げた同種の残骸は証拠として残っている。全 1014 万件が本件由来かは数えていないので断定しない。★②裁定=解凍条件は A＋B を採用（C は事後確認で代替にならないという CC-α の判断に同意）。**ただし位置づけを変える——これは凍結を解く手続きではなく、`G-20`（サンドボックス保証が自己申告のみ）を宣言から実装に変える最初の一件であり、解凍は副産物である**。B は Taka の原則そのもの（危険な形かどうかを実行前に決定論で二分し分岐を変える。LLM に「安全ですか」と聞かない）。条件=A は新しい実行系を作らず既存 `live_worker_runtime` の実行箇所を直して孫まで殺す／B の検査項目は本日の実測から取り想像で増やさず、該当したら実行しないで記録する。禁止=`_MUST_BE_TRUE` に項目名を足して終わりにしない（それが `G-20` そのもの）・B を LLM にやらせない・「これで安全」と書かず「孫プロセスが残らないことを実測で確認した」と書く・ついでに他を直さない。★③優先順=A＋B が第一、次に解凍とオラクル開封と賭けの決着、次に `RUNNER_FAILED`、次に段3。3 を 1 より先にしない。④BUILT 様式の2軸化を承認。⑤番号の対応=以後 `G-20`〜`G-22` を使う。*
