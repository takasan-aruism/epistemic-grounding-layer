# 宛: 設計 / 監査 ―― General Manager v1 責務表（今夜の実測誤判断から）

## 0. 狙い（Taka 逐語）

> 「General Manager v1 の狙いは『賢い LLM Manager』を作ることではない。
> **Claude が判断する前に、判断材料・比較対象・確認順序・停止条件を機械的に揃え、Claude の自由判断を最小化すること。**
> **最初に作るのは機能ではなく責務表。**」

材料は抽象論ではなく、**2026-08-18 に MGR（私）が実際に誤った管理判断 11件**。

---

## 1. 失敗11件の分解

### F1 経路表より先に grep して母数を自作した

| 欄 | 内容 |
|---|---|
| 本来最初に見る | `twoder/route_table.py`(18区間) ／ `function_table.py` |
| 実際に先に見た | 5 repo の `.py` 611本を自前走査 |
| 生じた誤判断 | 母数を自分で定義した。経路表に Domain 区間が0件であることに最後まで気づかなかった |
| 覆した証拠 | Taka の指摘 → 経路表を引いたら `domain/material/role/work_unit` が**全部0** |
| GM が先に揃える材料 | **管理表の一覧と、各表が答えられる問いの対応**（この問いはこの表が持つ） |
| 決定論に落ちるか | **落ちる**。「問い→参照すべき表」は固定表で引ける |

### F2 `task_trace` だけを見て「起きていない」と誤判定した

| 欄 | 内容 |
|---|---|
| 本来最初に見る | `task_trace` **と** `run_trace` の**両方** |
| 実際に先に見た | `task_trace` のみ |
| 生じた誤判断 | 「届いていない 12 ／ 不明 0」。**未観測を0件として扱った** |
| 覆した証拠 | `run_trace`(75件) に S01-S06 の記録が**在った** |
| GM が先に揃える材料 | **同じ事実を持ちうる記録面の一覧**（task 面 / run 面 / 台帳面） |
| 決定論に落ちるか | **落ちる**。「1面だけで不在を結論しない」は機械の規則にできる |

### F3 `run_trace` を合わせるまで記録の存在に気づかなかった

F2 と同一の根。**別立てにせず F2 に統合する**（責務表を水増ししない）。

### F4 `determine_role` の返り schema を確認せず文字列と決め打ちした

| 欄 | 内容 |
|---|---|
| 本来最初に見る | 部品の docstring 全文（返り値の記述） |
| 実際に先に見た | docstring の**1行目だけ** |
| 生じた誤判断 | 返りを文字列と仮定 → `TypeError: find() argument must be str, not tuple` |
| 覆した証拠 | 全文に「return a tuple containing the matched keyword and its role」 |
| GM が先に揃える材料 | **呼ぶ前に、その部品の引数と返り値の形** |
| 決定論に落ちるか | **落ちる**。`ast` で署名と docstring を出すのは機械の仕事 |

### F5 `etrace` の返りを `rows` と決め打ちした ／ F11 `next_legal_operation` の欄名を間違えた

| 欄 | 内容 |
|---|---|
| 本来最初に見る | 返り値の**鍵一覧** |
| 実際に先に見た | 自分が想定した鍵で `.get()` |
| 生じた誤判断 | F5「etrace 件数 0」＝**記録が無い**と誤報／F11「next=None」＝**実装と正本の不一致**と誤報寸前 |
| 覆した証拠 | 実物の鍵は `task_trace.events` ／ `next_operation` |
| GM が先に揃える材料 | **口を叩く前に、その口の返り鍵一覧** |
| 決定論に落ちるか | **落ちる**。`sorted(r.keys())` を先に出すだけ。**今夜4回とも、後から出して気づいた** |

### F6 `run_until_barrier` が工程を飛ばしたと誤認した

| 欄 | 内容 |
|---|---|
| 本来最初に見る | **通った工程列**（phase の並び） |
| 実際に先に見た | 状態の**始点と終点だけ** |
| 生じた誤判断 | 「GENERATE を飛ばす駆動の欠陥」と2回報告 |
| 覆した証拠 | 工程列に `GENERATE` が**在った**（止まった3件とも） |
| GM が先に揃える材料 | **正本の期待工程列 と 実際の工程列の差分** |
| 決定論に落ちるか | **落ちる**。`_MAP` と実績列の差分は機械計算 |

### F7 `zero_audit_findings_ever` を完了条件と誤読した

| 欄 | 内容 |
|---|---|
| 本来最初に見る | `upper_review_gate.py` の docstring（設計意図） |
| 実際に先に見た | 不変条件の**値だけ**（false が並んでいる） |
| 生じた誤判断 | 「出口が構造上ない」 |
| 覆した証拠 | 逐語「**Everything non-trivial stays a Claude barrier**」＝自動PASSの条件であって完了条件ではない |
| GM が先に揃える材料 | **その条件が何のための条件か**（門の目的）を条件値と一緒に出す |
| 決定論に落ちるか | **部分的**。目的文の添付は機械。**「その目的なら妥当か」の判断は Claude に残る** |

### F8 過去の修正履歴を確認する前に現在の原因を断定した

| 欄 | 内容 |
|---|---|
| 本来最初に見る | 同じ症状の**過去の設計文書**（`SHOW_THE_ARTIFACT_TO_THE_JUDGE` 2026-08-08） |
| 実際に先に見た | 現在の挙動のみ |
| 生じた誤判断 | 新規の欠陥として報告しかけた（実際は既知・処置済み・**別ケース**） |
| 覆した証拠 | Taka の「昔直していたと思う」→ 8/8 の文書に**11回 FAIL の実測**が逐語で在った |
| GM が先に揃える材料 | **症状語での過去文書検索結果**（この症状は過去に何回出たか） |
| 決定論に落ちるか | **落ちる**（検索と件数）。**同一/別ケースの判定は Claude に残る** |

### F9 `DISPOSE` / `UPPER_REVIEW` が自分の正規手番なのに「滞留」と呼んだ

| 欄 | 内容 |
|---|---|
| 本来最初に見る | `dispatch.py::_MAP` の **actor と `claude_barrier` 欄** |
| 実際に先に見た | 状態名だけ（`DISPOSITION_REQUIRED` = 滞留22件、と連想） |
| 生じた誤判断 | 「Taka が範囲外とした領域に入る必要がある」と裁定を仰いだ |
| 覆した証拠 | `_MAP` に `('DISPOSE','MANAGER',...,claude_barrier=True)` |
| GM が先に揃える材料 | **止まっている案件ごとに「次は誰の手番か」** |
| 決定論に落ちるか | **完全に落ちる**。`_MAP` の引き算のみ。★LLM 不要 |

### F10 部品が在ることと、本線から呼ばれていることを混同した

| 欄 | 内容 |
|---|---|
| 本来最初に見る | **呼び手の数**（import / `_use` の第1引数） |
| 実際に先に見た | ファイルの存在 |
| 生じた誤判断 | 「87/87 通った」と報告（実際は私の使い捨てスクリプト経由） |
| 覆した証拠 | 8913ファイル走査で**本線参照 0**が8件 |
| GM が先に揃える材料 | **部品ごとの本線呼び手数**（0なら未接続と表示） |
| 決定論に落ちるか | **落ちる**。`ast` で全列挙 |

### F12 依存が構造欄でなく散文に埋まっていた

| 欄 | 内容 |
|---|---|
| 本来最初に見る | `depends_on` **と** `status_note` の両方 |
| 実際に先に見た | `depends_on` と `description` |
| 生じた誤判断 | 「EVO-0063 を待つ item = 0件」 |
| 覆した証拠 | `EVO-0019.status_note` に逐語「これは EVO-0063 の後に」 |
| GM が先に揃える材料 | **構造依存 と 散文依存の差分**（散文にしか無い依存を名指しで出す） |
| 決定論に落ちるか | **落ちる**（語で検出）。**依存として採るかの判定は Claude に残る** |

---

## 2. 何が機械化でき、何が Claude に残るか

| # | 責務候補 | 機械化 | Claude に残る |
|---|---|---|---|
| R1 | **問い → 参照すべき管理表** の対応を出す | ★完全 | ― |
| R2 | **口/部品を叩く前に、引数と返り鍵の一覧**を出す | ★完全 | ― |
| R3 | **同じ事実を持つ記録面を全部**挙げ、1面だけの不在を結論させない | ★完全 | ― |
| R4 | **未観測 / 0件 / 不明** の3値で返す（母数を明記） | ★完全 | ― |
| R5 | **正本の期待工程列 と 実績列の差分**を出す | ★完全 | ― |
| R6 | 止まっている案件の **次の手番の主体**（`claude_barrier`）を出す | ★完全 | ― |
| R7 | 部品ごとの **本線呼び手数**（0＝未接続） | ★完全 | ― |
| R8 | **構造依存 と 散文依存の差分** | ★完全 | 依存として採るか |
| R9 | 症状語での **過去文書の件数と一覧** | ★完全 | 同一ケースか別ケースか |
| R10 | 門の条件値に **その門の目的文**を添える | ★完全 | その目的なら妥当か |
| R11 | **証拠が揃ったか**（次へ進む停止条件） | ★完全 | 揃わない時に進むか止めるか |
| R12 | **Domain へ渡す材料の束**（R1〜R10 の出力） | ★完全 | ― |
| R13 | **人へ上げる条件**（上申8条件の判定） | 部分 | ★価値判断・優劣がつかない選択 |

**機械化 11／13、Claude に残るのは 4つの「判断」だけ**（依存採否・ケース同一性・門の妥当性・止めるか進むか）。

## 3. 責務表として整理（Taka の7項目）

| Taka の問い | GM v1 の責務 |
|---|---|
| **何を見るか** | R1 問い→表の対応 ／ R3 記録面の全列挙 |
| **どの順で見るか** | 台帳 → 経路表 → 実行証拠 → 状態分布 → 正本（今夜の指示が既に正本） |
| **何と何を突き合わせるか** | R5 期待列×実績列 ／ R3 task面×run面 ／ R8 構造依存×散文依存 |
| **未観測と0件をどう分けるか** | R4 3値固定（届いた／紐づかない／双方で無い）＋母数明記 |
| **どこまで揃えば次へ進むか** | R11 停止条件表（例: 2面以上で確認・母数が出た・鍵を添えた） |
| **Domain へ何を渡すか** | R12 束（案件文面＋鍵一覧＋記録面＋期待列＋手番） |
| **どの条件で人へ上げるか** | R13 上申8条件の機械判定＋価値判断のみ Claude |

## 4. 今夜の実測が示す期待値

**11件の誤判断のうち、9件は R1〜R8 で防げた**（F1 R1／F2F3 R3R4／F4F5F11 R2／F6 R5／F9 R6／F10 R7／F12 R8）。
残る2件（F7 F8）も、R9 R10 が材料を出せば**判断だけが残る**。

**∴ GM v1 は「賢い Manager」ではなく、★13枚の表を先に出す機構**である。

## 5. まだ作らない

**新 Manager の実装には入っていない。** 本書は責務表のみ。
実装順序・どの責務から作るか・既存 `manager_v0` との関係は、次の裁定で決める。
