# 実装 → 設計/監査: Build 10R — 再起動した。**証拠①のみ成立。②③は指標として成立しなかった**（BUILT）

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-27 / TYPE=BUILT
- **運用方針 確認済（版: v1.7）**
- 実装源: `CC_DESIGN_2026-07-27_BUILD10R_SPEC_RESTART_AND_VERIFY.md` v1.0
- **受領した文書**: 上記実装源 / `CC_MGR_2026-07-27_BUILD10R_APPROVED.md`（写しで観測）
- **本文書は観測を書きます。判定・評価・提案をしません。**

## 1. 手順1 — 再起動前の状態【監査:IMPL】
```
旧プロセス : pid 215805 / 起動 木 7月 23 08:07:33 2026 / etime 4-10:26:48
ソース mtime : twoder/webui.py             2026-07-27 18:05:37
               dev-workcell/dw/dispatch.py 2026-07-27 18:05:20
```
**＝ 起動はソースより約4日古い状態でした。**

## 2. 手順2 — 停止 → 起動【監査:IMPL】
```
停止時刻 : 2026-07-27 18:37:55   （kill 215805 → 3秒後に消滅を確認）
起動時刻 : 2026-07-27 18:38:04
コマンド : python3 -m twoder.webui 8770      ← ★既存の起動手順。新しい起動方法を作っていません
新 pid   : 1594748 / 起動 月 7月 27 18:37:58 2026
待受     : LISTEN 100.107.6.119:8770
```

## 3. 手順3 — 修理を持つことの確認（3つの証拠）【監査:IMPL】
| # | 証拠 | 予想 | **実際** |
|---|---|---|---|
| **①** | 新起動時刻 > ソース mtime | 成立 | **成立**（18:37:58 > 18:05:37） |
| **②** | 拒否応答に `planner_outcome` キー | **出ない** | **出なかった**（`['blocked','dispatched','reason','refused','runnable','task_id']`） |
| **③** | `/api/submit` 応答に `INTENT_STRATEGY` | **★出る（主証拠）** | **★出なかった** |

### 3-1. ②は仕様の指示どおり「判定できず」とします
実装源 §1-1 の【未確認】どおり、**run-gate の early return は S3 の行に到達しませんでした。**
**キーが出なかったことを「修理が入っていない」の根拠にしていません。**

### 3-2. ★③は「段3e の有無」を測れる指標ではありませんでした【監査:IMPL】
`/api/submit` の応答は `webui.py:549` で**キーを明示列挙して組み立てています**:
```
{"task_id", "request_type", …}   ← ★INTENT_STRATEGY は元から含まれていない
```
**＝ 段3e が動いていても動いていなくても、この応答に `INTENT_STRATEGY` は現れません。**
**∴ ③は「新しいコードが動いている」の証拠になりません。** 予想が外れたというより、**指標として成立していませんでした。**

**なお実装源 §0-1 は「Build 9C の段1 応答に `INTENT_STRATEGY` が無かった」ことを『webui が古い』の裏づけとしていますが、`webui.py:549` の構造上、その欠落は起動の新旧と無関係です。**
**私はここまでを事実として出します。この観測が §0-1 の推論にどう影響するかの判定はしません。**

### 3-3. ★結果として、修理が新プロセスに入っていることは①だけで支えられています
- **②は判定不能、③は指標不成立。**
- **∴ 実装源が用意した「2つの独立した証拠」は揃っていません。** **1つ（①＝起動時刻とソース mtime の比較）のみです。**
- **私はこれ以上の確認手段を、本 SPEC の範囲内では持っていません。**

## 4. 守った禁止事項
- **`TASK-2DER-D6A93450` に `run_next` を打っていません**（§0-3・worker を動かさないため）。**代わりに存在しない task id を使いました。**
- **`/api/submit` は1回・依頼文は Build 9B/9C/10 と同一（1文字も変えていません）。**
- **run-gate / token gate を回避していません。** **token gate による拒否は起きませんでした。**
- **本番コードを変更していません**（本 build では1行も）。
- **トークンを文書・ログ・argv に出していません。**
- **§3 の裁定要求（(a)/(b)/(c)）には着手していません。**

## 5. 観測の限界（事実として）
- **`planner_outcome` が実際に理由を運ぶかは、依然として未確認です**（PLAN が失敗する機会がありませんでした）。
- **各操作は1回ずつしか実行していません。**
- **状況表の「実行中プロセス」行は、次ターンで「ソースより新しい起動」に変わりました**（計器側の独立した確認）。

## 6. commit
**していません**（MGR）。**本 build では本番コードを変更していません。**

---
*IMPL BUILT（Build 10R）。旧 pid 215805（4日10時間前起動）を停止し、既存コマンドで新 pid 1594748 を起動（18:37:58）。**証拠①（起動 > ソース mtime）のみ成立。②は run-gate の early return で判定不能（予想どおり）。★③は `webui.py:549` が応答キーを明示列挙しており `INTENT_STRATEGY` を元から含まないため、指標として成立しなかった。** ∴「2つの独立した証拠」は揃っていない。`TASK-2DER-D6A93450` には `run_next` を打っていない（worker を動かさないため）。本番コード無変更。*
