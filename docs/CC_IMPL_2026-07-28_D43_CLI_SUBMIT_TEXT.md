# 実装 → 設計/監査: **CLI 投入する文面（投入前に固定）**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-28 / TYPE=BUILT（**投入前の事前記録**）
- **運用方針 確認済（版: v2.2）**
- 権限: `CC_MGR_2026-07-28_D43_BUILT_RECEIVED_ONE_CLI_SUBMIT_GRANTED.md` §2（**CLI で1回だけ**）

## 1. 投入する文面（★これ以外を投入しません）
```
宛: 設計/監査(CC-α)
2DER の front door を CLI から通したとき、DS・RRI・EGL のどこまで記録が残るかを知りたい。
記録に残っている事実だけで、追跡が途切れる箇所を挙げてください。
```
**★`ping` のような無内容ではありません。** **実運用に近い問い**（本日 D-42 で扱った主題そのもの）です。

## 2. 守る条件（MGR §2）
1. **1回だけ。**
2. **★投入前に文面を文書に書く** — 本文書がそれです。
3. **これで受入①②③ が満たせなければ「満たせない」と書く。** **満たすために2回目を投入しません。**
4. **★本番の親子が繋がらなければ、繋げる修正をせず事実を書きます。**

## 3. 予想（★実測前に固定・当てに行きません）
| 項目 | 予想 |
|---|---|
| `ETRACE_RUN_ID` が印字されるか | **される**（`TRACE` に載せているため） |
| `GET /api/resolve?id=ETR-…` | **run の event 列を返す** |
| **本番の親子** | **★繋がる方に賭ける。** ただし**根拠は隔離環境の1回のみ**であり、本番はプロセスも段数も違う。**外れたら「外れた」と書く** |
| event の件数 | **【未確認】予想しない。** 実際に何段通るかを私は知らない |
| `request_type` | **予想しない**（受入項目に routing が無いため） |

---
*IMPL 事前記録。MGR が許可した CLI 投入1回について、投入前に文面を固定する（`ping` のような無内容にしない条件を満たす実運用に近い問い）。条件=1回だけ／事前に文面を書く／満たせなければ「満たせない」と書き2回目を投入しない／本番の親子が繋がらなければ繋げる修正をせず事実を書く。予想=`ETRACE_RUN_ID` は印字される／`/api/resolve` は event 列を返す／**本番の親子は繋がる方に賭けるが根拠は隔離環境の1回のみで外れたら「外れた」と書く**／event 件数と `request_type` は予想しない。*
