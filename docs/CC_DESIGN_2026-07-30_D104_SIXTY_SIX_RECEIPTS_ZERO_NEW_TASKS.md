# 設計/監査 → MGR（写: Taka / IMPL）: **D-104 — 66件の受信を機械で絞った。★task は1件も増えていない。★止まっている。★誰が送ったかは出せない**

- `BUILD_ROLE: 参照`（**調査のみ。★実装していない・★投入していない・★log を1行も読んでいない・★台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-30_D103_IT_WORKED_AND_IT_SHOWED_67.md` §2

## 0. ★2DER 優先原則の5点
| # | |
|---|---|
| ① 入口 | **`GET /api/receipt` ／ `GET /api/tasks`** |
| ② 何を試したか | **★「増えた66件は、新しい依頼を作ったか」を 2DER に聞いた**（★log を読まずに） |
| ③ **できなかったこと** | **★「誰が送ったか」は聞けない**（★記録に id が無い）／**★401 と 200 の内訳も聞けない**（★口が返すのは最後の1件だけ） |
| ④ 何を実装するか | **★何も実装しない** |
| ⑤ | **★該当しない** |

## 1. ★結論（★4つの事実。★判定しない）
> **① ★task は1件も増えていない。** **★155件のまま**（★本日3回 測って同じ: 01:38 / 07:50 / 13:05）。
> **② ★いま増えていない。** **★13:09 と 13:10 で `recv_count=67` が同じ。★最後の受信は 12:53:07 で止まっている。**
> **③ ★画面に自動送信の経路が無い**（★`setInterval` 0件・`setTimeout` 0件。**★POST は押したときの1箇所だけ**）。
> **④ ★POST しうる常駐プロセスが無い**（★`operator` / `runtime_supervisor` / `curl` は動いていない）。

---

## 2. ★測ったこと（★全部 front door と実装の読み）
```
再現1（★front door）: GET /api/tasks → ★155件
  ★本日の記録: 01:38 → 155 ／ 07:50 → 155 ／ 13:05 → ★155
  ∴ ★66件の受信があっても、★依頼は1件も増えていない

再現2（★front door・60秒 空けて2回）:
  13:09:11  recv_count=67  last_recv=2026-07-30T12:53:07
  13:10:11  recv_count=67  last_recv=2026-07-30T12:53:07
  ∴ ★止まっている。★いま増え続けてはいない

再現3（★配信物の JS）: "/api/submit" への POST は ★1箇所だけ（押したときの submit()）
  ★setInterval 0件 ／ ★setTimeout 0件 ∴ ★画面が勝手に送る経路は無い

再現4（★プロセス一覧）: operator / runtime_supervisor / curl の常駐 → ★該当なし
```

## 3. ★残っている可能性（★私は選ばない・★証拠が無い）
| # | 可能性 | **★これを否定できるか** |
|---|---|---|
| **A** | **認証を通らない POST が数えられた** | **★否定できない。** `RECV` は**★認証の前**に置かれている（`webui.py:642`）∴ **★401 も数える** |
| **B** | **同じ依頼文が繰り返し送られた** | **★否定できない。** task id は依頼文の `sha1` ∴ **★同じ文なら task は増えない**（★資料 §5.3-2 に「再起動後は同一文を1回 再投入して gate を立て直す」と在る） |
| **C** | **submit() が途中で失敗して task にならなかった** | **★否定できない** |

> **★どれか1つに決められない。** **★口が返すのは「最後の1件の status」だけで、★66件の内訳を返さないからである。**
> **★最後の1件は `status=200`**（12:53:11）∴ **★少なくともその1件は認証を通っている。** **★残り65件は分からない。**

## 4. ★これは「口の限界」がそのまま出た形である（★記録する）
```
口ができたこと : ★66件 増えたと分かった（★今朝までは分からなかった）
口ができないこと: ★誰が・何を・成功したか失敗したか
```
> **★MGR が D-102 §2 で自己申告した「id を落とした」が、★初日にそのまま効いている。**
> **★「置いたのに読めない」ではなく、★「数えたのに中身が分からない」である。**
> **★私は直さない**（★id を足すのは【C】・14件目）。**★ただし「口が在るから分かるようになった」とは書かない。**

## 5. ★私が言わないこと
| ★書かない | 理由 |
|---|---|
| **「Taka が押した」** | **★口はそれを答えられない。★推測である** |
| **「外部からの攻撃である」** | **★同じく推測。★401 が在ったかどうかも分からない** |
| **「投入は無かった」** | **★私と IMPL は投入していないと言えるが、★他は言えない** |
| **「安全である」「問題ない」** | **★分からないものを、分かったように書かない** |

## 6. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **66件の内訳（401 / 200 / 失敗）** | **★口が返さない。★log を読めない ∴ 出せない** |
| 2 | **いつからいつまでに増えたか** | **★口が返すのは「最後の1件」だけ。★分布は出せない** |
| 3 | **`/api/submit` 以外の POST が数えられていないか** | **★実装は `/api/submit` の1箇所だけ**（`webui.py:642`）。**★他の口は数えていない** |
| 4 | **155件が「増えていない」と言えるのは TASK-2DER だけか** | **★`/api/tasks` は `TASK-2DER` で絞っている**（D-94 実測）∴ **★それ以外が増えていても見えない** |

> **★4が重い。** **★「task は増えていない」は、★`/api/tasks` が見せる範囲での話である。**
> **★今日 私が3回 踏んだ「探した範囲を『無い』と書く」に、★また当たりかけた。**

---
*CC-α D-104（調査のみ・log を1行も読んでいない）。★**4つの事実（判定しない）**=①**task は1件も増えていない**（`/api/tasks` は本日3回とも155件: 01:38/07:50/13:05）②**いま増えていない**（13:09 と 13:10 で `recv_count=67` が同じ・最後の受信は 12:53:07 で止まっている）③**画面に自動送信の経路が無い**（`setInterval` 0件・`setTimeout` 0件で POST は押したときの1箇所だけ）④**POST しうる常駐プロセスが無い**（`operator`/`runtime_supervisor`/`curl` は動いていない）。★**残る可能性3つを CC-α は選ばない**=A **認証を通らない POST が数えられた**（`RECV` は**認証の前**に置かれており `webui.py:642` ∴ **401 も数える**）／B **同じ依頼文が繰り返し送られた**（task id は依頼文の `sha1` ∴ **同じ文なら task は増えない**。資料 §5.3-2 に「再起動後は同一文を1回 再投入して gate を立て直す」と在る）／C **submit() が途中で失敗して task にならなかった**——**どれも否定できない。口が返すのは「最後の1件の status」だけで66件の内訳を返さないからである**（**最後の1件は `status=200` なので少なくともその1件は認証を通っているが、残り65件は分からない**）。★**これは「口の限界」がそのまま出た形**=**口ができたのは「66件 増えたと分かった」ことで、できないのは「誰が・何を・成功したか失敗したか」**——**MGR が D-102 §2 で自己申告した「id を落とした」が初日にそのまま効いており、「置いたのに読めない」ではなく「数えたのに中身が分からない」である** ∴ **CC-α は直さない（id を足すのは【C】14件目）が、「口が在るから分かるようになった」とも書かない**。★**言わないこと**=「Taka が押した」（推測）／「外部からの攻撃」（同じく推測・401 が在ったかも分からない）／「投入は無かった」（**CC-α と IMPL は言えるが他は言えない**）／「安全である」「問題ない」（**分からないものを分かったように書かない**）。★確かめていないこと=**66件の内訳（401/200/失敗）は口が返さず log を読めないので出せない**／**いつからいつまでに増えたかの分布も出せない**／`/api/submit` 以外の POST は数えていない（実装は1箇所だけ）／**「task は増えていない」は `/api/tasks` が `TASK-2DER` で絞っている範囲の話であり、それ以外が増えていても見えない——これが最も重く、今日3回 踏んだ「探した範囲を『無い』と書く」にまた当たりかけた**。*
