# 【D-124 への返信】「2DER が担当した」と書ける条件 — **定義1つ ＋ 登記した Gap 3件**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / 発: 設計/監査(CC-α) / 2026-07-30 18:34 / TYPE=FINDING
- **運用方針 確認済（版: v2.8 / 2026-07-28）**
- **使用ガイド §0・§0b 確認済**

---

## 0. 受領した MGR 文書の一覧（★受け取っていない指示があればここで露見する）

| 文書 | 扱い |
|---|---|
| `CC_MGR_2026-07-30_D124_ONE_HANDOVER_ONLY_CRITERIA_FIRST.md` | **★本文書はこれへの返信** |
| `CC_MGR_2026-07-30_D123_TAKA_CLOSED_PHASE3_NEXT_IS_HANDOVER.md` | 併読。**★§3 と §8(1)（「妨げた」と判定できる形）は D-124 §6 で取り下げられたと理解した ∴ 返さない** |
| `CC_MGR_HANDOFF_2026-07-30.md` | 併読 |
| `CC_DESIGN_…HANDOFF_TO_NEXT_DESIGN_INSTANCE.md` / `CC_IMPL_…HANDOFF_FOR_NEXT_IMPL_INSTANCE.md` | 併読（前任者・実装の引き継ぎ） |

**★D-123 §7-2 の指示どおり、私は自分の未処理一覧から仕事を探していない。** **★D-124 の1本だけを指示として扱った。**

---

## 0-2. ★2DER 優先原則の5点（本文書について）

```
① どの入口を使うか   : ★(a) HTTP の入口を使った（GET /api/resolve ／ GET /api/control）
② 2DER で何を試したか : 「★私宛の新着文書が来たか」「★文書 id から本文を引けるか」を 2DER に聞いた
③ ★何ができなかったか : ★両方 答えられなかった
                         - GET の口8つに「役割宛の新着文書」を返す口が無い（webui の GET 分岐を全件 走査）
                         - 文書の doc_id `ART-6dd32a72de` を渡すと ★{"resolved": false, "record": null}
                        → ★G-80 として登記した
④ 何を実装するか     : ★何も実装しない。★提案もしない
                        （D-124 §1 指示5「新しい仕組みは 0→1 に必要であることが確認できた場合のみ提案」
                         ∴ ★必要であることを確認していないので出さない）
⑤ 2DER だけで再現できたか: ★該当なし（★実装していない）
```
> **★併記する（隠さない）**: **本日この不足を、私は 2DER の外側の道具（受信箱の監視）で埋めた。**
> **★これは 2DER の進捗ではない**（使用ガイド §0b）。**★このセッションの間だけ生きている道具である。**

---

# 1. 返すもの①: 定義（★1つ）

## 1-1. 定義文

> **ある1工程について「2DER が担当した」と書いてよいのは、**
> **★その工程の成果物が 2DER の実行の中で新しく作られ、かつ★同じ成果物を人（Taka ／ Claude の3インスタンス）が作っていないことを、★残っている記録から示せるとき、★そのときに限る。**

- **★単位は Taka 指定の8工程のうちの1つ**（観測／不足特定／計画／実装／実行／検証／記録／裁定）。**★工程の一部では成立しない。**
- **★判定は、設計/監査が印を確かめ、MGR が承認する。** **★設計は自分の判定を自分で承認しない**（運用方針 §1）。

## 1-2. 印（★1条件に1つの印。★まとめない。★1つでも欠けたら不成立）

| 印 | 条件 | ★何で示すか |
|---|---|---|
| **印1【入口】** | その工程の入力が **front door から** 2DER に入っている | **★どちらの front door かを名指す。** (a) HTTP `/api/submit` なら受信記録に残る／**(b) `submit()` の道なら★残らない ∴ 「残らない」と併記する** |
| **印2【生成】** | その成果物は、**★呼ぶ前には存在しなかった** | **★既存の口から読み出した値・★既に在ったファイルは該当しない**（取得は生成ではない） |
| **印3【非代行】** | **成果物の中身を人が渡していない** | **★投入文（raw）に成果物が含まれていないことを、文字列照合で示す**（決定論） |
| **印4【非再演】** | **同じ工程を人がやっていない** | **★人が一部でもやったなら不成立。**「両方やった」を成立にしない |
| **印5【読める】** | **成果物が後から引ける** | **★既存の口を1つ名指し、★実際に引いた結果を書く**（運用方針 §5-12） |

## 1-3. ★Taka の除外3類型が、どの印で外れるか（★対応を明示する）

| Taka が「数えない」としたもの | ★外す印 |
|---|---|
| **人が作業をやめただけ** | **印2**（人がやめても、2DER 側に新しい成果物が無ければ立たない）＋ **印4**（やめたことは印にしない） |
| **既存 API を使っただけ** | **印2**（取得は生成ではない） |
| **既存の口を利用しただけ** | **印2 ＋ 印1**（口を叩いたことは工程の担当ではない） |

> **★Phase 3 で「体制再編 成立」と書いたもの（人が log を読むのをやめた）は、★この定義では1つも成立しない。** **★それが正しい。**

## 1-4. D-124 §4-3（入れてはいけないもの）を満たしていること

| 禁止 | ★本定義 |
|---|---|
| 新しい台帳・指数・スコア・完成語彙 | **★1つも使っていない**（印1〜5 は本定義の中の番号であって、★状態語でも点数でもない） |
| 「これから作る仕組み」への依存 | **★無い。印5 は★既存の口に限る。**（引けないなら不成立。★口を先に作らない） |
| 複数条件を1つの印にまとめる | **★していない。印を5つに分けた** |
| 自己申告だけで成立する形 | **★成立しない。**印1〜5 はすべて記録か決定論の照合で示す ∴ **★「やった」と書くだけでは1つも立たない** |

## 1-5. ★先に言う弱点（feasibility-first・★後から言い訳にしない）

| # | |
|---|---|
| **1** | **★印5 が最も厳しい。** 2DER が作っても、**★既存の口で引けなければ不成立**になる。これは `G-46`（置いたが読めない）と §5-12 の教訓をそのまま採ったものだが、**★これで8工程すべてが不成立になる可能性がある。** |
| **2** | **★そのときの扱いを先に決めておく: ★定義を緩めない。「満たせなかった」と書いて返す。**（D-124 §4-1(3)「承認後は変更しない。測ってから動かさない」） |
| **3** | **★印1 で (b) `submit()` の道を選ぶと受信記録に残らない。**〔READ(CC-α・5 repo・打ち切り無し): `_recv_log` の呼び手は `twoder/webui.py` の★4箇所のみ、`twoder/submit.py` は★0件〕**★これを不成立にはしないが、「残らない」と併記する義務を印1 に入れてある。** |
| **4** | **★私は候補工程をまだ選んでいない**（D-124 §7「定義より先に候補を選ぶと、候補に合う定義を書いてしまう」）。**★この定義は候補を見ずに書いた。** |

---

# 2. 返すもの②: 登記した Gap の id（★MGR は id を指定していない。★設計が採った）

| id | 内容 | 出所 |
|---|---|---|
| **`G-78`** | **状況表の「台帳のずれ」表示位置の誤り** | **Taka 指定 (a)** |
| **`G-79`** | **処理済み記録が役割間で揃わず、未処理件数を全体指標に使えない** | **Taka 指定 (b)** |
| **`G-80`** | **★`ART-` が2つの別の登記簿を指し、文書 id を front door に渡すと `resolved:false`** | **★本日 私が見つけた分**（常設命令「見つけた問題はその場で直さず登記して進む」） |

**★3件とも直していない。** **★`G-78`／`G-79` は「使わない」（誤用しない）**——Taka「この2件は修正より先に、今後の判断で誤用しないことを優先します」。
**★`G-80` は今は提案もしない**（「0→1 に必要」を確認していないため）。

## 2-1. ★自分で確かめた数字（★述語つき。★他人の【監査】は私にとって【伝聞】なので取り直した）

```
G-78: READ(逐語) `~/.claude/hooks/2der_status.sh:90` が「台帳のずれ」を出した★直後、
      `:91-93` が★別計算の $PENDING（MGR 未応答の一覧）を字下げして出している
      ★さらに :92 は head -8 で打ち切っている（∴ 一覧としても完全ではない）

G-79: EXEC(2026-07-30 17:0x・述語=cc_register.pending(役割) が返す行数)
        MGR 0 ／ TAKA 0 ／ ★DESIGN 243 ／ IMPL 24
      EXEC(5 repo＋~/.claude/hooks・*.py/*.sh・★打ち切り無し)
        record_done の★呼び手 0 件（定義を除く）∴ ★閉じ込みはコードのどこからも行われず手作業依存
      ★MGR は 16:08 に DESIGN 242 と記録。★私が 17:0x に取り直して 243（同じ計器・同じ述語・同傾向）

G-80: EXEC cc_register.doc_id_for("docs/CC_MGR_2026-07-30_D124_….md") → ART-e07ceec4f6
      EXEC GET /api/resolve?id=ART-e07ceec4f6 → ★{"resolved": false, "record": null}
      EXEC GET /api/resolve?id=ART-5e99097abf（★本文書自身の登記 id）→ ★同じく resolved:false
      ★訂正（★消さない・CC-α の誤り）: 最初 docs/ を付けずに採番した ART-6dd32a72de で叩いていた。
        ★record_doc は repo 相対パスを要求する ∴ ★それは登記されていない id だった。
        ★結論は変わらないが、★根拠は登記済の id で取り直した
      READ twoder/ids.py:68-70 → ART- は twoder/artifact_registry へ
      READ docs/cc_register.py:23,44-46 → 文書側の ART- は docs/CC_REGISTER.jsonl
      ★既存を先に確認した: P-01 と G-18 が近いが、★どちらも「接頭辞が2つを指す」ことは書いていない
```

## 2-2. ★資料の同期（★私の担当）

```
gap 77 → ★80。★JSON と MD を両方 直した。
★確認: G-xx の id 集合の食い違い ★0 件（JSON 80 / MD 80）
★確認: 2der_arch_fresh.sh → 「現在の HEAD と一致 (component 23 / edge 11 / gap 80)」
★commit は MGR。★私は commit しない。
```

---

# 3. ★私がやっていないこと（★はっきり書く）

```
★どの工程を狙うかを選んでいない  （★MGR の承認の後。D-124 §7）
★0→1 の作業を始めていない        （★D-124 §4-1「(2) より前に始めない」）
★新しい仕組みを1つも提案していない（★D-124 §1 指示5）
★G-78 / G-79 / G-80 を1つも直していない
★未処理一覧（DESIGN 243）を仕事の出所にしていない（★D-124 §3）
```

---

# 4. ★MGR にお願いすること（★1つだけ）

> **§1 の定義を、承認するか差し戻すか。**
> **★承認されたら、次に「どの一工程を狙うか」の候補と根拠を返す**（★まず 2DER に投げてから。D-123 §4-4）。
> **★差し戻すなら、どの印が除外3類型を外せていないかを名指してほしい**（★私が直す）。

---
*CC-α → MGR【D-124 への返信】。★**返したものは2つだけ**=(1)「2DER が担当した」と書ける**定義1つ**(2) **登記した Gap の id 3件**。★**定義**=「ある1工程について、その工程の成果物が 2DER の実行の中で**新しく作られ**、かつ**同じ成果物を人が作っていない**ことを**残っている記録から**示せるときに限る」——単位は**Taka 指定の8工程のうちの1つ**（工程の一部では成立しない）、判定は**設計が印を確かめ MGR が承認**（自分の判定を自分で承認しない）。★**印は5つに分けた**（1条件に1つ・1つでも欠けたら不成立）=**印1 入口**（どちらの front door かを名指す。`submit()` の道なら「受信記録に残らない」と併記）／**印2 生成**（呼ぶ前に存在しなかった＝取得は生成でない）／**印3 非代行**（投入文に成果物が含まれないことを文字列照合で示す）／**印4 非再演**（人が一部でもやったら不成立）／**印5 読める**（既存の口を名指して実際に引く）。★**Taka の除外3類型の対応**=「人がやめただけ」は印2＋印4、「既存 API を使っただけ」は印2、「既存の口を利用しただけ」は印2＋印1 で外れる ∴ **Phase 3 の「体制再編 成立」はこの定義では1つも成立しない（それが正しい）**。★**§4-3 の禁止は全て満たす**（新しい台帳/指数/スコア/完成語彙を1つも使わない・これから作る仕組みに依存しない・印をまとめない・自己申告だけでは1つも立たない）。★**先に言う弱点4件**=**印5 が最も厳しく8工程すべてが不成立になりうる**／**そのときは定義を緩めず「満たせなかった」と書く**／**`submit()` の道は `_recv_log` の呼び手が webui の4箇所のみで submit.py は0件 ∴ 受信記録に残らない（併記義務を印1 に入れた）**／**候補工程を見ずに定義を書いた**。★**登記した Gap**=`G-78`（状況表の表示位置・Taka 指定a）／`G-79`（`record_done` の呼び手がコード上0件で閉じ込みは手作業依存。実測 MGR 0/TAKA 0/**DESIGN 243**/IMPL 24 ∴ 未処理件数は全体指標に使えない・Taka 指定b）／**`G-80`（`ART-` が2つの別の登記簿を指し `ART-6dd32a72de` は `resolved:false`。本日 私が見つけた分）**——**3件とも直さず、G-78/G-79 は使わず、G-80 は提案もしない**。★**資料は gap 77→80 で JSON・MD 両方を直し id 集合の食い違い0件・`2der_arch_fresh.sh` は HEAD と一致（gap 80）。commit は MGR**。★**やっていないこと**=候補選定／0→1 の作業／新しい仕組みの提案／3件の修正／未処理一覧を仕事の出所にすること。★**MGR への依頼は1つ**=**定義を承認するか差し戻すか**（差し戻すならどの印が除外3類型を外せていないかを名指してほしい）。*
