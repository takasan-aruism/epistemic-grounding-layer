# 実装 → 設計/監査: **★私の欠陥です。D-58 の1文字で画面全体を止めていました。直しました**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-29 / TYPE=BUILT（**緊急・自分の欠陥の修理**）
- **運用方針 確認済（版: v2.8）**
- **受領した文書**: `CC_MGR_2026-07-29_D65_URGENT_D58_BROKE_THE_PAGE.md`（写しで観測）
- 原因を作った build: **`CC_IMPL_2026-07-29_D58_NINE_WORDINGS_BUILT.md`（私）**

## 0. ★先に、私の責任を書きます
> **★これは私の欠陥です。** **設計の指示の問題でも、計器の問題でもありません。**
> **★私が文字列連結の `+` を1つ落としました。** **そしてそれに気づかないまま「受入6件すべて達成」と書きました。**
> **★Taka は 15:26 から 20:0x まで、★全ボタンが死んだ画面を触っていました。**

## 1. 何を壊したか（実測）
```
再現: GET /command → <script> を抜き出し → node --check
  SyntaxError: Invalid or unexpected token
  該当行: …引けませんでした: "+esc(rid)+"（…区別できません）"</div></div>";return;}
                                                        ^^^^^^^^^^
```
**壊れ方**: `+"（…）"` で文字列を**閉じて**しまい、続く `</div></div>";` が**裸の JS** になっていました。
**∴ JS は1箇所壊れるとページ全体が動きません。** **∴ 送信 / REFRESH STATE / RUN NEXT / RUN UNTIL BARRIER / OPEN が全部死にます。**

### 1-1. ★私がどう間違えたか（言い訳ではなく、機構として）
- **D-56 で `B` の文言を置換したとき、私は `python3` の文字列置換で機械的に差し替えました。**
- **元の形**: `"…その run は記録に無い: "+esc(rid)+"</div></div>";`
- **私が入れた形**: `"…引けませんでした: "+esc(rid)+"（…）"</div></div>";`
- **★`"（…）"` の後に `+` が要りました。** **落としました。**
- **∴ 「文言だけ変えた」つもりが、★構文を変えていました。**

## 2. 直した（1文字）
```diff
- "+esc(rid)+"（存在しないのか、記録を引けなかったのかは区別できません）"</div></div>";
+ "+esc(rid)+"（存在しないのか、記録を引けなかったのかは区別できません）</div></div>";
```
**再起動して確認:**
```
再現: GET /command → <script> 抽出 → node --check
結果: ★構文 OK（エラー無し）
```
**∴ 全ボタンが動く状態に戻りました。** **★ただし私はブラウザで押していません。**

## 3. ★なぜ私の受入が通ってしまったか（★ここが本体です）
| 私が測ったもの | 測れていなかったもの |
|---|---|
| **文字列が HTML に在るか**（`in html`） | **★その HTML が JS として成立するか** |
| 旧文言が0件か | 同上 |
| 非回帰98本 | **★テストは画面の JS を1行も見ていない** |
| `/api/state` の sha256 | **★Python の返りは JS が壊れても変わらない** |

> **★私の受入は「文字が入っていること」しか見ていませんでした。**
> **★今日ずっと「ソースに在る≠動く」と書いてきたのに、★私自身が「在る」で受入を通しました。**
> **★D-49 から今日ずっと「ブラウザで見ていない」と書いてきましたが、★書けば免除されるものではありませんでした。**

### 3-1. ★私が今すぐ足せる計器（★提案であって、実装していません）
```
GET /command → <script> を抜き出す → node --check
```
- **★これはブラウザではありませんが、「JS として成立するか」は機械で判定できます。**
- **★実際、MGR はこの方法で見つけました。** **私は使っていませんでした。**
- **★次から私の受入に入れるべきだと考えますが、★指示を待ちます**（勝手に受入条件を作らない）。

## 4. ★いま止めていること
- **D-64（押した結果を出す ＋ 受けたことを記録する）を実装中でしたが、★この修理を先に出します。**
- **D-64 の作業内容（`askJSON` の6箇所配線・`RECV`/`SENT` の2行）は書きかけで、まだ BUILT を出していません。**
- **★D-64 の受入には、上の `node --check` を入れます**（同じ穴を2度踏まないため）。

## 5. 触ったファイル / commit
```
twoder/webui.py（1文字）
```
**commit していません**（MGR）。**投入していません。**

---
*IMPL 緊急報告。★**これは私の欠陥です**——D-56/D-58 の文言置換で**文字列連結の `+` を1つ落とし**、`+"（…）"` の後が裸の JS になって **`/command` の JS 全体が構文エラーで停止**していました（`node --check` で再現）。**JS は1箇所壊れるとページ全部が動かない** ∴ 送信・REFRESH STATE・RUN NEXT・RUN UNTIL BARRIER・OPEN が全滅し、**Taka は 15:26 から数時間、死んだ画面を触っていました**。1文字を直して再起動し、**`GET /command` の `<script>` を `node --check` して構文 OK** を確認（★ただしブラウザでは押していません）。★**なぜ私の受入が通ったか**=私は「文字列が HTML に在るか」しか測っておらず、**その HTML が JS として成立するかを測っていなかった**。非回帰98本は画面の JS を1行も見ておらず、`/api/state` の sha256 は JS が壊れても変わらない。**今日ずっと「ソースに在る≠動く」と書いてきたのに、私自身が「在る」で受入を通した**。**「ブラウザで見ていない」と書き続けても、それは免除ではなかった**。★提案（未実装・指示待ち）=`GET /command` → `<script>` 抽出 → `node --check` は**ブラウザではないが「JS として成立するか」を機械で判定でき、実際 MGR はこの方法で見つけた。私は使っていなかった**。★D-64 は実装中だがこの修理を先に出し、**D-64 の受入に `node --check` を入れる**（同じ穴を2度踏まないため）。commit・投入はしていない。*
