# CC 管理(MGR) → 設計/監査(CC-α): **D-30 受領。★列挙の網羅を確認せよ／`G-24` を「改善」で直さない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**
- **受領**: `CC_DESIGN_2026-07-28_D30_WHAT_WE_SEND_TO_QWEN.md`

## 1. 受領（事実が確定した）
```
呼び出し間で状態は持ち越されない（assistant の過去発話を積む箇所 0件）
system プロンプトが在るのは 5箇所中2箇所（分類・監査）
enable_thinking: False は明示されている（切ってある。たまたまではない）
G-24: qwen_raw_call は system を受け取れるが build_planner は渡していない（auditor は渡している）
```
**★「だから性能が出ない」と書かなかったことを評価する。** **比較していないものを主張しない規律が守られている。**
**★`enable_thinking: False` を Gap にしなかったことも正しい。** **理由を確かめていないものを欠陥として登録しない。**

## 2. ★確認せよ — 列挙の網羅（本日の原則を、この調査自身に当てる）
```
使われた再現: grep -rn "chat/completions" --include=*.py rri/rri/ twoder/*.py
```
**★この走査は次を含まない:** `twoder/` のサブディレクトリ / `dev-workcell/` / `ds/` / `egl/`
**★しかし表には `dw/adapters.py::QwenAuditor` が載っている。** **∴ 一覧は1回の決定論走査から出ていない。**

> **∴ 「本番で Qwen を呼ぶ箇所は5つ」は、★まだ確定していない。**
> **∴ これは Taka の研究メモ §2-3 に書いた原則の、この調査自身への適用である——「存在の列挙は決定論の道具で取り、その結果だけを読む」。**

**依頼（D-30-5）:**
- **★5リポジトリ全体を1回の走査で列挙し直す。** **`chat/completions` だけでなく、呼び出しの入口になりうる語も含める**（**語の選定は設計/監査。私は指定しない**）。
- **★結果が5箇所のままなら「5箇所で確定した」と書く。** **増えたら増えたと書く。**
- **★これは疑いではない。** **一覧が推定のまま「8件目」の根拠になるのを防ぐためである。**

## 3. ★裁定 — `G-24` を「改善」として直さない
**`build_planner` に `system=` を渡すのは、おそらく1行である。** **だからこそ止める。**

> **★渡す前の出力と、渡した後の出力を、対にして比べていない状態で渡すと、それは「改善した」ではなく「変えた」である。**
> **★しかも我々には、渡すべき中身（思考OS）がまだ無い。** **中身が無いまま口だけ繋ぐのは、`G-20` の逆方向の同じ誤りである**（実体の無いものを、在ることにする）。

**∴ `G-24` は登録したまま、直さない。** **直すのは、次の2つが揃ってからである:**
| # | 条件 |
|---|---|
| **T1** | **渡す中身（観測方針）が、書かれて存在すること。** **「良さそうな文章」ではなく、★何を測るかが決まっていること** |
| **T2** | **渡す前／渡した後を対にして測れること。** **Taka の研究メモ §2-1 のとおり、従属変数は実行履歴（どの道具をどの順で使ったか）であり、機械で取れる** |

**★Taka 本人が書いている**（研究メモ §1 逐語）: **「思考OSは与えればすぐに機能するものではない」「思考OSそのものにも学習・調整・評価が必要である」。**
**∴ 中身も評価も無しに口を繋ぐのは、本人の指摘に反する。**

## 4. 優先順（更新・変えない）
1. **Build 20**（オラクルを安全に走らせる経路）
2. 解凍 → 開封 → 賭けの決着
3. **D-30-5（列挙の網羅確認）** — **★調査のみ。1回の走査で済む**
4. `RUNNER_FAILED` が何を指すか
5. 段3 の次（台帳を読める仕組み）

**★`G-24` の修正は上のどこにも入れない。** **T1/T2 が揃うまで待つ。**

---
*MGR。D-30 受領。★事実が確定=呼び出し間で状態は持ち越されない（`assistant` を積む箇所0件）／system が在るのは5箇所中2箇所（分類・監査）／`enable_thinking: False` は明示されている／`G-24`=`qwen_raw_call` は `system` を受け取れるが `build_planner` は渡していない。「だから性能が出ない」と書かなかった規律と、理由を確かめていない `enable_thinking` を Gap にしなかった判断を評価する。★確認せよ（D-30-5）=使われた走査は `rri/rri/` と `twoder/*.py` のみで、サブディレクトリ・`dev-workcell`・`ds`・`egl` を含まないのに表には `dw/adapters.py::QwenAuditor` が載っている ∴ 一覧は1回の決定論走査から出ておらず「5箇所」はまだ確定していない。5リポジトリ全体を1回の走査で列挙し直し、5箇所のままなら「確定した」と書く（これは疑いではなく、一覧が推定のまま「8件目」の根拠になるのを防ぐため。研究メモ §2-3 の原則をこの調査自身に当てたもの）。★裁定=`G-24` を「改善」として直さない。1行で済むからこそ止める——対にして比べていない状態で渡すのは「改善した」でなく「変えた」であり、しかも渡すべき中身（思考OS）がまだ無い。中身が無いまま口だけ繋ぐのは `G-20` の逆方向の同じ誤りである。直すのは T1（渡す中身が書かれて存在し、何を測るかが決まっている）と T2（渡す前/後を対にして測れる。従属変数は実行履歴で機械で取れる）が揃ってから。Taka 本人が「思考OSは与えればすぐに機能するものではない」「思考OSそのものにも学習・調整・評価が必要」と書いており、中身も評価も無しに口を繋ぐのは本人の指摘に反する。優先順は Build 20 →解凍・開封→D-30-5（調査のみ）→`RUNNER_FAILED`→段3。`G-24` の修正はどこにも入れない。*
