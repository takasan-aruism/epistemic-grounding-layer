# 設計/監査 → **実装(IMPL)**: **「無い」と出す9件を1回で直してください（承認済）。★投入は1回も要りません**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**送付状。★実装源は本文書ではない**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.8`）** / **承認**: `CC_MGR_2026-07-29_D58_APPROVED_PYTHON_EXCEPTION_AND_SYNC_ORDER.md`

## 1. ★実装源（これ1本）
```
CC_DESIGN_2026-07-29_D58_NINE_WORDINGS_BUILD_SPEC_v1_0.md   ← BUILD_ROLE: ★実装源
```
**★承認で2点変わった**（下の §2・§5）。**それ以外は SPEC のまま。**

## 2. ★承認で変わった点①：Python の例外が明示で許された
```
SPEC §5 は「Python を変えない」と書いたが、★窓の2件は render_report_page = Python の中に在る。
```
| **★許された** | **★許されない** |
|---|---|
| **文字列リテラルだけの書き換え** | **ロジックを1行でも変える** |
| | **`[-6:]` を変える**（★窓の大きさは直さない。**出すだけ**） |
| | endpoint・新しい fetch・新しい状態 |

**★受入が1つ増えた:**
> **`render_report_page` の変更が★文字列リテラルだけであることを、★差分で示すこと。**

## 3. ★直す9件（内訳を混ぜない）
| 種別 | 件数 | |
|---|---|---|
| **偽になる文** | **1** | `全 event の result は OK（失敗の記録なし）` → **★最優先** |
| **窓** | **2** | `/` の直近 DE・CHG の `なし` |
| **まとめている** | **6** | `/command` 5件 ＋ `/` 1件 |

**★残り15件を触らない。**

## 4. ★いちばん外しやすい2点
| # | |
|---|---|
| **1** | **★受入2「旧文言が1つも残っていない」。** **新文言が在ることだけ確かめると、★半分だけ直した状態を見逃す。** **9件の旧文言を1つずつ検索して0件を示すこと** |
| **2** | **★実例が在るのは `/command` #C の1件だけ**（`authority boundary`）。**★他8件は「実例を見つけていない」と書く。** **混ぜて書かない。作って試さない** |

## 5. ★承認で変わった点②：資料の同期の順番（★あなたに影響する）
```
旧: BUILT と同時に CC-α が資料を同期
★新: BUILT → 受入の確認 → MGR がコードを commit → ★その後に CC-α が同期
```
> **理由（MGR の実測）**: 旧の順だと**資料に記録される commit が必ず1つ前になり、正しく同期しても計器が必ず赤くなる**。
> **★偽の赤は、本物の赤を無視させる。**
> **∴ SPEC §8 は★取り消し。** **★あなたは資料に触らなくてよい**（これは変わらない）。

## 6. ★止まってよい場所
1. **`/` の2件で文字列だけを変えられなかった**（ロジックに触れる）→ **★止めて報告**
2. **旧文言を消すと別の分岐が壊れた** → 止めて報告
3. **★9件のどれかが、私の指定した場所に無かった** → **★私の数え間違い。報告してほしい**
4. SPEC が2通りに読める

## 7. ★投入は1回も要らない
```
受入3（実例が在る #C）→ TASK-2DER-B9B4DA3B（taka_authority = []）
受入1・2（文言の在/無）→ GET /command と GET / のソース
```
**★今日すでに投入は6回ある。** **7回目を作らずに全部検証できる。**

---
*CC-α → IMPL 送付状（D-58）。★実装源は `CC_DESIGN_2026-07-29_D58_NINE_WORDINGS_BUILD_SPEC_v1_0.md` の1本で、承認により2点変わった。★変更点①=**Python の例外が明示で許された**——窓の2件は `render_report_page`＝Python の中に在るため、**文字列リテラルだけの書き換えは許され、ロジックの変更・`[-6:]` の変更（窓の大きさは直さず出すだけ）・endpoint／新しい fetch／新しい状態は許されない**。**受入が1つ増え、`render_report_page` の変更が文字列リテラルだけであることを差分で示す**。★直す9件は偽になる文1（`全 event の result は OK` ＝最優先）／窓2（`/` の直近 DE・CHG）／まとめている6（`/command` 5 ＋ `/` 1）で、**残り15件を触らない**。★いちばん外しやすい2点=**受入2「旧文言が1つも残っていない」**（新文言が在ることだけ確かめると半分だけ直した状態を見逃すので、9件の旧文言を1つずつ検索して0件を示す）／**実例が在るのは `/command` #C の1件だけで他8件は「実例を見つけていない」と書き、混ぜて書かず作って試さない**。★変更点②=**資料の同期の順番**が「BUILT と同時」から「**MGR がコードを commit した後に CC-α が同期**」に変わった（旧の順だと資料に記録される commit が必ず1つ前になり、正しく同期しても計器が必ず赤くなる。**偽の赤は本物の赤を無視させる**）——SPEC §8 は取り消しだが、**IMPL が資料に触らない点は変わらない**。★止まってよい場所4件（`/` の2件で文字列だけを変えられなかった／旧文言を消すと別の分岐が壊れた／**9件のどれかが指定した場所に無かった＝CC-α の数え間違い**／2通りに読める）。★**投入は1回も要らない**（受入3 は `TASK-2DER-B9B4DA3B`、受入1・2 は `/command` と `/` のソース。今日すでに6回投入しており7回目を作らずに全部検証できる）。*
