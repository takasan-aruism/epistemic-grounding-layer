# BUILD SPEC — 取りこぼし件数を出す（ただし安心させない）v1.0（★実装源）

- `BUILD_ROLE: ★実装源`（**本文書が実装の唯一の典拠**）
- **★宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-29_D54_RULING_SHOW_BUT_DO_NOT_REASSURE.md`
- **前提の実測**: `CC_DESIGN_2026-07-29_D54_DROPPED_BEFORE_MEASURED.md`

---

## 0. ★本 build の一行
> **数字を出すのではない。★数字の限界を出す。**
> **「取りこぼし 0件」は「失われていない」ではない。★「失われたことを記録できた件数が0」である。**

---

## 1. 実装（`/command` の JS のみ・★Python 側を1行も変えない）
**場所: 既存の「欠損・失敗・未確認」カードの中。★新しいカードを作らない。**

### 1-1. ★3つの場合を、それぞれ違う文言で出す（★どれも空欄にしない）
| 場合 | **出す文言（★この意味を変えない）** |
|---|---|
| **(a) `dropped_before` が1件以上在る** | **「記録できた取りこぼし: N 件」** ＋ §1-2 の併記 |
| **(b) run は引けたが `dropped_before` が0件** | **「記録できた取りこぼし: 0 件」** ＋ **§1-2 の併記（★必須）** |
| **(c) ★run_id が無く run を引けない** | **「取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）」** ＋ §1-2 の併記 |

> **★(c) を「0件」と出さない。** **取れないことと、0であることは違う。**
> **★実測で (c) は実在する**: `TASK-2DER-B9B4DA3B` は `etrace_run_id = None`（CLI 投入・`G-36`）。

### 1-2. ★併記文言（MGR 逐語。★1文字も変えない・★どの場合でも必ず出す）
```
これは★記録できた取りこぼしの件数である。
取りこぼしの後に成功した記録が無ければ、件数そのものが残らない（G-38）
```

### 1-3. ★禁止（MGR 裁定 §3）
| # | 禁止 |
|---|---|
| **1** | **★「取りこぼしなし」「問題なし」と読める文言にしない** |
| **2** | **★緑・OK 色にしない**（`ok` クラスを使わない）。**★0 は良い知らせではない** |
| **3** | **★赤・警告色にもしない**（`bad` を使わない）。**0 でも非0でも、これは★事実であって評価ではない** |
| **4** | **`G-38` を塞ごうとしない**（fail-open は Taka の撤回により現状が正） |
| **5** | **★実例を作らない**（`emit` をわざと失敗させない） |

## 2. ★データの取り方（新しい取得を足さない）
```
再現（実測済）: dropped_before は event レコードそのものに載る（ds/ds/etrace.py:117）
∴ UI は /api/resolve?id=ETR-… を引いた時点で★既に持っている。
∴ 新しい fetch を1つも足さない。events を数えるだけ:
     N = Σ (event.dropped_before or 0)
```
- **★`truncated` が true の run では「取得した範囲での合計である」と併記すること**（全件見ていない可能性があるため）。

## 3. ★変えないもの
- **`webui.py` の Python 側を1行も変えない。** **endpoint を足さない。** **`ids.py` を変えない。**
- **既存カードを1つも消さない。** **「通過した処理」カードに入れない**（そこは「起きたこと」の場所）。
- **Event Trace の record を増やさない**（`emit` に触らない）。
- **勘定科目に触れない**（`G-62`/`G-64`〜`G-68`・据え置き）。

## 4. 受入（★投入なし。既存4 run で足りる）
| # | 条件 | 使うもの |
|---|---|---|
| **1** | **(b) の表示が出る**（0件＋併記文言） | `ETR-0f6fae35665a`（実測: dropped_before 0件） |
| **2** | **★併記文言が画面に出ること**（MGR 逐語と一致） | 同上。**★文言を貼って示す** |
| **3** | **(c) の表示が出る**（「取得できない」と出る。★0件と出ない） | **`TASK-2DER-B9B4DA3B`**（`etrace_run_id = None`・実測済） |
| **4** | **`ok`/`bad` クラスを使っていない** | **★差分を貼って示す**（D-50 と同じ検算） |
| **5** | 既存が壊れていない | `/api/state` の sha256 が実装前後で一致（Python を変えないので当然だが★実測で示す） |
| **6** | 非回帰 | 基準 91 passed / 7 failed・顔ぶれ diff |

### 4-1. ★予測（賭ける所と賭けない所）
| 予測 | 根拠 |
|---|---|
| **受入1・3 が通る方に賭ける** | どちらも私が front door で実測した（0件／`etrace_run_id=None`） |
| **(a) の表示** | **★確かめられない。** **実例が0件であり、作らない**（`G-38` と同じ扱いで未検証のまま登記する） |

## 5. ★止まってよい場所
| # | 条件 |
|---|---|
| 1 | **`/api/resolve` の返りに `dropped_before` を数える経路が無かった** → **★私の見立てが外れた。報告** |
| 2 | **JS だけでは足りず Python を変える必要が出た** → **★止めて報告**（新しい口に当たりうる） |
| 3 | **(c) の判定が `etrace_run_id` だけでは足りなかった** → 報告 |
| 4 | SPEC が2通りに読める |

## 6. ★未確認（引き継ぐ）
1. **(a)（非0）の表示は★検証できない**（実例0件・作らない）。**★`G-61` と同じ扱いで、未検証のまま登記する。**
2. **画面をブラウザで見ていない**（D-45 から続く）。
3. **`stderr` に落ちた実例が在るかを見ていない**（プロセスの標準エラー）。

---
*CC-α D-55 BUILD SPEC v1.0（実装源・宛 IMPL）。★一行=**数字を出すのではなく数字の限界を出す**——「取りこぼし 0件」は「失われていない」ではなく「**失われたことを記録できた件数が0**」である。★実装は `/command` の JS のみで Python 側を1行も変えず、既存の「欠損・失敗・未確認」カードの中に**3つの場合をそれぞれ違う文言で出す**（(a)1件以上=「記録できた取りこぼし: N 件」／(b)run は引けたが0件=「記録できた取りこぼし: 0 件」／**(c)run_id が無く引けない=「取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）」**）——**(c) を「0件」と出さない。取れないことと0であることは違う**（実測で (c) は実在: `TASK-2DER-B9B4DA3B` は `etrace_run_id=None`）。**併記文言は MGR 逐語を1文字も変えず、どの場合でも必ず出す**（「これは記録できた取りこぼしの件数である。取りこぼしの後に成功した記録が無ければ、件数そのものが残らない（G-38）」）。★禁止=「取りこぼしなし」「問題なし」と読める文言にしない／**緑・OK 色にしない（0 は良い知らせではない）**／**赤・警告色にもしない（0 でも非0でも事実であって評価ではない）**／`G-38` を塞がない／実例を作らない。★データは新しい取得を足さず、`dropped_before` は event に載る（`etrace.py:117`）ので `/api/resolve` を引いた時点で既に持っており `N = Σ(event.dropped_before or 0)` を数えるだけ。**`truncated` の run では「取得した範囲での合計である」と併記する**。★受入6件は**投入なし**（(b)=`ETR-0f6fae35665a`／**併記文言の一致を貼って示す**／(c)=`TASK-2DER-B9B4DA3B`／`ok`/`bad` クラスを使っていないことを差分で示す／既存の sha256 一致／非回帰 91/7）。★予測=受入1・3 が通る方に賭ける（どちらも front door で実測済）が、**(a) の表示は確かめられない**（実例0件で作らない。`G-38` と同じ扱いで未検証のまま登記）。★止まってよい場所4件（数える経路が無かった＝私の見立てが外れた／JS だけでは足りず Python が要る＝新しい口に当たりうる／(c) の判定が `etrace_run_id` だけでは足りない／2通りに読める）。★未確認=**(a) の表示は検証できない（`G-61` と同じ扱い）**／画面をブラウザで見ていない／`stderr` の実例を見ていない。*
