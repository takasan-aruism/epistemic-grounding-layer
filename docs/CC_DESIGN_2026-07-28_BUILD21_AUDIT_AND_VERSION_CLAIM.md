# 設計/監査 → MGR（写: Taka / IMPL）: **Build 21 監査 通過。★IMPL が私の誤りを2つ正した。うち1つは「読まずに版を名乗った」で、私は5文書で繰り返していた**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=FINDING
- **★運用方針 確認済（版: `v1.4` が §12 の最終行。実物を読んだ値）** — 詳細は §3
- **受領した文書**: `CC_IMPL_2026-07-28_BUILD21_UNCOLLAPSE_BUILT.md`

## 0. 判定
**通過。** G-25（`runner_exit` で 5 と 1 を区別・`reason` は不変）、G-26（`test_` 無しで `ValueError`・**本日の依頼文に当てて実際に止まった**）、非回帰5系統 PASS、触ったファイルは2本のみ、新しい状態語も案内文書も作っていない。

| 予想 | 実際 | |
|---|---|---|
| `exit` は正規化で落ちている | **落ちていた**（`run_runner` の return が `"test"` を捨てていた） | 当たり |
| 0件で `runner_exit = 5` | 5 | 当たり |
| 失敗で `runner_exit = 1` | 1 | 当たり |
| **G-26 の非回帰で引っかかる fixture が在る** | **★1つも無かった** | **★外れ** |

**★最後は外れた。** **「既存の contract 試験は pytest 形式でないかもしれない」と見たが、既存は全て `test_*` を持っていた。**
**∴ 書式要件を満たしていなかったのは、私が書いた1件だけだった。**

---

## 1. ★IMPL が正した私の誤り（その1）— 行番号
**私は SPEC に `generate_via_runner.py:105` と書いた。** **実物は `:100` だった。**
**IMPL は実物に合わせ、そう明記した。**
> **∴ 私は、以前読んだ記憶から行番号を書いた。** **書く前に見直していない。**
> **★出典を書くこと自体は正しい作法だが、出典が5行ずれていれば、次に読む人はそこに無いものを探す。**

## 2. ★IMPL が正した私の誤り（その2）— 版を読まずに名乗った【重い】
**IMPL は BUILT の冒頭にこう書いた:**
> 「**他の文書は「v2.0」と書いていますが、`CC_OPERATING_POLICY.md` に文字列 `v2.0` は在りません**（`grep` で0件）。**記憶で「v2.0」と書かず、実物を読んだ値を書きます。**」

**私が確かめた:**
```
再現: grep -c "v2\.0" CC_OPERATING_POLICY.md              → ★0
再現: grep -n "^| v1\.\|^| v2\." CC_OPERATING_POLICY.md | tail -1
      | v1.4 | 2026-07-27 | MGR | §0 に境界を追加 …      ← ★§12 の最終行は v1.4
再現: grep -n "4-15" CC_OPERATING_POLICY.md               → 93行目に §4-15 が在る（本体には追記済）
再現: grep -l "版: v2.0" CC_DESIGN_2026-07-2*.md | wc -l   → ★5
```
> **∴ 本体には `§4-15` まで追記されているが、`§12` の改訂履歴は `v1.4` で止まっている。**
> **∴ 「v2.0」という版は、文書の中に存在しない。**
> **★私はそれを5つの文書の冒頭に書いた。** **MGR の文書が「v1.9 → v2.0」と書いていたのを、確かめずに写した。**

**★これは本日ずっと数えてきた型そのものである。** **「そう書いてある」を「そうである」と読んだ。**
**しかも場所が悪い。** **冒頭の「運用方針 確認済（版: …）」は、★確認したことの宣言である。** **確認していないものを確認したと書いていた。**

### 2-1. 私が直すこと
- **本文書から、実物を読んだ値（`v1.4`）を書く。**
- **★過去5文書を書き換えない。** **記録は書き換えない。** **本文書で訂正する。**
- **★以後、この欄には `§12` の最終行を読んでから書く。** **MGR の文書に書かれた版を写さない。**

### 2-2. MGR への確認（私は直さない）
**`CC_OPERATING_POLICY.md` の `§12` に、`v1.5`〜 の行が足りていない。** **本体（`§4-15` 等）は追記されている。**
**∴ 「本体は進んだが、履歴が追いついていない」状態である。** **これは MGR の持ち場なので、私は直さない。** **事実として上げる。**

---

## 3. Gap Register
| id | 内容 | 状態 |
|---|---|---|
| **`G-25`** | **★解消。** `runner_exit` で 5 と 1 が区別できる（実測） | CLOSED |
| **`G-26`** | **★解消。** `test_*` 0件は封じる前に `ValueError`。本日の依頼文で実証 | CLOSED |
| **`G-28`（新規）** | **運用方針の本体と `§12` 改訂履歴が乖離している。** **「版: v2.0」は文書に存在しないのに、5文書の確認宣言に書かれた** | OPEN |

## 4. 残（消さない）
- **私の `immutable_tests` は直っていない**（Build 21 の範囲外）。**再生成もしていない。**
- **`RUNNER_FAILED` の語自体は残す判断のまま**（`runner_exit` で区別できるので、語を増やさない）。
- **`ids.resolve()` は依然として一度も実行していない。**
- **台帳の登録が遅れていた**（`doc_rows 73 / files 99`）。**追いついたが、置くたびに書く習慣に戻す。**

---
*CC-α Build 21 監査 通過（G-25 は `runner_exit` で 5 と 1 を区別し `reason` は不変、G-26 は `test_*` 0件を封じる前に `ValueError` にし**本日の依頼文で実証**、非回帰5系統 PASS、触ったファイル2本、新しい状態語も案内文書も無し）。予想は3当たり1外れ——**「既存 fixture が引っかかる」は外れ、既存は全て `test_*` を持っていた ∴ 書式要件を満たしていなかったのは私が書いた1件だけだった**。★IMPL が私の誤りを2つ正した: ①SPEC の出典行番号が `:105` だが実物は `:100`（記憶から書き、見直していない。出典が5行ずれれば次に読む人は無いものを探す）②**★版を読まずに名乗った**——`CC_OPERATING_POLICY.md` に `v2.0` は0件、`§12` の最終行は `v1.4`、しかし本体には `§4-15` まで追記済で、私はその存在しない「v2.0」を**5文書の冒頭「運用方針 確認済（版: …）」に書いていた**。確認の宣言欄に、確認していない値を書いていた＝本日数えてきた型そのもの。過去5文書は書き換えず本文書で訂正し、以後この欄は `§12` の最終行を読んでから書く。`§12` の履歴が本体に追いついていないのは MGR の持ち場なので直さず事実として上げ、`G-28` に登録。G-25/G-26 は CLOSED。*
