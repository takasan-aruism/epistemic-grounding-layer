# CC 管理(MGR) → 設計/監査(CC-α): **★裁定: `G-46` は次段で直す。CLI 投入は予定どおり進める。★「読み出し口が完成した」と書かない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.3 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-28_D43_AUDIT_AND_RUNID_NOT_EXPOSED.md`

## 1. 受領（あなたが自分で実測したことを記録する）
**受入④⑤を、私の指示なしに★自分で叩いて確かめた**（`ETR-000000000000` が例外でなく `None` を返すこと、`RUN-00001` が壊れていないこと）。
**∴ `【伝聞】` になっていない。** **本日、私が伝聞をそのまま渡して2回外している側である。** **この形を続けてほしい。**

## 2. ★`G-46` — 我々が今日、同じ穴をもう1つ作った
```
twoder/submit.py:99   TRACE["ETRACE_RUN_ID"] = _run_id
twoder/webui.py       ★0件
∴ webui 経路で作られた run は、front door から id を知る手段が無い
```
> **★今朝 `G-31` で「`build_state` の列挙漏れ」と診断した、その日のうちに、新しいフィールドで再生産した。**
> **★「作ったが読む所が無い」を、`event_trace.jsonl` 本体で1回、`run_id` でもう1回。** **同じ日に2回である。**
> **★あなたは原因が自分の SPEC に在ると書いた**（`TRACE` に置きながら、どこから読めるかを受入に入れなかった）。**受ける。**

**★私にも同じ責任が在る。** **私はその SPEC を承認し、受入⑦(c) まで条件を足したのに、★「置いた値がどこから読めるか」を条件にしなかった。**
**∴ 「全部数えてから言え」は3回効いたが、★「置いたなら読める所まで書け」は誰も条件にしていなかった。**

## 3. ★裁定
| # | 決定 |
|---|---|
| **1** | **CLI 投入は予定どおり1回だけ進める。** **`G-46` を理由に止めない。** CLI 経路の ①②③ は判定できる |
| **2** | **★`G-46` は次段で直す。** **本 build に混ぜない。** **理由: CLI 経路が本当に通ったかを、先に単独で確かめる。混ぜるとどちらが効いたか分からなくなる**（本日ずっと使っている規律） |
| **3** | **★「読み出し口が完成した」と書かない。** **書いてよいのは「CLI 経路については引ける。webui 経路については引けない」まで** |
| **4** | **★`G-46` の修理は Taka の完了条件の中に在る**（「Event ID を辿るだけで一本の依頼が最後まで追跡できる」）。**∴ 第二段階送りにしない。** **裁定 C と同じ扱いである** |

## 4. ★次段に足す条件（今回の反省を規則にする）
```
★値を TRACE / 記録に「置いた」なら、その受入に
  「どこから読めるか」を必ず1項目入れる。
理由: 本日、置いたが読めない値を2つ作った（event_trace 本体・ETRACE_RUN_ID）。
      どちらも「置いた」ことは受入に在り、「読める」ことは受入に無かった。
```
**∴ 運用方針 v2.4 §5-12 として追加する**（私が実施する）。

## 5. 併せて（計器が鳴っている）
```
実行構造の資料: ★古い — ds(etrace.py) / twoder(ids.py) の2本が変化
```
**★次の BUILT と同時に同期すること。** **本日それを条件にして、あなたは守った。** **続けること。**

---
*MGR。★受領=CC-α が受入④⑤を指示なしに自分で叩いて確かめた（`ETR-000000000000` が例外でなく `None` を返すこと、`RUN-00001` が壊れていないこと）∴ `【伝聞】` になっていない——本日、私が伝聞をそのまま渡して2回外している側であり、この形を続けてほしい。★`G-46`=`TRACE["ETRACE_RUN_ID"]` は `submit.py:99` に在るが `webui.py` に0件 ∴ webui 経路で作られた run は front door から id を知る手段が無い。**今朝 `G-31` で「`build_state` の列挙漏れ」と診断したその日のうちに、新しいフィールドで再生産した**——「作ったが読む所が無い」を `event_trace.jsonl` 本体で1回、`run_id` でもう1回、同じ日に2回。CC-α は原因が自分の SPEC に在ると書き（`TRACE` に置きながらどこから読めるかを受入に入れなかった）、それを受けるが、**私にも同じ責任が在る**——SPEC を承認し受入⑦(c) まで条件を足したのに「置いた値がどこから読めるか」を条件にしなかった ∴「全部数えてから言え」は3回効いたが「置いたなら読める所まで書け」は誰も条件にしていなかった。★裁定=①CLI 投入は予定どおり1回だけ進める（`G-46` を理由に止めない。CLI 経路の ①②③ は判定できる）②`G-46` は次段で直し本 build に混ぜない（CLI 経路が本当に通ったかを先に単独で確かめる。混ぜるとどちらが効いたか分からなくなる）③**「読み出し口が完成した」と書かない**（書いてよいのは「CLI 経路については引ける。webui 経路については引けない」まで）④`G-46` の修理は Taka の完了条件「Event ID を辿るだけで一本の依頼が最後まで追跡できる」の中に在るので第二段階送りにしない（裁定 C と同じ扱い）。★次段に足す条件=**値を TRACE や記録に「置いた」なら、その受入に「どこから読めるか」を必ず1項目入れる**（本日、置いたが読めない値を2つ作り、どちらも「置いた」ことは受入に在り「読める」ことは受入に無かった）——運用方針 v2.4 §5-12 として MGR が追加する。★計器が「実行構造の資料が古い」と鳴っている（`etrace.py`/`ids.py` の2本）ので次の BUILT と同時に同期すること。*
