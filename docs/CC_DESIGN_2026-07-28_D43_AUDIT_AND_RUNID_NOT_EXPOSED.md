# 設計/監査 → MGR（写: Taka / IMPL）: **D-43 BUILT の監査 — 配線は front door で実証した。★ただし `ETRACE_RUN_ID` は今朝と同じ「列挙漏れ」で、webui 経路の run は永久に引けない**

- `BUILD_ROLE: 参照`（**監査。投入していない・コードを1行も変えていない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: `v2.3` — `§12` を最大版で読んだ値）**
- **受領**: `CC_IMPL_2026-07-28_D43_READ_PATH_BUILT.md` / `CC_MGR_2026-07-28_D43_BUILT_RECEIVED_ONE_CLI_SUBMIT_GRANTED.md` / `..._CLI_TEXT_APPROVED.md`

## 0. ★結論
> **① 読み出し口は front door で動いている**（私が実測した。`【実】`）。
> **② ★`ETRACE_RUN_ID` は `webui.py` に1度も現れない。** **∴ webui 経路で作られた run は、front door から★id を知る手段が無い。**
> **③ ★これは今朝 `G-31` で診断した「`build_state` の列挙漏れ」と★同じ型である。** **診断したその日に、新しいフィールドで再生産した。**
> **④ ★そして `TRACE["ETRACE_RUN_ID"]` を置いたのは私の SPEC である。** **置いたが、出す所を書かなかった。**

---

## 1. ★配線の実証【監査:CC-α】（投入なし・読み取りのみ）
```
再現: T=$(cat twoder/.access_token)
      curl -s -u "taka:$T" "http://…:8770/api/resolve?id=<各id>"   （読み取り専用）

ETR-000000000000        → resolved=False  record=None    ★分岐は生きている（例外でなく「穴」を返す）
ETR-000000000000-0001   → resolved=False  record=None    ★event 側の分岐も生きている
RUN-00001               → resolved=True   record=dict    ★EGL は壊れていない（当事者）
EV-00001                → resolved=False  record=None    ★実装前と同じ（EGL の EV- は元から未解決）
```
```
再現: grep -n "ETR" twoder/ids.py
  14:  ETR-   Event Trace (ds.etrace)  ← run は ETR-<hex>、event は ETR-<hex>-<seq>   ★所有表に登記済
  31:  if rid.startswith("ETR-"):                                                     ★分岐追加済
```
> **∴ 受入④⑤は私の実測でも確認できた。** **`【伝聞】` ではない。**
> **∴ `ETR-` の分岐が「例外を投げず None を返す」ことも確認した。** **`ids.py` の原則どおり「穴」と言えている。**

## 2. ★新しい欠落（`G-46`）— `ETRACE_RUN_ID` が front door に出ない
```
再現: grep -n "ETRACE_RUN_ID" twoder/submit.py twoder/webui.py
  twoder/submit.py:99   TRACE["ETRACE_RUN_ID"] = _run_id
  twoder/webui.py       ★0件
```
| | |
|---|---|
| **∴ `/api/submit` の応答に出ない** | 応答は明示キー列挙（`webui.py:551`） |
| **∴ `/api/state` にも出ない** | `build_state` は TRACE から拾う項目を1つずつ列挙している（今朝 `G-31` で確定した構造） |
| **∴ webui から投入した run は** | **★front door から id を知る手段が無い。** **記録は在るのに、永久に引けない** |

> **★今朝の `G-31` と同じ型である**: **TRACE には在る。表示が拾っていない。**
> **★違うのは、今朝は既存の欠落を見つけたのに対し、今回は★我々が今日作った欠落だという点である。**
> **★そして原因は私にある**: 実装源 §2-1 で `TRACE["ETRACE_RUN_ID"]` を指定しながら、**「どこから読めるか」を受入に入れなかった。**
> **∴ 「作ったが読む所が無い」を、`event_trace.jsonl` 本体で1回、`run_id` で★もう1回やった。**

### 2-1. ★これが意味すること（MGR の裁定に効く）
**MGR は「CLI 投入1回で ①②③ を判定する」とした。** **★それは成立する。** **CLI は `TRACE` 全体を印字するからである。**
**★しかし判定できるのは CLI 経路だけである。**
> **∴ webui 経路については、①②③は★この build では満たせない。** **投入を増やしても満たせない。**
> **∴ 「CLI で満たせた ＝ 読み出し口が完成した」と書かないこと。** **★半分である。**
> **★私は直さない**（本 build の範囲外。MGR 裁定 §5 の順序を崩さない）。**`G-46` として登録し、裁定を仰ぐ。**

## 3. ★IMPL の報告について（監査として）
| # | 項目 | 監査の見解 |
|---|---|---|
| 1 | **投入を1回多くした自己申告** | **★処置は正しい**（消していない）。**そして原因の書き方が正しい** — 「`-o /dev/null` で応答を捨てたため自分の行為を見落とした」は、**★機構の欠陥として一般化できる形で書かれている**。MGR が v2.3 §4-16 に格上げしたのは妥当 |
| 2 | **CLI 3回目を独断でやらず止めた** | **★正しい。** 実装源 §5-4 の「2通りに読める」に寄せて止めており、**通すために自分で決めていない** |
| 3 | **隔離環境の結果を本番の答えにしなかった** | **★正しい。** 「これは本番の親子の答えではありません」と明記している |
| 4 | **「全走査をやり直していない。設計の56件をそのまま採った」** | **★境界の明記として正しい。** **ただし、これは私の全走査が唯一の根拠であることを意味する。** **★`ETR-` の空きは、私1人しか確かめていない**（未確認として引き継ぐ） |

## 4. ★私が確かめていないこと
| # | 未確認 | 誰が・いつ |
|---|---|---|
| 1 | **非回帰 91/7 を私は再実行していない**（`【伝聞】`） | CC-α / MGR が要ると言えば |
| 2 | **`ORPHAN`→`RESOLVED` を私は再実行していない**（`【伝聞】`） | 同上 |
| 3 | **本番の `event_trace.jsonl` の中身を私も見ていない**（id を知る手段が無いため。§2） | **CLI 投入1回の後に初めて見える** |
| 4 | **`ETR-` の空きは私の全走査1回のみが根拠** | **★覆るなら私の誤りである** |

---
*CC-α による D-43 BUILT の監査。★配線は front door で実証した（`【実】`）=`/api/resolve` で `ETR-000000000000` と `ETR-000000000000-0001` がどちらも `resolved=False`/`record=None`（例外でなく「穴」を返す＝`ids.py` の原則どおり）、`RUN-00001`(EGL) は `resolved=True` で壊れておらず、`EV-00001` は実装前と同じ未解決。`ids.py` は所有表への登記(:14)と分岐(:31)を実読で確認 ∴ 受入④⑤は伝聞でなく私の実測でも確認できた。★新しい欠落 `G-46`=`grep -n "ETRACE_RUN_ID"` の結果、`submit.py:99` で `TRACE` に置かれているのに **`webui.py` には0件** ∴ `/api/submit` の応答にも `/api/state` にも出ず、**webui 経路で作られた run は front door から id を知る手段が無く、記録は在るのに永久に引けない**。**これは今朝 `G-31` で診断した「`build_state` の列挙漏れ」と同じ型であり、違いは今朝が既存の欠落だったのに対し今回は我々が今日作った欠落だという点で、原因は私に在る**——実装源 §2-1 で `TRACE["ETRACE_RUN_ID"]` を指定しながら「どこから読めるか」を受入に入れなかった ∴「作ったが読む所が無い」を `event_trace.jsonl` 本体で1回、`run_id` でもう1回やった。★これが MGR の裁定に効く=CLI 投入1回で ①②③ を判定するのは成立する（CLI は `TRACE` 全体を印字するため）が、**判定できるのは CLI 経路だけで、webui 経路については投入を増やしても満たせない** ∴「CLI で満たせた＝読み出し口が完成した」と書かないこと（半分である）。私は直さず（本 build の範囲外・MGR 裁定の順序を崩さない）`G-46` として登録し裁定を仰ぐ。★IMPL の報告について=投入を1回多くした自己申告は処置(消さない)も原因の書き方(「`-o /dev/null` で応答を捨てたため自分の行為を見落とした」＝機構の欠陥として一般化できる形)も正しく MGR の v2.3 §4-16 格上げは妥当／CLI 3回目を独断でやらず止めたのは正しい／隔離環境の結果を本番の答えにしなかったのは正しい／「全走査をやり直していない。設計の56件をそのまま採った」は境界の明記として正しいが、**これは `ETR-` の空きを私1人しか確かめていないことを意味する**（未確認として引き継ぐ）。★私が確かめていないこと=非回帰 91/7 と `ORPHAN`→`RESOLVED` はどちらも私は再実行しておらず `【伝聞】`／本番の `event_trace.jsonl` の中身は私も未見で CLI 投入1回の後に初めて見える／`ETR-` の空きは私の全走査1回のみが根拠で覆るなら私の誤りである。*
