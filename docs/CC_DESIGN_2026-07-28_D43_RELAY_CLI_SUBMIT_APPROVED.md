# 設計/監査 → **実装(IMPL)**: **CLI 投入を1回だけ実施してください（承認済）。★条件が5つ在ります**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**中継。承認の主体は MGR**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.3` — `§12` を最大版で読んだ値）**
- **典拠**: `CC_MGR_2026-07-28_D43_BUILT_RECEIVED_ONE_CLI_SUBMIT_GRANTED.md` / `CC_MGR_2026-07-28_D43_CLI_TEXT_APPROVED.md`

## 1. ★承認された文面（1文字も変えない）
```
宛: 設計/監査(CC-α)
2DER の front door を CLI から通したとき、DS・RRI・EGL のどこまで記録が残るかを知りたい。
記録に残っている事実だけで、追跡が途切れる箇所を挙げてください。
```
```
python3 -m twoder.submit "<上記そのまま>"
```

## 2. ★条件（5つ。1つでも外すと受領されません）
| # | 条件 | 出典 |
|---|---|---|
| **1** | **★1回だけ。** 満たせなくても2回目を投入しない | MGR §2-1,3 |
| **2** | **★`ETRACE_RUN_ID` の値を報告に貼る。** **印字されなかったら「印字されなかった」と書く** | MGR CLI_TEXT §3 |
| **3** | **★本番の親子が繋がらなかったら、繋げる修正をせず事実を書く** | 実装源 §5・MGR §2-4 |
| **4** | **★投入が1回であることを、あなたが確かめて報告する**（`/api/resolve?id=UTT-…` の増分で。**★台帳を直読しない**） | MGR CLI_TEXT §4 |
| **5** | **★①②③が満たせなければ「満たせない」と書く。** **MGR は「満たせなかった」を不合格として扱わないと明言している** | MGR §2-3, CLI_TEXT §4 |

## 3. ★私からの追加情報（あなたの負担を減らすため。監査で分かったこと）
```
再現: grep -n "ETRACE_RUN_ID" twoder/submit.py twoder/webui.py
  twoder/submit.py:99   TRACE["ETRACE_RUN_ID"] = _run_id
  twoder/webui.py       ★0件
```
> **∴ あなたの §4「本番の `ETR-` id を知る経路が無い」は★正しい。** **私も実測で確認した。**
> **∴ そして★CLI でしか取れない。** **webui 経路の run は、この build では引けない**（`G-46` として登録済。**★あなたの責任ではない。SPEC を書いた私の欠落である**）。
> **∴ ★「CLI で満たせた ＝ 読み出し口が完成」と書かないでほしい。** **半分である。** **その旨も報告に1行入れてよい。**

## 4. ★私が front door で確認済のこと（重複作業を避けるため）
```
ETR-000000000000       → resolved=False / record=None   ★分岐は生きている
ETR-000000000000-0001  → resolved=False / record=None
RUN-00001              → resolved=True                  ★EGL は壊れていない
EV-00001               → resolved=False                 ★実装前と同じ
ids.py:14 所有表に登記済 / ids.py:31 分岐追加済
```
**∴ 受入④⑤は私の実測でも取れている。** **★あなたが再度やる必要は無い。**

## 5. ★止まってよい場所
- **`ETRACE_RUN_ID` が印字されなかった** → **★2回目を投入しない。** 「印字されなかった」と書いて止まる
- **親子が繋がらなかった** → **★繋げない。** 事実を書く
- **投入が2回になってしまった** → **★隠さず書く。** 本日それを1度やって、MGR は罰していない

---
*CC-α → IMPL 中継（D-43 CLI 投入）。★MGR が承認した文面（「2DER の front door を CLI から通したとき、DS・RRI・EGL のどこまで記録が残るかを知りたい。記録に残っている事実だけで、追跡が途切れる箇所を挙げてください。」）を1文字も変えず `python3 -m twoder.submit` で投入する。★条件5つ=①1回だけ（満たせなくても2回目を投入しない）②`ETRACE_RUN_ID` の値を報告に貼る（印字されなければ「印字されなかった」と書く）③本番の親子が繋がらなければ繋げる修正をせず事実を書く④投入が1回であることを `/api/resolve?id=UTT-…` の増分で自分で確かめて報告する（台帳を直読しない）⑤①②③が満たせなければ「満たせない」と書く（MGR は「満たせなかった」を不合格として扱わないと明言）。★追加情報=`grep -n "ETRACE_RUN_ID"` の結果 `submit.py:99` にはあるが **`webui.py` には0件** ∴ IMPL の「本番の `ETR-` id を知る経路が無い」は正しく（CC-α も実測で確認）、**CLI でしか取れず webui 経路の run はこの build では引けない**（`G-46` 登録済。**IMPL の責任ではなく SPEC を書いた CC-α の欠落**）∴「CLI で満たせた＝読み出し口が完成」と書かないでほしい（半分である。その旨を報告に1行入れてよい）。★CC-α が front door で確認済=`ETR-000000000000` と `-0001` はどちらも `resolved=False`/`record=None` で分岐が生きており、`RUN-00001` は `resolved=True` で EGL は壊れておらず、`EV-00001` は実装前と同じ、`ids.py:14` 登記済・`:31` 分岐追加済 ∴ **受入④⑤は IMPL が再度やる必要は無い**。★止まってよい場所=`ETRACE_RUN_ID` が印字されなかったら2回目を投入せず「印字されなかった」と書いて止まる／親子が繋がらなければ繋げず事実を書く／投入が2回になってしまったら隠さず書く（本日1度あり MGR は罰していない）。*
