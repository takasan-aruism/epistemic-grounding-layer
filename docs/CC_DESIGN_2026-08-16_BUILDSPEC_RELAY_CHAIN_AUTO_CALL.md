# 【BUILD SPEC】★`relay_chain` を **自動で 呼ぶ** ―― ★★足場 1本（★新部品 0 ／ 新口 0）

宛: MGR ／ 写: IMPL ／ 発: DESIGN ／ 2026-08-16 15:0x ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **MGR 14:55**（★『置いただけ』を 止める ／ ★受入 3つ ／ ★増やさない）

---

## 0. ★★これは 契約では ない（★先に 書く）

```
★★部品は ★既に 在る（`twoder/relay_chain.py`）／ ★★v2 の 契約は 別に 出した
★★★ここで 要るのは ★足場 1本 ＝ ★★記録から 材料を 組んで ★渡すだけ
★★∴ ★封印試験は 付けない（★足場は 2DER が 書く 部品では ない＝★Claude が 書く 側）
★★★書く 人 = ★MGR ／ ★行数は 報告する ／ ★★2DER の 実績に 数えない（★規律）
```

## 1. ★★既存の 形を そのまま 使う（★見本＝`_segments_from_records_view`）

**★`twoder/observed_edges.py:51-76` が ★同じ 事を している。★その 形を 写す。**

```
★①`evs`（★記録の 全行）を 受け取る
★②行を 均して 2DER の 部品へ 渡す
★③`_use(部品名, 関数, 引数…)` で 呼ぶ（★★呼んだ 事が 両側に 残る＝★`parts_auto_called` が 数える）
★④`out["…"] = _…_view(evs)` で ★既存の include に 欄を 1つ（★新口 0）
```

## 2. ★★材料の 組み方（★★記録の どの 欄から 採るか・★1つずつ）

```
★★`sends`     = ★記録の 行に ★`handed_to` が 在る 物
   ―― ★`from` = ★その行の `component.function` ／ ★`to` = ★`handed_to` の 値
   ―― ★★`run_id` = ★★★その行の `run_id` を そのまま（★★★無ければ None の まま 渡す＝★埋めない）

★★`receives`  = ★記録の `inputs` に ★`received_from` が 在る 物
   ―― ★`from` = ★`received_from` の 値 ／ ★`to` = ★その行の `component.function`
   ―― ★★`run_id` = ★★同上（★埋めない）

★★`locators`  = ★同じ 行の `inputs.at`（★★実装が 自分で 名乗った 位置だけ）
   ―― ★★★候補を 入れない ／ ★★推測で 埋めない（★無ければ 入れない）

★★`expected`  = ★`twoder/route_table.ROUTE` ＋ ★採用行（★`from` `to` の 2欄だけ）
   ―― ★★これは 循環しない（★★★経路が 在るか どうかを 探すのでは なく
      ★★『★経路表が 期待する 区間が ★実際に 結合したか』を 見る＝★★向きが 逆）
```

**★★★埋めない こと が 要（★`run_id` を None の まま 渡す）** ―― ★埋めると ★v2 の 修理が 無意味に なる。

## 3. ★★受入（★MGR の 3つ ＋ ★私から 2つ・★★全部 数）

```
★★①`parts_auto_called` の 『自動で 呼ばれている』= ★★5 → ★★6
★★②`linked` が ★★欄として 出る（★★人が 呼ばない＝★front door から 引ける）
★★★③出てはいけない 結果 = ★★『★`run_id` が 空／無い 区間が ★`linked` に 数えられない』
   ―― ★★確かめ方 = ★★★`linked` の 行を 全部 見て ★`run_id` が ★空の 物が ★★0件
   ―― ★★これが ★規律 v1.21 の 最初の 適用（★★否定側を 数で 書く）

★★★④（★私）★`by_status` の 6語が ★全部 出る（★★0件でも）
★★★⑤（★私）★★`checked`（★expected の 数）を 併記 ＝ ★★★分母を 隠さない
   ―― ★★`linked` N / `checked` M の 形で 書く（★★N だけを 報告しない）
```

## 4. ★★★出る 数について（★予想を 固定しない）

```
★★私の 見立て = ★★`split_run` が 多く 出る（★理由＝★`handoff_emit` は `run_id` を 渡していない＝★実測 0件）
★★★但し ★これを ★受入に しない ―― ★★出た 数が 事実 ／ ★見立ては 事実では ない
★★★『split_run が 多い』が 出たら ―― ★★それは 欠陥では なく ★★★次の 1手の 名前
   （＝ ★走行の 番号を 両側に 載せる＝★MGR の 順序 ②）
★★★『linked が 増えた』が 出たら ―― ★★★まず ③を 疑う（★★偽の linked を 1回 出した 実績が 在る）
```

## 5. ★★やらないこと

```
★★★`run_id` を 埋めない ／ ★★推測しない
★★★`handed_to` から ★新しい 経路を 作らない（★★これは 検査であって 発見では ない）
★★部品を 直さない（★★v2 の 契約で 直す＝★★足場では 直さない）
★★★`linked` の 数だけを 報告しない（★★分母と ★③を 必ず 添える）
```
