# BUILD SPEC — 継続中タスクを集約して出す v1.0（★実装源）

- `BUILD_ROLE: ★実装源`（**本文書が実装の唯一の典拠**）
- **★宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-29_D72_RULING_TRUNCATE_AND_SAY_SO.md`
- **典拠**: Taka 逐語（4区分・欠落5件） / **実測**: D-70 / D-71 / D-72

---

## 0. ★本 build の一行
> **155行の ID を、★5行の件数にする。**
> **★開いたときも「長文の壁」に変えない。** **★出せないものは「出せない」と理由つきで書く。**

## 1. ★既定（開く前）— ★ID を1件も出さない
```
継続中の別タスク  合計 155件
  停止中                  2件
  依存待ち               18件
  背景保持              117件
  現在前面化すべきもの   ★出せません（前面化を決める値が 2DER に無いため）
  どの区分にも入らない   18件（誰も進められない状態）
```
| # | 条件 |
|---|---|
| 1 | **★ID を1件も出さない**（いまは155行 出ている） |
| 2 | **★「現在前面化すべきもの」を消さない。「0件」とも書かない。** **★「出せません」＋理由** |
| 3 | **★「その他」と書かない。** **★「どの区分にも入らない」と書く**（「その他」は中身を隠す） |
| 4 | **★合計を出す。** **★各区分の和が合計と一致すること**（§5-1） |

### 1-1. ★区分の判定（★すべて決定論。LLM を使わない）
```
停止中              : dw_state == "BLOCKED"
依存待ち            : claude_barrier == True
背景保持            : claude_barrier == False かつ actor_role が "-" でない
どの区分にも入らない : claude_barrier == False かつ actor_role == "-" かつ dw_state != "BLOCKED"
現在前面化すべきもの : ★判定しない（値が無い）
```
> **★これは D-71 の実測（155件 全数）から導いた。** **★推測を1つも含まない。**

## 2. ★開いたとき — その区分だけを出す
| 出す | 出さない |
|---|---|
| **`task_id`** | **開始日**（★front door に無い） |
| **依頼文の先頭30字 ＋ ★「先頭30字です（全文ではありません）」** | **優先度**（★決める値が無い） |
| **状態**（`dw_state`） | |
| **なぜ表示されたか: 「COMPLETE でも BLOCKED でもないため」** | |

### 2-1. ★30字にした根拠（★実測。推測ではない）
```
再現: 155件の goal について、先頭N字の異なり数を数えた（打ち切り無し）
  20字 → 66種（89件が同じ先頭）
  30字 → ★113種（42件が同じ先頭）
  40字 → ★113種（★30字と同じ。増やしても増えない）
  60字 → 118種 / 80字 → 121種 / 120字 → 124種
∴ ★30→40 で1件も増えない。★60以降も5件ずつしか増えない。
∴ ★1字あたりの効きが最も良いのは30字である。
```

### 2-2. ★先頭は「区別」には使えない（★必ず併記する）
```
再現: 155件の goal のうち、★全文が異なるのは154件。全文の重複は★1組だけ。
∴ ★42件が同じ先頭を持つのは、内容が同じだからではない。★先頭が同じなだけである。
```
> **★∴ 先頭30字では見分けられない。** **★「見分けるためのもの」として出さない。**
> **★併記する意味**: 「**先頭30字です（全文ではありません）。先頭が同じでも別の依頼です**」
> **★これを書かないと、★同じものが並んでいるように見える。**

## 3. ★出さないものの書き方（★空欄にしない）
| 項目 | **出す文言** |
|---|---|
| **開始日** | **「開始日は出せません（front door から取れないため）」** |
| **優先度** | **「優先度は出せません（前面化を決める値が 2DER に無いため）」** |

## 4. ★変えないもの
- **★振る舞いを変えない。** **★色を使わない**（`ok` も `bad` も）。
- **★新しい endpoint を作らない。** **★`/api/state` の返りを1つも変えない。**
- **★LLM に分類も要約もさせない。** **★名前を作らない**（`G-73`・§6）。
- **★区分を増やさない**（Taka の4つ ＋ 残り1つ。**★5つ目の区分ではない**）。
- **勘定科目**（`G-62`/`G-64`〜`G-68` 据え置き）。**★投入しない。**

## 4-1. ★改訂1（2026-07-29・MGR 承認時の指摘。★隠さずに書く）
| # | 変更 | 理由 |
|---|---|---|
| **1** | **受入1 の示し方を★2段に分けた**（下表） | **★「`GET /command` の描画結果で示す」は★私たちには示せない。** **155件の ID は JS が実行時に描いており、`curl` の HTML には元から入っていない** ∴ **「消えた」も「在る」も curl では言えない** |
| — | 他は変えていない | 承認された内容を渡す前に書き換えない |

> **★これは今日の事故の裏返しである。** **あの時は「文字は在るのにページが死んでいた」。★今回は「消えたことを HTML では見られない」。**
> **★私は「配信物で検査する」を D-65 で学んだ直後に、★「配信物で見えないもの」を受入に書いた。**

---

## 5. 受入
| # | 条件 | 示し方 |
|---|---|---|
| **1a** | **★JS のソースに、全件を並べる箇所が無い** | **★差分で数える**（★ページ全体で数えない） |
| **1b** | **★実際の画面から155行が消えたこと** | **★我々には示せない。「確かめていない」と書く。** **★Taka にしか出せない** |
| **2** | **★各区分の和が合計と一致する** | **★§5-1 の書き方に従うこと** |
| **3** | **「現在前面化すべきもの」が「出せません」＋理由** | ソース |
| **4** | **開いたとき、先頭30字＋「先頭30字です（全文ではありません）」が出る** | ソース |
| **5** | **開始日・優先度が「出せません」＋理由** | ソース |
| **6** | **配信物の JS が `node --check` を通る** | **★配信物に対して**（`G-71`） |
| **7** | 既存が不変・非回帰 91/7 | sha256・テスト |

### 5-1. ★受入2 の書き方（★MGR の文言を1点だけ直す）
> **MGR:「受入に『合計が155になること』を入れること」**
> **★155 は★今日の値である。** **★明日 task が増えれば 155 ではなくなる。**
> **∴ 受入は「★各区分の和が、表示している合計と一致すること」とする。** **★数を固定しない。**
> **★MGR の意図（数が合わない表は嘘である）はそのまま守る。** **★固定値にすると、★明日 嘘になる。**

## 6. ★Gap の登記（MGR 裁定③・id は私が採る）
```
G-73: 一覧に出せる短い名前が無い。goal は依頼文そのもの（中央値3816字）。
      ★先頭30字では見分けられない（42件が同じ先頭。ただし全文は154/155 が異なる）。
      ★「名前を作る」は要約を作ることであり新しい能力である ∴ 今やらない。
      ★「あと1段で作れる」と書かない。
```

## 7. ★止まってよい場所
| # | |
|---|---|
| 1 | **区分の判定が §1-1 のとおりに書けない**（`actor_role` が取れない等）→ **★止めて報告** |
| 2 | **和が合計と一致しない** → **★止めて報告**（★合わせるために区分を動かさない） |
| 3 | **既定で ID を0件にすると、既存の機能が壊れる** → 止めて報告 |
| 4 | SPEC が2通りに読める |
| **5** | **★既定の ID が、JS ではなくサーバ側で入っていた** → **★止めて報告**（MGR の前提が誤りだったことになる） |

## 8. ★資料の同期
**★MGR の commit 後に私が同期する**（訂正後の順番: ①直す ②再起動 ③配信物で検査 ④設計が受入確認 ⑤MGR が commit ⑥設計が同期）。

---
*CC-α D-73 BUILD SPEC v1.0（実装源・宛 IMPL）。★一行=**155行の ID を5行の件数にし、開いたときも「長文の壁」に変えず、出せないものは理由つきで「出せない」と書く**。★既定は **ID を1件も出さず**（停止中2／依存待ち18／背景保持117／**現在前面化すべきもの＝出せません＋理由**／**どの区分にも入らない18**／合計155）、**「その他」と書かず「どの区分にも入らない」と書く**（「その他」は中身を隠す）。区分の判定は**すべて決定論**（停止中=`BLOCKED`／依存待ち=`claude_barrier True`／背景保持=`barrier False` かつ `actor_role != "-"`／どれにも入らない=`barrier False` かつ `actor_role == "-"` かつ `BLOCKED` でない／現在前面化は**判定しない**）で、**D-71 の155件 全数実測から導き推測を1つも含まない**。★開いたときは `task_id`・**依頼文の先頭30字**・状態・「なぜ表示されたか」を出し、開始日と優先度は出さない。★**30字の根拠は実測**（先頭N字の異なり数: 20字→66種／**30字→113種**／**40字→113種（30字と同じで1件も増えない）**／60字→118／80字→121／120字→124）∴ **1字あたりの効きが最も良いのは30字**。★**先頭は「区別」には使えない**——155件の goal のうち**全文が異なるのは154件で全文の重複は1組だけ** ∴ **42件が同じ先頭を持つのは内容が同じだからではなく先頭が同じなだけ**であり、**「先頭30字です（全文ではありません）。先頭が同じでも別の依頼です」を必ず併記する**（書かないと同じものが並んでいるように見える）。★出さないものは空欄にせず「開始日は出せません（front door から取れないため）」「優先度は出せません（前面化を決める値が 2DER に無いため）」と書く。★変えないもの=振る舞い・色・endpoint・`/api/state` の返り・**LLM に分類も要約もさせない**・区分を増やさない・勘定科目・投入しない。★受入は1a/1b に分けたものを含め8行で、**受入1a は「JS のソースに全件を並べる箇所が無いこと」を差分で示し、受入1b「実際の画面から155行が消えたこと」は我々には示せないので「確かめていない」と書く**（改訂1。**「`GET /command` の描画結果で示す」は curl では言えなかった——155件の ID は JS が実行時に描いており HTML には元から入っていない**）。**受入2 は MGR の「合計が155になること」を1点だけ直し「各区分の和が、表示している合計と一致すること」とする**——**155 は今日の値であり、固定すると明日 嘘になる**（MGR の意図「数が合わない表は嘘である」はそのまま守る）。★`G-73` を採番（**一覧に出せる短い名前が無い。goal は依頼文そのもの。先頭30字では見分けられない。「名前を作る」は要約であり新しい能力なので今やらない。「あと1段で作れる」と書かない**）。★止まってよい場所5件（**和が合計と一致しないときは合わせるために区分を動かさず止めて報告**／**既定の ID が JS ではなくサーバ側で入っていたら止めて報告**）。★資料の同期は MGR の commit 後に CC-α が行う。*
