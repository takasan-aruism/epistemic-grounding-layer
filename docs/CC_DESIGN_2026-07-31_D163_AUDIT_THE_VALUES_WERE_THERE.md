# 【監査＋判定案】**★B・C にも具体値は在った** — ★「書ける値が無かった」ではない

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 10:2x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **受領**: `CC_IMPL_2026-07-31_D163_CONTROLLED_ABC_BUILT.md`
- **★新しい名前で置いた** ／ **2DER 優先原則**: ①使っていない（★2DER の外の測定の監査）②③該当なし ④実装しない ⑤該当なし ／ **★`:8005` を私は叩いていない**

---

# 1. ★引き算であることを、私が検算した（★全一致）

| | 字数 | ★私の実測 sha1 |
|---|---|---|
| A | 3649 | `1eccaeb8bdc143dd90e171b8a3a4ca14ce2208cb` |
| B | 2502 | `85c9df78159d9836fae4ee5da229c9167a88aa25` |
| C | 2180 | `6ca1750966e2ea2f7f653f65b725d19a5d842929` |
```
★B に在って A に無い行: ★0件   ★C に在って B に無い行: ★0件   ＝ ★引き算だけで作られている
★★字数は IMPL の申告（3584/2437/2115）と★数十字ずれる。★理由: ★私は末尾の問いを含む全文を測り、
   ★IMPL は条件文のみを測った（★同じ物の★別の切り取り）。★どちらも誤りではない。★述語を書く。
```

---

# 2. ★私が足す、いちばん重い事実（★IMPL の §4 を1段 精密にする）

**IMPL は「★①に書ける具体値の種類が減ったのも事実である」と書いた。★正しい。★ただし足りない。**

| 具体値 | A | B | C |
|---|---|---|---|
| `qwen36_vllm` / `RTX 5090` / `30520` / `31179` / `32607` | **★在る** | **★無い** | **★無い** |
| **★`703029` / `4022456`（★メモリの具体値）** | **★在る** | **★★在る** | **★★在る** |

```
★★A-3 が基準②を満たしたのは、★①に qwen36_vllm / RTX 5090 / 30.5GB を書いたからである
   ＝ ★B・C が意図して除いた ★GPU・コンテナ由来の値である。
★★しかし ★メモリの具体値（703029・4022456）は ★B にも C にも★入力として在った。
∴ ★★B・C の②が0件だったのは「★書ける値が無かったから」ではない。★「★書かなかったから」である。
★★これは「B・C が悪い」でも「モデルが悪い」でもない。★事実である。
★★因果は測っていない（★1条件3本・prompt 以外は同一だが、★3本は少ない）。
```

---

# 3. ★測定設計の欠陥を1つ、★私が引き取る

**IMPL は「迷い」として1行 書いた**: **★A は「PLAN を JSON で出せ」という指示を含む prompt そのものであり、★§4 の問いと衝突する。**

```
★★これは IMPL の迷いではなく、★私の SPEC の欠陥である。
★A の定義を「実際の planner prompt」とし、★問いを末尾に足させた ＝ ★2つの仕事を同時に要求した。
★★証拠: ★A-2 と C-3 が `finish_reason=length` で切れた（★A-2 は completion_tokens=8192 を使い切った）。
★★`finish_reason=length` を「max_tokens 不足」と読むと誤る。★真因は prompt 側の設計であることが在る。
   ★今回まさにその形である（★1つの prompt に「JSON の PLAN を作れ」と「3つに分けよ」が同居）。
★★∴ A の結果は「★分けられるか」だけを測っていない。★「★2つを同時にできるか」を測っている。
★★★Taka の指示（「実際のplanner入力をそのまま渡して」）には従っている ∴ ★手順は正しい。
   ★解釈のときに、この混在を落とさないこと。
```

---

# 4. ★判定案（★`D-163` §6 の3条件を1本ずつ。★確定は MGR）

| 条件 | 3回の内訳 | ★判定案 |
|---|---|---|
| **A** | **3/3 が1回（A-3）／2/3 が1回（A-1）／×が1回（A-2・length 切れ）** | **★「3回とも分けられた」ではない。★割れた** |
| **B** | **3回とも②が0件** | **★分けられていない** |
| **C** | **3回とも②が0件**（★C-2 の `703029` は `test_plan` の中であって①ではない） | **★分けられていない** |

## 4-1. ★言い方（★`D-163` §6-1 の固定をそのまま当てる）
```
★「★現在の入力では分けられない」          ← ★A が3回とも満たさなかった ∴ これを書く
★「B にすれば／C にすれば分けられる」      ← ★書けない（★B・C は②を1回も満たしていない）
★「Qwen が使えない」                        ← ★書かない（★今回は入力の話である）
★★併記する: ★A は3回中1回だけ全条件を満たした。★1回で断定していたら結論が変わっていた
```

---

# 5. ★IMPL の作法で良かった点（★根拠つき・★成果判定に効くものだけ）
```
★★A-1 と C-2 は★英語で3区分を書いた ∴ ★日本語だけで数えていれば「0」と誤読していた。
   ★IMPL は SPEC §7 のとおり両方で数えた（★私が本日3回 踏んだ穴を、★踏まなかった）
★★`703029` が①ではなく `test_plan` に在ることを★目視で切り分けた（★部分一致で決めなかった）
★★「割れた」を隠さず、★3通り全部を出した
```

---

# 6. ★次に何をするかは、私は出さない（★材料だけ置く）
```
★★理由: ★§3 のとおり、★今回の A は「分けられるか」と「PLAN を出せるか」を同時に測っている。
   ★次を決めるには、★この混在を残すか外すかを決める必要が在り、★それは測定設計の変更である。
★★材料1: ★問いだけを聞く条件（★PLAN の指示を外した A'）を足せば、★混在を切り分けられる
        ★★ただしこれは「実際の planner 入力そのまま」ではなくなる ∴ ★Taka の指示の範囲を出る
★★材料2: ★B・C に具体値は在ったのに書かれなかった ∴ ★「値を出せ」と★出力契約に書く手が在る
        ★★ただしこれは Taka の曖昧さ③（出力契約が無い）への対処であり、★入力の選別とは別の軸である
★★どちらも★私が決めることではない。★MGR の裁定、または Taka の判断である。
```

---
**決めたこと**: **①引き算であることを検算して全一致（B に A に無い行0／C に B に無い行0。字数の差は測る範囲の違いで、述語を書けば矛盾しない） ②A が基準②を満たしたのは GPU・コンテナ由来の値によるが、メモリの具体値（`703029`・`4022456`）は B・C にも入力として在った ∴ B・C の0件は「書ける値が無かった」ではなく「書かなかった」である ③A は「PLAN を JSON で出せ」と「3つに分けよ」を同時に要求しており、これは私の SPEC の欠陥——`finish_reason=length` が2回 出たのを max_tokens 不足と読まない ④判定は「現在の入力では分けられない」（A は3回中1回のみ・割れた／B・C は②を1回も満たさない）で、「Qwen が使えない」とは書かない ⑤次の一手は私が出さない——混在を残すか外すかは測定設計の変更であり、材料だけ置く。**
