# 【D-124】Taka 指示 — **次フェーズの目的は1つだけ。0/8 → 1/8。判定基準を先に固定する**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 / 2026-07-28）**
- **★本文書は `D-123` の §3 を一部 取り下げる**（§6）。**D-123 の他の部分は生きている。**

## 0. ★2DER 優先原則の5点（本文書について）
```
① どの入口を使うか : ★使っていない（Taka 指示の伝達と裁定）
② 何を試したか     : ★なし
③ 何ができなかったか: ★該当なし ∴ ★本文書は実装依頼ではない
④ 何を実装するか   : ★本文書では何も実装させない
⑤ 2DER だけで再現できたか: ★該当なし
```

---

## 1. Taka 指示（★逐語・全文）

> **【MGR 指示】**
>
> **Phase 3 はこれで終了とします。評価は固定し、以後この数値を動かさないでください。**
>
> **次フェーズの目的は一つだけです。**
>
> **「2DER が自分で担当した工程」を 0/8 → 1/8 にすること。**
>
> **目標は「1件成立」です。2件目以降は今回の目標ではありません。**
>
> **進め方は次の条件を守ってください。**
>
> **1. 未処理一覧の件数（例: 242件）は現在、仕事量や優先順位の指標として使用しないこと。処理状態の記録が役割間で揃っていないため、判断材料として信用しません。**
>
> **2. 対象は私が渡したもの（現在は D-123）から選ぶこと。未処理一覧から自動で仕事を選ばないこと。**
>
> **3. 「2DER が担当した」と書く条件を先に定義してください。後から判定基準を変更しません。**
>
> **4. 人が作業をやめただけ、既存APIを使っただけ、既存の口を利用しただけのものは主体移管として数えません。**
>
> **5. 新しい仕組みは、0→1に必要であることが確認できた場合のみ提案してください。先回りの工事は行いません。**
>
> **あわせて、今回見つけた2件は既知課題として登記してください。**
>
> **- 状況表の「台帳のずれ」表示位置の誤り**
> **- 処理済み記録が役割間で揃っておらず、未処理件数を全体指標として利用できない問題**
>
> **この2件は修正より先に、今後の判断で誤用しないことを優先します。**
>
> **今回の成果判定は「主体移管を1件成立させられるか」のみです。その他の改善は評価対象に含めません。**

**★これは一次典拠である**（運用方針 §7-4）。**★逐語の外は私の解釈であり、以下は「私が足したこと」と明記する。**

---

## 2. 確定（★指示そのもの。★言い換えない）

| # | 確定 |
|---|---|
| 1 | **Phase 3 終了。★評価の数値は以後 動かさない**（上げも下げもしない） |
| 2 | **次フェーズの目的は1つだけ: 0/8 → 1/8** |
| 3 | **目標は「1件成立」。★2件目以降は今回の目標ではない** |
| 4 | **★判定基準を先に定義し、後から変更しない** |
| 5 | **★除外3類型は主体移管として数えない**（人がやめただけ／既存 API を使っただけ／既存の口を利用しただけ） |
| 6 | **★新しい仕組みは「0→1に必要」と確認できた場合のみ提案。★先回りの工事をしない** |
| 7 | **★成果判定は「1件 成立したか」のみ。★他の改善は評価に含めない** |

---

## 3. ★仕事の出所（★指示2 の実装）

```
★仕事は Taka が渡したもの（現在は D-123）から選ぶ。
★未処理一覧から自動で仕事を選ばない。
★未処理一覧の件数（242 / 24 など）を、仕事量にも優先順位にも使わない。
```
**★理由（Taka の言葉）**: **「処理状態の記録が役割間で揃っていないため、判断材料として信用しません」。**
**★私の追記**: **これは「今は使うな」であって「242件は嘘だ」ではない。** **数が何を指しているか分からない ∴ 使わない、である。**

---

## 4. ★判定基準を先に固定する（★指示3・★これが最初の作業）

### 4-1. 手続き（★この順以外で始めない）
```
(1) 設計/監査が「2DER が担当した」と書ける条件を★1つだけ 定義して返す
(2) ★MGR が承認する（★Taka 指示の除外3類型を満たしているかを見る）
(3) ★承認後は変更しない。★測ってから動かさない
(4) ★その後で初めて、0→1 を取りに行く
```
**★(2) より前に、0→1 の作業を始めない。** **★「やりながら基準を決める」を禁じる。**

### 4-2. 定義に必ず入る除外（★Taka 指示4。★緩めない）
| 数えない | |
|---|---|
| **人が作業をやめただけ** | ★Phase 3 で「体制再編 成立」と書いたものはこれに当たる |
| **既存 API を使っただけ** | |
| **既存の口を利用しただけ** | ★`GET /api/receipt` を使う類 |

**★私の追記（★解釈であり指示ではない）**: **3つとも「2DER の側で何かが起きた」ことを示していない。**
**∴ 定義は「★2DER の側で、★人がやらなかった判断または実行が起きた」を示せる形になるはずである。**
**★ただし具体形は設計が決める。★私は現物を読む立場に無い**（運用方針 §3-5）。

### 4-3. ★定義に入れてはいけないもの（★私が固定する）
```
★新しい台帳・新しい指数・新しいスコア・新しい完成語彙を使う定義にしない（Taka 常設命令）
★「これから作る仕組み」に依存する定義にしない（★それは先回りの工事になる）
★複数の条件を1つの印にまとめない（★1条件に1つの印）
★自己申告だけで成立する定義にしない
```

---

## 5. ★既知課題2件の登記（★指示。★私は id を指定しない）

**Taka の指示は「登記せよ」である。** **★Gap 台帳の唯一の書き手は設計/監査である**（07-28 に確定した責務点・運用方針 §6-8）。
**∴ ★私は内容で指名する。★id は設計が採り、返信に書く。**

| # | ★内容（★これで指名する） |
|---|---|
| **(a)** | **状況表の表示位置の誤り**: `2der_status.sh` が「台帳のずれ」を出力した直後に **MGR 未応答の一覧**を出しているため、**ずれの内訳に見える** |
| **(b)** | **処理済み記録が役割間で揃っていない**: `record_done` が事実上 MGR・TAKA にしか行われておらず、**未処理件数（DESIGN 242 / IMPL 24）を全体指標として使えない**（実測 2026-07-30 16:08） |

```
★Taka 指示（逐語）: 「この2件は修正より先に、今後の判断で誤用しないことを優先します。」
∴ ★直さない。★登記して、★使わない。
∴ ★これらを直す作業は、今回の成果判定に含まれない（★やっても評価されない）
```

---

## 6. ★D-123 の一部を取り下げる（★私の誤り。★訂正する）

**D-123 §3 と §8(1) で、私は設計にこう頼んだ:**
> 「『妨げた』と判定できる形を1つだけ決めておくこと」

**★これを取り下げる。**
```
★理由: Taka 指示5「先回りの工事は行いません」／
        「今回の成果判定は主体移管を1件成立させられるかのみ。その他の改善は評価対象に含めません」
∴ ★非回帰の畳む条件を"事前に形にしておく"のは、★まさに先回りの工事である。
```
- **★非回帰そのものの扱いは変わらない**（37本のみ／61本停止／暫定／再提案は「実際に妨げた時点」）。
- **★変わったのは「今のうちに判定形を作っておく」をやめたこと**だけである。
- **★私は D-123 を書き換えない。★ここで取り下げると書く**（前向きのみ）。

---

## 7. ★設計/監査がいま返すもの（★1つだけ）

```
★「2DER が担当した」と書ける条件の定義（★1つ）。
   - ★除外3類型を満たすこと（§4-2）
   - ★入れてはいけないものを使わないこと（§4-3）
   - ★併せて、§5 の既知課題2件を登記し、★採った id を返信に書くこと
```

**★これ以外は始めない。** **★0→1 の作業は、私が定義を承認してから。**
**★どの工程を狙うかの候補（D-123 §4-4）は、★定義の後に出す**（★定義より先に候補を選ぶと、★候補に合う定義を書いてしまう）。

**★私は実装へ直接書かない。** **★SPEC は設計が書き、設計から実装へ回す。**

---
*MGR【D-124】。★**Taka 指示（逐語 §1）**=Phase 3 終了・**評価の数値は以後動かさない**／**次フェーズの目的は1つだけで「2DER が自分で担当した工程」を 0/8 → 1/8**／**目標は1件成立、2件目以降は今回の目標ではない**／進め方5条件=**(1) 未処理一覧の件数を仕事量にも優先順位にも使わない**（処理状態の記録が役割間で揃っておらず判断材料として信用しない）**(2) 対象は Taka が渡したもの（現在は D-123）から選び、未処理一覧から自動で仕事を選ばない (3) 「2DER が担当した」と書く条件を先に定義し後から変更しない (4) 人が作業をやめただけ・既存APIを使っただけ・既存の口を利用しただけは主体移管として数えない (5) 新しい仕組みは 0→1 に必要と確認できた場合のみ提案し先回りの工事はしない**／**既知課題2件を登記**（状況表の「台帳のずれ」表示位置の誤り／処理済み記録が役割間で揃わず未処理件数を全体指標に使えない問題）——**この2件は修正より先に今後の判断で誤用しないことを優先**／**成果判定は「主体移管を1件成立させられるか」のみで、その他の改善は評価対象に含めない**。★**判定基準を先に固定する手続き**=設計が条件を1つ定義→**MGR 承認**→以後変更しない→**その後で初めて 0→1 を取りに行く**（★承認前に作業を始めない・「やりながら基準を決める」を禁じる）。**定義に入れてはいけないもの**=新しい台帳／指数／スコア／完成語彙、これから作る仕組みへの依存、複数条件を1つの印にまとめること、自己申告だけで成立する形。★**既知課題は内容で指名し id は設計が採る**（Gap 台帳の唯一の書き手は設計）——**登記して、使わない。直す作業は今回の成果判定に含まれない**。★**MGR は D-123 §3・§8(1)（「妨げた」と判定できる形を先に決める）を取り下げた**——**Taka 指示5「先回りの工事は行いません」に自分の依頼が反していたため**。非回帰の扱い（37本のみ・61本停止・暫定・実際に妨げた時点で再提案）は変わらない。★**設計がいま返すのは1つだけ**=「2DER が担当した」と書ける条件の定義＋既知課題2件の登記 id。**どの工程を狙うかの候補は定義の後**（★定義より先に候補を選ぶと、候補に合う定義を書いてしまう）。*
