# 設計/監査 → **実装(IMPL)**: **★同一文面を webui から1回だけ投入してください（承認済）。★止まったのは正しい**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**中継。承認の主体は MGR**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.8` — `§12` を最大版で読んだ値）**
- **典拠**: `CC_MGR_2026-07-29_D49_ALLOW_ONE_WEBUI_SUBMIT_SAME_TEXT.md`

## 1. ★先に — 止まったのは正しい（私の側からも書く）
**実装源 §6-1 に「`etrace_run_id` を足しても `/api/state` に出なければ、★私の見立てが外れた。報告する」と書いた。**
**★あなたはそのとおり止めた。** **直そうとせず、回避策も作らなかった。**
> **★そして原因は、あなたの実装ではない。** **私の受入条件が★最初から成立しない形だった。**
> **CLI 投入では trace ファイルが書かれない**（今朝 `G-36` として私自身が登記した）。**∴ UI からは読めない。**
> **★私は `G-36` を書いた当人でありながら、受入に反映しなかった。**

## 2. ★やること（1回だけ）
```
承認済の文面を1文字も変えず、★webui（/api/submit）から1回だけ投入する。
```
| # | 条件 |
|---|---|
| **1** | **★文面は承認済のものと1文字も同じ。** `task_id = sha1(raw_input)` なので**新しい DW task は増えない**（既存の `TASK-2DER-AC13F06E` を指す） |
| **2** | **★webui から1回だけ。** **CLI で投入し直さない** |
| **3** | **★`run_next` を叩かない** |
| **4** | **★CLI 側の run（`ETR-88bc41400d19`）と webui 側の run を★両方報告する。** **消さない** |
| **5** | **★webui を再起動してから**（本日2回踏んだ型）。**`cd /home/takasan` を明示**（v2.5 §4-17） |

## 3. ★この1回が持つ意味（あなたの報告に書いてよい）
> **これは「別の文面で投入し直す」ではない。** **★同じ文面を、別の入口から通す。**
> **∴ 同じ依頼が、入口によって「UI で追える／追えない」に分かれることを★実データで示せる。**
> **∴ この1回自体が `G-36` の実害の証明になる。**

## 4. ★あなたの報告で評価された点（記録として渡す）
**あなたは受入6（`etrace_run_id` が無くても画面が壊れない）について「皮肉にも今回の task がその実例です」と書いた。**
> **★失敗した場面を、別の受入の証拠として使った。** **捨てなかった。**
> **★MGR がこれを「良い報告である」と記録している。**

## 5. ★止まってよい場所（変わらない）
1. **webui 経由でも `etrace_run_id` が `None` だった** → **★止めて報告。** 私の見立てがさらに外れている
2. **既存キーの sha256 が変わった** → 止めて報告
3. **`/api/resolve` の返りが JS で描けない形** → 繋げる細工をせず報告
4. SPEC が2通りに読める

---
*CC-α → IMPL 中継（D-49 追加投入）。★止まったのは正しい=実装源 §6-1 が「`etrace_run_id` が `/api/state` に出なければ見立てが外れた。報告する」と先に決めており、IMPL はそのとおり止めて直そうとせず回避策も作らなかった。**そして原因は実装ではなく、CC-α の受入条件が最初から成立しない形だった**——CLI 投入では trace ファイルが書かれず（今朝 `G-36` として CC-α 自身が登記した）UI から読めない。**`G-36` を書いた当人でありながら受入に反映しなかったのは私である**。★やること=承認済の文面を1文字も変えず **webui（`/api/submit`）から1回だけ**投入する。条件=文面は1文字も同じで `task_id=sha1(raw_input)` ゆえ**新しい DW task は増えない**（既存の `TASK-2DER-AC13F06E` を指す）／webui から1回だけで CLI で投入し直さない／`run_next` を叩かない／**CLI 側の run(`ETR-88bc41400d19`)と webui 側の run を両方報告し消さない**／webui を再起動してから `cd /home/takasan` を明示。★この1回の意味=「別の文面で投入し直す」ではなく「**同じ文面を別の入口から通す**」であり、**同じ依頼が入口によって「UI で追える／追えない」に分かれることを実データで示せる ∴ この1回自体が `G-36` の実害の証明になる**。★評価された点=受入6について「皮肉にも今回の task がその実例です」と書き、**失敗した場面を別の受入の証拠として使い捨てなかった**ことを MGR が「良い報告である」と記録している。★止まってよい場所4件（webui 経由でも `None` なら止めて報告＝CC-α の見立てがさらに外れている／既存キーの sha256 が変わった／`/api/resolve` の返りが描けない形／2通りに読める）。*
