# 設計/監査 → **実装(IMPL)**: **Event Trace 第一段階を実装してください。実装源は1本です**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**送付状。★実装源は本文書ではない**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.2` — `§12` で日付が最も新しい行を採って確認）**

## 1. ★実装源（これ1本。他から実装しない）
```
CC_DESIGN_2026-07-28_D42_EVENT_TRACE_PHASE1_BUILD_SPEC_v1_0.md   ← BUILD_ROLE: ★実装源
```
**★同じ話題の他文書（草案 `..._D42_EVENT_TRACE_PHASE1_SPEC.md`、MGR の裁定文書、D-41 の棚卸し）は★背景であって実装源ではない。**
**★矛盾を見つけたら実装しないで報告すること。** **自分で解釈して埋めない。**

## 2. ★先に読む3節（ここを外すと不合格）
| # | 節 | 一行 |
|---|---|---|
| **1** | **§0-1 完了条件3件** | **`LEDGER_REGISTRY` 登記／④ fail-open の4条件／受入⑦(c) の全走査。★どれも「注記」ではない** |
| **2** | **§3-3 受入⑦(c)** | **★本 build の合否。** `open(a/w)` 等を全走査し、**出た行を1つ残らず4分類**する。**「見た範囲で無い」は不可** |
| **3** | **§3-5 実行前の必須手順** | **★webui を再起動する**（現行 pid 2746222 は13時間前起動でソースより古い）。**投入前にスナップショットを取る** |

## 3. ★私からの補足（SPEC に書いていない運用注意）
1. **★依頼文を1文字も変えないこと。** MGR 承認済。**通りやすくする書き換えをしない。**
2. **★投入は2回だけ。CLI が先、webui が後。** 順序を逆にしない（理由は SPEC §3-1）。
3. **★2件目が DW task を作らないのは設計どおりである**（SPEC §3-4 で事前に固定した）。**不具合として報告しないこと。**
4. **★「合流点が唯一である」は私が `grep` 1回で見たものが在る**（SPEC §4-1）。**あなたの全走査で覆ってよい。** **覆ったら、それは私の誤りであって、あなたの報告が正しい。**

## 4. ★止まってよい場所（feasibility-first）
**次のいずれかに当たったら、実装せずに★設計/監査へ報告すること:**
| # | 条件 |
|---|---|
| **1** | **合流点の唯一性が全走査で崩れた**（抜け道が1件でも出た） |
| **2** | **fail-closed を入れると既存テストが新たに落ちる**（`G-13` の既知3件以外） |
| **3** | **`ds/data/` への書き込みが権限やパスで通らない** |
| **4** | **SPEC の記述で2通りに読める箇所が在った**（★自分で決めない） |

**★「動くようにするために SPEC を少し変えた」を、報告なしでやらないこと。** **本日、それで失った時間が在る。**

---
*CC-α → IMPL 送付状。★実装源は `CC_DESIGN_2026-07-28_D42_EVENT_TRACE_PHASE1_BUILD_SPEC_v1_0.md` の1本のみで、草案・MGR 裁定文書・D-41 棚卸しは背景であって実装源ではない——矛盾を見つけたら実装せず報告し、自分で解釈して埋めないこと。★先に読む3節=§0-1 完了条件3件（`LEDGER_REGISTRY` 登記／④ fail-open の4条件／受入⑦(c) 全走査。どれも注記ではない）／§3-3 受入⑦(c)（本 build の合否。`open(a/w)` 等を全走査し出た行を1つ残らず4分類。「見た範囲で無い」は不可）／§3-5 実行前の必須手順（**webui を再起動する**——現行 pid 2746222 は13時間前起動でソースより古い／投入前にスナップショットを取る）。★運用注意=依頼文を1文字も変えない（通りやすくする書き換えをしない）／投入は2回だけで CLI が先・webui が後／**2件目が DW task を作らないのは設計どおり**なので不具合として報告しない／「合流点が唯一である」は CC-α が `grep` 1回で見たものが在り、全走査で覆ってよく、覆ったらそれは CC-α の誤りで IMPL の報告が正しい。★止まってよい場所=合流点の唯一性が崩れた／fail-closed で既存テストが新たに落ちた（`G-13` の既知3件以外）／`ds/data/` へ書けない／SPEC が2通りに読める（自分で決めない）——**「動くようにするために SPEC を少し変えた」を報告なしでやらないこと**。*
