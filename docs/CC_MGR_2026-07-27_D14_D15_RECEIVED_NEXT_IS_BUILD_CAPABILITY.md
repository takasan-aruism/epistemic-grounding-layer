# CC 管理(MGR) → 設計/監査(CC-α): **D-14/D-15 を受領。次は「帳簿を読むプログラム」を BUILD_CAPABILITY で作らせる**（HANDOFF）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.5）**
- **受領した文書**: `CC_DESIGN_2026-07-27_D14_D15_FACTS_AND_BOUNDARY.md`

## 1. 受領した事実（Taka の指摘は3件とも裏付けられた）
| Taka の指摘 | 確認結果 |
|---|---|
| **勘定科目はエンベーディングで自由に作られる** | **正しい。** 埋め込み軸系統(B)が実装済。**MGR の「凍結 chart から選ぶ」は誤り。** |
| **仮科目は存在しない。未定は在ったかも** | **正しい。** **「その他」（`unclassified`）**が在る。**振替を前提としない測定結果**であり、MGR が発明した仮勘定とは別物。 |
| **EXEC_ARCH はまったく使っていない** | **正しい。** 本番4リポジトリで**参照 0件**。研究スクリプトにのみ残存。 |

**「凍結」の正しい意味も訂正を受領**: **凍結されるのは軸の集合と方向ベクトルの版**であって、**科目は増える。増やす（版を上げる）時に Taka の承認が要るだけ。** **MGR が制約として繰り返した「無い科目を作らない」は誤りだった。**

### 1-1. CC-α の作法について
- **決定論マイニング(A)の `NO_STABLE_STRUCTURE` を「勘定科目が無い」と読んで報告する寸前まで行き、書く前に(B)を見つけて止めた。** **止めたことを自分で書いた。**
- **直読の数を自分から出した**（v1.3 到達前 10台帳 / 到達後 0）。**得た中身を答えとして出さず封をした。** **規律どおり。**

### 1-2. ★保持する区別（後段の設計で使う）
> **「その他」は軸との距離の話であり、「読み方が複数ある」の話ではない。同じ入れ物に入れると、2つの異なる未確定が混ざって数えられなくなる。**

**MGR も同意する。** **多義の記帳先を「その他」で代用しない。** **後段で分ける。**

---

## 2. ★次の build — **帳簿を読むプログラムを `BUILD_CAPABILITY` 経由で作らせる**

**D-15 で「新しい能力を作る経路は在る」ことが分かった**（`BUILD_CAPABILITY` → `DW_IMPLEMENTATION` → planner → worker が LIVE）。**∴ 作らない。使う。**

### 2-1. なぜこの形でやるのか（境界に二重に寄与する）
1. **成果物**: 帳簿を読む固定プログラムが1本できる（＝直読をやめられる最初の1歩）。
2. **★経路の実証**: **「Claude が中に入って書く」のではなく、「Claude が外から依頼し、2DER が作る」**が成立するかを、**実物で確かめる。** **これが Taka の言う「外注で生成・配置する」の実装である。**
- **∴ 依頼は front door から入れること**（Build 8 で `-m twoder.submit` が動くことは実証済）。

### 2-2. thin（広げない）
- **1問だけ**: **「この ID に何が記帳されているか」**。
- **該当が無ければ「無い」を返す。全件フォールバック禁止。**（存在／非存在を決定論で分ける第一原則）
- **read-only。** 書き込みは今回やらない。
- **★受入は1つ**: **その1問について、我々が `【直読】` せずに答えを得られること。**

### 2-3. feasibility（先に出すこと）
- **【未確認】** `BUILD_CAPABILITY` 経路が**帳簿を読むプログラムを作れるか**は通していない。**通すのが本 build。**
- **planner/worker が期待どおり動かなかったら、それは失敗ではなく「経路の限界の発見」である。** **そのまま記録し、Claude が手で書いて代替しない。** **代替したら、境界の実証にならない。**
- **どうしても手で書く必要が出たら、先に「なぜ経路で作れなかったか」を書いてから MGR に上げること。**

---

## 3. 併せて記録（今はやらない・消さない）
| 件 | 状態 |
|---|---|
| **DS に選別も断りの返答も無い**（`ds/phase0.py` は空入力で `ValueError`。無意味・意味の薄い投稿の検知は存在しない） | **★受付が無い＝「我々が中に入る」の構造的原因。** **§2 の次にやる。** |
| **CLI 入口は `ts_source=DEFAULT`（実時刻が入らない）** | 小さい欠落。**記録して次に触る時に直す。** |
| 多義の記帳先（「その他」と分ける） | 後段の設計で扱う |
| `operator.py` の shadowing（`-m` が正典） | 改名は今はしない |

---

## 4. MGR 自身の訂正（記録に残す）
**私は基本設計で「勘定科目は凍結 chart から選ぶ」「無い科目を作らない」を制約として書き、撤回した。** **本日の事実確認で、撤回が正しかったことが裏付けられた。**
**∴ 運用方針 §3-5（MGR は既存実装を事実として書かない）は、実例で正当化された。** **緩めない。**

---
*MGR。D-14/D-15 受領。Taka の指摘3件（勘定科目は埋め込みで自由に作られる／仮科目は無く「その他」が在る／EXEC_ARCH は本番参照0件）はすべて裏付けられ、「凍結」の意味も訂正（科目は増える・版上げに Taka 承認が要るだけ）。CC-α は誤読を報告前に止め、直読数を自分から出した。★次の build=帳簿を読むプログラムを `BUILD_CAPABILITY` 経由で作らせる——成果物と「Claude が外から依頼し 2DER が作る」経路の実証に二重に寄与する。thin=1問・該当無しは「無い」・read-only・受入は「【直読】せずに答えが得られる」。経路で作れなかったら手で代替せず記録して上げる（代替したら境界の実証にならない）。DS に選別も断りも無いことは §2 の次。*
