# 引き継ぎ ―― ESDE Evaluation 専任監査 → MGR

発: ESDE Evaluation 専任監査（Claude）／ 宛: MGR
根拠: Taka 指示 2026-08-23「今後あなたは台帳の変更に伴う DW サイドを扱う。かなり大きな変更が発生すると思われるので、現在の仕事は MGR に引き継いで終了とする」
台帳: `ITEM-2DER-EVO-0089` / `ITEM-2DER-EVO-0090`

**★私の commit・push はすべて完了している（未push 0）。★未完のコードは残していない。**

---

## 1. 今日 私が入れた物（★全部 `[Claude実装]` で分離・push 済）

| repo | commit | 内容 | 状態 |
|---|---|---|---|
| ds | `af75d1f` | `etrace._read_all` を流しながら絞る | **完了・検証済**（6.78秒/2.94GB → 0.77秒/0.02GB・返り 41件不変） |
| dev-workcell | `59f3149` | 判定器が試験結果を読めるようにする（`runner_exit`/`exit` の不一致） | **完了・本線で実走確認**（`INDETERMINATE` → `ORACLE_DEFECT`） |
| dev-workcell | `2ed451e` | task に `supersedes` を足す（書き手＋読み手） | **完了**（593 task 回帰 例外0） |
| dev-workcell | `ffe36a7` | `ORACLE_DEFECT` を分離し `PLANNING` で停止 | **完了**（595 task 回帰・動いたのは狙った1件のみ） |
| twoder | `1234f46` | 上級監査の入力（8欄中4欄が None だった） | **完了**（419 → 3393文字） |
| twoder | `7169c7b` | `supersede_seal`（やり直し宣言の決定論抽出） | **完了**（R4 6件 実発火） |

**★`twoder/webui.py` の私の変更10行だけ、MGR の commit に混ざっている**（`build_state` に `supersedes` / `superseded_by` を出す2箇所）。分離できなかったので、そちらの履歴に入っている。

## 2. ★MGR が引き取るもの（未処理・優先順）

### ★★A. 明細のバグ2件（★16:56 に出した・未修理・最優先）
`twoder/requirement_structure.py:49` の `_HEAD = ^\s*(.+?)[:：]\s*$`
- **① `## 仕様` `【試験】` `■ 制約` が見出しと認識されない。** `_section_kind` は正しく引けるのに使われない
- **② ①の帰結で、見出し行が前の明細の `source_text` に食い込む**
- **1箇所直せば両方消える**
- **★直した後の再監査用に before を6点 固定済み**（本引き継ぎ §5）
- 現状 fail-safe（`UNVERIFIED` に倒れるだけ）だが、**明細を DW に繋ぐ前には直っていること**

### B. DW 側の障害8件（13:36・`CC_DESIGN_2026-08-23_DW_OBSTACLES_FOR_DETAIL_SYSTEM.md`）
**①明細と task の対応（1:1 / N:1 / 1:N）が最優先。②③④⑥は全部①の従属。**

### C. ID 衝突の制約（13:56）
`dw_task_id = sha1(raw_input)` は **ts を含まない** ∴ 同じ依頼文は同じ ID → `create_task` が拒否。
**明細ごとに task を作る設計は、この制約を先に解かないと成立しない。**

### D. 台帳変更の影響面（17:15・`CC_DESIGN_2026-08-23_LEDGER_CHANGE_IMPACT_SURFACE.md`）
直触 93箇所 / 23ファイル / **アクセサを1度も使わない10ファイル**。
`findings`(5) `review`(4) `contract`(4) が改訂コスト上位。

### E. 台帳の統廃合（17:48・`CC_DESIGN_2026-08-23_LEDGER_CONSOLIDATION_SURVEY.md`）
56本中39本が7月停止。**①統合9本 ②登記簿から外す10本 → 56→37。★Taka 裁定待ち。**

## 3. ★Taka の裁定待ち3件（★MGR が代わりに決めてはいけない）

1. 台帳の統廃合 ①②を実行してよいか（私の意見: **②が先**・完全に可逆）
2. producer 是正の材料をどう作るか（私の意見: **固定要求を外して前後を測る**・分母は `PLANNING` 7件）
3. 順序 ―― `producer是正` が UNVERIFIED で止まったまま `TEST_REPAIR実証` へ進むか

## 4. ★止まっている物（★動かす役が居ない ―― 意図的にそうした）

| | 状態 | 誰の物か |
|---|---|---|
| `TASK-2DER-ED65242E` | **`PLANNING`**（`ORACLE_DEFECT` を分離して停止） | ★MGR の対照実験の成立側 |
| `TASK-2DER-EF6826DC` | **`BLOCKED`**（打ち切り・`ED65242E` へ supersede 済） | ★MGR の対照実験の失敗側 |
| `PLANNING` 7件 | 全部 `allow=True` / 6欄確定 | ★動かす役が **0**。放置すると永久停止 |

**★私は本日 監視を止めた。** 以後この2件を見るのは MGR。
**★7件中6件は、落ちている試験が `missing_file` / `malformed_json`** ＝ producer の固定要求由来。

## 5. ★明細のバグを直した後、再監査で見る6点（★後出しにしないため先に固定）

```
① 異常入力10件（空/None/空白/改行/1文字/マーカー/制御文字/5万字/見出しのみ/記号だけ）
   → ★全部 例外なし のまま（現状そうなっている）
② EF6826DC の分類 → ★変わらない（: 形式なので影響を受けないはず）
③ ED65242E の分類 → ★SPEC 8 / TEST 6 のまま（★変わると MGR の対照が壊れる）
④ Markdown 見出し `## 仕様` → SPEC が ★効くようになる
⑤ 見出し記号（■ ◆ ●）を足すなら、箇条書き（- ・ *）が ★壊れていないか
⑥ source_text に見出しが ★食い込まなくなる
```

## 6. ★引き継ぎで一番大事なこと（本日 6回 踏んだ型）

**「片側だけ見て断定」を私は本日6回やった。** 全部 記帳済み。内訳:
`build_plan` の副作用 / `run_until_barrier` の門 / 向きの数え方 / **Taka の判定を prompt を読まずに裏書き** /
着弾を件数だけで判定 / **検出器を自分の呼び出しバグで「壊れている」と誤報**。

**★2件は「書き手と読み手の両方を見る」だけで防げた。**
∴ 台帳が変わる時は **必ず両側を並べて測ること。** 今日の事故2件（`runner_exit`/`exit`、`view.get("findings")`）も同型。

## 7. 私の新しい担当（Taka 指示）

**台帳の変更に伴う DW サイド。**
∴ 上の B・C・D は**私の新しい担当と重なる** ―― MGR が設計を決めたら、DW 側の影響を私が測る。
**A（明細のバグ）と E（統廃合）は MGR と Taka の物。**
