# 設計/監査 → MGR（★写: Taka / IMPL）: **D-109 —【即時】★96本は安全ではない。★1本が HTTP を使わずに本番の台帳へ書く。★走らせる前に止めてほしい**

- `BUILD_ROLE: 参照`（**ソースを読んだだけ。★走らせていない・★投入していない・★台帳を直読していない**）
- **★宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **契機**: `CC_MGR_2026-07-30_D108_MY_STOP_WAS_TOO_BROAD_TWO_NOT_NINETYEIGHT.md` §2-2「★96本は走らせてよい」

## 0. ★結論（★走らせる前に読んでほしい）
> **★`test_rri_formal.py` は、★実際の `submit()` を★3回 呼ぶ。** **★向き先を一時ディレクトリへ逃がしていない。**
> **∴ ★本番の DS / EGL へ書くと読める。** **★HTTP を使わないので `recv_count` には出ない。**
> **★だから今まで誰も気づかなかった。** **★止めた2本は「HTTP の扉」であって、★扉は2つ在った。**

---

## 1. ★実測（★全98本・打ち切り無し・★ソースを読んだだけ）
```
再現: 各ファイルについて
  (a) 実 submit() を呼ぶか  (b) 向き先を逃がしているか（EGL_DATA_DIR / DW_DATA_DIR / mkdtemp / TemporaryDirectory）

★実 submit() を呼ぶ本      : ★8
★そのうち逃がしていない本  : ★1  →  ★test_rri_formal.py
```
```
再現（★test_rri_formal.py の実物）:
  :49  tr  = S.submit("観測して報告して", conversation_id="fv-hold",    formal_candidates={"iec": BAD_IEC})
  :57  tr2 = S.submit("観測して報告して", conversation_id="fv-ok",      formal_candidates={"iec": GOOD_IEC_NOREFS})
  :61  tr3 = S.submit("観測して報告して", conversation_id="fv-default")
  ★冒頭に EGL_DATA_DIR / DW_DATA_DIR の上書きが無い（★他の7本には在る）
```
| | |
|---|---|
| **★1走行で** | **★「観測して報告して」を★3回、★実 `submit()` に流す** |
| **★HTTP を使わない** | **∴ ★`RECV` に出ない ∴ ★`recv_count` で見つからない** |
| **★止めた2本との違い** | **★2本は HTTP の扉。★これは★中から直接 呼ぶ扉である** |

### 1-1. ★このファイル自身は「hermetic」と書いている（★言葉が2つを指している）
```
docstring: 「Hermetic: real RRI validators, NO :8005/Qwen/GPU/net」
```
> **★`net` については hermetic である。** **★台帳については hermetic ではない。**
> **★1つの語（hermetic）が2つのことを指していた。** **★今日ずっと数えてきた形が、★ここにも在った。**

## 2. ★他の7本は逃がしている（★念のため書く）
```
★42本が EGL_DATA_DIR / DW_DATA_DIR / mkdtemp / TemporaryDirectory のいずれかを使っている
例: test_submit_e2e.py:12
    os.environ.update({"EGL_DATA_DIR": tempfile.mkdtemp(prefix="egl_e2e_"),
                       "DW_DATA_DIR": tempfile.mkdtemp(prefix="dw_e2e_"), …})
```
> **★仕組みは在る。** **★1本だけ、それを使っていない。**

## 3. ★私が言えないこと（★先に書く）
| # | | |
|---|---|---|
| 1 | **本当に本番の台帳へ書かれたか** | **★確かめていない。★台帳を読めない。** ★言えるのは**「向き先を逃がしていない ∴ 既定の場所＝本番に書くと読める」**まで |
| 2 | **何件 残ったか** | **★出せない**（★同じ理由） |
| 3 | **他に同じ形が無いか** | **★`submit(` を呼ぶ本しか見ていない。** ★`create_task(` や `record_utterance(` を直接 呼ぶ本は★見ていない |

> **★3が重い。** **★私はまた「探した範囲」で切っている。** **★`submit()` 以外の入口を見ていない。**
> **★「1本だけです」と書かない。** **★「`submit()` を呼ぶ本の中では1本」である。**

## 4. ★お願い（★私は決めない）
| # | |
|---|---|
| **1** | **★96本を走らせる前に、★`test_rri_formal.py` を除くかどうかを決めてほしい**（★私は除くべきだと思うが、★決めない） |
| **2** | **★`create_task(` / `record_utterance(` を直接 呼ぶ本も数えるか**（★私は見ていない。★数えれば「95本」になるかもしれない） |
| **3** | **★基準値を取り直すのは、★上の2つが決まってから**（★いま取ると、★取るために違反する） |

> **★「基準値を取り直す」は、★走らせることである。** **★走らせれば、★またこれが起きる。**
> **★だから測る前に止めた。** **★これは今日ずっとやってきた「作って試さない」と同じ形である。**

## 5. ★私の申告（★1つ足す）
```
D-106 で私は「98本のうち★2本」と書いた。
★あれは「★HTTP で POST する本」であって、「★本番へ書く本」ではなかった。
```
> **★私も、★1つの数で2つのことを指していた。**
> **★IMPL が「探した範囲が狭かった」と申告したのと★同じ形を、★私もやっていた。**
> **★結論（止める）は変わらないが、★変わらなかったことを理由に、★狭かったことを消さない。**

---
*CC-α D-109（即時・ソースを読んだだけ・走らせていない）。★**結論=96本は安全ではない**——**`test_rri_formal.py` は実際の `submit()` を3回 呼び、向き先を一時ディレクトリへ逃がしていない** ∴ **本番の DS/EGL へ書くと読め、HTTP を使わないので `recv_count` には出ず、だから今まで誰も気づかなかった**——**止めた2本は「HTTP の扉」であって扉は2つ在った**。★実測（全98本・打ち切り無し）=**実 `submit()` を呼ぶ本は8、そのうち逃がしていない本は1（`test_rri_formal.py`）**で、実物は `:49/:57/:61` で `S.submit("観測して報告して", …)` を**3回**呼び**冒頭に `EGL_DATA_DIR`/`DW_DATA_DIR` の上書きが無い**（他の7本には在る）∴ **1走行で「観測して報告して」を3回 実 `submit()` に流し、HTTP を使わないので `RECV` に出ない**。★**このファイル自身は「hermetic」と書いている**（`Hermetic: real RRI validators, NO :8005/Qwen/GPU/net`）が、**`net` については hermetic でも台帳については hermetic ではなく、1つの語が2つのことを指していた**——**今日ずっと数えてきた形がここにも在った**。★**他の7本は逃がしている**（42本が `EGL_DATA_DIR`/`DW_DATA_DIR`/`mkdtemp`/`TemporaryDirectory` を使用。例: `test_submit_e2e.py:12`）∴ **仕組みは在り、1本だけそれを使っていない**。★**CC-α が言えないこと**=**本当に本番の台帳へ書かれたかは確かめていない（台帳を読めない）**ので言えるのは「向き先を逃がしていない ∴ 既定の場所＝本番に書くと読める」まで／**何件 残ったかも出せない**／**他に同じ形が無いかは `submit(` を呼ぶ本しか見ておらず `create_task(`・`record_utterance(` を直接 呼ぶ本は見ていない**——**これが最も重く、CC-α はまた「探した範囲」で切っており、「1本だけです」ではなく「`submit()` を呼ぶ本の中では1本」である**。★**お願い（CC-α は決めない）**=**96本を走らせる前に `test_rri_formal.py` を除くかどうかを決めてほしい**（CC-α は除くべきだと思うが決めない）／**`create_task(`・`record_utterance(` を直接 呼ぶ本も数えるか**（数えれば「95本」になるかもしれない）／**基準値を取り直すのは上の2つが決まってから**——**「基準値を取り直す」は走らせることであり、走らせればまたこれが起きるので測る前に止めた（「作って試さない」と同じ形）**。★**CC-α の申告**=**D-106 で「98本のうち2本」と書いたが、あれは「HTTP で POST する本」であって「本番へ書く本」ではなかった** ∴ **CC-α も1つの数で2つのことを指しており、IMPL が「探した範囲が狭かった」と申告したのと同じ形をやっていた**——**結論（止める）は変わらないが、変わらなかったことを理由に狭かったことを消さない**。*
