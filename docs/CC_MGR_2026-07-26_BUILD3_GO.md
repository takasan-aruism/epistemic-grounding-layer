# CC 管理(MGR): **Build 3 へ GO（Taka 直接）** + 実行条件と計器の確認（HANDOFF）

- `BUILD_ROLE: 参照`（実装源は `CC_DESIGN_2026-07-26_BUILD3_SPEC_LONG_INPUT.md` v1.0 の1本）
- 宛: IMPL(coder) / DESIGN/AUDIT(CC-α) / 写: Taka / 発: MGR / 2026-07-26 22:20 / TYPE=HANDOFF

## 0. 権限
Taka 直接発話（2026-07-26・逐語）:
> **「Build 3 進めて」**

**着手可。** 実装源は BUILD SPEC v1.0 の1本のみ。

## 1. MGR による SPEC の確認
**MGR 条件はすべて入っている**（主指標=遠隔取り違え率・**分岐点(STOP/ACT)から機械導出**／一致率は M2 として併記継続／**M1a UNSAFE と M1b WASTEFUL を分離**／飽和指標を使わない／分布と2バッチ／付加語は DESIGN が固定）。**異議なし。**

## 2. 実行順（SPEC §7-ii を採る）
**まず S（短） vs L3（3500字）の2点 × `P-ASSERTS-CONTEXT` のみで回し、差が出るかを先に見ること。**
- 概算: 21 fixture × 2長さ × 10 run × 2バッチ ≒ **840 呼出**。フルグリッド（3種×4長さ）は ≒5,040 呼出なので、**先に差の有無を見るのが合理的**。
- **差が出なければ細かく刻む意味がない。刻みを削ったら削ったと書く**（SPEC §7-ii）。
- **差が出たら**、そこで初めて `P-IRRELEVANT` / `P-RELATED` を足して**「長さの効果」と「文脈主張の効果」を分離**する。**ここが本命**（長いから壊れるのか、"前に言ったとおり"と書かれているから壊れるのかは別物）。

## 3. 計器（MGR が確認済み・IMPL は自分で再確認すること）
- **【監査:MGR】`http://localhost:8005/v1/models` は生存。`Qwen3.6-35B-A3B` / `max_model_len=65536`。** 3500字の周辺文でも context 長は問題にならない。GPU 2枚とも idle。
- **run 間ノイズ ≈0.10（≒6 fixture）。単発で言わない。分布と範囲で出す。**
- **`finish_reason=length` が出ても max_tokens 不足と即断しない**（[[llm-prompt-hygiene-not-budget]]）。**prompt を目視し、nonce / 近重複 / 途中切断を先に疑うこと。**
- **【設計:MGR ではない・DESIGN の役割】周辺文が強すぎないかは CC-α が実物を読んで判断する**（SPEC §7-i）。**IMPL は作って全文を台帳に記録するところまで。**

## 4. 結果の読み方（先に決めておく・後から都合よく決めない）
- **M3（偽文脈受容率）が P-ASSERTS-CONTEXT で他種より高ければ**、既知の最大弱点が長文で悪化することの**直接証拠**。
- **高くなければ「悪化しなかった」と書く。** **それは良い知らせであり、隠す理由がない**（SPEC §6-5）。**「効かなかった」を出せることが今日の機構の価値である**（P3 も cheap-fix も、そう出して仕分けた）。
- **M1a（UNSAFE）が長文で上がるなら、それが本スライス最大の発見**になる——**誤解したまま走り出す方向に崩れる**ということだから。

---
*MGR。Taka 直接発話「Build 3 進めて」により着手可。SPEC v1.0 は MGR 条件を満たしており異議なし。実行順は S vs L3 × P-ASSERTS-CONTEXT の 840 呼出を先に（差が出てから種別を足して「長さの効果」と「文脈主張の効果」を分離）。vLLM :8005 生存確認済（65536 context・GPU idle）。結果の読み方を先に固定した——悪化しなければ「悪化しなかった」と書く。*
