# CC 管理(MGR) → 設計/監査(CC-α): Build 1c — 最新 BUILT の独立監査 + 残る裁定2件 + 順番（HANDOFF）

- `BUILD_ROLE: 参照`（実装源でない。既存文書を名指すだけ）
- 宛: DESIGN/AUDIT(CC-α) / 写: IMPL / Taka / 発: MGR(交代後の新 instance) / 2026-07-26 19:45 / TYPE=HANDOFF
- **本文書は 19:20 に一度書き、19:45 に全面改訂した。** 書いている間に `PROSE_IS_NOT_RECORD_ADJUDICATION`（19:29）と `PROSE_REMOVED_CLAIM_REGISTRY_BUILT`（19:34）が着地し、旧版が求めていた「§2 と §5 の矛盾を裁定せよ」は**両者で既に解決済**だったため。旧版の該当節は破棄する。
- 対象: **`CC_IMPL_2026-07-26_PROSE_REMOVED_CLAIM_REGISTRY_BUILT.md`**（最新・未監査）
- 前段: `..._BUILD1C_ADJUDICATION_FINDING.md` → `..._BUILD1C_ADJUDICATION_APPLIED_BUILT.md` → `..._PROSE_IS_NOT_RECORD_ADJUDICATION.md`

## 0. MGR の立場
**最新 BUILT は未検証である。**【監査:IMPL】は自己申告であり独立再検証を経ていない。**したがって「汚染が完全に解けた」「HBB-30 が接地hit 0 で SUPERSEDE に到達した」を私は Taka へ成果として上げていない。** 監査が通ってから上げる（[[impl-built-is-unverified-until-audit]]）。

## 1. 依頼A（最優先）— 最新 BUILT の独立再検証（`【監査:CC-α】` で検証者名を明示）
1. **★核心**: 答えファイル（`hbb_candidates.json`）**なし**で HBB-30 が `SUPERSEDE` に到達するか。**かつ IMPL の主張「接地hit 0」＝出口が探索ではなく `CLAIM_STATUS` 登記だけから出ていること**を、**登記台帳を外した対照**（IMPL 報告では `UNKNOWN`/`CLARIFY_FIRST` に落ちる）まで含めて再現すること。**この対照が load-bearing の唯一の証拠である。**
2. **汚染解消3件の再現**（負の対照＝`NOT_FOUND` / 実在ファイル＝`GROUNDED` / PP1＝`UNKNOWN`）。**併せて「DE を足しても計器が動かない」を、CC-α 自身が新しい DE を1件足して確かめること**——IMPL が自分の実装を自分の追記で検査した形になっているため、独立性がここで弱い。
3. **非回帰 13/13**（既定）と `--check` GREEN。
4. **散文除外の副作用**: `evidence_refs` から「パス形式トークンだけ抽出」した結果、**以前 `GROUNDED` だった実在対象が拾えなくなっていないか**（偽陰性側）。IMPL 報告は真陽性1件（`preflight_gate.py`）のみで、**取りこぼしの母数を出していない。**
5. **M3 = 0/298 は変わっていない**＝実運用の発話では一度も発火しない機構であることを、成果の表現から落とさないこと。

## 2. 依頼B — 残る裁定2件
### ① `SUPERSEDE` 優先の扱い（**CC-α の予測が外れた件**）
- 裁定§5 は「registry 実装後に衝突は消える可能性が高い」としたが、**IMPL 実測では消えず 12/13 のまま**（`test 12`）。**原因は散文検索ではなく「量的主張そのものを存在照合の対象にしたこと」。**
- **予測が外れた事実を消さずに、改めて裁定すること**（裁定§5 が自ら「消えなければ改めて裁定する」と書いている）。
- IMPL の提案（失敗履歴あり時は `STRONGLY_DISCOURAGE_DW` を上に置く一段）は**提案止まりで未実装**＝規律どおり。採否は設計側で決める。
- MGR の注意（裁定ではない）: HBB-30 **1例のために優先表をいじるのは overfitting**、というのは CC-α 自身が §4 で立てた原則である。既定据え置き（非回帰優先）を続けるなら、**「HBB-30 は既定では解けていない」と明記した上で続けること。** 解けたことにしない。

### ② 弁別語 df 閾値 20 の妥当性
汚染が解けた今なら測れる、と IMPL が申し出ている。**測らせること。** 受入は前回裁定どおり「閾値を動かして結果が大きく動くならその脆さも報告」。

## 3. 順番（MGR 指示）
1. **依頼A・B（1c）** ＝ 本命。片付くまで飛ばない。
2. **(a) retention 補修** — **下記 §4 のとおり Taka の直接確認を私が取りに行っている。** 確認が取れ次第、DESIGN が `★実装源` BUILD_SPEC を1本書く（現在 (a) は IMPL でなく DESIGN 側で止まっている）。
3. **Build2（後者の縦串 retrieve + 意図誤り注入試験）** はその後。

## 4. 【伝聞】扱いについて（MGR の回答）
IMPL と CC-α が「MGR 経由の GO は Taka 直接発話ではないので【伝聞】」として着手を止めた判断を、**MGR は支持する。** 今日の過大報告の履歴を踏まえれば正しい慎重さである。
- 典拠は memory ではなく `CC_MGR_2026-07-26_RETENTION_REPAIR_A_GO.md` §権限（前 MGR instance が Taka 発話を引用した形）。**私（交代後の MGR）はその場に居ないので、私自身にとっても伝聞である。**
- **したがって私が Taka に直接確認を取る。** それまで (a) は着手しないこと。**この構図（MGR 経由の承認をどこまで一次典拠として扱うか）自体も Taka に諮る。**

## 5. 計器の注意（再掲）
LLM を挟む比較は temperature=0.7 で run 間ノイズ ≈0.10（≒6 fixture）。**単発の数値で効いた/効かないを言わない。分布 or paired。** 1c の判定系は決定論なのでこの限りでないが、**決定論だからこそ「対照を外した時にどう壊れるか」を必ず対で出すこと**（`..._PAIRED_MEASUREMENT_PRINCIPLE_FINDING.md`）。

---
*MGR。最新 BUILT は未監査ゆえ Taka へ成果として上げていない。依頼A=登記台帳を外した対照/CC-α 自身が DE を足しての汚染再検査/偽陰性側の母数/非回帰。依頼B=①優先表（CC-α の予測が外れた・既定据え置きなら「HBB-30 は既定では解けていない」と明記）②閾値20 を今測る。(a) は私が Taka に直接確認を取るまで着手しない。*
