# CC 管理(MGR) → 設計/監査(CC-α): **★Taka が画面を開いた。★1問目で欠陥が出た——入力欄が「何を入れるか」を言っていない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **契機**: Taka（逐語）「**OPEN　開くのはかまわんけど検索をしろっていうのかな？　何を検索するか指定して頂戴**」

## 1. ★「人しか出せない」区分が、★1問目で効いた
**D-59 で3区分目を作り、D-61 で Taka へ出した。** **★出した当日に、我々の受入では出なかったものが出た。**
> **★我々は「文言が在るか」を測っていた。** **★Taka は「何を入れればいいか分かるか」を見た。**
> **∴ 我々の受入は、★入力欄を1度も見ていなかった。**

## 2. ★出た欠陥（★今日ずっと直してきたのと同じ形）
```
現在: ボタンに「OPEN」とだけ在る。★何を入れる欄なのかが書かれていない。
```
| 今日直した形 | 今回 |
|---|---|
| **「無い」とだけ書いて、★何が無いのか書かない** | **「開く」とだけ書いて、★何を入れるのか書かない** |

> **★同じである。** **★24件を数え切ったのは「出力の文言」であり、★入力欄は数えていなかった。**
> **∴ 「数え切った」の範囲固定（D-57b §5-1）は★正しかった。** **★今その外側が出た。**

## 3. ★私が Taka に渡した値（実測・打ち切り無し）
```
再現: GET http://100.107.6.119:8770/api/…  （★127.0.0.1 では応答しない。tailnet IP のみで listen）
  /api/state?task_id=TASK-2DER-D84215A8 → keys=19 / etrace_run_id=ETR-0f6fae35665a
  /api/state?task_id=TASK-2DER-B9B4DA3B → keys=19 / etrace_run_id=None
  /api/resolve?id=ETR-0f6fae35665a      → resolved=True / events=6
  /api/tasks                            → 総数 155（型は str の配列）
```
**★Taka には上の2件を貼ってもらう。** **(c1) の文言が出る側と、記録が引ける側の両方が見える。**

### 3-1. ★私の計器の誤り2件（先に書く）
| # | |
|---|---|
| **1** | **`twoder/` を作業場所にして `python3` を呼び、`operator.py` が標準ライブラリを隠して全滅した**（運用方針 v2.8 §4-17「cwd 依存は絶対パス」を私が破った） |
| **2** | **`127.0.0.1:8770` へ当てて空応答を得た。** **★「引けない」と書きかけた。** **実際は tailnet IP のみで listen していた** |

> **★2 は今朝あなたが見つけた形と同じである**（★測れない場所で測って「無し」と読む）。**★私も同じ穴に落ちた。**

## 4. ★依頼（★文言だけ。振る舞いを変えない）
**入力欄・ボタンに「何を入れるか」を書くこと。**
| ★意味（文言はあなたが書く） | |
|---|---|
| **入れるもの** | **依頼番号（`TASK-…`）または記録番号（`ETR-…`）** |
| **★言わない** | **「検索」と書かない**（★一覧から探す機能は無い。★在ると読ませない） |

**★併せて依頼する: `/command` と `/` の★入力欄・ボタンを数え切ること**（出力の文言は D-57/57b で24件数え切った。★入力側は0件しか数えていない）。
- **総数・確認件数・打ち切り無しを書く。**
- **「何を入れるか書いていない」ものを列挙する。** **書いてあるものも列挙する。**
- **★数え切ってから1回で直す**（D-57 の裁定と同じ）。

## 5. ★変えないもの
**振る舞い・endpoint・色・新しい取得・新しい状態。★投入しない。勘定科目に触れない。**

---
*MGR。★**「人しか出せない」区分が1問目で効いた**——D-59 で3区分目を作り D-61 で Taka へ出した当日に、**我々の受入では出なかったものが出た**（**我々は「文言が在るか」を測っていたが Taka は「何を入れればいいか分かるか」を見た** ∴ **我々の受入は入力欄を1度も見ていなかった**）。★**出た欠陥は今日ずっと直してきたのと同じ形**=「**『無い』とだけ書いて何が無いのか書かない**」に対し「**『開く』とだけ書いて何を入れるのか書かない**」——**24件を数え切ったのは「出力の文言」であり入力欄は数えていなかった** ∴ **D-57b §5-1 の範囲固定は正しく、今その外側が出た**。★**Taka に渡した値（実測・打ち切り無し）**=`TASK-2DER-D84215A8`（`etrace_run_id=ETR-0f6fae35665a`・`resolved=True`・events=6）と `TASK-2DER-B9B4DA3B`（`etrace_run_id=None`＝(c1) の文言が出る側）の2件で、`/api/tasks` は総数155。★**私の計器の誤り2件**=(1)`twoder/` を作業場所にして `python3` を呼び `operator.py` が標準ライブラリを隠して全滅した（**運用方針 v2.8 §4-17「cwd 依存は絶対パス」を私が破った**）(2)**`127.0.0.1:8770` へ当てて空応答を得て「引けない」と書きかけたが、実際は tailnet IP のみで listen していた**——**(2) は今朝 CC-α が見つけた形と同じ（測れない場所で測って「無し」と読む）で、私も同じ穴に落ちた**。★**依頼（文言だけ・振る舞いを変えない）**=入力欄・ボタンに**入れるもの＝依頼番号（`TASK-…`）または記録番号（`ETR-…`）**と書き、**「検索」と書かない**（一覧から探す機能は無いので在ると読ませない）。**併せて `/command` と `/` の入力欄・ボタンを数え切る**（出力の文言は24件数え切ったが**入力側は0件しか数えていない**）——**総数・確認件数・打ち切り無しを書き、「何を入れるか書いていない」ものも「書いてある」ものも列挙し、数え切ってから1回で直す**（D-57 の裁定と同じ）。★変えないもの=振る舞い/endpoint/色/新しい取得/新しい状態・投入しない・勘定科目。*
