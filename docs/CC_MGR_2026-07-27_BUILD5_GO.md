# CC 管理(MGR): **Build 5 へ GO**（HANDOFF・短く）

- `BUILD_ROLE: 参照` / 宛: IMPL(coder) / DESIGN(CC-α) / 写: Taka / 発: MGR / 2026-07-27 04:50 / TYPE=HANDOFF

## 1. 権限
Taka 直接発話（2026-07-27・逐語）:
> **「LLMが選択をする際に選択のヒントになる情報を渡せば、LLMはそのどれかを選ぶこと自体は問題ない、はずなんだけど？ 試しに設計と実装に頼んで簡単なテストをさせてみればわかるよ」**

**着手可。** 実装源は `CC_DESIGN_2026-07-27_BUILD5_SPEC_GIVE_THE_FACT.md` の1本。**まず §5 の確認（決定論の判定が意図調べに渡っているか）をコードを読んで事実として報告すること**（LLM 不使用・安い）。

## 2. SPEC への MGR の評価（異議なし・2点を特に支持）
1. **交絡を先に申告している**: 事実文「指す対象は記録から特定できない」は戦略定義「対象が何を指すか意図が不明」と**ほぼ 1:1**。**∴ 効いても「賢くなった」と書かない。「事実を渡すと当たるようになった」と書く。** ——**測る前に言い換えを封じたのは正しい。**
2. **受入の重み付け**: **「既に正しかったものを壊していないか」を、当たるようになったかより重く置いた。** 支持する。**増やすより壊さない方が重い。**
3. 文面を DESIGN が固定し**全 fixture に一様適用**（IP1/IP2 に有利な事実だけ足すのは cherry-pick）——支持。

## 3. 追加なし
**MGR からの追加条件は無い。** 既定（3つの数字・per-fixture・paired/分布・上がらなければ上がらなかったと書く）は SPEC に入っている。

---
*MGR。Build 5 へ GO（Taka 直接）。まず「決定論の判定が意図調べに渡っているか」をコードで確認して報告。SPEC は交絡(事実文と戦略定義がほぼ 1:1)を測る前に申告し、非回帰を改善より重く置いている——両方支持。MGR からの追加条件なし。*
