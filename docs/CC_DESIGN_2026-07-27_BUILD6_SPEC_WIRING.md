# BUILD SPEC — Build 6: 意図調べを front door に配線する（段3e）+ 事実の出し分け + 直叩き閉塞3箇所

- **`BUILD_ROLE: ★実装源`**（本 build task の唯一の実装源。**IMPL はこの1本だけから作る**）
- 宛: IMPL（coder） / 写: MGR / Taka
- 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=BUILD_SPEC / v1.0
- 権限: `CC_MGR_2026-07-27_WIRING_DESIGN_ACCEPTED_WITH_CONDITIONS.md`（条件①②③つきで受領）
- 設計本体: `CC_DESIGN_2026-07-27_WIRING_DESIGN_v1.md`（`BUILD_ROLE: 参照`）

## 0. ★これは精度の改善ではない（先に固定・条件③）
**配線が通っても「2DER が良くなった」「精度が上がった」と書かない。**
正しくは: **「意図調べが本番の経路に入った。効果は別に測る。」**
**本 SPEC に精度の測定を含めない**（MGR 規律「精度の線に戻る新しい測定を混ぜない」）。

## 1. ★作らないもの（既存を読んだ結果・作り直し禁止）
**【監査:CC-α】以下は既に本番で動いている。新規に作らない。**
| 既存 | 場所 | 本 SPEC での扱い |
|---|---|---|
| 文脈の判定（`anchoring`: `UNRESOLVED`/`LOW`/`MEDIUM`/`HIGH`） | `submit.py` 段2 `context_binding.bind_context` | **出力をそのまま使う** |
| 何をしてほしいかの分類 | 段3b `request_type.classify_request_type` | **触らない**（意図調べと別軸） |
| 存在の接地 | 段3d `existence_grounding`（`preflight_gate` 内） | **出力をそのまま使う** |
| 指示語・束縛先の判定 | `preflight_gate` の `BIND-RULE-001` | **出力をそのまま使う** |

## 2. 配線（段3e を新設）
```
3d PREFLIGHT GATE（既存）
   ├ triggered かつ blocks → return TRACE（既存挙動・★変えない）
   └ 通過
★3e 意図調べ（新規）
4  ROUTING（既存）
```
- **3d で止まった入力では 3e を呼ばない**（無駄な LLM 呼出をしない）。
- **段4 の routing 入力に、3e の結果（応じ方）を乗せる。**
- **`submit()` の戻り値 `TRACE` に `INTENT_STRATEGY` を記録する**（既存キーは変えない）。

## 3. ★事実の出し分け（D-9 の中核・決定論・LLM 不使用）
### 3-1. 出す条件
```
指示語が有る  OR  過去参照が有る   → 文脈・接地の事実を出す
どちらも無い（自己完結した依頼）   → 文脈の事実を出さない
```
**根拠【監査:CC-α】**: Build 5 で 20 fixture 中 17 に「文脈が無い」を配った結果、**止まる率が 0.371 → 0.420 に寄り、自己完結した依頼を壊した**（BV3 0.93→0.57 / BV1 1.00→0.80）。**無関係な事実は「何か足りない」と読まれる。**

### 3-2. 文面（DESIGN 指定・IMPL は考えない）
| 判定（既存出力） | 出す行 |
|---|---|
| `anchoring == "UNRESOLVED"` | `直前の文脈は記録に存在しない。` |
| `anchoring == "LOW"` | `支配的な文脈が定まらない。` |
| `anchoring in {"MEDIUM","HIGH"}` | `直前の文脈が記録にある: <supporting_context_refs 由来の本文>` |
| 指示語あり・束縛先なし | `「<語>」が指す対象は記録から特定できない。` |
| 接地 `NOT_FOUND` / `UNKNOWN` / `GROUNDED` | `「<対象>」は記録に見つからない／記録にあるか判定できない／記録にある。` |

**禁止（Build 5 と同じ）**: **行動を示唆する語を足さない**（「だから聞き返せ」等）/ **戦略名とその定義語を入れない** / **入力ごとに文面を変えない。**

## 4. 直叩きの閉塞 — **3箇所のみ**（条件①）
| 対象 | 対応する依頼 | 通す経路（既存） |
|---|---|---|
| `twoder/live_worker_runtime.py:140` `admit_design_evidence` | 「この実行結果を開発根拠として登録する」 | 段1.5 **DE admission fast path**（既存） |
| `twoder/runtime_supervisor.py:218` `admit_design_evidence` | 同上 | 同上 |
| `twoder/experiment_candidate.py:116` `create_task` | 「この候補をタスクとして起こす」 | 段4 ROUTING → DW（既存） |

### 4-1. ★`record_plan` 2箇所は「保留」であって「対象外」ではない（条件①）
- `twoder/webui.py:315` / `twoder/build_planner.py:301`。
- **「閉塞対象から外した」と書かない。** **「対応する依頼型の有無が未確認のため保留」と書く。**
- **DESIGN の推し（内部工程だから orphan ではない）は未確認であり、確認せずに結論しない。**
- **確認を名前のある作業として残す**: **`MIGRATION-PLAN-CHECK`**（`record_plan` の上流に依頼が在るかをコードで確認する。LLM 不使用）。**本 SPEC には含めない。**

## 5. ★暫定の単純化を明記する（条件②）
> **本 SPEC は「1つの問い合わせ = 1つの明細」の前提で通す。**
> **これは Taka が「そもそもナンセンス」と指摘した前提そのものであり、暫定の単純化である。**

**見直す条件（先に書く）**:
1. **配線が1本通った後**（本 SPEC の完了後）、かつ
2. **明細への分解を誰がやるか（決定論か LLM か）が決まった後**。**LLM なら新しい判断点であり、既知の弱点が入る場所である。**
3. **account chart は完成・凍結につき触らない。**

**BUILT にもこの但し書きを書くこと。黙って既定にしない。**

## 6. 受入
1. **★既存 LIVE の非回帰が最優先**: `twoder/regression/test_preflight_gate.py` **13/13** + 触った経路の既存テスト全て。
2. **段3e が段3d 通過時のみ呼ばれること**（3d で止まった入力では呼ばれない）を記録で示す。
3. **段4 の routing 入力に応じ方が乗っていること**を `TRACE` で示す。
4. **事実の出し分けが決定論で再現する**（同じ入力なら同じ事実行）。
5. **★自己完結した依頼に文脈の事実が載らないこと**（D-9 の核心・Build 5 の欠陥の是正）。**LLM を呼ばずに prompt 文字列で示せる。最初に出すこと。**
6. **閉塞3箇所が front door 経由になり、既存の呼び出し元が壊れていないこと。**
7. **`record_plan` 2箇所を「保留」と明記**（「対象外」と書かない）。
8. **§5 の暫定の単純化を BUILT に明記。**
9. **精度の測定を含めない。** 配線が通ったことだけを示す。
10. **commit しない**（MGR）。**投げる前にコストが前回と大きく違うなら先に言う。**

## 7. ★MIGRATION-3 で何が足りなくなるかの予告（条件・feasibility-first）
**次の作業「我々の開発依頼を front door から入れる」（MIGRATION-3）で、本 SPEC の後に足りないものを先に書く:**
1. **依頼の投入口**: 現在 `submit()` を呼ぶのは我々の script である。**MGR/DESIGN の指示文（例「Build 5 を実行せよ」）を投入する経路が無い。** ＝ **投入口そのものを作る必要がある。**
2. **routing の結果に従う仕組み**: 2DER が「これは BUILD_CAPABILITY だ」と決めた時、**その決定に Claude が従う経路が無い**（今は Claude が自分で決めて自分で書いている）。**「従う」を実装する必要がある。**
3. **完走の判定**: 「設計→実装→監査→commit が1つ完走」を**誰がどう記録するか**が未定。
4. **【設計:CC-α】1 が最も重い。** 投入口が無い限り、MIGRATION-3 は始まらない。**次の設計はここになる見込み。**

---
*BUILD SPEC v1.0（★実装源）。★これは精度の改善ではない(配線が通っても「2DER が良くなった」と書かない)。作らないもの4つを明記(文脈判定・request_type・接地・束縛判定は既に本番で動いている)。段3e を 3d の後・4 の前に置く。D-9=指示語か過去参照が有る時だけ文脈の事実を出す。閉塞は3箇所、record_plan 2箇所は「保留」であって対象外ではない。★「1問い合わせ=1明細」は暫定の単純化と明記し見直し条件を先に書く。MIGRATION-3 で足りなくなるもの4点を予告(最重要=指示の投入口が無い)。*
