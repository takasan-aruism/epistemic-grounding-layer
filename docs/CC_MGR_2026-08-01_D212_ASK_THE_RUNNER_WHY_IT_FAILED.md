# 【D-212】次の1手を私が選ぶ ― ★`RUNNER_FAILED` の★理由を、★1回だけ取りに行く

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-08-01 19:2x / TYPE=STATUS
- **開発者規律 確認済（版: v1.1）** ／ **受領**: `CC_DESIGN_..._EVO0031_TWO_PREMISES_DO_NOT_HOLD.md` ／ `CC_IMPL_..._C2_ACCEPTANCE_C_MEASURED_BUILT.md`
- **ACTIVE は `EVO-0031` の1件のまま**（★C-2・S-3・C-4 は後回し。★戻す条件は台帳の note に在る）

## 1. ★私が選んだ1手
> ### **★`RUNNER_FAILED` と書いた当人（★試験の実行器）に、★★なぜ落ちたかを言わせる。★1回だけ。**
```
★理由: ★★いま分かっているのは「★落ちた」だけで、「★なぜ」は ★我々の推測しかない。
★★設計の実測: ★両 task とも ★`reason = RUNNER_FAILED` ／ ★`worker_run_ref = null`
   ＝ ★★試験は ★一度も走っていない。★★実行器が ★起動する前に落ちている可能性が高い【未確認】
★★★手がかり（★設計が既に出した・★これだけ違う）:
   ★前: `test_command = ['python3','/tmp/2der_human_view_sandbox/test_human_view.py']`（★絶対パス）
   ★今: `test_command = ['python3','test_human_view.py']`（★ファイル名だけ）
   ★★★∴ ★★`cwd` が違えば ★ファイルが見つからない。★これは ★開発者規律 第7章の
      「★作業ディレクトリに依存するコマンドは絶対パスで始める」と ★同じ形である。★ただし ★断定しない。
```

## 2. ★依頼（★1件。★実装しない。★取りに行くだけ）
```
★(a) ★`RUNNER_FAILED` を書いている場所から、★★実行器自身が残した理由（★stderr / 例外 / 終了コード）を
   ★★front door から読めるか ★3値で返す（★在って動く／在るが未配線／無い）
★(b) ★読めないなら ★★それが結果である。★★「★失敗の理由が読めない」＝ ★同型7件目として書く
   ★★★（★規律 v1.1 と ★第5章「失敗を正常に見える別の結果に置き換えない・理由を捨てない」の両方に当たる）
★(c) ★読めるなら ★逐語で持ち帰る。★★そこで初めて ★直す対象が決まる。
★★★★★直し方を ★今は書かない（★原因未確定のまま実装しない）。★`cwd` の件も ★候補のまま置く。
```

## 3. ★選ばなかった手（★書いておく）
```
★(A) ★実行器を直す → ★★却下。★原因が未確定。★v0.3 §12.6 の逆をやることになる
★(B) ★依頼文を変えて通るようにする → ★★却下。★★「黙って通す」形になる。★今日ずっと潰してきたもの
★(C) ★C-2 の受入(c) を別の方法で測る → ★後回し（★ACTIVE は1件）。★IMPL の測定結果は受領して残す
★(D) ★S-3 を別 task で作り直す → ★却下。★試験が走らない限り ★何度作っても合否が出ない
```

## 4. ★受領（★C-2 受入(c)・★後回しのまま記録だけ残す）
```
★IMPL 実測: ★空のままの雛形（★`def test_` 0行）を投入 → ★止まった。
   ★★ただし ★止めたのは ★契約の検査ではなかった ∴ ★受入(c) は ★★示せていない。
★★★これは ★★良い報告である。★「止まったから合格」と書かなかった。★★止めた主体を確かめている。
★★★★C-2 は ★DEFERRED のまま。★戻す条件（`EVO-0031` 完了）は ★台帳の note に在る。
```

## 5. ★状態（★台帳から）
| 件 | 状態 |
|---|---|
| `EVO-0031` 試験が走らない | **★IN_PROGRESS（ACTIVE・本書）** |
| `EVO-0022` C-2 ／ `EVO-0027` S-3 | ★DEFERRED（戻す条件＝`EVO-0031` 完了） |
| `EVO-0024` C-4 ／ `EVO-0023` C-3 | ★PROPOSED（後回し） |
| 後回し | `D-199` ／ `G-84` ／ 滞留6件 ／ ★マーカーだけの投入で task_id が返った2件（★1回の観測） |

---
**決めたこと**: **①次の1手は「`RUNNER_FAILED` と書いた実行器に、なぜ落ちたかを言わせる」——1回だけ取りに行く。いま分かっているのは「落ちた」だけで「なぜ」は推測しかないからである ②両 task とも `worker_run_ref` が null で試験は一度も走っておらず、実行器が起動前に落ちている可能性が高い（未確認）。唯一の違いは `test_command` が絶対パスかファイル名だけかで、これは規律 第7章と同じ形だが断定しない ③依頼は3値で1件——実行器自身の理由（stderr/例外/終了コード）が front door から読めるか。読めなければ「失敗の理由が読めない」が結果であり同型7件目として書く。読めたら逐語で持ち帰り、そこで初めて直す対象が決まる。直し方は今は書かない ④選ばなかった手を書いた——実行器を直す（原因未確定）／依頼文を変えて通す（黙って通す形）／C-2 を別法で測る（ACTIVE は1件）／S-3 を作り直す（試験が走らねば合否は出ない）⑤C-2 受入(c) の測定を受領。空の雛形は止まったが止めたのは契約の検査ではなく、IMPL は「止まったから合格」と書かずに止めた主体を確かめた。C-2 は DEFERRED のまま。**
