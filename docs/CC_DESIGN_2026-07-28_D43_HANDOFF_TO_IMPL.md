# 設計/監査 → **実装(IMPL)**: **prefix 是正 ＋ 読み出し口を実装してください**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**送付状。★実装源は本文書ではない**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.2` — `§12` で日付が最も新しい行を採って確認）**
- **承認**: `CC_MGR_2026-07-28_D43_SPEC_APPROVED_AND_ARCH_STALE.md`

## 1. ★実装源（これ1本）
```
CC_DESIGN_2026-07-28_D43_READ_PATH_BUILD_SPEC_v1_0.md   ← BUILD_ROLE: ★実装源
```
**★他の CC_* 文書（D-42 の SPEC / 監査 / MGR 裁定文書）は背景であって実装源ではない。**

## 2. ★この build は2つで1つ
> **prefix を直さずに読み出し口だけ作らない**（MGR 裁定）。
> **理由: いま 2DER は、実在する id を「穴だ」と報告している。** **衝突したままの id は、口を作っても引けない。**

## 3. ★特に外しやすい3点
| # | 一行 |
|---|---|
| **1** | **`ids.py` の docstring の所有表に `ETR-` を登記する**（SPEC §3-4）。**★省いたら不合格。** 登記しなければ次の誰かが同じ衝突を作る |
| **2** | **既存2件（`RUN-…` / `EV-…`）を1行も消さず書き換えず、「旧 prefix として意図的に残した」と報告に明記する**（§3-5） |
| **3** | **EGL 側を1行も変えない**（§3-6）。**`RUN-` も `EV-` も EGL のままである** |

## 4. ★止まってよい場所（SPEC §5 の再掲。自分で決めない）
- **`ETR-` が実は使われていた** → **★私の全走査漏れである。報告してほしい**
- **親子が繋がらなかった** → **★繋げる修正をしない。事実を報告する。** **MGR は「それを不合格として扱わない」と明言している**
- 既存の id 解決が変わった／SPEC が2通りに読める

## 5. ★資料の同期について（あなたの負担を1つ減らす）
**MGR は「BUILT と同時に資料を同期すること」と依頼した。** **★D-42 の分は私が済ませた**（`C-ETRACE` と辺2本を追加・commit を現在の HEAD に更新・`gap 45` で計器は「一致」に戻った）。
> **★ただし本 build で `ids.py` と `etrace.py` が再び変わる。** **∴ BUILT の時点でもう一度ずれる。**
> **★その2回目は私がやる。** **あなたは触らなくてよい。** **触った場合は報告だけしてほしい。**

## 6. 投入
- **★1回だけ**（新 prefix の run を1つ作るため）。**文面は問わない**（今回は routing に依存する受入項目が無い）。
- **★webui を再起動してから投入する**（本日2回踏んだ型）。

---
*CC-α → IMPL 送付状（D-43）。★実装源は `CC_DESIGN_2026-07-28_D43_READ_PATH_BUILD_SPEC_v1_0.md` の1本のみで、D-42 の SPEC・監査・MGR 裁定文書は背景。★この build は prefix 是正と読み出し口の2つで1つ（MGR 裁定「prefix を直さずに読み出し口だけ作らない」——実在する id を「穴だ」と報告している状態で口だけ作っても引けない）。★特に外しやすい3点=①`ids.py` の docstring の所有表に `ETR-` を登記する（**省いたら不合格**。登記しなければ次の誰かが同じ衝突を作る）②既存2件（`RUN-…`/`EV-…`）を1行も消さず書き換えず「旧 prefix として意図的に残した」と報告に明記③EGL 側を1行も変えない（`RUN-` も `EV-` も EGL のまま）。★止まってよい場所=`ETR-` が実は使われていた（**CC-α の全走査漏れなので報告してほしい**）／**親子が繋がらなかったら繋げる修正をせず事実を報告する（MGR は「それを不合格として扱わない」と明言）**／既存の id 解決が変わった／SPEC が2通りに読める。★資料の同期は D-42 分を CC-α が済ませた（`C-ETRACE` と辺2本を追加・commit を現在の HEAD に更新・計器は「一致」に復帰）が、**本 build で `ids.py` と `etrace.py` が再び変わるので BUILT 時点でもう一度ずれる——その2回目は CC-α がやるので IMPL は触らなくてよい**（触った場合は報告のみ）。★投入は1回だけで文面は問わず、webui を再起動してから行う。*
