# CC 管理(MGR) → 設計/監査(CC-α): **★D-18 のゴールを Taka の問いに置き換える — 「一本の流れに沿っているか」**（裁定・優先度変更）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**

## 0. Taka 直接発話（逐語）
> **「RRI、とあなたが言っているものは4軸７カテゴリやEGL登録の際の自動的な勘定科目の機能など全て整っている？　確実に一本の開発の流れにそっているかどうか、それだけ。もしそれができていればあなたが健忘になってもシステムは動く。」**

## 1. ★MGR の答え — **私は答えられない。答えてはいけない**
- **私は現物を読まない立場である**（運用方針 §3-5）。**∴ 既存実装について断定する資格を持たない。**
- **記憶で答えるのは、本日3回誤った形そのものである。**
- **∴ 私が答えるのではなく、機械が答える形にする。** **それが Taka の問いに対する唯一まともな返し方である。**

**本日の監査で確立している範囲だけを、出所つきで挙げる**（**これも私の伝聞であり、D-18 で検証されるべきもの**）:
| 機構 | 本日確立した範囲 | 未確立 |
|---|---|---|
| **4軸7カテゴリ** | `rri/request_resolution.py` に在り、**段3e として本番に配線され 18:42 に動作を確認**（TRACE に `INTENT_STRATEGY`） | **効果（当たるか）は未測定** |
| **勘定科目の自動設定** | **埋め込み系統(B)として実装済**（`s_embed_axes` → `s_account_axes` → `s_rthread_2br3` → `s_account_axis_names`）。**軸の凍結は Taka 承認が要る** | **★EGL 登録の経路に繋がっているかは未確認** |
| **一本の流れに沿っているか** | **★未確認。これを答える機構が無い。** | — |

## 2. ★D-18 のゴールを置き換える（延長ではない・目的の明確化）
**旧ゴール**: 孤児一覧を出す。
**新ゴール（Taka の問いそのもの）**:
> **★名前の付いた機構ごとに「本番の一本の流れに乗っているか / 実装済だが繋がっていないか / 試験専用か / 判定不能か」を、機械が1画面で答える。**

**判定に使うもの（既存・新規に作らない）**: `EDGE_INVENTORY` の状態語彙（`LIVE` / `IMPLEMENTED_UNWIRED` / `TEST_ONLY_ISLAND` / `WIRED_UNENTERED` / `UNKNOWN`）＋ `LEDGER_REGISTRY`。

### 2-1. ★最初に答えるべき2つ（Taka が名指ししたもの）
1. **4軸7カテゴリ** — 本番の流れに乗っているか。
2. **★EGL 登録時の勘定科目の自動設定** — **登録の経路に繋がっているか。繋がっていなければ「繋がっていない」と出す。**

**この2つが答えられれば D-18 は「効いた」と言ってよい。** **全機構の網羅は後でよい。**

### 2-2. 粒度の扱い（既定どおり）
**能力は複数ファイルに散るので1行にならない。** **既定の対処（`genesis` コミットでのグループ化・名前を付けない）をそのまま使う。**
**ただし §2-1 の2つについては、CC-α が「この機構はこのファイル群である」と明示的に対応を書いてよい**（**Taka が名指ししたため、推測ではなく指定である**）。

## 3. ★優先度を変更する
| 順 | 旧 | **新** |
|---|---|---|
| 1 | 台帳を読める仕組み | **★D-18（一本の流れに沿っているか）** |
| 2 | D-18 | 台帳を読める仕組み（`contract` 調査 D-22 を含む） |
| 3 | 忘れている機能の棚卸し | （D-18 に吸収） |

**理由**: **Taka の問いは「健忘しても動くか」であり、それが答えられない限り、他の作業の意味が測れない。** **かつ D-18 はコード構造の読みで作れる**（台帳の中身を読まなくてよい＝優先度2の完成を待たない）。

## 4. 禁止（膨らませない・v1.9）
- **新しい台帳を作らない。** **既存2つ（`LEDGER_REGISTRY` / `EDGE_INVENTORY`）の結合のみ。**
- **計器の健忘3件の修理は既定どおり含める**（`TODAY` のハードコード等）。**修理であって新機能ではない。**
- **「ついでに」を足さない。** **§2-1 の2つが答えられた時点で、いったん止めて MGR に上げること。**

---
*MGR。Taka の問い「4軸7カテゴリや EGL 登録時の自動勘定科目は整っているか。一本の開発の流れに沿っているか」に対し、★私は答えられないし答えてはいけない（現物を読まない立場・記憶で答えるのは本日3回誤った形）。∴ 機械が答える形にする。D-18 のゴールを置き換え=「名前の付いた機構ごとに、本番の一本の流れに乗っているか/実装済だが繋がっていないか/試験専用か/判定不能か」を機械が1画面で答える。★最初に答える2つ=①4軸7カテゴリ ②EGL 登録時の勘定科目の自動設定（登録経路に繋がっているか。繋がっていなければ「繋がっていない」と出す）。この2つが答えられれば D-18 は効いたと言ってよく、網羅は後でよい。★優先度変更=D-18 を1位に（Taka の問いが答えられない限り他の作業の意味が測れない・かつコード構造の読みで作れるので優先度2の完成を待たない）。禁止=新しい台帳を作らない・修理は含める・「ついでに」を足さない・2つが答えられたら止めて MGR に上げる。*
