開発者規律 確認済(v1.0)

# ★Taka 指示（2026-08-07 23:16）— ★戻せるようにするのが先。駆動役は後

宛: MGR ／ 発: DESIGN（監視兼務）／ 前提: [[CC_DESIGN_2026-08-07_TAKA_RULING_UPPER_REVIEW_IS_CLAUDE_CODE]]

## 1. 逐語（歪めない・要約しない）

> それは Claude code 使ってる側の意見として**当分大丈夫**。人が1件送ってどうこう、という部分は**そもそも WEBUI に許可申請が私向けに送られる仕様を後で追加する、でいい。それまでは自由にさせる**けど、**全件直す、的な動きは不要**。**現状は 2DER の精度を上げることが大事**。今の仕様で直した結果**それを元に戻したくなったらどうする？ と言う問題が残る**。もちろんそのための Git なのだろうが、**戻すアクションでまた3Claude 頼りは面倒すぎる**。

## 2. 確定事項

- **使用量の上限を仕様に入れない**（Taka「当分大丈夫」）。**金額は かからない**（★下記 §3 の訂正）。
- **駆動役の run-gate 問題は、後で WebUI の許可申請で解く。それまでは自由に動かしてよい。**
- **★全件を一括で直す動きは作らない。** 目的は件数ではなく **2DER の精度**。
- **★最優先＝「戻す」が 3Claude 抜きで成立すること。**

## 3. ★私の誤りの訂正（Taka が指摘）

`claude -p` の JSON に出る `total_cost_usd`（実測 0.190249）を**金額と読んだのは誤り**。認証は **OAuth サブスク（max）／`ANTHROPIC_API_KEY` 未設定** ∴ **追加請求は発生しない**。残るのは**使用量の上限**のみで、Taka の判定は「当分大丈夫」。**「1日いくらまで」の上限案は撤回。**

## 4. ★実測（戻す仕組みの現状）

`twoder/artifact_registry.py` に **欄はすべて在る**（`record_change` / `before_commit` / `after_commit` / `affected_artifact_ids` / `resolve_change` / `update_change_after_commit`）。**書かれていない。**

```
登録された成果物            : 4,518
★変更の記録(CHANGE_LOG)     : ★196 のみ
★before/after 両方が在る     : ★24 / 196
★最新の変更記録の日付        : ★2026-07-22（★2週間 停止）
★本日 作った物の登録         : ★0（senior_review.py / may_retry_after_senior_fail.py とも未登録）
★直近3件の affected_artifact_ids : ★0〜1 件（何を変えたかが空）
```

**∴ 「あの依頼の結果を戻す」は、いま 台帳から導出できない。** Git には履歴が在るが **「どの依頼の結果か」の紐付けが無い**ため、**戻す commit を人が探すことになる＝3Claude 頼り**。これが Taka の言う「面倒すぎる」の実体。

## 5. ★やること（順序が仕様の本体）

### 先: 戻せるようにする（★新台帳0・新 state 0・★欄は既存）

1. **`record_change` を、成果物を置く経路から必ず呼ぶ**（書くのをやめた場所を書き直すだけ）。
2. **`before_commit` を必ず埋める**（現状 24/196）。`after_commit` は commit 後に `update_change_after_commit` で閉じる。
3. **`task_id ↔ change_id` を1本 結ぶ**（どの依頼の結果かが台帳から引ける）。
4. **受入** = 「**task_id を1つ与えると、戻すための commit 範囲と対象ファイルが 台帳だけから出る**」。★人が探す工程が 0 であること。

### 後: 駆動役（★全件を回さない）

5. **既存の `dw.dispatch.run_until_barrier`（:145「THIS is the outer loop … NOT Claude」）を呼ぶだけ。新しいループを作らない。**
6. **一度に回す件数を小さく固定**（Taka「全件直す、的な動きは不要」）。
7. **run-gate（`webui.py:861` + `gate_decision.py`）は当面 自由**。**後で WebUI の許可申請に置き換える**（Taka 指示）。
8. **暴走の歯止めは既存で足りる**: `REWORK_ESCALATION_THRESHOLD=2` ／ `may_retry_after_senior_fail`＝1回のみ ／ `max_ops=12`。
9. **使用量の上限に当たったら止まり、台帳に「上限で止まった」と残す**（黙って失敗しない）。

## 6. 参考（今夜の実測・駆動役の分母）

```
★機械だけで進む : ★193件（監査70 / 作り直し55 / 実装55 / 上級監査12 / 完了の門1）
人が要る        : ★57件（計画48 / 処置7 / BLOCKED 2）
```

**∴ 戻す仕組みが無いまま駆動役を入れると、193件が戻せない状態で書き換わる。順序を逆にしない。**

## 7. 小さな指摘（本日の成果物）

`dev-workcell/dw/may_retry_after_senior_fail.py` の先頭に **`# <<<FILL: ここに実装>>>` が残っている**。動作に影響は無いが、**worker への目印がそのまま production に残っている**ので消すこと。
