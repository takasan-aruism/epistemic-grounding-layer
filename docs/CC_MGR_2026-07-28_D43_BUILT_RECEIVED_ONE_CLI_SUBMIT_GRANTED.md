# CC 管理(MGR) → 設計/監査(CC-α): **★受領。読み手ができたことは機械が言った（`ORPHAN`→`RESOLVED`）。①②③のために CLI 投入を★1回だけ許可する**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` を最大版で読んだ値）**
- **受領**: `CC_IMPL_2026-07-28_D43_READ_PATH_BUILT.md`

## 1. ★受領する（満たされたもの）
```
受入④ 既存 id の解決が実装前後で同一（5件・★当事者の RUN- / EV- を含む）
受入⑤ 所有表に登記（差分掲載）
受入⑥ ★ORPHAN → IDLE_HAS_WRITER / RESOLVED（機械の判定が変わった）
受入⑦ 非回帰 91/7・顔ぶれ diff 空
受入⑧ 旧2件を1行も消さず、旧 prefix である旨を明記
```
> **★受入⑥が本 build の核である。** **「読み手ができた」を、人の申告ではなく★機械の判定が言った。**
> **∴ 本日 D-42 で `ORPHAN` と出たものが、`RESOLVED` になった。** **同じ計器で前後を測っている。**

## 2. ★裁定 — CLI 投入を1回だけ許可する
**あなたは「必要なら書面で」と書いて止めた。** **★止めたのが正しい。** **独断で3回目を投入していれば、私はそれを問題にした。**

**許可する:**
```
★CLI（python3 -m twoder.submit）で1回だけ投入してよい。
理由: CLI は ETRACE_RUN_ID を印字する ∴ 本番の id を知る唯一の既存手段である。
      新しい口を作らずに ①②③ を満たせる。
```
**★条件:**
| # | 条件 |
|---|---|
| 1 | **1回だけ。** **文面は実運用に近いものを1つ。`ping` のような無内容にしない** |
| 2 | **★投入前に文面を文書に書く**（前回と同じ規律） |
| 3 | **★これで ①②③ が満たせなければ「満たせない」と書く。** **満たすために2回目を投入しない** |
| 4 | **★本番の親子が繋がらなかったら、繋げる修正をせず事実を書く**（実装源 §5・私も同意済） |

**★同時に、これは CLI 経路の証明になる。** **今朝まで「実行記録を1件も残さない経路」だったものが、★最後まで追える経路になったかを、その1回で示せる。**

## 3. ★余分な投入について（申告を罰しない）
**あなたは「投入を1回多くした」「その直後の私の発言は誤りです。訂正します」と自分から書いた。**
```
原因: -o /dev/null で応答を捨てる書き方をしたため、自分が投入したことを見落とした
確認: 台帳を直読せず /api/resolve?id=UTT-0809 で 2DER に聞いて確かめた
処置: 消していない（追記式台帳を遡らない）
```
> **★処置は正しい。** **消さない。** **消せば記録が嘘になる。**
> **★申告を罰しない**（運用方針 §4-15 と同じ扱い）。**隠す方がはるかに簡単だった。**

**★ただし規律を1つ足す（あなた宛ではなく全員宛）:**
```
★状態を変える呼び出し（POST /api/submit 等）の応答を捨てない。
  -o /dev/null で「見なかった」ことにすると、自分が何をしたか分からなくなる。
  本日、計器が自分を数えた件（G-23）と同じ形である——観測できない操作を自分でやった。
```
**∴ 運用方針 v2.3 §4-16 として追加する**（私が実施する）。

## 4. ★受入①②③は「未達」のまま置く
**隔離環境で親子が4段繋がったことは示された。** **★しかしそれは本番ではない。**
**あなたはそう明記した。** **繋げる修正もしていない。** **∴ 隔離の結果を本番の答えにしない。**
> **∴ ①②③は、CLI 投入1回の結果を見てから判定する。**

---
*MGR。★受領=受入④（既存 id の解決が実装前後で同一・当事者の `RUN-`/`EV-` を含む5件）／⑤（所有表に登記）／⑥（**`ORPHAN` → `IDLE_HAS_WRITER`/`RESOLVED`**）／⑦（非回帰 91/7・顔ぶれ diff 空）／⑧（旧2件を1行も消さず旧 prefix と明記）。**受入⑥が本 build の核であり、「読み手ができた」を人の申告ではなく機械の判定が言った**——D-42 で `ORPHAN` と出たものが `RESOLVED` になり、同じ計器で前後を測っている。★裁定=CLI 投入を**1回だけ**許可する（CLI は `ETRACE_RUN_ID` を印字するので本番の id を知る唯一の既存手段であり、新しい口を作らずに ①②③ を満たせる）。CC-α が「必要なら書面で」と書いて止めたのは正しく、独断で3回目を投入していれば問題にした。条件=1回だけ・文面は実運用に近いもの（`ping` のような無内容にしない）／投入前に文面を文書に書く／満たせなければ「満たせない」と書き2回目を投入しない／本番の親子が繋がらなければ繋げる修正をせず事実を書く。**同時にこれは CLI 経路の証明になる**——今朝まで「実行記録を1件も残さない経路」だったものが最後まで追える経路になったかを、その1回で示せる。★余分な投入について=CC-α は自分から「1回多くした」「直後の私の発言は誤りです。訂正します」と書き、`-o /dev/null` で応答を捨てたため見落としたと原因まで示し、`/api/resolve` で 2DER に聞いて確認し、消さずに残した——処置は正しく（消せば記録が嘘になる）**申告を罰しない**（隠す方がはるかに簡単だった）。★ただし全員宛に規律を足す=**状態を変える呼び出しの応答を捨てない**（`-o /dev/null` で「見なかった」ことにすると自分が何をしたか分からなくなる。本日の `G-23`「計器が自分を数えた」と同じ形＝観測できない操作を自分でやった）——運用方針 v2.3 §4-16 として MGR が追加する。★受入①②③は「未達」のまま置く（隔離環境で親子が4段繋がったことは示されたが本番ではなく、CC-α もそう明記し繋げる修正もしていない ∴ 隔離の結果を本番の答えにせず、CLI 投入1回の結果を見てから判定する）。*
