# 【裁定＋配線指示】`核を 実物の設計図に 当てる` — **★核0・★但し 鍵の形が 揃っていない4件 が 在ります**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 監視 / 発: 設計/監査(CC-α) / 2026-08-08 02:4x / TYPE=裁定＋配線指示
- **開発者規律 確認済（版: ★v1.18）** ／ 台帳: `ITEM-2DER-EVO-0035` ／ 効く先＝1・3・4
- **★★核0（★`trace_entry` は 合格済）／ ★新台帳0 ／ ★新エンドポイント0 ／ ★Claude の配線 ★上限10行**

---

## 1. ★★先に 測った（★鍵の形・★これが 今日 いちばん 効く所）

```
★設計図で ★`file` を持つ記述 = ★★32件（★178 のうち）
★実測（★`/home/takasan/` からの 相対として 解けるか）:
     ★★28件 = ★実在する
     ★★ 4件 = ★★実在しない ―― ★但し 中身が 別の話です:
        ★`C-DS-SELECT`      file = ★`"-"`（★★placeholder が 入っている）
        ★`C-EGL-ACCOUNTS`   file = ★`egl/structure/s_embed_axes.py, s_account_axes.py, …`（★★カンマ区切りで 4本）
        ★`C-EGL-EXECARCH`   file = ★`egl/structure/s_exec_arch_acd.py, s_task_contract.py`（★★2本）
        ★`C-QWEN-WORKER`    file = ★`twoder/qwen_worker.py, twoder/generate_via_runner.py`（★★2本）
★★∴ ★この4件は ★`UNTRACEABLE / missing=source` に なります ―― ★★動作としては 正しい。
★★★但し ★理由が 違います = ★★『鍵が 無い』のではなく ★★『1つの欄に 複数 入っている／印が 入っている』。
★★★★∴ ★結果を 読む時に ★この4件を ★『実物が 無い』と 読まないでください。
```

## 2. ★★決めてほしかった3点（★私の裁定）

```
★(a) ★`sources` を 組むのは = ★★既に在る作り方を そのまま 使う。
     ★逐語（★本日 配線した所）= `webui.py` の artifact 判定が
       ★5 repo（twoder / ds / rri / egl / dev-workcell）の `*.py` を 読んで dict にしている（★529本・0.1秒）。
     ★★鍵の形 = ★★`/home/takasan/` からの 相対（★実測で 28/32 が これで 解ける）。
     ★★★新しい仕組みを 作らない。★同じ組み方を 呼ぶだけ。

★(b) ★結果を 読む口 = ★★既存の 台帳（`GET /api/resolve?id=ITEM-2DER-EVO-0035` の note）。
     ★載せる物 = ★3値の 件数（★区分ごと）＋ ★`ABSENT` が 出たら ★その file と symbol を 逐語。
     ★★★但し 正直に 書きます = ★★『記述1件を 指定して 引く口』は ★★在りません。
        ★本件では ★件数と 逐語を 台帳に 置くまで。
        ★★『記述ごとに 引く』のが 要るなら ―― ★★それが ★次に作る機能です（★MGR の §3(b) のとおり）。

★(c) ★1回で ★178件 当てる。
     ★理由 = ★★区分に 分けると ★★鍵が 増える（★本日 数が 動いた原因は ★毎回 鍵でした）。
     ★★内訳は ★区分ごとに 出す（★数え方は 1つ・★見せ方だけ 分ける）。
```

## 3. ★★配線（★上限10行・★核は 作らない）

```
★(1) ★設計図（`egl/docs/2DER_EXECUTION_ARCHITECTURE.json`）を 読む。
★(2) ★5 repo の `*.py` を 読んで `sources` を 組む（★鍵 = `/home/takasan/` からの相対）。
★(3) ★すべての list の中の dict を ★1件ずつ ★`trace_entry(entry, sources)` に 渡す。
★(4) ★3値を 数え、★`ABSENT` の file と symbol を 集める。
★(5) ★台帳へ 1行 書く（★件数＋`ABSENT` の逐語）。
★★★LLM を 1回も 呼ばない（★MGR の受入(3)）。
```

## 4. ★★受入（★口・欄・★id）

```
★(1) ★口 = `GET /api/resolve?id=ITEM-2DER-EVO-0035` ／ ★欄 = note の中の 3値の件数
     ★★合計が ★★178 と 一致すること（★★178 は 本日 私が 数えた値 ―― ★★数える鍵は
     『すべての list の中の dict』。★★`id` を持たない6件も 含む）
★(2) ★★`ABSENT` が 1件でも 出たら ★file と symbol を 逐語で 出す。
     ★0件なら ★★『0件だった』と ★探した範囲（★5 repo の `*.py`・★529本）を 同じ所に 書く。
★★(3) ★★陰性 = ★★§1 の4件（`C-DS-SELECT` / `C-EGL-ACCOUNTS` / `C-EGL-EXECARCH` / `C-QWEN-WORKER`）が
     ★★`ABSENT` では なく ★`UNTRACEABLE` で 出ること
     ―― ★★ここが `ABSENT` に なったら ★『実物が 無い』と 誤って 読まれます。
★(4) ★LLM の呼び出し 0回 ／ ★(5) ★新台帳0・新エンドポイント0 ／ ★(6) ★Claude の配線行数（★上限10行）
★(7) ★戻せる（★置いた1本を 2DER が commit する＝★本日 2本 通した形）／ ★(8) ★61本を走らせない
```

## 5. ★★私が 言っていないこと

```
★『設計図が 間違っている』―― ★★測っていません。★測った後に 言えることです。
★『4件は 欄の書き方が 悪い』―― ★★形が 揃っていない、までです。★直すかは ★次の1件（★本件は 測るだけ）。
★『178件が 辿れる』―― ★★32件しか `file` を 持ちません。★残り146件は ★`UNTRACEABLE` の はずです。
   ★★但し ★これは ★予告では なく ★数え方から出る 見込みです ―― ★★実測で 違ったら ★それが 発見です。
★『記述ごとに 引ける』―― ★★引けません（★§2(b)）。
```
