# 【D-144】裁定 — **★今回 直すのは `Task` 1箇所。★GPU 固有のコードを書かない**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 00:5x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-31_RUNTIME_INSPECTION_COMMON_BASE_ORDER.md`（★逐語）

## 1. ★Taka が未裁定を1つ閉じた（★記録する）
```
★D-143 §6-1 で私はこう渡した:
   「工程4 に到達しないのは壊れているからではない。submit.py:377 が意図して DW task を作らない。
     ∴『直す欠陥』なのか『そういう経路』なのかが まだ決まっていない。★あなたの判断である。」
★Taka の答え: ★「Task生成」を配線の一部として繋げ。
∴ ★以後「設計どおりだから直さない」を根拠にしない。★裁定は出た。
```

## 2. ★今回の修正は1箇所（★正典「一度に一箇所だけ」）

**★配線を Taka の8点に写すと、最初に切れているのは `Task` である。**

| 配線 | 現状 | 根拠 |
|---|---|---|
| Request | **PASS** | `SUBMIT-_ayfew` から入力・受理時刻に到達（D-143） |
| RRI | **PASS** | `OBSERVE_CURRENT_STATE` / `RUNTIME_INSPECTION`・記録から引ける |
| **Task** | **★FAIL ← ★今回の修正対象** | `dw_task_ref=None`（この経路は task を作らない） |
| Runtime / Observation | 動いている（★Task の下流としては未確認） | `ARUN-00958`〜／`OBS-00959`〜 |
| Ledger | **FAIL** | ★生出力（`blob://…`）に到達できない |
| DW / Response | **FAIL / 未到達** | 要約でなく生の配列 |

> **★`Ledger`（生出力）は、D-143 で私が「次の最小修正候補」に挙げたものである。**
> **★今回は採らない。★正典が「一度に一箇所」「最初に切れている地点」と定めたためである。**
> **★取り下げではなく、順番が後ろになっただけである。**

## 3. ★受入条件（★MGR が固定する。★測ってから動かさない）

| # | 条件 |
|---|---|
| **1** | **★GPU 固有のコードを書かない。** 追加する分岐・定数・語彙に **`gpu` / `nvidia` を含めない**（★正典「GPU固有のコードを書くのではなく」） |
| **2** | **★再利用できることを、★GPU 以外の1語で示す**（CPU/Memory/Disk/Process/Network のどれか1つ）。**★実装するのではなく、★同じ経路に載ることを示す**（★能力追加は禁止） |
| **3** | **★新しいアーキテクチャ・新しい帳票（台帳）を作らない。★既存構造への最小差分のみ** |
| **4** | **★後方互換**: 既存の resolve 対象と、既存の `BUILD_CAPABILITY`/`MODIFY_EXISTING` 経路を壊さない。**★基準値は取る前に固定する** |
| **5** | **★修正後、必ず同じ検査を再実行する**（★同一の依頼文・1回のみ・投入直後に `receipt`） |
| **6** | **★途中で別の改善を実装しない。★止まったらそこで終了** |

## 4. ★報告の形（★正典の形だけを使う。★増やさない）
```
★止まった場合: Last PASS / First FAIL / 原因 / 修正内容 / 次回確認箇所 ★の5行のみ
★最終報告    : 接続できた配線 / まだ切れている配線 / 次回修正すべき箇所(1件) /
               Runtime Inspection を他の監視項目へ再利用できる状態か
★6区分は内部の判定に使ってよい。★Taka への報告は上の形にする
```

## 5. ★変わらないこと
```
★この修正は IMPL が書く ∴ ★2DER の担当工程に数えない。★主体移管 1/8 は動かない
★第1試行・第2試行の報告書を1文字も変えない
★GPU 使用率・プロセスの GPU 紐付けは★今回も触らない（★正典「GPU取得処理そのものの改善は目的ではない」）
★61本の非回帰は走らせない
```

## 6. ★事前状態（★MGR が保存）
```
2026-07-31T00:50:03+0900
  ds 3d94721 / rri b2e866a / egl 12bdf04 / dev-workcell 450566f / twoder d988640  ★全て未commit 0
  webui PID 3923096（07-30 23:49:07 起動）／ vllm PID 700957
★修正が twoder に入るなら再起動が要る。★全件 記録すること（run-gate 初期化も）
```

## 7. 手番
```
★設計: 調査（★どこで task を作らないと決めているか・★共通入口として何が既に在るか）→ SPEC → IMPL
★私  : 裁定と、Taka への最終報告
★Taka: 手番なし
```

---
**決めたこと**: **①Taka が「Task生成を繋げ」と裁定した＝「設計どおりだから直さない」はもう使えない ②今回 直すのは `Task` の1箇所だけ（`Ledger`＝生出力は順番が後ろになっただけで取り下げではない） ③GPU 固有のコードを書かない・GPU 以外の1語で再利用可能性を示す・新しい台帳やアーキテクチャを作らない ④報告は正典の形だけを使う。**
