# 設計/監査 → MGR（写: Taka / IMPL）: **Build 20 監査 通過。★受入⑤は実行不可能な項目を私が書いた。そして解凍を求める**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_IMPL_2026-07-28_BUILD20_GUARDED_ORACLE_PATH_BUILT.md`

## 0. 判定
**通過。** U1（precheck で止める）・U2（`_run_test` を使う・新しい実行系を作らない）・U3（独立の残存計測）が揃い、D2 は `PRECHECK_BLOCKED`、D1 は実行されて `survivors_independent = 0`、3ファイル無変更、非回帰 15/15、Build 18 保全物には当てていない。

---

## 1. ★受入⑤は、私が実行不可能な項目を書いた【設計:CC-α】
**私は「D1 に対する**本物のオラクル**の `exit` を貼れ」と書いた。**
**しかしオラクルは私の作業領域に在り、IMPL の手の届く場所に無い。** **私が意図的にそうした。**
> **∴ 私は「渡していないもの」を「使え」と指示した。** **矛盾している。**

**IMPL の対応は正しい:**
- **中身を開かずに sha256 で探した**（`77af566…` は5リポジトリに存在しないと確認）。
- **見つからないので、自分のダミーのオラクルで経路を実測し、そう明記した。**
- **★「本物ではない」と書いたうえで代替した。** **黙って埋めなかった。**

**∴ 受入⑤の未実施を欠落として数えない。** **私の記述の誤りである。**
**★正しくは「ダミーのオラクルで経路が動くことを示せ」だった。** **経路の検証に本物は要らない。**

## 2. ★3回目の「自己一致」が、私が要求した計器の中で起きた
**IMPL の実測で `survivors_independent = 1`（pid 3136727）と出たが、`ps -p` で既に存在しなかった。**
**原因は、探す文字列 `impl.py` が、実測を起動した自分のシェルの命令行にも入っていたこと。**
**対処: 探索を絶対パスに限定し、自分と先祖プロセスを除外した。**

> **★私が「`_run_test` の自己申告だけに依らない独立の計測」を要求し、その独立の計測自体が `G-23` を踏んだ。**
> **∴ 「独立に測れ」と言うだけでは足りない。** **「測る側が自分を数えない形にせよ」まで書く必要がある。**
> **∴ `G-23` に追記する**（本日3件目・**要求した計器の内部でも起きる**）。

## 3. 予想の扱い（IMPL の判断を支持）
**私は「出力が500字で切れる方に賭ける」と書いた。**
**IMPL は「それは本物のオラクルについての予想であり、自分のダミーでは検証できない」として、当たり外れを判定しなかった。**
**★正しい。** **測っていないものを当たりにも外れにもしない。**
**∴ この予想は、私が本物を走らせるときに決着する。**

---

## 4. ★解凍を求める（裁定は MGR）

### 4-1. U1〜U3 は満たされた
| 条件 | 状態 |
|---|---|
| U1 precheck を通す経路が在る | **在る**（D2 で実際に止まった） |
| U2 掃除の効く形で実行する | **在る**（`_run_test` 経由・新しい実行系を作っていない） |
| U3 実行後の残存を実測して貼る | **貼られた**（`survivors_independent = 0`・訂正つき） |

### 4-2. 私が解凍後に行う手順（先に固定する）
```
① run_oracle_guarded.run(
     artifact_dir = dev-workcell/contracts/out/SANDBOX_ARTIFACT-TASK-2DER-21F64D9D-regen/ws-8cff562b1d,
     oracle_path  = <私の作業領域のオラクル>,
     target_file  = impl.py)
② precheck が False なら、そこで止めて報告する（Build 18 保全物は既に True と観測されているが、私自身が通す）
③ 実行結果の exit と、切れていなければ出力を読む
④ ★survivors_independent を確認し、0 でなければ記録して kill する
⑤ 判定を FINDING に書く: 供給した T1〜T8 は通ったか / ★held-out は落ちたか
```
- **★オラクルの中身は公開しない。** **公開するのは「どの検査が通り、どれが落ちたか」の結果である。**
- **★私の賭け（held-out が少なくとも1つ落ちる）の当たり外れを、そのまま書く。**

### 4-3. 先に出す限界（feasibility-first）
- **オラクルは私の一時作業領域に在る。** **セッションが終われば失われる。**
- **∴ 今日のうちに走らせなければ、held-out による検定は失われる。** **sha256 は公表済なので、後から作り直しても「同じもの」とは言えない。**
- **★これは急かすためではなく、失われる性質を先に出すためである。** **MGR が「今はやらない」と判断するなら、その理由と併せて「検定は失われた」と記録すればよい。**

---

## 5. Gap Register
| id | 更新 |
|---|---|
| **`G-22`** | **★一部解消 → 配線された**（`run_oracle_guarded` が precheck を必ず通す）。**ただし `_run_test` を直接呼ぶ他の経路は素通りのまま。** **`OPEN` を維持し注記** |
| **`G-23`** | **追記**: 本日3件目。**「独立に測れ」と要求した計器の内部でも起きた。** **測る側が自分を数えない形（絶対パス・自分と先祖の除外）まで指示に含める** |

## 6. 私の記録（消さない）
- **オラクルは未開封**（`77af566…`）。**賭けは未決。**
- **`ids.resolve()` は依然として一度も実行していない。**
- **D-30 は提出済。MGR の応答（`..._D30_RECEIVED_COVERAGE_AND_NO_BLIND_FIX.md`）は未読。**

---
*CC-α Build 20 監査 通過（U1〜U3 が揃い、D2 は PRECHECK_BLOCKED、D1 は survivors_independent=0、3ファイル無変更、非回帰 15/15、Build 18 保全物には未適用）。★受入⑤は私が実行不可能な項目を書いた——オラクルは意図的に IMPL の手の届かない場所に在るのに「本物のオラクルで exit を貼れ」と指示した。IMPL は中身を開かず sha256 で探し、見つからないのでダミーで代替してそう明記した（黙って埋めなかった）∴ 未実施を欠落として数えない。正しくは「ダミーで経路が動くことを示せ」だった。★3回目の自己一致が、私が要求した独立計測の内部で起きた（`impl.py` という短い文字列が自分のシェルの命令行に一致）∴「独立に測れ」だけでは足りず「測る側が自分を数えない形にせよ」まで書く必要がある（`G-23` に追記）。予想（出力が切れる）は本物についてのものなのでダミーでは判定しないという IMPL の扱いを支持。★解凍を求める=U1〜U3 は満たされた。解凍後に私が行う手順を先に固定（precheck を自分で通す→実行→★survivors を確認→判定を FINDING に書く。オラクルの中身は公開せず結果だけ公開し、賭けの当たり外れをそのまま書く）。★限界を先に出す=オラクルは私の一時作業領域に在りセッションが終われば失われる ∴ 今日走らせなければ held-out による検定は失われる（sha256 公表済なので作り直しても同じとは言えない）。急かすためでなく失われる性質を先に出すためであり、MGR が今はやらないと判断するなら「検定は失われた」と記録すればよい。*
