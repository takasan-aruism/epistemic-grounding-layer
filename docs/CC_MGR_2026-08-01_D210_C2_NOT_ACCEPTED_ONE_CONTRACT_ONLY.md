# 【D-210】C-2 を★通さない ― ★雛形が★前回の契約を運んでいる／★直しは「弾く」側に置く

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-08-01 15:2x / TYPE=STATUS
- **開発者規律 確認済（版: v1.1）** ／ **受領**: `CC_IMPL_2026-08-01_C2_TEMPLATE_BUILT.md`（BUILT）／ `CC_DESIGN_2026-08-01_C2_AUDIT_THE_TEMPLATE_CARRIES_THE_OLD_CONTRACT.md`（監査）

## 1. ★裁定：★受入(b) は × ∴ ★C-2 は ★通さない（`DONE` にしない）
```
★実測（★設計が ★報告を読む前に自分で叩いた）:
   ★`next_request_template`（3519字）の中に ★契約が ★★2組 入っていた
   ★`def test_` の行数 ＝ ★5（★0 であるべきだった）
★★`contract_seal.extract_contract` は ★最初のマーカーを取る ∴ ★★古い契約が採られる。
★★★∴ ★人が下の空欄に新しい試験を書いても ★★採用されない。★★「★埋めたつもりで、埋まっていない」。
★★★★★これは ★★黙って間違う形である。★我々が一番 潰したかったものが ★雛形の中に入っていた。
```

## 2. ★直しの条件（★方法は設計が決める。★私は置き場所だけ決める）
> ### **★直しは「★雛形を綺麗にする」側ではなく「★弾く」側に置くこと。**
```
★理由: ★雛形を綺麗にしても ★★人が手で書いた依頼文が ★同じ形を作れる。★守られるのは ★雛形経路だけになる。
★★∴ ★★`extract_contract` 側で ★契約が ★2組以上 在ったら ★`ValueError`（★fail-closed）にする。
   ★★★これは ★既存関数への条件追加であり ★管理対象を ★増やさない（★規律9 に触れない）。
★★★★雛形から前回の契約を除くこと自体は ★やってよい。★ただし ★★それだけで通したとは書かない。
★★★★★★「★規律で気をつける」形にしない。★★★通らないなら通らないと ★機械が言う形にする。
```

## 3. ★受入（★差し替え。★これを満たすまで C-2 は通らない）
```
★(b-1) ★雛形をそのまま `extract_contract` に通すと ★★弾かれる（★古い方を黙って採らない）
★(b-2) ★雛形の中の契約は ★★1組だけ ／ ★その中身は ★空（★`def test_` が ★0行）
★(b-3) ★空欄を埋めて投入すると ★★埋めた方が採られる（★実測で示す。★思い込みで書かない）
★★★(a)(c)(d)(e) は `D-209` のまま。★★(d) は ★S-3 で実測して示すこと。
```

## 4. ★設計の自己申告について（★評価する。★罰しない）
```
★★設計は ★「★誤りは私の設計にある」と ★自分で書いた。★しかも ★★BUILT を読む前に ★自分で叩いて見つけた。
★★★これは ★本日ずっと言ってきた「★黙って間違う形を消す」の ★実行例である。
★★★★★∴ ★差し戻すが ★咎めない。★★同じ形を ★次の SPEC で ★先に潰すこと（★契約は1組・★複数は弾く）。
```

## 5. ★順序（★変えない）
| # | 件 | 扱い |
|---|---|---|
| **ACTIVE** | **C-2（`EVO-0022`）★受入(b) の差し替え分を直す** | **本裁定** |
| 直後 | S-3（`EVO-0027`）を ★C-2 の様式で投げ直す | 待ち |
| 次 | C-4（`EVO-0024`）表示反映 ／ C-3（`EVO-0023`） | 予約 |
| 後回し | `D-199` ／ `G-84` ／ 滞留6件（`BLOCK` の書く口） | 後回し |

---
**決めたこと**: **①C-2 は通さない。受入(b) が × で、雛形に契約が2組入っており `def test_` が5行あった。`extract_contract` は最初のマーカーを取るので古い契約が採られ、人が空欄を埋めても採用されない——「埋めたつもりで埋まっていない」＝黙って間違う形である ②直しは「雛形を綺麗にする」側ではなく「弾く」側に置く。雛形だけ直しても手書きの依頼文が同じ形を作れるので、`extract_contract` が契約2組以上で `ValueError` を返す形にする。既存関数への条件追加なので管理対象は増えない ③受入を差し替えた——雛形をそのまま通すと弾かれる／契約は1組だけで中身は空／埋めた方が採られることを実測で示す。(d) は S-3 で実測 ④設計は BUILT を読む前に自分で叩いて自分の設計の欠陥を見つけ、自分で申告した。差し戻すが咎めない。次の SPEC で同じ形を先に潰すこと ⑤順序は変えない。**
