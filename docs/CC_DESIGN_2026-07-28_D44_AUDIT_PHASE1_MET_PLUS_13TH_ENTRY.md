# 設計/監査 → MGR（写: Taka / IMPL）: **D-44 監査 — ★Taka ②は私の実測でも満たされた。そして再走査が★13番目の入口を出した（私の12入口は漏れていた）**

- `BUILD_ROLE: 参照`（**監査。投入していない・コードを1行も変えていない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8` — `§12` を最大版で読んだ値）**
- **受領**: `CC_IMPL_2026-07-28_D44_PHASE1_COMPLETION_BUILT.md`

## 0. ★結論
> **① Taka ②（合否）は★私の実測でも満たされている。** **`【伝聞】` ではない。**
> **② ★Taka ⑤（再走査）が仕事をした。** **私の「12入口」に無い★13番目が出た**（`dev-workcell/dw/workflow.py:37`）。
> **③ ∴「Phase 1 完了」とはまだ書けない。** **⑤の結果が「一覧が不完全だった」と言っているためである。**

---

## 1. ★合否の独立検算【監査:CC-α】（投入なし・読み取りのみ）
```
再現: curl -s -u "taka:$T" "http://…:8770/api/resolve?id=ETR-f0fe8461c407"   （読み取り専用）

resolved=True  count=21  total=21  truncated=False
component 内訳: {'SUBMIT': 1, 'DS': 1, 'RRI': 1, 'EGL': 18}
root=1 / root から辿れた=21 / 全=21 / ★孤立=0
```
| 項目 | 判定 |
|---|---|
| **`component` に `EGL` が現れたか** | **★現れた（18件）。`【実】`** |
| **親子が1本に繋がるか** | **★root 1つから21件すべてに到達。孤立0。`【実】`** |
| **打ち切りが無いか** | **★`truncated=False` / `count == total == 21`。`【実】`** |

> **∴ Taka ②「同一の CLI 依頼で DS→RRI→EGL を同一 run で追跡できる」は★満たされた。**
> **★前回（D-43）は3件で EGL が無かった。** **同じ口・同じ手順で、差が出た。** **これが本 build の効果である。**

## 2. ★Taka ⑤が出したもの — 13番目の入口（私の漏れ）
```
再現（★打ち切り無し）: grep -rn --include=*.py "create_task(" 全5repo \
  | grep -v "def create_task\|test_\|/regression/\|/experiments/"
総件数: 10 / 確認 10 / 打ち切り無し

★私の「12入口」に無かった行:
  dev-workcell/dw/workflow.py:37   W.create_task(task_id, project_id, goal, knowledge_packet, ts(), manager_identity)
```
**中身（実読）**: `run_standard_workflow()` — **1 Task Unit を CREATE→PLAN→GENERATE→AUDIT まで end-to-end で貫通させる関数**。
```
再現: 呼び手 → dev-workcell/run_task.py:13  from dw import workflow as WF
                egl/docs/report/ai_work_system_loop_demo.py:34（デモ）
```
| | |
|---|---|
| **★これは入口である** | `create_task` を直接呼び、**`submit()` を通らない** ∴ 合流点⓪を通らず `run_id` が付かない（⑨〜⑫と同じ形） |
| **★私の12入口に無かった** | **私が `create_task` の呼び手を数えたとき、この行を落としていた** |
| **★到達性** | **`run_task.py` は手動実行スクリプトであり、依頼経路ではない**（と読める。**★実行で確かめていない**） |

**∴ 13番目である。** **「12入口」は★不完全だった。**

### 2-1. ★他の差（数を合わせない）
| 対象 | 私（前回） | IMPL（今回） | 判定 |
|---|---|---|---|
| 書き込み経路 | **29** | **29** | **★2つの独立な計数が一致した** |
| `submit()` の呼び手 | 8（CLI 含む） | 9（`submit(` の行数） | **★母数の定義が違う。** 私が今回 alias 呼び出しを数えると**7**＋CLI で8 — **矛盾しない** |
| `create_task` | 3グループ | 10行 | **★13番目が出た**（§2） |
| `record_utterance` | 1グループ（`intervention`/`authority`） | 5行 | **★本番は `authority.py:125` / `intervention.py:76` / `submit.py:108` の3。他2は benchmark とデモ。矛盾しない** |

> **★IMPL が「数を合わせない」と書いたのは正しい。** **合わせていたら、13番目は出なかった。**

## 3. ★実装の検算
| # | 条件 | 判定 |
|---|---|---|
| 1 | **`emit` を `_idlock()` の外に置いた** | **★満たしている**（差分を実読。append の後） |
| 2 | **`contracts.py` の `GUARD_CONTRACTS` に1行足した** | **★満たしている**（差分に在る） |
| 3 | **例外メッセージが「Event Trace に書けなかったため中断した」と分かる** | **★満たしている**（`RuntimeError` の文言） |
| 4 | **⑤専用の形を作っていない** | 満たしている（①〜④と同じ `emit`） |
| 5 | **既存不変** | **★私は再実行していない**（`【伝聞】`。sha256 一致と非回帰 91/7 は IMPL 実測） |

### 3-1. ★特に評価する（記録する）
> **EGL 自身の試験が 20 passed / 3 failed だったとき、IMPL は「落ちなかった」と書かず、★自分の変更を一時退避して同じ試験を走らせ、「落ちた3本は変更前から落ちている」と示した。**
> **★これは「基準からの増分」を、基準を作り直して確かめる形である。** **今日いちばん厳しい検算をしている。**

## 4. ★「Phase 1 完了」と書かない理由（3点）
| # | 理由 |
|---|---|
| **1** | **★⑤の結果が「一覧が不完全だった」と言っている。** 13番目が出た以上、**14番目が無いとは言えない**（`G-32`） |
| **2** | **★宙に浮いた裁定2件が未実装のまま**（`G-46`／**合流点④の fail-closed 化＝Taka 逐語の裁定**）。**IMPL も「合流点④は現在も fail-open のままです」と明記している** |
| **3** | **★`DW_IMPLEMENTATION` 枝で `append_event` が走らないことは、誰も確かめていない**（今回は取得系に入ったため。MGR 条件3 が未達） |

## 5. ★MGR に上げる（私は決めない）
| # | 論点 | 材料 |
|---|---|---|
| **A** | **13番目（`dw/workflow.py:37`）をどう扱うか** | **(a) 入口一覧に加えて据え置く**（`run_task.py` は手動スクリプトで依頼経路でないと読める） **(b) 到達性を実行で確かめる** (c) 合流点を足す ← **★私は (a) を推す。** 理由: 依頼経路でないなら `run_id` が無いのは正常であり、**塞ぐと「呼び手の数だけ合流点が増える」膨張になる**（本日確定した原則に反する） |
| **B** | **Taka ⑤を「済」とするか** | **★私は「済」と言えない。** 13番目が出たので、**同じ走査をもう1度やっても14番目が出ない保証が無い**。**★ただし「もう1度やる」を無限に繰り返さない基準が要る**——これは MGR の裁定である |
| **C** | 宙に浮いた裁定2件の段取り | **★合流点④は Taka 逐語の裁定であり、未実装のまま日をまたぐ** |

## 6. ★未確認
| # | 未確認 | 誰が・いつ |
|---|---|---|
| 1 | **受入「既存不変」を私は再実行していない**（`【伝聞】`） | CC-α / MGR が要ると言えば |
| 2 | **`dw/workflow.py` が依頼経路から到達しないこと**（コード読みのみ） | **CC-α / 論点 A の裁定後** |
| 3 | **私の今回の走査も1回である。** 14番目が無いとは書かない | — |

---
*CC-α による D-44 監査。★合否を独立検算=`GET /api/resolve?id=ETR-f0fe8461c407`（読み取り専用・投入なし）で `count=total=21`・`truncated=False`、`component` 内訳 `SUBMIT:1/DS:1/RRI:1/**EGL:18**`、root 1つから21件すべてに到達し**孤立0** ∴ **Taka ②は `【実】` で満たされた**（前回 D-43 は3件で EGL が無く、同じ口・同じ手順で差が出たのが本 build の効果）。★**Taka ⑤（再走査）が仕事をした**=打ち切り無しで `create_task(` を数えると総10件で、そのうち **`dev-workcell/dw/workflow.py:37` が私の「12入口」に無かった**——`run_standard_workflow()` は1 Task Unit を CREATE→PLAN→GENERATE→AUDIT まで貫通させる関数で `create_task` を直接呼び `submit()` を通らないので合流点ⓠを通らず `run_id` が付かない（⑨〜⑫と同形）∴ **13番目であり「12入口」は不完全だった**。他の差は母数の定義違いで矛盾せず（書き込み経路は 29 対 29 で**2つの独立な計数が一致**、`submit()` の呼び手は私が今回 alias を数えて7＋CLI で8、`record_utterance` の本番は3）——**IMPL が「数を合わせない」と書いたのは正しく、合わせていたら13番目は出なかった**。★実装の検算=`emit` は `_idlock` の外・`contracts.py` に契約1行・例外文言・⑤専用の形なし をいずれも差分の実読で確認（既存不変は私は再実行しておらず `【伝聞】`）。★特に評価=EGL 自身の試験が 20/3 だったとき IMPL は「落ちなかった」と書かず**自分の変更を一時退避して同じ試験を走らせ「落ちた3本は変更前から落ちている」と示した**＝基準を作り直して増分を確かめる形で、今日いちばん厳しい検算である。★「Phase 1 完了」と書かない理由3点=⑤の結果が「一覧が不完全だった」と言っており13番目が出た以上14番目が無いとは言えない／**宙に浮いた裁定2件（`G-46` と合流点④の fail-closed 化＝Taka 逐語）が未実装のままで IMPL も「合流点④は現在も fail-open」と明記**／`DW_IMPLEMENTATION` 枝で `append_event` が走らないことを誰も確かめていない（MGR 条件3 が未達）。★MGR に上げる=A 13番目の扱い（**CC-α は「入口一覧に加えて据え置く」を推す**——依頼経路でないなら `run_id` が無いのは正常で、塞ぐと「呼び手の数だけ合流点が増える」膨張になり本日確定した原則に反する）／B **Taka ⑤を「済」と言えない**（13番目が出た以上14番目が出ない保証が無い。**ただし「もう1度やる」を無限に繰り返さない基準が要る**——MGR の裁定）／C 宙に浮いた裁定2件の段取り（**合流点④は Taka 逐語の裁定であり未実装のまま日をまたぐ**）。★未確認3件。*
