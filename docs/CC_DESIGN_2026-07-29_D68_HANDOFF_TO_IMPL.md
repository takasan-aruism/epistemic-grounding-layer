# 設計/監査 → **実装(IMPL)**: **`SENT` を成功時にも残してください（承認済）。★1点 訂正しました**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**送付状。★実装源は本文書ではない**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.8`）** / **承認**: `CC_MGR_2026-07-29_D68_APPROVED_BUT_DONT_OVERLOAD_THE_PATH_FIELD.md`

## 1. ★実装源（これ1本・★改訂1 入り）
```
CC_DESIGN_2026-07-29_D68_SENT_ON_SUCCESS_BUILD_SPEC_v1_0.md
```

## 2. ★改訂1（★これだけ読めば足りる）
| | |
|---|---|
| **旧（★取り消し）** | 「例外の型名を `path` 引数に `"/api/submit (TypeError)"` のように入れてよい」 |
| **★新** | **★`path` 欄にはパスだけ。型名は★別の欄に置く**（引数を1つ増やしてよい。**★増やすのは1つだけ**） |

> **★理由: これは「1つの欄が2つのことを指す」形である。**
> **★今日一日、我々はそれを潰してきた**（「取りこぼし 0件」／「取れない」／`欠損・失敗の記録なし`／`【伝聞】`／全24件）。
> **★`path` に `(TypeError)` が入れば、`path` はもはやパスではない。** **★後で読む者は、それをパスとして数えることになる。**
> **★私は今日の教訓を、その日のうちに自分で破った。** **★MGR が捕まえた。**

**★優先順位（MGR 裁定・そのまま渡す）**
```
上: 欄が1つのことだけを指すこと
下: 引数を増やさないこと
★「引数を増やさない」は小ささのための方針であって、正しさの条件ではない。
```

## 3. ★直す所（2箇所だけ・変わっていない）
| # | 場所 | 足すもの |
|---|---|---|
| 1 | `:573` の `return self._send({...})` の直前 | `SENT` / `/api/submit` / status=200 |
| 2 | `:632-633` の `except`（★`/api/submit` のときだけ） | `SENT` / `/api/submit` / status=500 / **★型名は別の欄** |

## 4. ★外しやすい3点
| # | |
|---|---|
| **1** | **★`/api/submit` 以外では何も書かない**（`approve`・`run_next` 等は対象外）。**★全アクセスログにしない**——`/tmp` で1000万件を消したばかりである |
| **2** | **★本文・依頼文・例外の文字列を残さない。** **★型名だけ**（依頼文は台帳に在る） |
| **3** | **★成功時の返り値（`_send` に渡す dict）を1つも変えない** |

## 5. ★受入で今回から入るもの
> **★配信物の JS が `node --check` を通ること**（`G-71`）。
> **★ソースではなく配信物に対して**——**生ソースは Python の埋め込みが混ざり、検査にかけられない**（MGR が1度失敗して分かった）。
```
GET /command → <script> を抽出 → node --check
```

## 6. ★確かめられないもの（先に決めてある）
- **★成功時の `SENT` が実際に出たかは、投入しないと示せない。** **★投入しない。**
- **∴ 「コードに在ることは示せるが、出たことは示せない」と書く。** **★未検証のまま登記する。**
- **★前例が在る**: `RECV` も同じ状態で入れ、**★Taka の初回操作で実際に出た**（`20:44:33 RECV /api/submit`）。

## 7. ★止まってよい場所（★5番が増えた）
1. `:573` の直前に置くと成功時の返り値が変わる → 止めて報告
2. ~~型名を `path` に入れる形が読みにくい~~ → **改訂1 で取り消し**
3. **`/api/submit` の返る道が3つ以外に在った** → **★私の数え間違い。報告してほしい**
4. SPEC が2通りに読める
5. **★欄を分けると `_recv_log` の呼び出し側が広く変わると分かった** → **★止めて報告**（MGR が改めて裁定する）

## 8. ★実行前に1つ
```
状況表: 実行中プロセス: twoder.webui(pid 3619595, 1h前起動: ★ソースが新しい=再起動が要る)
```
**★再起動は MGR がやる**（D-65 で決まった。**★あなたは再起動しない**）。**★受入を取る前に MGR に出すこと。**

---
*CC-α → IMPL 送付状（D-68）。★実装源は `CC_DESIGN_2026-07-29_D68_SENT_ON_SUCCESS_BUILD_SPEC_v1_0.md`（改訂1 入り）。★**改訂1**=旧「例外の型名を `path` 引数に入れてよい」を**取り消し**、**`path` 欄にはパスだけ・型名は別の欄に置く**（引数を1つだけ増やしてよい）——**理由はこれが「1つの欄が2つのことを指す」形であり、今日一日われわれが潰してきたものだから**（`path` に `(TypeError)` が入れば `path` はもはやパスではなく、後で読む者はそれをパスとして数える）。**CC-α は今日の教訓をその日のうちに自分で破り、MGR が捕まえた**。優先順位は「**欄が1つのことだけを指すこと ＞ 引数を増やさないこと**」で、「引数を増やさない」は小ささのための方針であって正しさの条件ではない。★直す所は2箇所（`:573` の直前に `SENT`/200、`:632-633` の `except` で `/api/submit` のときだけ `SENT`/500 ＋ **型名は別の欄**）。★外しやすい3点=**`/api/submit` 以外では何も書かない**（全アクセスログにしない。`/tmp` で1000万件を消したばかり）／**本文・依頼文・例外の文字列を残さず型名だけ**／**成功時の返り値を1つも変えない**。★受入に今回から入るのは**配信物の JS が `node --check` を通ること**（`G-71`。**ソースではなく配信物**——生ソースは Python の埋め込みが混ざり検査にかけられない）。★**成功時の `SENT` が出たことは投入しないと示せない**ので「コードに在ることは示せるが出たことは示せない」と書き未検証のまま登記する（**前例=`RECV` も同じ状態で入れ Taka の初回操作で実際に出た**）。★止まってよい場所は5件（2番は改訂1 で取り消し、**5番「欄を分けると呼び出し側が広く変わる」が増えた**）。★**再起動は MGR がやる**ので IMPL は再起動せず、受入を取る前に MGR に出すこと。*
