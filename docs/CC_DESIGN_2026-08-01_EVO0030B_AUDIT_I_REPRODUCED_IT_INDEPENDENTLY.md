# 【BUILT 監査 / `EVO-0030` 追補】**★独立に再現した** — ★同じ `FAIL` が入口で分かれる

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-08-01 13:4x / TYPE=FINDING
- **開発者規律 確認済（版: v1.1 / 2026-08-01）** ／ **受領**: `CC_IMPL_2026-08-01_EVO0030B_ROUTE_BY_ENTRY_BUILT.md`
- **★報告を読む前に差分を読み、★その後 ★自分で再現した** ／ **★production コードを1行も変えていない**

---

# 1. ★★独立に再現した（★IMPL の測定結果を借りていない）

```
★方法: ★`DW_DATA_DIR` を ★私の作業領域に向け、★`dw.workcell` の record_* だけで ★入口を2通り作った。
★★★★これは ★★2DER の外である（★開発者規律 第8章）。★2DER の実績に★数えない。
   ★★移管予定: ★★無い。★理由＝★これは ★2DER の機能ではなく、★★我々が入れた配線の検算だからである。
```

| 入口（★どちらも `rework_count = 0`） | verdict | ★私の実測 |
|---|---|---|
| **`READY_FOR_UPPER_REVIEW`**（★処置=全 REJECTED＋試験通過） | `FAIL` | **★`READY_FOR_REGENERATE`** |
| **`JUDGE_REQUIRED`**（★処置=REMAINS＋試験通過） | `FAIL` | **★`JUDGE_REQUIRED`（★戻らない）** |

```
★★★★★`rework_count = 0` を ★両方で確かめた ∴ ★★昇格規則（`:202`）の副作用ではない。
   ★★★★★分岐が ★意図してそこへ置いている。★★Taka の指摘どおりに動いた。
★★IMPL の (a)(b)(d)(e) の判定と ★★一致した。
```

## 1-1. ★★私の再現も1回 失敗した（★自己申告）
```
★1回目: ★試験を ★`passed: False` で作ったため ★狙った入口ができなかった。
   ★逐語のエラー: `phase UPPER_REVIEW は state=READY_FOR_REGENERATE から不可`
★★原因: ★DISPOSE の分岐（`workcell.py:172` 逐語 `elif (…) or (not tests_ok): state = "READY_FOR_REGENERATE"`）
   ＝ ★★試験が落ちていると ★`REMAINS` より★先に「作り直しへ」に落ちる。
★★★∴ ★`REMAINS` で `JUDGE_REQUIRED` を作るには ★試験が通っている必要が在った。★作り直して確認した。
```

---

# 2. ★★IMPL が、★私の SPEC より良い測り方をした（★名指しで書く）

```
★私の SPEC §2 は「★(e) は ★本番の1件で見てよい」と読める形だった。
★★★それでは ★区別がつかない: ★本番の1件は ★元から `JUDGE_REQUIRED` である ∴
   ★「★新しい分岐がそこへ置いた」のか「★昇格規則の副作用でそこに居る」のかを ★分けられない。
★★★★IMPL は ★★`DISPOSE` の `REMAINS` で ★`rework_count = 0` のまま `JUDGE_REQUIRED` を作った。
   ＝ ★★昇格規則と★混ざらない入口である。★★私が指定していない工夫である。
★★★★★★これは ★「★計器が2つの原因を区別できるか」を ★実装側が★自分で詰めた形である。★記録に残す。
```

---

# 3. ★逸脱1件（★IMPL が自分から申告）— **★差し戻さない**

```
★逸脱: ★測定用 task を ★front door でなく ★隔離環境（`DW_DATA_DIR` を一時領域）に ★5件 立てた。
   ★IMPL の理由（逐語）:「★1つでは (a)(b)(d)(e) を測れない」「★本番の台帳に測定用 task を1件も増やさない」
   ★★IMPL 自身が「★front door 経由が要るなら差し戻してほしい」と★書いている。
★★★★★私の判定: ★★差し戻さない。理由:
   ★① ★測っているのは ★`derive_state` の★分岐であり、★front door を通しても ★同じ結果になる
   ★② ★front door で立てれば ★本番の台帳に ★5件の滞留が増える ＝ ★★`EVO-0028` で止めたばかりの形に戻る
   ★③ ★IMPL は ★指示から外したことを ★★先に申告した（★隠していない）
★★★★★★ただし ★開発者規律 第8章の要求は ★満たす形で残す:
   ★「★これは 2DER の外である」＝ ★★そう書いた（★§1 と本節）
   ★「★移管予定を1行」＝ ★★無い。★理由＝★配線の検算であって 2DER の機能ではない。★恒久的に外でよい
```

---

# 4. ★差分（★独立に確認）

```
★`dev-workcell/dw/workcell.py`: ★`+12/-2`（★本体＋追補）／ ★`twoder/webui.py`: ★`+1`
★★書いたのは ★IMPL（Claude）＝ ★★`D-208` §3 の既定に従った（★worker には試していない・★そう書いてある）
★★★★2DER の実績に ★数えない（★行数が報告されている）
★触っていない: ★`:202` の昇格規則 ／ `_MAP` ／ `_ALLOWED` ／ 新しい state 名 ／ 台帳 ／ 計器
★★「規則が2箇所に在る」ことは ★`workcell.py:189-191` に ★コメントで書かれ、
   ★★畳む条件（`:202` をループ内へ移す）と ★足りないもの（既存 task への影響を測る手段）が★併記されている
★★★戻し方: ★2箇所を元に戻すだけ（★`git checkout` でも戻る）。★★可逆である。
```

---

# 5. ★判定

> ### **★`EVO-0030` は ★`DONE` にしてよい。★受入 (a)〜(e) すべて成立し、★私も独立に再現した。**

```
★★ただし ★書けるのはここまでである:
   ★★「★上級監査は ★書ける・★読める・★行き先を変える」——★★これは言える。
   ★★★「★循環が成立した」——★★まだ言えない。
      ★MGR が `D-197` §4 で固定した条件は3つで、★①（★指摘が仕組みへ書き戻る）は ★まだ測っていない。
      ★★②（★次は Qwen だけで通る）★③（★同じ穴で Claude が呼ばれる回数が減る）も ★測っていない。
★★★★★★∴ ★次に測るべきは ★★「①が起きたか」である。★私は ★次の1件を★提案しない（★ACTIVE は MGR が決める）。
```

---

# 6. ★やっていないこと
```
★production コードを1行も変えていない ／ ★commit していない ／ ★front door へ投入していない
★台帳を直読していない ／ ★本番の task を1件も増やしていない（★私の再現は★隔離領域）
★★循環の①②③ を ★測っていない（★§5）／ ★次の ACTIVE を ★提案していない
★★★後回しはそのまま: `D-199` ／ C-3 ／ `registered_at` の固定値 ／ C-2 ／ C-4 の表示反映
```

---
**決めたこと**: **①独立に再現した——`rework_count = 0` の入口を2通り作り、同じ `FAIL` が「通常の道からは作り直しへ」「既に人へ上がった所からは戻さない」と分かれることを確認した。両方とも作り直し回数0なので昇格規則の副作用ではなく、分岐が意図して置いている。IMPL の判定と一致した ②私の再現も1回 失敗した——試験を落ちた状態で作ったため狙った入口ができなかった。DISPOSE の分岐は試験が落ちていると `REMAINS` より先に「作り直しへ」に落ちるためで、作り直して確認した ③IMPL が私の SPEC より良い測り方をした——私の SPEC は本番の1件で (e) を見てよいと読める形だったが、それでは新しい分岐と昇格規則の副作用を区別できない。IMPL は `DISPOSE` の `REMAINS` で `rework_count = 0` のまま `JUDGE_REQUIRED` を作り、昇格規則と混ざらない入口にした。私が指定していない工夫である ④逸脱（測定用 task を隔離環境に5件）は差し戻さない——測っているのは `derive_state` の分岐で front door を通しても同じ結果になり、front door で立てれば `EVO-0028` で止めたばかりの滞留が5件 増える。IMPL は先に申告している。ただし第8章の要求どおり「2DER の外である」と書き、移管予定は無い（配線の検算であって 2DER の機能ではない）と理由つきで残す ⑤差分は `workcell.py +12/-2` と `webui.py +1` で、書いたのは IMPL（`D-208` §3 の既定に従い worker には試していない）。2DER の実績に数えない。規則が2箇所に在ることはコメントで書かれ、畳む条件と足りないものが併記されている。可逆である ⑥`EVO-0030` は `DONE` にしてよい。ただし言えるのは「上級監査は書ける・読める・行き先を変える」までで、「循環が成立した」はまだ言えない——`D-197` §4 の3条件のうち①②③ を1つも測っていない。次に測るべきは①だが、ACTIVE は MGR が決めるので提案しない。**
