# 設計/監査 → MGR（写: Taka / IMPL）: **D-93 — B-5 閉じる条件（1件ずつ）。★私の「完了した依頼は1件も無い」は誤りだった／★件数が MGR §6 と食い違う**

- `BUILD_ROLE: 参照`（**測定と設計提案のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-30_D92_RULINGS_TWO_ITEMS_CLOSE_ONE_RULING_CANCELLED.md` §6

## 0. ★2DER 優先原則の5点
| ① 入口 | **`GET /api/tasks` / `/api/state` / `/api/resolve`** |
|---|---|
| ② 何を試したか | **★完了した依頼（`COMPLETE`）を front door から最後まで読めるか／DW の状態と phase のうち本番で到達したものはどれか** |
| ③ **できなかったこと** | **★完了した1件は `/api/tasks` に出ない**（id を知らなければ辿り着けない）／**★その1件に `run_id` が無いので通過した処理が引けない** |
| ④ 何を実装するか | **★何も実装しない**（閉じる条件を出すだけ） |
| ⑤ | **★該当しない** |

## 0-1. ★先に訂正する（★私の誤り）
```
私が D-91 §1-⑤ に書いた: 「★完了した task が1件も無い ∴ 『最後まで』の『最後』を、まだ誰も見ていない」
実測（本日・全 task_id 160件）: ★COMPLETE が ★1件 在る（TASK-DW-EXEC-A-002）
```
> **★誤りである。** **★私は `_active_2der_tasks`（＝`COMPLETE` を除いた集合）と `TASK-2DER` 始まりの155件しか見ていなかった。**
> **★「無い」と書いた範囲が、★探した範囲だった**（`G-32`）。
> **★これも「その計器はその答えを出せる場所か」である。★本日 私は3回 踏んだ**（別経路4件→9件／入口 grep→構文木／今回）。

---

## 1. ★件数が MGR §6 と食い違う（★私は決めない）
| | MGR §6 | **★私の集計（資料の status から）** |
|---|---|---|
| 済 | **1** | **★3**（① `G-50` CLOSED ／ ② `ETRACE_RUN_ID` ／ ⑦ `G-76` CLOSED） |
| 部分 | **3** | **★2**（④ ⑤） |
| 未 | **3** | **★2**（③ `G-75` ／ ⑥ `G-49`） |
| 計 | 7 | **7** |

**★違いの中身**
| # | |
|---|---|
| **①** | **★D-92 裁定③で `G-50` を CLOSED にした。** ∴ **★① は「済」ではないか** |
| **⑦** | **★D-92 §5 で B-4 を受けた。`G-76` は CLOSED。** ∴ **★⑦ は「済」ではないか** |

> **★私は「済」と決めない。** **★①は「述語で数えた」だけで、★入口を塞いだわけではない。**
> **★⑦は「定義した」だけで、★正式な定義文書は無い**（私が B-4 で書いた区別そのもの）。
> **∴ ★「Gap が CLOSED」と「Taka の残件が済」が同じかどうかは、★MGR が決める。**
> **★これは今日ずっと直してきた形である**——**★1つの語（済）が2つのことを指しうる。**

---

## 2. ★閉じる条件（★1件ずつ・★実装しない）

### ③ DW枝の到達性確認 — `G-75` / **未**
**★測れる。★本日 測った。**
```
再現（★読み取りのみ・全 event）:
  ★宣言されている状態 13 → ★本番で到達 9
     READY_FOR_AUDIT 67 / READY_FOR_IMPLEMENTATION 49 / JUDGE_REQUIRED 18 / CREATED 12 /
     READY_FOR_UPPER_REVIEW 6 / READY_FOR_REGENERATE 3 / DISPOSITION_REQUIRED 2 / BLOCKED 2 / ★COMPLETE 1
  ★到達が確認できない 4: AUDIT_FAILED / IMPLEMENTING / PLANNING / REWORK
  ★宣言されている phase 10 → ★本番に出た phase ★10（★出ていないものは0件）
  ★dispatch._MAP の8状態 → ★8つとも本番で到達
```
| **★閉じる条件（提案）** | **「到達したか」を決定論の述語で1つ決め、★1回 走査して総数と到達件数を記録する**（`G-50` と同じ形） |
|---|---|
| **★述語の候補** | 「その状態が、`events.jsonl` から導出された `derive_state` の結果として★1件以上 現れたか」 |
| **★満たすと何が言えるか** | **★「13のうち9に到達、4は到達を確認できない」** |
| **★言えないこと** | **★未到達の4つが「到達できない」とは言えない。★「到達した記録が無い」だけである** |
| **★4つを到達させるのは条件に入れない** | **★実行が要る＝投入が要る ∴ 入れない**（feasibility を先に出す） |

### ④ DW fail-open 時の欠損可視化 — `G-38`/`G-69`/`G-70` / **部分**
| ★済んでいる | 画面に「取りこぼし」表示（★10箇所）／`dropped_before` を同じ返りから数える／「区別できません」の併記 |
|---|---|
| **★残り(1)** | **`G-69`「欠損・失敗の記録なし」が★2つの場合を1文言でまとめている**——**★これは閉じられる**（文言を2つに分ける・今日 何度もやった形） |
| **★残り(2)** | **★実例が0件**——**★これは閉じる条件に入れられない** |

**★(2) の理由（feasibility を先に）**
```
実例を出すには ①emit を例外で落とす ②その後 同じプロセスで emit が成功する の2つが要る。
∴ ★わざと壊して投入することになる。★Taka 常設命令（架空データを本番経路へ入れない）に反する。
```
| **★閉じる条件（提案）** | **`G-69` の文言を2つに分けたら ④ を「済」とする。★実例は条件にしない**（★「実例は原理的に出せない」と資料に書いて閉じる） |
|---|---|

### ⑤ 帳簿管理UIから一件の依頼を最後まで読める状態 — `G-46`/`G-69`/`G-73` / **部分**
**★本日、完了した1件で実際に試した（★私の誤りを直した結果）**
```
対象: TASK-DW-EXEC-A-002（★COMPLETE・本番で唯一）
  goal              ★取れる（"EXEC-A RUN_COMMAND primitive"）
  dw_state          ★取れる（COMPLETE）
  last_completed_op ★取れる（COMPLETE）
  next_operation    ★取れる（NONE）
  /api/resolve      ★resolved:true・events 6件
  ★etrace_run_id    ★空 → ★通過した処理が引けない
  ★/api/tasks に載っているか → ★載っていない
```
| ★分かったこと | |
|---|---|
| **★「最後」は読める** | **`COMPLETE` / `next_operation=NONE` まで front door から取れた** |
| **★しかし辿り着けない** | **★`/api/tasks` に出ないので、★id を先に知っている人だけが読める。★画面から到達できない** |
| **★通過した処理が読めない** | **★`run_id` が無い**（`ETRACE` より前に作られた task である可能性が高いが★確かめていない） |

| **★閉じる条件（提案・2つとも要る）** | |
|---|---|
| **(a)** | **★完了した依頼が、front door の一覧から辿れること**（★いまは出ない） |
| **(b)** | **★その1件について「通過した処理」が読めること**（★`run_id` が要る） |
> **★(b) は新しい依頼で満たすほうが早いが、★それは投入である ∴ 私は条件に入れるだけで、やらない。**

### ⑥ `acquisition` 固有イベントの必要性判断 — `G-49` / **未**
| **★閉じる条件（提案）** | **「必要」か「不要」を★1つ決めて書く。★それだけで閉じる** |
|---|---|
| **★測るものが無い** | **★これは判断であって測定ではない**（`G-49` note: 「Phase 2 で★必要性を判断すること」） |
| **★誰が決めるか** | **★MGR か Taka。★私は材料を出せる**（`acquisition` の境界イベントが無いと何が見えないか） |
| **★私の見立て（★材料。答えではない）** | **★Phase 2 の完了像6項目に `acquisition` 固有の粒度は出てこない** ∴ **「不要」に寄る材料が在る。★ただし決めない** |

---

## 3. ★まとめ（★閉じる条件が出せたもの／出せないもの）
| # | 件 | 閉じる条件 | **★満たせるか** |
|---|---|---|---|
| ③ | DW枝の到達性 | 述語1つ＋1回走査（`G-50` と同じ形） | **★満たせる。★本日 測った数字が在る** |
| ④ | 欠損可視化 | **`G-69` の文言を2つに分ける** | **★満たせる**（★実例は条件にしない） |
| ⑤ | 一件を最後まで読める | **(a) 一覧から辿れる ＋ (b) 通過処理が読める** | **★(a) は満たせる。★(b) は投入が要る** |
| ⑥ | `acquisition` の判断 | **必要／不要を1つ決める** | **★測定不要。★裁定だけ** |

## 4. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **未到達4状態が「到達できない」のか** | **★分からない。★「記録が無い」だけ** |
| 2 | **`TASK-DW-EXEC-A-002` に `run_id` が無い理由** | **★確かめていない。★「古い task だから」と書かない** |
| 3 | **`/api/tasks` が何を出す口なのか** | **★`TASK-2DER` 始まりだけかもしれないが、★確かめていない** |
| 4 | **③の述語が妥当か** | **★`derive_state` の結果で数える案である。★event の `phase` で数えれば別の数になる**（phase は 10/10 出ている） |

> **★4が重い。** **★状態で数えると 9/13、★phase で数えると 10/10 である。**
> **★同じ「到達した」が、★数え方で 69% と 100% になる。** **★述語を先に決めないと、どちらでも言える。**

---
*CC-α D-93（測定と設計提案のみ・実装なし）。★**先に訂正**=D-91 §1-⑤ の「完了した task が1件も無い」は**誤り**で、**全 task_id 160件を見ると `COMPLETE` が1件 在る（`TASK-DW-EXEC-A-002`）**——**CC-α は `_active_2der_tasks`（COMPLETE を除く集合）と `TASK-2DER` 始まりの155件しか見ておらず、「無い」と書いた範囲が探した範囲だった**（`G-32`）——**これも「その計器はその答えを出せる場所か」で、本日3回 踏んだ（別経路4件→9件／入口 grep→構文木／今回）**。★**件数が MGR §6 と食い違う**=MGR は済1／部分3／未3 だが、**資料の status から数えると 済3（①`G-50` CLOSED・②`ETRACE_RUN_ID`・⑦`G-76` CLOSED）／部分2（④⑤）／未2（③`G-75`・⑥`G-49`）**——**CC-α は「済」と決めず**、**①は「述語で数えた」だけで入口を塞いだわけではなく、⑦は「定義した」だけで正式な定義文書は無い**（B-4 で CC-α 自身が書いた区別）∴ **「Gap が CLOSED」と「Taka の残件が済」が同じかは MGR が決める**——**1つの語（済）が2つのことを指しうる、今日ずっと直してきた形である**。★**閉じる条件（1件ずつ）**=**③DW枝の到達性は測れる**（**宣言13状態のうち本番で到達9／未到達4＝`AUDIT_FAILED`・`IMPLEMENTING`・`PLANNING`・`REWORK`／宣言10 phase は本番に10 出ている／`dispatch._MAP` の8状態は8つとも到達**）で、**閉じる条件は「到達したか」を決定論の述語で1つ決めて1回走査し総数と到達件数を記録する（`G-50` と同じ形）**、**未到達4つが「到達できない」とは言えず「到達した記録が無い」だけ**であり**4つを到達させるのは実行＝投入が要るので条件に入れない**。**④は `G-69` の文言を2つに分ければ閉じられ、実例は条件に入れられない**——**実例を出すには emit をわざと落として投入することになり Taka 常設命令に反する** ∴ **「実例は原理的に出せない」と資料に書いて閉じる**。**⑤は完了した1件で実際に試し、`COMPLETE`／`next_operation=NONE` まで front door から取れた**が、**`/api/tasks` に載っていないので id を先に知っている人だけが読め画面から到達できず**、**`etrace_run_id` が空で通過した処理が引けない** ∴ **閉じる条件は (a) 完了した依頼が一覧から辿れること (b) その1件で通過した処理が読めること の2つで、(b) は投入が要るので CC-α は条件に入れるだけでやらない**。**⑥は測るものが無く「必要」「不要」を1つ決めれば閉じる判断事項**で、**決めるのは MGR か Taka、CC-α は材料を出せる**（**見立てとしては Phase 2 の完了像6項目に `acquisition` 固有の粒度は出てこないので「不要」に寄る材料が在るが決めない**）。★確かめていないこと=未到達4状態が「到達できない」のかは分からず「記録が無い」だけ／`TASK-DW-EXEC-A-002` に `run_id` が無い理由は確かめておらず「古い task だから」と書かない／`/api/tasks` が何を出す口なのか確かめていない／**③の述語が妥当か——状態で数えると 9/13、phase で数えると 10/10 であり、同じ「到達した」が数え方で 69% と 100% になる ∴ 述語を先に決めないとどちらでも言える（これが最も重い）**。*
