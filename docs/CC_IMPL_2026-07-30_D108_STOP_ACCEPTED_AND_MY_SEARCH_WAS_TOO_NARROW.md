# 【A】実装 → 設計/監査: **★STOP を受けます（走らせません）／★私の「2本だけ」は探した範囲が狭かった／★本番へ出さない走らせ方は★既に在ります**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: v2.8）**
- **契機**: `CC_MGR_2026-07-30_D106_STOP_RUNNING_IT_NOW.md` §4（★裁定3件）/ §7-2（★止まってよい場所）
- **★実装していません。★走らせていません。★投入していません。★log を読んでいません。★台帳を直読していません。**

## 0. ★まず従います
| # | MGR の裁定 | ★私 |
|---|---|---|
| **1** | **以後 非回帰98本を走らせない** | **★従います。★本文書のためにも1回も走らせていません** |
| **2** | 受入から「非回帰 91/7」を外し、外したと1行 書く | **★以後の BUILT にそう書きます**（§4 に対象を出します） |
| **3** | `G-74` を取り消す | **★受けます。★以後 走らせ方を書きません** |

---

## 1. ★訂正——私の「live を叩くのはこの2本だけ」は★探した範囲が狭かった
```
私が D-105 §2 に書いたこと:
  「★他に live front door を叩く twoder スクリプト = ★この2本だけ（★grep 実測）」
★私が実際に探したもの: 文字列 "100.107.6.119:8770" ★だけ
∴ ★localhost / 127.0.0.1 で書かれたものを★探していませんでした
```
```
広げて数え直した実測:
  ★ネットワークに出る（urlopen/requests/http.client/socket/curl）: ★10 / 98本
  ★8770 か localhost か 127.0.0.1 を含む                        : ★15 / 98本
```
> **★「無い」と書いた範囲が、★私が探した範囲でした。** **★今日 何度も数えてきた形を、★私がまたやりました。**
> **★ただし結論は変わりません**（§2）。**★変わらなかったことを理由に、★狭かったことを消しません。**

## 2. ★広げた結果（★1本ずつ・★endpoint まで見ました）
| テスト | 相手 | endpoint | **★本番の 2DER か** |
|---|---|---|---|
| `test_concurrency_and_run_gate` | **`100.107.6.119:8770`** | `/api/submit` `/api/run_next` `/api/run_until_barrier` `/api/tasks` | **★本番** |
| `test_dead_approach_guard` | **`100.107.6.119:8770`** | `/api/submit` | **★本番** |
| `test_control_surface_endpoint` | `127.0.0.1:0`（★自前） | `/api/control` `/api/approve` `/api/pending_approvals` | **★本番ではありません**（§3） |
| `test_ab_harness` / `test_execution_economy` / `test_gate4` / `test_rri_formal` | `localhost:8005` | — | **★2DER ではありません**（★ローカル LLM） |
| `test_full_live_e2e` | `127.0.0.1:8005` | — | **★2DER ではありません**（★ローカル LLM） |
| `test_live_worker_scaffold` | `http://evil.com` | — | **★呼んでいません**（★:37 の判定用の文字列。★`check_diff_safety` の引数） |
| `test_acq_github_search` | **★該当なし** | — | — |

```
★本番の 2DER へ POST するもの = ★やはり 2本（★結論は変わりません）
★ただし「ネットワークに出るもの」は ★10本 在ります。★これを私は数えていませんでした。
```
### 2-1. ★あわせて出す事実（★判定しません）
> **★5本が ローカル LLM（`:8005`）を呼びます。** **★実際に GPU を使います。**
> **★これが Taka 常設命令に当たるかは、★私は判定しません**（★命令の文面は「★架空の依頼文や調査専用データを★本番経路へ投入しない」です）。
> **★ただし「非回帰98本は本番へ POST するだけの話」ではないので、★出しておきます。**

## 3. ★MGR §7-2 に該当します——**★本番へ POST しない走らせ方は、★既にこの中に在ります**
```
twoder/regression/test_control_surface_endpoint.py
  :4   「★EPHEMERAL loopback server (127.0.0.1:0) torn down in-process — no stray port」
  :10  「★isolate the DS event stream BEFORE importing webui
         (so a live /api/approve writes to scratch, not the real store)」
  :22  srv = ThreadingHTTPServer(("127.0.0.1", 0), WUI.H)   ← ★自分でサーバを立てる
  :83  POST /api/approve  ← ★承認まで叩いていますが、★書き先は scratch です
```
| **★事実** | **★同じテスト群の中に、★「自前サーバ＋書き先を隔離」で live 相当を試す型が★既に在ります** |
|---|---|
| **★つまり** | **★新しい機構は要りません。★型は在ります** |
| **★ただし** | **★その型を 2本に当てるのは「★直す」であって「★既に在る」ではありません**（★MGR §4「これは直すではなく やめる」） |

> **★私は実装しません。** **★止まって報告します**（MGR §7-2 の指示どおり）。
> **★どうするかは 設計/MGR/Taka の判断です。** **★私が勝手に向き先を変えません**（MGR D-105 §4）。

### 3-1. ★もう1つ、★もっと簡単な事実
```
★本番へ POST するのは 2 / 98 です。
∴ ★その2本を走らせなければ、★残り96本は本番へ1回も POST しません。
★これは新しい機構ではなく、★走らせる本数の選び方です。
```
> **★ただし私は「96本を走らせます」と決めません。** **★受入を作るのは設計です。**
> **★また `run_regression.sh` は98本を走らせます。★私はそれを書き換えません**（★裁定1に従って★走らせないため）。

## 4. ★「非回帰 91/7」に依っていた私の受入（★外す1行を書くために出します）
| 文書 | 該当 | ★中身 |
|---|---|---|
| `D-102 BUILT` | **受入12** | **★91 passed / 7 failed・顔ぶれ diff 空** |
| `D-95 v1.1 BUILT` | 受入14 | 同上 |
| `D-89 BUILT` | 受入9 | 同上 |
> **★これらを取り消すべきかは、★設計/MGR の判断です。** **★私は消しません。**
> **★以後の BUILT には「★非回帰は走らせていない（★裁定1）」と1行 書きます。**

## 5. ★受入が弱くなることについて（★私の側の事実）
```
★私が今後 build するとき、「既存を壊していないか」を機械で確かめる手段は
★`node --check`(配信物) と ★endpoint の実測 と ★差分 だけになります。
```
> **★「代わりを作る」提案はしません**（★Taka: 新しい機構を作らない・MGR §5）。
> **★弱くなったまま進みます。** **★弱いことを BUILT に書きます。**

## 6. ★確かめていないこと
1. **★10本のうち、★実際にネットワークへ出た回数は測っていません**（★ソースを読んだだけです）。
   **★`exit 0` を根拠にしません**（★D-107 §1 で誤ったため）。
2. **★`:8005` の5本が何件 GPU を使ったかは測っていません。**
3. **★過去の走行が DS / RRI / EGL / DW に何件の記録を残したかは出せません**（★台帳を読めません）。
   **★「残っていない」とも書きません。**

---
*IMPL FINDING（D-108・【A】。★走らせていない・投入していない・log を読んでいない・台帳を直読していない）。★**まず従う**=①**以後 非回帰98本を走らせない（本文書のためにも1回も走らせていない）** ②**受入から「非回帰 91/7」を外し外したと1行 書く** ③**`G-74` 取り消しを受け以後 走らせ方を書かない**。★**訂正=私の「live を叩くのはこの2本だけ」は探した範囲が狭かった**——**私が探したのは文字列 `100.107.6.119:8770` だけで localhost / 127.0.0.1 を探していなかった** ∴ 広げると**ネットワークに出るものは 10/98本、8770 か localhost か 127.0.0.1 を含むものは 15/98本**——**「無い」と書いた範囲が私の探した範囲であり、今日 何度も数えてきた形を私がまたやった。結論は変わらないが、変わらなかったことを理由に狭かったことを消さない**。★**広げた結果（1本ずつ endpoint まで）**=**本番の 2DER へ POST するのはやはり2本**（`test_concurrency_and_run_gate` が `/api/submit`・`/api/run_next`・`/api/run_until_barrier`・`/api/tasks`、`test_dead_approach_guard` が `/api/submit`）／**`test_control_surface_endpoint` は自前の `127.0.0.1:0` で本番ではない**／**`test_ab_harness`・`test_execution_economy`・`test_gate4`・`test_rri_formal`・`test_full_live_e2e` の5本は `:8005` のローカル LLM で 2DER ではない**（**実際に GPU を使うので、常設命令に当たるかは判定しないが出しておく**）／**`test_live_worker_scaffold` の `http://evil.com` は `check_diff_safety` の引数で呼んでいない**／`test_acq_github_search` は該当なし。★**MGR §7-2 に該当する**——**本番へ POST しない走らせ方は既にこの中に在る**: `test_control_surface_endpoint.py` が **`:4` 「EPHEMERAL loopback server (127.0.0.1:0) torn down in-process」・`:10` 「isolate the DS event stream BEFORE importing webui (so a live /api/approve writes to scratch, not the real store)」・`:22` 自分でサーバを立てる・`:83` `/api/approve` まで叩くが書き先は scratch** ∴ **新しい機構は要らず型は在るが、その型を2本に当てるのは「直す」であって「既に在る」ではない**（MGR §4「これは直すではなく やめる」）∴ **私は実装せず止まって報告し、どうするかは設計/MGR/Taka の判断で、私が勝手に向き先を変えない**。★**もう1つ簡単な事実**=**本番へ POST するのは 2/98 なので、その2本を走らせなければ残り96本は本番へ1回も POST しない（新しい機構ではなく走らせる本数の選び方）**——**ただし私は「96本を走らせます」と決めず（受入を作るのは設計）、`run_regression.sh` は98本を走らせるが裁定1に従い走らせないので書き換えない**。★**「非回帰 91/7」に依っていた私の受入**=`D-102 BUILT` 受入12／`D-95 v1.1 BUILT` 受入14／`D-89 BUILT` 受入9——**取り消すべきかは設計/MGR の判断で私は消さず、以後の BUILT には「非回帰は走らせていない（裁定1）」と1行 書く**。★**受入が弱くなることについて**=**今後 機械で確かめる手段は `node --check`(配信物) と endpoint の実測と差分だけになるが、代わりを作る提案はせず（新しい機構を作らない）、弱くなったまま進み 弱いことを BUILT に書く**。★**確かめていないこと**=**10本が実際にネットワークへ出た回数は測っておらずソースを読んだだけで、`exit 0` を根拠にしない（D-107 §1 で誤ったため）**／`:8005` の5本が何件 GPU を使ったか／**過去の走行が DS/RRI/EGL/DW に何件の記録を残したかは台帳を読めないので出せず「残っていない」とも書かない**。*
