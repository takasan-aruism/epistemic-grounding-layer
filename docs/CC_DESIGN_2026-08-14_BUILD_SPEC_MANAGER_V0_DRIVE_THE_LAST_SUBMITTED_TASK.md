開発者規律 確認済(v1.0)

# 【BUILD SPEC】★常時起動 Manager v0 ―― ★★『★叩く人』を 2DER の 中へ 移す

宛: MGR（★承認）／ 写: IMPL ／ 発: DESIGN ／ 2026-08-14 05:5x ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **Taka 正本** `CC_MGR_2026-08-14_TAKA_NEXT_PHASE_AND_MANAGER_V0_SPEC_v0.1.md`（★宛=DESIGN）
**★手番は 動かさない**（★1行の 直しが IMPL に 在る）＝ ★これは ★次の段の 材料

**★Taka 逐語（★守る所）**
> 「★Manager v0 は ★知的な上司では なく、★台帳を見て ★既存の運行規則どおりに システムを進める ★駆動装置とする」
> 「★既存の 正式な 実行口を 使用し、★★裏口を 新設しない」／「★進められる案件が なければ ★休止する」

---

## 0. ★★先に 引いた（★★作る前に 探す・★決定論・LLM 0回）

```
★★【★在る＝★使う】
   ★① ★常駐の 形 = ★★`~/.config/systemd/user/` に ★★2本（`chat-web.service` ／ ★`twoder-webui.service`）
        ―― ★★★今夜 入口に 入れた 形を そのまま 使う（★★新しい 常駐の 作法を 作らない）
   ★② ★正式な 実行口 = ★★`/api/run_until_barrier`(★`webui.py:1015`) ／ `/api/run_next`
   ★③ ★状態を 引く口 = ★`/api/tasks` ／ `/api/state` ／ `/api/resolve`
   ★④ ★進め方の 規則 = ★★`dw/dispatch.py:30`（★`READY_FOR_IMPLEMENTATION → GENERATE / CODING_WORKER`）
        ―― ★★★Manager は ★この表を ★★持たない（★★引くだけ＝★規則を 2箇所に しない）

★★【★★在るが ★違う物＝★中身を 読んで 確かめた】
   ★`twoder/runtime_supervisor.py`(322行) = ★★★1回の LLM 呼び出しの ★再試行の 殻
      ―― ★逐語『★the thin recovery envelope that wraps ★a single LLM/worker call』
      ―― ★★∴ ★★駆動装置では ない（★★名前が 近いだけで 使い回さない＝★本日 4回 落とした 所）
```

## 1. ★★★v0 の 範囲は 『1件』―― ★★これは 選択では なく ★★門の 制約

```
★★実測（★私が 05:3x に 行で 確かめた）
   ★`_LAST`(webui.py:32) は ★★プロセスに 1つ ／ ★`gate_decision` は ★`task_id` の 一致を 見る
★★★∴ ★Manager は ★投入しない ∴ ★★門を 自分で 開けられない
★★★∴ ★★v0 が 進められるのは ★★『★最後に 投入された 案件』★1件だけ
   ―― ★2件目を 指すと ★★`TASK_MISMATCH` で 断られる（★★★裏口を 作って 回避しない）

★★★∴ ★v0 の 目的を 1行で = ★★『★投入した 人が ★その後 何も しなくても ★barrier まで 進む』
   ―― ★★Taka 受入『★MGR／Taka が run 系の 口を 手で 叩いた 回数 = 0』は ★★これで 満たせる
   ―― ★★★戻す条件（★案B＝門を task ごとに 持つ）= ★★`TASK_MISMATCH` が ★★1回でも 出たら
      ＝ ★★★その時に 初めて 案B を 出す（★数で 発火する・★先に 広げない）
```

## 2. ★★場合の 列挙（★★走らせる前に 出す・★Taka 常設）

```
★① ★進められる 案件が 無い            → ★★休止（★★1行 記録して 寝る＝★★『何もしなかった』を 残す）
★② ★`runnable` で ★門も 一致          → ★★`/api/run_until_barrier` を 1回 呼ぶ
★★③ ★門が `NOT_RUNNABLE`             → ★★呼ばない ／ ★★理由を 逐語で 記録 ／ ★休止
★★④ ★門が `TASK_MISMATCH`            → ★★呼ばない ／ ★★★案B の 戻す条件が 発火（★名指しで 記録）
★★⑤ ★`BLOCKED`                       → ★★呼ばない ／ ★理由を 記録（★★古い BLOCKED が 残る 場合も 同じ）
★★⑥ ★barrier で 止まった              → ★★★止まった 工程名と 理由を 記録（★★止まりを 失敗と 書かない）
★★⑦ ★完了した                        → ★完了を 記録 ／ ★次の 巡回で ①へ
★★⑧ ★入口が 落ちている（★応答なし）    → ★★★自分で 起こさない ／ ★★記録して 休止（★★常駐の 責務は systemd）
★★⑨ ★同じ案件で ★2回 続けて 同じ所で 止まる → ★★★叩き続けない（★★★止めて 記録＝★★人を 呼ぶ）
     ―― ★理由 = ★★★『進まない物を 押し続ける』は ★本日 何度も 出た 形
★★⑩ ★2つの Manager が 同時に 起きた    → ★★1つだけ 動く（★systemd の 1本＝★多重起動しない）
```

## 3. ★★作る物（★★1本・★★台帳 0増・★口 0増）

```
★★`twoder/manager_v0.py`（★新規 1本＝★これだけ）＋ ★`~/.config/systemd/user/twoder-manager.service`

★★【★中身＝★★判断を 持たない】
   ★① ★`/api/state`（★or `/api/tasks`）で ★★いまの 案件と 状態を 引く（★★台帳を 直読しない）
   ★② ★★進めてよいかは ★★★既存の 応答が 決める（★`run_until_barrier` の 返す `refused` / `cause`）
       ―― ★★★Manager は ★自分で 可否を 判定しない（★★規則を 2箇所に 置かない）
   ★③ ★1回 呼ぶ → ★★返ってきた `state` と `reason` を ★★★台帳へ 1行（★★`event_trace` へ emit）
   ★④ ★間隔 = ★★★60秒（★★短くしない＝★★叩き続けない）／ ★1巡回で ★★1案件・★1回だけ
   ★⑤ ★★`COMPONENT, FUNCTION = "MANAGER_V0", "tick"` ／ ★★★毎回 1行（★★何もしなかった時も）

★★【★★★持たせない（★Taka §2.3 逐語）】
   ★設計の 良し悪し ／ ★コードの 内容 ／ ★優先順位 ／ ★権限境界 ／ ★不可逆の 承認 ／ ★創作的 対処
```

## 4. ★★必須計器（★★Taka §2.5 は 見出しだけ ∴ ★私が 案を 出す）

```
★★① ★★人／Claude が ★run 系の 口を 叩いた 回数（★★★0 が 目標＝★★これが 主体移管の 実数）
     ―― ★★鍵 = ★`RUNGATE` と `run_next`/`run_until_barrier` の 記録の ★呼び手（★★自己申告に しない）
★★② ★Manager が 起こした 回数 ／ ★そのうち ★進んだ 回数（★★分母を 出す）
★★③ ★★止まった 工程名と 理由（★★★逐語・★切り詰めない）
★★④ ★★休止した 回数（★★『何もしなかった』を 0件に しない）
★★⑤ ★★`TASK_MISMATCH` の 回数（★★案B の 戻す条件＝★★数で 発火）
★★⑥ ★★★Claude が 書いた 実装行数（★★試金石の 数を 続ける）
```

## 5. ★★受入（★★Taka §2.6 を そのまま ＋ ★私から 3つ）

```
★★① ★同じ試金石を 再実行し ★★Claude が 書いた 実装行数 = ★★★0
★★② ★★MGR／Taka が run 系の 口を 手で 叩いた 回数 = ★★★0
★★③ ★★READY 状態から ★Manager 自身が ★実装工程へ 進める
★★④ ★止まった 場合 ★★停止工程と 理由が ★台帳から ★★一意に 引ける
★★⑤ ★Manager が 勝手に 設計・コード修正を しない（★★書き込みの 口を 持たない＝★実物で）
★★⑥ ★★既存の 正式な 実行口を 使い ★★★裏口を 新設しない（★★`dispatch` を 直接 import しない）
★★⑦ ★既定経路の 通常応答・★既存非回帰を 壊さない（★既定 `/api/control` ≤0.5秒）
★★⑧（★私）★★★『何もしなかった』巡回も ★1行 残る（★★不在が 遵守に 見えない）
★★⑨（★私）★★同じ所で 2回 止まったら ★★★叩くのを やめる（★実物1件で 確かめる）
★★⑩（★私）★★`TASK_MISMATCH` が 出たら ★★★件数と 案件名を 出す（★案B の 発火条件）
```

## 6. ★★やらないこと

```
★★複数案件の 優先順位を 持たせない（★Taka §1.3）／ ★Genesis の 統合を しない
★★Manager に 設計・実装・監査を させない ／ ★UI を 先に 作らない
★★★門（`_LAST`）の 直し（★案B）を ★この段で しない ―― ★★戻す条件は §1
★★★入口を Manager が 起こさない（★★常駐は systemd の 仕事＝★★責務を 2つに しない）
```

## 7. ★★言い方

```
★★『Manager が できた』と 書かない ―― ★★正しくは ★★『★手で 叩いた 回数が N → 0 に なった』
★★『自動で 進むように なった』と 書かない ―― ★★★1件・★最後に 投入された 案件 だけ
★★★『2DER が 実装した』は ★★worker が 走り ★Claude 0行 の 時だけ
```
