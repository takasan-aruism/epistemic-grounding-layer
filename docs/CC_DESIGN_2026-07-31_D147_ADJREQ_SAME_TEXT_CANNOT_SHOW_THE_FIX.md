# 【裁定要求】同じ依頼文では、この修正の効果を示せない — **★SPEC を書く前に止めた**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 02:1x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **受領**: `CC_MGR_2026-07-31_D147_PROVENANCE_IS_THE_ONE_FIX.md`（§6 で SPEC を書けと指示）
- **2DER 優先原則**: ①読み出しの口のみ ②「既存 task の CREATE に provenance が在るか」 ③該当なし ④**★何も実装しない・SPEC もまだ書いていない** ⑤該当なし

---

## 1. ★事実（★3点とも私が確かめた）

| # | 確かめたこと | 根拠 |
|---|---|---|
| 1 | **provenance は★CREATE イベントの `knowledge_packet` から読まれる** | `dispatch_provenance.py:66-77` **"Read the canonical provenance stamped by twoder.submit into the DW task's ★CREATE-event knowledge_packet"** |
| 2 | **★既存 task の CREATE は既に確定していて、3件を持っていない** | `GET /api/claude_packet?task_id=TASK-2DER-0C458F38` → provenance = **`{ds_input_id, etrace_run_id, dw_task_id}`**。**★`trace_id` / `rri_request_id` / `rri_intent_id` は無い** |
| 3 | **★同じ依頼文では CREATE は作り直されない** | task_id は依頼文の sha1 ∴ 同一文＝同一 id。`create_task` は既存 task を拒否し、`D-144` の実装は **`DW_TASK_CREATE_RESULT: NOT_CREATED: …`** を記録するだけ（★理由を捨てていないのは良い） |

> ### **∴ ★同じ依頼文で再投入しても、★修正した packet は書かれない。★必ず同じ3件の不足で落ちる。**
> **★これは「修正が効かなかった」ではない。★「修正が届かない」である。★区別しないと読み違える。**

---

## 2. ★何と衝突しているか（★正典の逐語）

```
★正典: 「修正後は必ず同じ検査を再実行すること」
★正典: 投入文について「文言を変更しない」
★事実: ★同じ文＝同じ task ＝ CREATE 確定済 ∴ ★再実行しても修正を通らない
```
**★私は決めない。★どれを採るかは正典の要求の解釈であり、★MGR の裁定である。**

---

## 3. ★選択肢（★私の推しは B。★理由つき）

| | 案 | ★結果として何が分かるか | ★問題 |
|---|---|---|---|
| **A** | **同じ文をもう1回 投入する**（正典どおり） | **★何も分からない。★必ず同じ3件で落ちる**（★予告できる） | **★「効かなかった」と読める記録が残る** |
| **★B** | **★別の観測依頼を1回**（例: いま開いているポートの状況） | **★新しい task_id ＝ ★新しい CREATE ＝ ★新しい provenance が載る ∴ ★修正の効果を実際に測れる** | **★「同じ検査」ではない** |
| C | 既存 task の CREATE を書き換える | 効果は測れる | **★却下。★「前向きのみ。過去を遡って書き換えない」に反する**（運用方針 §6-5）。**★私は推さない** |

### 3-1. ★私が B を推す理由
```
★(1) ★修正の効果を測れる唯一の道である（A では構造上 測れない）
★(2) ★正典の主目的に直接 効く——「Runtime Inspection が GPU 専用ではなく
      CPU/Memory/Disk/Process/Network へそのまま使える共通入口か」を、★実際に別項目で通すことになる
★(3) ★過去を書き換えない
★(4) ★架空の依頼文ではない——★`_CATALOG` に `listening_ports` が既に在り、
      ★我々は実際にポートの状況を知りたい場面が在る（★第1試行でも `ss -ltn` が走っている）
```
### 3-2. ★B を採る場合、私が確かめてほしいこと（★あなたが決めたら SPEC に入れる）
```
★依頼文は MGR が固定する（★設計が思いつきで書かない・★1文字も変えずに投入する）
★「同じ検査ではない」ことを報告に必ず1行 書く（★比較表の母数が変わるため）
★A も併せてやるか（★2回投入になる）は MGR が決める。★私は増やさない
```

---

## 4. ★私がやっていないこと
```
★SPEC を書いていない（★書けば、届かない検査を IMPL にやらせることになる）
★依頼文を自分で決めていない ★コードを1行も変えていない ★投入していない
★既存 task に手を出していない
```

---
**決めたこと**: **①provenance は CREATE イベントから読まれ、既存 task の CREATE は3件を持たず、同一文では CREATE が作り直されない——∴ 同じ依頼文では修正の効果を示せない（「効かない」ではなく「届かない」） ②これは正典「同じ検査を再実行」「文言を変更しない」との衝突であり、私は決めない ③私は B（別の観測依頼を1回）を推す——効果を測れる唯一の道で、正典の主目的（GPU 専用でない共通入口）の確認そのものになり、過去を書き換えないため。依頼文は MGR が固定すること。**
