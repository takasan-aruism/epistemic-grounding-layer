# 設計/監査 → MGR（写: Taka / IMPL）: **Build 15 監査 通過 ＋ 再起動前の差分 — 今回は Build 15 の修理だけが効く**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=FINDING
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_IMPL_2026-07-27_BUILD15_NONCE_VOCAB_BUILT.md`

## 0. 判定
**通過。** 変更は指定2ファイルのみ、`ts` を汚さず、`conformance_probe` を書き換えず、新語彙を作らず、GRANT に使われない列を足していない。

| 受入 | 結果 |
|---|---|
| ① 既定 `None` で id が不変 | **前後実測で一致（`APPROVAL-29ac9930ca`）** |
| ② `gate1b_ts`（**C2 の証拠**） | **`passed=True`・probe は1行も未変更** |
| ③ attempt 違いで id が異なる | 異なる |
| ④ 台帳の `task_id` が素（**C1 の証拠**） | `'T'`・suffix なし |
| ⑤ 既存 GRANT が引ける | **§1 で扱う** |
| ⑥ 非回帰4本 | **すべて PASS** |
| ⑦ 触らない3ファイルが無変更 | `git status` で無変更 |

---

## 1. ★受入⑤について — 私の SPEC が内部で矛盾していた【設計:CC-α】
**IMPL は「本番台帳の既存 GRANT を直接引く確認は、台帳に試験行を書かない規律を優先して実施していない」と申告した。**

**申告は正しい。そして原因は私の SPEC である:**
- **受入⑤は「既存 GRANT を引けること」を求めた。** **これは読み取りであり、試験行を書く行為ではない。**
- **しかし私は禁止7に「台帳に試験行を書かない」と並べて書いた。** **∴ 読みと書きが同じ制約に見える形になっていた。**
- **∴ IMPL が広く解釈したのは妥当である。** **私が分けて書くべきだった。**

### 1-1. ★そして受入⑤は、構造上すでに満たされている
```
authority.py:133  approval_id = sha1("task_id|operation_class|action_type|ts")   ← nonce None なら従来どおり
approval_registry._load_grant(approval_id)  ← ★id の一致で GRANT を探すだけ。再計算していない
```
**∴ 検証は id を再計算しない。** **∴ 式が拡張されても、台帳に既に在る id はそのまま引ける。**
**∴ ①（既定 `None` で id が前後一致）が示せた時点で、⑤は構造的に保証されている。**
**∴ 私の受入⑤は冗長だった。** **未実施を欠落として数えない。**

## 2. 私の SPEC の小さな誤り（IMPL が正した）
**私は `twoder/conformance_probe.py` と書いたが、実在は `twoder/probe/conformance_probe.py` だった。**
**IMPL が探索して正した。** **∵ 私はパスを実物で確かめずに書いた。** **本日この形は複数回出ている。**

---

## 3. ★再起動前の差分（私が出すと約束した分）【監査:CC-α】

```
再現: git -C <repo> log --since="2026-07-27 18:37:58" --oneline --name-only   （前回の再起動以降）
```
| repo | 前回の再起動以降の commit |
|---|---|
| **`twoder`** | **★0件**（コード変更は無い） |
| `dev-workcell` | 4件——**すべて実行記録**（`events.jsonl` / `MANIFEST.json`） |
| `rri` | 5件——**すべて `rri_records.jsonl`** |
| `ds` | 5件——**すべて `ds_events.jsonl`** |

**未 commit の本番コード**: `twoder/authority.py` / `twoder/generate_via_runner.py`（**＝Build 15 の修理そのもの**）

> **∴ 今回の再起動で新たに効くのは、Build 15 の修理だけである。**
> **∴ 前回（8 commit がまとめて効き、token gate が新しい壁になった）とは状況が違う。** **同じ警戒は要らない。**
> **★ただし「1回の再起動で1つだけ効く」は今回の事実であって、常態ではない。** **毎回差分を見る。**

---

## 4. 次（Build 16 の SPEC を出す）
**再起動 → 修理が入っていることを応答の形で確認 → `GENERATE` を1段 → 成果物をその場で受け取る。**
- **再起動は常設手順**（MGR 裁定）。**手順は省略しない。**
- **★`GENERATE` が通れば `run_runner` に初めて到達する。** **本日、worker は一度も成果物を出していない**（Build 12＝契約／Build 14＝token gate、いずれも手前で fail-closed）。

## 5. 残（消さない）
| 件 | 状態 |
|---|---|
| **worker が4状態を分けられるか** | **未決。** 私は「供給分は通り、held-out が少なくとも1つ落ちる」に賭けている |
| 受入オラクル | **未開封**（sha256 `77af566…`） |
| `ids.resolve()` | **未実行** |
| `G-16`（mint と検証の語彙不一致） | **修理済。次の `GENERATE` が通れば「解消」に更新する**（通るまで更新しない） |

---
*CC-α Build 15 監査 通過（指定2ファイルのみ・`ts` を汚さず・probe 未変更・新語彙なし・使われない列なし・非回帰4本 PASS）。★受入⑤の未実施は欠落として数えない——私の SPEC が「既存 GRANT を引く（読み）」と「台帳に試験行を書かない（書き）」を並べて書いたため IMPL が広く解釈したもので、分けて書かなかった私の誤り。かつ `_load_grant` は id の一致で引くだけで再計算しないので、①（既定 None で id が前後一致）が示せた時点で⑤は構造的に保証されており冗長だった。私の SPEC は probe のパスも誤っており（実在は `twoder/probe/conformance_probe.py`）IMPL が探索して正した。★再起動前の差分=前回の再起動(18:37:58)以降、`twoder` のコード commit は0件、他repo は全て実行記録のみ、未 commit の本番コードは Build 15 の2ファイルだけ ∴ 今回の再起動で新たに効くのは Build 15 の修理だけで、前回(8 commit・token gate が新しい壁)とは状況が違う。ただし「1回で1つだけ」は今回の事実であって常態ではないので毎回差分を見る。次=Build 16(再起動→応答で修理を確認→GENERATE 1段→その場で受け取り)。`GENERATE` が通れば `run_runner` に初めて到達する。*
