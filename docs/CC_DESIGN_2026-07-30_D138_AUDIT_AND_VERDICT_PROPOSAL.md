# 【監査＋判定案】E2E 検査 — **★最初の停止点は工程1（MGR の読みが正しい）／★工程2 は IMPL と判定が違う**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 22:5x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-30_PHASE4_E2E_INSPECTION_ORDER.md`
- **受領**: `CC_IMPL_2026-07-30_D137_E2E_INSPECTION_BUILT.md`（監査対象）／`CC_MGR_2026-07-30_D138_…md`（§2 の依頼）
- **2DER 優先原則**: ①読み出しの口のみ（`resolve`/`tasks`）②「入力内容を記録から取得できるか」を聞いた ③**★できなかった（§2）** ④実装しない ⑤該当なし ／ **★`:8005` を使っていない**

---

# 1. ★IMPL の申告を、私が独立に取り直した（★伝聞のまま使わない）

| # | IMPL の申告 | ★私の再検証 | |
|---|---|---|---|
| 1 | 依頼文 54字 / sha1 `0c458f38…` | **★正典から独立に再計算 → 54字 / `0c458f38c8c98b985a91974773329c0032275aa0`** | **★一致** |
| 2 | `SUBMIT-zOlryQ` は `resolved=false` | **★自分で叩いた → `{"resolved": false, "record": null}`** | **★一致** |
| 3 | 打たれた command は `nvidia-smi --query-gpu=index,memory.used,memory.total,name …` | **★`resolve?id=ARUN-00954` を自分で引いた → `adapter_provenance.command` が同一。`adapter=ACQ_MANUAL`** | **★一致** |
| 4 | DW task は作られていない | **★別出所: 私が検査前 22:25 に取った `tasks` 件数 156 → 検査後も 156**（★MGR の「dev-workcell 無変化」とも独立に一致＝**★3つ目の出所**） | **★一致** |

## 1-1. ★私の SPEC の落ち度（★IMPL の指摘は正しい）
```
★IMPL:「SPEC §3『判定を確定しない』と §4『6区分で表を出す』が2通りに読めた」
★正しい。★私の書き方の欠陥である。
★IMPL の処理（6区分で埋めたうえで「確定は設計/監査」と明記）は★適切であった。★差し戻さない。
```

---

# 2. ★MGR §2 の判定 — **★入力内容は、記録から取得できない**

**★応答が返した手がかりは3種しかない**（`webui.py:671-676` の返り値の欄を実読・打ち切り無し。**★発話や DS の ID 欄は1つも無い**）。**★3種すべてを叩いた。★連番は当てていない。**

| 手がかり | 結果 |
|---|---|
| `trace_key` = `SUBMIT-zOlryQ` | **★`resolved: false`** |
| `OBS-00955` | resolved=true。**★キーは10個で、`raw_content_hash`/`raw_blob_ref` はあるが、★「GPU使用状況」「要約してください」「`raw_text`」「`UTT-`」を1つも含まない** |
| `host_ref` | ★機器名であって入力ではない |
| （参考）`GET /api/tasks` | **★22:30 台の新規 0件** |

> ### **★∴ 正典 §工程1 の成立条件(c)「受理時刻と入力内容を取得できる」は、★後半が満たされない。**
> ### **★∴ 最初の停止点は工程2 ではなく★工程1 である。MGR の読みが正しい。**

**★言い方を正確にする**: **★「記録されていない」ではない。**`ds_events.jsonl` は更新されている。
**★「記録から辿る手がかりが返らない」である。** ∴ **★停止原因は IMPL の書いた `ID 接続不全` と同じ1つ**であって、別の欠陥ではない。

---

# 3. ★工程2 は IMPL と判定が違う（★差分だけ書く）

```
★IMPL: 工程2「ID 発行」= FAILED
★私  : 工程2「ID 発行」= ★PROVEN
★理由: 正典 §工程1(b) は「一意の request ID または task ID が発行された」であり、
        ★trace_key(SUBMIT-zOlryQ) は発行されている。
       ★「解決できない」ことは、★正典 §工程7(記録) の評価であって、★発行の可否ではない。
★∴ 欠陥を2箇所に数えない。★1つの欠陥を1箇所で数える。
```

## 3-1. ★工程4 が起きなかったのは「壊れている」ではなく **★設計どおり**である
```
実読 twoder/submit.py:377
    _rec("DW_TASK_ID", None)   # do NOT resume the old task; no capability-build task
∴ ★OBSERVE_CURRENT_STATE / RUNTIME_INSPECTION 経路は、★意図して DW task を作らない。
∴ ★「PLAN が呼ばれなかった」は★配線の欠落ではない。★経路が違う。
★IMPL は「壊れているのか設計どおりか私は確定しない」と正しく保留した。★ここで確定させる=★設計どおり。
```

---

# 4. ★工程ごとの判定案（★案である。★確定は MGR）

| 順 | 工程 | ★判定案 | actor | 主な証拠ID | 備考 |
|---|---|---|---|---|---|
| 1 | 受付 | **★FAILED** | 人(IMPL)が投入 → 2DER が受理 | `receipt 22:30:24.891921` | **★最初の停止点。**受理は成立、**★成立条件(c)の「入力内容を取得できる」が未達** |
| 2 | ID 発行 | **★PROVEN**（★IMPL と違う） | 2DER | `trace_key=SUBMIT-zOlryQ` | **★発行はされた。★解決できないことは工程9 で数える** |
| 3 | RRI | **PROVEN** | 2DER(RRI) | `request_type=OBSERVE_CURRENT_STATE` / `acquisition_method=RUNTIME_INSPECTION` | 呼ばれた証拠＋後工程へ渡った証拠 |
| 4 | 計画 | **NOT_REACHED** | — | — | **★設計どおり**（`submit.py:377`）。★`plan_source`・`runtime_recovery` は該当なし |
| 5 | 実行命令生成 | **NOT_REACHED** | — | — | 命令は **adapter 内蔵の定型**。★planner 生成ではない |
| 6 | 実環境での取得 | **OBSERVED_UNATTRIBUTED** | 記録上 `read-only DW executor` | `ARUN-00954`〜`00957` | command/host/時刻は特定できる。**★「2DER が生成した指示によって」が不成立** |
| 7 | 出力取得 | **FAILED** | 同上 | `OBS-00955`〜`00958` | **★5項目中、GPU使用率=取得不能／プロセスの GPU 紐付け=無し。★空を成功にしない** |
| 8 | 検証 | **NOT_REACHED** | — | — | **★出力はそのまま応答に載った**＝正典 §6 の不成立条件 |
| 9 | 記録 | **FAILED** | 2DER | `ARUN-`/`OBS-` は引ける | **★1つの ID から辿れない（到達 1/8）** |
| 10 | DW 要約 | **NOT_REACHED** | — | — | 本件に紐づく要約を返す口を**★引けない** |
| 11 | ユーザ返答 | **FAILED** | 2DER | `measured_state`（21行の配列） | **★要約ではなく生の配列。★使用率が欠落したまま** |

**★最終結果（案）**: **一周 不成立。最後に成立した工程=3（RRI）／★最初に成立しなかった工程=1（受付）。★終了条件 B で完了。**

## 4-1. ★2DER が担当したと証拠で言える工程（★案）
```
★3 RRI（分類と取得方式の決定）      ★6 取得の実行（★ただし命令の出所は planner ではない）
★9 記録の書き込み（★中身は読んでいない）
```
## 4-2. ★人・Claude がやったこと（★省略しない）
```
★POST /api/submit を1回（IMPL）。★これだけ。★run_next は0回。
★GPU を自分で測っていない（設計・IMPL とも）。★取得方法を補完していない。
```

---

# 5. ★最小修正候補は増やさない
**★IMPL の1件で足りる**（`trace_key` を既存 `resolve` に通す）。**★私は増やさない。★実施しない。**
**★理由の補強**: **★工程1 の未達と工程9 の未達は、★同じ1つの欠陥である** ∴ **★1件で2つ塞がる。**
**★GPU 使用率の欠落は別件。★ここでは出さない**（正典「複数の問題をまとめて修正する提案をしない」）。

---
**決めたこと**: **①IMPL の申告4点を独立に再検証し全一致（sha1／`resolved:false`／command／task 未作成は私の事前件数 156→156 が3つ目の出所） ②入力内容は応答が返す手がかり3種すべてで取得できず＝最初の停止点は工程1（MGR の読みが正しい。ただし「記録されていない」ではなく「辿る手がかりが返らない」） ③工程2 は IMPL の FAILED ではなく PROVEN を提案（発行はされた・解決不能は工程9 で数える。欠陥を2箇所に数えない）。工程4 の不発は設計どおり（`submit.py:377`）。**
