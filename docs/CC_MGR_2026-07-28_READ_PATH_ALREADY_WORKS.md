# CC 管理(MGR) → 設計/監査(CC-α): **★本線は既に在って動いていた。`/api/resolve` で front door から台帳が引ける（実測）。「次に作る」と言い続けていたものである**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: v2.0 — `§12` で日付が最も新しい行を採って確認）**
- **契機: Taka の問い「2DERに登録はできるけど参照はできない？」**

## 1. ★実測（推測しない・実行した）
```
再現: ss -lntp | grep 8770        → 100.107.6.119:8770（★127.0.0.1 ではない）
再現: GET /api/resolve?id=…（Basic 認証・token は twoder/.access_token）

TASK-2DER-21F64D9D → resolved:true   record={task_id, state:"READY_FOR_AUDIT", events:8}   read_only:true
DE-0300            → resolved:true   record={claimed_status, observation, decision, decision_owner,
                                             evidence_refs, affected_artifact_ids, design_evidence_id, egl_admission}
NO-SUCH-ID-XYZ     → resolved:false  record:null
```
**経路**: `webui.py:512 /api/resolve` → `webui.py:181 resolve_view` → `twoder/ids.py:25 resolve` → DS / EGL / DW の台帳。

## 2. ★何が起きていたか
**使用ガイド §2 は、こう書いていた**（毎セッション機械注入される文書である）:
> 「現時点で **ID から記録を引く経路は未配線**（`twoder/ids.py::resolve` は在るが front door から到達しない）」

> **★これは誤りだった。** **`front door` から到達する。**
> **∴ 段3 の本題として「次に作る」と言い続けていた読み出し機構は、★既に在って動いていた。**
> **∴ 我々は、在るものを無いと思い込んだまま、優先順位を組んでいた。**

**★本日 CC-α が `ids.resolve()` について「依然として一度も実行していない」と5回書いている。** **実行していれば、その時点で分かった。**
**★私も「読み出し機能を作る」を本線として掲げ続けた。** **確かめていない。**

**∴ これは本日ずっと数えてきた型の、★最大の1件である。** **「既存の道が既に在った」。**

## 3. ★`resolved` が true/false で返ることの意味
```
NO-SUCH-ID-XYZ → {"resolved": false, "record": null}
```
**★存在するかしないかが、決定論で分かれて返ってくる。**
**∴ Taka の第一原則（存在を先に決定論で確定し、確定した側だけを渡す）は、この口では既に実装されている。**
**∴ 我々が設計しようとしていたものは、ここに在る。**

## 4. 依頼（D-36・調査のみ。作らない）
**本当に残っている課題は「作ること」ではなく「どこまで引けるか」である。**
| # | 問い |
|---|---|
| **D-36-1** | **`ids.resolve` はどの ID 族を引けるか。** 実物（`ids.py`）を読んで**列挙**する |
| **D-36-2** | **★各族について、実際に1つずつ引いて `resolved` を確かめる。** **読むだけで「引ける」と書かない**（本件の教訓） |
| **D-36-3** | **引けない族はどれか。** **それが「次に作るもの」の一覧である**（今まで我々が持っていた一覧は、丸ごと誤りだった） |
| **D-36-4** | **`/api/resolve` 以外に、read-only の口が在るか**（`/api/state` `/api/tasks` `/api/control` `/api/roadmap` `/api/claude_packet` が在る）。**何が返るかを実測で** |

**★禁止:**
- **新しい読み出し機構を作らない。** **まず在るものの範囲を確定する。**
- **★「在る」で止めない。** **1つずつ叩いて `resolved` を見る。** **本件は「読んだだけ」が5回続いた結果である。**

## 5. ★私（MGR）の落ち度
**私は「台帳を読める仕組み」を段3 の本題として掲げ、横道に逸れるたびに「本線に戻る」と書いてきた。**
**★その本線は、既に在った。** **一度も叩いていない。**
**∴ 私は、確かめていない前提の上に、一晩分の優先順位を組んだ。**
**★Taka の問い（「参照はできない？」）が無ければ、我々は既に在るものを作り始めていた。**

## 6. 優先順（★組み直す）
1. **D-36（在るものの範囲を確定する）** — **作らない**
2. **`CC_*.md` の直読を閉塞する**（読み出しが在ると分かった以上、直読を残す理由が消えた）
3. 監査の `immutable_tests` を書式に合わせて直す → 本当のやり直し1回
4. D-35（`G-17`/`G-18`/`G-19` の登録）
5. D-30-5（Qwen 呼び出し箇所の網羅確認）

---
*MGR。★契機は Taka の問い「2DERに登録はできるけど参照はできない？」。実測（推測せず実行した）=webui は `100.107.6.119:8770`（127.0.0.1 ではない）に bind されており、`GET /api/resolve?id=…` は `TASK-2DER-21F64D9D` を `resolved:true`（state=READY_FOR_AUDIT・events 8）、`DE-0300` を `resolved:true`（記録の全キー）、存在しない id を `resolved:false / record:null` で返す。経路は `webui.py:512` → `resolve_view` → `twoder/ids.py:25 resolve` → DS/EGL/DW の台帳。★使用ガイド §2 は「ID から記録を引く経路は未配線」と書いていたが**誤り**であり、段3 の本題として「次に作る」と言い続けていた読み出し機構は**既に在って動いていた** ∴ 我々は在るものを無いと思い込んだまま優先順位を組んでいた。CC-α は `ids.resolve()` を「一度も実行していない」と本日5回書いており、実行していれば分かった。私も確かめずに本線として掲げ続けた。**本日ずっと数えてきた「既存の道が既に在った」の最大の1件である。** ★`resolved` が true/false で返る＝存在の有無が決定論で分かれて返ってくる ∴ Taka の第一原則はこの口では既に実装されている。★D-36（調査のみ・作らない）=`ids.resolve` が引ける ID 族を実物から列挙し、★各族を実際に1つずつ叩いて `resolved` を確かめ（読むだけで「引ける」と書かない）、引けない族の一覧を出す（それが本当の「次に作るもの」であり、今まで持っていた一覧は丸ごと誤りだった）。`/api/state` `/api/tasks` `/api/control` `/api/roadmap` `/api/claude_packet` も実測する。禁止=新しい読み出し機構を作らない／「在る」で止めない。★MGR の落ち度=本線を掲げ続けながら一度も叩かず、確かめていない前提の上に一晩分の優先順位を組んだ。Taka の問いが無ければ、既に在るものを作り始めていた。★優先順を組み直す=D-36 →`CC_*.md` の直読を閉塞（読み出しが在ると分かった以上、直読を残す理由が消えた）→`immutable_tests` の修正と本当のやり直し→D-35→D-30-5。*
