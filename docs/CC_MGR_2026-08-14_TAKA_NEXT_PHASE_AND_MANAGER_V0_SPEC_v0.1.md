宛: DESIGN ／ 写: MGR ／ 発: Taka ／ 2026-08-14 05:3x ／ 台帳: `ITEM-2DER-EVO-0058`

**★これは Taka 正本。★本文は書き換えていない。**（以下 逐語）
**★Taka 逐語 2026-08-14 05:4x:「まぁGPTも大概作文するので、変なところは実態ベースで修正して」**
∴ **★MGR が実測と突き合わせ、★訂正だけをこの節に置く（★本文には触らない）。**

---

## ★★MGR 実測による訂正（2026-08-14・front door から MGR が引いた値）

### ★訂正① 「今回止まった主因」は **1つではなく2つ**

> 本文 §0 逐語「今回止まった主因は、Workerの能力不足ではなく**「READYな案件を誰かが叩かないと進まない」**ことである」

**★実測では、叩いた。3回 叩いて 3回とも断られた。**
```
/api/run_next            … dispatched 1回（PLAN 通過）／ 断られた 2回
/api/run_until_barrier   … 断られた 3回
逐語 = "no runnable DW task (observe/blocked context)" / "NOT_RUNNABLE"
```
**★∴ 主因は2つ**:
1. **駆動主体が無い**（＝本文の指摘。cron 0本／user unit は `twoder-webui` のみ）
2. **★叩いても断られる** ―― `_LAST` は プロセスに1つ・`/api/submit` の度に上書き。
   **進捗だけの投入は `DW_TASK_ID=None` を返す ∴ 門が閉じる**
   ＝ **★台帳に記録を1行 残すと、進める口が閉じる**

**★これは Manager v0 の前提を変える**: v0 は `run_until_barrier` を叩き、その前後で台帳に記録を残す。
**いまのままでは Manager 自身の記録が自分の門を閉じ、v0 は動けない。**
∴ **案C（進捗だけの投入では `_LAST` を触らない・1行）は ①の前提**（②の本体は Taka の順序どおり ①の後）。

### ★訂正② §1.1「現在地」と §2.5「必須計器」は **見出しだけで中身が無い**

**★1.1 現在地（実測で埋める）**
```
PLAN                 ★動く（Qwen `build_planner` が本線に配線済み・54秒・Claude 0行）
Worker               ★★今夜は 0回 走っていない（最後の端まで自走は 2026-08-12）
Front Door           ★動く／★常駐化済み（kill -9 → 4.4秒で自力復帰・NRestarts=1）
run_until_barrier    ★口は在る／★★門が閉じて断られる（訂正①）
台帳                 ★動く（断られた事も 1行 残る＝対照0・断り+1 で確認）
★常時起動 Manager    ★★★無い（cron 0本／user unit は front door のみ）
経路表               S11 `RUNGATE.refuse`=PRESENT は在る／★★門の条件は無い／★handed_to 18件すべて空
```

**★2.5 必須計器（実測できる物だけ・案）**
```
① 人／Claude が run 系の口を手で叩いた回数     ★本日 6回（run_next 3 ／ run_until_barrier 3）→ 目標 0
② `RUNGATE/refuse` の件数                      ★機械が数える（既に記録に残る）
③ Manager が起動した案件数／止まった工程・理由  ★停止工程が一意に引けること
④ `[Claude実装]` commit の件数                  ★試金石 開始前 3件 → 目標 据え置き
⑤ 完了案件数                                    ★99 → 100 か
```

### ★訂正③ 「主要部品を既に持つ」は **Worker だけ今夜 未検証**

> 本文 §0 逐語「PLAN・Worker・Front Door・run_until_barrier・台帳など、実行に必要な主要部品を既に持つ」

PLAN / Front Door / run_until_barrier / 台帳 は **今夜 実測で確認**。
**Worker は今夜1度も走っていない**（門で止まったため）。∴ **「持つ」は過去の実績（2026-08-12）に基づく**。
**★『2DER が実装した』と書けるのは、worker が走ってから。**

### ★私の数え間違い（先に書く）
台帳に「3回」→「正確には5回」と書いたが、**★正しくは 6回**（run_next 3 ／ run_until_barrier 3）。
機械で数えられるのは `RUNGATE/refuse` の側（＝断られた 5回）。**★人が数えた値は証拠にしない。**

---

# 2DER 次段階の方向性と常時起動 Manager 最小構成 v0.1
経路表試金石で露出した欠落を、実装主体移管へつなぐための整理

## 0. 結論

現在の最優先は、「Claude の代わりに考える Manager」ではなく、「既存の2DERを自動で前へ進める最小 Manager」を置くこと。

2DERは、PLAN・Worker・Front Door・run_until_barrier・台帳など、実行に必要な主要部品を既に持つ。

今回止まった主因は、Workerの能力不足ではなく「READYな案件を誰かが叩かないと進まない」ことである。

したがって、まず駆動主体を内部化し、その後にRUNGATE判定・経路表の門条件・機能表へ進む。

設計自動化とUIは、経路表・機能表・運行状態が揃った後に着手する。

## 1. 全体の方向性

### 1.1 現在地

### 1.2 修正順序

① 常時起動 Manager v0 — READYな案件を検出し、既存の実行口を叩き、barrierまで進める。判断は最小限。

② runnable / RUNGATE整理 — 何を見て進行可否を決めるのか、古いBLOCKEDが新規案件を阻害するか、単一の_LAST依存が妥当かを確定する。

③ 経路表に『門の生理』を足す — 存在・通過だけでなく、gate条件、refusal理由、handoffの送受、詰まり回数を経路から引けるようにする。

④ 同じ試金石を再実行 — Claude実装0行、MGR手動起動0回、2DER単独で完了できるか測る。

⑤ 機能表 — 機能→Capability→経路→実績→失敗・ノウハウを結ぶ。

⑥ 設計自動化 — 既存構造を壊さない設計、既存ノウハウ選択、経路整合性確認から始める。新構造の創発設計は後段。

⑦ UI — 人体図鑑＋機能表＋現在状態＋停止理由を人間が判断できる画面にする。

### 1.3 今やらないこと

・複数案件の高度な優先順位判断をManagerに持たせる。
・Genesisの注意センターをいきなり統合する。
・Manager自身に設計・実装・監査を担当させる。
・経路表が未完成のままUIを先に作る。
・試金石で出た複数の問題を並列に広げて直す。

## 2. 常時起動 Manager の方向性

### 2.1 Manager v0 の目的

目的は「人やClaudeが手で次の口を叩く仕事を、2DER内部へ移す」ことだけ。

Manager v0 は知的な上司ではなく、台帳を見て既存の運行規則どおりにシステムを進める駆動装置とする。

### 2.2 最小責務

・台帳／task状態の変化を監視する。
・既存規則で runnable と判定された案件を検出する。
・既存の run_until_barrier 等の正式な実行口を呼ぶ。
・barrier、BLOCKED、完了のいずれかまで進める。
・どの案件を、誰が、いつ起動し、どこで止まったかを台帳へ残す。
・進められる案件がなければ休止する。

### 2.3 Manager v0 が判断しないもの

・設計の良し悪し
・コードの内容
・複数案件の戦略的優先順位
・権限境界の変更
・不可逆な変更の承認
・未知の例外に対する創作的対処

### 2.4 状態遷移イメージ

```
台帳に新状態
  ↓
Managerが検知
  ↓
runnableかを既存規則で確認
  ├─ No → 理由を記録して休止
  └─ Yes → 正式な実行口を呼ぶ
             ↓
        barrierまで進行
             ↓
     結果・停止理由を記録
             ↓
            休止
```

### 2.5 必須計器

### 2.6 受入条件（v0）

・同じ試金石を再実行し、Claudeが書いた実装行数 = 0。
・MGR／Takaが run 系の口を手で叩いた回数 = 0。
・READY状態からManager自身が実装工程へ進める。
・停止した場合、停止工程と理由が台帳から一意に引ける。
・Managerが勝手に設計・コード修正を行わない。
・既存の正式な実行口を使用し、裏口を新設しない。
・既定経路の通常応答・既存非回帰を壊さない。

## 3. Manager v1 以降（保留）

v0が成立してから、以下を段階的に追加する。v0と同時に入れない。

・複数 runnable 案件の優先順位付け
・期限・重要度・依存関係による選択
・BLOCKED案件の再評価・再起動
・経路表Workerとの連携による配線異常検出
・失敗ノウハウの強制参照と成績利用
・将来的なGenesis Attention Centerとの統合

## 4. 今回の試金石から固定する原則

**能力不足と運行不足を分ける** — Workerが書けないのか、そもそもWorkerまで届いていないのかを混同しない。

**『無い』の前に一覧を引く** — 口・部品・経路・既存機能を名前の推測で判断しない。

**経路表を先に見る** — 経路に在るものを『無い』と報告しない。

**存在だけでなく条件を見る** — 門が在るだけでは診断できない。何を見て開閉するかを経路に結びつける。

**止まりは失敗ではない** — 契約が不足して止まったなら、不足名を次の契約改善材料として残す。

**自動化は人の行為を置換した事実で測る** — 『自動化したつもり』ではなく、人／Claudeの介入回数を0へ近づける。

## 5. 次の一手

次の実装対象：常時起動 Manager v0

最初の要求は「READYな案件を既存の正式な口で自動的にbarrierまで進め、結果を記録して寝る」だけに限定する。

v0完成後、同一の自動アップデート案件を再度試金石として実行し、Claude実装0行・手動run 0回・完了99→100相当の主体移管を測る。
