# BUILD SPEC — 「取れない」を2つに割る（★文言だけ）v1.0（★実装源）

- `BUILD_ROLE: ★実装源`（**本文書が実装の唯一の典拠**）
- **★宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-29_D55_RULING_SPLIT_C.md`

---

## 0. ★本 build の一行
> **文言が偽になるなら、それは「安心させない」の失敗である。**
> **★直すのは文言だけ。振る舞いを変えない**（IMPL の判断は正しい）。

## 1. ★直す所（1関数・1分岐だけ）
**現在（実読）**
```javascript
function droppedBlock(rec){
  if(!rec) return "<div class='k'>取りこぼし</div>"
    +"<div class='v'>取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）</div>"+DROP_NOTE;
```
> **★この1分岐が、2つの場合を1つの文言でまとめている。**

## 2. ★割った後（★文言はこれを使う。意味を変えない）
| 場合 | 判定条件 | **文言** |
|---|---|---|
| **(c1)** | **`run_id` そのものが無い**（`s.etrace_run_id` が falsy） | **「取りこぼしの件数は取得できない（この依頼の `run_id` が記録に無い）」** |
| **(c2)** | **`run_id` は在るが、記録を引けなかった**（`resolved=false` / `events` が無い / 取得に失敗） | **「取りこぼしの件数は取得できない（`run_id` は在るが、記録を引けなかった）」** |

- **★どちらも「0件」と出さない。**
- **★どちらも併記文言（`DROP_NOTE`）を出す。**
- **★(c2) の文言に原因を書かない**（通信失敗か、記録が消えたか、を我々は知らない）。**「引けなかった」まで。**

### 2-1. ★判定に使う値（新しい取得を足さない）
```
(c1) : s.etrace_run_id が無い          ← /api/state の返りに既に在る
(c2) : run_id は在るが rec が無い       ← /api/resolve を引いた結果。既に持っている
```
> **★新しい fetch を1つも足さない。** **★新しい状態変数を作らない**（`(c1)/(c2)` は★表示の分岐であって状態ではない）。

## 3. ★変えないもの
- **振る舞いを変えない**（どちらも「0件と出さない」は現状どおり）。
- **`webui.py` の Python 側を1行も変えない。** **endpoint を足さない。**
- **(a)(b) の文言・`DROP_NOTE`・`truncated` 併記を1文字も変えない。**
- **色を使わない**（`ok` も `bad` も足さない）。
- **勘定科目に触れない**（`G-62`/`G-64`〜`G-68`・据え置き）。

## 4. 受入（★投入なし）
| # | 条件 | 使うもの |
|---|---|---|
| **1** | **(c1) の文言が出る** | **`TASK-2DER-B9B4DA3B`**（`etrace_run_id = None`・実測済） |
| **2** | **(c2) の文言が★画面のソースに在る** | **★実例が無いので `/command` の HTML に文言が在ることで示す**（§4-1） |
| **3** | **(b) が壊れていない**（0件＋併記） | `ETR-0f6fae35665a` |
| **4** | **`ok`/`bad` を足していない** | **★差分で数える**（★ページ全体で数えない。本日 私が粗く測って誤りかけた） |
| **5** | 既存が不変 | `/api/state` の sha256 が実装前後で一致 |
| **6** | 非回帰 | 基準 91 passed / 7 failed・顔ぶれ diff |

### 4-1. ★(c2) は実例が無い（先に書く）
```
再現（CC-α 実測）: 4 run すべて resolved=true。存在しない ETR-000000000000 は resolved=false だが、
                   それは「run_id が在る依頼」ではなく「無い id を手で入れた」場合である。
∴ ★「run_id は在るが引けない」依頼の実例は★見つかっていない。
```
> **★作って試さない**（MGR 条件3）。**∴ (c2) は★文言が在ることまでで受入とする。** **「動いた」と書かない。**
> **★`G-61`・(a) と同じ扱いで、★未検証のまま登記する。**

## 5. ★止まってよい場所
| # | 条件 |
|---|---|
| 1 | **(c1)/(c2) を分けると (b) の判定が壊れた** → **★止めて報告** |
| 2 | **判定に新しい値が要ると分かった** → **★止めて報告**（新しい取得は禁止） |
| 3 | **JS だけでは足りず Python が要る** → 止めて報告 |
| 4 | SPEC が2通りに読める |

## 6. ★資料の同期
- **`webui.py` が再び変わるため、実行構造の資料が古くなる。**
- **★同期は私（設計/監査）が BUILT と同時に行う。** **あなたは触らなくてよい。**

---
*CC-α D-56 BUILD SPEC v1.0（実装源・宛 IMPL）。★一行=**文言が偽になるならそれは「安心させない」の失敗**であり、**直すのは文言だけで振る舞いを変えない**（IMPL の判断は正しい）。★直す所は `droppedBlock` の `if(!rec)` の1分岐だけで、そこが2つの場合を1つの文言でまとめている。★割った後=**(c1)** `run_id` そのものが無い（`s.etrace_run_id` が falsy）→「取りこぼしの件数は取得できない（この依頼の `run_id` が記録に無い）」／**(c2)** `run_id` は在るが記録を引けなかった（`resolved=false`/`events` 無し/取得失敗）→「取りこぼしの件数は取得できない（`run_id` は在るが、記録を引けなかった）」。**どちらも「0件」と出さず、どちらも併記文言を出し、(c2) の文言に原因を書かない**（通信失敗か記録が消えたかを我々は知らないので「引けなかった」まで）。判定は `/api/state` と `/api/resolve` の**既に持っている値**だけを使い、**新しい fetch も新しい状態変数も作らない**（(c1)/(c2) は表示の分岐であって状態ではない）。★変えないもの=振る舞い／Python 側／(a)(b) の文言・`DROP_NOTE`・`truncated` 併記／色（`ok` も `bad` も足さない）／勘定科目。★受入6件は**投入なし**（(c1)=`TASK-2DER-B9B4DA3B`／**(c2) は実例が無いので `/command` の HTML に文言が在ることで示す**／(b)=`ETR-0f6fae35665a`／**`ok`/`bad` は差分で数える（ページ全体で数えない——本日 CC-α が粗く測って誤りかけた）**／既存の sha256 一致／非回帰 91/7）。★**(c2) は実例が無い**（4 run すべて `resolved=true`、`ETR-000000000000` は「無い id を手で入れた」場合であって「`run_id` が在る依頼」ではない）ので**作って試さず、文言が在ることまでで受入とし「動いた」と書かず `G-61`・(a) と同じく未検証のまま登記する**。★止まってよい場所4件（(b) の判定が壊れた／判定に新しい値が要る／Python が要る／2通りに読める）。★資料の同期は BUILT と同時に CC-α が行うので IMPL は触らなくてよい。*
