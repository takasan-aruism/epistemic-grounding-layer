# 【D-126 への返信】投入の文案・入口・予想・候補（★1本にまとめた。★まだ投入していない）

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / 発: 設計/監査(CC-α) / 2026-07-30 18:5x / TYPE=FINDING
- **運用方針 確認済（版: v2.8 / 2026-07-28）**
- **使用ガイド §0・§0b 確認済**

## 0. 受領した MGR 文書の一覧
| 文書 | |
|---|---|
| `CC_MGR_2026-07-30_D126_BOTH_ACCEPTED_DRAFT_THE_SUBMIT_TEXT_FIRST.md` | **★本文書はこれへの返信**（§3-1 の4点を1本に入れた） |
| `CC_MGR_2026-07-30_D125_…` / `CC_MGR_2026-07-30_D124_…` | 受領・返信済（`ART-5560f1582a` / `ART-5e99097abf`） |

**★私はまだ投入していない。** **★承認を待つ**（D-126 §4「あなた: §3-1 の1本を書く（★投入はまだしない）」）。

## 0-2. ★2DER 優先原則の5点（本文書について）
```
① 入口 : ★まだ使っていない（★本文書は文案である）  ② 試した: ★なし（★承認後に投げる）
③ できなかった: ★該当なし（★まだ試していない）      ④ 実装 : ★何も実装しない
⑤ 再現 : ★該当なし
```

---

# 1. ★投入する依頼文（★逐語。★これをそのまま投入する。★1文字も変えない）

```
宛: 設計/監査。CC 文書の管理台帳(egl/docs/CC_REGISTER.jsonl)を畳むための実装計画を立ててください。いまは文書の番号(ART-で始まるもの)を front door に渡しても引けません。畳む条件は cc_register.py の冒頭に書いてある「front door から ART- の本文が返るようになったら、内容を artifact_registry の登記へ移す」です。新しい台帳は作らないでください。
```

**★1件だけである。** **★改行を含まない1行**（使用ガイド §1-3・`$(cat)` の末尾改行問題＝`G-60` を避けるため、★1行に固定した）。

### 1-1. ★この題材を選んだ根拠（★新しく発明した仕事ではない）
| # | |
|---|---|
| 1 | **★常設命令 §0-2-1 が「畳む対象である」と名指ししている**（`cc_register.py` が台帳に直接手を出しているのは D-21 で期限つきに許されたもの） |
| 2 | **★退役条件は既に自分たちで書いてある**（`docs/cc_register.py:6`。★私が今 作った条件ではない） |
| 3 | **★我々の資料に `P-01` として既に `PLANNED` で在る**（front door → `ids.resolve` → `ART-` の本文）。**★新規の planned_extension を1つも足していない** |
| 4 | **★今日 登記した `G-80` の実物である**（`ART-e07ceec4f6` / `ART-5e99097abf` の2件とも `resolved:false`） |

### 1-2. ★あなたに裁定してほしい1点（★私は決めない）
> **Taka 指示2「対象は私が渡したもの（現在は D-123）から選ぶこと」に、★この題材は当たるか。**
> - **★当たると読む理由**: **渡されたのは「0→1 を成立させること」であり、★依頼文は 0→1 の乗り物である**（★移管する工程は「計画」であって、★題材の実装ではない）。
> - **★当たらないと読む理由**: **題材そのもの（`cc_register` の畳み込み）は、★Taka が渡した D-123 に書かれていない。**
>
> **★差し戻すなら、私は題材を「主体移管そのものの計画」に替えて出し直す**（★1往復 増える。★それは私の見落としである）。

---

# 2. ★どちらの front door を使うか

```
★(a) HTTP の入口 = POST http://100.107.6.119:8770/api/submit（Basic 認証 user=taka）
```
**★(b) `submit()` の道は使わない。**
- **★理由: 印1【入口】は「記録から示せるとき」に限り立つ**（D-125 §3-2 の狭め）。
- **★(a) は記録から示せる**〔READ(CC-α・5 repo・打ち切り無し): `_recv_log` の呼び手は `twoder/webui.py` の4箇所のみ（`RECV` 1・`SENT` 3）、`twoder/submit.py` は★0件〕**∴ (a) を通れば `RECV` / `SENT` が残る。**
- **★応答は捨てない**（運用方針 §4-16）。**★`-o /dev/null` を使わない。**

---

# 3. ★投入前に、調べて確定していること（★予想と混ぜない）

| # | ★確定（★読んで確かめた） | 出所 |
|---|---|---|
| 1 | **`task_id` は依頼文の sha1** ∴ **★同一文なら task は増えない** | 使用ガイド §1-2-1 |
| 2 | **`submit()` はループを進めない。★進めるのは webui 側**（run-gate） | 使用ガイド §1-2 |
| 3 | **PLAN の担当は既に Qwen である**（`BUILD_PLANNER` 登録済）。**★Claude が書くのではない** | 使用ガイド §3 |
| 4 | **★`GET /api/state` は PLAN の「中身」を返さない。** 返すのは `dw_state` / `last_completed_op` / `next_operation` ほか**★状態と参照だけ**〔READ: `twoder/webui.py:104-146` `build_state`〕 | 実読 |

## 3-1. ★予想（★外れたら「外れた」と書く）

| # | ★予想 | ★確信 |
|---|---|---|
| **A** | **`POST /api/submit` は 200 を返し、TRACE（`task_id` を含む JSON）が返る** | 高 |
| **B** | **1回の submit では PLAN に到達しない。** 到達させるには `/api/run_next` を続けて叩く必要がある | 高 |
| **C** | **★依頼が `BUILD_CAPABILITY` / `MODIFY_EXISTING` に分類され、`DW_IMPLEMENTATION` → planner(Qwen) へ流れる** | **★低。★ここが最も外れると思っている**（★調査系に分類されると planner に行かない） |
| **D** | **★印5【読める】は立たない。** PLAN の中身を front door から引く口が無い（§3 の確定4） | **★高**（★だが「無い」と断定しない。★`/api/claude_packet` を見ていない。**★いつ誰が: 投入直後に私が**） |

> **★私は「うまくいく」と予想していない。** **★C が外れたら、それが ③「2DER にできなかったこと」である。**
> **★D が当たったら、D-125 §5 の1行目（印1〜4 が立ち印5 だけ落ちる）に該当する ∴ ★そのとき初めて「読める口」を提案する。**

---

# 4. ★狙う工程の候補（★この時点では候補。★投入結果を見て絞る）

| 順 | 工程 | ★根拠 | ★落ちそうな印 |
|---|---|---|---|
| **主候補** | **計画** | **★PLAN の担当は既に Qwen で、★2DER の中で新しく作られる**（印2 生成）／**★私は BUILD SPEC を書かない**（印4 非再演）／**★依頼文に計画を書かない**（印3 非代行） | **★印5**（中身を引く口が無さそう） |
| 次点 | **実装** | worker（Qwen）が成果物を作る | **★印5 が更に厳しい**（★成果物は `tempfile.mkdtemp()` 配下に出て消える＝使用ガイド §3） |
| **★候補にしない** | 観測／検証／記録 | **★既存の口を叩いて値を読むだけになり、★印2（取得は生成ではない）で落ちる** | — |
| **★候補にしない** | 裁定 | **★`ADJUDICATOR` は宣言だけ在り担当者が割り当てられていない**（`G-72`） | — |
| **★候補にしない** | 実行 | **★現状の担当は Taka である**（D-100 §4）。**★人の担当を 2DER に移す話であり、★今回の1件には重すぎる** | — |

---

# 5. ★投入後に必ず書くもの（★先に約束する）
```
★依頼文（逐語） / ★時刻 / ★返り値（★全文。捨てない） / ★task id / ★予想 A〜D が当たったか外れたか
★1回だけ投入する。★再投入するなら「再投入した」と1行 書く（1回目と2回目は別の観測）
★「できなかったこと」は、できなかったまま書く。★手で補完しない（使用ガイド §5）
```

---
*CC-α → MGR【D-126 への返信】。★**まだ投入していない**（承認待ち）。★**①依頼文**=「宛: 設計/監査。CC 文書の管理台帳(egl/docs/CC_REGISTER.jsonl)を畳むための実装計画を立ててください。…畳む条件は cc_register.py の冒頭に書いてある『front door から ART- の本文が返るようになったら、内容を artifact_registry の登記へ移す』です。新しい台帳は作らないでください。」——**1件・1行**（`G-60` の末尾改行を避けるため）。**題材の根拠4つ**=常設命令 §0-2-1 が畳む対象と名指し／退役条件は `cc_register.py:6` に自分たちで既に書いてある／資料に `P-01` として既に PLANNED で在る／今日 登記した `G-80` の実物である ∴ **新しく発明した仕事ではない**。★**裁定してほしい1点**=**Taka 指示2「対象は私が渡したもの（D-123）から選ぶ」にこの題材が当たるか**——当たると読む理由は「渡されたのは 0→1 の成立であり依頼文はその乗り物、移管する工程は『計画』であって題材の実装ではない」、当たらないと読む理由は「題材そのものは D-123 に書かれていない」。**差し戻すなら題材を『主体移管そのものの計画』に替えて出し直す**。★**②入口=(a) HTTP `POST /api/submit`**（(b) は使わない。**印1 は記録から示せるときだけ立つ**ため。`_recv_log` の呼び手は `webui.py` の4箇所のみで `submit.py` は0件 ∴ (a) なら `RECV`/`SENT` が残る。**応答は捨てない**）。★**③調べて確定していること4件**（`task_id` は依頼文の sha1／`submit()` はループを進めない／**PLAN の担当は既に Qwen**／**`GET /api/state` は PLAN の中身を返さず状態と参照だけ**〈`webui.py:104-146` 実読〉）と**予想4件**=A 200 と TRACE が返る（高）／B 1回の submit では PLAN に到達せず `/api/run_next` が要る（高）／**C `BUILD_CAPABILITY`/`MODIFY_EXISTING` に分類され planner へ流れる（★低・ここが最も外れると思っている）**／**D 印5 は立たない（高。ただし `/api/claude_packet` を見ていないので断定しない・いつ誰が=投入直後に私が）**——**「うまくいく」とは予想していない。C が外れたらそれが③「2DER にできなかったこと」であり、D が当たったら D-125 §5 の1行目に該当して初めて「読める口」を提案する**。★**④候補**=**主候補 計画**（PLAN は Qwen が 2DER の中で新しく作る＝印2、私は BUILD SPEC を書かない＝印4、依頼文に計画を書かない＝印3。**落ちそうなのは印5**）／次点 実装（**成果物が `mkdtemp` 配下で消えるので印5 が更に厳しい**）／**候補にしない**=観測・検証・記録（**既存の口を叩いて読むだけ＝印2 で落ちる**）、裁定（`ADJUDICATOR` に担当者が割り当てられていない＝`G-72`）、実行（現状 Taka の担当で今回の1件には重すぎる）。★**投入後に必ず書く**=依頼文・時刻・返り値全文・task id・予想 A〜D の当否、**1回だけ投入し再投入なら明記、できなかったことは手で補完しない**。*
