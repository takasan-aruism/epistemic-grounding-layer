# 【調査】サンドボックスを現物で確かめた — **★「書けない」ではなく「書かない」**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 14:5x / TYPE=FINDING
- **運用方針 確認済（版: v2.9）** ／ **受領**: `CC_MGR_2026-07-31_D171_…`（§2-1「★押す前に設計が現物で確かめること」）
- **★新しい名前で置いた** ／ **★実装していない・提案していない**

---

# 1. ★結論（★`GENERATE` 解禁の判断材料。★短く）

> ### **★実務上の結論は変わらない（★押しても production には入らない見込みが高い）。**
> ### **★ただし理由が違う。★「到達不能だから」ではなく「★そう作ってあるから」である。**

---

# 2. ★現物（★5 repo `*.py` 全走査・打ち切り無し）

| # | 実際に効いているもの | 根拠（★逐語の場所） |
|---|---|---|
| **1** | **★本番は worker に `tempfile.mkdtemp(prefix="refora_")` を渡す** ∴ ★書き先は /tmp 配下 | `twoder/webui.py:268` |
| **2** | **★planner が、`target_workspace` を production 配下にした計画を弾く** | `twoder/build_planner.py:59,293`（★`PROD_REPO_ROOTS` は**★この2箇所のみ**） |
| **3** | **★runtime は `git commit` を実行しない**（★`AUTO_COMMIT_FORBIDDEN` が★assert で守られている） | `twoder/live_worker_runtime.py:204` |

| # | ★効いていない（★宣言だけ）のもの | 根拠 |
|---|---|---|
| **4** | **★`validate_sandbox` は「★設定辞書に `host_filesystem_unreachable` 等が True と書いてあるか」を見るだけ** ＝ **★OS レベルの隔離ではない** | `twoder/live_worker_scaffold.py:41-49` ／ `_MUST_BE_TRUE`（`:21-22`） |
| **5** | **★worker 側に production 拒否は無い**（`PROD_REPO` の出現 ★0）。★書き先は**★呼び手が渡す `sandbox_root`** で決まる | `twoder/live_worker_runtime.py:110,152-153` |

---

# 3. ★使用ガイドの記述より弱い（★訂正が要る）

```
★使用ガイド §3 の逐語: 「★worker は production repo に書けない。★`PROD_REPO_ROOTS` を★決定論で拒否する」
★★現物: ★`PROD_REPO_ROOTS` を見ているのは ★planner であって ★worker ではない。
★★∴ 「worker は書けない」は★正確でない。★正しくは「★本番の呼び方では書かない」である。
★★ガイドの改訂は MGR の担当 ∴ ★私は登記までにする（`G-90`）。
```

---

# 4. ★これが判断にどう効くか（★私は決めない）
```
★★押しても production に入らない、という結論は★変わらない（★上の1〜3が同時に効いている）。
★★ただし ★1（呼び手が temp を渡す）は★実装であって★構造ではない ∴ ★呼び手が変われば前提が崩れる。
★★∴ 「安全だから押してよい」ではなく、「★いまの呼び方なら入らない。★呼び方が変わったら測り直す」である。
★★★私は「押せ」とも「押すな」とも書かない。★禁止は Taka のものである。
```

---

# 5. ★登記（★2件。★id は設計が採った）
```
★G-89: ★「作る側と確かめる側を分ける」形を ★PLAN の段にも当てる（★案・未実装）
       ★MGR `D-171` §4 の指示。★③に直接 当たる唯一の残り案 ∴ 捨てない。★今は出さない
★G-90: ★サンドボックス保証は「書けない」ではなく「書かない」（★本調査）
★資料: gap 88 → 90。★JSON と MD の食い違い 0 件
```

---
**決めたこと**: **①押しても production に入らないという結論は変わらないが、理由は「到達不能」ではなく「そう作ってある」——本番が `tempfile.mkdtemp` を渡し、planner が production 配下の計画を弾き、`AUTO_COMMIT_FORBIDDEN` が assert で守られている、の3つが同時に効いている ②`validate_sandbox` は設定辞書に True と書いてあるかを見るだけで OS 隔離ではなく、worker 側に production 拒否は無い（書き先は呼び手が渡す値で決まる） ③使用ガイド §3 の「worker は `PROD_REPO_ROOTS` を決定論で拒否する」は現物より強い——見ているのは planner であり、ガイドの改訂は MGR の担当なので私は `G-90` として登記にとどめる ④私は「押せ」とも「押すな」とも書かない。**
