開発者規律 確認済(v1.0)

# 【BUILD SPEC】★internal 候補を **全部** 回す（★並列30・★3-seed・★間引かない）

宛: IMPL ／ 発: DESIGN ／ 2026-08-12 13:35 ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **Taka 逐語**「★LLM は30並列くらいできるらしいので ★ケチらずやっていいからね」／ MGR 受入7点（13:18）

**★前提（★段2 で 通った形を そのまま 使う）**: `menu_vote.ask(..., require_unanimous=True)` ／ 票3 ／ 割れたら `NOT_DECIDED` ／ 理由は 全文 ／ 記録は `event_trace`（★新台帳0）

---

## 1. ★★母数（★★走らせる時点で 引いて ★記録に 残す）

```
★★`644` を ★★定数で 書かない ―― ★★この数は 走行のたびに 動く（★実測 635 → 644）
★★∴ ★★走り始めに ★`static_edges` を 1回 引き、★★その値を ★記録の 先頭に 置く
   ★`total_internal` ／ ★`counted_by`（★数える鍵）／ ★`as_of`
```

## 2. ★★先に 決定論で 分類する（★★間引きでは ない・★名前を 出す）

```
★★`is_test` ／ `is_doc_snapshot` を ★★先に 分ける（★LLM 0回）
   ―― ★★★捨てない = ★★分けるだけ（★件数と ★名前を 出す）
   ―― ★★どちらも 回す か ★後回しにするかは ★★MGR の 裁定（★私は 決めない）
★★私の推し = ★★★全部 回す（★Taka『間引かない』／★分類は 後から 効かせられる）
```

## 3. ★★走らせ方

```
★★並列 = ★★30（★Taka 明示）／ ★1候補 = ★3-seed ／ ★呼び出し = ★★母数 × 3
★★1候補 終わるごとに ★1行 emit（★★途中で 止まっても ★何件 済んだかが 残る）
   ★`component="ANATOMIST"` ／ `function="route_edge_vote"`（★段2 と 同じ）
★★★同じ候補を 2回 書かない（★★既に 在る `candidate_id` は ★飛ばす）
   ―― ★★但し ★★『飛ばした件数』を 出す（★★0件なら 0件と 書く）
★★:8005 は ★他も 使う（★annotate_gate ／ account_gate）∴ ★★★並列30 を 超えない
```

## 4. ★★出す物（★MGR 受入 ＋ ★私から 2つ）

```
★① ★母数と 内訳（★§1 の 3つ）
★★② ★★`判定` ＋ ★`決められない` ＋ ★★`失敗` = ★★★母数（★★取りこぼし 0）
     ―― ★★`失敗` = ★LLM が 引けなかった件（★★★0件と 決めつけない）
★★③ ★★『決められない』を ★★名前で 並べる（★★数だけ 出さない）
★④ ★所要（秒）と ★呼び出し回数（★実測）
★★⑤ ★`reason_truncated` が ★★0件
★⑥ ★新台帳0 ／ ★口0増（★`GET /api/control?include=route_edge_votes`）
★★⑦ ★`is_test` ／ `is_doc_snapshot` の 内訳（★名前を 出す）
★★⑧（★私）★★`ROUTE` / `BORROW` / `NOT_DECIDED` の ★★件数と ★★割れ方の 内訳
     ―― ★★`disagreement` = 0 / 1 / 2 の ★件数（★★★『安定していた』と 書かない＝★数で 書く）
★★⑨（★私）★★飛ばした件数（★★再実行で 既に 在った物）
```

## 5. ★★受入（★★走らせる前に 宣言する）

```
★★① ★②の 足し算が ★★母数と ★1件も 違わない（★★違ったら 止めて 報告）
★★② ★`reason_truncated` = ★★0件
★★③ ★★`route_table.ROUTE` が ★★bytes 不変（★★『採用』は しない＝★MGR 裁定）
★★④ ★★既定の `/api/control` が ★0.5秒以内（★★1,900件の 記録が 増えても 段0 を 壊さない）
★★⑤ ★★LLM の呼び出し回数 = ★★母数 × 3（★★実測と 一致・★機械で 数える）
★★⑥ ★★途中で 止まった場合も ★★済んだ件数が 記録から 引ける（★1件ごとの emit）
★★⑦ ★新しい名前（定数・関数・欄）の 数を 報告
```

## 6. ★★言い方（★★MGR の 裁定を そのまま）

```
★★『経路表が 埋まった』と 書かない（★★数えていない 種別が 9種 在る）
★★『Worker が 経路を 判定できるように なった』と 書かない
★★正しくは = ★★『★母数 N のうち ★判定 a ／ ★決められない b ／ ★失敗 c』
★★『決められない』が 多くても ★★失敗では ない（★MGR 観察＝★★割れ方が 3回とも 同じ＝
   ★材料に 本当に 両義性が 在る）
```

## 7. ★★やらないこと

```
★★経路表へ 書き足さない（★★決まった物も＝★『採用』の 条件が 正本に 無い＝★Taka 裁定待ち）
★★問い2・問い3（★機能）を 作らない（★正本 §16 段3）
★★数えていない 9種を ★この段で 数えない
```
