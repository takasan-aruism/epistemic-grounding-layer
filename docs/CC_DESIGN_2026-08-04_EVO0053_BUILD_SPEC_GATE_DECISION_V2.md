# 【BUILD SPEC v2】`EVO-0053` — **★弱い assert を分割し、★理由を縛る1本を足す**

- `BUILD_ROLE: ★実装源` / **宛: IMPL**（★封入は MGR） / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-04 02:3x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.10）** ／ **★9項目 確認済（★§5）** ／ 親: `ITEM-2DER-EVO-0035`
- **★v1 との関係**: ★差し替えない・追記しない（規律）。**★本書が実装源**
- **★私の予告**: ★worker **10〜18行**（★v1 と同じ・★契約が変わっても関数は変わらない見込み）／★Claude の配線 **6〜10行**
- **★走行 0・★task 増 0・★commit 0**

---

## 1. ★私が自分で評価して確かめた（★指摘を鵜呑みにしない）

```
★v1 の1本目 最後の行:
   assert "156778F6" not in (v["reason"] or "") or "requested" not in (v["reason"] or "")
★★私が python で3ケース評価した実測:
   reason=None                                   → ★通る
   reason=''                                     → ★通る
   reason='requested T-REQ but current is T-CUR' → ★★通る
★★★∴ ★MGR の指摘より ★悪い。★TASK_MISMATCH の文面が出ていても ★通る
   ＝ ★あの行は ★★何も縛っていない。★『or』で繋いだため ★常に真になりうる。
★★★★私の誤り=★1つの assert に ★2つの意図（★非空である／★id が2つ並ばない）を入れ、★否定で繋いだ。
```

## 2. ★★契約の直し（★試験だけ・★依頼文と骨格は1文字も不変）

**★(A) 1本目の最後の行を ★2本の assert に分ける**（★1つの assert に2つの意図を入れない）
```python
def test_the_real_case_from_today_is_not_a_mismatch():
    """★本日 実際に踏んだ入力(v1.9=実データから採る)。★同じ id なのに mismatch と言われた形"""
    g = {"blocked": False, "runnable": False,
         "task_id": "TASK-2DER-156778F6", "reason": ""}
    v = impl.gate_decision(g, "TASK-2DER-156778F6")
    assert v["cause"] == "NOT_RUNNABLE", v
    assert v["allow"] is False
    assert v["reason"], v                      # ★① 非空であること(None も空文字も許さない)
    assert "requested" not in v["reason"], v   # ★② mismatch の文面を出さないこと
```

**★(B) 足す1本（★MGR の指定）**
```python
def test_not_runnable_reason_names_runnable():
    """★依頼文『NOT_RUNNABLE → no runnable task in the current submit context』を★試験で縛る。
    ★本日4回目の同型(依頼文に書いた条件を試験で縛っていない)への対処。"""
    v = impl.gate_decision({"blocked": False, "runnable": False,
                            "task_id": "T", "reason": ""}, "T")
    assert v["cause"] == "NOT_RUNNABLE"
    assert v["reason"] and "runnable" in v["reason"].lower(), v
```

```
★★★これで ★8本になる（★7 + 1）。★1本目以外の6本は ★1文字も変えない。
★★★★★★依頼文・骨格は ★不変 ＝ ★worker が作る関数は ★変わらない見込み（★予告に書いた）
```

## 3. ★★この走行は捨てる（★裁定の逐語）

```
★前回の走行の成果物は ★配置しない。★理由=★契約が欠陥品だったので ★通ったことに意味が無い。
★★★成果物が悪かったのではない ―― ★★7本 全通は ★事実だが ★★測っていたものが ★間違っていた。
★★★★MGR が『予告の当否を論じない』としたのも ★同じ理由 ∴ ★私も ★当否を書かない。
```

## 4. ★★本日4回目の同型（★数える・★隠さない）

| # | 件 | ★私が書かなかったもの |
|---|---|---|
| 1 | `EVO-0052` v1 | ★fixture が ★現実の note 形式でなかった |
| 2 | `EVO-0052` v2 | ★列挙の ★肯定側（5語すべて通る）を置かなかった |
| 3 | 1本 通す手順 | ★`verdict` の ★許される語（`PASS`/`FAIL`）を書かなかった |
| **4** | **本件** | ★依頼文に書いた ★理由の文面を ★1本も縛らなかった＋★assert を ★否定の or で繋いだ |

```
★★★共通=★★「★依頼文には書いた。★試験には書かなかった」。★★規律 v1.9/v1.10 は ★既に在る
★★★★★∴ ★規則を増やさない。★私が ★守れていない。
★★★★★★★今回 新しいのは ★★『★1つの assert に2つの意図を入れると ★弱くなる』
   ＝ ★★これは ★規則ではなく ★私の書き方の癖である。★次から ★意図ごとに1行にする。
```

## 5. ★★Qwen の監査が拾った（★記録に値する）

```
★run-0 の指摘は ★正しかった。★★我々（★私が書いた契約）の見落としを ★2DER 側の監査が ★実際に拾った。
★★但し ★1件である ∴ ★「監査が効くようになった」とは ★書かない（★本日の範囲での ★1例）。
★★★これまで監査の findings は ★全て REJECTED だった（★依頼文が渡っていないため）。
   ★★本件は ★★成果物ではなく ★契約の欠陥を指した ∴ ★種類が違う。★そこだけ記録する。
```

## 6. 受入
```
★(1) ★worker が書く（★Claude は本文0行）／★(2) ★8本 全通
★(3) ★★1本目が ★本当に縛ること=★`reason` が ★None/空なら ★落ちる（★実装が ★手で1回 確かめて逐語を書く）
★(4) ★本日の入力で ★`cause == "NOT_RUNNABLE"` ／ ★`reason` に ★`runnable` の語が在る
★(5) ★4語すべてが出る／★列挙外が出ない ／ ★(6) ★sha256 一致 ／ ★(7) ★行数を分ける
★(8) ★戻せる ／ ★(9) ★61本を走らせない
★★★★★予告を投入前に書く: ★worker の行数 ／ ★(3) で落ちることを ★確かめた方法
```

## 7. 禁止
```
★Claude が `gate_decision` の中身を書く ／ ★既存6本の試験を変える ／ ★依頼文・骨格を触る
★1つの assert に2つの意図を入れる ／ ★否定の or で条件を繋ぐ（★本件の原因）
★前回の成果物を配置する ／ ★『並行が直った』『run-gate を直した』と書く
★新しい台帳・エンドポイントを作る ／ ★61本を走らせる ／ ★commit する
★`twoder` 配下で python を動かす（★`operator.py` の罠）
```
