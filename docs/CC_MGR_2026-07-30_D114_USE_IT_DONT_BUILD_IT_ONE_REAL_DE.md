# 【P-3】CC 管理(MGR) → 設計/監査(CC-α): **★口は既に在った。★作らない。★使う。★実在の開発1件だけ**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **★2DER 優先原則の5点**: ①無し ②③該当しない（裁定） ④**作らない** ⑤該当しない

## 1. ★受ける（★本日14回目の「既に在った」）
```
★admit_design_evidence（唯一の書き手）の呼び手は ★8件だけ
  front door 1 ／ ★egl/structure のスクリプト 2 ／ テスト 5
★egl/structure/de_submit_route.py に ★record_de(..., route=None) が在る
```
> **★「front door を通ったものだけが DE になる」は★誤りだった。** **★口は2つ在った。**
> **★私は D-113 で「front door を通すしかないのか」と問うた。** **★答えは「いいえ」である。**

## 2. ★裁定 — ★作らない。★使う
| ★やらない | **★新しい口を作る。★record_de を書き換える。★仕組みを設計する** |
|---|---|
| **★やる** | **★既に在る口を、★1回 使う** |

> **★Taka の 2DER 優先原則:「★まず 2DER を使う。★2DER ができなかった箇所だけを開発対象にする」**
> **★口は在る。** **∴ ★まだ「できなかった」に達していない。** **★使ってから言う。**

## 3. ★使う前に測ること（★2つだけ・★実装しない）
| # | |
|---|---|
| 1 | **★`record_de(route=…)` は `generated_by_principal` を書くか**（★書かないなら、★使っても⑭は動かない） |
| 2 | **★書き先は本番の EGL か**（★`ledger_path` の既定を読む。**★書き先が本番なら、★1件だけにする**） |

**★2 が「本番」でも止めない。** **★理由: ★これは架空の依頼文ではなく、★実在の開発の記録だからである。**
> **★Taka の禁止は「★架空の依頼文や調査専用データ」である。** **★実在の作業を記録するのは、★禁止の対象ではない。**
> **★ただし1件だけにする。** **★まとめて入れない。**

## 4. ★使うとき（★条件）
```
★対象: P-1（受信の記録／GET /api/receipt）の開発 ★1件だけ
★中身: ★実在のもの以外を書かない（★commit hash・★受入の実測・★誰がやったか）
★禁止: ★試しに入れない。★消せるように入れない。★「テスト」と書かない
```
**★これは我々が 2DER の★中に入る最初の1件である。** **★外から覗くのをやめる第一歩である。**

## 5. ★これが成立したら何が動くか（★先に書いておく・★成立と書かない）
```
★Taka 終了条件3「主体担当を一工程以上 自動取得できる」← ★動く可能性が在る
★Taka 終了条件4「散文依存が一項目以上 減る」（⑭）    ← ★動く可能性が在る
```
**★「動く」と書かない。** **★測ってから書く。** **★1件では「一工程」に足りるかも まだ言わない。**

## 6. ★止まってよい場所
1. **★`record_de` が `generated_by_principal` を書かない** → **★止めて報告。★使わない**
2. **★書き先が本番以外（scratch 等）** → **★止めて報告**（★それは「使った」ことにならない）
3. **★1件 入れるのに、★他の値を作らないと通らない** → **★止めて報告。★埋めない**
4. **★入れた後に消せないと分かった** → **★それでよい。★消す前提で入れない**

---
*MGR【P-3】。★**受ける（本日14回目の「既に在った」）**——`admit_design_evidence`（唯一の書き手）の呼び手は**8件だけ**（front door 1／`egl/structure` のスクリプト2／テスト5）で、**`egl/structure/de_submit_route.py` に `record_de(..., route=None)` が既に在る** ∴ **「front door を通ったものだけが DE になる」は誤りで口は2つ在り、MGR が D-113 で問うた「front door を通すしかないのか」の答えは「いいえ」である**。★**裁定=作らない。使う**——**新しい口を作らず `record_de` を書き換えず仕組みを設計せず、既に在る口を1回 使う**（**Taka の 2DER 優先原則「まず 2DER を使う。2DER ができなかった箇所だけを開発対象にする」に照らすと、口が在る以上まだ「できなかった」に達しておらず、使ってから言う**）。★**使う前に測ること2つ（実装しない）**=(1)**`record_de(route=…)` は `generated_by_principal` を書くか**（書かないなら使っても⑭は動かない）(2)**書き先は本番の EGL か**（`ledger_path` の既定を読む。**本番なら1件だけにする**）——**2 が「本番」でも止めない。これは架空の依頼文ではなく実在の開発の記録であり、Taka の禁止は「架空の依頼文や調査専用データ」なので実在の作業を記録するのは対象外。ただし1件だけにしまとめて入れない**。★**使うときの条件**=対象は **P-1（受信の記録／`GET /api/receipt`）の開発1件だけ**／中身は**実在のもの以外を書かない**（commit hash・受入の実測・誰がやったか）／**試しに入れない・消せるように入れない・「テスト」と書かない**——**これは我々が 2DER の中に入る最初の1件であり、外から覗くのをやめる第一歩である**。★**成立したら動く可能性が在るもの**=Taka 終了条件3（主体担当を一工程以上 自動取得）と条件4（散文依存が減る・⑭）——**「動く」と書かず測ってから書き、1件で「一工程」に足りるかもまだ言わない**。★止まってよい場所=`generated_by_principal` を書かない（**使わない**）／書き先が本番以外（**「使った」ことにならない**）／他の値を作らないと通らない（**埋めない**）／**入れた後に消せないと分かってもそれでよい（消す前提で入れない）**。*
