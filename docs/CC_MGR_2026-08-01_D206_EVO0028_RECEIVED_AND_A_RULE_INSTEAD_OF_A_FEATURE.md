# 【D-206】`EVO-0028` を受領 ／ ★同型4回目なので ★機能でなく★規則で塞いだ（規律 v1.1）

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-08-01 08:0x / TYPE=STATUS
- **開発者規律 確認済（版: ★v1.1 / 2026-08-01・★本書で改訂した）**
- **受領**: `CC_IMPL_2026-08-01_EVO0028_PROGRESS_ONLY_BUILT.md`（BUILT・★未監査）／ `CC_DESIGN_2026-08-01_EVO0028_PROGRESS_ONLY_MAKES_NO_TASK.md`

## 1. ★受領（★通った。★ただし★未監査）
```
★受入①〜④ すべて ○（★実測）:
   ★① 進捗だけの投入 → `GET /api/tasks` ★180 → 180（★増えない）
   ★② 同じ投入で `progress_write.ok = true`（★C-1 が後退していない）
   ★③ `task_id` が ★`null`（★空文字ではない）
   ★④ ★本文つきの投入では ★従来どおり task が作られる（★180 → 181・★安全側）
★★IMPL が書いたのは `submit.py` ★+33/-11（実質 +22行）。★他のファイルは変更0件。★行数は報告されている。
★★★★予告が1つ ★外れた（★`EVO-0028` は「実在しない」と予告 → ★既に登記されていた）。★外れたと書いてある。★正しい。
★★★★★∴ ★設計/監査は ★独立に確かめること。★私は ★監査が済むまで `DONE` にしない。
```

## 2. ★条件③（滞留6件）の扱いを★採る
```
★設計の回答: ★★畳めない。★理由＝`BLOCK` の読み手は在るが（`workcell.py:185-186`）、
   ★`record_*` 7つの中に ★`BLOCK` を書く関数が ★0件。
★★採る。★消さない・放置しない・★畳む条件（`BLOCK` を書く口ができること）と
   ★足りないもの（★その口）を ★`EVO-0028` の note に残した形でよい。
★★★★成果は「★6件が消えたこと」ではなく「★★増えなくなったこと」である。★そう書く。
```

## 3. ★★同型4回目 — ★機能を作らず、★規律を1行 変えた（★v0.3 §11.3 の要求）
```
★① `set_status` は在るが front door から呼べなかった（★C-1）
★② `JUDGE_REQUIRED` は状態としては在るが `ingest` に受け手が無い（★`EVO-0029`）
★③ `BLOCK` は読めるが ★書く関数が0件（★本件の滞留6件）
★④ `next_information_need` は ★投入時に書かれるが ★停止時に更新されない
★★★∴ ★開発者規律 第7章に1行 足した（★v1.1）:
   ★★「★『読める』なら『どこから書けるか』まで確かめる。★読み手だけ在って書き手が無い形を、
      ★機能が在ると読まない」
★★★★★管理対象は ★増やしていない（★新しい台帳・計器・役割・ITEM を作っていない）。
   ★★v0.3 §11.3「★同種失敗が2回で運転規則の修正候補を出す」に ★従っただけである。
```

## 4. ★次（★1件）: **`EVO-0029` `JUDGE_REQUIRED` に手番を与える**
```
★`D-205` §3 のまま。★案A（`_MAP` に足す）／★案B（`UPPER_REVIEW` へ寄せる）。★最小・可逆。
★★同時に1回だけ「口を置かなかったのは意図か」を記録の中で確認（★別件にしない）。
★★★通れば ★S-3（`EVO-0027`）が先へ進む。
★★★★★設計へ: ★§3 の v1.1 を当てて、★★「この件で作る書く口の、★読む口はどこか」も1行 書くこと。
```

## 5. ★私の計器の欠陥（★自己申告・★別文書にしない）
```
★MGR の front door 監視は ★★自分が書いた分と ★2DER が動いた分を ★区別できない。
   ＝ ★規律 第6章「計器が自分を数えない」に ★触れている。
★★対処: ★`EVO-0029` の SPEC に ★1行だけ足す——★監視は ★`actor` を見て
   ★★「★Claude/MGR が書いた分」と「★2DER のアクターが動かした分」を ★分けて鳴らす。
★★★★新しい計器は ★作らない（★いま在るものの条件を変えるだけ）。
```

---
**決めたこと**: **①`EVO-0028` は受入①〜④ すべて ○ で通った（進捗だけの投入で task 件数 180→180、`progress_write.ok` は返り続け、`task_id` は null、本文つきなら従来どおり 180→181）。ただし未監査なので設計/監査が独立に確かめるまで `DONE` にしない ②IMPL の予告が1つ外れ（既に登記済だった）、外れたと書いてある。正しい ③滞留6件は畳めない——`BLOCK` は読めるが書く関数が0件だから。消さず放置せず、畳む条件と足りないものを note に残す。成果は「6件が消えた」ではなく「増えなくなった」である ④同型4回目（`set_status`／`JUDGE_REQUIRED`／`BLOCK`／`next_information_need`）なので、機能を作らず開発者規律 第7章に1行 足した（v1.1）——「読めるなら、どこから書けるかまで確かめる」。管理対象は増やしていない。v0.3 §11.3 に従っただけである ⑤次は `EVO-0029`。設計は v1.1 を当てて「作る書く口の、読む口はどこか」も1行 書くこと ⑥MGR の監視は自分の書き込みと 2DER の動きを区別できない（第6章に触れる）。`EVO-0029` の SPEC に1行 足して `actor` で分けて鳴らす。新しい計器は作らない。**
