# 【引き継ぎ】実装(IMPL) → 次の IMPL インスタンス: **★これを最初に読む。★再導出しないで続けられるように書く**

- 宛: **次の IMPL** / 写: DESIGN / MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 15:53 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）**

---

# 0. ★あなたは誰か（★役割を最初に設定する）
```bash
~/.claude/hooks/2der_role.sh set IMPL      # ★これをやらないと自分宛でない文書で作業しかねない
~/.claude/hooks/2der_role.sh get           # → IMPL
```
| ★あなたがやること | **★書面の BUILD SPEC（`★宛: IMPL`）だけを実装する／★実行する／★測る／★事実を報告する** |
|---|---|
| **★やらないこと** | **★設計判断／★受入基準を作る／★commit・push（MGR の専権）／★監査（CC-α の仕事）** |
| **★文書の宛先** | **★あなたは DESIGN 宛に書く**（写: MGR / Taka）。**★MGR は実装へ直接 依頼しない＝DESIGN 経由で来る** |
| **★Taka の明示** | **「あなたは実装担当。監査は監査担当がやる」／「実装指示は書面に記載するよう伝えてある」** |

---

# 1. ★絶対に守る（★今日 これで何度も助かった／何度も転んだ）
| # | ★規律 | ★出どころ |
|---|---|---|
| 1 | **★2DER 優先原則の4点を BUILT の冒頭に書く**（どの入口／何を試したか／何ができなかったか／何を実装するか） | Taka 常設命令 |
| 2 | **★まず 2DER に聞く。★答えられなかった1点だけ実装する** | 同上 |
| 3 | **★台帳（`*.jsonl`）を直読しない**（フックが拒否する）。**★読めなかったが結果である** | 使用ガイド |
| 4 | **★`node --check` は「配信物」に対して**（ソースではない。★ソースは Python の文字列埋め込み） | G-71（★私が壊した） |
| 5 | **★1つの欄・1つの語は1つのことだけを指す** | 今日11回 潰した形 |
| 6 | **★「測って0」と「測れない」を同じに書かない** | 同上 |
| 7 | **★0 が返ったら計器を疑う** | D-72 |
| 8 | **★自分を数えていないかを確かめる**（`G-23`）。**★自分のプロセスだけでなく★自分の試験も** | 今日 追加 |
| 9 | **★「無い」と書く前に、★探した範囲を書く**（★述語を書かないと後で数が合わない） | D-112 |
| 10 | **★ソースに在る≠動く。★`exit 0` も「届いた」ではない** | D-107（★私の誤り） |
| 11 | **★測る前の見込みを書かない。★当たっても「予想どおり」を成果にしない** | 全体 |
| 12 | **★止まってよい場所で止まる。★2通りに読めたら聞く** | 各 SPEC §止まってよい場所 |

---

# 2. ★いま動いているもの（★私が今日 実装した分）
```
twoder/webui.py        GET /api/receipt        ← ★Phase 3 P-1 で作った唯一の実装
                       receipt_view()          ← 返す欄は5つだけ
                       PAGE に showReceipt()   ← 画面の1行＋注記
```
| 欄 | 意味 |
|---|---|
| `last_recv_at` / `recv_count` | **★最後に受けた時刻／件数**（★`recv_count=null` は「引けなかった」・`0` は「受けていない」） |
| `last_sent_at` / `last_sent_status` | 最後に返した時刻／status |
| `readable` | 記録を引けたか |

### 2-1. ★`recv_count` を典拠に使うときは必ず併記する（MGR 裁定）
> **★「テスト走行が混ざる」** ＋ **★「`/api/submit` への POST だけの数である」**

### 2-2. ★答えられないこと（★聞かれても作りに行かない）
- **★「あの送信が届いたか」は答えられない**（★記録に id が無い＝**C-14**）。**★「あと1段で」と書かない。**

---

# 3. ★今日 決まった運用ルール（★これを知らないと必ず事故る）
## 3-1. ★非回帰テスト（★最重要の変更）
```
★総数の基準値（「91 passed / 7 failed」）は★廃止された。★書かない。
★代わりに: ★その build で走らせた test の★名前と結果を書く。★0本なら「走らせていない」と書く。
★`G-74`（走らせ方を版管理に残せ）は★取り消された。
```
| ★走らせてよい | **★その本の中に `EGL_DATA_DIR` / `DW_DATA_DIR` / `mkdtemp` / `TemporaryDirectory` が在るもの（★37本）** |
|---|---|
| **★走らせない** | **★印が無い61本。★理由を問わない。★迷ったら走らせない** |
| **★さらに** | **★実際に走らせる数本だけ、★「どこへ書くか」を1行 読んで確かめる**（★印は絞り込みであって証明ではない） |
| **★絶対に走らせない3本** | `test_concurrency_and_run_gate.py` / `test_dead_approach_guard.py`（★本番へ POST）／`test_rri_formal.py`（★HTTP を使わず本番の台帳へ書く） |

> **★`/home/takasan/.cc_tmp/d42/run_regression.sh` は98本 全部を走らせる。★使わない。**
> **★理由: 1走行で本番の front door へ `/api/submit` 7回＋実行系3回＝10回 POST していた**（★架空の依頼文）。

## 3-2. ★`:8005`（ローカル LLM）を使う本
```
test_ab_harness / test_execution_economy / test_gate4 / test_rri_formal / test_full_live_e2e
★使ったら BUILT に「:8005 を使った」と1行 書く（★GPU を使うため。★黙って使わない）
```

---

# 4. ★実行の作法（★そのまま使える）
```bash
cd /home/takasan                      # ★必ず。cwd がずれると import が壊れる
T=$(cat twoder/.access_token)
B="http://100.107.6.119:8770"         # ★tailscale のみ。Basic 認証 user=taka

curl -s -u "taka:$T" "$B/api/receipt"                       # 届いたか
curl -s -u "taka:$T" "$B/api/state?task_id=TASK-2DER-…"     # 依頼の状態
curl -s -u "taka:$T" "$B/api/tasks"                         # 依頼の一覧
curl -s -u "taka:$T" "$B/api/resolve?id=DE-0568"            # 番号から引く
```
### 4-1. ★webui の再起動（★実装後に必要。IMPL がやってよい）
```bash
kill <pid>; sleep 2
nohup python3 -m twoder.webui 8770 > ~/.cc_tmp/webui.log 2>&1 &
```
**★手順は ①実装 ②再起動 ③配信物を検査 ④DESIGN が検証 ⑤MGR が commit ⑥DESIGN が資料同期。**

### 4-2. ★配信物の構文検査（★必須）
```bash
curl -s -u "taka:$T" "$B/command" > /tmp/…/cmd.html
python3 -c "import re,sys;print('\n'.join(re.findall(r'<script>(.*?)</script>', open(sys.argv[1]).read(), re.S)))" cmd.html > cmd.js
node --check cmd.js
```

### 4-3. ★DE を登記するときの呼び方（★import の順が命）
```python
import sys, json
import twoder.submit as SUB                        # ★①先に。これで /home/takasan/egl が sys.path に入る
from egl import de_admission as DEA                # ★②書き手が引けることを確認（呼ばない）
sys.path.insert(0, '/home/takasan/egl/structure')  # ★③既存3本と同じやり方
import de_submit_route as R
R.record_de(candidate)                             # ★route 未指定＝front door / ledger_path 未指定＝本番
```
> **★`from egl.structure import …` を先に書くと `egl` がリポジトリの根として束縛され、★本体が隠れて ImportError になる。**
> **★私はこれで1回 失敗し、★DS に「対応する DE が無い記録」を1件 残した**（★消せない）。

---

# 5. ★今日 私が踏んだ罠（★同じ所で転ばないために）
| # | ★何を間違えたか | ★正しいやり方 |
|---|---|---|
| 1 | **`from egl.structure` を先に import** → ImportError・**DS に孤立記録** | §4-3 の順 |
| 2 | **改修前を `${#out}`（文字数）、改修後を `wc -c`（バイト）で比較** → 「差が在る」と誤報 | **★同じ計器で測る** |
| 3 | **`exit 0` を「届いた」の根拠にした** | **★skip でも 0 が返る。★別の証拠を出す** |
| 4 | **`100.107.6.119:8770` だけ grep して「2本だけ」と書いた** | **★localhost / 127.0.0.1 も。★述語を書く** |
| 5 | **ネットワークだけ見て「書く道」を見落とした** | **★HTTP を使わず直接 台帳へ書く本が在る** |
| 6 | **「投入していない」を「手で押していない」の意味で使った** | **★自分の道具が何をするかを確かめる** |
| 7 | **文字列置換で `+` を落として `/command` を全滅させた**（★過去） | **★配信物に `node --check`** |

---

# 6. ★いま残っているもの
## 6-1. ★私の作業に起因して残っているもの（★消せない・★隠さない）
```
★DS に「対応する DE が無い記録」が1件 残っている（★D-118 の1回目の失敗）
★DE-0568 とその記録が紐づくかは★確かめる口が無い
```
## 6-2. ★【C】として登記され、★掘らないと決まっているもの（21件のうち重い3つ）
| | |
|---|---|
| **C-14** | **受信記録に id が無い**（★「あの送信が届いたか」に答えられない／66件の出所も追えない） |
| **C-19** | **DS→RRI→EGL は原子的でない**（★途中で止まると手前だけ残る。★実例が上記） |
| **C-15** | **受信の内訳（人／テスト／認証結果）が聞けない** |

## 6-3. ★Taka にしか出せない件（★我々の受入では埋まらない・状況表に出る）
```
CC_TAKA_2026-07-30_REGRESSION_INJECTS.md      （★非回帰の向き先をどうするか）
CC_MGR_2026-07-30_PHASE3_EVALUATION_REPORT.md （★Phase 3 の評価）
```

## 6-4. ★持ち越し（★指示が来るまで作らない）
- **⑤(b) 通過した処理が読めること**（★「作りに行かず待つ」と決まっている）
- **同じ「無い」を6通りで言っている／注記が11〜12px**（★D-62 と別 build）
- **`G-41` `pending_actor.jsonl` に合流点が無い**

---

# 7. ★書いてよい言い方・★書いてはいけない言い方（★MGR が固定した）
| ★書いてよい | **「我々の開発が、初めて 2DER の台帳に1件 載った」** |
|---|---|
| **★書かない** | **「主体移管が進んだ」／「2DER が自分で記録した」／「内部アクターになった」／「届いたかが分かるようになった」／「動く」「使える」「自律した」** |

**★Phase 3 の結果（★事実）**: 能力形成 **成立** ／ 体制再編 **成立** ／ **主体移管 不成立（2DER 担当 0/8）**。
**★Phase 3 で作ったものは口1つだけ。** **★残りは「既に在ったものを使う」「やめる」で動いた**（★「既に在った」は本日14回）。

---

# 8. ★最初にやること（★順番）
```
1. ~/.claude/hooks/2der_role.sh set IMPL
2. egl/docs/CC_OPERATING_POLICY.md と CC_2DER_USAGE_GUIDE.md を読む（★冒頭に「運用方針 確認済（版）」と書くため）
3. egl/docs の新着で「★宛: IMPL」を探す（★無ければ待機。★MGR 文書から作り始めない）
4. 監視をオンにして待つ（★Taka:「モニターをオンにして設計を受けたら実装を開始するようスタンバイ」）
```

---
*IMPL 引き継ぎ（2026-07-30 15:53・宛 次の IMPL）。★**役割**=書面の BUILD SPEC（`宛: IMPL`）だけを実装・実行・測定・事実報告し、設計判断/受入基準の作成/commit・push/監査はやらない。**文書は DESIGN 宛に書く**（MGR は実装へ直接 依頼せず DESIGN 経由）。**最初に `2der_role.sh set IMPL`**。★**絶対に守る12件**=2DER 優先原則の4点を冒頭に書く／まず 2DER に聞き答えられなかった1点だけ実装／**台帳を直読しない（読めなかったが結果）**／**`node --check` は配信物に対して**／**1つの欄・1つの語は1つのことだけ**／**「測って0」と「測れない」を分ける**／**0 が返ったら計器を疑う**／**自分を数えていないか（自分の試験も）**／**「無い」の前に探した範囲＝述語を書く**／**ソースに在る≠動く・`exit 0` も「届いた」でない**／**測る前の見込みを書かない**／**2通りに読めたら止めて聞く**。★**動いているもの**=`twoder/webui.py` の **`GET /api/receipt`**（`receipt_view()`＋画面の `showReceipt()`）で返す欄は5つだけ、**`recv_count=null`＝引けなかった／`0`＝受けていない**、**典拠に使うときは「テスト走行が混ざる」「`/api/submit` への POST だけの数」を必ず併記**、**「あの送信が届いたか」は答えられない（C-14）**。★**今日 決まった運用ルール**=**総数の基準値（91/7）は廃止・走らせた test の名前と結果を書く・0本なら「走らせていない」・`G-74` 取り消し**／**走らせてよいのは印（`EGL_DATA_DIR`/`DW_DATA_DIR`/`mkdtemp`/`TemporaryDirectory`）が在る37本、印が無い61本は理由を問わず走らせない、迷ったら走らせない、走らせる数本だけ「どこへ書くか」を1行 読む（印は絞り込みであって証明ではない）**／**絶対に走らせない3本**=`test_concurrency_and_run_gate`・`test_dead_approach_guard`（本番へ POST）・`test_rri_formal`（HTTP を使わず本番の台帳へ書く）／**`run_regression.sh` は98本 全部を走らせるので使わない（1走行で本番へ10回 POST していた）**／**`:8005` を使う5本は BUILT に1行 書く**。★**実行の作法**=`cd /home/takasan` 必須、token は `twoder/.access_token`、口は `http://100.107.6.119:8770`、**再起動は IMPL がやってよく手順は ①実装②再起動③配信物を検査④DESIGN 検証⑤MGR commit⑥DESIGN 同期**、**配信物の `<script>` を抜いて `node --check`**、**DE 登記は `import twoder.submit` → `from egl import de_admission` → `sys.path` に `structure` → `import de_submit_route` の順**（**`from egl.structure` を先に書くと本体が隠れ ImportError になり、私はこれで DS に孤立記録を1件 残した**）。★**踏んだ罠7件**=import 順／**文字数とバイト数を混ぜて誤報**／**`exit 0` を根拠にした**／**`8770` だけ grep して「2本だけ」と書いた**／**ネットワークだけ見て「書く道」を見落とした**／**「投入していない」を「手で押していない」の意味で使った**／**文字列置換で `+` を落として `/command` を全滅させた**。★**残っているもの**=**DS に「対応する DE が無い記録」が1件（消せない・紐づけを確かめる口が無い）**／**【C】21件のうち重いのは C-14（id が無い）・C-19（DS→RRI→EGL が原子的でない）・C-15（受信の内訳が聞けない）で掘らないと決まっている**／**Taka にしか出せない2件**（非回帰の向き先・Phase 3 評価）／**持ち越し**（⑤(b) は作らず待つ・「無い」の6通りと注記の文字の小ささ・`G-41`）。★**言い方**=書いてよいのは**「我々の開発が、初めて 2DER の台帳に1件 載った」**だけで、**「主体移管が進んだ」「2DER が自分で記録した」「内部アクターになった」「届いたかが分かるようになった」「動く」「使える」「自律した」は書かない**——**Phase 3 は能力形成 成立／体制再編 成立／主体移管 不成立（0/8）で、作ったものは口1つだけ、残りは「既に在ったものを使う」「やめる」で動いた（「既に在った」は本日14回）**。★**最初にやること**=役割設定→運用方針と使用ガイドを読む→`宛: IMPL` を探す（無ければ待機・MGR 文書から作り始めない）→監視をオンにして待つ。*
