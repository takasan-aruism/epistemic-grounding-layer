# CC 管理(MGR) → 設計/監査(CC-α): **★緊急: D-58 が画面を壊していた。JS 構文エラーで★全ボタンが死んでいる。★受入は全部 `【実】` だった**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **契機**: Taka 逐語「**ボタン押しても何も変化しないよ。前は色々表示された**」

## 1. ★実測（推測なし）
```
再現: GET /command → 21218 バイト取得 → <script> を抜き出し → node --check
結果:
  /tmp/cmd.js:94
    …引けませんでした: "+esc(rid)+"（…区別できません）"</div></div>";return;}
                                                       ^^^^^^^^^^
  SyntaxError: Invalid or unexpected token

再現: grep -n "その run は引けませんでした" twoder/webui.py        → :770（現ディスク）
再現: git show b99f2f3:webui.py | grep -n 同上                    → :743（★commit 済）
∴ ★D-58 の commit に入っている。
```
**★壊れ方: `+"（…）"` で文字列を閉じ、その後の `</div></div>";` が★裸の JS になっている。**

## 2. ★意味（★これが全部を説明する）
> **★JS は1箇所壊れると、そのページの★全部が動かない。**
> **∴ 送信・REFRESH STATE・RUN NEXT・RUN UNTIL BARRIER・OPEN が★全部死ぬ。**
> **∴ Taka の「前は色々表示された」＝★D-58 より前は動いていた、である。**

**★プロセスは 15:26:43 起動・ファイルは 15:26:21。** **∴ ★15:26 から今まで、画面は壊れたままだった。**

**★D-63 で挙げた3機構（try/catch 無し・401・Authorization）は、★今回の原因ではない。** **★JS がそもそも1行も走っていない。**

## 3. ★最も重いのはここである（★我々の受入が通ってしまった）
| D-58 の受入 | 判定は | ★実際は |
|---|---|---|
| 新文言が在る | **`【実】`** | **★文字は在る。しかし★ページは死んでいた** |
| 旧文言が0件 | **`【実】`** | 同上 |
| 非回帰 91/7 | **`【実】`** | **★テストは JS を見ていない** |
| `ok`/`bad` 0件 | **`【実】`** | 同上 |

> **★我々は「文字列が在るか」を測って、「ページが動くか」を測ったことにしていた。**
> **★これは D-59 で私が書いた形そのものである——「★その計器は、その答えしか出せない場所ではないか」。**
> **★文字列検索は、壊れたページでも必ず「在る」と答える。** **∴ 0 でも 1 でも、意味が無かった。**

## 4. ★「人しか出せない」だと思っていたが、★違った（★訂正する）
**私は D-59 §3 で「画面を実際に開く」を★人しか出せない区分に置いた。**
> **★誤りである。** **★`node --check` で機械的に出る。** **★我々がやっていなかっただけである。**
> **★「人しか出せない」は、★我々が測れないことの言い訳になっていた。**

**∴ ★区分を1つ動かす: 「JS が構文として成立しているか」は★決定論である。**
**★人しか出せないのは「読んで分かるか／使えるか」であって、「動くか」ではない。**

## 5. ★依頼（★最優先。D-64 より先）
| # | |
|---|---|
| **1** | **★`:770` の文字列を直す**（`</div></div>` を文字列の中に戻す）。**★1箇所。他を触らない** |
| **2** | **★同じ壊れ方が他に無いか、`node --check` が通るまで確認する。** **★1件ずつ直さない——通るまで出た分を全部** |
| **3** | **★`node --check` を、以後 `PAGE` を触る build の★受入に必ず入れる**（`node` の有無は実測で確認済・v22.22.2） |
| **4** | **★直したら私に出す。私が commit し、その後にプロセスを再起動する**（★状況表が「ソースが新しい＝再起動が要る」と鳴っている） |

**★D-64（A/B）は、これが終わってから。** **★A/B は依然として要る**（次に黙ったとき原因が分かるようにするため）。

## 6. ★私の落ち度
**★私は D-58 を「9件すべて直っている」と受領し commit した。** **★JS が動くかを一度も測らなかった。**
**★Taka が押すまで、誰も気づかなかった。** **★これは私の受領の欠陥である。**

---
*MGR 緊急。★**実測**=`GET /command` の `<script>` を `node --check` にかけると **`SyntaxError`（94行目）**で、`+"（…区別できません）"` が文字列を閉じた後の `</div></div>";` が**裸の JS** になっている。`twoder/webui.py:770`（現ディスク）／`git show b99f2f3:webui.py`（**commit 済**）の双方に在り **D-58 の commit に入っている**。★**意味**=**JS は1箇所壊れるとそのページの全部が動かない** ∴ 送信・REFRESH STATE・RUN NEXT・RUN UNTIL BARRIER・OPEN が**全部死ぬ**——Taka の「前は色々表示された」は **D-58 より前は動いていた**ということであり、プロセスは 15:26:43 起動・ファイルは 15:26:21 なので**15:26 から今まで画面は壊れたままだった**。**D-63 で挙げた3機構（try/catch 無し・401・`Authorization`）は今回の原因ではない——JS がそもそも1行も走っていない**。★**最も重いのは我々の受入が通ってしまったこと**=D-58 の受入は「新文言が在る」「旧文言が0件」「非回帰 91/7」「`ok`/`bad` 0件」がすべて `【実】` だったが、**文字は在ってもページは死んでおり、テストは JS を見ていない**——**我々は「文字列が在るか」を測って「ページが動くか」を測ったことにしていた**。**これは D-59 で MGR が書いた「その計器は、その答えしか出せない場所ではないか」そのもので、文字列検索は壊れたページでも必ず「在る」と答えるので 0 でも 1 でも意味が無かった**。★**「人しか出せない」だと思っていたが違った（訂正する）**=MGR は D-59 §3 で「画面を実際に開く」を人しか出せない区分に置いたが**誤りで、`node --check` で機械的に出る。我々がやっていなかっただけである**——**「人しか出せない」が我々が測れないことの言い訳になっていた** ∴ **区分を1つ動かし「JS が構文として成立しているか」は決定論とする。人しか出せないのは「読んで分かるか／使えるか」であって「動くか」ではない**。★**依頼（最優先・D-64 より先）**=(1)`:770` の文字列を直す（`</div></div>` を文字列の中に戻す。1箇所・他を触らない）(2)**同じ壊れ方が他に無いか `node --check` が通るまで確認する（1件ずつ直さず出た分を全部）**(3)**`node --check` を以後 `PAGE` を触る build の受入に必ず入れる**（`node` の存在は実測済・v22.22.2）(4)**直したら MGR に出し、MGR が commit した後にプロセスを再起動する**（状況表が「ソースが新しい＝再起動が要る」と鳴っている）——**D-64（A/B）はこれが終わってから。ただし A/B は依然として要る**。★**MGR の落ち度**=**D-58 を「9件すべて直っている」と受領し commit したが JS が動くかを一度も測らず、Taka が押すまで誰も気づかなかった。これは MGR の受領の欠陥である**。*
