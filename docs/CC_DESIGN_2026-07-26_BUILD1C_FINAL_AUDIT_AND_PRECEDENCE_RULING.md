# 設計/監査 → MGR/Taka: 1c 最終監査（MGR 依頼A）+ 優先表の裁定（依頼B①③）

- `BUILD_ROLE: 参照`
- 宛: MGR / Taka / 写: IMPL / 発: 設計/監査(CC-α) / 2026-07-26 / TYPE=FINDING
- 対象: `CC_IMPL_2026-07-26_PROSE_REMOVED_CLAIM_REGISTRY_BUILT.md`（MGR 依頼A の対象は前段 BUILT だが、**その後の訂正実装まで含めて監査した**）

## 0. 結論
**【監査:CC-α】依頼A の核心は満たされている。汚染は解けており、HBB-30 は答えファイル無し・接地hit 0 で `SUPERSEDE` に到達する。** 依頼B ① を裁定する（**私の予測は外れた。IMPL 提案を、今日の原則から正当化して採用する**）。

## 1. 依頼A — 私が直接実行した検証【監査:CC-α】
すべて**私が `rri` を import して直接叩いた**結果。IMPL の報告値と一致。

| # | 検証 | 結果 |
|---|---|---|
| 1 | **非回帰 13/13**（既定 `SUPERSEDE_PRIORITY_ENABLED=False`） | **13/13 PASS** ✓ |
| 2 | **★核心: 答えファイル無しで HBB-30 が `SUPERSEDE` へ** | **`state=DECLARED_UNVERIFIED`**・`searched=['DE_LEDGER','FILE_MANIFEST','DOCS']`（**`hbb_candidates.json` を含まない**）・**`declared_prior=[{id:'CLAIM-HBB30-6X', source_de:'DE-0106'}]`** ✓ |
| 3 | **接地hit が 0 であること**（＝探索で拾ったのではない） | **`grounding_hits=0` / `mention_hits=0` / `self_referential_hits=0`** ✓ |
| 4 | **登記台帳が load-bearing** | 台帳パスを外すと **`UNKNOWN`** に落ち、戻すと `DECLARED_UNVERIFIED` ✓ |
| 5 | **負の対照**（実在しない造語） | **`NOT_FOUND`** ✓（前回は自分の DE 報告で `UNKNOWN` に汚染されていた） |
| 6 | **PP1「Watcher 仕様」** | **`UNKNOWN`**・接地hit 0 ✓（前回は `GROUNDED` に汚染） |
| 7 | **実在ファイル `preflight_gate.py`** | **`GROUNDED`**・接地hit 9・自己言及 0 ✓ |

### 1-1. ★意味（ここが本題）
**HBB-30 の答えは、探索が拾ってきたのではない。** 接地hit は 0 であり、**我々が意図して登記した地位（`CLAIM-HBB30-6X` / 出所 DE-0106）だけ**から出ている。
> **「たまたま書いてあった」ではなく「意図して登記した」に基づく。**

**この間に DE が2件増えたが、計器は一切動かなかった**（IMPL 報告・私も負の対照で確認）。**前回は DE を1件書いただけで負の対照が壊れていた。＝汚染の口が構造的に塞がっている。**

### 1-2. 依頼A-3（裁定§2 の受入条件）について
MGR 指摘のとおり **受入条件は前段では未達**だった（PP1 が `UNKNOWN`→`GROUNDED` に変化）。**ただしそれは私の裁定§2 自体が誤りだったためであり、§2 撤回後の現在は PP1 が `UNKNOWN` に戻っている**（上表6）。**受入条件は「§2 を撤回する」という形で解消された。** 未達のまま放置していない。

### 1-3. 依頼A-4（df 閾値20 の記録）
**【監査:CC-α】`check_existence(..., df_threshold=None)` として引数化されており、返り値に df が載る実装になっている**ことを確認。**ただし閾値の妥当性評価は未実施**（IMPL 申告どおり）。§3 で裁定する。

## 2. 依頼B① — `SUPERSEDE` 優先の裁定【私の予測は外れた】
**私は「registry 実装後に衝突は消える可能性が高い」と書いた。IMPL の実測では消えなかった（12/13・`test 12` が落ちる）。私の予測は誤りである。**
- 原因は IMPL の分析どおり: **散文を検索していたことではなく、量的主張そのものを照合対象にしていること。** 登記台帳が `DECLARED_UNVERIFIED` を返す以上、存在ゲートは発火し続ける。

### 裁定【設計:CC-α】: **IMPL 提案を採用する。ただし場当たりの追加ではなく、今日の原則から導く。**
優先表を**事前固定**し、以下の順とする:

| 優先 | 出口 | 根拠の種別 |
|---|---|---|
| **1** | **`STRONGLY_DISCOURAGE_DW`**（失敗履歴あり） | **起きたことの記録**（我々が実際に失敗した事実） |
| 2 | `SUPERSEDE`（登記された地位） | **我々が下した判断の記録** |
| 3 | `CLARIFY_FIRST`（出典が曖昧） | 入力の表層signal |
| 4 | その他既存 | — |

**正当化**: 本日立てた原則「**接地の証拠は『起きたことの記録』であって『書いたことの記録』ではない**」を、**出口の優先にも適用する。**
- **失敗履歴は「我々に実際に起きたこと」の記録**。
- **登記された地位は「我々が下した判断」の記録**。
- **前者が後者に優先する。** ＝ IMPL の提案は場当たりではなく、**原則の当然の帰結**である。

**これで両立する**: `test 12`（失敗履歴あり）は `STRONGLY_DISCOURAGE_DW` のまま／**HBB-30 は失敗履歴が無い**（`similar_failure_history=False` を私が確認済）ので `SUPERSEDE` に到達する。

**受入**: 優先表を有効にした上で **(i) 非回帰 13/13 (ii) HBB-30 → `SUPERSEDE`** の**両方**を示すこと。**片方だけなら採用しない。**

## 3. 依頼B③ — df 閾値20【裁定】
- **汚染が解けたので今なら測れる。測ること。**
- **出すもの**: 閾値を変えた時の **負の対照 / PP1-3 / 実在ファイル / HBB-30** の判定表。**閾値を動かして結果が大きく動くなら、その脆さも報告する。**
- **閾値は事前固定・記録**。**測定後に都合の良い値へ動かさない**（動かすなら理由を記録）。

## 4. 依頼B② について
**既に裁定済**（`CC_DESIGN_2026-07-26_PROSE_IS_NOT_RECORD_ADJUDICATION.md`）。MGR の依頼と私の裁定は同時並行で走っており、**MGR の見立て（一過性バグとして塞がず構造で決める／ただし大掛かりな分離機構を先に建てるな）と結論は一致している**: 我々が採ったのは **散文を外す＋小さな登記台帳1本**であり、**大掛かりな分離機構ではない。**

## 5. 現時点の正味（過大にしない）
- **【監査:CC-α】HBB-30 は「登記した地位」から正しい出口に届く。** これは**設計が意図した経路で届いた**ということであって、**HBB-30 を自律的に解いたことではない。**「約6倍は未検証だ」と最初に判断したのは人間であり、我々はそれを**登記して再利用可能にした**だけである。
- **【監査:CC-α】発火率 0/298** ＝ 実発話に「以前作った X」型が出てこない。**現時点では fixture 専用の機構**。
- **未評価**: df 閾値の頑健性。**未達→解消**: 裁定§2 の受入条件（§1-2）。

---
*DESIGN CC-α 最終監査。依頼A の核心は満たされた——接地hit 0・答えファイル無し・登記台帳が load-bearing・汚染3件すべて解消を私が直接確認。依頼B① は私の予測外れを認めた上で IMPL 提案を採用、ただし今日の原則（起きたことの記録 > 書いたことの記録）から導出した。両立確認を受入条件とする。*
