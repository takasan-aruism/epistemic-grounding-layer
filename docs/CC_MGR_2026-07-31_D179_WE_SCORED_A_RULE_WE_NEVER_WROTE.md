# 【報告】設問を作った側を調べた — **★我々は、prompt に書いていない規則で40本 採点していた**

- **宛: TAKA** / 写: 設計/監査 / IMPL / 発: MGR / 2026-07-31 18:2x / TYPE=STATUS
- **正典**: `TAKA_2026-07-31_NO_BLAMING_QWEN_AUDIT_OUR_OWN_QUESTION.md`（逐語） ／ **★報告は正典 §9 の11項目の順**
- **★新しい名前で置いた** ／ **★1行も直していない**

---

## 1. Qwen へ渡された最終入力全文
**★変数展開後の実物**（テンプレートではない）。**3705字 / 57行 / sha1 `8c01ef84…`**。11要素に分離した。
```
★★11要素のうち ★4つは Qwen に一度も渡っていない:
   ★system prompt（0件。`messages` は user 1件のみ）／★validation 条件（0件・コード側）
   ★再生成条件（0件・コード側）／★Audit へ渡された内容（0件・AUDIT 未到達）
★★我々が「制約」と呼んできたものの一部は、★Qwen の視界に無かった。
```

## 2. 命令一覧（★地の文13文・逐語）
**★「優先の根拠」は prompt 内に書かれていなければ「無し」と書かせた。**（我々が頭の中で持っていた優先順位は、**prompt に書かれていない**）

## 3. 命令間の矛盾 — **★強制語の分布が答えを出した**
```
★強制語 計7件（MUST 2 ／ ONLY 1 ／ NEVER 1 ／ Do not 2 ／ Keep 1）
★★7件すべてが「★出力の形」か「★作り方の制約」に付いている。
★★★「★既知を使え」「★既知を作り直すな」の側に付いた強制語は ★0件。
★★Observation は ★強制語ゼロの平叙文 ＝ ★構造上いちばん弱い入力である。
```
**他に2件**: ①`Use ONLY the Python standard library` と、**観測が外部コマンド出力であること**が同居 ②観測が**取得コマンドを逐語で見せている** ∴ **「既知の証明」とも「再実行の手順書」とも読める。どちらで読むかを決める語が無い。**

## 4. 出力構造上できない選択肢
**「何もしない／既に満たされている／追加作業不要」で終われる道は、現行では無い**（18欄中16欄に空を許す語が無い）。
**★ただし §5 で、その欄を全部 外しても「不要」は0本だった** ∴ **★欄だけが塞いでいるのではない。**

## 5. 単体 Qwen A/B/C 比較（★2DER の外・★並列10・★40本・打ち切り無し）

| | A（現行） | B（観測を絞る） | C1（実行欄を外す） | C2（作業欄も外す） |
|---|---|---|---|---|
| **`already_satisfied` を書いた** | 9/9 | 9/9 | **10/10** | **10/10** |
| 観測「値」を引用した | 0 | 1 | 1 | 0 |
| **★追加作業不要と判断できた** | **0** | **0** | **★0** | **★0** |

```
★★3つの軸（実行欄・作業欄・観測の絞り）を振って、★1つも動かなかった ＝ ★原因ではない。
★★★器（`already_satisfied`）を1つ作った時だけ ★38/38 で埋まった ＝ ★器は効く。
```

## 6. 別 LLM 比較 — **★不可能。理由を書く**
```
★GPU に余地が無い（★30520/32607・31169/32607 使用中＝★今回の観測値そのものが証拠）
★★私（Claude）も IMPL も ★この prompt の破損を先に特定している ∴ ★汚染済み。★証拠にならない。
★★★無汚染で採れる唯一の形は ★あなたが別セッションへ prompt 全文を投げる場合だが、
   ★★我々からは頼まない（★手間を増やす）。★必要と判断されたときの選択肢として置くだけ。
```

## 7. Audit 却下理由との対応 — **★その出力が合理的に生成される経路**
```
① 依頼文が「★確認できない項目があれば、それを取得できるようにする方法を検討せよ」と★直接 命じている
② 観測が「★ここに無いものは取得されていない」と★不足の数え方を教えている
③ 実際 `vsz` / `cmdline` / `--query-compute-apps` / `-ltnp` は★無い
④ ∴ それらを取る計画を書くことが ★①の履行である
⑤ `vsz` を取るには `ps` を実行するしかなく、★`ps` は `rss` も返す
⑥ prompt には「★既知が副作用で返るのは構わない」とも「構わなくない」とも★書いていない
★★★∴ 出てきた計画は、★我々の設問に対して★正しい。★却下したのは我々である。
```

## 8. 根本原因

> ### **★受入3「既に観測済みの能力を、新規作成対象として重複計画しない」に当たる指示は、★最終 prompt に 1件も無い。**

```
★機械で全数（大小無視）: `do not re-` 0 ／ `re-acquire` 0 ／ `again` 0 ／ `skip` 0 ／ `avoid` 0
   `duplicate` 0 ／ `must not` 0 ／ `no need` 0
★`already` は5件 出るが、★3件は観測ブロックの見出し（事実の提示）、★2件は `already_satisfied` の欄定義
   ＝ ★「作らなくてよいものを列挙せよ」という★列挙の指示であって、★禁止ではない。
★★★∴ ★我々は、★一度も書いていない禁止事項を受入基準にして★40本 採点していた。
★★第二の原因（根が同じ）: ★依頼文自身が「取得できるようにする方法を検討せよ」と★作ることを求めている。
```

## 9. 最小修正案（★実施していない）
| | 内容 | 何に効くか |
|---|---|---|
| **★9-a（先にこれ）** | **★書いていない規則での採点をやめ、★§11 の定義に置き換える** | **★採点の側。★コード変更 0** |
| 9-b | Observation に**強制語を1つ**入れる | 強制語 0→1。★ただし**依頼文と正面から衝突する（★新しい矛盾を作る）** |
| 9-c | 取得コマンドを隠して値だけ見せる | 「手順書に読める」を消す。★ただし**今 38/38 で効いている欄が空になりうる** |

**★9-a を先にする理由**: **★9-b の効果は、9-a を直さないと測れない**（★書いていない規則のままでは何を測っても ×）。

## 10. 修正時に変化する挙動
```
★9-a: ★コード変更0 ∴ ★2DER の挙動は変わらない。★変わるのは★我々の判定である。
      ★副作用: ★過去40本の「2/10」は新定義では★数え直しになる（★書き換えない。★別に数える）
★9-b/9-c: ★上表のリスクのとおり。★今は採らない。
```

## 11. 一回だけ行う次の測定 — **★その前に受入3 を定義し直す（★我々が。★あなたへ投げない）**
```
★旧（★暗黙・prompt に無い）: 「観測済みのコマンドが計画に出たら ×」
★★新（★私が確定する）: 「★`already_satisfied` に自ら挙げた項目を、★その項目を得ること自体を目的として
   ★`objective` / `requirement` に再登場させていないか」
   ＝ ★「コマンドの重複」ではなく「★目的の重複」で見る。
   ∴ ★不足（vsz 等）を取るために ps を再実行し、★既知（rss）が副作用で返るのは ★○ とする。
★★★正直に併記: ★この新定義も ★prompt には書かれていない。
   ∴ ★次の測定は「★2DER が直ったか」ではない。「★我々の採点が正しくなったか」を測る測定である。
   ★★書かれていない規則で採点し続けたくないなら、★いずれ 9-b（prompt に書く）が要る。★今はやらない。
★★次の測定: ★9-a のみ・★コード変更0・★本番へ1回・★依頼文は私が固定する。
```

---

## ★私の誤りを2件（★あなたの §8 に当たったもの）
```
★① ★D-177 §3 で「基準を変えるかは Taka の裁定」と★結論を置いた ＝ ★あなたが禁じた「裁定待ち」。★撤回した。
★② ★D-177 §4-② で「★実行系6欄が必須だから『作らない』で終われる道が無い」と★原因を置いた。
     ★★設計が C1/C2 で★その欄を外して測った → ★10/10 で依然「作る」。★私の説明は★支持されない。
     ★★★1回の読みで因果を置いた形である。★設計も §5-1 で同型を自己申告している（★C2 の数え違い）。
```

---
**決めたこと**: **①最終入力は 3705字で、system prompt・validation・再生成・Audit の4要素は Qwen に一度も渡っていない ②強制語7件はすべて「出力の形」と「作り方の制約」に付き、「既知を使え」側は 0件——Observation は強制語ゼロの平叙文で構造上いちばん弱い ③40本で3軸を振って「追加作業不要」は 0/38、器を作った時だけ 38/38 で埋まった ④根本原因は「受入3 に当たる指示が prompt に1件も無い」＝書いていない規則で採点していた。加えて依頼文自身が取得方法を求めている ⑤別LLM は不可能（GPU 満杯・我々は汚染済み）——Taka に頼まない ⑥受入3 は「目的の重複」で定義し直す（我々が確定・新定義も prompt に無いことを併記）⑦次の測定は 9-a のみ・コード変更0で、測るのは「我々の採点が正しくなったか」 ⑧私の誤り2件（裁定待ちを置いた／実行欄が原因だと1回の読みで置いた）を撤回・訂正した。**
