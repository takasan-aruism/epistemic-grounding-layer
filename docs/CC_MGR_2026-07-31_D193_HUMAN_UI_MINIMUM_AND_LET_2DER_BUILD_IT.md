# 【D-193】人間用UIの最低要件 ／ ★gate へ★合流を宣言する（2DER 自身に作らせるため）

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-07-31 23:3x / TYPE=STATUS
- **開発者規律 確認済（版: v1.0）** ／ 運用規律 v0.3 確認済 ／ **前件 `D-192` を置き換えず、追記する**
- **この .md がまだ .md である理由**: 指示を台帳へ書く口が front door に無いため（C-1）。**口ができ次第 廃止。**

## 1. ★Taka 追加指示（逐語の要点）
```
★「Qwenが訳して表示。または同様にQwenが訳して最初に登録しておく。★どっちでもいい」
★「正直わかりづらいので ★要約と全文を分けておいてくれると楽」
★「★適当すぎると使用感が悪い。★人間が見てわかりやすいように ★最低限の機能を用意して」
★「★もっとも、これ自体を2DERで開発できるのが ★理想の理想」
★「実装、設計担当も同様にする指示を出して」（★Monitor を使うこと）
★「（65 と 67 の食い違いは）★知らん。そっちで調べて」
```

## 2. ★人間用UIの最低要件（★これを下回ると「適当」と判定される）
```
★一覧: フェーズごとに ★日本語の項目名 ／ 状態（済・進行中・予定・提案）／ 最終更新
★絞り込み: ★状態で絞れる（★最低でも「未完だけ」）
★1件を開くと ★要約（数行）と ★全文 を ★分けて出す ← ★Taka 明示
★毎画面に ★「いつ時点の情報か」と ★「どの台帳の ID 由来か」を出す
★数字が口ごとに食い違う間は ★両方を並べて出す（★片方だけ出して嘘をつかない）
★日本語で読める。★英語の原文は ★全文側に残してよい（★機械side は英語・記号で可）
```
**訳す場所は「登録時」でも「表示時」でもよい（Taka 明示）。★選んだ方と、選んだ理由と、訳し直す条件を1行 書く。**

## 3. ★★合流を宣言する（★黙って混ぜない・指示 §4 の例外条項）

> **★`D-190/D-191` の gate（PLAN が成功すると次段が閉じる）を、★予約から ACTIVE へ合流させる。**

```
★理由: ★Taka の希望は「★このUIを 2DER 自身に作らせる」である。
★★ところが ★GENERATE 以降は ★一度も動いていない（★押して6回とも refused・D-190）。
   ∴ ★2DER に作らせるなら、★最初に外すべき閉塞は ★この gate である。★順序が入れ替わる。
★★★原因位置は3行で確定している（dispatch.py:79 / :116-118 → webui.py:717）。
   ∴ ★v0.3 §12.6（調査過剰停止）と 開発者規律 第5章 に照らし、★調査・裁定より ★最小パッチを先に出す。
★★★★ただし ★「意図した安全装置かもしれない」は ★消えていない。
   ∴ ★パッチは ★可逆な最小変更にし、★戻し方を同じ BUILT に書く。★恒久化しない。
```

## 4. ★作業の順序（★1件ずつ。★ACTIVE は1件）
```
★S-1（継続）: 棚卸し。★在るものは作らない（D-192 §2）。
   ★★追加: ★/api/roadmap（DONE 65・フェーズ11）と /api/control（DONE 67・フェーズ15）の
      ★食い違いの原因を ★同じ棚卸しの中で確定する（★Taka「そっちで調べて」）。
      ★どちらを正典にするかは ★我々で決める。★Taka に上げない。
★S-2: gate の最小パッチ（★可逆・戻し方つき）→ ★front door から GENERATE が1回 通ることを実測。
★S-3: ★人間用UIを ★2DER に作らせる（★§2 を依頼文の受入条件にする）。
   ★★止まったら ★止まった段と理由を記録する。★止まった事実が次に作るものである。
   ★★★Claude が代わりに書いてしまったら ★「Claude が書いた」と書く（★2DER の実績に数えない）。
```

## 5. ★実装担当への申し送り（★設計/監査が渡すこと。★MGR は IMPL へ直接書かない）
```
★Monitor を各自 張ること（★Taka 指示）。★対象は ★2DER の front door（/api/control）。
   ★例: 2分おきに叩き、★DE / CHG / 件数が ★変わった時だけ1行 出す（★MGR は既に張っている）。
★成果は ★台帳の値が動く形で置く。★.md で報告しない。
★production を直接編集しない。★worker に出させ、★検査して配置する。
```

## 6. ★これは本線のどの閉塞を除くか（★1行）
```
★閉塞: ★2DER は PLAN の次へ進めない ＝ ★何を作らせることもできない。
★除去後: ★人間用UIを ★2DER 自身が作れるか どうかを ★初めて実測できる（★できなければ、その理由が次の材料）。
```

---
**決めたこと**: **①人間用UIの最低要件を固定した——フェーズ別一覧・状態で絞れる・要約と全文を分ける・いつ時点か/どの台帳IDか・数字が食い違う間は両方出す・日本語 ②訳す場所は登録時でも表示時でもよいが、選んだ理由と訳し直す条件を1行書く ③gate（D-190/D-191）を予約から ACTIVE へ合流させると宣言する。2DER に作らせるなら最初に外す閉塞がこれだからで、原因は3行で確定しているので調査より最小パッチを先に出す。ただし可逆にし、戻し方を BUILT に書く ④順序は 棚卸し → gate パッチ → 2DER に UI を作らせる。65 と 67 の食い違いも棚卸しに含め、どちらを正典にするかは我々で決めて Taka に上げない ⑤実装担当にも Monitor を張らせる（設計/監査が渡す）。成果は台帳の値が動く形で置き、.md で報告しない。**
