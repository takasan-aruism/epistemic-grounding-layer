# 実装 → 設計/監査: **9件を1回で直しました。「無い」を「何が無いのか」まで書く形に**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-29 / TYPE=BUILT
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-29_D58_NINE_WORDINGS_BUILD_SPEC_v1_0.md` v1.0
- **受領した文書**: 上記 / `CC_DESIGN_2026-07-29_D58_HANDOFF_TO_IMPL.md` / `CC_MGR_2026-07-29_D58_APPROVED_PYTHON_EXCEPTION_AND_SYNC_ORDER.md`（写しで観測）

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 結果（★2軸）
### 経路
- [x] **9件すべて直した**（1回で・文言だけ）
- [x] **止まってよい場所には当たりませんでした**（§5）
### 実測
- [x] 受入1 **新文言が在る**（★2件は配信 HTML でなくコードで示す・§2-1）
- [x] 受入2 **旧文言は8種すべて0件**
- [x] 受入3 実例が在る #C は出る分岐に入る（`taka_authority = []`）
- [x] 受入4 **`ok`/`bad` を足していない**（★差分だけで数え、追加10行中0件）
- [x] 受入5 既存が不変（sha256 `acc9ddba5f0f4bec` 一致）
- [x] 受入6 非回帰 91 passed / 7 failed（顔ぶれ diff 空）

---

## 1. 直した9件（★文言だけ・振る舞いを1つも変えていません）
| # | 旧 | **新** |
|---|---|---|
| **偽になる文** | `全 event の result は OK（失敗の記録なし）` | **「取得した event の中に、失敗の記録は在りません」**＋`truncated` なら**「★全 event を見ていません（打ち切られています）」**を併記 |
| **窓1** `/` | `なし`（直近 DE） | **「直近6件の中には在りません」** |
| **窓2** `/` | `なし`（直近 CHG） | **「直近6件の中には在りません」** |
| **A** | `現在 runnable な DW task はありません。` | **「進められる DW task は在りません。まだ投入していない／投入したが DW task にならなかった／BLOCK された、のいずれかです。この画面では区別できません」** |
| **B** | `その run は記録に無い: <id>` | **「その run は引けませんでした: `<id>`（存在しないのか、記録を引けなかったのかは区別できません）」** |
| **C** | `authority boundary なし` | **「記録された authority boundary は在りません（記録されなかったものが在るかは、この画面では分かりません）」** |
| **D** | `欠損・失敗の記録なし` | **「記録された欠損・失敗は在りません」**＋併記**「記録されなかった欠損・失敗が在るかどうかは、この画面では分かりません」** |
| **E** | `EGL の source_refs / claims` → `無い` | **「この経路では取れません（EGL に根拠が無いのか、この経路に載らないのかは区別できません）」** |
| **F** `/` | off-ramp フラグ → `なし` | **「記録された off-ramp フラグは在りません」** |

**★書き方の規律4つを全件に当てました**: 「無い」と言い切らない／区別できないなら「区別できません」と書き原因を推測しない／「この画面では」を付ける／色を使わない。

## 2. 受入1・2（★2画面とも実取得して確認）
```
再現: GET /command と GET /（認証つき）を取得し、文言を検索した
  ①偽になる文        在り        A  在り
  ①truncated 併記    在り        B  在り
  D                在り        C  在り
  D併記              在り        E  在り
  ★②直近DE/CHG      配信 HTML には無い（§2-1）
  ★F               配信 HTML には無い（§2-1）

受入2（旧文言・8種を1つずつ検索）:
  全 event の result は OK（失敗の記録なし） → 0件
  現在 runnable な DW task はありません。   → 0件
  その run は記録に無い:                    → 0件
  authority boundary なし                → 0件
  欠損・失敗の記録なし                      → 0件
  <div class='v'>無い</div>              → 0件
  <td class="dim">なし</td>              → 0件
  <span class="dim">なし</span>          → 0件
  ★1つも残っていません。
```

### 2-1. ★窓2件と F が配信 HTML に出ない理由（★「無い」で終わらせません）
```
再現: grep -c "直近6件の中には在りません" twoder/webui.py            → 2
再現: grep -c "記録された off-ramp フラグは在りません" twoder/webui.py → 1
再現: GET / の実データを数える
  直近 DE のチップ数 = 6 / 直近 CHG のチップ数 = 6 / off-ramp フラグの行数 = 9
∴ どれも空でない ∴ 空のときだけ出る文言は、いま配信される HTML に現れない。
```
- **∴ この3件は「コードに在るが、いまの実データでは発火しない」ものです。**
- **★実装源 §6-1 が「実例を見つけていない」に挙げた側と一致します。** **作って試していません。**
- **★∴ 私は「画面に出た」とは書きません。** **書けるのは「コードに在る」ことと「発火しない理由が実データで説明できる」ことです。**

## 3. 受入3・4・5・6
```
受入3: GET /api/state?task_id=TASK-2DER-B9B4DA3B → taka_authority = [] ∴ #C の新文言が出る分岐に入る
受入4: git diff webui.py の追加10行のうち pill ok / pill bad / class='ok' / class='bad' → ★0件
       （★ページ全体では数えていません。差分だけで数えました）
受入5: /api/state sha256  before acc9ddba5f0f4bec / after acc9ddba5f0f4bec  ★一致
受入6: 非回帰98本 91 passed / 7 failed（基準と★顔ぶれ diff 空）
差分:  webui.py | 10 insertions(+), 9 deletions(-)
```

## 4. ★Python 側（`/` の3件）で守ったこと
- **文字列リテラルだけを変えました。**
- **`de = rep.get("recent_de", [])[-6:]` / `chg = …[-6:]` を1行も変えていません**（実測で確認・§2-1 の再現に掲載）。
- **`or` の分岐構造・出す/出さないの判定を変えていません。**
- **∴ 実装源 §7-1「文字列だけを変えられなかったら止めて報告」には当たりませんでした。**

## 5. 止まってよい場所（実装源 §7・いずれにも当たりませんでした）
| # | 条件 | 実際 |
|---|---|---|
| 1 | `/` の2件で文字列だけを変えられなかった | **変えられました**（ロジック無変更を実測） |
| 2 | 旧文言を消すと別の分岐が壊れた | **壊れていません**（非回帰・sha256 とも不変） |
| 3 | **9件のどれかが指定の場所に無かった** | **★9件すべて指定の場所に在りました**（設計の数え間違いはありません） |
| 4 | SPEC が2通りに読める | 無し |

## 6. 実例が在るもの / 見つけていないもの（★混ぜていません）
| 実例が**在る** | **#C のみ**（`taka_authority = []` で実際に分岐に入る） |
|---|---|
| **実例を見つけていない** | 偽になる文（`truncated` の run が無い）／#A・#B・#D・#E ／ `/` の窓2件・#F |

**★作って試していません。**

## 7. 触ったファイル / commit / 資料
```
twoder/webui.py のみ（JS 6件 ＋ Python の文字列リテラル3件）
```
**commit していません**（MGR）。**投入していません。** **資料の同期はしていません**（MGR 裁定で「MGR の commit を待ってから設計/監査が行う」に変更されたことを写しで観測しています）。

---
*IMPL BUILT（D-58）。「無い」と言い切っていた9件を**1回で・文言だけ**直した（振る舞い・分岐条件・出す/出さないの判定を1つも変えていない）。内訳=**偽になる文1**（`全 event の result は OK（失敗の記録なし）` → 「取得した event の中に、失敗の記録は在りません」＋`truncated` なら「全 event を見ていません（打ち切られています）」。「全 event」と書かず「OK」という評価語も使わない）／**窓2**（`/` の直近 DE・CHG → 「直近6件の中には在りません」。**`[-6:]` を1行も変えていない**）／**まとめている6**（A〜F を実装源の文言どおり）。書き方の規律4つ（言い切らない／区別できないと書き原因を推測しない／「この画面では」を付ける／色を使わない）を全件に当てた。★受入=新文言は `/command` と `/` の**両方を実取得**して確認し、**旧文言8種はすべて0件**。**#C は `taka_authority=[]` で実際に分岐に入る**。`ok`/`bad` は**差分だけで数えて追加10行中0件**（ページ全体では数えていない）。`/api/state` の sha256 は `acc9ddba5f0f4bec` で前後一致、非回帰 91/7・顔ぶれ diff 空。★**窓2件と F は配信 HTML に現れない**——コードには在る（`grep -c` で 2 と 1）が、実データが空でない（直近 DE 6件・CHG 6件・off-ramp フラグ 9行）ため空のときだけ出る文言が発火しないからで、**実装源 §6-1 の「実例を見つけていない」側と一致する。作って試しておらず、「画面に出た」とは書かない**。★止まってよい場所4件はいずれにも当たらず、特に **9件すべてが指定の場所に在った**（設計の数え間違いは無い）。実例が在るのは #C のみで、他8件は実例を見つけていない——**混ぜて書いていない**。触ったのは `twoder/webui.py` のみ（JS 6件＋Python の文字列リテラル3件）、commit・投入・資料同期はしていない。*
