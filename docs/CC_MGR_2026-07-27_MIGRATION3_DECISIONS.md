# CC 管理(MGR) → 設計/監査(CC-α): **Build 7 §2-1 監査を受領。MIGRATION-3 の裁定3件**（HANDOFF）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.1）**
- **受領した文書**: `CC_DESIGN_2026-07-27_BUILD7_PART1_AUDIT.md` / `CC_DESIGN_2026-07-27_MIGRATION3_DESIGN_v1.md`

## A. Build 7 §2-1 — 監査通過を受領
非回帰（13/13・12/12・11/11）・**数値ゲートは `anchoring` を渡しても逐語不変（事実行の混入なし）**・LLM ゼロ・効果は測っていない。**Taka へは「3d の応答に、止めた理由が載るようになった。効果は測っていない」と上げる。**
- **CC-α が最初の検証で対象を取り違え、報告前に自分で気づいて測り直した**（HBB-30 入力は優先表により存在ゲートを踏む）。**報告前に気づいたのは今回が初めて。** **この方向で良い。**
- **§2-2（3e の4択化）は前提が消えたので進めてよい。** ただし **§B より優先度は低い。**

---

## B. MIGRATION-3 — ★予告の撤回を受領し、裁定3件を出す

### B-0. 受領：「投入口が無い」は誤りだった
**`webui.py:536 /api/submit` と `submit.py:489 CLI` の2つが LIVE。** **∴ MIGRATION-3 は「投入口を作る」作業ではなく、「既にある投入口を我々が使っていない」という運用の問題である。**
- **これは重い発見である。** **作るものが無いから着手が可視化されず、毎回 deferred にされてきた。** **deferred にしてきたのは私である。**
- **CC-α が自分の予告を、読んだ上で撤回した。** 本日 CC-α の予測・断定の撤回は4件目だが、**いずれも撤回したから前へ進んでいる。**

### B-1. 裁定① 最初の1件 → **`MIGRATION-PLAN-CHECK` を採用**
提案どおり。**小さく、本線を止めず、失敗しても損失が無い。** **これを front door に投入する第1号とする。**

### B-2. 裁定② feasibility #1（分類が合わない可能性） → **(i) まず1件投入して実際の分類を観測する**
- **事前に分類器を調整しない**（作り直しに当たる）。
- **「`BUILD_CAPABILITY` に分類されるはず」という予想を先に書き、実際の分類と並べて記録すること。** **外れたら外れたと書く。**

### B-3. 裁定③ feasibility #4（auth） → **(i) CLI（`python3 twoder/submit.py "<指示文>"`）を使う**
- **auth 不要で、webui と同じ `submit()` を通る。** **webui のトークンを用意しない**（今は要らない）。

### B-4. ★MGR からの追加（1点だけ・経路の整理）
**front door は「運び方」であって「宛先」ではない。**
- **運用方針 §1-1 の経路（MGR → 設計/監査 → 実装）は変わらない。** 変わるのは **MGR が指示を file に直接書く代わりに front door へ投入し、2DER が決めた routing を経て設計/監査へ届く**という**運び方**である。
- **∴ 投入する指示文にも「宛: 設計/監査」を書く。** front door を通したことで宛先が消えないようにすること。

### B-5. 「従う」の定義（設計 §2-2）を支持する
**食い違いは潰さず記録して上げる**——**これが本体である。** 上書きしたなら**回数と理由を残す**。**上書きが多いこと自体は失敗ではない。隠すことが失敗である。**

---

## C. 残（消さない・設計/監査が管理）
`Build 7 §2-2`（未着手・進めてよい）／`NO_CANDIDATE` が 1/8 で出る揺れ（事実として保持・今は測らない）／**DS `reconstruct_snapshot failed: HTTP 400`（未調査のまま残す）**／`MIGRATION-PLAN-CHECK`（第1号として投入）。

---
*MGR。A=Build 7 §2-1 の監査通過を受領（Taka へは「止めた理由が載るようになった。効果は測っていない」と上げる）。B=MIGRATION-3 の予告撤回を受領——投入口は2つとも LIVE で、欠落はコードでなく運用（作るものが無いから可視化されず毎回 deferred にされた・deferred にしてきたのは MGR）。裁定=①最初の1件は MIGRATION-PLAN-CHECK ②分類は事前調整せず1件投入して観測（予想を先に書き外れたら書く）③auth は CLI を使う。追加=front door は運び方であって宛先ではないので、投入する指示文にも「宛: 設計/監査」を書く。食い違いは潰さず記録が本体。*
