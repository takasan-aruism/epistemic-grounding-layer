# CC 管理(MGR): **Build 4 へ GO（Taka 直接）** + 事前登録する予測と3つの数字（HANDOFF）

- `BUILD_ROLE: 参照`（実装源は `CC_DESIGN_2026-07-26_BUILD4_SPEC_MENU_RESTRICTION_AND_ORIGIN.md` の1本）
- 宛: IMPL(coder) / DESIGN/AUDIT(CC-α) / 写: Taka / 発: MGR / 2026-07-27 01:05 / TYPE=HANDOFF

## 0. 権限
Taka 直接発話（2026-07-27・逐語）:
> **「Build 4 進めて」**

**着手可。** 実装源は BUILD SPEC v1.0 の1本のみ。**PART 1 の受入①（LLM を呼ばない証明）を最初に出すこと**（安い・決定的）。

## 1. SPEC への MGR の確認
- **異議なし。** 原則の適用箇所が2つだけで、新しい発想を足していない点も良い。
- **§8 の訂正を受領する**（「`origin` があれば除外フィルタは要らない」は **私の誤り**。過去491件は永久に `UNSPECIFIED` なので歴史用にフィルタは残し、**境界のレコード id を記録する**）。**CC-α の指摘が正しい。**
- **§4 の自己申告を受領**: 「**本日私が書いた設計文書のうち、精度を上げる介入を指示したのは本 SPEC の PART 1 だけ**」——**数えて出したことを評価する。**

## 2. ★追加条件（`..._THE_TEST_WAS_THE_PROBLEM.md` の裁定を反映）
### 2-1. 数字は**3つ並べて出す**（1つだけ出すのを禁じる）
| # | 指標 | 現状（同一 run・20 fixture × 3 seed = 60） |
|---|---|---|
| 1 | **遠隔の誤り率**（止まる/動くを跨ぐ） | **2/60 = 0.033** |
| 2 | **別解も可の一致率** | **55/60 = 0.917** |
| 3 | 厳密一致率（従来指標・捨てない） | **47/60 = 0.783** |

### 2-2. ★事前登録する予測（当たっても外れても記録する）
**メニュー制限の標的である遠隔2件は、両方とも「`context` が空なのに `CONTEXT_RESOLVE`」（IP1/IP2）である。**
> **予測（MGR）: この2件が構造的に消えるなら、遠隔 0.033 → 0.000 / 別解可 0.917 → 0.950 / 厳密 0.783 → 0.817。**
- **これは算術上の上限であって、予測が当たる保証ではない。** メニューから外した結果、モデルが**別の遠隔誤り**（例: `DEFER` へ流れる）を起こす可能性がある。**そちらが増えたら増えたと書く。**
- **外れたら外れたと書く。** 本日 CC-α の予測は2回外れ、そのたび**外れたと書いたから前へ進めた。**

### 2-3. 期待ラベルの見直しは**測定より先に・字面のみを根拠に**
- **IP1/IP2 は `acceptable_strategies` が空**である。近縁3件は `INTENT_PROBE` vs `PREMISE_PROBE`＝**どちらも止まって聞き返す側**であり、**字面から `PREMISE_PROBE` が擁護できるかを DESIGN が判定すること。**
- **★厳禁**: **数字を良くするために後からラベルを足さない。** **(i) 字面のみを根拠に (ii) 実測より先に (iii) 理由を1文で記録し (iv) 変更前後の両方の数字を出す。**
- **本質的に揺れていて1つに決められないものは「揺れている」と記録する。** 無理に正解を決めない。

### 2-4. per-fixture を報告に含める
**平均値だけの報告は受け取らない。** **どの fixture がどう外したかを必ず書く**（`per_fix` は既に記録されている。**捨てているのは報告の側である**）。

## 3. 目的の再確認（Taka）
> **揺れている入力に「唯一の正解を当てる」ことを目標にしない。「どう解釈できるか」を出し、揺れているなら揺れていると扱い、危険な側に倒れないことを目標にする。**

**7戦略には既に揺れの受け皿がある**（聞き返す/選択肢を出す/複数の見方を出す）。**メニュー制限は「揺れの受け皿を残したまま、成立しない選択肢だけを外す」介入である。** **受け皿まで削らないこと**——`INTENT_PROBE`/`PREMISE_PROBE`/`CHOICE`/`BOUNDED_MULTI_VIEW` を前提無しのまま残すのは正しい。

---
*MGR。Taka 直接「Build 4 進めて」により着手可。SPEC に異議なし（私の「除外フィルタ不要」の誤りを CC-α が訂正したのを受領）。追加条件=3つの数字を並べる/事前登録した予測（遠隔 0.033→0.000・別解可 0.917→0.950・厳密 0.783→0.817 は算術上限であり保証でない・別の遠隔誤りが増えたら書く）/期待ラベルの見直しは測定より先に字面のみで/per-fixture を報告に含める。揺れの受け皿（聞き返す・選択肢・複数の見方）は削らない。*
