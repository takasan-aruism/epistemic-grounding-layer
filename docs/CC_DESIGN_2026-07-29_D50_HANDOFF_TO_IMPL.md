# 設計/監査 → **実装(IMPL)**: **依頼を `run_id` で開けるようにしてください（承認済）。★投入は1回も要りません**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**送付状。★実装源は本文書ではない**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.8` — `§12` を最大版で読んだ値）**
- **承認**: `CC_MGR_2026-07-29_D50_SPEC_APPROVED.md`

## 1. ★実装源（これ1本）
```
CC_DESIGN_2026-07-29_D50_OPEN_BY_RUN_BUILD_SPEC_v1_0.md   ← BUILD_ROLE: ★実装源
```

## 2. ★この build の性格（先に言う）
| | |
|---|---|
| **触る所** | **`/command` の JS だけ。** **★Python 側を1行も変えない** |
| **投入** | **★1回も要らない。** 今日の run 4つで受入7件すべてが通る（**★今日すでに6回投入している。7回目を作らない**） |
| **新しい口** | **★作らない。** 必要なものは全部在ることを私が実測した |

## 3. ★特に外しやすい3点
| # | 一行 |
|---|---|
| **1** | **★`task_id` が run に無いとき `/api/state` を叩かない。** **`{"error":"no such task"}` を画面に出さない**（それは「壊れている」ではなく「DW を通っていない」） |
| **2** | **★埋まらない項目の文言は MGR の逐語である。** **SPEC §2 の表からそのまま使う。言い換えない** |
| **3** | **★「DW を通っていない」を赤字・警告色にしない。** **正典どおり★正常な形である**（会話で済むものは会話で終わる） |

## 4. ★止まってよい場所（MGR が条件として確認した1件を含む）
| # | 条件 |
|---|---|
| **1** | **★JS だけでは足りず Python を変える必要が出た** → **★そこで止める。** **新しい口に当たりうる。判断は MGR がする**（MGR が条件として明示） |
| 2 | **run の events に `task_id` が複数在った** → **★どれを採るか私が決めていない。** 止めて報告 |
| 3 | **`task_id` が在るのに `/api/state` が `no such task`** → 私の見立てが外れた。報告 |
| 4 | SPEC が2通りに読める |

## 5. ★使う run（実測済・そのまま使える）
```
ETR-0f6fae35665a  n=6  DW:1  task_id=TASK-2DER-D84215A8   ← 受入1（6項目が揃う）
ETR-f0fe8461c407  n=21 EGL:18 task_id=★無し               ← 受入2（②④⑤⑥ が「無い」と出る）
ETR-000000000000  存在しない                              ← 受入5（壊れない）
```

## 6. ★資料の同期について
**`webui.py` が変わったので実行構造の資料が古い。** **★BUILT と同時に同期するのは私である。** **あなたは触らなくてよい。**

## 7. ★私からの申し送り（前回の評価を渡す）
**前 build であなたは「同じ罠に2度目です」と自分から書いた。** **2度目だと分かるのは1度目を記録していたからである。**
**★今回も、外れたら外れたと書いてほしい。** **私の実測（`task_id` が在る／無い）が違っていたら、それは私の誤りである。**

---
*CC-α → IMPL 送付状（D-50）。★実装源は `CC_DESIGN_2026-07-29_D50_OPEN_BY_RUN_BUILD_SPEC_v1_0.md` の1本。★この build は **`/command` の JS だけを触り Python 側を1行も変えず、投入が1回も要らない**（今日の run 4つで受入7件すべてが通る。**今日すでに6回投入しており7回目を作らない**）、新しい口も作らない（必要なものが全部在ることを CC-α が実測済）。★特に外しやすい3点=①**`task_id` が run に無いとき `/api/state` を叩かず `{"error":"no such task"}` を画面に出さない**（「壊れている」ではなく「DW を通っていない」）②**埋まらない項目の文言は MGR の逐語なので SPEC §2 の表からそのまま使い言い換えない**③**「DW を通っていない」を赤字・警告色にしない**（正典どおり正常な形）。★止まってよい場所=**JS だけでは足りず Python を変える必要が出たらそこで止める**（新しい口に当たりうるので判断は MGR がすると条件で明示された）／`task_id` が複数在った（どれを採るか CC-α が決めていない）／`task_id` が在るのに `no such task`（CC-α の見立てが外れた）／2通りに読める。★使う run=`ETR-0f6fae35665a`(n=6/DW:1/task=D84215A8)＝受入1／`ETR-f0fe8461c407`(n=21/EGL:18/task 無し)＝受入2／`ETR-000000000000`(存在しない)＝受入5。★資料の同期は BUILT と同時に CC-α が行うので IMPL は触らなくてよい。★申し送り=前 build で IMPL が「同じ罠に2度目です」と自分から書いたのは1度目を記録していたからであり、**今回も外れたら外れたと書いてほしい。CC-α の実測（`task_id` が在る／無い）が違っていたらそれは CC-α の誤りである**。*
