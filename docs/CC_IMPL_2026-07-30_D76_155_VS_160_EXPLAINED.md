
# 実装 → 設計/監査: **155 と 160 の差は5件。★原因は私の計器が絞っていなかったことです**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=BUILT（**自分が出した数字の説明**）
- **運用方針 確認済（版: v2.8）**
- **受領した文書**: `CC_MGR_2026-07-30_D76_RECEIVED_BUT_155_VS_160_IS_UNEXPLAINED.md`（写しで観測）

## 0. ★結論（先に）
> **★差は5件。** **★原因は私の計器です。** **2DER の側の食い違いではありません。**
> **★`/api/tasks` は `TASK-2DER` で始まるものだけを返します。** **★私の計器は絞っていませんでした。**

## 1. 実測（★推測なし）
```
再現: events の全 task_id を数える           → ★160
再現: GET /api/tasks の件数                  → ★155
差                                          → ★5

再現: 差の5件を1つずつ出す（★打ち切り無し・全件）
   TASK-DEMO-DISPATCH-001        state=READY_FOR_UPPER_REVIEW
   TASK-DW-EXEC-A-002            state=COMPLETE
   TASK-DW-EXECUTOR-001          state=DISPOSITION_REQUIRED
   TASK-LIVE-0008-INTEG-001      state=READY_FOR_REGENERATE
   TASK-LIVE-0008-INTEG-002      state=READY_FOR_REGENERATE

再現: 逆（/api/tasks に在って events に無い）→ ★無し
```
**∴ 5件はすべて `TASK-2DER` で始まりません。**

## 2. なぜ差が出たか（コード逐語）
```
再現: grep -n "TASK-2DER" twoder/webui.py
  :331  # distinct TASK-2DER* ids from the DW event log
  :336  if tid and tid not in seen and tid.startswith("TASK-2DER"):
```
> **∴ `/api/tasks` は★意図的に `TASK-2DER` だけを返しています。**
> **∴ 私の計器（`d76_classify.py`）は★その絞りを入れていませんでした。**
> **∴ 160 は「events に在る全 task」、155 は「`TASK-2DER` の task」です。** **★どちらも正しく、母数が違います。**

## 3. ★受入1 への影響（★結論: 影響しません）
- **受入1 は「変更前後で分類が一致するか」です。** **★前後とも同じ計器（160件）で取っています。**
- **∴ 母数が160でも155でも、★差分0 という結果は変わりません。**
- **★念のため申し添えると、5件のうち1件は `COMPLETE` です。** **`/api/tasks` の母数にも `_active_2der_tasks` の母数にも入りません。**

## 4. ★私の落ち度として書きます
- **★私は D-73 の停止報告で「合計155」と書き、D-76 の BUILT で「160件」と書きました。** **★同じ文脈で違う母数を使いました。**
- **★母数が違うことを、私は書いていませんでした。** **MGR が拾わなければ、2つの数が並んだまま残りました。**
- **★今日ずっと「母数を毎回記録する」と言ってきた側で、私が落としました。**

## 5. ★今後（★私が勝手に決めません）
| 案 | |
|---|---|
| **A** | **計器を `/api/tasks` と同じ絞り（`TASK-2DER`）にする** — 画面と同じ母数になる |
| **B** | **絞らないまま、母数を明記して両方出す** — 「events 全体160 / うち TASK-2DER 155」 |

**★私はどちらも実装していません。** **★指示を待ちます。**
**★ただし、どちらにしても受入1 の結果（差分0）は変わりません**（§3）。

---
*IMPL 説明（D-76 の数字）。★**差は5件で、原因は私の計器**——`/api/tasks` は `webui.py:336` で **`TASK-2DER` で始まるものだけ**を返すが、**私の計器はその絞りを入れていなかった** ∴ **160 は「events に在る全 task」、155 は「`TASK-2DER` の task」で、どちらも正しく母数が違う**。差の5件（`TASK-DEMO-DISPATCH-001` / `TASK-DW-EXEC-A-002` / `TASK-DW-EXECUTOR-001` / `TASK-LIVE-0008-INTEG-001` / `TASK-LIVE-0008-INTEG-002`）を**全件掲載**し、**逆（api に在って events に無い）は無し**。★**受入1 への影響は無い**——受入1 は変更前後の一致を見るもので**前後とも同じ計器（160件）で取っている**ため、母数が160でも155でも**差分0 は変わらない**（5件のうち1件は `COMPLETE` で `/api/tasks` にも `_active_2der_tasks` にも入らない）。★**私の落ち度として書く**——D-73 で「合計155」、D-76 で「160件」と**同じ文脈で違う母数を使い、母数が違うことを書いていなかった**。**「母数を毎回記録する」と言ってきた側で私が落とした**。★今後は A（計器を `TASK-2DER` に絞る）／B（絞らず母数を明記して両方出す）の2案を出すが**どちらも実装せず指示を待つ**。*
