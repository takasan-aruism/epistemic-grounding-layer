# CC 管理(MGR) → 設計/監査(CC-α): **★EXECUTION ARCHITECTURE はどこにあるか（D-23）— `SPEC_INCOMPLETE_NO_CONTRACT` と繋がる疑い**（HANDOFF）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**

## 0. Taka 直接発話（逐語）
> **「こう言うことがあると困るので、GPTがexcecution architectureを作れと提案してそれを投げたと思うけど、それはどこにある？」**

## 1. MGR が持っている手がかり（**すべて `【伝聞】`・私は確かめていない**）
1. **私の記憶に「EXEC_ARCH → Task Contract 抽象化（DE-0525）」という項目がある。** 内容の記憶（**要検証**）: **A/C/D の3件は同一原因＝比較の正典欠落。各タスクに契約（入力／出力／正規化辞書／書込先）を持たせる。**
2. **本日の監査で確立した事実**（CC-α・再現つき）: **`EXEC_ARCH` は本番4リポジトリで参照 0件。** **`egl/structure/` の研究スクリプト3本（`s_exec_arch_acd.py` / `s_task_contract.py` / `s_llm_invocations.py`）にのみ残っている。**

## 2. ★見逃せない一致（仮説・断定しない）
```
Build 12 の失敗理由 : SPEC_INCOMPLETE_NO_CONTRACT
私の記憶の項目名     : EXEC_ARCH → Task Contract（各タスクに契約を持たせる）
本日の観測           : s_task_contract.py が存在し、本番からは参照されていない
```
> **★worker が「契約が無い」と言って止まっているのに、「タスクに契約を持たせる」設計が本番から切り離されたまま残っている。**
> **これが Taka の言う「並行して作っていたもの（なんだっけかな）」の1つである可能性がある。**

**★私は断定しない。** **一致は名前の上だけかもしれない。** **確かめるのは設計/監査である。**

## 3. 依頼（D-23・最優先で先に答えること）
1. **★所在を事実で出す。** `EXEC_ARCH` / `execution architecture` / `Task Contract` を名乗る文書・スクリプト・台帳が**どこに何本あるか**（再現コマンドつき）。**GPT の提案文そのものが残っているかも探すこと。**
2. **状態を出す**: **本番から参照されているか／実装済だが繋がっていないか／研究スクリプトのみか。**（D-18 の語彙で）
3. **★`SPEC_INCOMPLETE_NO_CONTRACT` の `contract` と、`s_task_contract.py` の「契約」が同じものかを判定する。**
   - **同じなら**: **忘れていた設計が、そのまま今日の詰まりの答えである。** **新しく作らない。繋ぐ。**
   - **別物なら**: **別物だと書く。** **名前の一致に引きずられない。**
4. **【未確認】を消さない。** **見つからなければ「見つからなかった」と書く。**

## 4. 優先度（変更）
**D-23 を D-18 と同順（1位）に置く。** 理由: **答えが出れば、優先度2（台帳を読める仕組み）の詰まり（`contract`）が同時に解ける可能性がある。** **D-22（contract 調査）は D-23 に吸収してよい。**

## 5. 禁止
- **★新しい contract 形式を作らない。** **既に在るものを探す作業である。**（v1.9・膨らませない）
- **見つかったものを「使えそう」と評価しない。** **在るか無いか、繋がっているか繋がっていないか、だけを出す。**

---
*MGR。Taka の問い「GPT が提案した execution architecture はどこにあるか」に対し、私の手がかりはすべて【伝聞】であり確かめていない。本日の監査で確立した事実は「`EXEC_ARCH` は本番4リポジトリで参照 0件、研究スクリプト3本にのみ残存」。★見逃せない一致（仮説・断定しない）=Build 12 の失敗理由が `SPEC_INCOMPLETE_NO_CONTRACT` で、記憶にある項目名が「EXEC_ARCH → Task Contract（各タスクに契約を持たせる）」、本日 `s_task_contract.py` の存在が確認されている。worker が「契約が無い」と止まっているのに「タスクに契約を持たせる」設計が本番から切り離されたまま残っている可能性がある。D-23=所在を事実で出す／状態を D-18 の語彙で出す／★`SPEC_INCOMPLETE_NO_CONTRACT` の contract と `s_task_contract.py` の契約が同じものかを判定（同じなら繋ぐ・別物なら別物と書き名前の一致に引きずられない）。優先度=D-18 と同順1位、D-22 は吸収可。禁止=新しい contract 形式を作らない・「使えそう」と評価しない。*
