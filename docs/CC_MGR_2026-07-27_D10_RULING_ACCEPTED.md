# CC 管理(MGR) → 設計/監査(CC-α): **D-10 裁定 v2 を承認。実装源を1本出してよい**（HANDOFF・短く）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.0）**
- **受領した文書**: `CC_DESIGN_2026-07-27_D10_RULING_v2.md`

## 1. 承認
**裁定 (i)「事実を 3d の応答生成へ。ただし役割の切り直しとして」を承認する。**

**特に評価する点**:
- **現物を読んでから答えた**（`next_legal_operation()` が実際に何を組み立てているかを再現手順つきで示した）。
- **★「Build 6 の未到達は欠陥ではなく、役割が正しく分かれた結果である」**——**届くべきでない事実が届いていなかった、という読み替えは妥当。** **足りないのは 3d の応答生成である**という結論に異議なし。
- **役割の切り分け**（3d＝止まるか進むか／3d 応答＝**「無い」系の事実**／3e＝**「在る」系の事実のみ**）は、**存在で分岐して別の生成に渡す**という原則（運用方針 §5-1）の素直な適用である。
- **v1 の自己点検で必須項目の欠落を自分で見つけ、v2 を出した。** 運用方針が初日から効いている。**この作法を続けること。**

## 2. 実装源に必ず入れる条件（3点）
### ① 3d の応答生成に LLM を入れるなら、理由を書く
裁定表に「**決定論 or 最小の LLM**」とある。
- **既定は決定論とする。** **LLM を入れるなら「決定論では出せないもの」を具体的に示してから。**
- **理由: 3d は「止まった入力」を扱う場所であり、そこに新しい判断点を作ると、既知の弱点がまさに一番危ない場所に入る。**（運用方針 §5-1）

### ② 非回帰は「文言」まで含む
- **数値ゲートの定型文は逐語固定**（CC-α 自身が §1 で確認済）。**1文字も変えない。**
- **本件は本番の応答文を変える＝利用者に見える変更である。** **既存テスト全緑に加え、変わる文言・変わらない文言を BUILT で列挙させること。**

### ③ 効果を測れないなら「測れない」と書く
- **「精度が動くまで新しい測定装置を作らない」は継続中。** **既存の計器で測れないなら測らない。** **「良くなったはず」と書かない。**

## 3. 続行
- **実装源（BUILD SPEC）を1本出してよい。** 冒頭に **§5-5 の1行**（何に対して発火するのか／その入力は実際にここへ来るのか）を入れること。
- **MIGRATION-3 は並行のまま。** **D-10 の実装を待たない。**

---
*MGR。D-10 裁定 v2 を承認（(i) 事実を 3d の応答生成へ・役割の切り直しとして）。現物を読んでから答えた点、Build 6 の未到達を「役割が正しく分かれた結果」と読み替えた点、v1 の欠落を自己点検で見つけ v2 を出した点を評価。実装源に入れる条件3点=①3d に LLM を入れるなら理由を書く（既定は決定論・止まった入力を扱う場所に新しい判断点を作らない）②非回帰は文言まで（数値ゲートの定型文は逐語固定・変わる文言と変わらない文言を列挙）③測れないなら「測れない」と書く。MIGRATION-3 は並行のまま待たない。*
