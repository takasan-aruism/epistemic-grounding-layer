# 【D-153】確定案 — **★「引用した」と「使った」は別。★次の1件は planner の入力**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 03:2x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-31_HOLD_THE_DIAGRAM_PROVE_PLAN_USES_OBSERVATION.md`

## 1. ★監査を依頼する（★3点だけ。★増やさない）
```
(a) cites_source_ids の4件が★今回の投入の egl_source_refs と一致するか（★別投入の id ではないか）
(b) ★受入3 の判定＝「tool.py の新規作成は、ss -ltn で既に取れている能力の重複計画である」か
(c) ★受入4 の語の数え方（「観測」「既に」「取得済」「ss -ltn」「プロセス」＝各0）に★打ち切りが無いか
```

## 2. ★私の確定案（★設計の監査後に固める）

| | |
|---|---|
| **Last PASS** | **受入2**（★今回の Observation の ID を、取り違えずに引用した） |
| **First FAIL** | **受入3**（★重複計画。★`ss -ltn` で既に取れている能力を「作れ」と計画した） |
| **結論** | **★「Observation を使って PLAN が立つ」は★まだ証明できていない** |

```
★「引用した」＝ ★受入1・2 は立った
★「使った」  ＝ ★受入3・4 は立たない
★★この2つを1つの言葉にしない。★「概ね引用できた」と書かない。
★★Taka の問いは「使って立つか」である ∴ ★答えは まだ NO である。
```

## 3. ★原因は1点に特定できている（★IMPL の実読）
```
★planner のプロンプトに入るのは ★goal と trace_id だけ（build_planner.py:99-126）
★OBS- の id は ★plan 生成の★後に cites_source_ids へ入る（build_planner.py:203）
∴ ★planner は id を「引用すべきもの」としてすら見ていない。★中身は なおさら見ていない
∴ ★受入3・4 が届かない直接原因は★ここ1点である
```

## 4. ★次の1件を承認する（★正典「一度に一箇所」）

> ### **★`build_planner.py` の `_plan_prompt` に、★観測の中身を渡す。**

| 条件 | |
|---|---|
| **1** | **★既に在る観測を渡すだけ。★新しい台帳・API・ID 族を作らない** |
| **2** | **★GPU 固有・ポート固有の語を入れない**（★`gpu`/`nvidia`/`port` を diff に0件） |
| **3** | **★観測を Task の下流へ移さない**（★線は動かさない。★参照と中身を渡すだけ） |
| **4** | **★後方互換**: `BUILD_CAPABILITY`/`MODIFY_EXISTING` 経路の PLAN を壊さない。★基準値は取る前に固定 |
| **5** | **★受入は Taka の4条件のまま。★1文字も緩めない** |
| **6** | **★同じ依頼文で再実行**（★観測経路は task を経由しないので観測は毎回 走る。★ただし task は同じ id に当たる ∴ ★新しい依頼文が要るなら私が固定する。★設計が決めない） |
| **7** | **★PLAN が出たら止める。★GENERATE へ進まない** |

**★これは「能力追加」ではない**（★既に在る観測を、★既に在るプロンプトに渡すだけ）。**★止まる理由にしない。**

## 5. ★変わらないこと
```
★Ledger は保留のまま（★取り下げではない）
★図の修正は保留のまま（★この証明の後に Taka が決める）
★GENERATE を押さない ／ ★観測を Task の下流へ移さない
★2DER 担当工程数の差分は ★0（★今回も IMPL が書いた）。★0 と書く
```

## 6. ★IMPL の判断を2つ評価する（★根拠つき）
```
★(1) ★「1箇所＝provenance に1行 足す」が実行不能だと分かったとき、★止まらずに★狭い方を採った
      （★同じ分岐の中でブロックを観測の後ろへ移す・★新しい処理を足さない）＝ ★Taka の新しい線どおり。
      ★かつ ★迷いを1行 書いた ＝ ★指示どおりの止まらない進み方である。
★(2) ★P-3（受入3 は立たない見込み）を「いちばん外れてほしい所」と書いて先に出し、
      ★外れなかったと書いた ＝ ★予想を成績にしていない。
```

## 7. 手番
```
★設計: §1 の監査 → 判定案 → §4 の SPEC を IMPL へ
★私  : 確定 → Taka へ6項目
★Taka: ★手番なし（★図の判断は証明の後）
```

---
**決めたこと**: **①「引用した（受入1・2 ○）」と「使った（受入3・4 ×）」を1つの言葉にしない ∴ Taka の問いへの答えは まだ NO ②原因は1点＝planner のプロンプトに goal と trace_id しか入らず、id は plan 生成の後に付く ③次の1件は `_plan_prompt` に観測の中身を渡すこと（能力追加ではない・GPU/ポート固有語を入れない・観測を下流へ移さない） ④Ledger と図の修正は保留のまま、担当工程数の差分は 0。**
