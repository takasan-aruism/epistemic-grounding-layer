# 【BUILD SPEC】`EVO-0051` — **★note に `run=` を1つ。★過去154件には ★届かない（0件）**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-03 02:5x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.6）** ／ **★9項目 確認済（★§6）** ／ **★3値 確認済（★§1）** ／ 親: `ITEM-2DER-EVO-0035`
- **★裁定の在り処**: `ITEM-2DER-EVO-0051` の `status_note`（逐語:「★台帳の note から、その投入の実時刻へ到達できること」「★まず既存の口・既存の欄で到達できないかを先に確かめる」「★読む口と書く口を対で名指しする」）
- **★走行 0・★task 増 0・★commit 0**

---

## 1. ★条件(b)「既存で済むか」を ★先に全部 当たった（★結果: 済まない）

| 既存の手がかり | 使えるか | ★逐語・実測 |
|---|---|---|
| ITEM の **`task_ids`** 欄 | **★使えない** | ★欄は**在る**（record の欄に実在）。★但し **全走査で `task_ids` を持つ記録は ★0件**（history 全体・108/108 ITEM） |
| **`SUBMIT-<key>`** の解決口 | **★鍵を知っていれば使える／★探せない** | `ids.py:28 _resolve_submit_trace` は `runs/<trace_key>.trace.json` を読む＝**実在する**。★但し **key を知る手段が台帳に無い** |
| **`/api/receipt`** | **★使えない** | `webui.py:438` 逐語「**★1件ごとには答えられない: 記録に依頼を特定する id が無い（時刻とパスだけ）**」「**★ここで id を足さない（別 build）**」 |
| **`/api/etrace`** の3口 | **★run_id を知っていれば使える** | `?run_id=` `?event_id=` `?task_id=` のみ。**★run の一覧を返す口が無い** |

```
★★∴ ★既存だけでは ★到達できない。★足りないのは ★★「note と run を結ぶ1つの値」だけである。
★★★★MGR が復元できた1件は ★自分のセッションに `trace_key` を持っていたためで、★台帳からは辿っていない。
```

## 2. やること（★2箇所・★新しい台帳0・新しい欄0・新しい口0）

**★書く口**（`twoder/progress_seal.py:53-56`・**既存関数に引数1つ**）
```python
def note_of(progress, ★run_id=None):
    """set_status の note に載せる1行(新しい欄を作らず、主体を記録に残す)。"""
    return "actor=%s stage=%s via=front_door%s%s" % (
        progress.get("actor"), progress.get("stage"),
        ★(" run=" + run_id) if run_id else "",
        (" note=" + progress["note"]) if progress.get("note") else "")
```
**★呼び手**（`twoder/submit.py:228-229`・**既に手元に在る値を渡すだけ**）
```python
        _wrote = None if _why else _RM.set_status(_prog["item"], _prog["status"], ts=ts,
                                                  note=progress_seal.note_of(_prog, ★run_id=ETRACE.current_run_id()))
```
```
★`ETRACE` は ★`submit.py:99` で ★既に import 済み（★:101 で `open_run` 済 ∴ ★`current_run_id()` は ★この時点で必ず在る）
★★`run=` は ★`next=` と ★同じ流儀（★note という ★既存の1欄の中の ★固定書式）＝ ★★新しい欄ではない
★★★読む口 ＝ ★★既存の `GET /api/etrace?run_id=`（★EVO-0041 で通した口・★何も足さない）
★★戻し方: ★引数1つと ★1式を消す。★可逆
```

## 3. ★★受入（★裁定の逐語「セッションの情報を使わず台帳だけで」）

```
★(1) ★配線後に1件 投入 → ★`GET /api/resolve?id=<その ITEM>` の `status_note` に ★`run=ETR-…` が★在る
★(2) ★★その `run=` だけを頼りに ★`GET /api/etrace?run_id=…` を叩き、★`ENTRY` の `ts` を ★逐語で出す
     ★★★使ってよいのは ★台帳から読めた値だけ（★投入の応答・trace_key・セッションの記憶を ★使わない）
     ★★★★これが「★MGR が1件で騙された形を繰り返さない」の実体
★(3) ★★★母数への効果を ★数で書く: ★過去の ★154件 は ★★0件が到達可能になる（★§4）。
     ★「154件が直る」と ★書かない
★(4) ★既存が壊れない: ★`next=` の抽出（`(?:^|note=)next=…`）が ★従来どおり効く
     ★（★`run=` は ★`actor=`/`stage=` の側に入り ★`note=` より前 ∴ ★`next=` の位置は変わらない——★1件で確かめる）
★(5) ★書いていない側が壊れない（★`?run_id=` `?event_id=` `?task_id=` が従来どおり）
★(6) ★戻せる ／ ★(7) ★Claude が書いた行数（★2DER の実績に数えない）
★★★★★予告を投入前に書く: ★変更行数 ／ ★(3) の数字
```

## 4. ★★過去には届かない（★先に言う・★これが本件の限界）

```
★実測（★全走査・打ち切り無し 108/108 ITEM）:
   ★固定の偽日付のまま ★154件 ／ ★そのうち `task_ids` を持つもの ★★0件
★★台帳は ★追記式 ∴ ★過去の note に ★後から `run=` を書けない（★書けば ★改竄になる）
★★★∴ ★★本件で到達可能になるのは ★★今後 書かれる note だけ。★★過去154件は ★★0件のまま。
★★★★★∴ ★親 `EVO-0035` の受入『★この2日を台帳だけで説明できる』は ★★本件では ★満たされない。
   ★「★この2日」を説明するには ★別の判断が要る（★例: 説明対象を ★今後の分に切り替える／
     ★過去分は ★git log で復元したと ★明記して残す）——★★どちらも ★MGR の裁定。★私は選ばない。
```

## 5. ★増える代わりに廃止（★規律9）

```
★畳めるもの: ★★無い（★本件だけでは ★「git log を漁る運用」は ★畳めない——★§4）
★★★∴ ★本件は「★増やさずに ★今後の分だけ塞ぐ」件である。★畳めると ★書かない
```

## 6. ★9項目（私の分）
```
1 置いたなら読めるか＝★受入(1)(2) が ★まさにそれ（★書いた run= を ★台帳から読んで ★実時刻へ）
2 読めるなら書けるか＝★書く口 `note_of` と ★読む口 `/api/etrace?run_id=` を ★§2 で ★対で名指しした（規律 v1.1）
3 理由を捨てない＝★★§4 で「★過去は0件」を ★数で書いた（★「直る」と ★丸めない）
4 作っていないのでは＝★★`run_id` は ★既に在る（`current_run_id()`）。★無いのは ★note に載せる1式だけ
5 走ったか＝★受入(1)(2) は ★実投入で測る／6 名前＝★`run=`（★`next=` と同じ流儀・★既存 id 名）
7 依頼と試験の矛盾＝★成果物を作らない ∴ 該当なし
8 計器が自分を数えないか＝★★受入(2) は ★台帳から読めた値だけを使う（★自分の応答を証拠にしない）
★9 増える代わりに廃止＝★★§5（★★畳めるものが無い、と ★正直に書く）
```

## 7. 禁止
```
★過去の note に後から `run=` を書く（★改竄）／ ★新しい台帳・新しい欄・新しいエンドポイントを作る
★`/api/receipt` に id を足す（★`webui.py:438` が ★別 build と明記）／ ★`next=` の位置を動かす
★61本を走らせる ／ ★commit する ／ ★`twoder` 配下で python を動かす（★`operator.py` の罠）
```
