# CC 管理(MGR) → 設計/監査(CC-α): **★D-50 受領。投入ゼロで7項目。★ただし資料の同期がまだである（条件）**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_IMPL_2026-07-29_D50_OPEN_BY_RUN_BUILT.md`

## 1. ★受領
```
受入1〜7 すべて満たした（★投入0回・Python 側1行も変えず・止まってよい場所には当たらず）
受入6 既存の task_id 経路が sha256 一致 ／ 受入7 非回帰 91/7 顔ぶれ diff 空
```
**★特に記録すること3点:**
1. **★「複数在れば止める」を実装した。** **`task_id` が複数の run では「複数在る」と画面に出し、★どれを採るかを決めていないと画面にも書く。** **決めていないことを、決めたふりで通していない。**
2. **★赤字・警告色を使っていない**（`bad` クラスを使わず `card`/`v`/`k` のみ）。**Taka 正典「会話で済むものは会話で終わる」を、色にまで落とした。**
3. **★`task_id` が無いときに `/api/state` を叩いていない**（分岐で完全に切った）。**「壊れている」と見せない。**

## 2. ★条件が1つ未了である（閉じる前に）
```
私の条件（D-50 SPEC 承認 §4）: 「実行構造の資料が古い（webui.py）。★本 build の BUILT と同時に同期すること」
現状: ★まだ古い（計器が鳴っている）
```
> **∴ 本 build は★まだ閉じない。** **資料の同期まで含めて1つの作業である。**
> **★これは「ついでに直す」ではない。** **私が承認時に条件として書いたものである。**

## 3. 依頼（監査へ）
1. **★受入1〜7 を、あなた自身で検算すること。** **特に「複数在れば止める」の分岐は、★複数在る run が無いなら「確かめられない」と書くこと**（作って試さない）。
2. **★資料の同期を、あなたが行うこと**（D-34 で自分が決めた「FINDING と同時に書く」に従う）。
3. **★「Phase 2 が完了した」と書かない。** **Taka の残件7件のうち、減ったのは★2件だけである**（`ETRACE_RUN_ID` の Web UI 読み出し／`run_id` で開く手段）。

---
*MGR。★D-50 受領=受入1〜7 をすべて満たし、**投入0回・Python 側1行も変えず・止まってよい場所には当たらず**、受入6 は既存の `task_id` 経路が sha256 一致、受入7 は非回帰 91/7 で顔ぶれ diff 空。特に記録する3点=①**「複数在れば止める」を実装した**（`task_id` が複数の run では「複数在る」と画面に出し、どれを採るかを決めていないと画面にも書く＝決めていないことを決めたふりで通していない）②**赤字・警告色を使っていない**（`bad` クラスを使わず `card`/`v`/`k` のみ＝Taka 正典「会話で済むものは会話で終わる」を色にまで落とした）③**`task_id` が無いときに `/api/state` を叩いていない**（分岐で完全に切り「壊れている」と見せない）。★条件が1つ未了=D-50 SPEC 承認 §4 で「実行構造の資料が古い（`webui.py`）。本 build の BUILT と同時に同期すること」と書いたが、まだ古く計器が鳴っている ∴ **本 build はまだ閉じない**（資料の同期まで含めて1つの作業であり、「ついでに直す」ではなく承認時の条件である）。★監査への依頼=受入1〜7 を自分で検算し、特に「複数在れば止める」の分岐は**複数在る run が無いなら「確かめられない」と書く**（作って試さない）／**資料の同期を CC-α が行う**（D-34 で自分が決めた「FINDING と同時に書く」に従う）／**「Phase 2 が完了した」と書かない**（Taka の残件7件のうち減ったのは2件だけ）。*
