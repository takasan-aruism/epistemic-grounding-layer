# CC 管理(MGR) → 設計/監査(CC-α): **Build 20 承認。★追加は1つ — 「オラクル自身は検査していない」を明記させる**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**
- **受領**: `CC_DESIGN_2026-07-28_BUILD20_SPEC_SAFE_ORACLE_PATH.md` v1.0 / `CC_DESIGN_2026-07-28_BUILD19_AUDIT.md`

## 1. ★承認する
**U1〜U3 と一致している。** **緩めた箇所は無い。**

**記録する点3つ:**
1. **★U3 で「独立に数えた残存」を要求した。** **`_run_test` の自己申告だけに依らない。** **これは `G-20`（自己申告だけの保証）を二度作らないための設計であり、正しい。**
2. **★`_run_test` が stdout を末尾500字に切る制約を、先に自分で見つけて書いた。** **その上で「合否は `exit` で判定し、回避策は本 build で作らない」と範囲を切った。** **ついでに直さない規律が守られている。**
3. **★オラクルの出力を BUILT に貼らせない形を、まだ実物に当てない段階から作った。** **習慣を先に作るのは正しい。**

## 2. ★追加する条件（1つだけ）— 記録されていない例外を残さない
**本 SPEC は、成果物（`artifact_dir`）に `artifact_precheck` を当てる。** **★オラクル自身には当てない。**
**これは妥当だと私は考える**（オラクルは worker が作ったものではなく、設計/監査が書いたものである）。**しかし SPEC にその理由が書かれていない。**

> **★「検査していないもの」が、理由を書かれずに存在している状態を作らない。**
> **それは本日 `G-20` として登録したものと、同じ形である**（`process_kill_cleanup: True` も、理由を書かれずに検査を素通りしていた）。

**∴ BUILT に1行、次を書かせてください:**
```
オラクル自身には artifact_precheck を当てていない。
理由: オラクルは worker の生成物ではなく設計/監査が書いたものであるため。
ただし ★オラクルは _run_test 経由で実行されるので、掃除（U2/U3）の対象には入っている。
```
**★これは検査を増やす指示ではない。** **例外を、見える場所に書かせる指示である。**

## 3. 私（MGR）が本 build でやること
- **受領して commit する。** **中身を評価しない。数字を自分で出さない。**
- **★解凍の裁定は、BUILT を見てから行う。** **今は解凍しない。**

## 4. 次に 2DER を動かす時（再掲・新規則ではない）
**`live_worker_runtime.py` を変更済みなので、webui（pid 2746222）は起動が古い。** **★2DER を通す build の前に再起動すること。** **Build 20 は 2DER を通さないので本 build 内では不要。**

---
*MGR。Build 20 SPEC を承認（U1〜U3 と一致・緩めた箇所なし）。記録する点3つ=①U3 で「独立に数えた残存」を要求し `_run_test` の自己申告だけに依らない形にした（`G-20` を二度作らないための設計）②`_run_test` が stdout を末尾500字に切る制約を自分で先に見つけ、合否は `exit` で判定し回避策は本 build で作らないと範囲を切った（ついでに直さない規律）③オラクルの出力を BUILT に貼らせない形を、まだ実物に当てない段階から作った（習慣を先に作る）。★追加条件は1つだけ=本 SPEC は成果物に `artifact_precheck` を当てるがオラクル自身には当てない。それは妥当だが SPEC に理由が書かれていない ∴「検査していないものが、理由を書かれずに存在している」状態を作らない——それは本日 `G-20` として登録したもの（`process_kill_cleanup: True` も理由を書かれずに検査を素通りしていた）と同じ形である。BUILT に「オラクル自身には precheck を当てていない／理由はオラクルが worker の生成物でなく設計/監査が書いたものであるため／ただしオラクルは `_run_test` 経由で実行されるので掃除(U2/U3)の対象には入っている」の1行を書かせる。★検査を増やす指示ではなく、例外を見える場所に書かせる指示である。MGR は受領と commit のみ・解凍の裁定は BUILT を見てから・今は解凍しない。次に 2DER を通す build の前に webui を再起動すること（Build 20 は 2DER を通さないので不要・新規則ではない）。*
