# 【BUILD SPEC v3】`EVO-0052` — **★試験を1本 足すだけ。★これで通らなければ ★走行を止めて Taka へ上げる**

- `BUILD_ROLE: ★実装源` / **宛: IMPL**（★封入は MGR） / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-03 14:4x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.10）** ／ **★9項目 確認済（★§5）** ／ 親: `ITEM-2DER-EVO-0035`
- **★v1/v2 との関係**: ★どちらも**差し替えない・追記しない**（規律）。**★本書が実装源**。**★1件1本は維持**
- **★走行 0・★task 増 0・★commit 0**

---

## 1. ★MGR の指摘を独立に再現した（★仮説でない）

```
★成果物(TASK-2DER-7EEF8E96・32行)を front door から取り、★私が動かした逐語:
   ALLOWED_NEXT = {"IMPL", "DESIGN", "MGR", "NONE", ★"ETR"}
★5語と列挙外を全部 入れた実測:
   DESIGN→DESIGN ok / IMPL→IMPL ok / MGR→MGR ok / NONE→NONE ok / FOO→None ok
   ★★TAKA→None（★落ちる）  ★★ETR→ETR（★拾ってはいけない値を拾う）
★★∴ MGR の指摘は ★正しい。★私も同じ2件を得た。
```

**★なぜ `ETR` が入ったか（★私の見立て・★未確認）**: 依頼文にも試験にも `ETR` は無い。**★`run=ETR-…` が同じ note に必ず入る**ため、worker がそこから拾った可能性がある。**★worker に聞いていない ∴ 断定しない。**

## 2. ★足す封印試験（★1本だけ。★既存9本は1文字も変えない）

```python
def test_all_five_next_values_are_accepted():
    """★列挙した5語は ★全部 通ること（★v1.10: 列挙は『全部通る』と『列挙外は弾く』を対で置く）。
    ★fixture は台帳の実物の形（先頭 actor=・途中に run=ETR-… が在る）を使う。"""
    for w in ("DESIGN", "IMPL", "MGR", "TAKA", "NONE"):
        note = ("actor=Claude stage=VERIFY via=front_door run=ETR-445f404ede06 "
                "note=next=%s 本文" % w)
        v = impl.effective_state([{"status": "IN_PROGRESS", "status_note": note}])
        assert v["next"] == w, (w, v)
```
```
★★これで ★10本になる（★9 + 1）。★既存9本は ★そのまま封入する（★緩めない・並べ替えない）
★★★`run=ETR-…` を fixture に ★わざと入れた ―― ★`ETR` を拾う実装は ★この1本で落ちる
```

## 3. ★依頼文・骨格

```
★1文字も変えない（★v2 のまま）。★変えるのは ★試験だけ。
★★理由: ★依頼文には ★既に「DESIGN / IMPL / MGR / TAKA / NONE のいずれか」と ★5語 書いてある
   ∴ ★依頼文は ★正しく、★縛っていなかったのは ★試験である（★3回とも同じ形）
```

## 4. 受入

```
★(1) ★同じ依頼文＋10本の試験で ★1回 走らせる（★worker が書く・Claude は本文を書かない）
★(2) ★10本 全通
★(3) ★★5語すべてを ★front door 経由の実測で確かめる（★TAKA を含む）／★`ETR` と `FOO` は `None`
★(4) ★実データで ★`ITEM-2DER-EVO-0052` の history を入れ、★私の監視の正規表現と ★値も添字も一致
★(5) ★sha256 一致 ／ ★(6) ★行数を分ける（worker N ／ Claude M）／ ★(7) ★戻せる ／ ★(8) ★61本を走らせない
★★★★(9) ★★★これで通らなければ ★★走行を止めて Taka へ上げる（★裁定の逐語）。
     ★その時に書くこと=★3回の穴の一覧（①現実でない fixture ②否定側だけ ③列挙の欠落）と
     ★★「試験を足す」以外の方法が要る、という判断材料。★私が方法を選ばない
★★★★★予告を投入前に書く: ★worker の行数の見込み ／ ★(3) で落ちると思う語
```

## 5. ★9項目（私の分）
```
1 置いたなら読めるか＝★受入(3)(4) は ★front door から測る
2 読めるなら書けるか＝★書く側は ★本件でも触らない（★v1 §6 のまま）
3 理由を捨てない＝★★§1 に ★`ETR` の出所の見立てを ★【未確認】と明記して残した（★断定しない）
4 作っていないのでは＝★成果物は在る。★無いのは ★試験1本
5 走ったか＝★受入(3) は ★実行後の成果物で測る／6 名前＝★不変
★7 依頼と試験の矛盾＝★★3回目。★§3 のとおり ★依頼文は正しく ★試験が縛っていなかった
8 計器が自分を数えないか＝★受入(4) は ★私の監視の正規表現と ★独立2経路で突き合わせる
★9 増える代わりに廃止＝★★「列挙を書いたら通る例を1つだけ試す」やり方を畳む（★v1.10 が規律側で塞いだ）
```

## 6. ★★私の側の記録（★3回とも私の契約が原因）

```
★1度目 v1: ★fixture が ★現実の note 形式でなかった（★`note=` 始まりの作文）
★2度目 v2: ★否定側（FOO を弾く）だけ置き、★★肯定側（5語すべて通る）を ★1本も置かなかった
★3度目 本件: ★その結果 ★`TAKA` の欠落と ★`ETR` の混入が ★素通りした
★★★共通=★★「★列挙を書いたのに ★列挙を試験していない」。★★v1.10 で規律側は塞がれた
★★★★★★★★但し ★これで3回目 ∴ ★★受入(9) のとおり ★次に外したら ★方法を疑う（★私も含めて）
```

## 7. 禁止
```
★成果物を Claude が書く・直す ／ ★既存9本の試験を変える・消す ／ ★依頼文・骨格を触る
★`ETR` を許可値に加える（★依頼文に無い）／ ★`set_status` を触る（★範囲外）
★v1/v2 の .md を差し替える・追記する ／ ★新しい台帳・エンドポイントを作る
★61本を走らせる ／ ★commit する ／ ★`twoder` 配下で python を動かす
```
