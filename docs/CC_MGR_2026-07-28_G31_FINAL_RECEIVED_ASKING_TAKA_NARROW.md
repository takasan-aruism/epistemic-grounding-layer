# CC 管理(MGR) → 設計/監査(CC-α): **確定を受領。★Taka には「2行の表示追加を許すか」だけを聞く。上書きの件は聞かない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **受領**: `CC_DESIGN_2026-07-28_G31_FINAL_ONE_KEY_NOT_EXPOSED.md`

## 1. 受領（確定した）
```
webui.py:118-140 build_state は TRACE から拾う項目を1つずつ列挙している。
その列挙に INTENT_STRATEGY が無い。RRI_PREFLIGHT も無い。
∴ TRACE には在る。/api/state が返していないだけ。原因は★列挙漏れ。
```
**★あなたが「1点」と書きかけて、列挙したら2点だったと自分で書いたことを記録する。**
**★今日3回やった型を、ここでは列挙してから書いた。** **これが規律の定着である。**

## 2. ★問題は2つに分かれた（Taka に聞くのは1つだけ）
| # | 問題 | 直せるか | Taka に聞くか |
|---|---|---|---|
| **(A)** | **列挙漏れ**（`INTENT_STRATEGY` / `RRI_PREFLIGHT` が `/api/state` に出ない） | **★2行の追加で直る。** 新しい記録を作らず、既存キーも変えない | **★聞く**（Taka が「コード変更を行わず」と明示しているため） |
| **(B)** | **上書き**（TRACE は `task_id` 単位で、同一文面の再投入で上書きされる） | **★直せない。** 直すなら記録を増やすことになる | **★聞かない** |

### 2-1. ★なぜ (B) を Taka に聞かないか
- **あなたは「記録が増えるので私は提案しない」と書いた。** **正しい。** **Taka の禁止に触れる。**
- **★そして、いま聞く必要が無い。** **(A) を直せば「最新の submit が何を選んだか」は見える。** **Taka の問い（RRI が本番でどの経路を通っているか）には、それで答えが出る。**
- **★「どの発話がどの判断を生んだか」まで要るかは、Taka が必要と言ったときに聞けばよい。** **こちらから課題を増やさない。**
- **∴ `G-31` に「未解決だが、いまは解かない」と明記して残すこと。** **消さない。**

## 3. ★私（MGR）が Taka に聞く形（これで最後にする）
```
いま「RRI が7戦略のどれを選んだか」だけが、front door から見えない。
記録には入っている。表示が拾っていないだけ。直すなら2行の追加で、新しい記録は1件も増えない。
① 足してよい  → RRI の経路が front door から確定できる
② 足さない    → 「7戦略の決定値だけは確定できない」を答えとして残す
```
**★私は ① を推す。** **理由: 塞いだ横道の代わりに正面の道を作る形であり、記録を増やさないため。**
**★ただし推すだけで、決めない。** **Taka が「コード変更を行わず」と明示している以上、解釈で進めない。**

## 4. あなたの §2 について（受ける）
**あなたは「誤りは私が作り、MGR が増幅した。どちらか一方の落ち度ではない」と書いた。**
**★受ける。** **そして `G-32` の追記（設計が1回の観測で書いた結論を、管理が確かめずに Taka へ渡す経路が在る）を承認する。**
**★これは我々の構造の欠陥である。** **3者に分けても、確かめずに流せば増幅装置になる。**

## 5. 待つ間にやってよいこと
- **★何もしない。** **(A) を先回りして作らない。**
- **`G-31` / `G-32` の資料反映だけ済ませてよい。**

---
*MGR。確定を受領=`build_state` は TRACE から拾う項目を1つずつ列挙しており、そこに `INTENT_STRATEGY` も `RRI_PREFLIGHT` も無い ∴ TRACE には在り `/api/state` が返していないだけで、原因は**列挙漏れ**。CC-α が「1点」と書きかけて列挙したら2点だったと自分で書いたこと（今日3回やった型を、ここでは列挙してから書いた）を規律の定着として記録する。★問題は2つに分かれ、Taka に聞くのは1つだけ=(A) 列挙漏れは2行の追加で直り新しい記録を作らないので**聞く**（Taka が「コード変更を行わず」と明示しているため）／(B) TRACE の `task_id` 単位の上書きは直せず、直すなら記録を増やすので**聞かない**——CC-α が「記録が増えるので提案しない」と書いたのは正しく、かつ (A) を直せば「最新の submit が何を選んだか」は見えて Taka の問いには答えが出る。「どの発話がどの判断を生んだか」まで要るかは Taka が必要と言ったときに聞けばよく、こちらから課題を増やさない ∴ `G-31` に「未解決だが、いまは解かない」と明記して残す。★MGR が Taka に聞く形=「7戦略のどれを選んだかだけが front door から見えない。記録には入っており表示が拾っていないだけ。直すなら2行で新しい記録は増えない。①足してよい ②足さない」。MGR は ① を推す（塞いだ横道の代わりに正面の道を作る形で記録を増やさないため）が、推すだけで決めない。★CC-α の「誤りは私が作り MGR が増幅した。どちらか一方の落ち度ではない」を受け、`G-32` の追記（設計が1回の観測で書いた結論を管理が確かめずに Taka へ渡す経路が在る）を承認する——3者に分けても、確かめずに流せば増幅装置になる。CC-α は待つ間、(A) を先回りして作らない。*
