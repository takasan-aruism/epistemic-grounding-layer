# 【D-146】確定 — **★`Runtime` は PASS にしない。★同じ id で束ねただけで、流れていない**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 01:4x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-31_RUNTIME_INSPECTION_COMMON_BASE_ORDER.md`

## 1. ★採る判定（★設計案から1点だけ変える）

| 配線 | 設計案 | **★確定** | 理由 |
|---|---|---|---|
| Request | PASS | **PASS** | `task_id` → `goal`／`ENTRY.ts`（receipt と 300µs 差） |
| RRI | PASS | **PASS** | — |
| Task | PASS | **PASS** | 作られ、引ける |
| **Runtime** | **PASS（但し書き）** | **★PASS にしない** | **§2** |
| Observation | PASS | — | ★Runtime の下流 ∴ ここまで来ない |
| Ledger / DW / Response | FAIL | **FAIL** | — |

## 2. ★理由（★設計自身が書いた事実を採る）
> **設計の但し書き（逐語）**: 「**★但し『task が起動したから走った』ではない**（★観測は submit の中で走り、★記録が `task_id` で束ねられている）。**★因果ではなく同居である**」

```
★正典が評価すると言っているのは★配線である。「★以下が★一本の流れとして最後まで接続されること」
★流れは Task ↓ Runtime である。
★事実: 観測は第1試行・第2試行（★task が無かった回）でも同じように走っていた。
∴ ★観測は task の下流ではない。★task が無くても走る。
∴ ★Task → Runtime の矢印は、★まだ示されていない。
★「同じ id で束ねられている」は接続ではない。★これを PASS と書けば「概ね接続」になる。
```
**★設計がこの事実を自分で書いたことを評価する。★書かなければ PASS で通っていた。**

## 3. ★次にやることは「修正」ではない（★これが今回いちばん重要な裁定）

> ### **★`run_next` を1回 押して、Task → Runtime が流れるか見る。★コードを1行も変えない。**

```
★理由: ★task は CREATED（PLAN 待ち）のまま止まっている。★我々が押していないからである。
★∴ ★「流れない」と確かめたわけではない。★「流していない」だけである。
★1回の観測で状態を断定しない（運用方針 §4-13）。★押せば分かることを、推測で書かない。
★これは修正ではない ∴ 正典「一度に一箇所だけ修正」に触れない。
★コード変更0行 ∴「設計変更は禁止」「能力追加は禁止」にも触れない。
```

### 3-1. ★条件（★増やさない）
```
★同じ task（TASK-2DER-0C458F38）に対して ★run_next を押す。★再投入しない
★押した回数と各回の返り値を全部 書く（★応答を捨てない）
★★止まったらそこで終了。★直さない・迂回しない
★PLAN が動いたら、★そこで一度 止めて私に返す（★GENERATE へ進まない・本番投入は Taka の案件）
★GPU 取得・生出力・A-3 は★触らない
```

## 4. ★`Ledger`（生出力）は次ではない（★順番を私が決める）
```
★設計は「First FAIL = Ledger ∴ 次は Ledger」と案を出した。★筋は通っている。
★しかし §2 で First FAIL は ★Runtime へ前倒しになった ∴ ★Ledger はさらに後ろである。
★取り下げではない。★順番である。
```

## 5. ★A-3（新形式 `trace_key` が解けない）
```
★設計の案（webui.py 側で prefix を常に SUBMIT- にする）を★私も推す。
★理由も採る: ids.py 側で扱うと ★TASK- が「DW task」と「submit trace」の2つを指す
             ＝ ★本日13回 潰してきた形の再生産になる。
★★ただし今は やらない。★§3 の確認が先である。
```

## 6. ★設計の自己申告（★評価する）
```
★「webui.py:661 を自分で読んでいながら、task が出来ると prefix が外れることを予見しなかった。
   ★D-140 の前提を、私の SPEC が外した」
★★設計ミスを自分から出した。★IMPL の判断を「正しい」と書き、★自分の指示を「足りなかった」と書いた。
★重複登記も避けた（★選別の件は既存 G-06 が在ると走査して確認し、新規登記しなかった）。
```

## 7. 手番
```
★設計: §3 を SPEC にして IMPL へ（★1回押す・止まったら終了）
★私  : 結果を確定 → Taka へ★5行で報告
★Taka: 手番なし
```

---
**決めたこと**: **①`Runtime` を PASS にしない——観測は task が無かった回でも同じように走っており、task の下流ではない。同じ id で束ねられているのは接続ではない ②次にやるのは修正ではなく `run_next` を1回 押す確認（コード0行・「流れない」ではなく「流していない」だけだから） ③`Ledger` は順番が後ろへ、A-3 は `webui.py` 側で直す案を推すが今はやらない。**
