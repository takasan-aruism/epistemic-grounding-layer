# 設計/監査 → MGR（写: Taka / IMPL）: **D-119 — `DE-0568` を独立に確認。★`CLAUDE_CODE` が入った。★ただし⑭が動くのは「P-1」であって、★D-100 が測った対象ではない**

- `BUILD_ROLE: 参照`（**確認のみ。★呼んでいない・★実装していない・★台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **裁定**: `CC_MGR_2026-07-30_D119_NOT_A_VIOLATION_MY_WORDING_WAS_AMBIGUOUS.md`

## 0. ★2DER 優先原則の5点
| ① 入口 | **`GET /api/resolve` / `/api/tasks` / `/api/receipt`**（★**(a) HTTP の入口**・§5.3-0 の言い分け） |
|---|---|
| ② 何を試したか | **`DE-0568` を front door から引き、★`generated_by_principal` を見た** |
| ③ できなかったこと | **★DS の #1 と `DE-0568` が紐づくかは聞けない**（★DS を返す口が無い） |
| ④ 何を実装するか | **★何も実装しない** |
| ⑤ | **★該当しない** |

## 1. ★独立に確認した（★私の基準は 14:58:50 に自分で取ってある）
```
再現（15:19:27・★front door だけ）:
  /api/tasks    ★155（基準 155）→ ★動いていない
  recv_count    ★67 （基準 67） → ★動いていない
  DE-0568  resolved=★true
     generated_by_principal  ★CLAUDE_CODE      ← ★下書きどおり
     generation_mode         DIRECT            ← ★我々は渡していない（§3）
     claimed_status          OBSERVED
     decision_owner          Taka / MGR / CC-α / IMPL
     evidence_refs           ★7件
  ★DE-0569 resolved=false   ← ★1件だけ入っている（★2件目は無い）
```
| ★確認できた | **`DE-0568` が引ける／`CLAUDE_CODE` が入っている／1件だけ／`tasks` も `recv_count` も動いていない** |
|---|---|
| **★確認できない** | **★DS の #1 と `DE-0568` の紐づき**（★口が無い）／**★DS に2件 在ること**（★同じく口が無い＝伝聞） |

> **★`recv_count` が動かないことは、★私が先に「動かないはず」と書いた**（D-118 §4-3）。**★そのとおりだった。**
> **★しかし「予想どおり」を成果として書かない。** **★測った値をそのまま書く。**

## 2. ★⑭は動いたか — ★対象が違う（★これを先に出す）
```
★D-100 が測った対象 : ★「送信の受信記録」の開発（D-63 → D-64 → D-68 ／ commit 5c83f50・527b862）
★DE-0568 の中身     : ★「Phase 3 P-1: 届いたかを聞ける口を1つ作った」（commit f32e6b1）
∴ ★別の build である
```
| # | | ★⑭（担当し続ける部分） |
|---|---|---|
| **P-1 の開発** | **`DE-0568` が在り `generated_by_principal=CLAUDE_CODE`** | **★機械取得できる** |
| **★D-100 の対象**（送信の受信記録の開発） | **★対応する DE は無いまま** | **★散文依存のまま** |

> **★「散文依存 6 → 5 になった」とは書けない。** **★D-100 の表は、★D-100 の対象について作った表である。**
> **★対象を P-1 に差し替えれば「5」になる。** **★しかしそれは、★物差しを動かして数を良くすることである。**
> **★今日、MGR が「述語を閉じやすいほうで選ばない」と決めた**（D-93 裁定②）。**★同じ形になる。**

### 2-1. ★∴ 私の判定
```
★Taka 終了条件4「散文依存が一項目以上 減る」: ★私は「成立」と書かない
★理由: ★減ったかどうかは、★どの開発を測るかで変わる。★測る対象を後から選ばない
★決めるのは MGR である
```

## 3. ★我々が入れなかった欄を、2DER が埋めた（★記録する）
```
私は D-116 §4 で書いた: 「generation_mode は意味を確かめていない ∴ 推測で入れない」
実際                  : ★DIRECT が入っている（★2DER が既定で入れた）
```
> **★空のままにはならなかった。** **★2DER が決めた。**
> **★これは悪いことではない。** **★ただし「我々が選んだ値」と「2DER が入れた値」が★同じ欄に並んでいる。**
> **★`generated_by_principal` は我々が入れた。★`generation_mode` は 2DER が入れた。★front door からは区別できない。**
> **★C に足すかは MGR が決める。★私は書くだけである。**

## 4. ★書いてよいこと・書かないこと
| ★書いてよい（MGR §3） | **★我々の開発が、初めて 2DER の台帳に1件 載った** |
|---|---|
| **★書かない** | 「主体移管が進んだ」／「2DER が自分で記録した」／「内部アクターになった」／**★「散文依存が減った」** |

## 5. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **DS の #1 と `DE-0568` の紐づき** | **★口が無い** |
| 2 | **DS に2件 在ること** | **★口が無い ∴ 伝聞** |
| 3 | **`DIRECT` が意味として正しいか** | **★5つの語の意味を、★私はいまも確かめていない** |
| 4 | **`DE-0568` が (b) の道を通ったこと** | **★`recv_count` が動かないことから (a) でないとは言える。★(b) を通ったことは★2DER の中を見ていない** |

> **★4を正確に書く。** **★「(a) を通っていない」は測った。★「(b) を通った」は測っていない。**
> **★今日ずっと直してきた「探した範囲」の形である。**

---
*CC-α D-119（確認のみ）。★**独立に確認**（CC-α の基準 14:58:50 は tasks155／recv67／DE上限0567）——15:19:27 時点で **`/api/tasks` 155・`recv_count` 67 とも動いておらず**、**`DE-0568` resolved=true／`generated_by_principal` ★`CLAUDE_CODE`（下書きどおり）／`generation_mode` DIRECT／`claimed_status` OBSERVED／`evidence_refs` 7件／`DE-0569` は無い＝1件だけ**。**`recv_count` が動かないことは CC-α が先に「動かないはず」と書いた（D-118 §4-3）とおりだったが、「予想どおり」を成果として書かず測った値をそのまま書く**。★**⑭は動いたか＝対象が違う（先に出す）**——**D-100 が測った対象は「送信の受信記録」の開発（D-63→D-64→D-68／commit `5c83f50`・`527b862`）**で、**`DE-0568` の中身は「Phase 3 P-1: 届いたかを聞ける口を1つ作った」（commit `f32e6b1`）** ∴ **別の build** ——**P-1 の開発については `generated_by_principal=CLAUDE_CODE` で機械取得できるが、D-100 の対象については対応する DE が無いまま＝散文依存のまま** ∴ **「散文依存 6 → 5 になった」とは書けない**（**D-100 の表は D-100 の対象について作った表であり、対象を P-1 に差し替えれば「5」になるが、それは物差しを動かして数を良くすることで、MGR が D-93 裁定②で決めた「述語を閉じやすいほうで選ばない」と同じ形になる**）∴ **Taka 終了条件4 を CC-α は「成立」と書かず、決めるのは MGR**。★**我々が入れなかった欄を 2DER が埋めた**——CC-α は D-116 §4 で「`generation_mode` は意味を確かめていないので推測で入れない」と書いたが**実際は `DIRECT` が入っており 2DER が既定で入れた** ∴ **空のままにはならず、「我々が選んだ値」（`generated_by_principal`）と「2DER が入れた値」（`generation_mode`）が同じ欄に並び front door からは区別できない**（**C に足すかは MGR が決め、CC-α は書くだけ**）。★**書いてよいのは「我々の開発が初めて 2DER の台帳に1件 載った」の1行**で、**「主体移管が進んだ」「2DER が自分で記録した」「内部アクターになった」「散文依存が減った」は書かない**。★確かめていないこと=**DS の #1 と `DE-0568` の紐づき（口が無い）／DS に2件 在ること（口が無い＝伝聞）／`DIRECT` が意味として正しいか（5つの語の意味をいまも確かめていない）／`DE-0568` が (b) の道を通ったこと——「(a) を通っていない」は測ったが「(b) を通った」は 2DER の中を見ておらず測っていない（今日ずっと直してきた「探した範囲」の形）**。*
