# 【D-207】`EVO-0030` を登記 ― ★上級監査の結果に★行き先を与える（★worker に書かせる。★私は書かない）

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-08-01 12:3x / TYPE=STATUS
- **開発者規律 確認済（版: v1.1 / 2026-08-01）** ／ **受領**: 設計/監査の指摘（★「直すのは小さいが、マネージャの仕事ではない」）

## 1. ★指摘を採る（★私の線引きを確認してもらった形）
```
★設計: 「★UPPER_REVIEW に ★AUDIT と同じ種類の分岐を ★4本 足すだけ。★数行。
        ★★ただし DW の本番コードなので ★worker に書かせるべきもの。
        ★★★マネージャが自分で書けば『★Claude は 2DER の外の市民』が崩れる」
★★採る。★私は ★上級監査（★判定）までを代行し、★★配線（★実装）は ★代行しない。★この線は動かさない。
```

## 2. ★登記（★`ITEM-2DER-EVO-0030`・★PROPOSED）
```
★title: ★上級監査の結果に行き先を与える（★読める＋効く）
★実測（★note に入れた）:
   ★① `FAIL` を ingest しても ★`dw_state` は ★`READY_FOR_UPPER_REVIEW` のまま ★動かない
   ★② `review` 本文は ★`GET /api/state` の ★全19欄に ★出ない（★打ち切り無しで走査）
★★★＝ ★書ける・記録される・★しかし ★読めず ★効かない。★同型6回目の ★3つ目の変種。
```

## 3. ★設計へ（★BUILD SPEC に落とすこと）
```
★① ★分岐は ★`AUDIT` と★同型で書く。★内訳（★何本・どの条件）は ★設計が決める。★私は指定しない。
   ★★設計が示した例: ★`PASS` かつ試験も通過 → ★完了へ ／ ★`FAIL` → ★作り直しへ（★理由つき）
      ／ ★判断できない → ★人へ上げる
★② ★★規律 v1.1 を当てる: ★書く口を作るなら ★読む口を★対で名指しすること。
   ★★★＝ ★`review` の本文が ★front door から ★読めること。★「効く」だけでは受けない。
★③ ★★実装は ★worker に書かせる。★Claude が書いたら ★「Claude が書いた」と★行数つきで書き、
   ★★2DER の実績に ★数えない。★（★C-1 のように ★worker に作らせられない配線なら ★例外理由を先に書く）
★④ ★可逆にする。★戻し方を ★同じ BUILT に書く。★新しい状態語・台帳・計器を ★作らない。
★★★★★受入（★これを満たさなければ通ったと言わない）:
   ★(a) ★`FAIL` を入れたら ★state が ★作り直し側へ ★動く
   ★(b) ★`PASS`＋試験通過なら ★完了側へ ★動く
   ★(c) ★入れた `review` の本文が ★front door から ★読める
   ★(d) ★判断できない場合の ★人へ上げる先が ★決まっている（★宙に浮かせない）
```

## 4. ★私が代行した上級監査の扱い（★取り消さない・★独立性は落ちている）
```
★判定 `FAIL` は ★記録された（★`dev-workcell/events.jsonl` に ★1件 追記）。
★★`single_party: true` と ★「独立レビューを経ていない」を ★結果の中に書いてある。
★★★∴ ★設計/監査が動いたら ★再検証すること。★一致したら ★一致したと書く。★違えば ★私の誤りである。
```

## 5. ★1つ学んだこと（★自分の言い方を訂正する）
```
★私は「★進捗を書いても依頼は増えない」と ★Taka に報告した。★★正確ではない。
★★実測: ★本件の登記は ★task が ★立った（`TASK-2DER-622C8AB9`）。★理由＝★マーカーの外に ★本文を書いたから。
★★★`EVO-0028` の設計どおりである（★本文が在れば依頼でもあるので ★安全側に倒す）。★欠陥ではない。
★★★★∴ 正しい言い方: ★★「★マーカーだけの投入なら ★増えない」。★以後 そう書く。
```

---
**決めたこと**: **①設計の指摘を採る。私は上級監査（判定）までを代行し、配線（実装）は代行しない。この線は動かさない ②`ITEM-2DER-EVO-0030`「上級監査の結果に行き先を与える（読める＋効く）」を登記した。実測は、FAIL を ingest しても `dw_state` が動かず、`review` 本文は `/api/state` の全19欄に出ないこと——書ける・記録される・しかし読めず効かない、同型6回目の3つ目の変種 ③設計へ: 分岐は AUDIT と同型で、本数と条件は設計が決める。規律 v1.1 を当てて読む口を対で名指しする。実装は worker に書かせ、Claude が書いたら行数つきで書いて 2DER の実績に数えない。可逆にし、戻し方を BUILT に書く ④受入は4つ——FAIL で作り直し側へ動く／PASS＋試験通過で完了側へ動く／review 本文が front door から読める／判断できない場合の上げ先が決まっている ⑤私が代行した FAIL 判定は記録済で `single_party: true` と明記してある。設計が戻ったら再検証し、違えば私の誤りである ⑥私の「進捗を書いても依頼は増えない」は不正確だった。マーカーの外に本文を書けば依頼は立つ（EVO-0028 の設計どおり・安全側）。正しくは「マーカーだけの投入なら増えない」。**
