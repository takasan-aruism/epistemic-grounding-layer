# CC 管理(MGR) → 設計/監査(CC-α): **★D-50 — 「案件（依頼）を鍵にして開く」の SPEC。★新しい口を作らずにできるかを先に確かめる**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **正典**: `PHASE2_DIRECTION_MANAGEMENT_UI_v1_0.md`（Taka 逐語）

## 1. ★なぜこれが次か（Taka の完了像の①に直接効く）
```
Taka の完了像①: 「案件を開く」
今の UI       : ★task を開く（GET /api/state?task_id=… / DW task が無ければ error）
今日の実例     : CLI 投入の依頼は DW task が作られず ★UI で開けなかった
```
> **∴「1件の依頼」と「1件の DW task」は同じではない。**
> **★そして Taka の正典は「会話で済むものなら会話で終わる」と書いている。**
> **∴ 正しく振る舞った依頼ほど、いまの UI では開けない。** **これが①の欠落である。**

**★既に登記済の「足りないもの」1・2 がこれである。** **新しい課題ではない。**

## 2. ★最初にやること（作る前に確かめる。10回目を作らない）
**私の見立て**（★合わせないこと・実物で確かめること）:
```
/api/resolve?id=ETR-… は★既に動く（今日 実測済）。
∴ /command が ETR- を受け付ければ、★新しい endpoint 無しで依頼を開ける可能性が在る。
```
| # | 確かめること |
|---|---|
| **1** | **`/api/resolve?id=ETR-…` だけで、完了像の何項目が埋まるか。** **★埋まらない項目を名指しする** |
| **2** | **`task_id` が在る依頼では、いまの画面と★どう繋げるか**（run から task へ辿れるか） |
| **3** | **新しい endpoint が要るかどうか。** **要るなら「要る。理由は◯◯」と書いて★止める**（作らない） |

## 3. ★条件
1. **★新しい endpoint を作らない**（要ると分かったら止めて上げる）。
2. **★既存の画面を壊さない。** **`task_id` で開く形を消さない。** **足すだけ。**
3. **★Event Trace をこれ以上精密にしない**（Taka 明示）。**出す作業である。**
4. **★実装案を SPEC の外に広げない。** **「ついでに `/` も直す」をしない。**
5. **★投入は原則しない。** **今日の `ETR-0f6fae35665a` と `ETR-88bc41400d19` が既に在る。** **要るなら理由つきで1回だけ申請する。**

## 4. ★併せて片付けること（小さい2件）
| # | 何を |
|---|---|
| **1** | **実行構造の資料が古い**（`webui.py` が変わった）。**★次の BUILT と同時に同期する** |
| **2** | **到達性21件の独立判断が未着のままである。** **★やるなら今出す。やらないなら「やらなかった」と書いて閉じる。** **宙に浮かせない** |

## 5. ★私が決めたこと（Taka は「継続してください」とだけ言った）
**★中心は動かさない。** **「Event Trace を管理UIへ作用させる」のままである。**
**★本件はその中心の中に在る**（Event Trace の `run_id` を UI の鍵にする話であるため）。
**★これは横道ではない。** **もし横に見えるなら、私に言うこと。**

---
*MGR。D-50。★なぜ次かは Taka の完了像①「案件を開く」に直接効くため——今の UI は task を開く形（`task_id` が無ければ error）で、今日 CLI 投入の依頼は DW task が作られず UI で開けなかった ∴「1件の依頼」と「1件の DW task」は同じではなく、**Taka の正典が「会話で済むものなら会話で終わる」と書いている以上、正しく振る舞った依頼ほど今の UI では開けない**——これが①の欠落であり、既に登記済の「足りないもの」1・2 と同じもので新しい課題ではない。★最初にやることは作る前の確認（10回目を作らない）=`/api/resolve?id=ETR-…` は既に動くので `/command` が `ETR-` を受け付ければ新しい endpoint 無しで依頼を開ける可能性が在る（**MGR の見立てであり合わせず実物で確かめる**）——①`/api/resolve` だけで完了像の何項目が埋まるか（埋まらない項目を名指し）②`task_id` が在る依頼で今の画面とどう繋げるか ③新しい endpoint が要るなら「要る。理由は◯◯」と書いて**止める**。★条件=新しい endpoint を作らない／既存の画面を壊さず `task_id` で開く形を消さず足すだけ／Event Trace をこれ以上精密にしない／実装案を SPEC の外に広げない（「ついでに `/` も直す」をしない）／**投入は原則しない**（今日の `ETR-0f6fae35665a` と `ETR-88bc41400d19` が在る。要るなら理由つきで1回だけ申請）。★併せて片付ける小さい2件=実行構造の資料が古いので次の BUILT と同時に同期／**到達性21件の独立判断が未着なので、やるなら今出し、やらないなら「やらなかった」と書いて閉じる（宙に浮かせない）**。★MGR が決めたこと=Taka は「継続してください」とだけ言ったが**中心は動かさない**（「Event Trace を管理UIへ作用させる」のまま）。本件は Event Trace の `run_id` を UI の鍵にする話なので中心の中に在り横道ではない——**横に見えるなら私に言うこと**。*
