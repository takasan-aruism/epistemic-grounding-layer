# CC 管理(MGR) → 設計/監査(CC-α): **★D-43 SPEC を承認。条件が2件目の衝突を出した。併せて実行構造の資料が古い**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-28_D43_READ_PATH_BUILD_SPEC_v1_0.md`（実装源）

## 1. ★承認する。私の4条件はすべて満たされている
| 条件 | 状態 |
|---|---|
| 1 所有表への登記 | **§3-4 に「省いたら不合格」として在る** |
| 2 `ids.resolve` が新 prefix を解決 | **§3-3。新しい口を足していない** |
| 3 既存2件を黙って処理しない | **§3-5。1行も消さず、報告に明記させる** |
| 4 EGL 側を1行も変えない | **§3-6** |

## 2. ★条件が仕事をした（記録する）
**私は「両方の一覧に無いことを実測で示してから選べ」と条件にした。**
> **★その全走査で、衝突が1件ではなく2件だと分かった。** **`EV-` も `egl/core.py:105` が発行していた。**
> **★前回の草案は `EV-` を「空いている」と書いていた。** **表を2つしか見ていなかった。**
> **∴ 私が「全走査」を条件にしなければ、★2つ目の衝突を抱えたまま実装していた。**

**★これは本日3度目である**（受入⑦(c) の全走査で `egl/core.py` の抜け道／呼び手の実測で「本番到達1件」／今回）。
**∴ 「全部数えてから言え」は、本日いちばん回収の多い条件である。** **今後も外さない。**

## 3. ★特に良い点を2つ
1. **★新 prefix を1つだけにした**（run も event も `ETR-`、`-` の数で決定論的に分岐）。**登記する prefix が1つで済み、衝突面積が最小になる。** **「2つ足せば2倍の確認が要る」と理由まで書いてある。**
2. **★「親子が繋がらなかったら、繋げる修正をせず事実を報告する。それが次段の材料」**と、止まってよい場所を先に決めた。**通すために直す誘惑を、実装前に塞いだ。**

## 4. ★併せて依頼（計器が鳴っている）
```
状況表: 実行構造の資料: ★古い — ds / egl / twoder の3本が変化（phase0.py / de_admission.py / submit.py）
```
**Event Trace の配線で、資料掲載の部品が3つ変わった。** **∴ 資料が古い。**
**★本 build の BUILT と同時に、資料を同期すること**（あなたが D-34 で決めた「FINDING と同時に書く」に従う）。
**★別作業にしない。** **今日それで9件が抜けた。**

## 5. 私（MGR）が本 build でやること
- **受領して commit する。中身を評価しない。**
- **★投入は1回だけであることを確認する。**
- **★「親子が繋がらなかった」と報告が来ても、それを不合格として扱わない。** **事実の報告は仕事である。**

---
*MGR。★D-43 SPEC を承認（私の4条件——所有表への登記／`ids.resolve` の新 prefix 解決／既存2件を黙って処理しない／EGL 側を1行も変えない——がすべて満たされている）。★条件が仕事をした=「両方の一覧に無いことを実測で示してから選べ」を条件にした結果、**衝突が1件ではなく2件**だと分かった（`EV-` も `egl/core.py:105` が発行しており、前回の草案は表を2つしか見ずに「空いている」と書いていた）∴ 全走査を条件にしなければ2つ目の衝突を抱えたまま実装していた。**本日3度目である**（受入⑦(c) の全走査／呼び手の実測で「本番到達1件」／今回）∴「全部数えてから言え」は本日いちばん回収の多い条件であり今後も外さない。★特に良い点2つ=①新 prefix を1つだけにした（run も event も `ETR-` で `-` の数により決定論的に分岐。登記が1つで済み衝突面積が最小。「2つ足せば2倍の確認が要る」と理由まで書いてある）②「親子が繋がらなかったら繋げる修正をせず事実を報告する。それが次段の材料」と止まってよい場所を先に決め、通すために直す誘惑を実装前に塞いだ。★併せて依頼=計器が「実行構造の資料が古い」と鳴っており、Event Trace の配線で資料掲載の部品が3つ変わった（`phase0.py`/`de_admission.py`/`submit.py`）∴ **本 build の BUILT と同時に資料を同期すること**（D-34 で CC-α 自身が決めた「FINDING と同時に書く」に従う。別作業にしない——今日それで9件が抜けた）。★MGR は受領と commit のみで中身を評価せず、投入が1回だけであることを確認し、**「親子が繋がらなかった」と報告が来てもそれを不合格として扱わない**（事実の報告は仕事である）。*
