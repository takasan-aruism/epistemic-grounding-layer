# 設計/監査 → MGR（写: Taka / IMPL）: **Build 10R 監査 — 通過。私の指標③は不成立だったが、結論は別経路で確定した（また「当たったが理由が違う」）**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=FINDING
- **運用方針 確認済（版: v1.7）**
- **受領した文書**: `CC_IMPL_2026-07-27_BUILD10R_RESTART_VERIFY_BUILT.md`

## 0. 判定
**通過。** 再起動は既存手順で行われ、`TASK-2DER-D6A93450` に `run_next` を打たず、gate を迂回せず、本番コードを1行も変えていない。**Taka の実サービスは復旧している**（私も独立に確認：`pid 1594748` / `18:37:58` 起動 / `100.107.6.119:8770` 待受）。

**★IMPL の指摘は正しい。私の証拠③は指標として成立しない。** **先に認める。**

---

## 1. ★私の誤り — 応答の組み立てを読まずに指標にした【監査:CC-α】
```
再現: sed -n '549,556p' twoder/webui.py
  return self._send({"task_id": ..., "request_type": ..., "acquisition_method": ...,
                     "next_legal_operation": ..., "trace_key": ..., "deferred_active_tasks": ...,
                     "measured_state": ..., "egl_source_refs": ..., "host_ref": ..., "non_guarantee": ...})
```
**`/api/submit` の応答はキーの明示列挙である。** **`INTENT_STRATEGY` は最初から入らない。**
**∴ 段3e が動いていても動いていなくても、応答には現れない。** **指標として成立しない。**

**∴ Build 10R SPEC §0-1 で私が「Build 9C の応答に `INTENT_STRATEGY` が無かった＝webui が古い第2の証拠」と書いたのは、根拠として誤りである。撤回する。**

---

## 2. ★しかし結論は正しかった — TRACE ファイルで確定した【監査:CC-α】

**`webui.py:542-543` は TRACE 全文をファイルに書いている**（`_atomic_write(RUNS / f"{tid}.trace.json", ...)`）。
**∴ 応答に出なくても、段3e が動いたかは TRACE ファイルで確かめられる。** **私はこれを SPEC に書き落としていた。**

```
再現: cd /home/takasan/twoder/runs && ls -t *D6A93450*.trace.json
TASK-2DER-D6A93450-gIjS_A.trace.json   2026-07-27 16:31:43   INTENT_STRATEGY = ★無し   （Build 9C・旧プロセス）
TASK-2DER-D6A93450-qyvf4Q.trace.json   2026-07-27 18:09:23   INTENT_STRATEGY = ★無し   （Build 10・旧プロセス）
TASK-2DER-D6A93450.trace.json          2026-07-27 18:42:23   INTENT_STRATEGY = ★あり   （Build 10R・新プロセス）
                                       → {"strategy":"DIRECT","status":"AUTO_CONFIRMED","anchoring":"MEDIUM", …}
```

> **∴ 旧プロセスは段3e を持っていなかった。新プロセスは持っている。**
> **∴「webui が4日前のコードで動いていた」は、TRACE によって独立に確定した。**

### 2-1. ★これは本日2回目の「当たったが理由が違う」である
| | Build 9A | **本件** |
|---|---|---|
| 結論 | 「配線は入らない」→ **正しかった** | 「旧 webui は段3e を持たない」→ **正しかった** |
| 私が挙げた根拠 | 「一時 workspace に留まる**可能性**」→ **誤り**（実際は決定論的な拒否） | 「応答に `INTENT_STRATEGY` が無い」→ **誤り**（応答は元から含まない） |

**∴ 私は今日、同じ形の誤りを2回した。** **結論が当たると、根拠の誤りは見つかりにくい。**
**∴ 規律に1つ足す**: **指標を決めるとき、「その指標は、対象が偽のときに実際に値が変わるか」を先に確かめる。** ——**変わらない指標は、当たっても何も測っていない。**
**★今回それを見つけたのは IMPL である。** **実装が設計の計器の誤りを捕まえた形であり、正しく機能している。**

---

## 3. ★「証拠が1つしか揃わなかった」について — ①だけで十分である【監査:CC-α】
**IMPL は「2つの独立した証拠は揃っていない」と正しく報告した。** **ただし私の判断は次のとおりである。**

**① だけで結論できる**:
```
新プロセス起動 18:37:58  >  webui.py mtime 18:05:37 / dispatch.py mtime 18:05:20
```
**Python はモジュールを起動時に読み、キャッシュされたバイトコードはソースの mtime で無効化される。**
**∴ 起動がソースより後であれば、新しいソースが読み込まれている。** **①は弱い証拠ではない。**

**∴ 私が「2つの独立した証拠」を要求したのは、過剰であった。** **そして §2 により、結果的に3つ目（TRACE）で裏づけられた。**
**∴ 修理が新プロセスに入っていることは確定してよい。**

---

## 4. 併せて観測された事実（記録・断定しない）
**同じ依頼文に対する段3e の結果が、実行ごとに違う:**
| 実行 | プロセス | `INTENT_STRATEGY` |
|---|---|---|
| Build 9B（CLI・新しいコード） | 新規 | **`status: NO_CANDIDATE` / `strategy: null`** |
| Build 10R（webui・新プロセス） | 新規 | **`status: AUTO_CONFIRMED` / `strategy: DIRECT`** |

**∴ 同じ入力・同じコードで、段3e の出力が揺れる。**
**既報と整合する**（以前「`NO_CANDIDATE` は8回中1回」を観測）。**各1回なので断定しない**（v1.5 §4-13）。**精度の線なので今は測らない。記録に残す。**

---

## 5. 受入の確認【監査:CC-α】
| # | 受入 | 判定 |
|---|---|---|
| 1 | 手順1〜3 の記録（pid / 時刻 / mtime / コマンド） | **満たす**（停止・起動時刻とコマンドを逐語で記載） |
| 2 | ①②③ を個別に・②は「判定できず」 | **満たす**。**★③が指標として不成立であることを自分で見つけて報告したのは、受入を超える良い仕事である** |
| 3 | 予想と実際 | **満たす**（②は予想どおり、③は「予想が外れたのではなく指標不成立」と正確に書き分けている） |
| 4 | 再起動の事実と時刻 | **満たす** |
| 5 | 当該 task に `run_next` を打っていない | **満たす** |
| **6** | **二択に丸** | **★6回連続で欠落** |
| **7** | **`ledger_query.py` 削除** | **★4回連続で記載なし** |
| 8〜9 | 判定しない / commit しない / 再現コマンド併記 | **満たす** |

### 5-1. 受入6・7 について【設計:CC-α】
**6回・4回と続いているのは、私の SPEC の作り方の問題である。** **二択にしても書かれなかった。**
**∴ 文書の受入項目として書くのをやめ、次からは「BUILT の定型見出しとして必須にする」形に変える。**
**7 については、私が独立に確認する**（下記）。

```
再現: ls -la /home/takasan/twoder/ledger_query.py
```
**→ 未実行。次の監査で確認する。** **【未確認】のまま残す。**

---

## 6. 残（消さない）
| 件 | 状態 |
|---|---|
| **`planner_outcome` が実際に理由を運ぶか** | **未確認。** PLAN が失敗する機会が無かった。**MGR の (a)/(b)/(c) 裁定待ち。私は (c) を推す** |
| `twoder/ledger_query.py` の削除 | **【未確認】**（4回連続で報告に無い） |
| `test_submit_e2e` の既存3失敗 | 未調査・別件 |
| 段3e の揺れ | 記録のみ。今は測らない |
| **オラクル** | **未開封**（sha256 `8d709d1…bff722`）。**`ids.resolve()` も未実行** |

---
*CC-α Build 10R 監査。通過（既存手順で再起動・当該 task に run_next を打たず・gate 迂回なし・本番無変更。実サービスは復旧＝私も独立確認）。★IMPL の指摘は正しい: `/api/submit` の応答は `webui.py:549` でキーを明示列挙しており `INTENT_STRATEGY` を元から含まない ∴ 私の証拠③は指標として成立せず、SPEC §0-1 の「第2の証拠」は根拠として誤りなので撤回する。★しかし結論は TRACE ファイルで独立に確定した——`twoder/runs/*D6A93450*.trace.json` の3本が、16:31(9C)=無し / 18:09(10)=無し / 18:42(10R)=あり を示す ∴ 旧プロセスは段3e を持たず新プロセスは持つ。★本日2回目の「当たったが理由が違う」（Build 9A と同型）。規律に追加: 指標を決めるとき「対象が偽のときにその指標は実際に値が変わるか」を先に確かめる——変わらない指標は当たっても何も測っていない。★これを見つけたのは IMPL であり、実装が設計の計器の誤りを捕まえた形で正しく機能している。★「証拠が1つしか揃わない」問題については、①（起動時刻 > ソース mtime）だけで結論できる（Python はソース mtime でバイトコードを無効化する）ので、私が2つを要求したのが過剰だった。修理が新プロセスに入っていることは確定してよい。併せて段3e の出力が同一入力で揺れる（9B=NO_CANDIDATE / 10R=DIRECT AUTO_CONFIRMED）——既報と整合、各1回なので断定せず記録のみ。受入は二択が6回・ledger_query 削除が4回連続で欠落しており、これは私の SPEC の作り方の問題なので次から BUILT の定型見出しとして必須にする。*
