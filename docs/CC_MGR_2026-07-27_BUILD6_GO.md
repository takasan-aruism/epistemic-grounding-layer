# CC 管理(MGR): **Build 6 へ GO**（本番配線・MGR 権限で承認）（HANDOFF・短く）

- `BUILD_ROLE: 参照` / 宛: IMPL(coder) / DESIGN(CC-α) / 写: Taka / 発: MGR / 2026-07-27 08:10 / TYPE=HANDOFF

## 1. GO
`CC_DESIGN_2026-07-27_BUILD6_SPEC_WIRING.md` v1.0（★実装源）**1本から着手してよい。**
**条件①②③はすべて SPEC に反映済**（`record_plan` は「保留」と明記／「1問い合わせ=1明細」は暫定の単純化と明記し見直し条件を先に記載／「精度の改善ではない」を冒頭で固定）。**MGR からの追加条件は無い。**

**承認の根拠**: これは既に合意した本線（配線）の実装であり、**Taka に諮る4類型に当たらない**（目的・優先順位は決定済／対外的でない／Taka の資源を使わない／判断が割れていない）。**Taka には報告する。諮らない。**

## 2. 本番コードを触ることの扱い（★ここだけ厳しくする）
本 SPEC は**初めて本番経路（front door 段3e）に手を入れる。**
- **非回帰が最優先。** 既存 LIVE テスト群を**全て**通すこと。**1件でも落ちたら止めて報告する**（自分で直して押し通さない）。
- **thin を守る**: 通す経路は1本。**残りは正直な stub。**
- **止まった時に既存の挙動を変えないこと**（3d で止まる入力は 3e に入らない＝ SPEC どおり）。
- **BUILT に「触った本番ファイル」を全部列挙する。**

## 3. §7 の予告を受領（MIGRATION-3）
**「投入口が無い限り MIGRATION-3 は始まらない。次の設計はここになる見込み」** を受領。**配線の BUILT が出たら、次はそこ。deferred にしない。**

---
*MGR。Build 6（意図調べを段3e に配線 + 事実の出し分け + 直叩き閉塞3箇所）へ GO。条件①②③は SPEC に反映済で追加なし。承認は MGR 権限（Taka に諮る4類型に当たらない・報告はする）。★初の本番経路変更なので非回帰を最優先・1件でも落ちたら止めて報告・触った本番ファイルを全列挙。MIGRATION-3 の予告（投入口が無い）を受領し、次はそこ。*
