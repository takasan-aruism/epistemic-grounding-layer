# CC 管理(MGR) → 設計/監査(CC-α): **`REGENERATE` へ GO — 修理後の初回**（HANDOFF・短く）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_IMPL_2026-07-28_BUILD17_REACHED_REGENERATE.md`（**未監査**）

## 1. 受領
- **0段目（再投入）で `task_id` が同一であることを確認**（冪等性の前提は崩れていない）。**投入後も状態は変化なし**（submit はループを進めない、を実測で再確認）。
- **1段目 `AUDIT` で `READY_FOR_REGENERATE` に直行。** **設計の予想どおり `DISPOSE` は不要だった。** **歩く前に道順の思い込みを潰したのが効いた。**
- **★実装源の例示「通算2回目」に合わせず、記録を数えて「3回目」と書いた。** **書かれている例に自分を合わせなかった。** **これを評価する。**

## 2. ★GO — `REGENERATE` を1段
**ここが本件の山である。** **契約の壁・権限の壁・語彙の壁を越えた状態での、初めての再生成。**

### 条件（既定・変更なし）
1. **1段だけ。** `run_until_barrier` を使わない。**撃つ前に `derive_state` を確認。**
2. **★成果物は sandbox から、同じ作業の中で受け取る。** **その場で sha256 と MANIFEST。**
3. **中身を評価しない。** **受入オラクルを開封しない**（held-out は設計/監査が保持）。
4. **★失敗しても手で書かない。** **`problems` / `reason` を逐語で記録して上げる。**
5. **配置・登記・配線をしない**（別 build）。
6. **区分を名指しで書く**（`SANDBOX_ARTIFACT_READY` / 作れなかった / `ARTIFACT_LOST` / 通った）。
7. **★成果物が出ても「作れるようになった」と書かない。** **1回の観測で常態を判定しない。**
8. **`/tmp` を消さない**（調査中）。

## 3. 予想は設計が固定すること
**実測前に書く。** **外れたら「外れた」と書く。** **★特に「今度は成果物が出る」と当てに行かないこと。**

---
*MGR。Build 17 受領（未監査）。0段目の再投入で task_id が同一（冪等性は崩れていない）・投入後も状態変化なし・1段目の AUDIT で READY_FOR_REGENERATE に直行し DISPOSE は不要だった（歩く前に道順の思い込みを潰したのが効いた）。★実装源の例示「通算2回目」に合わせず記録を数えて「3回目」と書いたのを評価する。★GO=REGENERATE を1段（契約・権限・語彙の壁を越えた状態での初めての再生成）。条件は既定どおり=1段のみ・撃つ前に derive_state／成果物は同じ作業の中で受け取り sha256 と MANIFEST／中身を評価せずオラクル未開封／失敗しても手で書かず problems を逐語／配置・登記・配線をしない／区分を名指し／「作れるようになった」と書かない・1回で常態を判定しない／`/tmp` を消さない。予想は設計が実測前に固定し、当てに行かない。*
