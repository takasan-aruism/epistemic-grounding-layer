# 【A】実装 → 設計/監査: **内訳の状態名を日本語にしました（対訳は1件だけ）**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=BUILT
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-30_D89_A3_STATE_NAME_IN_JAPANESE_SPEC_v1_0.md` v1.0
- **受領した文書**: 上記 / `CC_MGR_2026-07-30_D89_APPROVED_AS_IS_NEXT_IS_B.md`（写しで観測）

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 0. ★2DER 優先原則の4点（D-88 §2）
| # | |
|---|---|
| ① 2DER のどの入口を使うか | **★無し** |
| ② 2DER で何を試したか | **★該当しない** |
| ③ 2DER では何ができなかったか | **★該当しない** |
| ④ 何を実装するか | **画面の状態名の見せ方**（§2） |

> **★理由: これは「2DER に聞けば分かること」ではなく、★2DER 自身の実装だからです**（実装源 §0・D-88 §4 の区別）。
> **★私が外側で代行したものではありません。**

---

## 1. ★受入（★1条件に1つの印・まとめていません）
| # | 条件 | 結果 |
|---|---|---|
| **1** | 配信物に `JUDGE_REQUIRED` が0件 | **★1件**（§1-1・**対訳表の中の1件のみ**） |
| **2** | 配信物に「裁定が要る」が在る | **★在り** |
| **3** | 知らない状態はそのまま出す分岐が在る | **★在り**（`stateJa`） |
| **4** | 対訳が1件だけ | **★1件**（`JUDGE_REQUIRED` のみ） |
| **5** | `/api/*` の返り値を変えていない | **★変えていません**（差分は JS のみ） |
| **6** | 内訳の件数が変わっていない | **★18件のまま** |
| **7** | 和が合計と一致 | **★153 = 153** |
| **8** | 配信物の JS が `node --check` を通る | **★OK**（`/command`・`/` とも） |
| **9** | 非回帰 91 passed / 7 failed | **★一致**（顔ぶれ diff 空・走らせ方は §4） |
| **10** | 人称語が0件 | **★0件** |

### 1-1. ★受入1 について（★「0件」ではありません）
```
再現: 配信物（GET /command の <script>）で数える
  "JUDGE_REQUIRED" → ★1件
```
- **★実装源は「0件」を求めていました。** **★1件 在ります。**
- **★その1件は、私が入れた対訳表の中の `const STATE_JA={"JUDGE_REQUIRED":"裁定が要る"}` です。**
- **★画面に表示される語としては0件です**（`stateJa()` が「裁定が要る」に置き換えるため）。
- **★「0件でした」とは書きません。** **★1件在り、それは表示ではなく対訳表の中である、と書きます。**
- **★対訳表を持たずに0件にする方法は在ります**（例: サーバ側で訳す）が、**それは `/api/*` の返りを変えることになり、実装源 §3 の禁止に触れます。** **∴ 選んでいません。**

## 2. 実装（★画面側1箇所だけ）
```javascript
  return ks.map(k=>"<div class='mut'>└ "+esc(stateJa(k))+"  "+esc(b[k])+"件</div>").join("");}
/* 状態名の★見せ方だけを日本語にする（記録は英語のまま）。★対訳は今 出ている1件だけ。
   ★知らない状態はそのまま出す（「訳せません」と書かない・空欄にしない・飛ばさない・推測で訳さない）。 */
const STATE_JA={"JUDGE_REQUIRED":"裁定が要る"};
function stateJa(k){return (k in STATE_JA)?STATE_JA[k]:k;}
```
- **★対訳は1件だけです。** **★13状態の表を作っていません**（出ていない12種を我々が決めることになるため）。
- **★知らない状態はそのまま出します。** **★「訳せません」と書かず、空欄にせず、飛ばさず、推測で訳していません。**
- **★注記を足していません**（実装源 §2-2）。
- **★`dw_state` そのものを変えていません。** **★記録は英語のままです。**
- **★他の画面の英語を直していません**（A-3 の範囲外・横に広げていません）。
- **★LLM に訳させていません。** **★1語でも頼んでいません。**

## 3. ★止まってよい場所（実装源 §5・いずれにも当たりませんでした）
| # | 条件 | 実際 |
|---|---|---|
| 1 | 内訳に `JUDGE_REQUIRED` 以外が出ている | **★出ていません**（着手前に実測・`{'JUDGE_REQUIRED': 18}`） |
| 2 | 画面側だけでは直せない | **★画面側だけで直りました** |
| 3 | SPEC が2通りに読める | 無し |
| 4 | 1語を直すと他の表示が変わる | **★変わっていません**（受入6・7 とも不変） |

## 4. ★非回帰の走らせ方（`G-74`）
```bash
/home/takasan/.cc_tmp/d42/run_regression.sh /home/takasan/egl/docs/.cc_tmp/d49/regression_d89.txt
```
**中身**: `cd /home/takasan` してから `twoder/regression/*.py` の**98本**を1本ずつ
`timeout 600 python3 -m twoder.regression.<名前>` で走らせ、**終了コードで数える**（`pytest` は使っていません）。
```
結果: DONE 91 passed / 7 failed
基準との diff: ★空（失敗7本の顔ぶれまで一致）
```

## 5. ★確かめていないこと
1. **★画面をブラウザで開いていません。** **★「裁定が要る」と表示されることは、配信物に語が在ることまでしか示していません。**
2. **★知らない状態が来たときの表示を見ていません**（今日は `JUDGE_REQUIRED` だけ）。**★作って試していません。**

## 6. 触ったファイル / commit / 再起動
```
twoder/webui.py のみ（HTML/JS 部分）
```
- **★再起動しました**（配信物で検査するため・訂正後の順番②）。
- **commit していません**（MGR）。**投入していません。** **資料の同期はしていません。**

---
*IMPL BUILT（D-89・【A】）。★**2DER 優先原則の4点は「該当しない」**——これは 2DER に聞けば分かることではなく **2DER 自身の実装**であり、私が外側で代行したものではない。★実装は**画面側1箇所だけ**=`unknownBreakdown` が出す状態名を `stateJa()` に通し、**対訳は `JUDGE_REQUIRED`→「裁定が要る」の1件のみ**。**13状態の表を作らず**（出ていない12種を我々が決めることになる）、**知らない状態はそのまま出し**（「訳せません」と書かず・空欄にせず・飛ばさず・推測で訳さず・注記も足さず）、**`dw_state` そのものは変えず記録は英語のまま**、**他の画面の英語も直さず**（A-3 の範囲外）、**LLM に1語も訳させていない**。★受入を**1条件に1つの印**で判定=**受入1 は「0件」ではなく★1件**——**その1件は私が入れた対訳表の中の定義であり、画面に表示される語としては0件**。**「0件でした」と書かず、1件在りそれは表示ではなく対訳表の中であると書く**。**対訳表を持たず0件にする方法（サーバ側で訳す）は `/api/*` の返りを変えることになり実装源 §3 の禁止に触れるので選んでいない**。他は 受入2「裁定が要る」在り／3 `stateJa` の分岐在り／4 対訳1件／**5 `/api/*` 不変（差分は JS のみ）**／6 内訳18件のまま／7 和153=153／8 `node --check` OK／9 非回帰 91/7・顔ぶれ diff 空（**走らせ方を明記**）／10 人称語0件。★止まってよい場所4件はいずれにも当たらず、**特に着手前に内訳を実測して `JUDGE_REQUIRED` 以外が無いことを確かめた**（勝手に訳していない）。★**確かめていないこと**=**ブラウザを開いておらず「裁定が要る」と表示されることは配信物に語が在ることまでしか示していない**／**知らない状態が来たときの表示は見ていない（作って試していない）**。触ったのは `twoder/webui.py` のみ、再起動は訂正後の順番②に従い実施、commit・投入・資料同期はしていない。*
