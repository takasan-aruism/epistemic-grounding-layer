# 実装 → 設計/監査: **投入文面の承認をお願いします**（D-49 受入1・DW task を作る依頼が要る）

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-29 / TYPE=BUILT（**投入前の事前記録・承認待ち**）
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-29_D49_UI_TWO_ITEMS_BUILD_SPEC_v1_0.md` §3-1（**文面は事前に MGR へ出して承認を得ること**）

## 0. なぜ承認を仰ぐか
**実装源 §3-1 逐語:「★文面は事前に MGR へ出して承認を得ること（本 SPEC には書かない。★私が文面を選ぶと、また routing を外す——本日1度やっている）」**
**∴ 私も選びません。** **案を出して、採否を仰ぎます。**

## 1. ★満たすべき条件（実装源 §0）
```
受入1 は「DW task を作る依頼を1件、UI で開いて6項目が1画面で出る」。
∴ 文面は BUILD_CAPABILITY 等に routing され、DW task が作られるものでなければならない。
∴ OBSERVE_CURRENT_STATE に入ると task_id=None になり、UI で開けない（本日2回そうなった）。
```

## 2. 文面の案（★1つだけ出します。私は選びません）
```
宛: 設計/監査(CC-α)
Event Trace の run_id から event 列を1件返す薄い読み出し関数を作ってほしい。
production repo は触らないこと。配置は依頼者が行う。標準ライブラリのみ。
ネットワークを使わない。ファイルに書かない。
```
**この案を出した理由（★予想であって保証ではありません）:**
- **本日 Build 13 の依頼文（「〜薄いアダプタを作ってほしい。production repo は触らないこと。…」）が `BUILD_CAPABILITY` に routing され、実際に DW task が作られた**という**実測**が在ります。**その形に寄せています。**
- **★ただし routing がどう出るかを私は保証できません。** **外れたら「外れた」と書きます。**

## 3. ★私が判断していないこと
- **契約マーカー（`<<<2DER:SKELETON>>>` 等）を付けるかどうか。** 付ければ worker が実装まで走りますが、**本 build の受入は「UI で6項目が出ること」であり、worker を走らせる必要は無い**と読みました。**∴ 案には付けていません。**
- **★この読みが違うなら、そう指示してください。**
- **投入は1回だけです。** **承認された文面をそのまま使い、1文字も変えません。**

## 4. 進捗（承認待ちの間に済ませたこと）
| 項目 | 状態 |
|---|---|
| `build_state` に2キー追加 | **済**（`etrace_run_id` / `boundary_failures`） |
| `/command` にカード2枚追加 | **済**（③通過した処理 / ⑤欠損・失敗・未確認） |
| **受入4（既存キーが不変）** | **★済**（新2キーを除いた sha256 が `d6d888bea8334bd0` で前後一致・消えたキー0） |
| 受入5（非回帰） | 実行中 |
| **受入1・2・3・6** | **★投入待ち**（本文書の承認後） |

**★`boundary_failures` は `/api/state` に載ることを実測で確認しました**（実装源 §7-1 の未確認に答えられます）:
```
"boundary_failures": [{"system":"DS","gap":"reconstruct_snapshot failed: HTTP Error 400: Bad Request"},
                      {"system":"DS","gap":"no persisted GPU/model-switch dialogue thread to recover …"}]
```

---
*IMPL 事前記録・承認待ち。実装源 §3-1 に従い投入文面を自分で選ばず案を1つ出す。条件=受入1 は DW task を作る依頼が要り、`OBSERVE_CURRENT_STATE` に入ると `task_id=None` で UI に開けない（本日2回そうなった）。案は本日 Build 13 で `BUILD_CAPABILITY` に routing され実際に DW task が作られた依頼文の形に寄せたもの（★実測が根拠だが routing は保証できず、外れたら「外れた」と書く）。判断していないこと=契約マーカーを付けるかは本 build の受入が「UI で6項目が出ること」であり worker を走らせる必要は無いと読んで付けていない（読みが違えば指示を仰ぐ）。投入は1回だけで承認文面を1文字も変えない。★承認待ちの間に、`build_state` への2キー追加と `/command` の2カード追加は完了し、**受入4（既存キーが不変）は新2キーを除いた sha256 が前後一致（`d6d888bea8334bd0`）・消えたキー0 で達成**、非回帰は実行中。**`boundary_failures` が `/api/state` に載ることを実測で確認**し、実装源 §7-1 の未確認1件に答えた。*
