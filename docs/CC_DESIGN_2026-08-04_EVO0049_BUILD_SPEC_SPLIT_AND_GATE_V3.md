# 【BUILD SPEC v3】`EVO-0049` — **★依頼文に1行 足す（★曖昧さが私の側に在った）**

- `BUILD_ROLE: ★実装源` / **宛: IMPL**（★封入は MGR） / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-04 07:4x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.11）** ／ 親: `ITEM-2DER-EVO-0035` ／ 方針: `ITEM-2DER-EVO-0057`
- **★v1/v2 との関係**: ★差し替えない・追記しない。**★本書が実装源**。**★試験は v2 の18本を1文字も変えない**
- **★私の予告**: ★試験 18本（★不変）／★Claude の配線 **11行**（★実装の実測を採る）／★worker の行数は書かない
- **★走行 0・★task 増 0・★commit 0**

---

## 1. ★実装の指摘は正しい（★私も再現した）

```
★私が再現した実測:
   REAL_A len=300 ★空行0 改行6 箇条書き行0 句点3
   REAL_B len=300 ★空行0 改行0 箇条書き行1（★先頭が '8) '=数字+`)`） 句点1
★★(A) の読み方（★①が空行の有無に関わらず全体を1件にする）→ ★REAL_A が1件 ∴ ★★2本 落ちる
★★(C) の読み方（★①は空行が実際に在る時だけ範囲を作る）→ ★両方 通る
★★★∴ ★依頼文の散文が ★どちらにも読める ＝ ★★曖昧さは ★私の側に在る。
★★★★★試験は (C) に固定されているが、★worker が (A) を採れば ★1走行 無駄になる ＝ ★★費用の問題。
```

**★∴ 依頼文に1行 足す。**★「そのまま封入して落ちたら書く」は選ばない ―― **★1走行 無駄にする理由が無い**（★曖昧さは ★私が作ったものであり、★worker の落ち度ではない）。

## 2. ★★足す1行（★これだけ・★他は1文字も変えない）

★契約①の依頼文、**★「・切れ目の見つけ方（★この順で重ねる。★重複した範囲は 後から出たものを ★捨てる）:」の直後**に1行:

```
・★①②③は それぞれ「その印が ★実際に在る所」だけで範囲を作る。
  ★空行が1つも無い text で ①が text 全体を1件にする、ということは ★しない。
  ★どの印も見つからなかった時だけ、★最後の規則（1件の "PARAGRAPH"）を使う。
```

```
★★試験は ★1文字も変えない（★v2 の18本）。★骨格も不変。
★★★★これで ★依頼文と試験が ★同じ読み方（C）を指す ＝ ★v1.11（依頼文に書いた規則は試験で縛る）が ★両側から成立
```

## 3. ★私の不備（★数える）

```
★本日 ★5回目の同型=★『依頼文が ★曖昧で、★試験だけが正しい』
   ①fixture が実形式でない ②列挙の肯定側なし ③verdict の許容語を書かず ④assert に2つの意図 ★⑤本件
★★★但し ★①〜④と ★違う点=★★今回は ★投入前に ★実装が見つけた（★走らせる前）。
   ★★★『試験は通り実データで落ちる』ではなく ★『走らせる前に費用を止めた』＝ ★形としては ★改善している
★★★★★私が次からやること=★依頼文に「順に重ねる」「捨てる」と書いたら、★★その場で ★極端な入力（★印が0件）を
   ★1つ ★自分で当てる（★試験を書く前に）。
```

## 4. 受入（★v2 の §7 を ★そのまま引き継ぐ）
```
★(1) worker が2件を書く（★Claude は本文0行）／★(2) ★18本 全通
★(3) 保存則が機械で出る ／★(4) 門の判定が明細ごとに出る
★(5) 4つの対照の結果を全部 書く（★陽性は ★実物に無ければ『無い』と書いて ★作らない＝実装の申告どおり）
★(6) 配線行数 ／★(7) 戻せる ／★(8) 61本を走らせない ／★(9) 出せなければ『出せなかった』と書いて止まる
★★★★(10) ★★もし ★18本のうち ★どれかが落ちたら ―― ★★『依頼文をもう一度 直す』のではなく
     ★★★★★『★どの読み方で落ちたか』を ★逐語で書いて ★止まる（★私が ★6回目を作らないため）
```

## 5. 禁止（★v1/v2 を引き継ぐ）
```
★試験を変える（★18本は確定）／ ★骨格を変える ／ ★依頼文の ★他の行を触る
★既存資産（preflight_gate / request_thread / request_resolution）を作り直す
★段0 で割る ／ ★原文を書き換える ／ ★4軸の LLM 測定 ／ ★段2 の分岐
★新しい台帳・エンドポイントを作る ／ ★61本を走らせる ／ ★commit する
★`twoder` 配下で python を動かす
```
