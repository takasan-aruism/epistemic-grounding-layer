# 【BUILD SPEC】**★取得コマンドの列挙を外し、値だけを見せる**（1箇所）— ★H1 を潰す

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 09:5x / TYPE=BUILD_SPEC
- **運用方針 確認済（版: v2.8）** ／ **裁定**: `CC_MGR_2026-07-31_D160_…` §4
- **★新しい名前で置いた**（`D-158` §2-2）。**★`ART-c0fd9dbc6a` は役目を終えた。**
- **★2DER 優先原則の例外**（IMPL が書く）。**★担当工程数の差分は 0 の見込み。★0 なら「0」と書く。**

---

# 1. ★やること（★1箇所だけ）

> ### **★`_observation_facts` の1つ目のブロック（`ALREADY ACQUIRED FOR THIS TASK`）から、★取得コマンドの列挙を外す。★`VALUES ALREADY OBSERVED` は残す。**

| 条件 | |
|---|---|
| **★外すもの** | **★`ap["command"]` の列挙**（`build_planner.py:118` の `acquired by running [...]`） |
| **★残すもの** | **★`VALUES ALREADY OBSERVED` の値ブロック**（★これが今回の主役） |
| **★どう残すか** | **★観測が「既に取得済である」ことは残してよい**（★`OBS-` の id・取得先・時刻・結果）。**★「どの命令で」だけを外す** |
| **★作らないもの** | 新しい台帳・API・ID 族 ／ **★新しい指示文を planner に足さない**（★「観測を使え」と書かない＝規則の追加になる） |
| **★語の禁止** | **★diff に `gpu`/`nvidia`/`port`/`docker`/`ps ` を0件**（★大小無視・打ち切り無しで示す） |
| **★触らないもの** | 観測の選別（`G-06`）／ Ledger（保留中）／ 観測を Task の下流へ移す設計変更 |

---

# 2. ★受入（★1条件に1つの印）

| # | 受入 | ★示し方 |
|---|---|---|
| **A** | **★プロンプトからコマンドが消えた** | **★`_observation_facts` の出力に `ps`/`nvidia-smi`/`docker` の★コマンド列が出ないこと**（★関数を読んで示す。★実行して示すなら出力を貼る） |
| **B** | **★値ブロックは残っている** | `VALUES ALREADY OBSERVED` が出ること |
| **1〜4** | **★Taka の4条件のまま**（★1文字も緩めない） | 前回と同じ示し方 |
| **★補** | **★実際に観測された値が計画本文に出るか** | **★観測値からトークンを作り、★英語＋数値の両方で数える。★一般語（`container`/`memory`/`used` 等）を除いた★具体値が1つでも出るか**（★前回は0件） |
| 補 | 後方互換 | 基準値11件（`D-151 SPEC §3-1`）を取り直して1件ずつ |
| 補 | 1箇所・語の禁止 | 変更ファイル／hunk／挿入削除行数 ／ 禁止語0件 |

## 2-1. ★測り方（★私が本日4回 踏んだ形。★同じ穴に落ちない）
```
★★入れ子まで見る（★最上位キーだけで「無い」と読まない。★私は昨夜これで誤った）
★★英語と日本語の両方で数える（★計画が英語で書かれることが在る）
★★「全部0」を「きれい」と読まない  ★★部分一致で判定せず、★キーを列挙して目視する
```

---

# 3. ★確認（★依頼文は MGR が固定する。★私は決めない）
```
★★同じ依頼文では届かない（★実証済）∴ ★新しい依頼文が要る。★MGR が固定する。
★★あなたは①修正を先に進めてよい。
★手順: ①修正 → ②再起動（全件記録）→ ③文を機械で抜く（字数・sha1）→ ④task_id を投入前に予告
       → ⑤投入1回 → ⑥直後に receipt → ⑦run_next 1回 → ⑧PLAN が出たら★止める
★★GENERATE へ進まない
```

# 4. ★予告（★投入前に固定する）
```
★★私は「効く」と予告しない（★MGR も同じ）。★入力を足す方向は★2回 試して2回とも届かなかった。
★★止まれば H1（コマンドの列挙が原因）／★止まらなければ H1 が落ちる。★どちらでも結果である。
★★受入3 が立たなかったら「★コマンドを外しても効かなかった」と書く。★言い換えない。
```

# 5. ★やってはいけないこと
```
★観測の選別に手を出す（★H2 の領域・`G-06`。★今回は触らない）
★planner に「観測を使え」という★指示文を足す（★規則の追加になる）
★観測を Task の下流へ移す ／ ★GENERATE を押す ／ ★Ledger を触る
★新しい台帳・API・ID 族 ／ ★同名で文書を差し替える ／ ★commit する
★★別の欠陥を見つけたことを理由に止まる（★止まってよいのは実行不能・破壊・戻せない証拠のときだけ）
```

# 6. ★報告（★正典の6項目）
```
1. 変更した1箇所  2. 受入 A・B・1〜4・補 の判定（★1条件に1つの印）  3. Last PASS  4. First FAIL
5. ★2DER 担当工程数の前回差分（★0 なら「0」）  6. 次に直す1件
★予告の当否  ★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---
**決めたこと**: **①直すのは1箇所——`_observation_facts` の `ALREADY ACQUIRED` ブロックから取得コマンドの列挙だけを外し、`VALUES ALREADY OBSERVED` は残す（「既に取得済である」ことは残してよく、「どの命令で」だけを外す） ②受入は Taka の4条件のままに A（コマンドが消えた）B（値は残る）と補（具体値が本文に出るか）を足し、測り方は入れ子まで見る・英語と日本語の両方で数える・全部0 をきれいと読まない ③「効く」とは予告しない——止まれば H1、止まらなければ H1 が落ちる。観測の選別（H2・`G-06`）には今回 触らない。**
