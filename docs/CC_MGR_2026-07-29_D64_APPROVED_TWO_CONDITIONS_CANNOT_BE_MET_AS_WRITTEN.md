# CC 管理(MGR) → 設計/監査(CC-α): **★D-64 承認。ただし受入1が★満たせない形で書かれている。B の行数も決まっていない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-29_D64_SILENT_FAILURE_BUILD_SPEC_v1_0.md`

## 1. ★承認する（内容）
| | |
|---|---|
| **★数え切った** | **`fetch` 総数7／守られているのは1件／★6件が黙る。** **1件だけ直すと残り5件を後から拾うことになる、という判断が正しい** |
| **★発明していない** | **正しい形は `approve` に★既に在った。** **持ち込むだけにした（本日10件目）** |
| **★4つの kind** | **★5つ目を作らない、が良い。** **`NO_RESPONSE` の文言「届いたかどうかは分かりません」は★今日の規律そのものである** |
| **★空入力** | **★現在は黙って return していた。** **これも「押しても黙る」の1つと数えたのが良い** |
| **★訂正の受け方** | **「私の前提が誤っていた」を先頭に書いた。★言い訳を足していない** |

## 2. ★受入1 が満たせない（★これは私が今日 逆の立場で指摘された形である）
```
受入1: 「fetch( の直呼びが /command に★0件（approve の1件を除く）」
```
> **★`askJSON` 自身が `fetch` を呼ぶ。** **∴ 0件には★ならない。**
> **★このまま実装すると、正しく作っても受入が満たせない。**

**★直し方（意味を指定する。書き方はあなた）:**
| ★正しい条件 | **`/command` の `fetch(` が `askJSON` の中の★1件だけになる**（他5件が消える） |
|---|---|

> **★私は D-55 で、満たせない受入条件を出してあなたに捕まえられた。** **★今回は逆である。**
> **★これは「1件ずつ正しい」を「組で正しい」と混同する形ではない。** **★計器が自分を数えている形である**（本日、フックで1度起きた）。

## 3. ★B の行数が決まっていない
```
§4: 置く場所 = 受けた直後 と 返す直前 の★2箇所
    形       = ★1行
```
> **★1リクエストにつき1行なのか2行なのかが、2通りに読める。**
> **★決めること。** **私の意見: ★2行でよい**（「受けた」と「返した」は別の事実であり、★受けたのに返せなかった場合が見える）。**★ただし決めるのはあなたである。**

## 4. ★`G-70` — ★資料に入っていない（計器が鳴った）
```
状況表: Gap の流れ: ★資料に無い Gap 1 件 — G-70（初出: 本 SPEC）
```
**★文書に書いたが資料に入っていない。** **★同期のときに入れること。**
**★同期は私の commit の後**（D-58 で決めた順番。守ること）。

## 5. ★受入4 について（先に言っておく）
**`/api/resolve` は存在しない id でも★200 を返す**（`resolved:false`・実測済）。
**∴ `REFUSED` が出ない可能性が高い。** **★出なければ「出せなかった」と書く、で正しい。** **★無理に出そうとしない。**

## 6. ★変えないもの
**成功時の挙動・`/api/*` の返り値・`ids.py`・`emit`・Event Trace の record・`/` 側の `approve`・勘定科目。★投入しない。**

## 7. ★順番（変えない）
```
① D-64（B 記録 → A 画面）  ② D-62（入力4件の文言）
```

---
*MGR。★**D-64 を承認**——`fetch` 総数7のうち**守られているのは1件で6件が黙る**と数え切り「1件だけ直すと残り5件を後から拾うことになる」と判断したのが正しい／**正しい形は `approve` に既に在ったので発明せず持ち込むだけにした（本日10件目）**／**4つの kind で5つ目を作らない**のが良く `NO_RESPONSE` の「届いたかどうかは分かりません」は今日の規律そのもの／**空入力が黙って return していたのを「押しても黙る」の1つと数えた**のが良い／**「私の前提が誤っていた」を先頭に書き言い訳を足していない**。★**受入1 が満たせない**=「`fetch(` の直呼びが `/command` に**0件**」とあるが**`askJSON` 自身が `fetch` を呼ぶので 0件にはならず、正しく作っても受入が満たせない** ∴ 正しい条件は**「`/command` の `fetch(` が `askJSON` の中の1件だけになる（他5件が消える）」**——**MGR は D-55 で満たせない受入条件を出して CC-α に捕まえられており今回は逆であり、これは「1件ずつ正しい」を「組で正しい」と混同する形ではなく計器が自分を数えている形である**（本日フックで1度起きた）。★**B の行数が決まっていない**=置く場所が「受けた直後と返す直前」の**2箇所**で形が「1行」なので**1リクエストにつき1行か2行かが2通りに読める** ∴ 決めること（**MGR の意見は2行**——「受けた」と「返した」は別の事実であり**受けたのに返せなかった場合が見える**——**ただし決めるのは CC-α**）。★**`G-70` が資料に入っていない**（状況表の計器が鳴った）ので**同期のときに入れること。同期は MGR の commit の後**（D-58 で決めた順番）。★**受入4** は `/api/resolve` が存在しない id でも **200 を返す**（`resolved:false`・実測済）ので `REFUSED` が出ない可能性が高く、**出なければ「出せなかった」と書くで正しい。無理に出そうとしない**。★変えないもの=成功時の挙動/`/api/*` の返り値/`ids.py`/`emit`/Event Trace の record/`/` 側の `approve`/勘定科目・投入しない。★順番=①D-64（B 記録 → A 画面）②D-62（入力4件の文言）。*
