# 【BUILT】D-180/D-181 — **★規則が prompt に渡った（強制語 0→3）。★この1回は 母数3対のうち ×1件**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: MGR / Taka / 発: 実装(IMPL) / 2026-07-31 18:5x / TYPE=BUILT
- **運用方針 確認済（版: v2.9）** ／ **実装源**: `CC_DESIGN_2026-07-31_D180_BUILD_SPEC_WRITE_THE_RULE.md` ＋ 追補 `…D181_THE_TEXT_AND_A_DENOMINATOR_PROBLEM.md`
- **`:8005` は 2DER が内部で呼んだ**（★私は直接 叩いていない）／**★2DER 担当工程数の前回差分: 0**

---

# 1. ★変更した1箇所（★禁止語の走査を先に）

## 1-1. ★禁止語（★大小無視・打ち切り無し・★先に出した）
```
gpu 0件 ／ nvidia 0件 ／ port 0件 ／ docker 0件 ／ disk 0件 ／ memory 0件 ／ ps 0件 ／ ss 0件
```
## 1-2. 差分
```
twoder/build_planner.py ／ ★1 hunk ／ ★1挿入・0削除（@@ -133,0 +134 @@ _observation_facts）
足した位置: ★`if not blocks: return ""` の直後（★SPEC §1 の逐語どおり）
★触っていないこと(diff 内の出現 0件): STRUCTURED_KEYS ／ EXECUTABLE_KEYS ／ _plan_prompt ／ validate_plan
★文言は SPEC §2 の逐語（★打ち直していない）
```

---

# 2. ★§3 の物差しでの判定（★母数と×を両方・★「項目」と「対」の両方）

## 2-1. ★母数
| 項目 | 逐語 | 道具 |
|---|---|---|
| 1 | 「Commands \`ps -eo pid,rss,comm --sort=-rss\` and \`nvidia-smi --query-gpu=…\` are known to be effective.」 | `ps` / `nvidia-smi` |
| 2 | 「Docker and network monitoring commands are known.」 | `docker` |

> **★母数（項目）= 2 ／ ★母数（対）= 3**（★`D-181` §2-2 が未確定のため両方 書く）

## 2-2. ★判定（★1対ずつ）
| 対 | 成果物側での出現 | 判定 |
|---|---|---|
| （項目1, `ps`） | **0回** | **×にしない**（★成果物側に出てこない） |
| （項目1, `nvidia-smi`） | **0回** | **×にしない** |
| （項目2, `docker`） | **1回** | **★×**（★前後160字に語彙表の語が無い） |

**★× 1件 ／ 母数（対）3 ・ 母数（項目）2**

**★×の実物（前後160字・逐語）**
```
"files_expected": ["memory_reporter.py","test_memory_reporter.py"],
"steps": ["Define JSON schema for input data (processes, gpu, docker).",
          "Implement memory_reporter.py to read JSON file.", …
```
**★判定の理由**: `docker` が**入力スキーマの列挙**として出るだけで、**★「不足」「追加」「対応付け」等の語が近くに1つも無い** ∴ 物差しどおり ×。**★命令の重複では判定していない。**

## 2-3. ★★この数字を前と引き算しない（★SPEC §3-4）
```
★×14/109 は :8005 単体・38本の値である。★今回は 本番1回・母数3対 ∴ ★引き算しない。
★★書けるのは「★この1回、母数3対のうち ×は1件だった」だけである。
★★母数3では、規則が無くても ×0〜1 は普通に出る（★設計が §2 で先に書いたとおり）。
```

---

# 3. ★Audit 6欄

| 欄 | |
|---|---|
| **却下対象** | **★無し（PASS）。** ★ただし §2-2 の ×1件 は別に立てている |
| **証拠** | ★Observation 側の強制語 **0→3件**（`MUST` 2 / `Do NOT` 1 / `MUST NOT` 1 ＝ `MUST` 系 2＋`Do NOT` 1＝3）・`RE-ACQUISITION RULE` を prompt に含むことを**実行して確認**／`already_satisfied` に2項目／`plan_source=QWEN_BUILD_PLANNER`・`runtime_recovery={attempts:4, 8192, RECOVERED}`／`CREATED→READY_FOR_IMPLEMENTATION` |
| **★入力間の矛盾** | **★1件 新たに見えた。** ★規則は「上に挙がっているものは取得済として扱え」と言うが、**★`already_satisfied` に何を書くかは計画が自分で決める** ∴ **★計画が「道具は知っている」とだけ書けば、★規則の対象が「道具」になり、★値は対象外になる。** 実際 今回の `already_satisfied` は **★値ではなく道具の話**になった（前回は「基本の一覧は取れている」＝内容寄り、今回は「コマンドは有効だと分かっている」＝道具寄り） |
| **★推定原因** | **★規則が「上に挙がっているもの」を指す語で書かれており、★何を挙げるかを計画側に委ねているため。** ★観測ブロックには値も道具名も両方 在る ∴ ★どちらを「既得」と宣言してもよい構造になっている |
| **★修正候補（1件）** | **★`already_satisfied` の欄の説明に「★観測ブロックに現れた実際の値を、その項目の根拠として1つ以上 含めること」を1文 足す**（★欄の定義のみ。★`STRUCTURED_KEYS` は触らない） |
| **★修正候補のリスク（★空にしない）** | **★「値を書け」は★規則の追加である**（★今回まで我々は「観測を使え」を足さない線を守ってきた）。**★これを足すと、★以後の測定は「値が書かれたか」を prompt が強制した結果になり、★自発的に使ったのかが★測れなくなる。** ∴ **★足すなら「足した以後の測定は自発性を測っていない」と併記する必要が在る。★私は実施していない** |

---

# 4. ★予告の当否

## 4-1. ★設計の予想（P-1〜P-8）
| # | 予想 | 結果 |
|---|---|---|
| **P-1** | 強制語 0→3件 | **★当たり**（`MUST`2 ＋ `Do NOT`1。★`MUST NOT` を含む） |
| **P-2** | `TASK-2DER-E8F8CA7B` | **★当たり**（61字 / sha1 `e8f8ca7b…`・機械抽出・打ち直していない） |
| **P-3** | 分類 `OBSERVE_CURRENT_STATE` | **★当たり** |
| **P-4** | `already_satisfied` は埋まる | **★当たり**（2項目） |
| **P-5** | 母数（対）3〜4 | **★当たり**（対3。★項目は2） |
| **P-6** | ×は0〜1 | **★当たり（★ただし設計自身が「証拠に使うな」と禁じた項目）** |
| **P-7** | 値の引用は予告しない | ★該当なし（**★実測 0件**） |
| **P-8** | **計画は「不足を取る」と書き続ける＝作業は消えない** | **★★外れた。** ★今回の計画は **★取得作業を書いていない**——`steps` は「入力 JSON の schema を定義」「JSON を読む」「パースする」で、**★`ps`/`nvidia-smi` は成果物側に1回も出てこない**。**★データは与えられる前提になった** |

## 4-2. ★私（IMPL）の予想（★投入前に固定・`impl_pre_registration.txt`・記録 18:41:46）
| # | 予想 | 結果 |
|---|---|---|
| **I-1** | `already_satisfied` は埋まる | **★当たり** |
| **I-2** | 母数 項目3〜4 / 対3〜4 | **★半分 外れ**（★対3＝当たり／★項目2＝**下限を下回った**） |
| **I-3** | **計画は `ps` を拡張フィールドで再実行する形を書く** | **★★外れた**（★再実行を書かなかった） |
| **I-4** | 値の引用は0件のまま | **★当たり**（0件） |
| **I-5** | PLAN は成立する | **★当たり** |

**★私の予想も設計の予想も、★同じ所（P-8／I-3）で外れた。** **★2人とも「取得作業は残る」と見ていた。★残らなかった。**

---

# 5. ★私が行った操作（★全件）
```
★実装: build_planner.py 1箇所（1挿入）  ★禁止語の走査を★修正直後・投入前に出した
★運用: webui 再起動 1回（旧 PID 1740645 → 新 PID 2291744 / 18:40:57）
        操作者=IMPL ／ 理由=規則を本番へ反映 ／ ★既存運用 ／ ★2DER の担当に数えない ／ ★run-gate 初期化
★投入: POST /api/submit ★1回（18:41:46。receipt last_recv_at=18:41:46.588849・recv_count 79→80）
★実行: POST /api/run_next ★1回（18:42:07→18:43:36・89.1秒）。★PLAN が出た所で止めた
★★計器の誤りを1件 自己申告: ★道具名の照合を★大小無視にしていなかったため `Docker` を落とし、
   ★母数を 2対 と誤って出した。★直して 3対 に訂正した（★×の件数は 0→1 に変わった）
★していないこと: GENERATE を押していない ／ 語彙表に語を足していない ／ 欄を足していない
                  Ledger・図・(c) の patch に触っていない ／ commit していない ／ テストは0本
```

# 6. ★次に直す1件（★1件だけ・★実施しない）
> **★§3 の「修正候補」と同じ1件＝`already_satisfied` の欄の説明に「★実際の値を根拠として1つ以上 含める」を1文 足す。**
> **★ただし §3 に書いたリスク（★以後は自発性を測れなくなる）を併記して、★採否は設計と MGR が決める。★私は決めない。**

---
*IMPL → 設計/監査（写: MGR / Taka）。D-180/D-181＝`_observation_facts` に RE-ACQUISITION RULE を **1挿入**（1 hunk・禁止語8種すべて0件・`STRUCTURED_KEYS`/`EXECUTABLE_KEYS`/`_plan_prompt`/`validate_plan` は diff 内 0件・文言は逐語）。**規則は prompt に渡った——Observation 側の強制語は 0→3件（`MUST`2＋`Do NOT`1、`MUST NOT` を含む）で、実行して確認した。** **物差しの結果は「★この1回、母数（対）3・母数（項目）2 のうち ×1件」**——×は `docker` が入力スキーマの列挙として出るだけで前後160字に語彙表の語が無いもの。**×14/109 とは母数も経路も違うので引き算していない。母数3では規則が無くても×0〜1 は普通に出る（設計が先に書いたとおり）。** **★設計の P-8 と私の I-3 が同じ所で外れた——2人とも「取得作業は残る」と見ていたが、今回の計画は `ps`/`nvidia-smi` を成果物側に1回も出さず、データは与えられる前提（入力 JSON を読む）になった。** Audit で新たに見えた入力間の矛盾は「**規則は『上に挙がっているもの』を指すが、何を挙げるかは計画側が決める** ∴ 計画が『道具は知っている』と書けば規則の対象が道具になり値は対象外になる」——今回の `already_satisfied` は前回の内容寄りから**道具寄り**に変わった。修正候補は「欄の説明に実際の値を根拠として含めさせる」1件だが、**リスクは「それは規則の追加であり、以後は自発的に使ったのかを測れなくなる」**ため実施していない。**計器の誤りを1件 自己申告——道具名の照合が大小無視でなく `Docker` を落として母数を 2対 と誤り、直して 3対（×は 0→1）に訂正した。** GENERATE は押さず、commit もしていない。担当工程数の差分は 0。*
