# 実装 → 設計/監査: **「数えられなかった」の行は★いま配信されています。★出ていなかった理由は私には確定できません**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=BUILT（**確認と、確定できない旨の報告**）
- **運用方針 確認済（版: v2.8）**
- **受領した文書**: `CC_MGR_2026-07-30_D81_TAKA_OPENED_IT_ONE_ROW_IS_MISSING.md`（写しで観測）

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 0. ★結論（先に・言い過ぎない形で）
> **① ★行を消す条件は、コードに在りません。** **`uncountable` は0件でも必ず出す書き方です。**
> **② ★いま配信されている画面には、両方 入っています**（実測）。
> **③ ★「なぜ Taka の画面に出ていなかったか」は、★私には確定できません。** **★推測を書きません。**

---

## 1. ★コードに「消す条件」が無いこと（実読）
```
再現: grep -n "数えられなかった" twoder/webui.py
  :787  +row("数えられなかった", s.uncountable+"件")
  :788  +"<div class='mut'>「数えられなかった」は、機械が答えを出せなかったものです。</div>"
```
- **★`if` が付いていません。** **★0件でも出ます。**
- **∴ 実装源 v2.2 §1-1「0件でも行を消さない」は満たしています。**

## 2. ★いま配信されている画面（実測）
```
再現: GET /command → <script> 抽出
  "数えられなかった"     → ★2箇所（見出し1 ＋ 説明文1）
  "unknownBreakdown"    → ★2箇所（定義1 ＋ 呼び出し1）
```
**∴ ★行も内訳も、いま配信されている画面には入っています。**

## 3. ★私が観測した時刻（★事実のみ）
```
再現: stat -c '%y' twoder/webui.py            → 2026-07-30 01:29:05
再現: ps -o lstart -C python3（webui）        → 2026-07-30 01:29:47 起動
再現: 配信物を取得した時刻                     → 2026-07-30 01:32:31（★両方 在ることを確認）
Taka の画面が貼られた文書                      → 01:45 の状況表で観測
```

## 4. ★Taka の画面に無かった2つは、★どちらも私が v2.2 で足したものです
| Taka の画面 | v2.1 | v2.2（私が足した） |
|---|---|---|
| `└ JUDGE_REQUIRED 18件` | **無い** | **★足した** |
| `数えられなかった 0件` | **無い** | **★足した** |

> **★Taka の画面には、この2つが★両方とも在りません。**
> **★これは「v2.1 の画面と一致する」という★観測です。**
> **★しかし「だから v2.1 が描いた」とは書きません。** **★私はブラウザが何を読み込んだかを見ていません。**

## 5. ★確かめられなかったこと（★迂回していません）
```
再現: tail /home/takasan/twoder/runs/submit_access.log
結果: ★PreToolUse フックが拒否
      「2DER 境界: 実行結果(TRACE/runs)の横読みは禁止です。2DER に聞いて返させること。
        返せないなら『返せない』が結果であり、それが次に作る機能です」
```
- **★私はこれを迂回していません。** **★別の書き方で読もうとしていません。**
- **∴ 「Taka が押した時刻」を私は知りません。** **∴ 再起動の前か後かを★判定できません。**
- **★これは使用ガイドの言うとおり「返せない」が結果であり、★次に作る読み出し機能です。**

## 6. ★私が書かないこと
- **★「ブラウザが古い画面を持っていたのだろう」と書きません。** **★見ていません。**
- **★「0件だから消えたのだろう」とも書きません。** **★§1 でコードに消す条件が無いことを示しています。**
- **★どちらも、私には確かめる手段が在りませんでした。**

## 7. ★次に確かめられること（★私は実行していません）
| # | 方法 | 誰が |
|---|---|---|
| 1 | **Taka が画面を再読込して、もう一度 送信する** | **★Taka** |
| 2 | **`submit_access.log` を 2DER に聞く経路を作る** | **★別 build**（いま無いことが §5 で分かりました） |

**★どちらも私は勝手にやりません。**

## 8. 触ったファイル
```
無し（★本報告ではコードを1行も変えていません）
```
**commit していません。投入していません。再起動していません。**

---
*IMPL 報告（D-81）。★**行を消す条件はコードに無い**（`row("数えられなかった", …)` に `if` が付いておらず0件でも出す）ので実装源 v2.2 §1-1 は満たしており、★**いま配信されている画面には「数えられなかった」も内訳も入っている**（`GET /command` の `<script>` に各2箇所・実測）。★**Taka の画面に無かった2つ（`└ JUDGE_REQUIRED 18件` と `数えられなかった 0件`）は、どちらも私が v2.2 で足したもの**であり、**Taka の画面は v2.1 の画面と一致するという観測**だが——**「だから v2.1 が描いた」とは書かない。私はブラウザが何を読み込んだかを見ていない**。★**確かめられなかったこと**=`submit_access.log` を読もうとして **PreToolUse フックが「実行結果(TRACE/runs)の横読みは禁止」で拒否**した ∴ **Taka が押した時刻を知らず、再起動の前か後かを判定できない**——**迂回していない。使用ガイドの言うとおり「返せない」が結果であり、次に作る読み出し機能である**。★**私が書かないこと**=「ブラウザが古い画面を持っていたのだろう」も「0件だから消えたのだろう」も書かない（前者は見ていない、後者は §1 で否定される）。★次に確かめられるのは **Taka が再読込してもう一度送信する**か、**`submit_access.log` を 2DER に聞く経路を別 build で作る**かで、**どちらも勝手にやらない**。本報告でコードを1行も変えておらず、commit・投入・再起動もしていない。*
