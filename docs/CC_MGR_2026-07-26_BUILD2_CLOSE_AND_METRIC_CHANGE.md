# CC 管理(MGR): Build 2 を区切る + **主指標の変更を承認（一致率 → 遠隔取り違え率）** + 次スライス（HANDOFF/裁定）

- `BUILD_ROLE: 参照` / 宛: DESIGN/AUDIT(CC-α) / IMPL(coder) / 写: Taka / 発: MGR / 2026-07-26 21:45 / TYPE=HANDOFF
- 対象: `CC_DESIGN_2026-07-26_BUILD2_REAUDIT_AND_BROKEN_CRITERION.md`

## 1. 区切り（監査を通ったもののみ）
- **確定**: **遠隔の取り違えは 11/11 で routing ごと反転する**（問い合わせ規則に非依存の構造的結果）。
- **未決**: 近縁側（IMPL 自己申告の交絡・CC-α も `build_query` の実読で交絡の実在を確認）。**結論に使わない。**
- **未検証**: 長文での意図調べの崩れ／再開経路の実データ（記録1件）。
- **本番コード変更ゼロ**（`preflight_gate` の根の修正は条件未成立のため不要のまま）。

## 2. ★裁定 — 主指標の変更を**承認する**（MGR 権限で決める。Taka には報告のみ）
> **意図調べに必要なのは「一致率を上げること」ではなく「遠隔の取り違えをしないこと」である。**

**承認理由**: (i) 遠隔側は**問い合わせ規則に依存しない構造的結果**であり、本日唯一確定した知見である。(ii) 近縁の取り違えが routing を壊さない以上、**一致率の残り 0.29 の大半は routing に影響しない差**である可能性が高い。(iii) これは目的の変更ではなく**測り方の変更**であり、目的（接地しない前提で走り出すのを止める）は不変。

**条件（緩めない）**:
1. **遠隔の定義を、私（設計）が置いた組ではなく `routing の分岐点`（probe/defer で止まる vs action で動く）から機械的に導くこと。** CC-α 自身が §4 末尾でこの弱点を書いている。**分岐点自体の妥当性は別問題として残す。**
2. **一致率を捨てない。** 併記して測り続ける（後から見直せる形を残す）。**過去の測定を新指標で見直すと結論が変わり得る**と CC-α が書いた——**変わるなら変わったと書くこと。都合の良い方だけ採らない。**
3. **報告は BROKEN-UNSAFE と BROKEN-WASTEFUL を必ず分けて出す**（止まるべき時に動く／動くべき時に止まる は対称でない）。**DIVERGENT-CONTENT は「壊れた」に数えない**——CC-α が「今は定義しない。定義すれば後から都合よく決めることになる」としたのを支持する。

## 3. 次スライス（優先順）
1. **★長文での意図調べの崩れを測る**（下流でなく **LLM 側**を、長文で、**対照設計**で）。**現時点で最大の未検証領域**であり、IMPL と CC-α の見立てが一致している。既知の最大弱点（**主張された文脈を検証せず受け入れる**・独立3回の目撃）は**長文で悪化する理由はあっても改善する理由がない**。実発話の **42% が400字超・平均3446字**＝実運用そのもの。
   - **計器規律**: LLM を挟むので **run 間ノイズ ≈0.10**。**分布 or paired。単発の数値で言わない。**
   - **飽和指標を使わない**（`k=8` で頭打ちの指標は劣化に鈍感＝「壊れていない証拠」にならない）。
2. **近縁の規則変動感度**（規則を消すのでなく**振る**・付加語は**事前に決めて記録し測定後に動かさない**）。**IMPL に自分の計器の規則を選ばせない——付加語は DESIGN が指定する。**
3. 再開経路の実データ検証は **記録が貯まるまで測らない**（(a) は前向きのみ）。

## 4. CC-α の自己申告について【MGR】
「**目録から名前で選び、意味を読まない癖**」（`acquire` / DE の `note` — 4度目）を自分で類型化し、恒久規律（**一覧から選んで spec に書く時は実体を1つずつ読む**）を立てたことを受領する。**この記録を消さないこと。** 本日 CC-α は裁定同士の干渉3回・計器誤り複数を自分で数えている——**数えていることが機構である。**

---
*MGR。Build 2 を区切る（確定=遠隔 11/11・未決=近縁の交絡・未検証=長文と再開経路）。★主指標を「一致率」から「遠隔取り違え率」へ変更することを MGR 権限で承認（条件=遠隔を分岐点から機械導出・一致率は併記継続・UNSAFE/WASTEFUL を分けて報告）。次スライスは長文での意図調べの崩れ（LLM 側・対照設計・飽和指標を使わない）。*
