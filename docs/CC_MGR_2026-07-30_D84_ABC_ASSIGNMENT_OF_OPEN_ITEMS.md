# CC 管理(MGR) → 設計/監査(CC-α): **★A/B/C を先に判定する（Taka 原則）。★いま開いている案件を振った**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **典拠**: `docs/TAKA_2026-07-30_ABC_TRIAGE_PRINCIPLE.md`（★Taka 逐語・全文）

## 1. ★原則（★手を動かす前に判定する）
```
A = Phase2 本来の責務（完了条件に直接必要）  → ★実装する
B = Phase2 残務（必ず終わらせる未完了項目）  → ★実装する。Aと分けて管理
C = Phase3候補（改善案・方向ずれ・運営改善・勘定科目・Claude Code 対策）
    → ★実装しない。★設計も議論もしない。★登記だけ。★件数だけ管理
```
**★C を見つけたことは、次の作業へ移る理由にならない。**

## 2. ★いま開いている案件の判定（★私が振った）
### 【A】★いま実装する
| # | 案件 | 根拠 |
|---|---|---|
| **A-1** | **18件の材料取得**（★表示成立に必要な範囲だけ） | **Taka:「現在の表示と Phase2 残件を成立させるために必要な範囲に限定」** |
| **A-2** | **表示成立に必要な★裁定候補だけを出す** | 同上 |
| **A-3** | **155行→5行 の完了**（★コードは commit 済。★残るのは Taka の実画面確認） | Phase2 の中心 |
| **A-4** | **実行構造の資料の同期**（`webui.py` が変わった） | **★本線の記録維持。★これが古いと次の判断が狂う** |

### 【B】★Phase2 残務（★Aの後）
**Taka の残件7件のうち未達分。** **★件数と現況は★資料（`2DER_EXECUTION_ARCHITECTURE.json`）から出すこと。★私は記憶で数えない。**
> **★あなたに依頼: ★資料から B の一覧を出す**（7件それぞれ 済／部分／未 と、対応する Gap id）。**★A-1〜A-4 が終わってから。**

### 【C】★Phase3候補（★登記だけ・件数だけ）
**本日 出たもの（★内容は既に各文書に保存済。★ここでは指し示すだけ）:**
| # | |
|---|---|
| C-1 | **勘定科目の設計**（`G-62` / `G-64`〜`G-68`） |
| C-2 | **方向ずれの記録・検出・本線復帰の管理**（事例001 保存済） |
| C-3 | **`claude_barrier` の名前衝突**（同じ名前が2つを指す） |
| C-4 | **`ADJUDICATOR` に担当者が割り当てられていない**（18件の根） |
| C-5 | **`G-70` / `G-71` / `G-73` / `G-74`**（受信記録・JS 構文検査・一覧用の短い名前・テスト手順が版管理の外） |
| C-6 | **Claude Code 対策**（★違反・逸脱・誤分類を帳簿と機械で検出する） |

**★C は これ以上 掘らない。** **★件数のみ: 現在 6件。**
> **★C の新規は「1行 足して件数を更新する」だけにすること。** **★理由も対策も書かない。**

## 3. ★以後の書き方（3者すべて）
**★文書の冒頭に `【A】` `【B】` `【C】` を1つ書く。** **★判定より先に手を動かさない。**
**★1つの文書に A と C を混ぜない。** **★混ざったら C を切り出して登記だけにする。**

## 4. ★次の1手（★A-1 から）
**★18件の材料取得を始めること**（D-82 §3 の10項目・D-83 の7点の範囲）。
**★表示に要らない項目は「取れない」と書いて先へ進む。** **★止まってよい場所は D-82 §3-2 のまま。**

---
*MGR。★**原則**=A（Phase2 本来の責務・完了条件に直接必要）と B（Phase2 残務）は**実装する**、C（Phase3候補: 改善案・方向ずれ・運営改善・勘定科目・Claude Code 対策）は**実装も設計も議論もせず登記だけ・件数だけ管理**——**C を見つけたことは次の作業へ移る理由にならない**。★**いま開いている案件の判定**=【A】**A-1 18件の材料取得（表示成立に必要な範囲だけ）／A-2 表示成立に必要な裁定候補だけを出す／A-3 155行→5行 の完了（コードは commit 済・残るのは Taka の実画面確認）／A-4 実行構造の資料の同期（`webui.py` が変わった。本線の記録維持で、これが古いと次の判断が狂う）**、【B】**Taka の残件7件のうち未達分**（**件数と現況は資料から出す。MGR は記憶で数えない**。CC-α に依頼: **資料から B の一覧を出す**——7件それぞれ 済／部分／未 と対応する Gap id。**A-1〜A-4 が終わってから**）、【C】**Phase3候補6件**（勘定科目／方向ずれ管理／`claude_barrier` の名前衝突／`ADJUDICATOR` に担当者が割り当てられていない／`G-70`・`G-71`・`G-73`・`G-74`／Claude Code 対策）——**これ以上 掘らず件数のみ管理し、C の新規は「1行 足して件数を更新する」だけで理由も対策も書かない**。★**以後の書き方**=**文書の冒頭に `【A】``【B】``【C】` を1つ書き、判定より先に手を動かさない。1つの文書に A と C を混ぜず、混ざったら C を切り出して登記だけにする**。★**次の1手は A-1**=**18件の材料取得**（D-82 §3 の10項目・D-83 の7点の範囲）で、**表示に要らない項目は「取れない」と書いて先へ進み、止まってよい場所は D-82 §3-2 のまま**。*
