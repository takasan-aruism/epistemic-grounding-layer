# 設計/監査 → MGR（写: Taka / IMPL）: **D-55 監査 — ★受入は満たされている。ただし IMPL の自己申告した判断に★1点、文言が偽になる場合が在る**

- `BUILD_ROLE: 参照`（**監査。投入していない・コードを1行も変えていない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_IMPL_2026-07-29_D55_DROPPED_BEFORE_UI_BUILT.md`

## 0. ★結論
> **① 受入は私の実測でも満たされている。**
> **② ★IMPL が自己申告した判断（(c) の範囲を広げた）は★振る舞いとしては正しい。**
> **③ ★しかし文言が、その場合に★偽になる。** **「run_id が記録に無い」と書くが、`run_id` は在る。**
> **④ ★これは私の SPEC の欠落である。** **私は (c) を1通りしか定義しなかった。**

---

## 1. ★実測（front door と差分）
### 1-1. 配信されている画面に文言が在るか（★私が `/command` を実取得した）
```
再現: GET /command （認証つき）
  併記文言「これは★記録できた取りこぼしの件数である。」            ★在り
  「件数そのものが残らない（G-38）」                                ★在り
  「記録できた取りこぼし: 」                                        ★在り
  「取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）」★在り
  「取得した範囲での合計である」（truncated 併記）                  ★在り
```
### 1-2. Python 側を変えていないか
```
再現: git diff --stat -- webui.py → 22 insertions / 6 deletions
再現: git diff -- webui.py | grep "^@@" → 724 / 765 / 787 / 802 行目
再現: grep -n "^PAGE = " webui.py → ★617
∴ 変更4箇所はすべて PAGE(JS)の中。★Python 側は1行も変わっていない。
```

### 1-3. ★色 — 私の計器が粗く、危うく誤報した（自己申告）
```
私が最初に打った: /command の HTML 全文に "pill bad" が在るか → ★在り
★これは誤りの読み方である。
再現: grep -n "pill bad" webui.py → :655 guardCard / :662 BLOCKED カード（★どちらも既存）
再現: git diff -- webui.py | grep -c "pill bad" → ★0
∴ D-55 は色を1つも足していない。★在るのは D-55 以前からのものである。
```
> **★私は「ページ全体に在るか」を測り、「本 build が足したか」を測っていなかった。**
> **★IMPL の測り方（差分で数える）が正しい。** **私の測り方が粗かった。**
> **★本日5回目の、私の計器の問題である**（`head -8` / 存在しないパス / `git diff HEAD~1` / cwd / 本件）。

---

## 2. ★IMPL の自己申告への判定（本監査の中心）
**IMPL の申告（逐語）**
```
実装源の (c) は「run_id が無く run を引けない」です。
★run_id は在るが /api/resolve が引けない場合も、私は (c) と同じ表示にしました。
理由: どちらも「取得できない」であり、0件と出すべきでないのは同じだからです。
★ただし文言は「この依頼の run_id が記録に無い」のままです。
★run_id が在るのにこの文言が出る余地があります。★これは私の判断です。
```
**実装の実体（実読）**
```javascript
function droppedBlock(rec){
  if(!rec) return "…取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）…";
```
| 項目 | 判定 |
|---|---|
| **振る舞い（0件と出さない）** | **★正しい。** 「取れない」を「0」と出さないのは本 build の中心である |
| **★文言** | **★偽になりうる。** `run_id` が在って `/api/resolve` が失敗した場合、**「run_id が記録に無い」は★事実に反する** |
| **誰の欠落か** | **★私である。** SPEC の (c) は「run_id が無く run を引けない」の1通りしか定義していない |

> **★これは本 build の趣旨に正面から反する。**
> **本 build は「0 を安心の材料にさせない」＝★誤解を生む表示を禁じる build である。**
> **∴ その build が、★別の誤解（「記録に無い」）を生む文言を持つのは通せない。**
> **★ただし私は直さない。** **文言は MGR 逐語であり、変えるなら★MGR の裁定が要る。**

### 2-1. ★MGR に上げる（私は決めない）
| 案 | 内容 |
|---|---|
| **(A)** | **文言を原因に踏み込まない形にする**（例:「取りこぼしの件数は取得できない」だけにし、括弧の理由を落とす） |
| **(B)** | **2通りに分ける**（`run_id` 無し／`run_id` 在るが引けない） |
| (C) | 現状のまま。**★`run_id` が在って引けない場合が実在するかを先に測る** |

**【設計:CC-α】★(C) を先に置くことを推す。** **理由: その場合が★実在するかを私は確かめていない。**
**★実在しないなら、直す前に「実在しない」と書く方が正確である。** **本日9回「既存の道が既に在った」を数えている。**
**★ただし推すだけで決めない。**

---

## 3. ★IMPL が投入前に自分で直した1件（記録する）
```
IMPL:「取りこぼしを常時表示にすると、D-49 の『欠損・失敗の記録なし』の分岐が二度と成立しなくなります。
      ∴ 組み立てを分け、記録なしの表示を残しています。
      ★一度は壊す書き方をしましたが、投入前に気づいて直しています。」
```
> **★これは「壊した」ではなく「壊す前に気づいた」である。** **そして★自分から書いた。**
> **★D-49 で自分が作った表示を、D-55 で自分が壊しかけ、自分で気づいた。** **記録が効いた形である。**

## 4. ★受入の判定
| # | 条件 | 判定 |
|---|---|---|
| 1 | (b) 0件の表示 | **★満たされた**（`【実】` 文言を画面から取得） |
| 2 | 併記文言が MGR 逐語と一致 | **★満たされた**（`【実】`） |
| 3 | (c)「取得できない」・0件と出さない | **★満たされた**（`【実】`）。**★ただし §2 の文言問題が残る** |
| 4 | `ok`/`bad` を使っていない | **★満たされた**（差分で0。§1-3） |
| 5 | 既存が不変 | **`【伝聞】`**（sha256 は IMPL 実測。私は再実行していない） |
| 6 | 非回帰 91/7 | **`【伝聞】`** |
| — | **(a)（非0）の表示** | **★検証していない**（実例0件・作らない）。**`G-61` と同じ扱いで未検証のまま登記** |

## 5. ★未確認
| # | 未確認 | 誰が・いつ |
|---|---|---|
| 1 | **`run_id` が在るのに `/api/resolve` が引けない場合が★実在するか** | **★これが §2 の裁定に効く1点である。** CC-α / MGR の指示があれば |
| 2 | 受入5・6 を私は再実行していない | CC-α / 要請時 |
| 3 | **画面をブラウザで見ていない**（HTML の文字列一致で判定した） | D-45 から継続 |
| 4 | (a) の表示（実例0件） | **作らない** |

---
*CC-α D-55 監査。★結論=①受入は実測でも満たされている ②**IMPL が自己申告した判断（(c) の範囲を `run_id` が在るが引けない場合まで広げた）は振る舞いとしては正しい** ③**しかし文言がその場合に偽になる**（「run_id が記録に無い」と書くが `run_id` は在る）④**これは私の SPEC の欠落**（(c) を1通りしか定義しなかった）。★実測=`/command` を実取得して併記文言・`G-38` 参照・(a)(b) 文言・(c) 文言・`truncated` 併記のすべてが在ることを確認。`git diff` は 22/6 で `@@` は 724/765/787/802 行目、`PAGE` は 617 行目 ∴ **Python 側は1行も変わっていない**。★色について**私の計器が粗く危うく誤報した**=`/command` の HTML 全文に `pill bad` が在ったので「在り」と出したが、`grep -n "pill bad" webui.py` は `:655 guardCard` と `:662 BLOCKED` の**既存2箇所**で、`git diff | grep -c "pill bad"` は **0** ∴ D-55 は色を1つも足していない。**私は「ページ全体に在るか」を測り「本 build が足したか」を測っていなかった。IMPL の測り方（差分で数える）が正しく私の測り方が粗かった——本日5回目の私の計器の問題**。★本監査の中心=IMPL の申告どおり文言は「この依頼の run_id が記録に無い」のままで、`run_id` が在って `/api/resolve` が失敗した場合に**事実に反する** ∴ **「0 を安心の材料にさせない」build が別の誤解を生む文言を持つのは通せない**が、文言は MGR 逐語なので**私は直さず裁定を仰ぐ**——(A) 原因に踏み込まない文言にする／(B) 2通りに分ける／(C) 現状のまま、**ただし `run_id` が在って引けない場合が実在するかを先に測る**——**CC-α は (C) を先に置くことを推す**（その場合が実在するかを確かめておらず、実在しないなら直す前に「実在しない」と書く方が正確。本日9回「既存の道が既に在った」を数えている）が推すだけで決めない。★IMPL が投入前に自分で直した1件を記録=取りこぼしの常時表示で D-49 の「欠損・失敗の記録なし」分岐が成立しなくなるところを、**一度は壊す書き方をしたが投入前に気づいて直し、自分から書いた**（自分が作った表示を自分で壊しかけ自分で気づいた＝記録が効いた形）。★受入は1〜4 が `【実】`、5・6 は `【伝聞】`、**(a) は検証していない**（実例0件・作らない・`G-61` と同じ扱いで未検証のまま登記）。★未確認=**`run_id` が在るのに引けない場合が実在するか（§2 の裁定に効く1点）**／受入5・6 は再実行していない／画面をブラウザで見ていない（HTML の文字列一致で判定）／(a) は作らない。*
