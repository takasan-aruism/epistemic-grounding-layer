# 宛: 設計 / 監査 ―― EVO-0063 受入①の実測と、締めが打てない件

## 1. 受入①（emit の呼び手を機械で数える）

**★鍵を明示する** ―― 5 repo の `.py` ／ 除外 `.git` `__pycache__` `node_modules` `runs/` `docs/`。
**構文木（`ast`）で全列挙**。grep による人手列挙をしていない。

```
呼び手ファイル母数     35
emit 呼出箇所母数      65
task_id を 渡している   17
task_id を 渡していない 48   （74%）
```

**登録時の「13ファイル・18箇所」とは一致しない。**
ただし**登録時の鍵が台帳に書かれていない**ため、増減とは断定しない。
`twoder` だけに絞っても **17ファイル・43箇所**で、やはり一致しない。**鍵が違えば比較そのものが無効。**

## 2. 突き合わせ（経路表・etrace の測定と）

| 経路表の測定 | emit の走査 | 一致 |
|---|---|---|
| S01-S06 が task に紐づかない | `submit.py` 0/5 ／ `ds/phase0` 0/1 ／ `ds/phase1` 0/1 ／ `rri/request_type` 0/1 ／ `rri/intent_record` 0/2 | **一致** |
| S07 が未観測 | `egl/de_admission.py` に emit が **2箇所**（task_id 0） | **精密化** ―― 口が無いのではなく**記録が出ていない** |
| ①届いている 6区間 | `manager_v0` 3/3 ／ `domain_dw` 4/5 ／ `generate_via_runner` 2/2 ／ `workcell` 1/1 が task_id 有 | **一致** |

**独立した2つの計器が同じ結論を指した**（自己一致ではない）。

## 3. 受入②③④

- ② 3値 = **①届いている 6 ／ ②記録は在るが task に紐づかない 9 ／ ③双方で未観測 3**（母数18）
- ③ 母数を明記 = **経路表18区間 ／ emit 65箇所**（0件も0件と書く）
- ④ **新台帳 0 ／ 新口 0 ／ 新 Manager 0 ／ LLM 0回**

## 4. ★締めが打てない（新しい欠陥 F）

**EVO-0063 を台帳上で閉じられなかった。**

front door から `/api/submit` で記録を投げた結果:

```
task = TASK-2DER-69F11310 ／ request_type = OBSERVE_CURRENT_STATE
→ ITEM-2DER-EVO-0063 には 付かなかった
  status = PROPOSED のまま ／ history 4件は すべて 今日より前
  この task に item へ結ぶ欄は ★無し（本文に EVO-0063 の語は在るが、語は結び目にならない）
```

他の口も当たった（**探した範囲を書く**）:

| 口 | 可否 |
|---|---|
| `/api/receipt` | 読み取り専用 |
| `/api/approve` | `APPROVE_ACCOUNT` 限定 |
| `/api/pending_approvals` | 読み取り専用 |
| `/api/ingest` | `(task_id, actor_role, result)` ＝ **task 単位**であり item ではない |
| `/command` | 旧 UI の HTML を返すだけ |

**∴ 欠陥 F ―― 台帳の item へ記録・状態を足す口が front door に無い（少なくとも MGR が引ける範囲に）。**
一方で item が DONE になる経路は在る（今夜 `EVO-0074` が DONE）ので、
**恐らく「item に紐づいた task が完了する」形**だが、**その紐づけを作る口が引けない。**

**私は台帳を直接触らない**（Taka の常設制約・フックも拒否する）ので、ここで止める。

## 5. 現状

- **測定は完了した**（受入①〜④ をすべて満たす）
- **台帳上の締めは打てていない** ―― `ITEM-2DER-EVO-0063` は **PROPOSED のまま**
- A〜E は**直していない**。F を加えて **6件**が EVO-0063 の発見。

**★注意**: 今回作った集計は **Claude の使い捨てスクリプト**であり、
**General Manager の完成機能として数えない**（Taka 明示）。

## 6. Taka へ

**締めの打ち方を1つ決めてほしい。** 私の側からは打てない。

```
A. 2DER 側に「item へ記録を足す口」を作る       ← 新口1つ（原則に反する可能性）
B. item に紐づく task を立てる口を使う          ← 口の名前が私には引けない
C. Taka が台帳側で EVO-0063 を DONE にする      ← 口を増やさない
```

私の見立ては **C**（今回は口を増やさず閉じ、F は次の本線候補として台帳へ）。
ただし**これは口の設計に関わる**ため、私が決めない。
