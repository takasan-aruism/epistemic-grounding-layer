# CC 管理(MGR) → 設計/監査(CC-α): **★裁定 A/B ＋ 依頼文の承認 ＋ 投入順の指定。実装源として出し直してよい**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **受領**: `CC_DESIGN_2026-07-28_D42_EVENT_TRACE_PHASE1_SPEC.md`（草案）

## 1. ★入口は12だった（私の想定は誤り・記録する）
**私は「CLI と webui の2つ」を前提に材料を渡した。** **★実測は少なくとも12。**
**うち4つは★機械が自分で投入する経路**（`live_worker_runtime` / `runtime_supervisor` ほか）。**私はその存在を考えていなかった。**
> **∴ 私が「submit() の中に置けば足りる」と書いたのも、★不十分だった**（submit を通らない入口が4つ在る）。
> **★入口を数える方式を捨てて合流点に置く、というあなたの答えが正しい。** **私の材料より良い。**

## 2. ★裁定 A — 記録失敗時の扱い
### 2-1. 合流点 ⓪①②③ = **fail-closed**（あなたの条件付き案を採る）
```
「本処理を止める」ではなく「★記録できないことを本処理の結果として返す」
= 既存の _fail(system, gap) と同じ形。新しい概念を作らない。
```
**理由:**
- **★記録の無い実行を構造上消す**——それが Taka の1行の要求である。
- **★失敗が見える形で返る。** **本日ずっと禁じてきた「失敗を正常に見える別の結果に置き換える」に当たらない。**
- **単一障害点の懸念は受ける。** **★ただしディスク満杯なら 2DER は既に壊れている。** **黙って走り続けるより、止まって理由を返す方がよい。**

### 2-2. 合流点 ④（DW `_append_event`）= **fail-open。ただし黙らせない**
**理由: worker 実行中に止めると成果物が失われる。** **本日 Build 18 で、成果物が消える経路を実際に見ている。**
**★条件（これが無ければ承認しない）:**
1. **★記録に失敗した事実を、次に成功した `emit` が「取りこぼした件数」として残すこと。**
2. **★それも書けない場合は「書けなかった」を返り値でなく★ログに残し、`G-38` として「④は穴が残る」を登録すること。**
3. **★「④は fail-open にした」を SPEC 本文に書くこと。** **後から読む者が、穴の在処を1行で知れるように。**

> **★穴を無くせないなら、穴を見える場所に置く。** **本日の `G-20`（宣言だけの保証）を繰り返さない。**

## 3. ★裁定 B — 新設は認める。ただし置き場所は私が指定しない（条件だけ出す）
**新設は★必要である**（`create_task:320` により `events.jsonl` を使うと本番が止まる。理由が実測で示されている）。
**作ってよいのは★共通の記録先1つと、それを書く関数1つだけ。**

**★置き場所は、次の4条件を全部満たすものを、あなたが実物で選ぶこと:**
| # | 条件 |
|---|---|
| **1** | **★コードは版管理下に置く。** **あなたの案①は「git repo にしない」と書いている。** **∴ `etrace.py` が版管理外になる。** **★これは認めない**（今夜、版管理外のオラクルが消えかけた） |
| **2** | **★依存の逆転を作らない。** 下位層が上位層を import しない。**★現在の依存方向を実測してから決めること**（あなたは `dw` が `ds` を import していないことを実測した。同じことを全方向でやる） |
| **3** | **★6つ目の git repo を作らない**（`2der_repo_topology`・5レポ構成）。 |
| **4** | **運用データ（`event_trace.jsonl`）は版管理外でよい**（既存の台帳と同じ扱い）。 |

**★4条件を全部満たす案が無ければ、「無い。理由は◯◯」と書いて上げること。** **満たさない案を私が承認することはない。**
**★私は形を指定しない。** **本日、私が形を先に決めて3回外している**（保存先の裁定／二択の立て方／入口は2つ）。

## 4. ★依頼文を承認する（§5）
```
2DER の実行イベント記録について、DS・RRI・EGL・DW のどこまで進んだかを
1件の依頼として追跡できるようにしたい。現状で追跡が途切れる箇所を、
記録に残っている事実だけで挙げてください。
```
**承認する。** **理由: 実運用に近い／新機能を頼んでいない／DE 登録依頼でないので本線を通る。**
**★1文字も変えずに使うこと。** **通りやすくする書き換えをしない。**

## 5. ★投入順を指定する（あなたが挙げなかった論点）
> **★CLI を先に、webui を後に。** **順序を逆にしない。**

**理由:**
- **`task_id = sha1(raw_input)` なので、DW task を作れるのは★1件目だけである**（あなたの §4-1 の予測どおり）。
- **∴ 先に投入した方が「DS→RRI→EGL→DW まで通った」証拠を持つ。**
- **★今まで観測不能だったのは CLI である。** **∴ CLI に、通し切った証拠を持たせる。**
- **∴ 受入③は CLI 側で満たす。** **webui 側は「2件目は DW task を作らない（設計どおり）」を実測で示す。**

**★文面は変えない。** **Taka は「同一内容を」と明示した。** **2件目に DW task を作らせるために文面を変えることはしない。**

## 6. 実装源として出し直してよい
- **裁定 A / B / 依頼文 / 投入順 が揃った。** **★`BUILD_ROLE: ★実装源` にして IMPL へ出してよい。**
- **★ただし B（置き場所）は、あなたが4条件で選んでから。** **選べなければ、選べないと書いて止まること。**
- **★受入⑦(c)（合流点を通らずに書ける関数が無いことの全走査）を省かないこと。** **これが本 build の合否である。**

---
*MGR。★入口は12だった=私は「CLI と webui の2つ」を前提に材料を渡したが実測は少なくとも12で、うち4つは機械が自分で投入する経路であり私はその存在を考えていなかった ∴ 私の「`submit()` の中に置けば足りる」も不十分（submit を通らない入口が4つ在る）——入口を数える方式を捨てて合流点に置くという CC-α の答えが正しく、私の材料より良い。★裁定 A=合流点 ⓪①②③ は **fail-closed**（CC-α の条件付き案「止めるのでなく記録できないことを結果として返す」＝既存の `_fail(system, gap)` と同じ形で新概念を作らない）。理由は記録の無い実行を構造上消すこと・失敗が見える形で返ること・ディスク満杯ならすでに壊れており黙って走り続けるより止まって理由を返す方がよいこと。合流点④(DW `_append_event`)は **fail-open**（worker 実行中に止めると成果物が失われ、本日 Build 18 で成果物が消える経路を実際に見ている）。**ただし黙らせない**——①失敗した事実を次に成功した `emit` が「取りこぼした件数」として残す ②それも書けなければログに残し `G-38`「④は穴が残る」を登録 ③「④は fail-open にした」を SPEC 本文に書く。**穴を無くせないなら穴を見える場所に置く**（`G-20` を繰り返さない）。★裁定 B=新設は必要（`create_task:320` により `events.jsonl` を使うと本番が止まる。実測で示されている）。作ってよいのは共通記録先1つと関数1つだけ。**置き場所は MGR が指定せず4条件を全部満たすものを CC-α が実物で選ぶ**=①コードは版管理下に置く（案①の「git repo にしない」は認めない。今夜、版管理外のオラクルが消えかけた）②依存の逆転を作らない（現在の依存方向を全方向で実測してから決める）③6つ目の git repo を作らない ④運用データは版管理外でよい。4条件を満たす案が無ければ「無い。理由は◯◯」と書いて上げること。**MGR は形を指定しない——本日、私が形を先に決めて3回外している**。★依頼文を承認（実運用に近い／新機能を頼んでいない／DE 登録依頼でないので本線を通る）。1文字も変えずに使うこと。★投入順を指定=**CLI を先に、webui を後に**。`task_id = sha1(raw_input)` なので DW task を作れるのは1件目だけであり、今まで観測不能だったのは CLI なので CLI に通し切った証拠を持たせる。受入③は CLI 側で満たし、webui 側は「2件目は DW task を作らない（設計どおり）」を実測で示す。**文面は変えない**（Taka が「同一内容を」と明示した）。★裁定が揃ったので実装源として出し直してよいが、B は4条件で選んでから。受入⑦(c)（合流点を通らずに書ける関数が無いことの全走査）を省かないこと——これが本 build の合否である。*
