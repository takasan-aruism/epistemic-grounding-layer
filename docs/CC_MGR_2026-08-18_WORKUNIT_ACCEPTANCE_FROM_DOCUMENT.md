# 宛: 設計 / 監査 ―― ② WorkUnit の completion / required_tests を文書由来へ移した実測

## 1. 着手前の実測（私の言葉がどれだけ残っていたか）

```
WorkUnit                87 件
completion の異なり      2 文（81件 / 6件）  ← 両方とも私が書いた
required_tests の異なり  2 通り = completion のコピー
```

`assemble_work_unit` の仕様が `required_tests = [completion]` のため、**独立した情報は 0** だった。
「②は半分」ではなく「中身は私の2文」が正確。

## 2. 材料の分母（先に数えた）

```
source の異なり 85
  封印試験あり     59   ← 決定論で移せる
  試験の記述なし   21
  ファイルが無い    4   ← source が実在しない（別の欠陥）
  test 関数だけ     1
```

## 3. 作った部品（すべて 2DER 製・私は1行も書いていない）

| 部品 | task | 状態 |
|---|---|---|
| `extract_test_names` | TASK-2DER-1B79DD10 | COMPLETE・置かれ commit |
| `assemble_acceptance` | TASK-2DER-D9352FBE | COMPLETE・置かれ commit |
| `assemble_work_unit_v2` | TASK-2DER-6F23C584 経由 | COMPLETE・置かれ commit |
| `extract_sealed_test_names`（厳密版） | TASK-2DER-7C7AE062 | **JUDGE_REQUIRED で停止** |

## 4. 通した結果（87件・本番の部品で）

| 指標 | 前 | 後 |
|---|---|---|
| completion の異なり | 2 | **13** |
| required_tests の異なり | 2 | **48** |
| 機械が通した件数 | ― | **87 / 87**（例外 0） |
| 私の日本語が残る欄 | completion 全件 | **0** |

`completion` の内訳: `NO_COMPLETION_IN_DOCUMENT` 29 ／ `TESTS_ALL_PASS:12` 15 ／ `:11` 9 ／ `:8` 6 ほか。

## 5. 未完（隠さず出す）

1. **12件が壊れている。** v1 抽出器は「END を含む行」で範囲を切るため `SEND_ONLY` に当たって早期終了する。**原因は私の依頼文**（部分一致で札を判定させた）。厳密版は JUDGE_REQUIRED で停止中。
2. **29件は決定論では埋まらない。** 文書に試験の記述が無い。
3. **`completion` の語の形は私が決めた。** 文書から来るのは本数と試験名だけ。「Domain が受入を自分で定義した」ではない。
4. **source が実在しない 4件**は未処理。

## 6. 今夜見つけた機構の欠陥（機械へ入れる候補）

| # | 欠陥 | 証拠 |
|---|---|---|
| 1 | 入口が拒否理由を返さない | `raw` に `2DER:IMMUTABLE_TESTS` の字面 → `ValueError: Missing markers`。**4回投げ分けて初めて見えた**（END だけ・山括弧だけは通る） |
| 2 | 意図の門が揺れる | **同じ raw** が `RRI_INTENT_HOLD` → 次で `ALLOW`。保留された契約を再挑戦させる者がいない（私が手で投げ直した） |
| 3 | 試験が通っても壊れていることがある | 48通りのうち12件。**別経路で数えるまで表に出なかった** |
| 4 | 投入済み・未完の契約を引く口が不明 | 私の sha 逆引きは全件 None を返した。**「口が無い」のか「私の引き方が誤り」かを私はまだ分けられていない** |

## 7. 監査への依頼

- **②を「閉じた」と呼べるか。** 48通りは器に入ったが 12件は壊れており、29件は空。私の見立ては**未完**。
- **6章の4件のうち、どれを先に機械へ入れるべきか。** 私の見立ては #2（止まった物が誰にも拾われない）が最も重い。
- **#3 の再発防止。** 「別経路で数える」を私の手番でなく機構に置く形は在るか。
