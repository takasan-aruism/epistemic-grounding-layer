開発者規律 確認済(v1.0)

# 【BUILD SPEC】★18区間が ★記録に 出ているかを ★3値で 出す（★埋めない・★作らない）

宛: IMPL ／ 発: DESIGN ／ 2026-08-11 23:05 ／ 台帳: `ITEM-2DER-EVO-0058`
出所: MGR 22:44（`ETR-cd8c72f74b41`）＝「★この段の 終わり＝★18区間の うち どれが 記録に出ているかが ★3値で front door から 引ける」

---

## 1. ★★ケース表（★★走らせる前に 出す・★★これが 判定の 全部）

```
★材料は 2つだけ
   ★★(あ) ★記録側 = ★`event_trace` の 行（★front door `GET /api/etrace` から 引く）
   ★★(い) ★源側   = ★`emit(component, function, …)` の ★呼び手（★grep・★`--include=*.py`）

★★C1 ★記録に (component, function [, phase]) の行が ★1件以上          → ★★`PRESENT`
★★C2 ★記録0件 ＋ ★源に ★emit の呼び手が 在る                         → ★★`EMPTY`
★★C3 ★記録0件 ＋ ★源に ★emit の呼び手が ★無い                        → ★★`NOT_OBSERVED`
★★C4 ★emit の 第2引数が ★★動的な値（★実例 = ★S02 = `phase0.py:61` が
      `record.get("record_type")` を 渡す）
      → ★★★源だけで `NOT_OBSERVED` に しない = ★★記録側の 実値で C1/C2 を 決める
★★C5 ★同じ (component, function) を ★6区間が 共有（★`DW/_append_event`）
      → ★★`phase` で 分ける（★S09 CREATE / S12 PLAN / S13 GENERATE /
        S16 AUDIT / S17 DISPOSE / S18 COMPLETE = ★★6区間とも phase が 在る ∴ 衝突なし）
★★C6 ★`phase` が None の区間 → ★★phase を 条件に しない（★足さない）
★★C7 ★★記録が 引けない（★timeout / 口が落ちる）
      → ★★★3値に しない = ★★『引けなかった件数』を ★別立てで 出す（★★0 として 数えない）
★★C8 ★S14 の `handoff` / `receipt` は ★★この段では 使わない（★3値のみ・★次の段）
```

## 2. ★★源側は 先に 数えた（★★決定論・★2026-08-11 23:00）

```
★母数 = ★★18区間
★★源に emit の呼び手が 在る = ★★★17/18
   ★S01 `ds/etrace.py:65` ／ ★S03 `submit.py:246` ／ ★S04 `submit.py:292` ／
   ★S05 `submit.py:374` ／ ★S06 `intent_record.py:68` ／ ★S07 `de_admission.py:183` ／
   ★S08 `submit.py:692` ／ ★S09/S12/S13/S16/S17/S18 `dw/workcell.py:97` ／
   ★S10 `webui.py:949` ／ ★S11 `webui.py:929` ／
   ★S14 `generate_via_runner.py:203` ／ ★S15 `live_worker_runtime.py:172`
★★源だけでは 決まらない = ★★★1/18（★S02 = ★C4）

★★∴ ★★★`NOT_OBSERVED` の 見込み = ★★源側からは ★★0件
   ―― ★★但し ★★これは ★『呼び手が 在る』であって ★★『呼ばれた』では ない
      = ★★★C1/C2 の 分かれ目は ★★記録を 引くまで 決まらない（★予想を 書かない）
```

## 3. ★★出し方（★口0増・★新台帳0・★新語0）

```
★★既存の `GET /api/control` の 返りに ★`route_table` を 足す
   ★`{"total": 18,
      "by_value": {"PRESENT": {"count": n, "ids": [...]},
                   "EMPTY": {...}, "NOT_OBSERVED": {...}},
      "unreadable": {"count": n, "ids": [...]},   ← ★★C7（★0件なら 0件と書く）
      "rule": "C1..C8 の どれで 決めたか"}`
★★区間ごとに ★★どのケースで 決めたかを 1行（★★`rule` が 空の行 = 0件）
```

## 4. ★★受入（★MGR の5つ ＋ ★私から2つ）

```
★★① ★18区間 すべてに 3値の どれかが 付く（★★母数18を 明記）
★★② ★`NOT_OBSERVED` が ★件数と ★名前で 出る
     ―― ★★実データで 出たら ★★『★本日 初めて 出た』と 記録に 書く（★MGR 条件）
★★③ ★front door から ★1回で 引ける（★口0増）
★★④ ★★決定論（★★LLM 0回・★人が 書いた文を 読む所 0箇所）
★★⑤ ★新台帳0 ／ ★新語0（★3値は 既存の語）
★★⑥（★私）★★C7 の『引けなかった』を ★★別立てで 出す（★★3値に 混ぜない）
★★⑦（★私）★★S02 が ★どのケースで 決まったかを ★1行（★★C4 の 実演＝★動的な値の 扱い）
```

## 5. ★★やらないこと（★MGR 名指し・★『ついでに』を やらない）

```
★★資料(.md)を 生成物に しない ／ ★機能↔部品の 索引を 作らない ／
★★自動更新を 作らない ／ ★残り7本の 門の配線を しない ／ ★★区間を 埋めない
★★『経路表を 作った』と 書かない = ★★★『18のうち n が 記録に 出ている』と 書く
```
