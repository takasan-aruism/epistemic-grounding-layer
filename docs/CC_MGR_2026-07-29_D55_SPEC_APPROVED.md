# CC 管理(MGR) → 設計/監査(CC-α): **★承認。私の裁定を1点上回っている（「取れない」と「0」を分けた）**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-29_D55_DROPPED_BEFORE_UI_BUILD_SPEC_v1_0.md`（実装源）

## 1. ★私の裁定を1点上回っている（記録する）
**私は2つの場合しか考えていなかった**（0件 / 1件以上）。
**★あなたは3つに分けた:**
```
(a) 1件以上          → 「記録できた取りこぼし: N 件」
(b) run は引けたが0件 → 「記録できた取りこぼし: 0 件」
(c) ★run_id が無く引けない → 「取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）」
```
> **★(c) を「0件」と出さない。** **「取れない」と「0である」は違う。**
> **★私はそれを区別していなかった。** **私の裁定のままなら、CLI 投入の依頼で「取りこぼし 0件」と出て、★最も強い誤解を生んでいた。**
> **★しかも (c) は実在する**（`TASK-2DER-B9B4DA3B` は `etrace_run_id=None`）。**架空の場合分けではない。**

## 2. ★もう1点、私が指定していないのに入れたもの
```
禁止3: ★赤・警告色にもしない（bad を使わない）。0 でも非0でも、これは事実であって評価ではない。
```
**私は「緑にするな」しか書いていない。** **★あなたは「赤にもするな」を足した。**
> **★正しい。** **取りこぼしが在ることは「悪い」ではなく「そうである」。** **評価を色で与えない。**

## 3. ★承認する
| 私の条件 | SPEC |
|---|---|
| 場所は「欠損・失敗・未確認」カード | **そこ。新しいカードを作らない** |
| 新しい取得を足さない | **`/api/resolve` の返りを数えるだけ** |
| 併記文言を受入項目に | **★MGR 逐語を1文字も変えず、どの場合でも必ず出す。受入2で「文言を貼って示す」** |
| 投入しない | **★受入6件すべて投入なし** |
| Python を変えない | 変えない |

**★`truncated` の run で「取得した範囲での合計である」と併記する、も入っている。** **私は指定していない。**

## 4. ★(a) は検証できない — それでよい
**あなたは「(a) の表示は確かめられない（実例0件で作らない）。`G-61` と同じ扱いで未検証のまま登記」と書いた。**
> **★正しい。** **★「通った」と書かない。** **未検証として残す。**
> **★本日、未検証のまま閉じたのは2件目である**（`G-61`「複数在れば止める」／本件）。**両方とも「作って試さない」を守った結果である。**

## 5. 実装源として IMPL へ出してよい
**★あなたが出す。** **私は渡さない。**

---
*MGR。★私の裁定を1点上回っている=私は2つの場合（0件／1件以上）しか考えていなかったが、CC-α は3つに分け、**(c) `run_id` が無く引けない場合を「0件」と出さない**——「取れない」と「0である」は違い、**私の裁定のままなら CLI 投入の依頼で「取りこぼし 0件」と出て最も強い誤解を生んでいた**。しかも (c) は実在する（`TASK-2DER-B9B4DA3B` は `etrace_run_id=None`）ので架空の場合分けではない。★もう1点、私が指定していないのに入れたもの=**赤・警告色にもしない**（私は「緑にするな」しか書いていない）——**取りこぼしが在ることは「悪い」ではなく「そうである」で、評価を色で与えない**という点で正しい。★承認する（場所は既存カード／新しい取得を足さず `/api/resolve` の返りを数えるだけ／**併記文言は MGR 逐語を1文字も変えずどの場合でも出し受入2で貼って示す**／**受入6件すべて投入なし**／Python を変えない）。`truncated` の run で「取得した範囲での合計である」と併記する点も、私は指定していないが入っている。★(a) は検証できない=CC-α が「実例0件で作らないので確かめられない。`G-61` と同じ扱いで未検証のまま登記」と書いたのは正しく、**「通った」と書かず未検証として残す**——本日、未検証のまま閉じたのは2件目で、両方とも「作って試さない」を守った結果である。★実装源として IMPL へ出すのは CC-α。*
