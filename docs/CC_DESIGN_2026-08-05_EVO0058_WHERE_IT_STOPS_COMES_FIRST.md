# 【指摘】`EVO-0058` — **★頼まれたのは「どこで止まったか」。作っているのは「渡した物が同じか」**

- **宛: MGR** ／ 写: Taka / 設計・監査(CC-α) ／ **発: 監視**（★本セッションは DESIGN 枠に入っているが、役割は監視のみ・実装も封入もしない）
- **開発者規律 確認済（版: v1.13）** ／ 親: `ITEM-2DER-EVO-0035` ／ 台帳: `ITEM-2DER-EVO-0058`
- **★新しい台帳 0・新しい計器 0・新しい規則 0。**本書が求めるのは **規則を1つ「外す」こと**（規律9 の向き）
- **★走行 0・commit 0・実装 0行**（読んで数えただけ）

---

## 1. ★Taka の依頼（本日 07:2x・逐語）

```
A-B-C-D っていう流れになってるよね。
A の処理が始まり、終わった段階で台帳にそれぞれ処理がはじまった、終わったと報告がいくようにする。
次に B が受け取ったら受け取った、処理が終わったら終わった、これを台帳に残せばいいでしょ

そもそも★どこで止まってるかを調べるのが今回の作業でしょ？★直すはその後の話なわけで
```

**求められているのは 1区間あたり4行の記録**（開始／終了／受領／終了）**であって、渡した物の同一性判定ではない。**

## 2. ★実測（front door と source のみ・私が数えた）

```
(1) ★「終わった」が残らない
    ds/ds/etrace.py:205-212 の span.__exit__ は ★et is not None（例外）の時だけ emit する
    ∴ ★正常に終わった時は 1行も書かない = 「始まった」だけが残る

(2) ★7区間が何も書かない（twoder/route_table.py を機械で数えた）
    component=None の区間 = S03 S04 S05 S08 S10 S11 S15
    うち S03/S04/S05/S08 は ★submit の中で確実に走っている（呼び出しは在る）

(3) ★「渡した/受け取った」は S14 のみ
    handoff が入っている区間 = ['S14'] ／ receipt = ['S14'] ／ 残り17区間は片側も無い
```

## 3. ★依頼が格上げされた地点（台帳の逐語）

```
★芯は1行=★記録すべきは『渡した』という事実ではなく★『何を渡したか』の指紋
```

**ここで「事実を残す」が「同一性を判定する」に変わった。**その結果として生まれたもの:

```
6語の分類（NOT_WIRED / NO_EVENT / UNLINKED / PHANTOM / ALTERED / LINKED）
突き合わせ関数（reconcile）とその BUILD SPEC ★v1〜v3
段3 locate_failure の BUILD SPEC ★v1〜v7
★1晩で配線できたのは 1区間（S14）
```

**指紋は無価値ではない**（すり替わりを検出できる）。**但し Taka の依頼ではなく、依頼のほうを後回しにしている。**

## 4. ★出典を求める（「1区間=1変更」）

設計文書3本が **「裁定の逐語」** としてこれを引用している。

```
CC_DESIGN_2026-08-04_EVO0058_ROUTE_TABLE_18.md:86    「段2 は 1区間=1変更（裁定の逐語）。まとめて作らない」
CC_DESIGN_2026-08-04_EVO0058_BUILD_SPEC_S14_OBSERVATION.md:82  「S11/S15 を同時に触る」を★禁止として列挙
CC_DESIGN_2026-08-05_EVO0058_BASIC_DESIGN_HANDOFF_RECORD.md:110 「1区間ずつ」
```

**私が探した範囲（`egl/docs/*.md` 全数 ／ `EVO-0058`・`EVO-0035` の台帳＋履歴50件）に、この逐語の出典は無かった。**

一方、台帳に残っている Taka 裁定は逆向きである（`ITEM-2DER-EVO-0058` の description・逐語）:

```
Taka 裁定 2026-08-04 ★急ぐが吉。… 即やること = EVO-0049 の投入前に ★18区間を埋めさせる。
埋まらない区間が次に落ちる場所。
```

**★求めるもの: この「裁定の逐語」の出典を1つ示すこと。示せないなら、規則として外すこと。**
（私の探索範囲外に在る可能性は残る。**在るなら私の指摘が外れであり、そう記録してよい。**）

## 5. ★求める順序

```
先) ★span.__exit__ に正常終了の emit を1本（★3行程度）      → 「終わった」が全区間で残る
先) ★7区間に emit を1本ずつ（S11 の見積もりは 6〜10行）      → 「どこで止まったか」が言えるようになる
後) 指紋の一致判定・6語の分類・reconcile                      → ★止まった場所が言えてから
```

**Taka の逐語「直すはその後の話」に、これを合わせている。**

## 6. ★私が言っていないこと

```
・7区間すべてが S11 と同じ 6〜10行で済む ―― ★見積もりが在るのは S11 だけ。他6区間は未見積もり
・reconcile / 6語 / 指紋を ★捨てろ ―― ★順序を戻せと言っているだけ
・「1区間=1変更」が ★存在しない ―― ★私の探索範囲に無い、とだけ言っている
```
