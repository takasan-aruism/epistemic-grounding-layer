# CC 管理(MGR): **D-9 + 配線の設計に着手してよい**（本線の切り替え）（HANDOFF・短く）

- `BUILD_ROLE: 参照` / 宛: DESIGN/AUDIT(CC-α) / 写: IMPL / Taka / 発: MGR / 2026-07-27 07:00 / TYPE=HANDOFF

## 1. GO
**D-9（事実は関係するときだけ渡す）+ 配線 を、1本の設計として書いてよい。** D-8 監査は通った。**精度の線はここで締める。**

## 2. 1本に含めるもの（分けない）
1. **事実の出し分け**: 「直前の文脈は記録に存在しない」を出すのは **指示語または過去参照がある時だけ**（P2 が既に決定論で判定している）。**無関係な事実を配らない**（D-8 §5 = CC-α 自身が特定した欠陥）。
2. **配線**: **front door → 意図調べ → 決定論の事実の受け渡し** という経路が**存在しない**（意図調べは本番から参照0件）。**この経路の設計。**
3. **直叩きの閉塞**: **書き5箇所**（`admit_design_evidence` 2 / `record_plan` 2 / `create_task` 1）。**読み15箇所は後回し・閉じないと決めてよい。**
4. **1問い合わせ = 複数明細（D-6）との関係を1段落で書く。** Taka 指示（複式簿記）は**入口の設計そのもの**なので、別々に書くと三度手間になる。**ただし account chart は完成・凍結につき触らない。**

## 3. 規律（変えない）
- **既存資産を作り直さない**（`twoder/submit.py` は既にある。**まず読む**）。
- **thin**: 通す経路は1本でよい。**残りは正直な stub**。
- **feasibility-first**: 難所は設計時に先出し（原因＋3択）。
- **数字を出す時は「front door を通った入力の中で」。** **「2DER が良くなった」と書かない。**
- **精度の線に戻る新しい測定を、この設計に混ぜない。**

## 4. 判定基準（移行完了と言ってよい条件・再掲）
1. 我々の開発依頼が front door から入る（直叩きが閉じている）
2. 2DER 側が routing を決めている（Claude の結果を書き写しているのでない）
3. その状態で本線作業が1つ完走（設計→実装→監査→commit）
**現状3つとも未達。1つでも欠けたら「移行した」と言わない。**

---
*MGR。D-9 + 配線を1本の設計として着手してよい（精度の線はここで締める）。含めるもの=事実の出し分け(関係する時だけ)/front door→意図調べ→事実の受け渡しの経路(現在存在しない)/直叩きの書き5箇所の閉塞/D-6(複式簿記)との関係を1段落。既存資産を作り直さない・thin・feasibility-first。移行完了の3条件は現状すべて未達。*
