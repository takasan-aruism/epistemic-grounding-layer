# 【BUILD SPEC】**★観測の「値」を planner に渡す**（1箇所）— ★値は在る。★Ledger 再開ではない

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 09:4x / TYPE=BUILD_SPEC
- **運用方針 確認済（版: v2.8）** ／ **裁定**: `CC_MGR_2026-07-31_D158_…` §3
- **★これは新しい名前の文書である**（★`D-158` §2-2: **★相手に届けるものは同名で差し替えない**）。**★前の SPEC（`ART-e1f5edfff2`）は役目を終えた。**
- **★2DER 優先原則の例外**（IMPL が書く）。**★担当工程数の差分は 0 の見込み。★0 なら「0」と書く。**

---

# 1. ★食い違いの決着（★MGR §3。★私が測った）

| 主張 | ★実測 | 判定 |
|---|---|---|
| **IMPL**「値は Observation 記録に入っていない（hash と参照のみ）」 | **★正しい。** `OBS-00979` のキーは `observation_id / acquisition_run_id / source_id / raw_content_hash / raw_blob_ref / content_type / normalization_status / taint_flags / evidence_eligible` ＝ **★値は無い** | **★正しい** |
| **設計**「既に在る値を渡すだけ」 | **★正しい。★ただし在り処が違った。** **★`GET /api/state?task_id=…` の `egl.measured_state` に★人が読める値が在る** | **★正しい（★在り処を私が誤って言っていた）** |

**★到達できた値（★実データ・私が引いた）**
```
"container qwen36_vllm — Up 5 days"
"GPU 0 memory used = 30520 / 32607 MiB (NVIDIA GeForce RTX 5090)"
"GPU 1 memory used = 31179 / 32607 MiB (NVIDIA GeForce RTX 5090)"
"pid 703029 rss=4022440KiB (VLLM::Worker_TP)"
```
> ### **★∴ 到達できる値が在る ∴ ★これが今回の1件である。★Ledger 再開ではない**（MGR `D-158` §3 の分岐）。

**★併せて**: **★計画の `knowledge_packet` には `task_context` と `provenance` しか無い**（★`measured_state` は入っていない）＝ **★planner に値が届いていない理由はここである。**

---

# 2. ★やること（★1箇所だけ）

> ### **★planner が読む所に、★観測の「値」（`MEASURED_STATE`）を載せる。**

| 条件（★増やさない） | |
|---|---|
| **★既に在る値を渡すだけ**（★`MEASURED_STATE` は submit の時点で既に記録されている）。**★新しい台帳・API・ID 族を作らない** |
| **★1箇所。★どこに載せるか（packet 側か `_plan_prompt` 側か）は★あなたが選ぶ。★狭い方を採り、★理由を1行 書く** |
| **★GPU 固有・コンテナ固有・ポート固有の語を入れない**（★`gpu`/`nvidia`/`port`/`docker` を diff に0件・大小無視・打ち切り無しで示す） |
| **★観測を Task の下流へ移さない**（★線は動かさない。★値を渡すだけ） |
| **★後方互換**: `BUILD_CAPABILITY`/`MODIFY_EXISTING` 経路の PLAN を壊さない |
| **★受入は Taka の4条件のまま。★1文字も緩めない** |

---

# 3. ★受入（★1条件に1つの印。★「概ね」と書かない）

| # | 受入（★Taka 逐語） | ★示し方 |
|---|---|---|
| 1 | `cites_source_ids` が空でない | 実値 |
| 2 | 対象 Observation の ID を引用する | その task の `egl.source_refs` と一致するか |
| 3 | 既に観測済みの能力を、新規作成対象として重複計画しない | `objective`/`files_expected`/`steps` の実文 |
| **4** | **★証拠から不足が判定された場合だけ改修 Task を作る ← ★今回の本番** | **★計画が「★観測の結果 何が取れていないか」を★観測の値に基づいて述べているか**（★`unresolved_assumptions` に不足の定義を★先に固定していたら不成立） |
| **★補** | **★値が本文に届いたか** | **★`objective`/`scope`/`steps`/`completion_criteria`/`requirement` に★実際に観測された値の文字列（例: コンテナ名・数値）が★1回でも出るか**（★前回は0回） |
| 補 | 後方互換 | 基準値11件（`D-151 SPEC §3-1`）を取り直して1件ずつ |
| 補 | 1箇所・語の禁止 | 変更ファイル／hunk／挿入削除行数 ／ 禁止語 0件 |

## 3-1. ★測り方の注意（★私が本日3回 踏んだ形。★同じ穴に落ちないため）
```
★★計画は英語で書かれることが在る。★日本語の語で数えて「全部0」を得ても、★それは「無い」ではない。
★★語で数えるときは★英語と日本語の両方で数える。★「全部0」を「きれい」と読まない。
★★部分一致で判定しない（★私は `content` を含むキーを「値が在る」と誤検出した）。★キーを列挙して目視する。
```

---

# 4. ★確認（★依頼文は MGR が固定する。★私は決めない）
```
★★同じ依頼文では届かない（★実証済）: task_id は依頼文の sha1 ∴ ★同じ文＝同じ task＝CREATE 確定済
∴ ★新しい依頼文が要る。★MGR が固定する。★あなたは①修正を先に進めてよい。
★手順: ①修正 → ②再起動（全件記録）→ ③文を機械で抜く（字数・sha1）→ ④task_id を投入前に予告
       → ⑤投入1回 → ⑥直後に receipt → ⑦run_next 1回 → ⑧PLAN が出たら★止める
★★GENERATE へ進まない
```

# 5. ★予告（★投入前に固定する）
```
★★私は「受入4 が立つ」と予告しない。★値を見せても、不足を観測から述べないかもしれない。
★★そのときは「★値を見せても効かなかった」と書く。★言い換えない。★それも結果である。
★予告するのは1点だけ: ★補（値が本文に届く）は★立つ。★理由は入力の話であって能力の話ではない。
   ★前回 同じ形の予告を外している（★概念は届いたが値は届かなかった）∴ ★確信は「中」と書く。
```

# 6. ★やってはいけないこと
```
★観測を Task の下流へ移す設計変更  ★GENERATE を押す  ★新しい台帳・API・ID 族の追加
★Ledger（生出力）を触る（★保留中）  ★別の欠陥を見つけたことを理由に止まる
★「概ね」と書く  ★commit しない  ★61本の非回帰は走らせない  ★:8005 を自分で叩かない
★★同名で文書を差し替える（★D-158 §2-2。★沈黙の原因だった）
```

# 7. ★報告（★正典の6項目）
```
1. 変更した1箇所  2. 受入1〜4＋補 の判定（★1条件に1つの印）  3. Last PASS  4. First FAIL
5. ★2DER 担当工程数の前回差分（★0 なら「0」）  6. 次に直す1件
★予告の当否  ★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---
**決めたこと**: **①食い違いは両者とも正しかった——OBS 記録に値は無い（IMPL が正しい）が、`/api/state` の `egl.measured_state` に人が読める値が在る（設計が正しい。ただし在り処を私が誤って言っていた）∴ 到達できる値が在り、今回の1件は「値を渡す」で確定、Ledger 再開ではない ②直すのは1箇所で、載せ場所は IMPL が狭い方を選ぶ（`knowledge_packet` に `measured_state` が無いことが、値が届いていない理由） ③受入4 が今回の本番で、「立つ」とは予告しない——立たなければ「値を見せても効かなかった」と書く。★本文書は新しい名前で置いた（同名差し替えが5時間の沈黙を招いたため）。**
