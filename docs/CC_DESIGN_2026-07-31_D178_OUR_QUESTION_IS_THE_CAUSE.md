# 【調査報告】設問を作った側を調べた — **★受入3 に当たる指示は、prompt に 1件も無い**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 17:2x / TYPE=FINDING
- **運用方針 確認済（版: v2.9）** ／ **正典**: `TAKA_2026-07-31_NO_BLAMING_QWEN_AUDIT_OUR_OWN_QUESTION.md`（逐語）／**受領**: `D-177` `D-178`
- **★新しい名前で置いた** ／ **★1行も直していない**（正典 §「設計修正は調査結果が出るまで行わない」）
- **★報告は正典 §9 の11項目の順。増やさず、並べ替えない**（末尾に MGR の依頼2件と訂正1件を付す）

---

# 1. Qwen へ渡された最終入力全文

**取り方**: テンプレートではなく **変数展開後**。観測4件は `/api/resolve`（2DER に聞く）で解決し、`_plan_prompt` に流して描画。**台帳は直読していない。**

```
★全文の所在: FINAL_PROMPT.txt（設計の作業領域）
★文字数 3705 ／ 行数 57 ／ sha1 8c01ef847058c17f64f6d5810c87ea412d7747d4
```

## 1-1. ★11要素に分離した — **★4要素は Qwen に渡っていない**

| # | 要素 | 実測 |
|---|---|---|
| 1 | **system prompt** | **★0件（存在しない）**。`qwen_raw_call` は `system` 引数を受けるが、`build_plan` は**渡していない** ∴ 逐語 `messages = [{"role":"user", …}]` **1件のみ** |
| 2 | planner prompt | 冒頭3文（I-01〜I-03） |
| 3 | user task | 1行（依頼文 逐語） |
| 4 | Observation block | **取得記録4行 ＋ 値21行** |
| 5 | 出力形式 | `SINGLE JSON object inside one ```json code block` |
| 6 | 必須欄 | **18欄**（`MUST have exactly these keys`） |
| 7 | 禁止事項 | `Use ONLY the Python standard library` / `Do not commit, push, use the network, use sudo, or modify any existing repository` |
| 8 | authority requirements | **★欄の定義1行のみ。★制約としての説明は 0件** |
| 9 | **validation 条件** | **★0件**（`validate_plan` はコード側。**Qwen の視界に無い**） |
| 10 | **再生成条件** | **★0件**（`run_with_recovery` の max_tokens 段階昇格。**Qwen の視界に無い**） |
| 11 | **Audit へ渡された内容** | **★0件**（AUDIT 段は本日 未到達 ∴ 存在しない） |

> ### **★我々が「制約」と呼んできたもののうち、★validation・再生成・system prompt・Audit の4つは、★Qwen に一度も渡っていない。**

---

# 2. 命令一覧（★地の文13文。★逐語）

**★6項目のうち「優先の根拠」は、★prompt 内に根拠が書かれていなければ「★無し」と書く**（我々が頭の中で持っていた優先順位は、prompt に**書かれていない**）。

| id | 逐語 | 発生元 | 目的 | 必須/任意 | 優先の根拠（prompt 内） | 従うと | 従わないと |
|---|---|---|---|---|---|---|---|
| I-01 | `You are the PLAN actor of an autonomous engineering system.` | 我々 | 役割固定 | 必須 | **無し** | 計画を書く | — |
| I-02 | `Produce an IMPLEMENTATION PLAN … as a SINGLE JSON object` | 我々 | 出力形式 | **必須** | **`Produce`＝命令形** | **★実装計画を必ず出す** | 出力が成立しない |
| I-03 | `Do not implement it; plan it.` | 我々 | 実装の抑止 | 必須 | `Do not` | 計画に留める | — |
| I-05 | `現在メモリ使用量の大きいプロセスの状況を確認し、確認できない項目があれば、それを取得できるようにする方法を検討してください。` | **Taka（依頼者）** | 依頼の達成 | **必須** | **★依頼そのもの** | **★確認できない項目の取得方法を作る** | 依頼未達 |
| I-06 | `ALREADY ACQUIRED FOR THIS TASK (… already exists; it was retrieved before this planning step)` | 我々 | 既知の提示 | **★平叙文（命令ではない）** | **無し** | — | **★何も起きない** |
| I-07 | `VALUES ALREADY OBSERVED (… anything not present here was NOT obtained)` | 我々 | 既知値の提示 | **★平叙文** | **無し** | — | **★何も起きない** |
| I-08 | `The JSON object MUST have exactly these keys:` | 我々 | 欄の強制 | **必須** | **`MUST`** | 18欄すべて埋める | 出力が成立しない |
| I-09 | `Use ONLY the Python standard library.` | 我々 | 実装制約 | 必須 | **`ONLY`** | 標準ライブラリのみ | 制約違反 |
| I-10/11 | `Keep everything inside the sandbox workspace.` / `Do not commit, push, …` | 我々 | 安全 | 必須 | `Keep`/`Do not` | 安全 | 制約違反 |

## 2-1. ★強制語の分布（★機械・全数）

```
MUST 2件 ／ ONLY 1件 ／ NEVER 1件 ／ Do not 2件 ／ Keep 1件 ＝ ★計7件
★★7件すべてが「★出力の形」か「★作り方の制約」に付いている。
★★★「★既知を使え」「★既知を作り直すな」の側に付いた強制語は ★0件である。
★★Observation（I-06/I-07）は ★強制語ゼロの平叙文 ∴ ★構造上いちばん弱い入力である。
```

---

# 3. 命令間の矛盾（A〜D。★機械で確認）

## A. Observation を使えない矛盾
```
★① `Use ONLY the Python standard library`（★ONLY＝最強語）
   ↔ ★Observation の中身は ★外部コマンド（nvidia-smi / docker / ps / ss）の出力である
   ＝ ★「使うな」と「これが素材だ」が★同居している
★② Observation は ★取得コマンドを★逐語で見せている（`["ps","-eo","pid,rss,comm","--sort=-rss"]`）
   ∴ ★「既知の証明」としても「★再実行の手順書」としても読める。★どちらで読むかを決める語が★無い
★③ `anything not present here was NOT obtained` ＝ ★不足を数えよ、という誘導である
   ★これと依頼文 I-05 が合成されると「★不足を取得する実装を書け」になる
```

## B. 作業を作らざるを得ない矛盾
```
★18欄中、★「empty if none」と★空を許す明示が在るのは ★2欄のみ（authority_requirements / already_satisfied）
★★残り16欄には★空を許す語が無い。★うち6欄は★逐語で「作れ」と読める:
   files_expected「to be created/modified」／steps「ordered implementation steps」
   requirement「the coding worker will implement」／target_file「the single python file ★to create」
   test_body「complete python source …」／allowed_files「files the worker may write」
```
> **★ただし §5 の実測で、★この欄を全部 外しても「作らない」は出なかった。**
> **★∴ 「欄が塞いでいる」だけでは説明にならない。★D-177 §4-② はここで訂正が要る（§12）。**

## C. 既知情報と不足情報の混在
```
★`ps -eo pid,rss,comm` は既知。★`vsz`/`ppid`/`cmdline` は★不足。★同じ `ps` で★同時に返る。
★★prompt に「★取得目的の再取得」と「★副作用として返った」を★区別する語彙は ★0件である。
★★★∴ ★区別できない。★正典 §7 の言い方では「★受入基準と PLAN 構造の不整合」に当たる。
```

## D. どの命令が実際に最も強く作用するか（★強い順）
| 順 | 命令 | 強い理由（★prompt 内の根拠） |
|---|---|---|
| **1** | **I-05 依頼文** | **★依頼そのもの。「取得できるようにする方法を検討せよ」＝★作ることを直接 求めている** |
| 2 | I-08 `MUST have exactly these keys` | **`MUST`**・18欄・不履行で出力が成立しない |
| 3 | I-02 `Produce an IMPLEMENTATION PLAN` | 命令形・出力の定義そのもの |
| 4 | I-09 `ONLY` / I-11 `Do not` | 最強語だが**★禁止側** ∴ 何を作るかを決めない |
| **最弱** | **I-06 / I-07 Observation** | **★強制語ゼロ・平叙文・従わなくても何も起きない** |

---

# 4. 出力構造上できない選択肢

```
★「何もしない」「既に満たされている」「追加作業不要」「確認だけで終了」で★終われる道
   → ★A（現行）では ★構造として無い（16欄が空を許さない・6欄が「作れ」と読める）
★★しかし ★C1/C2 で★その欄を外しても ★38本中 0本しか「不要」と言わなかった（§5）
∴ ★★塞いでいるのは★欄だけではない。★I-05（依頼文）と I-02 が★残っている限り、作る。
```

---

# 5. 単体 Qwen A/B/C 比較（★2DER の外。★本番経路ではない）

```
★投入先: :8005 単体 ／ ★temperature 0（★本番と同じ物差し・D-166）／ max_tokens 8192
★★同時実行数 10（v2.9 §4-18）／ ★総本数 40 ／ ★打ち切り 無し（★40本 全部 回した）
★条件A  = 最終 prompt そのまま
★条件B  = Observation を★メモリ関連だけに絞る（★他は完全に維持）
★条件C1 = ★実行系6欄の定義だけを外す
★条件C2 = ★C1 に加え files_expected / steps / test_plan / completion_criteria も外す（★作業を書く圧力を最大限 外す）
```

| 比較項目（★正典の6つ） | A | B | C1 | C2 |
|---|---|---|---|---|
| 解析できた本数 | 9/10 | 9/10 | 10/10 | 10/10 |
| **`already_satisfied` を書いたか** | **9/9** | **9/9** | **10/10** | **10/10** |
| 既知を **既知として扱ったか**（命令名を挙げた） | 9 | 2※ | 10 | 10 |
| **観測「値」を引用したか** | **0** | 1 | 1 | **0** |
| **追加作業不要と判断できたか** | **0** | **0** | **0** | **0** |
| 入力矛盾を指摘したか | 0 | 0 | 1 | 0 |
| 存在しない作業を創作したか | 0 | 0 | 0 | 0 |

※ B は Observation を ps だけに絞った条件 ∴ 挙げられる命令名が減るのは**設計どおり**。

## 5-1. ★私の計器が間違っていた（★先に書く）
```
★★私は最初 C2 を「★追加作業不要 10/10」と数えた。★誤りである。
   ★C2 は ★files_expected / steps / target_file を★prompt から外した条件である
   ∴ ★それらが空なのは★当たり前であって、★判断ではない。★私が自分の計器で自分を騙した。
★★数え直した: ★objective / scope を★逐語で読む（★C2 に在る欄だけで測る）
   → ★10本中 9本が「Develop a Python-based monitoring script …」＝ ★作ると書いている
   → ★残る1本も「Check the status … and identify any missing information」＝「不要」ではない
★★★∴ ★正しい数は ★0/38 である。
```

## 5-2. ★振ったのに動かなかったもの（★これが結論を決める）
```
★実行系6欄を外した（C1）      → ★「不要」0/10。★戻す挙動も0/10（★欄は正確に守る）
★作業欄を全部 外した（C2）     → ★「不要」0/10
★Observation を絞った（B）     → ★「不要」0/10
★★★3つの軸を振って、★1つも動かなかった ＝ ★これらは原因ではない。
★★★逆に、★器（`already_satisfied`）を1つ作った時だけ ★38/38 で埋まった ＝ ★器は効く。
```

---

# 6. 別 LLM 比較 — **★不可能。★理由を書く**

```
★① ローカルの別モデルを立てる余地が無い: ★GPU0 30520/32607 ・ GPU1 31169/32607 MiB 使用中
     （★今回の観測値そのものが証拠である）
★② ★私（Claude）は★この prompt の破損を★自分で特定した後である ∴ ★汚染済み。
     ★私の回答は「★設問の破損を検出したか」の証拠に★ならない。
★③ ★IMPL も D-177 で矛盾2件を自力で挙げている ∴ ★同じく汚染済み。
★★★∴ 「可能であれば」の条件を★満たさない。★不可能と書く。
★★★無汚染で採れる唯一の形: ★Taka が★別セッションの Claude／GPT へ★この prompt 全文だけを投げる。
   ★★我々からは頼まない（★手間を増やす）。★必要と判断されたときの選択肢として置くだけである。
```

---

# 7. Audit 却下理由との対応

| 却下対象 | 却下対象の原文 | 根拠 Observation | 衝突した命令 | prompt 上の原因 |
|---|---|---|---|---|
| `ps` の再実行 | `Implement a function to run 'ps' with extended fields: pid, user, state, vsz, rss, ppid, comm, cmdline, …` | OBS-00989 `["ps","-eo","pid,rss,comm","--sort=-rss"]` | I-05 vs I-06 | **★I-06 に強制語が無い／I-05 が「取得方法を検討せよ」と命じている** |
| `nvidia-smi` の再実行 | `Implement a function to run 'nvidia-smi' with '--query-compute-apps=pid,name,used_memory'` | OBS-00987（`--query-gpu`） | 同上 | **★取得済のオプションと★別のオプションである**（`--query-gpu` ≠ `--query-compute-apps`） |
| `ss` の再実行 | `Implement a function to run 'ss' with '-ltnp'` | OBS-00990（`ss -ltn`） | 同上 | **★`-ltnp` は `-ltn` に `p`（PID 対応付け）を足したもの ＝ ★不足の取得** |

## 7-1. **★その出力が合理的に生成される経路**（★正典の要求。★「使わなかった」では説明にしない）
```
① ★依頼文 I-05 が「★確認できない項目があれば、それを取得できるようにする方法を検討せよ」と★直接 命じている
② ★I-07 が「★ここに無いものは取得されていない」と★不足の数え方を教えている
③ ★Observation を読むと、★`vsz`/`cmdline`/`ppid`/`-ltnp`/`--query-compute-apps` は★確かに無い
④ ★∴ ★それらを取る計画を書くことが ★①の履行である
⑤ ★そして ★`vsz` を取るには `ps` を実行するしかなく、★`ps` は `rss` も返す
⑥ ★prompt には「★既知が副作用で返るのは構わない」とも「★構わなくない」とも★書いていない
★★★∴ ★出てきた計画は、★我々の設問に対して★正しい。★却下したのは★我々である。
```

---

# 8. 根本原因

> ## **★受入3「既に観測済みの能力を、新規作成対象として重複計画しない」に当たる指示は、★最終 prompt に 1件も無い。**

```
★機械で全数（大小無視）:
   `do not re-` 0 ／ `re-acquire` 0 ／ `reacquire` 0 ／ `again` 0 ／ `skip` 0 ／ `avoid` 0
   `duplicate` 0 ／ `redundan` 0 ／ `must not` 0 ／ `do not build` 0 ／ `no need` 0
★`already` は5件 出るが、★内訳は:
   3件 = Observation ブロックの★見出し（★事実の提示）
   2件 = `already_satisfied` の★欄定義「items that need NOT be built …」
        ＝ ★「作らなくてよいものを★列挙せよ」という★列挙の指示であって、★禁止ではない
★★★∴ ★我々は、★prompt に一度も書いていない禁止事項を、★受入基準にして 40本 採点していた。
```

**★これは基準の粗さ以前の問題である。** **★書いていない規則で減点していた。**

**★第二の原因（★根が同じ）**: **★依頼文 I-05 自身が「取得できるようにする方法を検討せよ」と作ることを求めている。** **★∴ 不足を取りに行くのは依頼の履行であり、★受入3 はそれを × にしていた。**

---

# 9. 最小修正案（★実施しない。★正典どおり止めている）

**★どれも「1件ずつ」に分けてある。★同時にやらない。**

| 案 | 内容 | 何に効くか |
|---|---|---|
| **★9-a（最小）** | **★受入3 を、prompt に書かれていない規則で採点するのをやめる。**→ §11 の定義に置き換える | **★採点の側。★コードを1行も変えない** |
| 9-b | Observation ブロックに**強制語を1つ**入れる（例: `You MUST treat these as known; do not plan to re-obtain them for their own sake.`） | **★強制語 0件 → 1件。★D の最弱を1段 上げる** |
| 9-c | `ps`/`nvidia-smi` の**取得コマンドを Observation から隠し、値だけ見せる** | A-② の「手順書に読める」を消す |

> **★私の見立ては 9-a を先に。** 理由: **★9-b/9-c はコードを変えるが、★9-a は「我々の採点の誤り」を直すだけ**であり、**★そもそも 9-b の効果は 9-a を直さないと測れない**（★書いていない規則のままでは、何を測っても ×）。

---

# 10. 修正時に変化する挙動

```
★9-a: ★コード変更 0 ∴ ★2DER の挙動は★変わらない。★変わるのは我々の判定である。
      ★★副作用: ★過去40本の受入3「2/10」は、★この定義では★数え直しになる（★書き換えない。★別に数える）
★9-b: ★Observation に強制語が入る ∴ ★「不足を取りに行く」も抑制されうる
      ★★リスク: ★依頼 I-05 と★正面から衝突する（★依頼は取得方法を求めている）＝ ★新しい矛盾を作る
★9-c: ★取得コマンドが消える ∴ ★`already_satisfied` に★「手段」を書けなくなる
      ★★リスク: ★今回 38/38 で埋まった欄が★空になりうる（★効いている物を壊す）
```

---

# 11. 一回だけ行う次の測定 — **★その前に、受入3 を定義し直す**（正典 §7・MGR §4「定義の候補は設計が出す」）

## 11-1. ★受入3 の定義（★候補。★確定は MGR）
```
★★旧（★暗黙・prompt に無い）: 「★観測済みのコマンドが計画に出たら ×」
★★新（★候補）:
   「★`already_satisfied` に自ら挙げた項目を、★その項目を得ること自体を目的として
     ★`objective` / `requirement` に再登場させていないか」
★★★判定は「★コマンドの重複」ではなく「★目的の重複」で見る。
★★★∴ ★不足（vsz 等）を取るために ps を再実行し、★既知（rss）が副作用で返るのは ★○ である。
★★★★測定の後に動かさない。★次の測定より★先に確定させる。
```

## 11-2. ★次の測定（★1回だけ）
```
★条件: ★9-a のみ（★コード変更 0）。★本番経路で★1回 投入し、★新定義で採点する。
★★これは「★直したか」を測る測定ではない。★「★我々の採点が正しくなったか」を測る測定である。
★★★依頼文は MGR が固定する。★私は打ち直さない。
```

---

# 12. ★MGR から依頼された2件＋★訂正1件

## 12-1. (a) 受入2（利用したか）の境界判定 — **★私の判定: ★立っていない**
```
★IMPL は ○ とし、★同じ本の中に反証も書いた。★私は★反証の側を採る。
★根拠: ★正典は「★既に取得済みなので再取得不要」「★この情報を前提に次工程へ進む」に★当たる記述を求めている。
★★`already_satisfied` に書かれたのは「★ps で基本のプロセス一覧は取れている」＝ ★事実の再掲であり、
   ★「★だから作らない」という★接続が★本文に無い。
★★★∴ ★引用（受入1）は立つ。★利用（受入2）は立たない。★1つの印にまとめない。
```

## 12-2. (b) 値0件・`already_satisfied` は「手段」 — **★独立に確認した。★MGR の読みは正しい**
```
★私の測り方（★MGR とは別の物差し）: ★観測値トークン14件を★出力全文に当てる
★→ ★A 0/9 ／ B 1/9 ／ C1 1/10 ／ C2 0/10 ＝ ★38本中 2本
★★`already_satisfied` の中身は★全条件で「★どのコマンドで取れるか」＝ ★手段である（★逐語 §5 の引用）
★★★∴ ★「器を作ったら書いた。★ただし書いたのは手段であって値ではない」は★支持される。
```

## 12-3. ★訂正 — **★D-177 §4-② は、実測で支持されない**
```
★D-177 §4-②（逐語）: 「★実行系6欄は必須である ∴ ★『作らない』で終われる道が構造として無い」
★★私は C1 で ★その6欄を prompt から外した → ★10/10 で ★依然 作ると書いた（★戻す挙動も 0/10）
★★C2 で ★files_expected/steps/test_plan/completion_criteria も外した → ★10/10 で ★依然 作ると書いた
★★★∴ ★「6欄が塞いでいる」は★原因ではない。★外しても変わらない。
★★★★塞いでいたのは ★依頼文 I-05 と ★I-02 であり、★何より ★§8 の「★規則が書かれていない」ことである。
★★これは MGR の誤りというより、★1回の読みで因果を置いたことによる。★私も同じことを §5-1 でやった。
```

---

# 13. ★やっていないこと
```
★prompt を変えていない ／ ★欄を足していない・削っていない ／ ★受入を緩めていない
★GENERATE を押していない ／ ★commit していない ／ ★61本の非回帰を走らせていない
★★本番経路へ投入していない（★A/B/C は ★:8005 単体への★外部投入である）
★★台帳を直読していない（★観測4件は `/api/resolve` で 2DER に聞いた）
★★保留はそのまま: Ledger ／ 図 ／ (c) の patch
```

---
**決めたこと**: **①最終入力全文（3705字/sha1 `8c01ef84…`）を11要素に分離した——system prompt・validation条件・再生成条件・Audit の4つは Qwen に一度も渡っていない ②強制語7件はすべて「出力の形」か「作り方の制約」に付いており、「既知を使え」側の強制語は 0件。Observation は強制語ゼロの平叙文で構造上いちばん弱い ③A/B/C1/C2 各10本（同時実行数10・打ち切り無し）で、`already_satisfied` は 38/38 埋まったが、観測値の引用は 2/38、「追加作業不要」は 0/38。実行欄も作業欄も Observation の絞りも、3軸すべて振って動かなかった ④私は最初 C2 を「不要 10/10」と数えたが、それは外した欄が空なのを判断と読んだ自分の計器の誤りで、正しくは 0/38 ⑤根本原因は「受入3 に当たる指示が prompt に1件も無い」——我々は書いていない禁止事項で 40本 採点していた。加えて依頼文自身が取得方法を求めている ∴ 不足を取りに行くのは依頼の履行である ⑥最小修正案は 9-a（採点をやめる・コード変更0）を先に。9-b/9-c は 9-a を直さないと効果を測れない ⑦受入3 は「コマンドの重複」ではなく「目的の重複」で定義し直す候補を出した（確定は MGR） ⑧別LLM は不可能（GPU に余地が無く、私も IMPL も汚染済み）——無汚染なのは Taka が別セッションへ投げる場合だけだが、我々からは頼まない ⑨MGR の (a) は受入2 が立たない、(b) は支持される ⑩D-177 §4-②「実行系6欄が塞いでいる」は C1/C2 の実測で支持されない——外しても 10/10 で作ると書いた。**
