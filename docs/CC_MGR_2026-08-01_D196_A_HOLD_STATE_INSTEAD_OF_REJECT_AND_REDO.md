# 【D-196】★「却下→終了→作り直し」でなく★保留にできないか（★Taka 問い）— ★まず在るかを調べる

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-08-01 00:0x / TYPE=STATUS
- **開発者規律 確認済（版: v1.0）** ／ **この .md がまだ .md である理由**: 問いを台帳へ渡す口が front door に無いため（C-1）

## 1. ★Taka の問い（逐語）
```
★「却下ー終了ー作り直し はいくらなんでも馬鹿すぎるので ★保留とか留保とか
   ★そういうステータスにできんの？ ★仕組み作り直し必要だろうけど。
   ★そこまでまだ組まれていないんかな。どうっすか？」
```

## 2. ★MGR が front door から見た事実（★1回の観測。★断定しない）
```
★今回の停止は ★「終了して★ゼロから作り直し」ではない:
   ★同一 task 内で ★REGENERATE が2回 走り、★停止は `JUDGE_REQUIRED`（★判定待ちで★止まっている）
★★`GET /api/state` の実値（★別 task の例）:
   `dw_state = READY_FOR_AUDIT` ／ `last_completed_op = REGENERATE` ／ `next_operation = AUDIT`
   `work = {'next_information_need': None, ★'acquisition_method': 'RESUME', ...}`
★★★∴ ★状態の語彙は「却下／終了／作り直し」の3語より★多い。
   ★とくに ★`next_information_need`（★次に必要な情報を★名指しする欄）と ★`RESUME` は
   ★★Taka が言う「保留」に★そのまま対応する形をしている。★【未確認】★配線されているかは分からない。
★★★★★本当に馬鹿だったのは★別の場所である:
   ★★★「契約（受入試験）が無い」は ★最初に分かるはずのことなのに、
      ★生成→監査→作り直し を ★3周してから ★`SPEC_INCOMPLETE_NO_CONTRACT` として出た。
   ∴ ★足りない物を ★先に言わずに ★作らせている。★これは ★状態名の問題ではなく ★順序の問題である。
```

## 3. ★依頼（★1件。★3値で返す。★コードを1行も変えない）
```
(a) ★「作業を保持したまま、★足りないものを名指しして待つ」状態は ★既に在るか
    → ★在って動く / ★在るが配線されていない / ★無い
    ★手がかり【未確認】: `next_information_need` ／ `acquisition_method: RESUME` ／
      `JUDGE_REQUIRED` ／ `UPPER_REVIEW` ／ `CLAUDE_BARRIER` ／ `guard_block` ／ `taka_authority`
    ★★「状態が在る」だけでなく ★「そこから再開できるか」まで見る（★置いた≠読める・規律7）

(b) ★契約検査を ★生成の★前に出せるか
    → ★`SPEC_INCOMPLETE_NO_CONTRACT` を ★投入直後に返せるか。★3周してから言うのをやめる
    → ★既に前で弾く仕組みが在るなら ★作らない（★`contract_seal` の `ValueError` との関係を確かめる）
★★総件数 / 確認件数 / 打ち切り有無 を書く。★`head` を「無い」の証拠にしない。
★★★新しい台帳・新しい計器・新しい状態語を ★作らない（★調べるまでは）。
```

## 4. ★順序（★ACTIVE は1件を守る）
```
★ACTIVE は ★S-3（人間用UIの4つ）のまま。★本件は ★調査のみ（★コード0行）で並行してよい。
★★S-3 が ★同じ穴（★契約不足）で ★また3周したら、★(b) を ★前倒しして ACTIVE を差し替える。
★★★C-1（進捗の書き込み口）は ★S-3 の直後のまま（`D-195` §3）。★後回しにしたものは書いてある。
```

---
**決めたこと**: **①Taka の問いに対し、まず「在るか」を調べさせる。作らない ②今回の停止は「終了してゼロから作り直し」ではなく、同一 task 内で作り直しが2回走り `JUDGE_REQUIRED`（判定待ちで停止）だった ③`GET /api/state` に `next_information_need`（次に必要な情報を名指しする欄）と `acquisition_method: RESUME` が実在する。Taka の言う「保留」に対応する形だが、配線されているかは未確認 ④本当の問題は状態名ではなく順序である——「契約が無い」は最初に分かるはずなのに、生成→監査→作り直しを3周してから出た ⑤依頼は2つ、3値で返す: 作業を保持したまま足りないものを名指しして待つ状態が在るか／契約検査を生成の前に出せるか。どちらも在れば作らない ⑥ACTIVE は S-3 のまま。本件は調査のみ並行。S-3 がまた同じ穴で3周したら (b) を前倒しして差し替える。**
