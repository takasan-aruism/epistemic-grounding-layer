# 【D-140】第2試行の裁定 — **★過去記録を後付けで書き換えない／★この修正は主体移管に数えない**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-30 23:2x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）**
- **★正典**: `TAKA_2026-07-30_PHASE4_INSPECTION_001_TRIAL2_ORDER.md`（★逐語・全文）。**★本文書は裁定であって言い換えではない。**

---

## 1. ★裁定（★実装前に固定する。★測ってから動かさない）

| # | 論点 | ★裁定 |
|---|---|---|
| **1** | **★過去の `SUBMIT-zOlryQ` を解決できるようにするか** | **★過去の記録に後から行を足して解決させることを禁じる。**（運用方針 §6-5「前向きのみ。過去を遡って書き換えない」）。**★既存の記録だけで決定論的に解けるなら可。★解けないなら「解けない理由」を書き、★新規試行でのみ検証する**（正典が明示的に許している道） |
| **2** | **★テストの投入** | **★テストのために本番の入口へ架空の依頼を投入しない**（Taka 常設命令）。**★必須テスト1・2・7・8 は読み出しだけで行う。★3〜6 は第2試行の実投入で確かめる**（★実投入は1回だけ） |
| **3** | **★この修正の主体** | **★IMPL が書く**（Taka が §9 で明示）。**★2DER 優先原則の例外であることを明記する** ∴ **★この修正を「2DER が担当した工程」に数えない。★1/8 は動かない** |
| **4** | **★非回帰テスト** | **★61本は走らせない**（据え置き）。**★今回の必須テスト8本は、この修正の受入であって非回帰ではない。★走らせた test の名前と結果を書く**（★総数を書かない） |
| **5** | **★止まる場所** | **★最初の新しい停止点で終了。★そこで直さない・迂回しない・再投入しない**（正典 §5） |
| **6** | **★第1試行の報告書** | **★1文字も変更しない**（Taka 明示）。**★第2試行は別文書に書く** |
| **7** | **★GPU 使用率** | **★今回の修正に混ぜない。★再び取得不能でも ID 修正の失敗として扱わない**（正典 §6-C） |

## 1-2. ★特に注意する（★fail-closed の読み方）
```
★正典 §3「Fail-closed」の6条件は、★どれか1つでも当たれば「解決成功にしない」である。
★「だいたい辿れた」を成功にしない。★入力内容が取れないなら、★それは失敗である。
★これは第1試行で我々が工程1 を FAILED にしたのと同じ線である。★線を動かさない。
```

---

## 2. ★事前状態（★MGR が保存した。★役割 §9）

```
保存時刻: 2026-07-30T23:24:14+0900
  ds  0834c0f(main)   rri 9c3c9da(main)   egl a8e5db5(main)
  dev-workcell 450566f(master)   twoder 760820c(master)   ★5 repo とも 未commit 0
稼働プロセス: webui PID 3814663（07-30 12:44:07 起動） / vllm PID 700957（07-26 03:54:10 起動）
資料: component 23 / edge 11 / gap 83 ／ CC_REGISTER doc_rows 572
```
> **★注意: 修正は `twoder` に入る見込みである。★入れたら webui の再起動が要る可能性が高い。**
> **★再起動は「不可避な開始操作」として★全件 記録すること**（操作内容/操作者/理由/既存運用か/主体判定への影響）。
> **★再起動したら run-gate が初期化される**（使用ガイド §1-2-1）。**★それも記録する。**

---

## 3. ★手番（★正典 §9 のまま。★私は割り当てを変えない）

| 誰 | ★やること |
|---|---|
| **設計** | **★調査（正典 §2 の6項目・表形式）を IMPL へ指示 → 修正が ID 接続1件に限定されているか確認 → 後方互換確認 → E2E 証拠監査 → 工程別 判定案** |
| **IMPL** | **★調査 → 最小修正 → 必須テスト → 投入1回 → 証拠回収 → BUILT** |
| **MGR** | **★事前/事後の状態保存（本文書に事前分）／actor 判定／最終裁定／第1試行との比較報告／★次の停止原因を1件に固定** |

**★commit は私が行う。★IMPL は commit しない。**

---

## 4. ★禁止（★正典から、我々が破りやすい順に）
```
★GPU 取得機能の同時修正 ／ ★Planner・DW の同時改造 ／ ★複数 Gap の一括処理
★検査結果を成立させるための手動補完 ／ ★第1試行結果の書換え
★「概ね解決できた」と書くこと（★正典の6区分と fail-closed で判定する）
```

---

## 5. 手番
```
★設計: 調査指示（BUILD SPEC）を1本 → IMPL
★私  : 修正後の裁定と、第1試行との比較報告
★Taka: ★手番なし（★報告まで待つ）
```

---
**決めたこと**: **①過去記録に後から行を足して `SUBMIT-zOlryQ` を解決させることを禁止（解けないなら理由を書き新規試行で検証） ②テストのために本番へ架空の依頼を投入しない（読み出しで済む4本と、実投入で確かめる4本に分ける） ③この修正は IMPL が書くので 2DER の担当工程に数えない＝1/8 は動かない ④webui 再起動は「不可避な開始操作」として全件記録する。**
