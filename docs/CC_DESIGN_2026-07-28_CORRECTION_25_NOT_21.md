# 設計/監査 → MGR（写: Taka / IMPL）: **★訂正（急ぐ）— 実呼び出しは 21 ではなく 25。独立判断の対象は 17 ではなく 21 である**

- `BUILD_ROLE: 参照`（**訂正。★到達性の判断は1件も書いていない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8` — `§12` を最大版で読んだ値）**
- **★緊急度: 高** — **MGR は私の「21」を採って範囲を17件に絞った。** **IMPL が着手する前に直す。**

## 0. ★結論
```
32行（機械的一致・固定した母数）
 − 7（呼び出しでない行）
 ─────────
 = ★25（実際の呼び出し）        ← 私は「21」と書いた。★誤り
 − 4（私が到達性を公開済＝汚染済）
 ─────────
 = ★21（独立判断の対象）        ← MGR は「17」と書いた。★私の誤りが原因
```

## 1. ★呼び出しでない7行（機械的に特定・全掲載）
```
再現: 32行のうち、コメント/docstring/文字列リテラル/JavaScript/別ライブラリを機械的に抽出

  2  egl/egl/core.py:122              コメント（「submit() の外からも呼ばれる」）
  5  egl/structure/s2_extract.py:144  ex.submit(work, k)          ← ThreadPoolExecutor
 15  twoder/counterfactual_runner.py:5  docstring
 22  twoder/select_and_create.py:91   コメント
 24  twoder/submit.py:98              文字列リテラル entry="submit()"
 31  twoder/webui.py:637              JavaScript（onclick="submit()"）
 32  twoder/webui.py:650              JavaScript（async function submit()）
```
**∴ 32 − 7 = ★25。**

## 2. ★私の誤りの機構（本日の型の8つ目）
**先行文書 `CC_DESIGN_2026-07-28_ENTRY_COUNT_FIXED_DEFINITION.md` §3 の表に、こう書いた:**
```
| ★別関数の同名呼び出し | 4 | select_and_create.py:150（…）／egl/docs/evidence/…:57（同上・証跡ファイル）／
                             s_retention_repair_a.py の4行のうち…  ← ★ここで途切れている
```
> **★「4」と書いたが、列挙は2件で途切れている。** **残り2件を私は書いていない。**
> **★そして「4」を 25 から引いて「21」にした。**
> **∴ ★数えていない数を引いた。** **列挙が途中で終わっていることに、自分で気づかなかった。**

**★本日の型の8つ目である**:
| 型 | 内容 |
|---|---|
| 1〜7 | 既登録 |
| **8（新）** | **★根拠の列挙が途中で切れているのに、その件数を計算に使った** |

**★1〜7は「見えていなかった」型である。** **8は★「自分が書いた文章を読み返していない」型である。** **別種である。**

## 3. ★これが与える影響
| # | 影響 |
|---|---|
| 1 | **MGR の「17件」は★私の誤りに由来する。** **正しくは21件** |
| 2 | **∴ IMPL が17件で着手すると、★4件が誰にも判断されないまま残る** |
| 3 | **★どの4件が漏れるかは、私が「別関数」として引いた4件が特定できない以上、★決められない。** ∴ **25件から公開済4件を引いた21件を、そのまま対象にするのが安全である** |

## 4. ★私はまだ到達性を1件も書いていない（手順の遵守）
- **MGR §3-1「あなたは17件について到達性の判断をまだ書かない」を守っている。**
- **★本文書は件数の訂正であって、到達性の判断ではない。**
- **★私は自分の判断を、IMPL の文書を読む前に書き終える**（MGR §3-3）。**手元で書き、IMPL が出してから公開する。**

## 5. ★併せて（`s_retention_repair_a.py` の4行について）
**私は先行文書で「同じ `phase0.record_utterance` である（実読）」と書いた。** **∴ 別関数ではない。**
> **★つまり「別関数の同名呼び出し 4」という区分自体が、実体を持っていなかった可能性が高い。**
> **★ただし断定しない。** **`egl/docs/evidence/…:57` と `select_and_create.py:150` の2件は実際に別関数である**（実読済）。
> **∴ 正しくは「別関数 2件」だった可能性が在るが、★その2件も「呼び出しではある」ので 25 からは引かない。**

---
*CC-α 訂正（急ぐ）。★**実呼び出しは 21 ではなく 25**、∴ 独立判断の対象は **17 ではなく 21** である（32行 − 呼び出しでない7行 = 25、− 公開済4件 = 21）。呼び出しでない7行は機械的に特定して全掲載した（`core.py:122` コメント／`s2_extract.py:144` の `ThreadPoolExecutor.submit`／`counterfactual_runner.py:5` docstring／`select_and_create.py:91` コメント／`submit.py:98` 文字列リテラル／`webui.py:637,650` JavaScript）。★誤りの機構=先行文書 §3 の表に「別関数の同名呼び出し 4」と書いたが**列挙は2件で途切れており残り2件を書いていない。にもかかわらずその「4」を 25 から引いて「21」にした** ∴ **数えていない数を引いた**。**本日の型の8つ目=根拠の列挙が途中で切れているのに、その件数を計算に使った**——1〜7は「見えていなかった」型だが、8は**「自分が書いた文章を読み返していない」型**で別種である。★影響=MGR の「17件」は私の誤りに由来し正しくは21件で、17件で着手すると4件が誰にも判断されないまま残る。どの4件が漏れるかは特定できない以上、**25件から公開済4件を引いた21件をそのまま対象にするのが安全**。★手順は守っており、**到達性の判断は1件も書いていない**（本文書は件数の訂正であって判断ではない）。私は自分の判断を IMPL の文書を読む前に手元で書き終え、IMPL が出してから公開する。★併せて=`s_retention_repair_a.py` の4行は「同じ `phase0.record_utterance`」と実読で書いており別関数ではない ∴「別関数の同名呼び出し 4」という区分自体が実体を持っていなかった可能性が高いが断定はせず、実際に別関数なのは `egl/docs/evidence/…:57` と `select_and_create.py:150` の2件で、**その2件も「呼び出しではある」ので 25 からは引かない**。*
