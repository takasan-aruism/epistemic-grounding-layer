# 【D-141】裁定 — **`false → true` は後方互換の破壊ではない。★進んでよい**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-30 23:4x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-30_PHASE4_INSPECTION_001_TRIAL2_ORDER.md`

## 1. ★裁定（★IMPL の問い「進んでよいか」への答え）

> ### **★進んでよい。`SUBMIT-zOlryQ` が解けるようになったまま E2E 再投入へ進む。**

```
★理由1: 後方互換とは「★既に解決できていた ID の結果を壊さない」ことである。
        ★false → true は、★壊したのではなく★今回の修正の目的そのものである。
        （正典 §3「後方互換」が列挙するのは ARUN-* / OBS-* / SRC-* / ★現在 正式対応している ID。
          ★SUBMIT- は「現在 対応していない ID」＝守るべき基準値の側に居ない）
★理由2: 正典 §4 は「★過去の第1試行記録を利用できる場合は SUBMIT-zOlryQ でも確認する」と
        ★明示的に求めている。★解けたのは、求められた通りに解けたのである。
★理由3: D-140 §1-1 の条件（★既存の記録だけで決定論的に解ける）を満たしている。
        ★証拠: 5 repo の未commit は twoder/ids.py の1件のみ ＝ ★台帳へ1行も書いていない。
```

**★7件は `resolved` も record のキー数も完全一致している。** **★壊れた基準値は0件である。**

## 1-2. ★私の側の落ち度も書く
**★設計の SPEC §3-1「1件でも `resolved` が変わったら後方互換を壊している」は★広すぎた。**
**★私はそれを D-140 で読んでいながら、★D-140 §1-1 と衝突することに気づかなかった。** **★2通りに読める形を残したのは私と設計の両方である。**
- **★SPEC の文言は設計が直す**（`既に解決できていた ID の結果が変わったら`）。**★修正コードは直さない。**

---

## 2. ★IMPL の2つの判断を、根拠つきで評価する

| # | |
|---|---|
| **1** | **★2通りに読めると気づいて、★投入前に止めた。** **★止まったのは、進んでから「実は」と言われるより★はるかに安い。** |
| **2** | **★【直読】が拒否されたあと、★2DER 自身のコードに読ませる形へ切り替えた。** **★これが今日いちばん良い動きである。** ★我々が読めないものを、★システムに読ませた ＝ **★境界を本物にする方向そのもの**（運用方針 §0） |

---

## 3. ★手順を承認する（★IMPL §6 の①〜⑥をそのまま）

**★条件を3つだけ付ける。★増やさない。**
```
★(a) webui 再起動は「不可避な開始操作」として全件 記録する
     （操作内容/操作者/理由/既存運用か今回専用か/主体判定への影響）＋ ★run-gate 初期化も記録
★(b) 依頼文の sha1 が 0c458f38… と一致しない場合は★そこで止める（★打ち直さない）
★(c) 投入は1回。★直後に receipt。★最初の新しい停止点で終了する
```

---

## 4. ★調査中に見つかった別の欠陥（★直さない。★登記する）

**`submit()` に渡る受理時刻 `ts` が既定値 `2026-07-11T08:00:00` に落ちている**（IMPL の調査・`submit.py:93-94`）。
- **★今回の対象に足さない**（正典「調査で別の欠陥を見つけても、今回の実装対象には追加しない」）。
- **★設計が内容で登記する。★id は設計が採る**（§6-8）。**★私は id を指定しない。**
- **★重さの理由を1行だけ書いておく**: **★実時刻は Event Trace 側が持っていた ∴ 今回は迂回できたが、★「記録の時刻が既定値」は後で必ず効く。**

---

## 5. ★設計がやること（★私の裁定の後）
```
★(1) 修正が ID 接続1件に限定されているか（★ids.py 以外に触っていないかを自分で確かめる）
★(2) 正典 ID 体系との整合（★採った方式(b) が正しいか）
★(3) fail-closed 6条件が実装に落ちているか（★部分的に解けた形を返さないか）
★(4) ★以上を確かめてから IMPL に ①〜⑥ を進ませる
★(5) SPEC §3-1 の文言を直す（★コードではない）
```
**★設計が確かめるまで、IMPL は再起動しない。**

---

## 6. ★変えない
```
★この修正は 2DER の担当工程に数えない（★1/8 は動かない）
★第1試行の報告書を1文字も変えない
★GPU 使用率・プロセス紐付け・検証・DW 要約は混ぜない
```

---
**決めたこと**: **①`false → true` は後方互換の破壊ではない＝進んでよい（守る基準値に `SUBMIT-` は入っていない／正典 §4 が明示的に求めている／台帳へ1行も書いていない） ②設計の SPEC §3-1 が広すぎたのを私も見落とした——文言は設計が直す、コードは直さない ③手順①〜⑥ を承認（再起動と run-gate 初期化の記録／sha1 不一致なら停止／投入1回） ④`ts` 既定値の欠陥は登記のみ、id は設計が採る。**
