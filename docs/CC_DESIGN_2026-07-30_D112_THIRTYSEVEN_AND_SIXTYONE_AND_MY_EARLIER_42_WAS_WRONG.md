# 設計/監査 → MGR（写: Taka / IMPL）: **D-112 — ★37本／61本（MGR の数と一致）。★私の以前の「42本」は誤りだった／★「印が在る」は「逃がしている」の証明ではない**

- `BUILD_ROLE: 参照`（**測定のみ。★走らせていない・★投入していない・★台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **裁定**: `CC_MGR_2026-07-30_D110B_FLIP_THE_DEFAULT_RUN_ONLY_WHAT_IS_PROVEN_SAFE.md` §3

## 0. ★2DER 優先原則の5点
| ① 入口 | **★無し**（★これは 2DER への問いではなく、★我々の道具の測定である） ／ ②③ **該当しない** ／ ④ **実装しない** ／ ⑤ **該当しない** |
|---|---|

## 1. ★数（★MGR の述語をそのまま使った）
```
★述語（MGR §3・★私が変えていない）:
  「その本の中に EGL_DATA_DIR / DW_DATA_DIR / mkdtemp / TemporaryDirectory の★いずれかが在る」

再現（★全98本・打ち切り無し）:
  ★全数            98
  ★印が在る（走らせてよい候補）  ★37
  ★印が無い（走らせない）        ★61
  ★内訳: mkdtemp 37 ／ DW_DATA_DIR 19 ／ EGL_DATA_DIR 15（★重複あり）
```
> **★MGR の「98 − 61 = 37」と一致した。**

## 2. ★私の以前の数「42本」は誤りだった（★自己申告）
```
D-109 で私が書いた: 「★42本が EGL_DATA_DIR / DW_DATA_DIR / mkdtemp / TemporaryDirectory のいずれかを使っている」
★実際に私が打った述語: "DW_DATA_DIR\|EGL_.*DIR\|DS_.*DIR\|tmpdir\|TemporaryDirectory\|mkdtemp"
★MGR の述語         : "EGL_DATA_DIR\|DW_DATA_DIR\|mkdtemp\|TemporaryDirectory"
∴ ★私は `DS_.*DIR` と `tmpdir` を余分に入れていた → ★42 になった
```
| | |
|---|---|
| **★何が起きたか** | **★同じ対象を、★違う述語で数えて 42 と 37 になった** |
| **★なぜ気づけなかったか** | **★私は D-109 に「42本」とだけ書き、★述語を書かなかった** |
| **★今日の裁定に照らすと** | **★G-50 で私自身が「述語を1つ決めて書く」を実行した。★その翌時間に、★述語を書かずに数を出していた** |

> **★数だけ書くと、★後から比べられない。** **★「数の横に、何を数えたかを書く」を、★私が守っていなかった。**

## 3. ★「印が在る」は「逃がしている」の証明ではない（★先に出す）
```
★この述語が言えること : 「その本のどこかに、逃がす記述が★在る」
★言えないこと         : 「★本番へ書く前に、★その逃がしが効いている」
```
| ★起こりうること | **★`mkdtemp` を別の用途に使い、★台帳の向き先は既定のまま、という本が在りうる** |
|---|---|
| **★私は確かめていない** | **★37本を1本ずつ読んでいない。★読めば分かるが、★今は読んでいない** |
| **★どちらに倒すか** | **★MGR の既定に従えば「迷うなら走らせない」** ∴ **★37本も「候補」であって「安全」ではない** |

> **★私は「37本は安全である」と書かない。** **★「印が在るのが37本である」と書く。**
> **★これは今日ずっと数えてきた「1つの語が2つを指す」を、★私が作らないためである。**

## 4. ★MGR へ（★私は決めない）
| # | |
|---|---|
| **1** | **★37本を「候補」と呼ぶか「走らせてよい」と呼ぶか**（★私は「候補」と呼ぶべきだと思うが、★決めない） |
| **2** | **★1本ずつ読んで「逃がしが効いているか」を確かめるか**（★37本。★読むだけで走らせない） |
| **3** | **★①②が決まるまで、★私は1本も走らせない** |

## 5. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **37本それぞれで逃がしが効いているか** | **★読んでいない**（§3） |
| 2 | **61本の中に、実際には書かない本が在るか** | **★見ていない。★「印が無い」だけである** |
| 3 | **印が4つで足りるか** | **★MGR の述語をそのまま使った。★他の逃がし方が在るかは見ていない** |

---
*CC-α D-112（測定のみ・走らせていない）。★**数**=MGR の述語（「その本の中に `EGL_DATA_DIR`/`DW_DATA_DIR`/`mkdtemp`/`TemporaryDirectory` のいずれかが在る」）を**変えずに**全98本・打ち切り無しで数え、**印が在る37／無い61**（内訳 `mkdtemp` 37／`DW_DATA_DIR` 19／`EGL_DATA_DIR` 15・重複あり）で**MGR の「98 − 61 = 37」と一致**。★**CC-α の以前の「42本」は誤りだった（自己申告）**——D-109 で「42本が…のいずれかを使っている」と書いたが、**実際に打った述語は `DS_.*DIR` と `tmpdir` を余分に含んでおり**、MGR の述語との差で **42 と 37 になった** ∴ **同じ対象を違う述語で数えていた**——**気づけなかったのは D-109 に「42本」とだけ書き述語を書かなかったからであり、G-50 で CC-α 自身が「述語を1つ決めて書く」を実行したその翌時間に、述語を書かずに数を出していた**（**数だけ書くと後から比べられない。「数の横に、何を数えたかを書く」を CC-α が守っていなかった**）。★**「印が在る」は「逃がしている」の証明ではない（先に出す）**——**この述語が言えるのは「その本のどこかに逃がす記述が在る」までで、「本番へ書く前にその逃がしが効いている」は言えない**（**`mkdtemp` を別の用途に使い台帳の向き先は既定のまま、という本が在りうる**）——**CC-α は37本を1本ずつ読んでおらず**、**MGR の既定「迷うなら走らせない」に従えば37本も「候補」であって「安全」ではない** ∴ **「37本は安全である」と書かず「印が在るのが37本である」と書く**。★**MGR へ（CC-α は決めない）**=**37本を「候補」と呼ぶか「走らせてよい」と呼ぶか**（CC-α は「候補」と呼ぶべきだと思うが決めない）／**1本ずつ読んで逃がしが効いているか確かめるか**（37本・読むだけで走らせない）／**①②が決まるまで CC-α は1本も走らせない**。★確かめていないこと=**37本それぞれで逃がしが効いているか（読んでいない）**／**61本の中に実際には書かない本が在るか（「印が無い」だけ）**／**印が4つで足りるか（MGR の述語をそのまま使い、他の逃がし方は見ていない）**。*
