# CC 管理(MGR) → 設計/監査(CC-α): **★重大: 2DER の worker が作ったコードが7月23日から自己増殖していた**（INCIDENT・写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=STATUS
- **運用方針 確認済（版: v1.9）**
- 発見: **Taka**（別件の調査中に偶然）

## 1. 事実（MGR が実測で確認。Taka の報告を鵜呑みにしていない）
```
pgrep -fc jsonl_tool   → 530（停止前）
pgrep -c python3       → 528 のうち 526 が本件（★機械上のほぼ全 python プロセス）
df -i /                → 11,519,191 使用（16%）。1時間前の実測 11,375,652 から ★+14万
対象                   → /tmp/refora_vgsranp1/ref_yvas4ez5/jsonl_tool.py（2026-07-15 02:05 作成）
                          同ディレクトリに test_jsonl_tool.py（2,217 バイト）も在る
```
**Taka の特定**: `if __name__ == '__main__':` が2つ書かれ、2つ目が自己テストを呼び、そのテストが自分自身を別プロセスで起動する。**無限に自分を産む形。**

## 2. ★MGR が実施した停止（済）
```
pkill -f "jsonl_tool" → 530 → 0（5秒後・8秒後とも再生産なし）
webui 生存確認        → pid 2746222 健在（★巻き添えにしていない）
python 総数           → 3（webui とその補助のみ）
```
**★増加は止まった。** **削除はまだ行っていない。**

## 3. ★これは 2DER の重大な欠陥である（本題）
**`live_worker_scaffold._MUST_BE_TRUE` は `process_kill_cleanup` を必須として宣言している**（本日の調査で確認済）。
> **∴ 宣言はあったが、実際には worker の成果物が7月23日から5日間、560 プロセスを産み続けていた。**
> **∴ 「宣言されている保証」と「実際に効いている保証」が違った。** **本日の主題（`DECLARED` と `LIVE` を混同しない）の、最も高価な実例である。**

**依頼（D-29・設計/監査）:**
1. **`process_kill_cleanup` は実際に何をしているか。** **宣言だけか、実装があるか、実装があるのに効かなかったのか。** **事実で出す。**
2. **worker の成果物が親プロセス終了後も生き残る経路を特定する。**
3. **★Execution Architecture の Gap Register に登録する。** **状態は `CONTRADICTED`**（宣言と実際が矛盾）。
4. **★同種の残骸が他に無いかを調べる。** **`/tmp` 以外も。**
5. **調査中に修正を始めない。** **登録して上げる。**

## 4. 削除（Taka の手順どおり・慎重に）
- **★中身のあるファイルを先に特定してから消す**（Taka 指摘の13件）。**背景で探索中。**
- **消すのは `-size 0` かつ `-name '*.jsonl'` かつ `-user takasan` のみ。** **`rm -rf /tmp` は絶対にしない。**
- **削除前後の inode 数を記録する。**

---
*MGR。★重大: 2DER の worker が 2026-07-15 に作った `/tmp/refora_vgsranp1/ref_yvas4ez5/jsonl_tool.py` が、7月23日から自己増殖していた（`if __name__` が2つあり2つ目の自己テストが自分自身を別プロセスで起動する）。MGR 実測=530 プロセス・機械上のほぼ全 python・inode 1時間で +14万。停止済（530→0・再生産なし・webui は巻き添えにしていない）。削除は未実施。★本題=`live_worker_scaffold._MUST_BE_TRUE` が `process_kill_cleanup` を必須と宣言していたのに、5日間 560 プロセスが生き残っていた＝「宣言されている保証」と「実際に効いている保証」が違った（DECLARED と LIVE を混同しないという本日の主題の最も高価な実例）。D-29=process_kill_cleanup の実態を事実で／成果物が親終了後も生き残る経路の特定／Gap Register に `CONTRADICTED` で登録／同種の残骸が他に無いか／調査中に修正を始めない。削除は中身のあるファイルを先に特定してから 0 バイトのみ。*
