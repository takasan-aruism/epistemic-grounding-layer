# 設計/監査 → MGR（写: Taka / IMPL）: **D-63 — ★押しても黙る機構をコードで特定した。投入していない。★ただし「これが起きた」とは言えない**

- `BUILD_ROLE: 参照`（**調査のみ。★投入していない・コードを1行も変えていない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **契機**: Taka 逐語「**送信ボタンを押したけど何も動かないよ**」

## 0. ★結論（言い過ぎない形で）
> **★「押しても黙る」機構は★コードに在る。** **3つ特定した**（§1）。
> **★ただし「今回それが起きた」とは言えない。** **★サーバが1行も記録していないためである**（MGR 実測: ログ0バイト）。
> **∴ ★機構は言える。原因は言えない。** **★分けて書く。**

---

## 1. ★黙る機構（コードで特定・3件）
### 1-1. ★`submit()` に `try/catch` が無い
```
再現: awk 'NR>=617' twoder/webui.py | grep -c "try{\|catch"  → ★2
再現: その2件の位置 → :754 / :755（どちらも★JSON.parse の中）
∴ ★submit() / load() / run() / openByRun() に try/catch は★1つも無い。
```
```javascript
const r = await fetch("/api/submit", {…}).then(x => x.json());
```
> **★`fetch` が失敗するか、`x.json()` が失敗すると、★そこで Promise が reject する。**
> **★捕まえる者が居ない。** **∴ 画面は「2DER 処理中…」のまま★止まる。** **★エラーも出ない。**

### 1-2. ★`/api/submit` は認証を要求し、拒否は★本文0バイトで返る
```
再現: webui.py:527-529  def do_POST(self): if not self._auth_ok(): return self._deny()
再現: webui.py:486-488  def _deny(self): send_response(401) … Content-Length: 0
```
> **∴ 認証が通らなければ★401・本文0バイトである。**
> **∴ `x.json()` は★空文字を parse しようとして失敗する。** **∴ §1-1 の経路に落ちる。**

### 1-3. ★`/api/submit` の fetch だけ `Authorization` を付けていない
```
再現: grep -n "AUTHZ" twoder/webui.py
  :455 const AUTHZ = "Basic …"          ← ★/ 側で定義
  :459 fetch("/api/approve", { … "Authorization": AUTHZ })   ← ★approve は付けている
再現: :651 fetch("/api/submit", { headers:{"Content-Type":"application/json"} })  ← ★付けていない
```
| | |
|---|---|
| **★事実** | **`/api/approve` は明示で付け、`/api/submit` は付けていない** |
| **★ただし** | **同一オリジンなら、ブラウザが Basic 認証を★自動で付ける場合が在る** |
| **∴** | **★「付けていないから失敗した」とは言えない。** **★条件次第である** |

---

## 2. ★「これが起きた」と言えない理由（★最重要）
```
MGR 実測: /home/takasan/webui_8770.log は ★0 バイト（4時間25分 稼働・GET も多数）
```
> **∴ サーバは★受けたことも断ったことも記録していない。**
> **∴ 「401 が返った」も「そもそも届いていない」も、★どちらも証拠が無い。**
> **★私は §1 の3件を「機構」として書いた。** **★「原因」として書いていない。**

**★確かめる手段は「もう1度 押す」しかない。** **★確かめない**（Taka 常設の禁止・架空の依頼を本番経路へ入れない）。
**★「たぶん 401 だろう」を書かない。**

## 3. ★これは Phase 2 の完了像の真逆である（MGR §3 に同意）
| Taka の完了像 | 今回 |
|---|---|
| **欠損・失敗・未確認が★見える** | **★押した結果が入らず／画面は黙り／記録も残らない** |

> **★今日ずっと「出力の文言」と「入力の文言」を直してきた。**
> **★しかし「押した結果が どうなったか」は、★文言以前に★1文字も出していなかった。**
> **∴ D-57（出力24件）・D-62（入力8件）の外側に、★もう1つ在った。**

## 4. ★言えることだけを整理する（3区分・D-59 の形）
| 区分 | 内容 |
|---|---|
| **決定論** | `submit()` に try/catch が無い／`_deny()` は本文0バイト／`/api/submit` の fetch に `Authorization` が無い — **★すべてコードから determinable** |
| **判断** | **★どれが起きたかは、記録が無いので判断できない** |
| **★人しか出せない** | **★ブラウザの開発者コンソールに何が出たか**（`fetch` の失敗／401／CORS 等）— **★Taka にしか見えない** |

## 5. ★MGR へ（私は決めない）
| # | 論点 |
|---|---|
| **A** | **★「押した結果が見えない」を直すか**（`try/catch` を足して画面に出す）。**★これは文言ではなく振る舞いの変更である** |
| **B** | **★サーバが1行も記録していないことを直すか**（ログ0バイト）。**★`G-38` と同じ「記録できなかったことが残らない」形である** |
| **C** | **★Taka にコンソールを見てもらうか**（`CC_TAKA_` の2件目） |

**【設計:CC-α】★C を先に置くことを推す。** **理由: A も B も「何が起きたか」を知らないまま直すことになる。**
**★ただし推すだけで決めない。** **★A/B は投入なしで作れるが、★何を直すのかが決まらない。**

## 6. ★未確認（推測で埋めない）
| # | |
|---|---|
| 1 | **どこで消えたか**（ブラウザから出なかった／サーバが断った） — **★確かめない**（もう1度押すしかないため） |
| 2 | **ブラウザが Basic 認証を自動で付けたか** — **★我々の側からは分からない** |
| 3 | **ログが0バイトなのは設定か障害か** — **★見ていない** |

---
*CC-α D-63（調査のみ・投入なし）。★結論=**「押しても黙る」機構はコードに3件在るが、「今回それが起きた」とは言えない**（サーバが1行も記録していないため）∴ **機構は言える。原因は言えない。分けて書く**。★機構3件=①**`submit()` に try/catch が無い**（`PAGE` 全体の try/catch は2件で どちらも `JSON.parse` の中。`submit()`/`load()`/`run()`/`openByRun()` には1つも無い）ので `fetch` か `x.json()` が失敗すると Promise が reject して**捕まえる者が居ず、画面は「2DER 処理中…」のまま止まりエラーも出ない** ②**`/api/submit` は認証を要求し拒否は本文0バイト**（`do_POST` が `_auth_ok()` 不成立で `_deny()`、`_deny()` は 401＋`Content-Length: 0`）なので `x.json()` が空文字を parse して失敗し①の経路に落ちる ③**`/api/submit` の fetch だけ `Authorization` を付けていない**（`/api/approve` は `AUTHZ` を明示で付けている）——**ただし同一オリジンならブラウザが Basic 認証を自動で付ける場合が在るので「付けていないから失敗した」とは言えない**。★**「これが起きた」と言えない理由**=`webui_8770.log` が**0バイト**（4時間25分稼働・GET も多数）でサーバは受けたことも断ったことも記録しておらず、「401 が返った」も「そもそも届いていない」も証拠が無い ∴ 3件は**機構**として書き**原因**として書いていない。**確かめる手段は「もう1度押す」しかないので確かめない**（Taka 常設の禁止）。**「たぶん 401 だろう」を書かない**。★**Phase 2 の完了像の真逆**=押した結果が入らず画面は黙り記録も残らない——**今日ずっと出力の文言と入力の文言を直してきたが「押した結果がどうなったか」は文言以前に1文字も出していなかった** ∴ D-57（出力24件）・D-62（入力8件）の外側にもう1つ在った。★3区分=**決定論**（try/catch 無し・`_deny` は本文0バイト・`Authorization` 無し＝すべてコードから determinable）／**判断**（どれが起きたかは記録が無いので判断できない）／**人しか出せない**（**ブラウザの開発者コンソールに何が出たか＝Taka にしか見えない**）。★MGR へ=A「押した結果が見えない」を直すか（**文言ではなく振る舞いの変更**）／B サーバが1行も記録していないことを直すか（**`G-38` と同じ「記録できなかったことが残らない」形**）／C Taka にコンソールを見てもらうか——**CC-α は C を先に置くことを推す**（A も B も「何が起きたか」を知らないまま直すことになる）が推すだけで決めない。★未確認=どこで消えたか（**確かめない**）／ブラウザが Basic 認証を自動で付けたか（**我々の側からは分からない**）／ログ0バイトが設定か障害か（**見ていない**）。*
