# 設計/監査 → **実装(IMPL)**: **Phase 1 完了条件5件を実装・実測してください**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**送付状。★実装源は本文書ではない**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.7` — `§12` を最大版で読んだ値）**
- **確定**: `CC_MGR_2026-07-28_D44_A_CONFIRMED_PROCEED.md`（(A) で進めてよい。訂正は来なかった）

## 1. ★実装源（これ1本）
```
CC_DESIGN_2026-07-28_D44_PHASE1_COMPLETION_BUILD_SPEC_v1_0.md   ← BUILD_ROLE: ★実装源
```

## 2. ★合否は1つだけ
> **Taka ②「同一の CLI 依頼で DS→RRI→EGL を同一 run で追跡できる」**
> **∴ `GET /api/resolve?id=ETR-…` の返りに `component="EGL"` の event が★現れるかどうか。**
> **★現れなければ「現れなかった」と書く。** **繋げる修正をしない。**

## 3. ★今回いちばん手間がかかるところ（先に言う）
**Taka ⑤: 12入口と全書き込み経路を★打ち切りなしで再走査。過去の走査を1つも流用しない。**
| # | 一行 |
|---|---|
| 1 | **★総件数を先に出してから中身を見る。** 「◯件出た」を書く |
| 2 | **★`head` / `tail` / `limit` / `-m` を1つも使わない**（v2.6 §3-8）。**使っていないことをコマンドごと貼る** |
| 3 | **★件数が前回（12入口 / 65行 / 57件中35件）と違ったら、違ったと書く。** **前回に合わせない** |
| 4 | **★私の §1 の計数（台帳12・依頼経路から到達7・合流点4）と突き合わせる。** **一致なら「独立な2つの計数が一致した」、不一致なら★どちらが正しいか確かめる** |

> **★4が本 build でいちばん価値が高い。** **本日、私の計数は2回誤っている**（`head -8` で切れた／`0` の false negative）。
> **∴ あなたの計数が私と違ったら、★私が誤っている可能性の方が高い。** **遠慮なく「設計の計数が誤り」と書いてほしい。**

## 4. ★実装で外しやすい2点
| # | 一行 |
|---|---|
| 1 | **★`emit` を `_idlock()` の中に入れない**（SPEC §2-1-4）。ロック保持時間を延ばすと `H6: 並行採番の直列化` に触る。**入れないと `object_id` が取れないなら、★中に入れず報告する** |
| 2 | **★`egl/egl/contracts.py` の `GUARD_CONTRACTS["core.append_event"]` に1行足す**（SPEC §2-1-5）。**契約表に書かないと、次の誰かが「なぜここで止まるのか」と外す** |

## 5. 投入
- **★前回と同一文面・1回だけ**（SPEC §5）。**`cd /home/takasan` を明示してから実行**（v2.5 §4-17）。
- **webui の再起動は不要**（CLI 経路）。
- **★私の予測**: **EGL の event は出る方に賭ける／親子は繋がる方に賭ける／件数と `request_type` は予想しない。** **外れたら「外れた」と書いてほしい。**

## 6. ★本 build に含めないもの（混ぜない）
- `G-46`（`ETRACE_RUN_ID` を webui から）／**合流点④の fail-closed 化**（Taka 裁定・未実装）
  → **★どちらも Taka の5件に無い。** **私が MGR に「宙に浮いている」と明示して預けた**（SPEC §3）。**あなたは触らない。**
- 進行経路の run／`_emit_pending` への emit／`rthread_events`・`dw/authorization`／`twoder` 自身の台帳9つ。

## 7. ★止まってよい場所
1. **私の計数と違った** → 合わせず両方書く
2. **`_idlock` の外で `object_id` が取れない** → 中に入れず報告
3. **EGL の event が出なかった** → 私の予測が外れた。繋げる修正をせず報告
4. **fail-closed で EGL 自身のテストが落ちた** → 件数と名前を書く
5. SPEC が2通りに読める

---
*CC-α → IMPL 送付状（D-44）。★実装源は `CC_DESIGN_2026-07-28_D44_PHASE1_COMPLETION_BUILD_SPEC_v1_0.md` の1本。★合否は Taka ②のみ=`GET /api/resolve?id=ETR-…` の返りに `component="EGL"` の event が現れるかどうかで、**現れなければ「現れなかった」と書き繋げる修正をしない**。★いちばん手間がかかるのは Taka ⑤（12入口と全書き込み経路を打ち切りなしで再走査・過去を1つも流用しない）=総件数を先に出す／`head`/`tail`/`limit`/`-m` を1つも使わずコマンドごと貼る／**件数が前回(12入口/65行/57件中35件)と違ったら違ったと書き前回に合わせない**／**CC-α の計数(台帳12・依頼経路から到達7・合流点4)と突き合わせ、一致なら「独立な2つの計数が一致した」、不一致ならどちらが正しいか確かめる**——**これが本 build でいちばん価値が高く、本日 CC-α の計数は `head -8` と `0` の false negative で2回誤っているので、計数が違ったら CC-α が誤っている可能性の方が高い。遠慮なく「設計の計数が誤り」と書いてほしい**。★実装で外しやすい2点=`emit` を `_idlock()` の中に入れない（ロック保持時間を延ばすと `H6` に触る。入れないと `object_id` が取れないなら中に入れず報告）／`egl/egl/contracts.py` の `GUARD_CONTRACTS["core.append_event"]` に1行足す（契約表に書かないと次の誰かが「なぜ止まるのか」と外す）。★投入は前回と同一文面で1回だけ、`cd /home/takasan` を明示、webui 再起動は不要。**CC-α の予測=EGL の event は出る方・親子は繋がる方に賭け、件数と `request_type` は予想しない。外れたら「外れた」と書いてほしい**。★本 build に含めないもの=`G-46` と合流点④の fail-closed 化（どちらも Taka の5件に無く、CC-α が MGR に「宙に浮いている」と明示して預けた。IMPL は触らない）／進行経路の run／`_emit_pending`／`rthread_events`・`dw/authorization`／`twoder` 自身の台帳9つ。★止まってよい場所5件。*
