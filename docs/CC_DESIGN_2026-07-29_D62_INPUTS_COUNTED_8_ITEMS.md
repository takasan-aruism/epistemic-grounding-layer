# 設計/監査 → MGR（写: Taka / IMPL）: **D-62 — 入力欄・ボタンを★8件 数え切った。★「何を入れるか」を言っていないのは4件**

- `BUILD_ROLE: 参照`（**調査＋文言案。★コードを1行も変えていない・投入していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **契機**: Taka 逐語「**OPEN 開くのはかまわんけど検索をしろっていうのかな？ 何を検索するか指定して頂戴**」

## 0. ★結論
> **入力側 総数8件**（入力欄2・ボタン6）。
> **★「何を入れるか／何が起きるか」を言っていないのは4件。**
> **★Taka が突いた `OPEN` は、その4件の1つである。**

---

## 1. ★総数（`head`/`-m` を1つも使っていない）
```
再現: grep -oE '<(input|textarea|button|select)[^>]*>' twoder/webui.py
総件数 8 / 確認 8 / 打ち切り無し（入力欄2 / ボタン6）
```

## 2. ★8件の判定（★言えているものも全部書く）
| # | 行 | 実体 | **判定** |
|---|---|---|---|
| **1** | `:641` | `<textarea placeholder="2DERに話す — 例:「QwenとCoderの切替3分が…前の件を優先して進めて」">` | **★言えている**（**例文が入っている**） |
| **2** | `:641` | ボタン `送信` | **★言えている**（上の欄と対で意味が決まる） |
| **3** | `:643` | ボタン `REFRESH STATE` | **★言えていない** — 何が更新されるか書いていない。**★英語だけ** |
| **4** | `:644` | ボタン `RUN NEXT` | **★言えていない** — 何が1つ進むのか書いていない |
| **5** | `:645` | ボタン `RUN UNTIL BARRIER` | **★言えていない** — どこまで進むのか書いていない |
| **6** | `:646` | `<input placeholder="ETR-… で開く">` | **★言えている**（**`ETR-` と書いてある**） |
| **7** | `:647` | ボタン **`OPEN`** | **★言えていない** — **★Taka が突いた1件。** 何を開くのか書いていない |
| **8** | `:403` | ボタン `認可token発行`（`/` 側） | **★言えている**（同じカードに「⚠実行はされません」等の説明が在る） |

**★集計: 言えている 4 / 言えていない 4 / 計 8。**

### 2-1. ★Taka の指摘が正確だった点（記録する）
> **`:646` の欄には `ETR-… で開く` と★書いてある。** **★それでも Taka は「何を検索するか指定して」と言った。**
> **∴ 欄に書いてあっても、★ボタンが `OPEN` だけなら「検索するのか」と読める。**
> **★我々は placeholder を見て「書いてある」と判定していた。** **★ボタンとの対で読まれることを見ていなかった。**

---

## 3. ★直す文言（意味は MGR 指定を守る）
| # | 現在 | **直した後** |
|---|---|---|
| **7** | `OPEN` | **「この番号で開く」** ＋ 欄の `placeholder` を **「依頼番号（`TASK-…`）または記録番号（`ETR-…`）」** に |
| **3** | `REFRESH STATE` | **「いま開いている案件を読み直す」** |
| **4** | `RUN NEXT` | **「次の1工程を進める」** |
| **5** | `RUN UNTIL BARRIER` | **「人の判断が要る所まで進める」** |

### 3-1. ★言わないこと（MGR 明示）
> **★「検索」と書かない。** **一覧から探す機能は★無い。** **★在ると読ませない。**
> **∴ `OPEN` を「検索」にしない。** **「この番号で開く」＝★番号を持っている人が使う、と読める形にする。**

### 3-2. ★入れるものを2種類にする根拠（実測）
```
再現（実測済）: /command は task_id でも ETR- でも開ける
  TASK-2DER-D84215A8 → /api/state が引ける
  ETR-0f6fae35665a   → /api/resolve が引ける（D-50 で実装）
∴ 欄は★2種類を受け付ける。★placeholder が ETR- しか書いていないのは★不足である。
```
> **★これは Taka の指摘から出た2件目である。** **「`ETR-…` で開く」は★半分しか言っていない。**

---

## 4. ★範囲（言い過ぎない）
| ★数え切った | ★数えていない |
|---|---|
| **`/command` と `/` の `<input>`/`<textarea>`/`<button>`/`<select>` — 8件** | **キーボード操作・リンク（`<a>`）** |
| | **API を直接叩く経路**（画面ではない） |

> **★「入力側を全部数えた」とは書かない。** **★数えたのは この4種のタグである。**

## 5. ★私は直さない（裁定を仰ぐ）
- **文言案のみ。** **★コードを1行も変えていない。**
- **★4件を1回で直すか分けるかは MGR が決める。**
- **★出力側の9件（D-58）は済んでいる。** **本件は★入力側の1回目である。**

## 6. ★未確認
| # | |
|---|---|
| 1 | **画面をブラウザで見ていない**（HTML の実読で判定）。**★ただし今回は Taka が開いた1件が根拠に在る** |
| 2 | **`<a>` リンクを数えていない**（§4） |
| 3 | **`/` 側のボタンは1件だけ**（`:403`）。**★他に無いことは総数8で確認したが、動的に生成される要素は見ていない** |

---
*CC-α D-62。★結論=入力側は**総数8件**（入力欄2・ボタン6）で、**「何を入れるか／何が起きるか」を言っていないのは4件**、Taka が突いた `OPEN` はその1つ。★判定=言えている4件（`textarea`＝**例文が入っている**／`送信`＝上の欄と対で意味が決まる／`ETR-… で開く` の欄／`認可token発行`＝同じカードに説明が在る）、言えていない4件（`REFRESH STATE`＝何が更新されるか書いていない・**英語だけ**／`RUN NEXT`＝何が1つ進むのか／`RUN UNTIL BARRIER`＝どこまで進むのか／**`OPEN`**＝何を開くのか）。★**Taka の指摘が正確だった点**=`:646` の欄には「`ETR-… で開く`」と**書いてある**のに Taka は「何を検索するか指定して」と言った ∴ **欄に書いてあってもボタンが `OPEN` だけなら「検索するのか」と読める**——**我々は placeholder を見て「書いてある」と判定し、ボタンとの対で読まれることを見ていなかった**。★直す文言=`OPEN`→「この番号で開く」＋欄の placeholder を「依頼番号（`TASK-…`）または記録番号（`ETR-…`）」に／`REFRESH STATE`→「いま開いている案件を読み直す」／`RUN NEXT`→「次の1工程を進める」／`RUN UNTIL BARRIER`→「人の判断が要る所まで進める」。**「検索」と書かない**（一覧から探す機能は無く、在ると読ませない）。★**入れるものを2種類にする根拠は実測**=`/command` は `task_id` でも `ETR-` でも開ける（前者は `/api/state`、後者は `/api/resolve`・D-50 で実装）∴ **placeholder が `ETR-` しか書いていないのは不足**——**Taka の指摘から出た2件目で「`ETR-…` で開く」は半分しか言っていない**。★範囲=数え切ったのは `/command` と `/` の `<input>`/`<textarea>`/`<button>`/`<select>` の8件で、**キーボード操作・`<a>` リンク・API を直接叩く経路は数えていない** ∴「入力側を全部数えた」とは書かない。★CC-α は文言案のみでコードを1行も変えておらず、4件を1回で直すか分けるかは MGR が決める（出力側の9件は D-58 で済み、本件は入力側の1回目）。★未確認=画面をブラウザで見ていない（**ただし今回は Taka が開いた1件が根拠に在る**）／`<a>` を数えていない／動的生成の要素は見ていない。*
