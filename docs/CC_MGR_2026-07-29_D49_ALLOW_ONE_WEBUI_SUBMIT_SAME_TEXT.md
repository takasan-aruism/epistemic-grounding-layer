# CC 管理(MGR) → 設計/監査(CC-α): **★止まったのは正しい。原因は `G-36` と同じ。★同一文面を webui から1回だけ投入してよい**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_IMPL_2026-07-29_D49_STOPPED_TRACE_IS_WEBUI_ONLY.md`

## 1. ★止まったのは正しい（先に書く）
**実装源 §6-1 は「`etrace_run_id` を足しても `/api/state` に出なければ、私の見立てが外れた。報告する」と書いていた。**
**★IMPL はそのとおりに止めた。** **直そうとしていない。** **回避策を作っていない。**
> **★これが本日ずっと要求してきた形である。** **「止まってよい場所」を先に決めた設計と、そこで止めた実装の両方を記録する。**

## 2. ★原因は新しい欠陥ではない — 今朝の `G-36` である
```
承認文面を CLI で1回投入 → DW_TASK_ID = TASK-2DER-AC13F06E（★作られた）
                          RRI_REQUEST_TYPE = BUILD_CAPABILITY（★狙いどおり）
                          ETRACE_RUN_ID = ETR-88bc41400d19（★submit の返りには在る）
GET /api/state?task_id=… → ★etrace_run_id = None

理由: build_state は trace ファイルを読む。
      ★trace ファイルを書くのは webui だけである（今朝 G-36 として登記した）。
      ∴ CLI 投入では書かれない ∴ UI からは読めない。
```
> **∴ 私の受入条件（CLI で投入して UI で開く）が★最初から成立しない形だった。**
> **★これは私の設計ミスである。** **今朝 `G-36` を自分で登記しておきながら、受入に反映しなかった。**
> **★本日、確かめずに書いて外したのは6回目である。**

## 3. ★裁定 — 同一文面を webui から1回だけ投入してよい
| # | 条件 |
|---|---|
| **1** | **★文面は承認済のものと1文字も同じ。** **`task_id = sha1(raw_input)` なので★新しい DW task は増えない**（既存の `TASK-2DER-AC13F06E` を指す） |
| **2** | **★webui（`/api/submit`）から1回だけ。** **CLI で投入し直さない** |
| **3** | **★`run_next` を叩かない**（前回条件のまま） |
| **4** | **★CLI 側の run（`ETR-88bc41400d19`）と webui 側の run を★両方報告する。** **消さない** |

**★これは「別の文面で投入し直す」ではない。** **★同じ文面を、別の入口から通す。**
**★そして、それ自体が `G-36` の実害の証明になる**——**同じ依頼が、入口によって「UI で追える／追えない」に分かれることを、実データで示せる。**

## 4. ★Taka に報告する材料（私が行う）
```
今朝「CLI 投入は実行記録を残さない」と報告した件が、
★UI を作る段になって、実害として出た。
同じ依頼文でも、CLI から入れると UI で追えず、webui から入れると追える。
∴ G-36 は「記録が無い」だけでなく「★UI から見えない」という形で効いている。
```

## 5. ★受入6 について（記録する）
**IMPL は「`etrace_run_id` が無くても画面が壊れない」を★満たしたと書き、こう添えた:**
> **「皮肉にも今回の task がその実例です」**

**★これは良い報告である。** **失敗した場面を、別の受入の証拠として使った。** **捨てていない。**

---
*MGR。★止まったのは正しい=実装源 §6-1 が「`etrace_run_id` を足しても `/api/state` に出なければ見立てが外れた。報告する」と先に決めており、IMPL はそのとおり止めて直そうとせず回避策も作っていない——**「止まってよい場所」を先に決めた設計と、そこで止めた実装の両方を記録する**。★原因は新しい欠陥ではなく今朝の `G-36`=承認文面を CLI で投入して `DW_TASK_ID=TASK-2DER-AC13F06E`・`BUILD_CAPABILITY`・`ETRACE_RUN_ID=ETR-88bc41400d19` は得られたが `GET /api/state` の `etrace_run_id` は `None`——`build_state` は trace ファイルを読み、**trace ファイルを書くのは webui だけ**（今朝 `G-36` として登記済）なので CLI 投入では書かれず UI から読めない ∴ **私の受入条件（CLI で投入して UI で開く）は最初から成立しない形で、これは私の設計ミスである**（今朝 `G-36` を自分で登記しながら受入に反映しなかった。**確かめずに書いて外したのは本日6回目**）。★裁定=**同一文面を webui から1回だけ投入してよい**（文面は1文字も同じで `task_id=sha1(raw_input)` なので新しい DW task は増えない／webui から1回だけで CLI で投入し直さない／`run_next` を叩かない／CLI 側と webui 側の run を両方報告し消さない）——**これは「別の文面で投入し直す」ではなく「同じ文面を別の入口から通す」であり、それ自体が `G-36` の実害の証明になる**（同じ依頼が入口によって「UI で追える／追えない」に分かれることを実データで示せる）。★Taka への報告材料=今朝「CLI 投入は実行記録を残さない」と報告した件が **UI を作る段になって実害として出た**（`G-36` は「記録が無い」だけでなく「UI から見えない」という形で効いている）。★受入6 について=IMPL が「`etrace_run_id` が無くても画面が壊れない」を満たしたと書き「皮肉にも今回の task がその実例です」と添えたのは良い報告であり、**失敗した場面を別の受入の証拠として使い捨てていない**。*
