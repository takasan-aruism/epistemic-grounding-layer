# CC 管理(MGR): **(a) は着手保留（HOLD）** / 1c 監査の受領 + 次の1手（HANDOFF）

- `BUILD_ROLE: 参照`
- 宛: IMPL(coder) / DESIGN/AUDIT(CC-α) / 写: Taka / 発: MGR / 2026-07-26 19:55 / TYPE=HANDOFF
- 対象: `CC_DESIGN_2026-07-26_RETENTION_REPAIR_A_BUILD_SPEC.md` / `CC_DESIGN_2026-07-26_BUILD1C_FINAL_AUDIT_AND_PRECEDENCE_RULING.md`

## 1. ★HOLD — (a) retention 補修は**まだ着手しない**
BUILD SPEC v1.0（★実装源）は**受領した。中身に異議はない**（小さい・前向きのみ・配線しない・決定論）。**ただし着手条件が満たされていない。**
- SPEC の典拠は `CC_MGR_..._RETENTION_REPAIR_A_GO.md`＝**前 MGR instance が Taka 発話を引用した文書**。**交代後の私はその場に居らず、私にとっても伝聞である。**
- **私は今、Taka に直接確認を取りに行っている**（`CC_MGR_..._BUILD1C_ADJUDICATION_AUDIT_ORDER.md` §4）。**回答はまだ来ていない。**
- **IMPL へ: 回答が私から届くまで (a) に着手しない。** SPEC は用意済みのまま置く。**着手の合図は私が出す。**
- 併せて Taka に「**MGR 経由の承認をどこまで一次典拠として扱うか**」も諮っている。ここが決まるまで、**MGR 引用のみを典拠に LIVE を触らない**を既定とする。

## 2. 1c 最終監査の受領【MGR】
CC-α が `rri` を直接叩いて7項目を検証したこと、**接地hit 0 / 登記台帳を外すと `UNKNOWN` に落ちる対照 / 汚染3件の解消**を確認したことを受領する。**この監査を経たので、私は Taka へ上げる。**
- **過大にしない点も受領**: HBB-30 は「設計が意図した経路で届いた」のであって「自律的に解いた」ではない。**発火率 0/298＝現時点では fixture 専用**。この2点を落とした表現を私は使わない。

### 残った未検証2点（消さないこと）
1. **偽陰性側の母数が未提示**（依頼A-4）。散文を外したことで、**以前は拾えていた実在対象を取りこぼしていないか**。真陽性は `preflight_gate.py` 1件（hit 9）のみ。**実在対象を複数本、対で出すこと。**
2. **「CC-α 自身が新しい DE を1件足して汚染再検査」は実施の明示がない**（IMPL 報告への同意と負の対照の再現に留まる）。**独立性はここだけ弱い。**次の BUILT に含めれば足りる。

## 3. 次の1手（1c・これは進めてよい）
裁定 B①（優先表: 失敗履歴 > 登記された地位 > 表層signal）を **IMPL が実装**する。**受入は CC-α 指定どおり両方**:
- **(i) 非回帰 13/13**（優先表を**有効にした状態**で）
- **(ii) HBB-30 → `SUPERSEDE`**
- **片方だけなら採用しない。** 片方しか出せなかった場合は**出せなかったことを書く**（既定に戻して隠さない）。

続けて **B③ df 閾値20 の頑健性**を測る（閾値を動かした時の 負の対照/PP1-3/実在ファイル/HBB-30 の判定表。**測定後に都合の良い値へ動かさない**）。§2 の偽陰性母数はこの測定に同梱してよい。

---
*MGR。(a) は Taka 直接確認待ちで HOLD（SPEC は受領・着手の合図は私が出す）。1c 最終監査は受領し Taka へ上げる。未検証2点=偽陰性側の母数・CC-α 自身の DE 追加による汚染再検査。次の1手=優先表の実装（受入は非回帰13/13 と HBB-30 SUPERSEDE の両方）→ 閾値20 の頑健性。*
