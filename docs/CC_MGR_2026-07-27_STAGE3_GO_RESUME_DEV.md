# CC 管理(MGR) → 設計/監査(CC-α): **段3 へ — 開発を再開する（worker 再挑戦 + 台帳を読める仕組み）**（HANDOFF）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**

## 0. 権限
Taka 直接発話（2026-07-27・逐語）:
> **「成果物はあなたたちAIが２DERの機能を忘れないために必要なもんだ。今後管理して状況を忘れず、正しい内容を更新して使うようにしてくれりゃいい。開発を進めて」**

**∴ 提示・承認の手続きは不要。** **資料は運用に載せ、開発を再開する。** **作業指示書 §11 の「まず資料を完成させて提示」は、この発話で解除された。**

## 1. ★資料は「使う」ものになった（運用条件）
1. **更新義務**: **Capability / Edge / entrypoint / LLM 呼出 / 正本 / state machine を変えたら、資料の更新を完了条件に含める**（指示書 §8）。
2. **★更新忘れは機械が出す**: 状況表に **`実行構造の資料: 現在の HEAD と一致 / ★古い — 要更新`** を追加した（記録 commit と現 HEAD の比較。**新しい機構は作らず git と既存 JSON のみ**）。**コードが動けば自動で「古い」に変わる。**
3. **未達4件は消さない**（EventStore 等の責務比較／Development Context 構想との差分／Markdown と JSON の乖離検証 P-05／提示）。**資料の gap として残す。**
4. **★資料を「読む」義務**: **設計に入る前に、該当箇所を資料で確認すること。** **資料に無ければ「無い」と分かる。** **それが今日まで無かった。**

## 2. 段3（開発再開）— この順で
| # | 件 | 根拠 |
|---|---|---|
| **1** | **★worker 再挑戦（contract を添える）** | D-22 で契約の正体が判明（`skeleton` + `immutable_tests` + 封印 sha256、**依頼者が渡す**）。**Build 12 の失敗は我々が渡さなかったから。** |
| **2** | **台帳を読める仕組み**（front door → `ids.resolve` + 本文を返す1段） | 優先度1の統合設計は承認済（`..._PRIORITY1_APPROVED_RUN_THE_WORKER.md`） |
| **3** | 済んだら **`CC_*.md` の直読を潰す** | `..._CRUSH_THE_SIDE_CHANNELS.md` §4 の順序 |

**★1 と 2 は同じ作業である。** **worker に作らせるものが 2 だからである。** **別々に数えない。**

## 3. 条件（既定の再掲・追加なし）
- **contract を新設しない**（既存 `contract_seal` の形式を使う）。
- **★失敗しても手で書かない。** **理由（`planner_outcome` / `problems`）を記録して上げる。**
- **1段ずつ・`run_until_barrier` を使わない・token を迂回しない。**
- **成果物は sandbox から**その場で**受け取り sha256。** **配置は検査の後。**
- **「作れるようになった」と書かない。**

## 4. 未処理の解消（MGR から）
**`D21_PATH_CONVENTION_DEFECT`（`doc_id` の誤り）を受領した。** **台帳の初日欠陥であり、隠さず出したことを評価する。**
**★修正は段3 と並行してよい。** **ただし「膨らませない」——`doc_id` の生成規則を1つに直すだけ。** **列を足さない。**

---
*MGR。Taka「開発を進めて」により段3 へ。資料は提示不要で運用に載せる（更新義務／更新忘れは状況表の「実行構造の資料」行が機械的に出す／未達4件は gap として残す／★設計に入る前に資料で確認する義務）。段3=①worker 再挑戦（contract を添える。Build 12 の失敗は我々が渡さなかったから）②台帳を読める仕組み③済んだら CC_*.md の直読を潰す。★①と②は同じ作業（worker に作らせるものが②）。条件は既定どおり（contract を新設しない／失敗しても手で書かない／1段ずつ／sandbox から即受け取り／「作れるようになった」と書かない）。D-21 の doc_id 欠陥は並行修正可、ただし生成規則を1つに直すだけで列を足さない。*
