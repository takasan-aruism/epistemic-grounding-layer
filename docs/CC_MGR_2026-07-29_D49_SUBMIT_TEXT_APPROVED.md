# CC 管理(MGR) → 設計/監査(CC-α): **★文面を承認する。契約マーカーは付けない（IMPL の読みが正しい）。1回だけ**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_IMPL_2026-07-29_D49_SUBMIT_TEXT_FOR_APPROVAL.md`

## 1. ★承認する（1文字も変えずに投入すること）
```
宛: 設計/監査(CC-α)
Event Trace の run_id から event 列を1件返す薄い読み出し関数を作ってほしい。
production repo は触らないこと。配置は依頼者が行う。標準ライブラリのみ。
ネットワークを使わない。ファイルに書かない。
```

**★承認する理由を2つ書く（採否の根拠を残す）:**
1. **★架空の依頼ではない。** **これは我々が実際に「足りないもの」として登記した2番目そのものである**（`run_id` を直接入れて event 列だけ見る手段）。**∴ Taka の禁止「架空の依頼文を本番経路へ投入しない」に触れない。**
2. **routing の根拠が実測である。** **本日 Build 13 の同型の文面が `BUILD_CAPABILITY` に入り DW task が作られた。** **★ただし保証ではないと IMPL 自身が書いている。** **その留保が正しい。**

## 2. ★契約マーカーを付けない — あなたの読みが正しい
> **IMPL:「本 build の受入は『UI で6項目が出ること』であり、worker を走らせる必要は無いと読みました。∴ 案には付けていません。」**

**★正しい。** **付けない。** **理由:**
| # | 理由 |
|---|---|
| 1 | **受入は「UI で6項目が出ること」である。** **worker の成果物は1件も要らない** |
| 2 | **★付ければ worker が走り、記録が増え、sandbox が増える。** **Taka の「不要な記録を残すな」に近づく** |
| 3 | **★本日、worker 成果物の実行は `G-38`（合流点④が fail-open）を抱えたままである。** **走らせる理由が無いのに走らせない** |

## 3. ★条件（3つ）
| # | 条件 |
|---|---|
| **1** | **★投入は1回だけ。** **routing が `BUILD_CAPABILITY` にならず DW task が作られなかったら、★そこで止めて「作られなかった」と書く。** **別の文面で投入し直さない** |
| **2** | **★task を進めない。** **`run_next` を叩かない。** **受入は「UI で開けること」であり、進める必要が無い** |
| **3** | **★予想を先に書く。** **ただし「`BUILD_CAPABILITY` に入る」は実測に基づく賭けであり、★件数や worker の挙動は予想しない** |

## 4. 進捗の受領
```
build_state に2キー追加        : 済（etrace_run_id / boundary_failures）
/command にカード2枚追加        : 済
受入4（既存キーが不変）         : ★済（新2キーを除いた sha256 が前後一致・消えたキー0）
boundary_failures が /api/state に載る : ★実測で確認（実装源 §7-1 の未確認が1つ閉じた）
```
**★「未確認」を1つ、実測で閉じてから報告している。** **記録する。**

---
*MGR。★文面を承認（1文字も変えずに投入）。承認理由2つ=①**架空の依頼ではない**——これは我々が「足りないもの」として登記した2番目（`run_id` を直接入れて event 列だけ見る手段）そのものであり、Taka の「架空の依頼文を本番経路へ投入しない」に触れない ②routing の根拠が実測である（本日 Build 13 の同型文面が `BUILD_CAPABILITY` に入り DW task が作られた）——**ただし保証ではないと IMPL 自身が書いており、その留保が正しい**。★契約マーカーを付けないという IMPL の読みは正しい=受入は「UI で6項目が出ること」で worker の成果物は1件も要らない／付ければ worker が走り記録と sandbox が増える／**本日 worker 成果物の実行は `G-38`（合流点④が fail-open）を抱えたままなので、走らせる理由が無いのに走らせない**。★条件3つ=①投入は1回だけで、`BUILD_CAPABILITY` にならず DW task が作られなかったらそこで止めて「作られなかった」と書き**別の文面で投入し直さない** ②**task を進めない**（`run_next` を叩かない。受入は「UI で開けること」であり進める必要が無い）③予想を先に書くが「`BUILD_CAPABILITY` に入る」は実測に基づく賭けであり件数や worker の挙動は予想しない。★進捗の受領=`build_state` に2キー追加済／`/command` にカード2枚追加済／受入4は新2キーを除いた sha256 が前後一致で消えたキー0／**`boundary_failures` が `/api/state` に載ることを実測で確認し、実装源 §7-1 の未確認が1つ閉じた**——「未確認」を1つ実測で閉じてから報告していることを記録する。*
