# 設計/監査 → **実装(IMPL)**: **押した結果を出し、受けたことを記録してください（承認済）。★受入1 を訂正しました**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**送付状。★実装源は本文書ではない**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.8`）** / **承認**: `CC_MGR_2026-07-29_D64_APPROVED_TWO_CONDITIONS_CANNOT_BE_MET_AS_WRITTEN.md`

## 1. ★実装源（これ1本・★改訂1 が入っている）
```
CC_DESIGN_2026-07-29_D64_SILENT_FAILURE_BUILD_SPEC_v1_0.md   ← BUILD_ROLE: ★実装源
```

## 2. ★改訂1 で変わった2点（★これだけ読めば足りる）
| # | |
|---|---|
| **1** | **★受入1 を訂正した。** 旧「`fetch(` の直呼びが0件」は**満たせない条件だった**（`askJSON` 自身が `fetch` を呼ぶ）。**新: `/command` の `fetch(` が★`askJSON` の中の1件だけになる**（他5件が消える） |
| **2** | **★B は2行に確定**（`RECV` と `SENT`）。**理由: 1行だと「受けたが返せなかった」が1行も残らない——★それは今まさに起きたことと同じ形である** |

## 3. ★この build の一行
> **原因を知って直すのではない。★原因を知るための計器である。**
> **★次に Taka が押したとき、初めて何が起きたか分かる。** **それが目的である。**

## 4. ★外しやすい3点
| # | |
|---|---|
| **1** | **★4つの kind 以外を作らない**（`NO_RESPONSE` / `REFUSED` / `UNREADABLE` / `OK`）。**5つ目が要ると思ったら★止めて報告** |
| **2** | **★原因を推測して画面に出さない。** **「401 でした」と書けるのは★実際に 401 を受け取ったときだけ**（`REFUSED` で status が在るとき） |
| **3** | **★`/` 側の `approve` を触らない。** **★あれが正しい形の出所である**（そこから持ち込むだけ） |

## 5. ★確かめられないものが1つ在る（先に決めてある）
- **B（サーバが受けた記録）は★投入しないと「出たこと」を示せない。** **★投入しない。**
- **∴ 「コードに在ることは示せるが、出たことは示せない」と書く。** **★`G-61`・(a)・(c2) と同じ扱いで未検証のまま登記する。**
- **★MGR 承認済である。** **「確かめられなかった」と書いても不合格にならない。**

## 6. ★受入4 について（MGR が先に言った）
> **`/api/resolve` は存在しない id でも★200 を返す**（`resolved:false`・実測済）。
> **∴ `REFUSED` の実例は★出ない可能性が高い。** **★出なければ「出せなかった」と書く。** **★無理に出そうとしない。**

## 7. ★実行前に1つ
```
状況表: 実行中プロセス: twoder.webui(pid 3565640, 4h前起動: ★ソースが新しい=再起動が要る)
```
**★webui を再起動してから受入を取ること。** **★本日2回踏んだ型である。** **`cd /home/takasan` を明示**（v2.5 §4-17）。

## 8. ★止まってよい場所
1. **`askJSON` にすると成功時の挙動が変わる** → 止めて報告
2. **4つの kind で分けられない場合が出た** → **5つ目を作らず報告**
3. **B を2行で書けない**（既存のログ機構が無い等）→ 止めて報告
4. SPEC が2通りに読める

## 9. ★資料の同期
**`G-70` は★既に資料に入っている**（実測: JSON 70件・MD 2箇所）。**★あなたは資料に触らなくてよい。**
**★`webui.py` の変更ぶんの同期は、MGR の commit 後に私が行う**（D-58 で決めた順番）。

---
*CC-α → IMPL 送付状（D-64）。★実装源は `CC_DESIGN_2026-07-29_D64_SILENT_FAILURE_BUILD_SPEC_v1_0.md` の1本（改訂1 入り）。★改訂1 の2点=①**受入1 を訂正**（旧「`fetch(` の直呼びが0件」は `askJSON` 自身が `fetch` を呼ぶので**満たせない条件**だった。新は「`/command` の `fetch(` が `askJSON` の中の1件だけになる」）②**B は2行に確定**（`RECV`/`SENT`。**1行だと「受けたが返せなかった」が1行も残らず、それは今まさに起きたことと同じ形**）。★一行=**原因を知って直すのではなく原因を知るための計器であり、次に Taka が押したとき初めて何が起きたか分かる**。★外しやすい3点=**4つの kind 以外を作らない**（5つ目が要ると思ったら止めて報告）／**原因を推測して画面に出さない**（「401 でした」と書けるのは実際に受け取ったときだけ）／**`/` 側の `approve` を触らない**（あれが正しい形の出所）。★**B は投入しないと「出たこと」を示せない**ので「コードに在ることは示せるが出たことは示せない」と書き未検証のまま登記する（**MGR 承認済で「確かめられなかった」と書いても不合格にならない**）。★受入4 は `/api/resolve` が存在しない id でも 200 を返すため **`REFUSED` の実例は出ない可能性が高く、出なければ「出せなかった」と書き無理に出そうとしない**。★**webui を再起動してから受入を取る**（本日2回踏んだ型・`cd /home/takasan` を明示）。★止まってよい場所4件。★`G-70` は既に資料に入っており（実測）IMPL は資料に触らなくてよい。`webui.py` 分の同期は MGR の commit 後に CC-α が行う。*
