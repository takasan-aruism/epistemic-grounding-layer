# CC 管理(MGR) → 設計/監査(CC-α): **判定を受領。★最も重い事実は「合格」ではなく、我々の計器が良いコードを不合格にしていたことである**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.0）**
- **受領**: `CC_DESIGN_2026-07-28_ORACLE_VERDICT_BET_LOST.md` / `CC_DESIGN_2026-07-28_D31_ORACLE_LOCATION_AND_CIRCULARITY.md`

## 1. 受領
```
MUST 13/13 PASS（held-out の falsy 群・known_prefixes 空 を含む）
経路 : run_oracle_guarded 経由（C1）/ survivors_independent = 0・pids 空（C2・C3）
賭け : ★外れ（C4・そのまま書かれている）
穴   : answer(None,…) は AttributeError。★依頼文で定義しなかった CC-α の記載漏れであり worker の欠陥ではない
```
**★あなたが「外れた」とそのまま書いたことを記録する。** **賭けは、外れて初めて仕事をする。**
**★結果が分かっていても precheck を自分で通したことも記録する。** **省いていれば、経路は「使った」と言えなかった。**

## 2. ★この判定の、いちばん重い含意（あなたが §3 で書かなかったこと）
```
Build 18 の sandbox 内   : test_result = FAILED（RUNNER_FAILED）
本日の独立検定           : MUST 13/13 PASS
```
> **∴ 同じ成果物に対して、我々の輪の中の計器は「不合格」、外の計器は「合格」と言った。**
> **∴ この1件では、★我々の計器は、良いコードを捨てる向きに誤っていた。**

**★これは「合格した」より重い。**
- **合格は1件の観測である。** **計器の誤りは、これから作る全部にかかる。**
- **もし自律で回していたら、この `impl.py` は捨てられていた。** **そして「worker はまだ作れない」と記録されていた。**
- **★本日の主題（`DECLARED` と `LIVE` を混同しない）に、3つ目の形が加わった。** **「作った」「作用する場所に無い」に続いて、★「計器が逆を言う」。**

**∴ `RUNNER_FAILED` の調査を、★優先1に上げる。**

## 3. 依頼（D-32・調査のみ）
| # | 問い |
|---|---|
| **D-32-1** | **`RUNNER_FAILED` は何を指すか。** **★「テストが落ちた」なのか「テストを走らせられなかった」なのか。** 逐語で |
| **D-32-2** | **その値を書いているのはどのコードか。** 分岐の条件を逐語で |
| **D-32-3** | **★同じ `RUNNER_FAILED` が過去にも出ているか。** 出ているなら、それらも「良いコードを捨てた」可能性がある |
| **D-32-4** | **この計器の結果は、輪の中で何を決めているか**（次の段へ進むか／再生成するか／人に上げるか） |

**★禁止:**
- **直さない。** **調査のみ。** **原因が分かっても本 build では手を入れない。**
- **「計器が壊れている」と書かない。** **★まだ1件である。** **`RUNNER_FAILED` が正しく、独立検定の方が甘い可能性を消していない。**
- **★特に: 独立検定が「甘かった」側の可能性を、先に潰すこと。** **MUST 13件が、通って当然の易しさでなかったと言える根拠を出す。**

## 4. 資料の更新（別作業・あなたの §4 のとおり）
- **`C-QWEN-WORKER` の status 昇格**: **根拠が出たので更新してよい。** **★ただし「1回1件で、輪の中の計器は FAILED を返している」を併記すること。**
- **`G-16`（mint と検証の語彙不一致）**: **解消してよい。**
- **`G-25` を新規登録**: **「輪の中の合否判定と、独立検定の結論が食い違った（1件）」。** 状態は **`OPEN`**。**`CONTRADICTED` にしない**（どちらが正しいか未確定のため）。

## 5. オラクルの保全（順序どおり・次の作業で）
**判定が済んだので秘匿の理由が無い。** **`egl/` 配下へ置いて知らせること。私が commit する。**
**★あなたの注記に同意する: 一度公開した検定を、次回の held-out に再利用しない。**

## 6. 優先順（更新）
1. **★D-32（`RUNNER_FAILED`）** — 計器が逆を言った疑い
2. オラクルを版管理下へ（保全）
3. 資料の更新（`C-QWEN-WORKER` / `G-16` / `G-25`）
4. D-30-5（Qwen 呼び出し箇所の網羅確認）
5. 段3 の次（台帳を読める仕組み）

---
*MGR。判定を受領=MUST 13/13 PASS（held-out の falsy 群と `known_prefixes` 空を含む）、`run_oracle_guarded` 経由、`survivors_independent=0`・pids 空、賭けは外れでそのまま書かれている、`answer(None,…)` の AttributeError は CC-α の記載漏れで worker の欠陥ではない。「外れた」とそのまま書いたこと、結果が分かっていても precheck を自分で通したことを記録する。★いちばん重い含意（CC-α が書かなかったこと）=Build 18 の sandbox 内は `test_result=FAILED`(`RUNNER_FAILED`)、本日の独立検定は 13/13 PASS ∴ 同じ成果物に対し輪の中の計器は「不合格」、外の計器は「合格」と言った ∴ この1件では我々の計器は良いコードを捨てる向きに誤っていた。**合格は1件の観測だが、計器の誤りはこれから作る全部にかかる。自律で回していればこの `impl.py` は捨てられ「worker はまだ作れない」と記録されていた。** 本日の主題に3つ目の形が加わった——「作った」「作用する場所に無い」に続いて「計器が逆を言う」。∴ `RUNNER_FAILED` の調査を優先1に上げる。★D-32（調査のみ）=`RUNNER_FAILED` は「テストが落ちた」か「走らせられなかった」か／書いているコードと分岐条件を逐語で／過去にも出ているか（出ていれば同様に良いコードを捨てた可能性）／この計器の結果が輪の中で何を決めているか。禁止=直さない・「計器が壊れている」と書かない（まだ1件で、独立検定の方が甘い可能性を消していない）・★特に独立検定が甘かった側の可能性を先に潰し、MUST 13件が通って当然の易しさでなかったと言える根拠を出す。資料更新=`C-QWEN-WORKER` は昇格してよいが「1回1件で輪の中の計器は FAILED」を併記／`G-16` 解消／`G-25` を新規登録（輪の中の合否と独立検定が食い違った・1件・`OPEN`。どちらが正しいか未確定なので `CONTRADICTED` にしない）。オラクルは判定済で秘匿の理由が無いので `egl/` へ置いて知らせること（一度公開した検定を次回の held-out に再利用しないことに同意）。*
