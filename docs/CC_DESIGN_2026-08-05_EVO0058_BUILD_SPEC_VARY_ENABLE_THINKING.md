# 【BUILD SPEC】`EVO-0058` — **★自由度を1つだけ振る（`enable_thinking=False`）**

- `BUILD_ROLE: ★実装源` / **宛: IMPL**（★走行は MGR） / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-05 00:4x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.13）** ／ 親: `ITEM-2DER-EVO-0035` ／ 裁定: `CC_MGR_2026-08-05_EVO0058_MY_HYPOTHESIS_WAS_WRONG_VARY_ONE.md` §4
- **★v1.8 の宣言**: **★核は無い・★2DER 工程 0**（★引数を1つ足すだけ。★worker に書かせるものが無い）
- **★私の予告**: ★Claude **1行**（★引数1つ）／★worker **0件**／★走行 **1回**（MGR）
- **★結果の予想は書かない**（★裁定の逐語「★予想は書かない」）
- **★task 増 1（★走行のため）・★commit 0**

---

## 1. 変更（★1箇所・★1引数）

`twoder/qwen_worker.py:78-82` の `RS.supervised_text_call(...)` に **★`enable_thinking=False` を1つ足す**。

```python
                task_class=tp.get("task_class") or "BUILD_CAPABILITY", ts=tp.get("ts") or "2026-07-11T08:00:00",
                enable_thinking=False)          # EVO-0058: 1つだけ振る(監査 dw/adapters.py:144 と同じ形)
```

```
★他は1文字も変えない。★特に ★ladder / ★prompt / ★seed / ★schema / ★parse は ★触らない（★裁定の明示的禁止）
★★渡し方は ★既に在る（`runtime_supervisor.py:77-78` が ★None でなければ chat_template_kwargs に載せる）
★★★∴ ★新しく作る物は ★0。★8箇所で ★既に使われている引数を ★9箇所目に渡すだけ
```

## 2. ★★そのままでは走らせられない（★先に言う・★実測）

```
★`submit.py:513` 逐語 = `dw_task = "TASK-2DER-" + hashlib.sha1(raw_input.encode()).hexdigest()[:8].upper()`
★`workcell.py:368-369` 逐語 = `if _read_events(task_id): raise WorkflowViolation("task ... already exists")`
★★∴ ★v5 の本文を ★1文字も変えずに再投入すると ★★同じ task_id になり ★新しい走行にならない
★★★しかも `submit.py:544-547` は ★create_task を ★try/except pass で包んでいる
   ＝ ★★弾かれたことが ★分からない形で ★通る（★『走ったつもり』になる）
```

**★対処（★契約は byte 一致のまま・★測っているものを変えない）**
```
★変えてよいのは ★★契約の3ブロックの ★外側だけ（★例: 投入の冒頭に ★1行 足す）
★★変えてはいけない = ★依頼文 ／ ★`<<<2DER:SKELETON>>>` の中 ／ ★`<<<2DER:IMMUTABLE_TESTS>>>` の中
★★★これは ★2026-08-04 の私の裁定と同じ形＝★『測る強さ』は ★1ミリも変わらない
★★★★★証明のしかた = ★受入(2)（★3ブロックの sha256 を ★v5 と突き合わせる）
```

**★v5 の3ブロックの sha256（★私が実測。★突き合わせる基準）**
```
★依頼文 1792字  sha256(先頭16) = 5a35d4ca6c8634cc
★骨格     34字  sha256(先頭16) = f055b3893fa5521a
★試験   7343字  sha256(先頭16) = 94ef4dc80f21e951
★★取り方 = ★`<<<2DER:SKELETON>>>` / `<<<2DER:IMMUTABLE_TESTS>>>` の ★次の行から ★`<<<2DER:END>>>` の ★前の行まで
   ／ ★依頼文は ★`**★依頼文**` 直後の ``` 〜 ``` の中。★私が V5.md から取った値である
```

## 3. 受入

```
★(1) ★変更が ★1引数だけであることを ★diff で示す（★行数を書く）
★(2) ★★契約3ブロック（依頼文/骨格/試験）の ★sha256 が ★v5 と ★一致することを ★逐語で示す
     ★★一致しなければ ★★止まる（★測っているものが変わっている）
★(3) ★走行 ★1回。★task_id が ★★新しいこと（★§2 の衝突を踏んでいないこと）を ★逐語で示す
★(4) ★★`GET /api/resolve?id=TASK-…` の `generation.per_attempt` を ★全行 逐語で持ち帰る
     （★`content_length` / `finish_reason` / `parse_status` / `schema` / `attempt_index`）
★(5) ★★★分岐を ★そのまま書く（★裁定の逐語）:
     ★(a) content が出た → ★『思考出力が予算を食っていた』の ★実証1件
     ★(b) また 8192 を食い切って content 0 → ★『思考ではない』∴ ★次は (ii) ladder の上限
     ★★どちらでも ★次に振る自由度が ★決まる ＝ ★★どちらでも ★収穫である
★(6) ★戻せる（★引数1つを消すだけ）／★(7) ★61本を走らせない ／★(8) ★commit しない
★★★(9) ★出せなかったら ★『出せなかった』と書いて ★止まる
```

## 4. ★★これで分からないこと（★先に言う）

```
★★1走行で分かるのは ★★『この1引数で ★content が出るか出ないか』だけ。
★『生成が良くなった』『2DER が直った』とは ★書かない ―― ★試験が通るかは ★別の話である。
★★★また ★1件では ★『毎回そうなる』とは ★言えない（★再現は ★次の単位）。
★★★★★§5 の ★無意味な6回の resample は ★★今回 直さない（★同時に振らない・★裁定の逐語）。
   ★戻る条件 = ★本走行が ★(a)(b) どちらかに ★決着した直後。
```

## 5. 禁止

```
★`ladder` / `prompt` / `seed` / `schema` / `parse` / ★契約の中身を ★同時に触る（★裁定の明示的禁止）
★2つ以上の自由度を ★1走行で振る ／ ★resample の6回を ★本件で直す
★契約3ブロックを ★1文字でも変える ／ ★sha256 を確かめずに ★『同じ契約だ』と書く
★★同じ本文を再投入して ★『走った』と書く（★§2＝★沈黙して弾かれる）
★結果の予想を書く（★裁定の逐語）／ ★『思考が原因だった』と ★走行前に書く
★61本を走らせる ／ ★commit する ／ ★`twoder` 配下で python を動かす
```
