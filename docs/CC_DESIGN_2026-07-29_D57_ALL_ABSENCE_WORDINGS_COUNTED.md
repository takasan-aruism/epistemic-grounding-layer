# 設計/監査 → MGR（写: Taka / IMPL）: **D-57 — 「無い」と出す文言を★14件 数え切った。★まとめているのは5件（うち1件は登記済）**

- `BUILD_ROLE: 参照`（**調査＋文言案。★コードを1行も変えていない・投入していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-29_D57_STOP_FINDING_ONE_AT_A_TIME_COUNT_THEM_ALL.md`

## 0. ★結論
> **総数14件。** **★まとめているのは5件**（`欠損・失敗の記録なし`＝`G-69` 登記済 を含む）。
> **★まとめていないのは9件。** **列挙した**（MGR 条件2）。
> **★1段ずつ見つけるのをやめた。** **これが「数え切る」の結果である。**

---

## 1. ★総数（`head`/`-m` を1つも使っていない）
```
再現: awk 'NR>=617' twoder/webui.py | grep -oE '"[^"]*(無い|なし|ありません|取得できない|引けない|0 件)[^"]*"'
      （617 = PAGE の開始行。★UI の文言だけを対象にした）
総件数 14 / 確認 14 / 打ち切り無し
```

## 2. ★14件の判定（★まとめていないものも全部書く）
| # | 文言 | 判定 | 理由 |
|---|---|---|---|
| **1** | `backend が拒否(DW state 変化なし)` | **まとめていない** | 1回の操作の結果を述べている。世界についての「無い」ではない |
| **2** | `runnable な DW task がありません(観測 / BLOCK 結果)` | **まとめていない** | **★理由を2つとも名指ししている**（観測 / BLOCK） |
| **3** | `現在 runnable な DW task はありません。` | **★まとめている** | (1)まだ投入していない (2)投入したが DW task にならなかった (3)BLOCK された — **★3つを1つで出す** |
| **4** | `その run は記録に無い: <id>` | **★まとめている** | (1)そんな run は存在しない (2)存在したが引けなかった — **★`(c1)/(c2)` と同じ形が★ここにも在る** |
| **5** | `この依頼は DW を通っていない。DW 上の現在地は無い` | **まとめていない** | D-50 で意図して書いた。事実＋帰結 |
| **6** | `DW task が無いため、次の操作は記録されていない` | **まとめていない** | 理由を述べている |
| **7** | `authority boundary なし` | **★まとめている** | (1)authority boundary が起きなかった (2)★起きたが記録されていない |
| **8** | `run_id は在るが event が引けない: <id>` | **まとめていない** | D-56 で分けた |
| **9** | `この依頼の run_id は記録に無い` | **まとめていない** | D-56 で分けた |
| **10** | `欠損・失敗の記録なし` | **★まとめている** | **`G-69` として登記済**（4段目） |
| **11** | `EGL の source_refs / claims` → `無い` | **★まとめている** | (1)EGL に根拠が無い (2)★この経路では取れない |
| **12** | `全 event の result は OK（失敗の記録なし）` | **★まとめている** | **★2重に危うい**（§2-1） |
| **13** | `取りこぼしの件数は取得できない（run_id は在るが、記録を引けなかった）` | **まとめていない** | D-56 |
| **14** | `取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）` | **まとめていない** | D-56 |

**★集計: まとめている 5 / まとめていない 9 / 計 14。**

### 2-1. ★#12 は2重に危うい（実読で確かめた）
```javascript
(bad.length ? …失敗した event の一覧…
            : "<div class='v'>全 event の result は OK（失敗の記録なし）</div>")
```
| # | 危うさ |
|---|---|
| **1** | **「全 event」と書くが、★`truncated` の run では全 event を見ていない。** **★偽になりうる** |
| **2** | **「失敗の記録なし」は `G-69` と同型**（起きなかったのか、記録されなかったのか） |

> **★同じブロックの中に「`boundary_failures` → run からは取れない」が既に在る。** **∴ 取れないものが在ることは既に書かれている。**
> **∴ その隣で「全 event は OK」と言えば、★取れない範囲まで OK に見える。**

---

## 3. ★直す文言（意味は MGR 指定。文言は私が書く）
| # | 現在 | **直した後** |
|---|---|---|
| **3** | `現在 runnable な DW task はありません。` | **「進められる DW task は在りません。まだ投入していない／投入したが DW task にならなかった／BLOCK された、のいずれかです。この画面では区別できません」** |
| **4** | `その run は記録に無い: <id>` | **「その run は引けませんでした: `<id>`（存在しないのか、記録を引けなかったのかは区別できません）」** |
| **7** | `authority boundary なし` | **「記録された authority boundary は在りません（記録されなかったものが在るかは、この画面では分かりません）」** |
| **10** | `欠損・失敗の記録なし` | **「記録された欠損・失敗は在りません」＋併記「記録されなかった欠損・失敗が在るかどうかは、この画面では分かりません」**（★MGR 指定の意味そのまま） |
| **11** | `無い` | **「この経路では取れません（EGL に根拠が無いのか、この経路に載らないのかは区別できません）」** |
| **12** | `全 event の result は OK（失敗の記録なし）` | **「取得した event の中に、失敗の記録は在りません」＋`truncated` のときは「★全 event を見ていません」を併記** |

### 3-1. ★書き方の規律（私が守ったこと）
1. **★「無い」と言い切らない。** **「記録された◯◯は在りません」**（記録されたもの、に限定する）。
2. **★区別できないなら「区別できません」と書く。** **原因を推測しない。**
3. **★「この画面では」を付ける。** **画面の限界であって、世界の事実ではない。**
4. **★色を使わない**（`ok` も `bad` も）。**振る舞いを変えない。**

---

## 4. ★実例が無いもの（作らない）
| # | 実例 |
|---|---|
| 3 | **★在る**（起動時に TASK が無い状態は普通に起きる） |
| 4 | **★在る**（`ETR-000000000000` を入れれば出る。**★私は `/api/resolve` で実測済**） |
| 7 | **★見つけていない**（`TASK-2DER-B9B4DA3B` の `taka_authority` は `[]` ＝この文言が出る。**★実は在る**） |
| 10 | **★見つけていない**（parts が空になる依頼を見つけていない） |
| 11 | **★見つけていない** |
| 12 | **★見つけていない**（4 run すべて result=OK なので**この文言は出る**。ただし `truncated` の run が無いので★偽になる場面は見ていない） |

> **★作って試さない。** **`G-61`・(a)・(c2) と同じ扱い。**

## 5. ★私は直さない（裁定を仰ぐ）
- **文言案は書いた。** **★コードは1行も変えていない。**
- **★D-56 に混ぜない**（MGR 明示）。**D-56 は「(c) を2つに割る」で閉じる。**
- **★5件を一度に直すか、分けるかは MGR が決める。**

## 6. ★未確認
| # | 未確認 | 誰が・いつ |
|---|---|---|
| 1 | **`/` 側（開発状況の画面）の文言を数えていない**（`PAGE` = `/command` のみを対象にした） | **★「14件」は `/command` の数である。** MGR が要ると言えば `/` も数える |
| 2 | **`truncated` が true になる run の実例** | **作らない** |
| 3 | 画面をブラウザで見ていない | D-45 から継続 |

---
*CC-α D-57。★結論=`/command` で「無い／なし／0件」と出す文言は**総数14件**で、**まとめているのは5件**（`欠損・失敗の記録なし`＝`G-69` 登記済 を含む）、**まとめていないのは9件で全部列挙した** ∴ 1段ずつ見つけるのをやめ「数え切る」を実行した。★判定=まとめている5件は #3「現在 runnable な DW task はありません」（まだ投入していない／DW task にならなかった／BLOCK された の**3つを1つで出す**）／#4「その run は記録に無い」（存在しない／引けなかった＝**`(c1)/(c2)` と同じ形がここにも在る**）／#7「authority boundary なし」（起きなかった／起きたが記録されていない）／#10「欠損・失敗の記録なし」（`G-69` 登記済）／#11「EGL の source_refs / claims → 無い」（EGL に根拠が無い／この経路では取れない）。**#12「全 event の result は OK（失敗の記録なし）」は2重に危うい**——「全 event」と書くが `truncated` の run では全 event を見ておらず**偽になりうる**うえ「失敗の記録なし」は `G-69` と同型で、**同じブロックに「`boundary_failures` → run からは取れない」が既に在るので、その隣で「全 event は OK」と言えば取れない範囲まで OK に見える**。★直した後の文言案を6件書いた（意味は MGR 指定・文言は CC-α）。守った規律=**「無い」と言い切らず「記録された◯◯は在りません」と限定する／区別できないなら「区別できません」と書き原因を推測しない／「この画面では」を付ける（画面の限界であって世界の事実ではない）／色を使わず振る舞いを変えない**。★実例=#3・#4・#7 は在る（#7 は `TASK-2DER-B9B4DA3B` の `taka_authority` が `[]` で実際に出る）、#10・#11・#12 の**偽になる場面**は見つけていない（`truncated` の run が無い）——**作って試さない**。★**CC-α は直さず文言案のみ**（D-56 に混ぜない。5件を一度に直すか分けるかは MGR が決める）。★未確認=**「14件」は `/command` の数で `/` 側を数えていない**／`truncated` の run の実例（作らない）／画面をブラウザで見ていない。*
