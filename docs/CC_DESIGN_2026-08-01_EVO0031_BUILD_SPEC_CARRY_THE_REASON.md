# 【BUILD SPEC / `EVO-0031`】**★捨てている2つを、捨てずに渡す**（★4欄 → 6欄）

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-01 20:1x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.1 / 2026-08-01）**
- **★裁定の在り処**: **★`ITEM-2DER-EVO-0031` の `status_note`**（★`.md` ではない）。逐語:
  「依頼=webui.py:327-329 の test_result に runner_exit と runner_stdout_tail を足す BUILD SPEC を最小・可逆で出すこと。
   実装は twoder 配下なので Claude が書く例外(行数報告・2DERの実績に数えない)。
   受入=RUNNER_FAILED の逐語理由が front door から読める」
- **★★これは ★指示が `.md` を経由せずに届いた ★1回目である**（★`D-213` §4 の廃止条件は「2回続けて」）

---

# 1. ★やること（★1箇所・★2欄 足すだけ）

```python
# twoder/webui.py:327-329（★`cw()` の中・GENERATE/REGENERATE の記録を作る所）
              "test_result": {"status": "PASSED" if gr.get("ok") else "FAILED",
                              "ok": bool(gr.get("ok")), "reason": gr.get("reason"),
                              "artifact_sha256": gr.get("artifact_sha256"),
                              ★"runner_exit": gr.get("runner_exit"),              # ← ★足す
                              ★"runner_stdout_tail": gr.get("runner_stdout_tail")},  # ← ★足す
```
```
★★`generate_via_runner.generate` は ★既に この2つを返している（`generate_via_runner.py:213-216` 逐語）
   ∴ ★★作らない。★★受け取って渡すだけである。
★★★★戻し方: ★この2行を消す。★★可逆である。
★★★★★★`generate_via_runner` に手を入れない ／ ★`record_generate` の引数の形を変えない
   （★`test_result` は dict なので ★欄が増えても ★受け側は壊れない見込み【★実装が確かめること】）
```

---

# 2. ★★私が先に測って書いておくこと（★通ってから言わない）

```
★★① ★★過去の task は ★測り直せない。
   ★理由: ★捨てた値は ★台帳に★書かれていない ∴ ★遡って読めない。
   ★★∴ ★`TASK-2DER-B37727E3` と `TASK-2DER-CCCAEAA8` の ★`RUNNER_FAILED` の理由は ★★永久に分からない。
   ★★★★測るのは ★★新しい走行だけである。★★「前の task で確かめる」と ★書かないこと。
★★② ★★`runner_exit` が ★`None` で返る可能性が在る【★未確認】。
   ★理由: ★`generate_via_runner.py:146` 逐語 `_test = res.get("test") or {}` ＝ ★`res["test"]` が無ければ空 dict
   ★★∴ ★★実行器が ★★起動する前に落ちていれば ★`exit` も `stdout` も ★入っていない。
   ★★★★その場合 ★足しても ★`null` が出るだけである。★★それも ★結果である（★「★理由が記録されていない」）
   ★★★★★★∴ ★受入は ★「★値が出る」ではなく ★「★値が読める（★`null` なら `null` と読める）」にする（★§3）
```

---

# 3. ★受入（★裁定の逐語＋★§2 の但し書き）
```
★(1) ★`GET /api/claude_packet?task_id=…` の ★`test_result` に ★`runner_exit` と `runner_stdout_tail` が★在る
★(2) ★★新しい走行を1回 起こし、★その値を ★逐語で持ち帰る
   ★★`exit` が ★5 なら ★★試験が1件も集まっていない ／ ★1 なら ★試験は走って落ちた
   ★★★`null` なら ★★「★実行器が起動前に落ちている」＝ ★★それも逐語で書く（★★失敗ではない）
★(3) ★★戻せる（★2行を消したら ★元の4欄に戻る）——★戻して確かめ、★また足す
★★★★★予告を投入前に書く: ★(1)(2)(3) の予想 ／ ★変更行数（★2行の見込み）／ ★`exit` の予想（★書けないなら「予想しない」）
```

---

# 4. ★★新しい走行の作り方（★★私は指定しない・★理由を書く）
```
★★`EVO-0031` を測るには ★新しい task が要る。★★しかし ★どう作るかは ★あなたが決めてよい:
   ★① 既存の `TASK-2DER-CCCAEAA8` を ★`run_next` で進める（★★REGENERATE が走れば ★新しい記録になる）
   ★② 新しい依頼文を投入する（★★task が1件 増える）
★★★私が ①を推す: ★★task を増やさない。★★`EVO-0028` で止めた形に戻さない。
★★★★★ただし ★①で ★REGENERATE が走るかは ★★私は確かめていない【★未確認】。★走らなければ ★②へ。
   ★★★★★★「①を試した／走らなかった」も ★結果である。★書いて戻すこと。
```

# 5. ★禁止 ／ 6. ★報告
```
【禁止】★`generate_via_runner` を変える（★既に返している）／ ★`record_generate` の形を変える
        ★`cwd` や `test_command` を直す（★★原因が未確定。★`exit` を見てから）
        ★新しい台帳・計器・状態語・エンドポイントを作る ／ ★commit する
        ★★`twoder` 配下で python を動かす（★`operator.py` の罠）
【報告】1 ★変更行数（★Claude が書く例外 ∴ ★2DER の実績に数えない）／ 2 ★受入 (1)(2)(3)
        3 ★予告の当否 ／ 4 ★★`exit` と `stdout` の ★逐語（★`null` なら `null` と書く）
        5 ★①②のどちらで走行を作ったか（★①が走らなかったならそう書く）
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---

# 7. ★MGR の1点を受ける（★`status_note` の逐語）
```
★逐語:「★MGR の指定が『ACTIVE な ITEM』と曖昧だった件は ★IMPL の張り方
   （EVO-0021..0032 ＋ 未完全件を見る）を ★標準として採る」
★★★私の申し送り（`D-213` §2）も ★「ACTIVE な ITEM」と書いていた ∴ ★★私の指定も曖昧だった。
   ★★IMPL が ★`DONE` の item にも裁定 note が載ることに気づいて広げたのが ★正しい。
★★★★★私の監視も ★同じ範囲へ広げる（★`EVO-0021`〜`0031` 固定 → ★未完全件も見る形へ）。★私が自分で直す。
```

---
**決めたこと**: **①裁定は `ITEM-2DER-EVO-0031` の `status_note` に在り、`.md` を経由せずに届いた1回目である（廃止条件は2回続けて）②やることは `webui.py:327-329` に `runner_exit` と `runner_stdout_tail` の2欄を足すだけ。`generate_via_runner` は既に返しているので作らず、受け取って渡すだけ。2行を消せば戻る ③先に測って書いておく——過去の task は測り直せない（捨てた値は台帳に書かれていないので遡れない）∴ 2つの task の `RUNNER_FAILED` の理由は永久に分からず、測るのは新しい走行だけである ④`runner_exit` が `null` で返る可能性が在る（実行器が起動前に落ちていれば `exit` も `stdout` も入っていない）。その場合も結果なので、受入は「値が出る」ではなく「値が読める（`null` なら `null` と読める）」にした ⑤新しい走行の作り方は指定しない。既存 task を `run_next` で進める案を推す（task を増やさず `EVO-0028` で止めた形に戻さない）が、REGENERATE が走るかは確かめていないので、走らなければ新規投入でよく「①を試した／走らなかった」も結果として書かせる ⑥`cwd` や `test_command` を直すことを禁じた——原因が未確定で、`exit` を見てから決める ⑦MGR の「ACTIVE な ITEM」が曖昧だった件を受ける。私の申し送りも同じ書き方だったので私の指定も曖昧であり、IMPL が `DONE` の item にも裁定 note が載ると気づいて広げたのが正しい。私の監視も同じ範囲へ広げる（私が自分で直す）。**
