# 実装 → 設計/監査: **答えを1件も変えずに、読み直しをやめました**（160件の分類が完全一致）

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=BUILT
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-30_D76_PASS_EVENTS_BUILD_SPEC_v1_0.md` v1.0
- **受領した文書**: 上記 / `CC_MGR_2026-07-30_D76_APPROVED_SAY_WHICH_BARRIER.md`（写しで観測）

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 結果（★2軸）
### 経路
- [x] **実装した**（3ファイル・省略可能な引数を足しただけ）
- [x] **止まってよい場所には当たりませんでした**（§5）
### 実測
- [x] **★受入1（最優先）: 160件の分類が変更前後で★差分0行**
- [x] **★受入3: 引数を渡さない版でも★差分0行**（省略時の振る舞いが同じ）
- [x] 受入2 既存の呼び手を1つも書き換えていない
- [x] 受入4 所要を測った（**★結果であって目的ではありません**）
- [x] 受入5 非回帰 91 passed / 7 failed（顔ぶれ diff 空）
- [x] 受入6 **配信物の JS が `node --check` を通る**（`/command` と `/` の両方）

---

## 1. ★受入1（答えが変わっていないこと）— 最優先
```
再現: 変更【前】に 160件の (task_id, state, actor_id, claude_barrier) を保存
      変更【後】に同じものを取り、diff
結果: ★差分 0行（160件すべて一致）

再現: 変更【後】に★引数を渡さない版でも取り、変更前と diff
結果: ★差分 0行（＝受入3）
```
- **★変更前の値は、変更前にしか取れません。** **★先に取りました。**
- **★これは「速くなったか」ではなく「答えが変わっていないか」の確認です。**

### 1-1. `_active_2der_tasks` も別に確かめました
```
再現: 旧（読み直す）と新（渡す）を同じプロセスで並べて実行
  旧: 153 件 / 4.709 秒
  新: 153 件 / 0.036 秒
  ★順序も含めて完全一致（== で True）
```

## 2. 実装（★口を1つ足しただけ）
| ファイル | 変更 |
|---|---|
| `dev-workcell/dw/workcell.py` | `derive_state(task_id, events=None)` / `completion_blockers(task_id, events=None)` ＋ **`_filter_events` を1つ追加** |
| `dev-workcell/dw/dispatch.py` | `next_legal_operation(task_id, events=None)`（**受け取ったものをそのまま `derive_state` と `completion_blockers` へ渡す**） |
| `twoder/submit.py` | `_active_2der_tasks` が**既に読んでいた `_read_events(None)` の結果を渡す**（1行の追加＋2行の書き換え） |

**★守ったこと**
1. **省略時は今までどおり自分で読みます**（受入3 で実測・差分0）。
2. **既存の呼び手を1つも書き換えていません**（省略できるため触る必要がありません）。
3. **キャッシュを持っていません**（1回の処理の中で渡すだけ・保持していません）。
4. **並列化していません。**
5. **絞り込み規則を `_read_events` と同一にしました**:
```python
def _filter_events(events, task_id):
    """★絞り込み規則は `_read_events` と同一でなければならない
    (`_ordinal` は読み込み時に付いており、ここでは付け直さない=同じ値がそのまま残る)。"""
    return [e for e in events if task_id is None or e.get("task_id") == task_id]
```
> **★`_ordinal` は `_read_events` が読み込み時に付けます。** **★私は付け直していません。** **∴ 渡された events の `_ordinal` は、読み込んだときの値がそのまま残ります。**
> **∴ 実装源 §4-4 の「`_ordinal` の付き方が違う」には当たりませんでした。**

## 3. 受入2（既存の呼び手が変わっていないこと）
```
再現: cd /home/takasan/dev-workcell && git diff --stat
  dw/dispatch.py | 10 ++++++----
  dw/workcell.py | 22 ++++++++++++++++------
  2 files changed, 22 insertions(+), 10 deletions(-)
```
- **削除行はすべて「署名の行」と「その直後の1行」です**（`def …(task_id):` → `def …(task_id, events=None):` 等）。
- **★呼び手（`derive_state(...)` を呼んでいる既存の箇所）を1つも書き換えていません。**
- **`twoder/submit.py` は実装源 §1-3 が名指しした `_active_2der_tasks` の中だけです。**

## 4. 受入4（★数字だけ出します）
```
160件の分類:  9.880 秒 → ★0.102 秒
_active_2der_tasks: 4.709 秒 → ★0.036 秒
```
**★「速くなった」を成果として書きません。** **★目的は答えを変えずに読み直しをやめることでした。** **★速くなったのは結果です。**

## 5. 受入5・6
```
非回帰98本: 91 passed / 7 failed（基準と★顔ぶれ diff 空）
配信物の JS: /command 16509 バイト → ★構文 OK
             /          999 バイト → ★構文 OK
★ソースではなく配信物に対して検査しました（G-71）。
```

## 6. 止まってよい場所（実装源 §4・いずれにも当たりませんでした）
| # | 条件 | 実際 |
|---|---|---|
| 1 | 省略時の振る舞いが変わる | **変わっていません**（受入3・差分0） |
| 2 | 分類が1件でも食い違った | **1件も食い違っていません**（160件・差分0） |
| 3 | 他の呼び手を書き換えないと通らない | **書き換えていません** |
| 4 | `_ordinal` の付き方が違う | **同じです**（§2-5） |
| 5 | SPEC が2通りに読める | 無し |

## 7. ★UI は変えていません
- **実装源 §3「UI をこの build で変えない」に従いました。**
- **∴ 155行の ID は★まだ出ます。** **★集約表示は D-73 の続きであり、本 build は下ごしらえです。**
- **★「画面が速くなった」とは書きません。** **★画面はまだ変えていません。**

## 8. 触ったファイル / commit / 再起動
```
dev-workcell/dw/workcell.py / dev-workcell/dw/dispatch.py / twoder/submit.py
```
- **★再起動しました**（配信物で検査するため。訂正後の順番 ②に従いました）。
- **commit していません**（MGR）。**投入していません。** **資料の同期はしていません。**

---
*IMPL BUILT（D-76）。★**目的は速くすることではなく「答えを変えずに読み直しをやめる」ことであり、それを満たした**——**160件の (task_id, state, actor_id, claude_barrier) が変更前後で差分0行**、**引数を渡さない版でも差分0行**（省略時の振る舞いが同じ）、`_active_2der_tasks` も**153件が順序まで完全一致**。変更前の値は変更前にしか取れないので**先に保存した**。★実装=`derive_state` / `completion_blockers` / `next_legal_operation` に**省略可能な引数を1つ**足し、`_filter_events` を追加、`submit._active_2der_tasks` が**既に読んでいた events をそのまま渡す**。守ったこと=省略時は自分で読む／**既存の呼び手を1つも書き換えない**／**キャッシュを持たない**／並列化しない／**絞り込み規則を `_read_events` と同一にし `_ordinal` を付け直さない**（∴ 止まってよい場所4 に当たらなかった）。★受入4 の数字=**9.880秒 → 0.102秒**、`_active_2der_tasks` は **4.709秒 → 0.036秒**——**「速くなった」を成果として書かない。目的は答えを変えないことで、速くなったのは結果である**。非回帰 91/7・顔ぶれ diff 空、**配信物の JS は `/command`・`/` とも `node --check` で構文 OK**（ソースではなく配信物）。止まってよい場所5件はいずれにも当たらず。★**UI は変えていない**（実装源 §3）∴ **155行の ID はまだ出る**し**「画面が速くなった」とは書かない**——集約表示は D-73 の続きで、本 build は下ごしらえである。再起動は訂正後の順番②に従って実施、commit・投入・資料同期はしていない。*
