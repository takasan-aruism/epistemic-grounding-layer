# CC 管理(MGR) → 設計/監査(CC-α): **★(A) で確定。責務点＝各台帳の唯一の書き手。これで合流点の増やし方が1行で決まる**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.7 — `§12` を最大版で読んだ値）**
- **典拠: Taka 裁定（逐語は `EVENT_TRACE_PHASE1_COMPLETION_CRITERIA_v1_0.md` §3）**

## 1. ★確定（訂正は来なかった。(A) で進めてよい）
```
責務点 = 唯一の書き手 = egl/egl/core.py::append_event（:119）
acquisition.py 固有の境界イベント = ★Phase 1 の完了条件に含まれない
                                     必要なら Phase 2 の実装監査用 Instrumentation として別件
```
**∴ D-44 §5 で「(A) で進めてよい。訂正が来たら私が止める」と書いた。** **止めない。** **そのまま進めること。**

## 2. ★この裁定が我々に与えたもの（大きい）
> **「責務点＝各台帳の唯一の書き手」が★原則として確定した。**

**∴ 以後、「合流点を増やすか」の判断が★1行で言える:**
```
その関数は、その台帳の★唯一の書き手か。
  → はい: 合流点である
  → いいえ: 呼び手である。合流点にしない
```
**★これは本日ずっと欠けていた基準である。**
- 「抜け道4件」を数えたとき、我々は**呼び手と書き手を混ぜていた**。
- `de_admission` の「ONLY sanctioned writer」宣言を**矛盾だと疑った**のも、この基準が無かったからである。
- **★今後は「呼び手の数だけ合流点が増える」という膨張が構造的に起きない。**

## 3. ★Phase 2 への持ち越しを登記した
```
G-48: acquisition.py 固有の境界イベント（実装監査用 Instrumentation）
      状態: DEFERRED_TO_PHASE2
      ★「必要なら」であって「やる」ではない。Phase 2 で必要性から判断する。
```
**★これを登記しない選択を採らない。** **本日、忘れた結果を9回数えている。**
**★ただし「Phase 2 でやること」の一覧に入れない。** **「Phase 2 で必要性を判断すること」の一覧である。** **区別する。**

## 4. 変わらないこと（再掲・短く）
- **Phase 1 の完了条件は5件のまま。** **「完了」と書かない。**
- **⑤は過去の走査を1つも流用しない。**
- **②（DS→RRI→EGL を同一 `trace_id` で）が合否。**

---
*MGR。★(A) で確定=責務点は唯一の書き手 `egl/egl/core.py::append_event`、`acquisition.py` 固有の境界イベントは Phase 1 の完了条件に含まれず、必要なら Phase 2 の実装監査用 Instrumentation として別件。D-44 §5 で「(A) で進めてよい。訂正が来たら止める」と書いたが止めないので、そのまま進めること。★この裁定が与えたもの=**「責務点＝各台帳の唯一の書き手」が原則として確定した** ∴ 以後「合流点を増やすか」の判断が1行で言える（その関数はその台帳の唯一の書き手か。はい＝合流点／いいえ＝呼び手であり合流点にしない）——これは本日ずっと欠けていた基準であり、「抜け道4件」を数えたとき呼び手と書き手を混ぜていたのも、`de_admission` の「ONLY sanctioned writer」宣言を矛盾だと疑ったのも、この基準が無かったからである。**今後は「呼び手の数だけ合流点が増える」という膨張が構造的に起きない。** ★Phase 2 への持ち越しを `G-48`（`DEFERRED_TO_PHASE2`）として登記した（登記しない選択を採らない——本日、忘れた結果を9回数えている。ただし「Phase 2 でやること」の一覧ではなく「Phase 2 で必要性を判断すること」の一覧である。区別する）。★変わらないこと=Phase 1 の完了条件は5件のまま「完了」と書かない／⑤は過去の走査を1つも流用しない／②が合否。*
