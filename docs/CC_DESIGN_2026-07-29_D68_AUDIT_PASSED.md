# 設計/監査 → MGR（写: Taka / IMPL）: **D-68 監査 — ★受入1〜5 は私の実測で満たされている。★6 は投入なしでは示せない（先に決めたとおり）**

- `BUILD_ROLE: 参照`（**監査。コードを1行も変えていない・投入していない・★資料はまだ同期していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_IMPL_2026-07-29_D68_SENT_ON_SUCCESS_BUILT.md`

## 0. ★結論
> **① 受入1〜5 は★私の実測で満たされている。**
> **② ★改訂1（`path` 欄にパスだけ・型名は別の欄）が★そのとおり実装されている。**
> **③ ★プロセスは新しいソースで動いている**（起動 22:01:27 ＞ mtime 22:00:54）。
> **④ ★資料はまだ同期していない**（MGR の commit を待つ・D-58 の順番）。

---

## 1. ★改訂1 が守られている（★本監査の中心）
```
再現: grep -n "def _recv_log" -A 12 twoder/webui.py

:527 def _recv_log(self, kind, path, status=None, ★error_type=None):
:533 # ★1つの欄は1つのことだけを指す。path にはパスだけを入れ、型名は error_type 欄に置く。
:534 line = "%s %s %s%s%s\n" % (時刻, kind, path,
                                "" if status is None else " status=%s" % status,
                                "" if error_type is None else " error_type=%s" % error_type)
```
| | |
|---|---|
| **`path` 欄** | **★パスだけ。** 型名が混ざっていない |
| **増やした引数** | **★1つだけ**（`error_type`） |
| **★コメント** | **★理由が1行 書かれている**（「1つの欄は1つのことだけを指す」）。**★後で読む者が、なぜ欄を分けたか分かる** |

> **★私が今日 自分で破った教訓が、★実装のコメントとして残った。**

## 2. ★受入1・2（★呼び出しの全数・打ち切り無し）
```
再現: grep -n "_recv_log(" twoder/webui.py | grep -v "def _recv_log"
:547 RECV /api/submit                       ← ★認証の前（そのまま）
:550 SENT /api/submit 401                   ← 既存
:575 SENT /api/submit 200                   ← ★追加（正常）
:637 SENT /api/submit 500 error_type=…      ← ★追加（例外）

再現: 同じ一覧から /api/submit を含まない呼び出しを数える → ★0
```
| 受入 | 判定 |
|---|---|
| **1** 3つの道すべてに `SENT` | **★満たされた**（`【実】`。401 / 200 / 500 の3つ） |
| **2** `/api/submit` 以外では書かない | **★満たされた**（`【実】`。★該当0件） |
| **3** `path` 欄にパス以外が入っていない／増やした引数は1つ | **★満たされた**（`【実】`。§1） |

## 3. ★受入4（`node --check`・★今回から入った検査）
```
再現: GET /command → <script> 抽出（16509 バイト）→ node --check → ★エラー無し
★ソースではなく配信物に対して測った（MGR が「生ソースは検査にかけられない」と実測済）
```
> **★`G-71` の計器が、2回目も通った。**

## 4. ★受入5（既存が不変）
```
再現: GET /api/state?task_id=TASK-2DER-B9B4DA3B → keys=19 / sha256=1cfd27a833dd
★D-56 監査で私が測った値と★同一である（1cfd27a833dd）
```
> **★これは決定論の再実行ではない。** **★前回の値を私が持っていて、それと突き合わせた。**
> **★同じ数字が出ること自体は検算にならないが、★「変わっていないこと」を言うにはこの比較が要る。**

## 5. ★プロセスが新しいソースで動いていること（★私が確かめた）
```
再現: pgrep → pid 3637931 / ps -o lstart → 水 7月 29 22:01:27
      date -r twoder/webui.py            → 水 7月 29 22:00:54
∴ ★起動が mtime より後である。
```
> **★本日2回、「古いプロセスで測る」を踏んだ。** **★今回は先に確かめた。**
> **★ただし「再起動は MGR がやる」（D-65）に対し、★誰が再起動したかを私は確かめていない**（§7-2）。

## 6. ★受入6（★投入なしでは示せない・先に決めたとおり）
> **★成功時の `SENT` が実際に出たかは、`/api/submit` を叩かないと分からない。** **★投入しない。**
> **∴ 「コードに在ることは示せるが、出たことは示せない」。** **★未検証のまま登記する。**
> **★次に Taka が押したときに分かる。** **★前例: `RECV` は D-64 で入れ、Taka の初回操作で実際に出た。**

## 7. ★未確認
| # | | 誰が・いつ |
|---|---|---|
| 1 | **非回帰 91/7 を私は再実行していない** | **★`【伝聞・決定論】`。** 再実行は検算にならない（D-59） |
| 2 | **誰が再起動したか**（D-65 で「MGR がやる」と決めた） | **★確かめていない。** MGR が答えられる |
| 3 | **成功時 `SENT` の実例** | **★作らない**（投入しない） |
| 4 | 画面をブラウザで見ていない | **★Taka にしか出せない**（`CC_TAKA_…` 未回答） |

## 8. ★次
- **★MGR がコードを commit → ★その後に私が資料を同期する**（D-58 の順番）。
- **★私は次を提案しない。** **順番は MGR が決めている**（① D-68 ② 継続中タスクの集約 ③ D-62）。

---
*CC-α D-68 監査。★結論=①受入1〜5 は実測で満たされている ②**改訂1（`path` 欄にパスだけ・型名は別の欄）がそのとおり実装されている** ③プロセスは新しいソースで動いている（起動 22:01:27 ＞ mtime 22:00:54）④資料はまだ同期していない（MGR の commit を待つ）。★**改訂1 が守られている**=`_recv_log(self, kind, path, status=None, error_type=None)` で `path` 欄はパスだけ・増やした引数は1つだけ・**「1つの欄は1つのことだけを指す」という理由がコメントに1行残っている**（後で読む者がなぜ欄を分けたか分かる）——**CC-α が今日 自分で破った教訓が、実装のコメントとして残った**。★受入1・2=`_recv_log` の呼び出しは4件（`RECV`／`SENT 401`／**`SENT 200` 追加**／**`SENT 500 error_type=…` 追加**）で3つの道すべてに `SENT` が在り、`/api/submit` を含まない呼び出しは**0件**。★受入4=`GET /command` の `<script>`(16509 バイト)を `node --check` に通して**エラー無し**（**ソースではなく配信物に対して**測った）——`G-71` の計器が2回目も通った。★受入5=`/api/state` が `keys=19`/`sha256=1cfd27a833dd` で**D-56 監査で CC-α が測った値と同一**——**これは決定論の再実行ではなく、前回の値を持っていて突き合わせたものであり、「変わっていないこと」を言うにはこの比較が要る**。★プロセスの新旧を**先に確かめた**（本日2回「古いプロセスで測る」を踏んだため）。★受入6 は**投入なしでは示せない**ので「コードに在ることは示せるが出たことは示せない」と書き未検証のまま登記（**前例: `RECV` は Taka の初回操作で実際に出た**）。★未確認=非回帰は `【伝聞・決定論】`（再実行は検算にならない）／**誰が再起動したかを確かめていない**（D-65 で「MGR がやる」と決めた件・MGR が答えられる）／成功時 `SENT` の実例は**作らない**／画面は**Taka にしか出せない**。★次は MGR の commit → その後に CC-α が資料を同期。**CC-α は次を提案しない**。*
