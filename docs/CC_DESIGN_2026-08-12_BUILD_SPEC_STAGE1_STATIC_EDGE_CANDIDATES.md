開発者規律 確認済(v1.0)

# 【BUILD SPEC】★段1 ―― ★機械による 候補生成（★Worker を 呼ばない・★総数と 分布だけ）

宛: IMPL ／ 発: DESIGN ／ 2026-08-12 07:15 ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **正本** `CC_MGR_2026-08-12_TAKA_ROUTE_AND_FUNCTION_SPEC_v0.1.md` §5 / §6 / §16 / §17 / §21.3

**★正本の 逐語（★守る所）**
> 「★段A だけを 実装する。★Worker は 呼ばない」／「★機械は 意味を 決めない。★機械は 観測可能な 存在だけを 列挙する」／「★段1で 総数を 確認する」／「★ここで 一度 停止する」

---

## 1. ★★先に 出した母数（★★私が 07:0x に 実測・★決定論）

```
★`.py` の本数 = ★★556 ／ ★構文解析できなかった file = ★★0
★★静的な接続(A→B)の 候補 = ★★★821（★`from …` 687 ／ `import …` 134）
★source の repo = ★twoder 534 ／ egl 227 ／ dev-workcell 35 ／ rri 20 ／ ds 5

★★∴ ★★★821件は ★Worker に 渡せる数では ない（★正本 §21.3『候補爆発』が ★現実に 出た）
★★∴ ★★段1の 出力は ★★『総数と 分布』だけ（★★一覧を front door に 全部 載せない）
   ―― ★載せると ★`/api/control` が また 重くなる ＝ ★★段0 の 逆戻り
```

## 2. ★★作る物（★1つ・★機械だけ）

```
★★`twoder/static_edges.py`（★新規 1本）
   ★★入力 = ★5 repo の `*.py`（★★数える鍵は §3）
   ★★処理 = ★`ast` で ★`Import` / `ImportFrom` を 取る（★★正規表現で 数えない）
   ★★出力 = ★★下の §4 の 集計（★★一覧は 返さない）
   ★★LLM = ★★0回 ／ ★台帳へ 書かない ／ ★git を 触らない ／ ★file を 変更しない

★★front door = ★★`GET /api/control?include=static_edges`（★★段0 の 形に そのまま 乗る）
   ―― ★★既定では 計算しない（★正本 §15『聞かれた問いだけを計算する』）
   ―― ★口 0増 ／ ★新台帳 0
```

## 3. ★★数える鍵（★★1行 書く＝★★本日 2度 落とした所）

```
★★対象 = ★`/home/takasan/{ds,rri,egl,twoder,dev-workcell}` 配下の ★`*.py`
★★除く = ★`.git` ／ `__pycache__` ／ ★★シンボリックリンクを 追わない
★★含む = ★★試験（`test_*.py` / `regression/`）も ★★含める
   ―― ★★理由 = ★★『含めない』と 決めると ★★試験だけが 使う部品が ★★見えなくなる
   ―― ★★但し ★★`is_test` を ★★欄として 持つ（★★後で 外せる形＝★数を 消さない）
★★`egl/docs/` 配下の `.py`（★過去の投稿物）も ★★含める ＋ ★`is_doc_snapshot` を 持つ
★★∴ ★★正本 §1 の『約389本』と 私の『556本』は ★★★鍵が 違うだけ
   ―― ★★どちらが 正しい ではなく ★★★同じ鍵で 引き直した数を 併記する
```

## 4. ★★出す物（★正本 §16・§17 の 5つ ＋ ★私から 2つ）

```
★① ★候補総数
★② ★runtime evidence あり ／ なし
   ―― ★★`evidence` の 鍵 = ★★`event_trace` に ★その (component, function) の行が 在るか
     （★★昨日 作った `route_observed._records()` を ★そのまま 使う＝★新しい部品を 作らない）
★③ ★既存経路表との 重複 あり ／ なし（★`route_table.ROUTE` の 18区間と 突き合わせる）
★④ ★同一 repo ／ repo 跨ぎ
★⑤ ★接続種別（★`import` ／ `from import`）
★★⑥（★私）★★`is_test` ／ `is_doc_snapshot` の 内訳（★★後で 鍵を 変えられる形）
★★⑦（★私）★★『数えられなかった』の 件数（★構文解析に 失敗した file）＝ ★★0件なら 0件と 書く
```

## 5. ★★受入（★数で・★★走らせる前に 宣言する）

```
★★【★★宣言（★私が 07:0x に 出した値）】
   ★`.py` = ★★556 ／ ★候補 = ★★821 ／ ★`from` 687 ／ `import` 134 ／ ★解析失敗 0
   ―― ★★★ずれたら 止めて 報告（★★どちらかの 鍵が 違う＝★数を 争わない・鍵を 見る）

★★① ★`GET /api/control?include=static_edges` で ★上の ①〜⑦ が 引ける
★★② ★★既定（★`include` なし）では ★★計算しない ＝ ★所要が ★0.5秒以内の まま
★★③ ★★一覧（821件の 明細）を ★★返さない（★★総数と 分布だけ＝★正本 §16 の 終了条件）
★★④ ★★LLM の呼び出し ＝ ★★0回（★機械で 数える）
★★⑤ ★★同じ問いを 2回 引いて ★同じ（★★時刻の欄を 除く＝★本日 決めた 定型）
★★⑥ ★★数える鍵が ★★答えの中に 1行 在る（★★`counted_by` 欄＝★後から 鍵を 確かめられる）
★★⑦ ★新台帳0 ／ ★口0増 ／ ★★呼び手が 0件の 部品を 作らない
★★⑧ ★走らせた試験の 名前を 書く
```

## 6. ★★やらないこと（★★正本『ここで 一度 停止する』）

```
★★Worker を 呼ばない（★★段2 は 別）／ ★★候補の 意味を 判定しない
★★経路表に 書き足さない（★★登録は 段C・★Manager か 2DER 本体）
★★`Function` 一覧を 作らない（★★段3）
★★優先順位を 付けない ―― ★★★但し ★正本 §21.3 の 決定論の属性
   （★runtime evidence ／ repo 跨ぎ ／ 種別）は ★★上の 分布で 既に 出る
```
