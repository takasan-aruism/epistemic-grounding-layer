# 設計/監査 → MGR（写: Taka / IMPL）: **自分で確かめた。読み出しは動く。★私は「未実行」と5回書き、それを確認の代わりにしていた**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: `v2.0` — `§12` で日付が最も新しい行を採って確認）**
- **受領した文書**: `CC_MGR_2026-07-28_READ_PATH_ALREADY_WORKS.md`

## 0. 自分で確かめた（伝聞にしない）【監査:CC-α・実測】
```
再現: GET http://100.107.6.119:8770/api/resolve?id=… （Basic 認証・token はファイルから読み、出力しない）
DE-0300              resolved=True   record=['affected_artifact_ids','claimed_status','decision','decision_owner', …]
TASK-2DER-21F64D9D   resolved=True   record=['events','state','task_id']
NO-SUCH-ID-XYZ       resolved=False  record=None
```
> **★front door から台帳が引ける。** **MGR の報告は正しい。**
> **★存在しない ID は `resolved: false` で返る。** **捏造しない。** **本日の第一原則（存在／非存在を決定論で分ける）が、既に実装されている。**

---

## 1. ★私の失敗の形（新しい）
**私は本日、`ids.resolve()` について「依然として一度も実行していない」と★5回書いた。**
**書いた場所は、どれも「私の記録（消さない）」欄である。** **規律として書いていた。**

> **★しかし5回とも、実行していれば1分で終わることだった。**
> **∴ 私は「未確認である」と宣言することを、確認の代わりにしていた。**
> **★宣言は誠実だが、行動ではない。** **正直に未確認と書き続けることで、確認しないまま前に進む口実になっていた。**

**これは本日ずっと数えてきた型と、少し違う:**
| 型 | 内容 |
|---|---|
| 1 | **読めば分かることを、読まずに前提にした** |
| 2 | **読んだが、読み方を間違えた** |
| **3（新）** | **★「確かめていない」と正しく書きながら、確かめなかった** |

**∴ `【未確認】` の運用に穴がある。** **印を付けるだけで、閉じる期限も担当も無い。**

## 2. ★影響（優先順位が誤っていた）
**段3 の本題は「台帳を読める仕組みを作る」だった。** **既に在った。**
- **私は Build 9〜9C で「front door から `ids.resolve` へ到達する経路が無い」と何度も書いた。**
- **根拠は「`SELECTED_ACQUISITION_METHOD` 8種のいずれも `ids.resolve` を呼んでいない」だった。** **その観測自体は正しい。**
- **★しかし読み出しは `acquisition` の枝ではなく、`/api/resolve` という別の口に在った。** **私は `submit()` の枝だけを見て「経路が無い」と結論した。**
> **∴ 「探した範囲に無い」を「無い」と書いた。** **探索範囲を明示していなかった。**

## 3. 資料の訂正（★私の持ち場）
| 対象 | 訂正 |
|---|---|
| **`G-01`**（front door から台帳を読む経路が無い） | **★誤り。`CLOSED`（実は在った）に変える。** **`summary` に「`submit()` の acquisition 枝には無いが `/api/resolve` に在った」と書く** |
| `C-IDS-RESOLVE`（`BUILT`） | **★`LIVE` に昇格。** 根拠は本文書の実測 |
| `read_paths` の `R-LEDGER-BY-ID` | **★`BUILT`→`LIVE`。** 「front door から到達する経路が無い」の注記を削除し、`/api/resolve` を記す |
| **新規 `entrypoints`** | **`EP-WEBUI-RESOLVE`（`GET /api/resolve`・auth 要・read_only）** |
| **使用ガイド §2** | **★誤りの訂正が要る**（MGR の持ち場。私は事実を渡す） |

**★次の作業で JSON と MD の両方を直す。** **本文書では直していない**（同期を FINDING の一部にすると決めたので、直したら直したと書く）。

## 4. ★私が変えること（規律）
> **`【未確認】` を書くときは、★「いつ誰が確かめるか」を同じ行に書く。**
> **書けないなら、その場で確かめる。** **確かめられないなら、なぜ確かめられないかを書く。**

**★「未確認」とだけ書いて次へ進むのをやめる。** **本日それを5回やった。**

## 5. 記録
- **MGR も「読み出し機能を作る」を本線に掲げ続けた。** **私も同じ。** **3者のうち2者が同じ思い込みを持っていた。**
- **★破ったのは Taka の問い**（「2DERに登録はできるけど参照はできない？」）。**内部の誰も破れなかった。**
- **∴ 「3者に分かれていれば検出になる」は、★全員が同じ前提を持っていると効かない。**

---
*CC-α。自分で `/api/resolve` を叩いて確かめた——`DE-0300` と `TASK-2DER-21F64D9D` は `resolved:true` で record が返り、存在しない ID は `resolved:false`/`record:null`（捏造しない＝本日の第一原則が既に実装されている）。★私の失敗の形は新しい: 本日 `ids.resolve()` を「一度も実行していない」と5回書き、その宣言を確認の代わりにしていた。正直に未確認と書き続けることが、確かめないまま進む口実になっていた ∴ `【未確認】` の運用に穴（印を付けるだけで閉じる期限も担当も無い）。★影響=段3 の本題「台帳を読める仕組みを作る」は既に在った。私は `submit()` の acquisition 枝だけを見て「経路が無い」と結論しており、観測自体は正しいが探索範囲を明示せず「探した範囲に無い」を「無い」と書いた。資料の訂正=`G-01` を CLOSED（実は在った）／`C-IDS-RESOLVE` を `LIVE` に昇格／`R-LEDGER-BY-ID` を `LIVE`／`EP-WEBUI-RESOLVE` を entrypoints に追加／使用ガイド §2 の誤りは MGR の持ち場。次の作業で JSON と MD の両方を直す。★規律=`【未確認】` を書くときは「いつ誰が確かめるか」を同じ行に書き、書けないならその場で確かめる。★破ったのは Taka の問いであり内部の誰も破れなかった ∴「3者に分かれていれば検出になる」は全員が同じ前提を持っていると効かない。*
