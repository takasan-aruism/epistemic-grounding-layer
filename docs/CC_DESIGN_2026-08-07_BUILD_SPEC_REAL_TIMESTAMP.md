# 【BUILD SPEC】`(A)` — **★記録に 本当の時刻を入れる（★核0・★起動時に1回 だけ を 弾く）**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 監視 / 発: 設計/監査(CC-α) / 2026-08-07 22:4x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: ★v1.18）** ／ 台帳: `ITEM-2DER-EVO-0035` ／ 効く先＝Taka 裁定 (A)
- **★★核0 ／ ★新台帳0 ／ ★新しい欄0 ／ ★Claude の配線 ★上限8行**

---

## 1. ★★測ったこと（★源・逐語）

```
★時刻は ★1箇所から 出ている: ★`twoder/webui.py:40` → `TS = "2026-07-11T09:00:00"`
★そこから ★記録を書く呼び出しへ 流れる（★同ファイル内で ★9箇所）:
     :462 record_disposition(…, ★TS, "2der-auto-dispose")
     :482 record_plan(…, ★TS, "claude-manager")
     :484 record_disposition(…, ★TS, "claude-manager")
     :494 record_upper_review(…, ★TS, "claude-senior")   ← ★本件の対象
★`senior_review.make_actor(ts, …)` は ★ts を 引数で受け取る ∴ ★渡しているのは 呼び手（webui）。

★★壊れる物を 先に 確かめた（★実装可能性を 先出し）:
   ★`twoder/regression` / `dev-workcell` / `rri` / `ds` の中で
     ★この定数を 参照している試験 = ★★0件（grep）
   ★`.claude/hooks` の中 = ★★0件
★★★∴ ★★literal に依存している物は 見つからない。★『壊れない』とは書かない（★見つからないだけ）。
```

## 2. ★★核が 0 である 理由

```
★足す物は ★『今の時刻』1つ ∴ ★worker に 書かせる物が ありません。
★★でっち上げない（★本日 私は これを1度 守り、★MGR も 採りました）。
★★★∴ ★本件は ★配線だけの1件です。
```

## 3. ★★配線（★上限8行）

```
★(1) ★`TS` を ★定数から ★★『呼ぶたびに 今の時刻を返す物』へ 変える。
★★★(2) ★★module の 読み込み時に ★1回だけ 評価する形に しない。
     ★理由(★実測) = ★webui は ★★12時間 動き続けた実績が在る(★本日 pid が 12h 前起動)
     ∴ ★起動時に1回 評価すると ★★その日ぜんぶが ★同じ時刻になる=★今と同じ穴が 残る。
★(3) ★形は ★既存に合わせる = ★ISO の文字列（★`2026-08-07T22:45:12` の形）。
     ★★欄を 増やさない（★既存の `ts` に 入れるだけ・★MGR 基本設計のとおり）。
★(4) ★★既存の記録は ★1件も 書き換えない（★遡って直すのは ★実物と違う形を作ること）。
```

## 4. ★★受入（★口・欄・★id を 載せる物として 書く）

```
★(1) ★口 = `GET /api/state?task_id=…` ／ ★欄 = `upper_reviews[].ts`
     ★id = ★★配線後に 監査した走行1本（★★報告に その id を 書く）
     ★読める物 = ★★本日の日付（★`2026-07-11` ではない）

★★(2) ★★これが 本 SPEC の 要（★『起動時に1回』を 弾く陰性）=
     ★同じ webui プロセスのまま、★★時間を空けて 2本 監査し、
     ★★2本の `ts` が ★★互いに違うこと。
     ★★同じなら ―― ★『定数をやめた』ように 見えて ★★起動時刻で 固まっている。
     ★id = ★その2本（★両方 報告に 書く）

★(3) ★★陰性(遡って直していない) = ★既存の記録（例 `TASK-2DER-B37727E3` の3件）の `ts` が
     ★★`2026-07-11T09:00:00` の まま であること。

★(4) ★Claude の配線行数（★上限8行）／ ★(5) ★戻せる ／ ★(6) ★61本を走らせない ／ ★(7) ★commit しない
```

## 5. ★★これが 出たら 何が 決まるか

```
★★本日 私が 答えられなかった問い ―― ★『この記録は 今日 入った物か』―― が ★答えられるようになります。
★★★そして ★(B)(★根拠が在る時だけ やり直しを1回 許す)が ★効いたかを ★確かめられます
   （★MGR の順序どおり・★(A) が 先である 理由）。
```

## 6. ★★私が 言っていないこと

```
★『壊れない』―― ★★literal に依存する物が ★見つからなかった、まで（★§1）。
★『3件の 出どころが 分かる』―― ★★分かりません。★これから入る記録が 区別できるだけです。
★『既存の記録を 直す』―― ★★直しません（★§3(4)）。
★『(B) も 同時に』―― ★★(A) だけです（★Taka 裁定の順序）。
```
