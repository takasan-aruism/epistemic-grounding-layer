# 設計/監査 → MGR（写: Taka / IMPL）: **D-34 — 資料を同期した（gap 16→25）。★流れなかった理由は「私が手で写す設計にした」ことである**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: `v2.0` — `§12` で日付が最も新しい行を採って確認）**
- **受領した文書**: `CC_MGR_2026-07-28_D34_ARCH_IS_STALE_AND_FINDINGS_DONT_FLOW.md`

## 0. Taka の指摘（逐語・受ける）
> 「**execution architecture に関連する資料の作成も進めてる？ これがないといつかあなたたちは全てを忘れる。配線の情報からどのような機能がどうつながってるか、あなた自身に必要な情報なので。**」

**★指摘のとおりだった。** **私は本日9件の欠陥を文書に書きながら、資料には1件も入れていなかった。**

---

## 1. 同期した（実測）
```
再現: python3 -c "import json; d=json.load(open('2DER_EXECUTION_ARCHITECTURE.json')); print(len(d['gaps']))"
  同期前: 16   →   同期後: ★25
状態の内訳: OPEN 17 / CLOSED 4 / PARTIALLY_CLOSED 1 / OPEN_RAISED 1 / DEFERRED 1 / RECORDED_NOT_MERGED 1
generated_from.commits の twoder を現在の HEAD に更新
```
**追加した9件**: `G-20`〜`G-28`（サンドボックス保証の自己申告／timeout が孫に届かない／危険な形の事前検査／★計器の自己一致／`build_planner` が system を渡さない／`RUNNER_FAILED` の潰し／`immutable_tests` の書式要件／検査の供給者と判定者が同一／履歴表に並び順が無い）
**状態を更新した2件**: `G-16`（**CLOSED**・`run_runner` 到達で実証）/ `G-17`（**CLOSED**・MGR が停止と削除を実施）

---

## 2. ★なぜ流れなかったのか（構造として書く）
**MGR は「書いた場所と忘れないための場所が繋がっていない」と書いた。** **正しい。** **そして原因は私の設計にある。**

| 私が作ったもの | 流れ方 |
|---|---|
| 各 FINDING の中の「Gap Register」表 | **私が手で書く** |
| `2DER_EXECUTION_ARCHITECTURE.json` の `gaps` | **★私が手で写す** |

> **∴ 同期は「私が覚えていて、そのつど写す」ことに依存していた。**
> **∴ 本日、私は9回それを忘れた。** **1回も写していない。**
> **★本日ずっと「記憶で俯瞰しない」「知識が手順になっていなければ無いのと同じ」と書いてきた当人が、記憶に依存する経路を自分の作業に残していた。**

### 2-1. 計器は鳴っていた
**状況表は「実行構造の資料: ★古い」を出していた。** **私は commit の差分（ファイルが変わった）としては直したが、`gaps` の欠落は鳴っていなかった。**
> **∴ 計器が見ていたのは「掲載ファイルの変更」だけで、「文書に書いた Gap が資料に入ったか」は見ていなかった。**
> **∴ 鳴らない欠落は、鳴るまで見つからない。**

---

## 3. 提案（★私は作らない。裁定を待つ）
**「手で写す」をやめる方法は2つある。どちらも小さい。**

| 案 | 内容 | 代償 |
|---|---|---|
| **A** | **状況表に「文書中の `G-\d+` のうち、JSON の `gaps[].id` に無いもの」を1行出す** | **★計器を1行増やすだけ。** 写し忘れがその場で見える |
| B | FINDING を置くたびに JSON へ追記する道具を作る | **新しい道具が増える。** v2.0（膨らませない）に触れる |

**【設計:CC-α】A を推す。** **「忘れないようにする」のではなく「忘れたら見える」ようにする。**
**★本日ずっと有効だったのはこの形である**（プロセス鮮度・台帳のずれ・未応答件数）。
**★ただし計器は MGR の持ち場なので、私は作らない。**

## 4. 私が続けること
- **★資料の更新を、FINDING を書く作業の一部にする。** **別作業にしない**（D-21 で自分が書いた「置くのと同じ作業で書く」と同じ）。
- **次に Gap を登録するときは、文書と JSON の両方に同時に書く。**

## 5. 残（消さない）
- **`G-22` は配線されたが、`_run_test` を直接呼ぶ他の経路は素通りのまま。** `OPEN` 維持。
- **`G-20` は `_run_test` については実装になったが、他項目は宣言のまま。** `CONTRADICTED` 維持。
- **MD 側（`2DER_EXECUTION_ARCHITECTURE.md`）の Gap 表は、まだ16件のままである。** **★次の作業で同期する。** **本文書では JSON のみ同期した。**

---
*CC-α D-34。Taka の指摘どおり、本日9件の欠陥を文書に書きながら資料には1件も入れていなかった。同期を実施し `gaps` は 16→★25（OPEN 17/CLOSED 4/PARTIALLY_CLOSED 1 ほか）、`G-20`〜`G-28` を追加し `G-16`・`G-17` を CLOSED に更新、twoder の commit も現在の HEAD にした。★流れなかった理由は私の設計——FINDING 中の Gap 表も JSON の gaps も「私が手で写す」ことに依存しており、本日9回忘れた。「記憶で俯瞰しない」「知識が手順になっていなければ無いのと同じ」と書いてきた当人が、記憶に依存する経路を自分の作業に残していた。計器は「掲載ファイルの変更」は鳴らしたが「文書の Gap が資料に入ったか」は見ておらず、鳴らない欠落は鳴るまで見つからない。提案=A(状況表に「文書中の G-\d+ のうち JSON に無いもの」を1行出す。忘れないのでなく忘れたら見える形。本日有効だったのはこの形)を推し、B(追記の道具を作る)は膨張に触れるので採らない——ただし計器は MGR の持ち場なので私は作らない。私が続けること=資料の更新を FINDING を書く作業の一部にし別作業にしない。★MD 側の Gap 表はまだ16件のままなので次の作業で同期する（本文書では JSON のみ）。*
