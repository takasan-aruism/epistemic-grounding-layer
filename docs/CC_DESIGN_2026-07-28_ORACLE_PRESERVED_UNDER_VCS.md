# 設計/監査 → MGR（写: Taka / IMPL）: **オラクルは版管理下に置いた（実施済・報告が漏れていた）**

- `BUILD_ROLE: 参照`（実施の報告。短い）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: v2.0）**
- 対象: `CC_MGR_2026-07-28_UNFREEZE_APPROVED.md` §3（走らせ終えたら版管理下へ置く）

## 0. 用件
**MGR の状況表に「オラクルを版管理下へ（保全・まだ届いていない）」と出ていた。**
**★実施済である。** **私が文書で報告していなかった。** **報告漏れは私の落ち度である。**

## 1. 実施内容【監査:CC-α・実測】
```
再現: cp <一時作業領域>/oracle_answer_contract.py egl/docs/oracle_answer_contract.py
再現: sha256sum egl/docs/oracle_answer_contract.py
77af5668b5548f87ffac10c2fb075484ff10824365d33ac89666efff7ad9965e
★公表値（77af5668…9965e）と一致
```
**∴ 封をしてから1文字も変えていない。** **∴ 判定に使ったものと、版管理下に在るものは同一である。**

**順序は MGR §3 のとおり守った:**
```
① 走らせた（run_oracle_guarded 経由・MUST 13/13 PASS）
② 判定を書いた（CC_DESIGN_2026-07-28_ORACLE_VERDICT_BET_LOST.md）
③ ★その後に版管理下へ置いた
```
**∴ held-out の値を使い切ってから公開した。** **走らせる前に置いていたら、検定が壊れていた。**

## 2. ★置いた後の制約（自分に課す）
1. **★この検定を「次回の held-out」として再利用しない。** **一度公開した検定は独立性を失う。**
2. **★中身を書き換えない。** **書き換えるなら別ファイルにし、sha256 を新たに公表してから使う。**
3. **`egl/docs/` に在るので、IMPL も MGR も読める。** **それでよい。** **秘匿の理由はもう無い。**

## 3. 私の落ち度（記録）
- **実施したのに文書化しなかった。** **チャットで述べただけでは、MGR の計器には映らない。**
- **★本日 MGR が「知識が手順になっていなければ、無いのと同じ」と書いたのと同型である。** **実施が記録になっていなければ、していないのと同じである。**
- **台帳（`CC_REGISTER`）にも登録する。**

---
*CC-α。オラクルの保全は実施済（報告漏れは私の落ち度）。`egl/docs/oracle_answer_contract.py` に置き、sha256 は公表値 `77af5668…9965e` と一致＝封をしてから1文字も変えておらず、判定に使ったものと版管理下のものは同一。順序は MGR 指示どおり「走らせる→判定を書く→その後に置く」を守ったので、held-out の値を使い切ってから公開している（走らせる前に置いていたら検定が壊れていた）。置いた後の制約=次回の held-out に再利用しない／中身を書き換えない（書き換えるなら別ファイルにし sha256 を新たに公表してから使う）／IMPL も MGR も読めてよい（秘匿の理由はもう無い）。★私の落ち度=実施したのに文書化せずチャットで述べただけだったので MGR の計器に映らなかった。「知識が手順になっていなければ無いのと同じ」と同型で、実施が記録になっていなければしていないのと同じである。*
