# 【D-173】確定 — **★「作らない」を書く欄が★存在しない。★却下された PLAN は様式の中で最も素直な出力**

- **宛: TAKA** / 写: 設計/監査 / IMPL / 発: MGR / 2026-07-31 15:0x / TYPE=STATUS
- **正典**: `TAKA_2026-07-31_EXPOSE_THE_REJECTION_REASON_PLAN_INPUTS.md`（★逐語・様式） ／ **★新しい名前で置いた**

## 1. ★あなたの様式で（★設計の露出を確定として採る）

```
AUDIT_RESULT: FAIL

FAILED_CLAIM:
既に取得済みの情報を再取得対象としている

CONFLICTING_INPUTS:
- Observation（prompt 6行・逐語）:
    "ALREADY ACQUIRED FOR THIS TASK (each line is an observation record that already exists;
     it was retrieved before this planning step)"
- Observation（prompt 12行・逐語）:
    "VALUES ALREADY OBSERVED (… anything not present here was NOT obtained)"
- PLAN（出力・逐語）:
    "Implement process RSS collection using subprocess to run ps command."
    "Implement GPU memory collection using subprocess to run nvidia-smi."
    "Implement Docker container status collection using subprocess."

REJECTION_REASON:
観測済み事実を既知情報として扱わず、未確認情報として再計画している。
Observation は「既に取得済」と明示しているのに、PLAN は同じ3つの取得手段を実装対象として並べた。

LIKELY_CAUSE（★仮説である。★実証していない）:
★PLAN の出力様式に「★既に取得済だから作らない」を書く欄が★存在しない。
★17欄すべてを列挙して確認した結果、「既知」「不要」「skip」「already」に当たる欄は ★0件。
∴ 観測が「既知」として提示されても、★出力は必ず「作るもの」を要求される。
```

## 2. ★あなたの仮説を、★現物が1段 強めた（★弱めたのではない）
```
★あなたの仮説: 「★『不足情報を取得せよ』が『既存観測を優先せよ』より★強く作用している」
★★現物: ★「既存観測を優先せよ」に当たる★欄が ★0件である。
∴ ★★「強弱」ではなく「★片方が存在しない」である。★あなたの読みの方向は当たっており、★程度が違った。
```

## 3. ★対応づけた衝突（★3件。★片方だけ書かない）
| # | 入力A | 入力B | 衝突 |
|---|---|---|---|
| **1** | Observation「★既に在る」 | 制約「`steps`＝★実装手順」「`requirement`＝★worker が実装する仕様」「`files_expected`＝★作る/変えるファイル」 | **★「既に在る」と言われた物を、★「作るもの」としてしか出力できない** |
| **2** | 依頼「★取得できるようにする方法を検討せよ」 | Observation の取得記録に★コマンドそのものが入っている | **★求めた直後に★取得コマンドの一覧が置かれている ∴ ★実装候補と読める** |
| **3** | 役割「★Do not implement it; plan it.」 | 制約「`test_body`＝★テストの完全な python ソース」 | **★実装するなと言いながら、★同じ prompt で実装を要求している** |

**★衝突が無い所も書く**: **禁止の側**（標準ライブラリのみ／commit 禁止／既存 repo を変更しない）に**★衝突は無い**。**★衝突しているのは「★何を出力させるか」の側だけである。**

## 4. ★PASS/FAIL だけにしない（★あなたの要求）
> ### **★4つの入力のうち、★3つ（役割定義・欄の定義・依頼）が「★作れ」と言い、★1つ（Observation）だけが「★既に在る」と言っている。★3対1である。**
> ### **★しかも Observation 自身が両義的である**（★「既に在る」と言いながら★取得コマンドを見せている）。
> ### **★∴ 却下された PLAN は、★与えられた様式の中では★最も素直な出力である。**

**★「Qwen が悪い」とは書かない。★「PLAN が悪い」とも書かない。★様式がそう要求している。**

## 5. ★誰の却下理由か（★正確に）
```
★★`QwenAuditor`（DW の AUDIT 段）は ★本日1度も到達していない ∴ ★その却下理由は存在しない。
★★上に露出したのは ★我々（設計/監査）の却下理由である。
★★「既存 Audit の判定理由」を DW の auditor に出させるなら、★それは別件である（★言われた時点でやる）。
```

## 6. ★やっていないこと（★あなたの「確認後」を守る）
```
★PLAN を修正していない ／ ★新規の監査を追加していない ／ ★GENERATE を実行していない
★prompt を変更していない ／ ★コードを1行も変えていない ／ ★投入していない
```

## 7. ★この確認から出る、★ただ1つの次の1件（★着手していない）
> **★出力様式に「★既に取得済だから作らない」を書ける欄を1つ足す。**

```
★根拠: ★§1 の LIKELY_CAUSE が「★欄が0件」だからである ∴ ★欄を足すのが★直接の対処である
★★これは「LLM に指示を足して当てにいく」ではない。★★出力の器に、★存在しない選択肢を1つ作る話である
★★予想（★先に書く）: ★受入3 は★動く見込みが在る（★初めて「作らない」と書ける）
                      ★受入4 は★別である（★不足を観測から述べさせる話）
★★★着手していない。★あなたの「確認後」の合図を待つ。
```

---
**決めたこと**: **①露出した却下理由はあなたの様式のとおりで、`LIKELY_CAUSE` は「出力様式に『既に取得済だから作らない』を書く欄が存在しない（17欄を全部列挙して0件）」 ②あなたの仮説は方向が当たっており、程度が違った——「強弱」ではなく「片方が存在しない」 ③衝突は3件（既在のものを作るものとしてしか出力できない／取得方法を求めた直後に取得コマンドが置かれている／実装するなと言いながらテストの完全なソースを要求）で、禁止の側には衝突が無い ④3対1であり、Observation 自身も両義的 ∴ 却下された PLAN は様式の中では最も素直な出力である ⑤露出したのは我々の却下理由で、`QwenAuditor` のものは存在しない ⑥次の1件は「作らない」を書ける欄を1つ足すことだが、着手していない。**
