# 実装 → 設計/監査: **例外を別の欄に分け、内訳を数えて出すようにしました**（私の欠陥2件の修理）

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=BUILT
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-30_D73_TASK_SUMMARY_BUILD_SPEC_v2_2.md`（**v1.0 / v2.0 / v2.1 は見ていません**）
- **受領した文書**: 上記 / `CC_MGR_2026-07-30_D73v22_APPROVED_TWO_KINDS_OF_ZERO.md`（写しで観測）
- 私の欠陥の指摘元: `CC_DESIGN_2026-07-30_D79_BUILT_AUDIT_AND_I_DID_THE_BROWSER_CHECK.md`

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 0. ★直したのは、私が作った欠陥2件です
| # | 私が作ったもの | 直した形 |
|---|---|---|
| **1** | **例外（引けなかった）を `unknown_state`（表に載っていない）に足していた** — **1つの欄が2つのことを指す** | **★`uncountable` を分けた** |
| **2** | **内訳を「固定の説明文1行」で済ませ、受入4〜7 をまとめて1つの印にした** | **★実際の `state` を数えて出す** |

> **★1 は、今日ずっと潰してきた形（1つの欄が2つのことを指す）を、★私が作っていました。**
> **★2 は、まとめて印を付けたせいで未達が隠れました。** **★1項目1つの印にします。**

---

## 1. 受入（★1項目ずつ・まとめません）

| # | 条件 | 結果 |
|---|---|---|
| **1** | **`uncountable` が `unknown_state` と別の欄になっている** | **★済**（§2） |
| **2** | **`uncountable` が0件でも行を消さない** | **★済**（実測0件だが行を出しています・§2） |
| **3** | **「数えられなかった」の下に1行 添える** | **★済**「機械が答えを出せなかったものです。」 |
| **4** | **和に `uncountable` を入れる** | **★済**（117+18+18+0 = 153・§3） |
| **5** | **内訳を数えて出す（固定文にしない）** | **★済**（`JUDGE_REQUIRED 18件`・§3） |
| **6** | **内訳が出せないときは「出せません」＋理由** | **★済**（`unknownBreakdown` の分岐・§2） |
| **7** | **内訳の和が `unknown_state` と一致** | **★済**（18 = 18・§3） |
| **8** | **配信物の JS が `node --check` を通る** | **★済**（`/command` 17772 バイト・`/` 999 バイト） |
| **9** | **非回帰 91 passed / 7 failed** | **★済**（顔ぶれ diff 空・走らせ方は §5） |
| **10** | **画面から ID が消えたことを見た** | **★未確認。私はブラウザを開いていません**（§6） |

## 2. 実装（2箇所）
```python
# webui.py::deferred_summary — ★(a) と (b) を分ける
out = {"total": …, "background": 0, "our_turn": 0, "unknown_state": 0,
       "uncountable": 0, "unknown_breakdown": {}, "blocked_outside": 0}
for t in ids:
    try:
        n = _D.next_legal_operation(t, events=evs)
    except Exception:
        out["uncountable"] += 1                # ★「決まっていない」に混ぜない
        continue
    if n.get("actor_id") == "-":
        out["unknown_state"] += 1
        st = n.get("state") or "?"             # ★内訳は固定文にせず、実際の state を数える
        out["unknown_breakdown"][st] = out["unknown_breakdown"].get(st, 0) + 1
    …
```
```javascript
// ★内訳。出せなければ理由つきで書く（★固定文を出して「出した」と扱わない）
function unknownBreakdown(b){
  if(!b) return "<div class='mut'>└ 内訳は出せません（状態を数えた結果がサーバから来ていないため）</div>";
  const ks=Object.keys(b);
  if(!ks.length) return "";
  return ks.map(k=>"<div class='mut'>└ "+esc(k)+"  "+esc(b[k])+"件</div>").join("");}
```

## 3. 実測（★今日の値です。実装に固定していません）
```
再現: cd /home/takasan && python3 -c "…"（sys.path に twoder と dev-workcell・★webui.deferred_summary を直接呼ぶ）

集約: {'total': 153, 'background': 117, 'our_turn': 18, 'unknown_state': 18,
       'uncountable': 0, 'blocked_outside': 2}
内訳（数えた結果）: {'JUDGE_REQUIRED': 18}

★和 = 153（背景117 + Claude18 + 決まっていない18 + 数えられなかった0） / total = 153 → 一致
★内訳の和 = 18 / unknown_state = 18 → 一致
所要 0.096 秒
```

### 3-1. ★2種類の0（★混ぜません）
| 欄 | 今日の値 | **意味** |
|---|---|---|
| **数えられなかった** | **0件** | **★測って0です。** 例外は数えられます |
| **Taka の手番** | **「出せません」** | **★測れません。** 人を指す担当者の値がそもそも在りません |

> **★どちらも画面に出しますが、★同じ書き方をしていません。**

## 4. 画面に出る形
```
進行中の依頼  153件
  この一覧に、終わった依頼と止まった依頼は入っていません。

  背景で進んでいる（Qwen が動かす）    117件
  Claude の手番                        18件
  誰がやるか決まっていない              18件
  └ JUDGE_REQUIRED  18件
  数えられなかった                       0件
  「数えられなかった」は、機械が答えを出せなかったものです。
  いま前に出すべきもの   出せません（前に出す順番を決める値が 2DER に無いため）
  合計                                153件

  ※ 止まっている依頼が 2件 ありますが、この一覧の対象外です。
  ※ Taka の手番かどうかはこの画面では出せません（2DER に人を指す担当者の値が無いためです）。
```
- **★`JUDGE_REQUIRED` は私が決め打ちしていません。** **★実際に数えた結果です。** **★状態が2種以上あれば全部出ます。**

## 5. ★非回帰の走らせ方（`G-74`）
```bash
/home/takasan/.cc_tmp/d42/run_regression.sh /home/takasan/egl/docs/.cc_tmp/d49/regression_d73v22.txt
```
**中身**: `cd /home/takasan` してから `twoder/regression/*.py` の**98本**を1本ずつ
`timeout 600 python3 -m twoder.regression.<名前>` で走らせ、**終了コードで数える**（`pytest` は使っていません）。
```
結果: DONE 91 passed / 7 failed
基準との diff: ★空（失敗7本の顔ぶれまで一致）
```

## 6. ★確かめていないこと
1. **★画面をブラウザで開いていません。** **★受入10 は未確認です。**
   - **★ただし「Taka にしか出せない」とは書きません。** **★設計/監査は `curl` で取得して文字化け・字の大きさ・言い回しを機械的に測りました。** **★私はそれをやっていませんでした。**
2. **★字の大きさ（11〜12px）と、同じ「無い」に6通りの言い方が在ることは、★直していません。** **★本 build の範囲外です。** **★指示を待ちます。**
3. **★`uncountable` が1件以上出た画面を見ていません**（今日は0件）。**★作って試していません。**

## 7. 触ったファイル / commit / 再起動
```
twoder/webui.py のみ
```
- **★再起動しました**（配信物で検査するため・訂正後の順番②）。
- **commit していません**（MGR）。**投入していません。** **資料の同期はしていません。**

---
*IMPL BUILT（D-73 v2.2）。★**直したのは私が作った欠陥2件**=①**例外（引けなかった）を `unknown_state`（表に載っていない）に足していた＝1つの欄が2つのことを指す形を、今日ずっと潰してきた側の私が作っていた** → **`uncountable` を分けた** ②**内訳を固定の説明文1行で済ませ受入4〜7 をまとめて1つの印にしたせいで未達が隠れた** → **実際の `state` を数えて出し、受入は1項目1つの印にした**。★受入10項目を**1つずつ**判定=`uncountable` を別欄に（0件でも行を消さない）／下に1行添える／**和に入れる（117+18+18+0=153 一致）**／**内訳を数えて出す（`JUDGE_REQUIRED 18件`・決め打ちしていない・2種以上なら全部出す）**／出せないときは「出せません」＋理由／**内訳の和 18 = `unknown_state` 18 一致**／**配信物の `node --check` OK**／**非回帰 91/7・顔ぶれ diff 空（走らせ方を明記）**／**受入10 の画面確認は未確認**。★**2種類の0 を混ぜない**=「数えられなかった 0件」は**測って0**（例外は数えられる）、「Taka の手番」は**測れない**（人を指す担当者の値がそもそも無い）——**どちらも出すが同じ書き方をしない**。★**確かめていないこと**=**ブラウザを開いていない**が、**「Taka にしか出せない」とは書かない**——**設計/監査は curl で取得して文字化け・字の大きさ・言い回しを機械的に測っており、私はそれをやっていなかった**／**字の大きさ（11〜12px）と同じ「無い」に6通りの言い方が在る件は直しておらず本 build の範囲外で指示待ち**／**`uncountable` が1件以上出た画面は見ていない（今日は0件・作って試していない）**。触ったのは `twoder/webui.py` のみ、再起動は訂正後の順番②に従い実施、commit・投入・資料同期はしていない。*
