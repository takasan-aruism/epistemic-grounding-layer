# 【BUILT / D-204 §3】監視を**★自分の手番**へ張り替えた（★新設ではない）

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: MGR / Taka / 発: 実装(IMPL) / 2026-08-01 03:3x / TYPE=BUILT
- **開発者規律 確認済（版: v1.0 / 2026-07-31）** ／ **実装源**: `CC_DESIGN_2026-08-01_D204_RELAY_TO_IMPL_WATCH_YOUR_OWN_TURN.md`
- **★コードは1行も書いていない**（★監視の張り替えのみ）／ **`:8005` を叩いていない** ／ **commit していない**

---

# 1. ★張り替えた監視の5項目（★§3。★書いていないものは張らない）

| # | | |
|---|---|---|
| **入力元** | **`GET /api/state?task_id=<id>`** を ★6件（front door のみ） | `TASK-2DER-` の `B37727E3`／`24EF3F6E`／`786DC614`／`5483F350`／`322B1B62`／`1F4C8C42` |
| **算出規則** | 各 task の **`dw_state｜last_completed_op｜next_operation`** を `"%s" %` で文字列化し、6件を並べて**1本の署名**にする（★2分ごと） | ★要求した id と **返ってきた `task_id` が一致するか**まで見る（★不一致は `WRONG_TASK` として鳴らす） |
| **鳴らす条件** | **★署名が前回と変わった時だけ1行**／**★読めない時も1行**（`UNREADABLE`・`WRONG_TASK`・空）／**★生存は30分ごとに1行** | ★動かない間は鳴らさない |
| **廃止条件** | **★滞留5件（②）は `ITEM-2DER-EVO-0028` が通ったら外す**（★進捗のみの投入で task を立てなくなれば滞留は消える見込み）／**★`B37727E3`（①）は その task が終わったら外す** | ★先に書いた |
| **重複確認** | **★`/api/control` を叩かない**（MGR と重複）／**★`/api/roadmap` を叩かない**（設計と重複）／**★台帳を直読しない** | ★見るのは「★自分の手番が来たか」だけ |

**★本数**: **★2本のまま**（★文書監視＋本監視）。**★増えていない**——front door を見ていた壊れた1本は**★停止済**で、その枠を張り替えた。

---

# 2. ★1本目に何が出たか（★基準値と明記されているか）

```
[起動時の基準値・★台帳が動いた証拠ではない]
TASK-2DER-1F4C8C42=CREATED|None|PLAN TASK-2DER-24EF3F6E=CREATED|None|PLAN
TASK-2DER-322B1B62=CREATED|None|PLAN TASK-2DER-5483F350=CREATED|None|PLAN
TASK-2DER-786DC614=CREATED|None|PLAN TASK-2DER-B37727E3=JUDGE_REQUIRED|None|BLOCKED
```
**★「起動時の基準値・台帳が動いた証拠ではない」を★機械が刻んでいる**（★私が後から書いた文ではない）。
**★空を基準値にしていない**——★張る前に6件すべてを叩き、**★要求した id と返り値の `task_id` が一致すること**を確かめた（★6/6 一致）。

---

# 3. ★壊れていた計器を直した形

```
★原因（★設計が確定してくれたとおり）: `recent_de` / `recent_chg` の要素は ★辞書ではなく★文字列
   ∴ ★`.get(...)` を呼ぶと落ち、★標準出力が空になり、★空が基準値になっていた。
★★直した形: ★★値の取り出しは全部 ★`'%s' % d.get(...)` にした（★MGR の監視と同じ形）。
★★★さらに ★沈黙しない形を足した:
   ・★JSON が読めなければ ★`UNREADABLE` を★出力して鳴らす（★黙って空にしない）
   ・★返ってきた `task_id` が要求と違えば ★`WRONG_TASK(...)` を鳴らす
     （★`/api/state` は ★task_id を付けないと★別の task を返すことを ★7月30日に実測している）
```

---
*IMPL → 設計/監査（写: MGR / Taka）。D-204 §3 の申し送りどおり、**監視を「自分の手番が来たか」へ張り替えた（新設ではなく張り替え。本数は文書監視と合わせて2本のまま——front door を見ていた壊れた1本は停止済）。** 5項目は**入力元**=`GET /api/state?task_id=` を6件（`B37727E3` と滞留5件）、**算出規則**=`dw_state｜last_completed_op｜next_operation` を `"%s"` で文字列化した署名を2分ごとに比較し要求 id と返り `task_id` の一致まで見る、**鳴らす条件**=署名が変わった時／読めない時／30分ごとの生存、**廃止条件**=滞留5件は `ITEM-2DER-EVO-0028` が通ったら外し `B37727E3` はその task が終わったら外す、**重複確認**=`/api/control`（MGR）も `/api/roadmap`（設計）も叩かず台帳も直読しない。**1本目は機械が「起動時の基準値・台帳が動いた証拠ではない」と刻み、空を基準値にしていない（6件すべて id 一致を張る前に確認）。** 壊れていた計器は原因（要素が文字列）どおり **`'%s' % x` に直し**、さらに `UNREADABLE` と `WRONG_TASK` を鳴らして**沈黙しない**形にした。**コードは1行も書いていない。`:8005` を叩いていない。commit していない。***
