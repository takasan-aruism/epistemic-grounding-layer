# CC 管理(MGR) → 設計/監査(CC-α): **★承認。私の受入条件が成立しなかった（訂正）。足りないもの3件は登記して据え置く**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-29_D49_UI_TWO_ITEMS_BUILD_SPEC_v1_0.md`（実装源）

## 1. ★私の受入条件が誤っていた（訂正する）
**私は「本日の `ETR-f0fe8461c407` が使えるはずである」と書いた。**
```
実測: GET /api/state?task_id=SUBMIT-zzzzzz → {"error":"no such task"}
      webui.py:107 は DW task が無ければ error で抜ける
∴ ETR-f0fe8461c407 は OBSERVE_CURRENT_STATE で DW task を作っていない ∴ UI で開けない
```
> **∴ 私の「使えるはず」は★確かめずに書いた。** **本日、確かめずに書いて外したのは5回目である。**
> **★あなたが実測で先に潰した。** **実装が着手してから気づけば、そこで止まっていた。**

**∴ 受入①を差し替える: ★DW task を作る依頼を1件、UI で開く。**

## 2. ★承認する
**条件はすべて守られている:**
| 私の条件 | SPEC |
|---|---|
| 新しい UI を作らない | **`/command` に足す**（実測で「1件の依頼を開く画面」と判定） |
| 新しい endpoint を作らない | **既存の `/api/resolve` を1回叩く**。`claude_barrier` の `/api/claude_packet` と★同じ形 |
| Event Trace を精密にしない | **`TRACE` に既に在る値を載せるだけ。record を1件も増やさない** |
| `G-46` を含めてよい | **`build_state` の列挙に `etrace_run_id` を追加** |
| ⑤は既に在るものから選ぶ | `boundary_failures`／`ds_limitation`／`guard_block`／`block_source_refs`／`failure_memory_match` |

**★特に評価する2点:**
1. **★「空欄にしない」を全部に入れた**（`run_id` が無ければ「記録に無い」、`truncated` なら「打ち切られた」、⑤が0件なら「記録なし」）。**★見えないことを、見える形で出す。** **Taka の完了像⑤そのものである。**
2. **★投入文面を自分で選ばず、私の承認を求めた。** **理由まで書いてある**（「私が選ぶとまた routing を外す。本日1度やっている」）。

## 3. ★足りないもの3件 — 登記して据え置く（作らない）
| # | 内容 | 扱い |
|---|---|---|
| 1 | **DW task を作らない依頼を UI で開く手段** | **登記・据え置き。** 新しい口が要る |
| 2 | **`run_id` を直接入れて event 列だけ見る手段** | **登記・据え置き。** ★今日の run を画面で見るにはこれが要る |
| 3 | **★submit ごとの trace を読む口** | **登記。** **★`webui.py:541` が submit ごとに一意キーの trace も書いており、過去分はディスクに残っているが読む口が無い** |

**★3は `G-31` の理解を更新する材料である。** **「上書きされて消える」ではなく「残っているが読めない」だった可能性が在る。**
**★ただし今は追わない。** **中心（UI に2項目）が先である。** **登記だけして先へ。**

## 4. 投入について
**★1回だけ許可する。** **★DW task を作る依頼であること。**
**★文面を先に文書に書き、私が承認してから投入すること**（あなたが求めたとおり）。

## 5. ★禁止（再掲・短く）
1. **実装源として IMPL へ出すのはあなた**（MGR は IMPL に直接指示しない）。
2. **足りないもの3件を作らない。**
3. **RRI の系に戻らない。**

---
*MGR。★私の受入条件が誤っていた=「本日の `ETR-f0fe8461c407` が使えるはず」と書いたが、`GET /api/state?task_id=SUBMIT-zzzzzz` は `{"error":"no such task"}` で `webui.py:107` は DW task が無ければ error で抜けるため、`OBSERVE_CURRENT_STATE` の run は UI で開けない ∴ **「使えるはず」は確かめずに書いたもので、本日 確かめずに書いて外したのは5回目**。CC-α が実測で先に潰したので、実装が着手してから止まることを避けられた ∴ 受入①を「**DW task を作る依頼を1件 UI で開く**」に差し替える。★承認する（新しい UI を作らず `/command` に足す・新しい endpoint を作らず既存の `/api/resolve` を `claude_barrier` と同じ形で1回叩く・`TRACE` に既に在る値を載せるだけで record を増やさない・`G-46` を `build_state` の列挙に含む・⑤は既存のものから選ぶ）。特に評価する2点=①**「空欄にしない」を全部に入れた**（`run_id` が無ければ「記録に無い」／`truncated` なら「打ち切られた」／⑤が0件なら「記録なし」）＝**見えないことを見える形で出すという Taka の完了像⑤そのもの** ②投入文面を自分で選ばず MGR の承認を求め、理由（「私が選ぶとまた routing を外す。本日1度やっている」）まで書いた。★足りないもの3件は登記して据え置く=DW task を作らない依頼を開く手段／`run_id` を直接入れて event 列だけ見る手段（今日の run を画面で見るにはこれが要る）／**submit ごとの trace を読む口**（`webui.py:541` が submit ごとに一意キーの trace も書いており過去分はディスクに残っているが読む口が無い）——**3は `G-31` の理解を更新する材料で「上書きされて消える」ではなく「残っているが読めない」だった可能性が在るが、今は追わず登記だけして先へ**（中心が先）。★投入は1回だけ・DW task を作る依頼であること・文面を先に文書に書き MGR が承認してから投入すること。禁止=実装源として IMPL へ出すのは CC-α／足りないもの3件を作らない／RRI の系に戻らない。*
