# 【BUILD SPEC】`_plan_prompt` に**「作らない」を書ける欄を1つ足す**（★1箇所）— ★前後比較つき

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 16:1x / TYPE=BUILD_SPEC
- **運用方針 確認済（版: v2.9）** ／ **正典**: `TAKA_2026-07-31_D174_EVALUATION_AND_SELF_IMPROVEMENT_LOOP.md`（逐語）／**裁定**: `D-174`（(a) prompt のみ）／`D-175`（評価）／`D-176`（依頼文）
- **★新しい名前で置いた** ／ **★2DER の外ではない**（★本番経路への投入を含む）／ **★担当工程数の差分は 0 の見込み**

## 0. ★目的（★Taka の再定義。★取り違えない）
```
★★目的は「★prompt を足したら動いたか」★ではない。
★★「★PLAN が Observation を★どのように解釈し、★どの論理で計画を立てたか」を★明らかにすることである。
★★∴ ★受入は「動いた/動かない」ではない。★§3 の4点である。
```

---

# 1. ★やること（★1箇所だけ）

> ### **★`twoder/build_planner.py` の `_plan_prompt` に、★「★既に取得済だから作らない」を書ける欄を★1つ足す。**

| 条件 | |
|---|---|
| **★足す先** | **★prompt の JSON 欄の定義のみ**（★`D-174` の裁定 (a)） |
| **★触らない** | **★`STRUCTURED_KEYS` / `EXECUTABLE_KEYS`**（★設計が確認済: ★検査は「欄が無いこと」しか見ず、worker は6欄しか消費しない ∴ ★壊れない） |
| **★欄の性格** | **★「作らない」と書ける器を1つ作るだけ。★「観測を使え」という★指示を足さない**（★規則の追加になる） |
| **★語の禁止** | **★`gpu`/`nvidia`/`port`/`docker`/`disk`/`memory` を diff に0件**（★特定の主題に寄せない・★大小無視・打ち切り無しで示す） |
| **★欄名** | **★あなたが決める。★狭い方を採り、理由を1行 書く**（★例: 既存の語彙に寄せる） |

---

# 2. ★確認（★依頼文は MGR が固定済み）

## 2-1. ★依頼文（★`D-176` から機械で抜く。★打ち直さない）
```
現在メモリ使用量の大きいプロセスの状況を確認し、確認できない項目があれば、それを取得できるようにする方法を検討してください。
```
**★設計が事前に計算した値（★一致を確認してから投入する）**
```
★字数 62 ／ ★sha1 6818e4bb774433feaa9375909b615f0cce64aa21
★★予告 task_id: TASK-2DER-6818E4BB   ← ★違う id が出たら「外れた」と書く
★★前回（D-159）の task は TASK-2DER-5ABBA4A1 ∴ ★別 task になることを設計が確認済
★★★主題も文の形も前回と同じ。★変えたのは「多く使っている」→「使用量の大きい」だけ
   ＝ ★prompt の欄1つ以外は何も変わらない ＝ ★最も締まった前後比較
```

## 2-2. 手順
```
① 修正（★1箇所）→ ② webui 再起動（★全件 記録・run-gate 初期化も）
③ 依頼文を機械で抜く（★字数・sha1 の一致を確認）→ ④ task_id を投入前に予告
⑤ POST /api/submit ★1回 → ⑥ ★直後に GET /api/receipt（★他の口を叩く前に）
⑦ POST /api/run_next?task_id=… ★1回 → ⑧ ★PLAN が出たら★止める（★GENERATE へ進まない）
```

---

# 3. ★評価（★Taka の4点。★測る前に固定。★1と2を1つの印にまとめない）

| # | ★見るもの | ★合格の書き方（★これ以外を合格にしない） |
|---|---|---|
| **1** | **引用したか** | **★値を書いただけでは不可。★「既知情報として扱った」と読める形であること** |
| **2** | **★利用したか** | **★「既に取得済なので再取得不要」／「この情報を前提に次工程へ進む」に★当たる記述が在ること** |
| **3** | **無視した場合** | **★FAIL とだけ書かない。★①何を無視したか ②なぜそう判断したと考えられるか ③prompt 内のどの指示が優先された可能性があるか ★まで書く** |
| **4** | **★Prompt の論理整合性（★最重要）** | **★「Observation を使わない」ではなく「★Prompt が Observation を使えない設問になっている」可能性を見る** |

## 3-1. ★Audit の出力様式（★Taka の追加・6欄。★`D-172` の5欄を置き換える）
```
★却下対象 ／ ★証拠 ／ ★入力間の矛盾 ／ ★推定原因 ／ ★修正候補 ／ ★修正候補のリスク
★★`修正候補のリスク` を★空にしない。★「リスクが無い」と書くなら★なぜ無いかを書く
★★PASS でも「なぜ通ったか」を同じ6欄の形で書く（★「却下した」ではなく「なぜ」が分かる監査にする）
```

---

# 4. ★予告（★投入前に固定する。★外れたら「外れた」と書く）
```
★R-1: ★分類は `OBSERVE_CURRENT_STATE`（★前3回と同型の文）        …確信 高
★R-2: ★task_id は `TASK-2DER-6818E4BB`                            …確信 高（★決定論）
★R-3: ★受入1（引用）は立つ                                        …確信 高（★前回も立った）
★R-4: ★受入2（利用）は ★予告しない                                 …★根拠が無い
★R-5: ★受入3（重複計画しない）は ★予告しない                        …★40本で 2/10。★1回では言えない
★★★1回の結果で「動いた/動かない」を断定しない（運用方針 §4-13）。
   ★40本の実測では ★2/10 である ∴ ★1回 成立しても「直った」と書かない。★1回 失敗しても「効かない」と書かない。
   ★★書けるのは「★この1回はこうだった」だけである。
```

---

# 5. ★やってはいけないこと
```
★`STRUCTURED_KEYS` / `EXECUTABLE_KEYS` を触る  ★「観測を使え」という指示文を足す
★GENERATE を押す  ★Generate / Audit の改善を混ぜる（★Taka 明示）  ★観測の選別に触る（`G-06`・H2）
★Ledger / 図の修正 / (c) の patch に触る（★保留のまま）  ★commit する  ★61本の非回帰は走らせる
★★依頼文を打ち直す  ★★同名で文書を差し替える  ★★別の欠陥を見つけたことを理由に止まる
```

# 6. ★止まってよい場所
```
★依頼文の sha1 が `6818e4bb…` と違う → ★止める（★打ち直さない）
★1箇所で収まらないと分かった → ★止めて設計へ聞く（★`STRUCTURED_KEYS` に触る必要が出たら、それは (b) である）
★★それ以外は止まらない。★2通りに読めたら狭い方を採り、★迷いを1行 書く
```

# 7. ★報告
```
1. 変更した1箇所（★ファイル/hunk/挿入削除行数・★禁止語0件の走査結果）
2. ★評価4点の判定（★1条件に1つの印。★1と2を分ける）
3. ★Audit 6欄（★リスク欄を空にしない）
4. ★予告 R-1〜R-5 の当否
5. ★2DER 担当工程数の前回差分（★0 なら「0」）
6. ★次に直す1件（★原因が Prompt なら Prompt。★ただし1件だけ）
★★`:8005` は 2DER が内部で呼ぶ。★あなたは直接 叩かない。★呼ばれたら1行 書く
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---
**決めたこと**: **①`_plan_prompt` に「既に取得済だから作らない」を書ける欄を1つ足す（prompt のみ・`STRUCTURED_KEYS` は触らない・「観測を使え」という指示は足さない・特定主題の語を diff に入れない） ②依頼文は MGR 固定のものを機械で抜き、設計が事前に計算した 62字/sha1 `6818e4bb…`/予告 task_id `TASK-2DER-6818E4BB` と一致を確認してから投入1回 ③評価は Taka の4点（引用と利用を分ける・無視なら何を/なぜ/どの指示が優先されたかまで・論理整合性が最重要）、Audit は6欄でリスク欄を空にしない ④予告は R-1〜R-3 のみで受入2・3 は予告しない——40本で 2/10 なので、1回 成立しても「直った」と書かず、1回 失敗しても「効かない」と書かない。**
