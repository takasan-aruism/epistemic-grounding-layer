# 【D-145】★今回の修正で `Request` が切れていないか — ★確かめてから判定する

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 01:2x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-31_RUNTIME_INSPECTION_COMMON_BASE_ORDER.md`

## 1. ★私が気づいた1点（★IMPL の5行と食い違う可能性）

```
★第2試行で `Request` を PASS にした根拠は「trace_key から入力内容・受理時刻に到達できる」だった。
★今回、trace_key の形が変わり（TASK-…-<乱数>）、★それが resolve できない（A-3 ×）。
∴ ★新しい依頼について「入力内容へ到達できる」が★成り立たなくなった可能性が在る。
∴ ★Taka の配線でいう First FAIL は `Task` の下流ではなく、★`Request` かもしれない。
```

**★IMPL の C-1（既存 ID 10件 不変）は正しい。★だがそれは「昔の ID がまだ解ける」であって、**
**★「新しい依頼で通る」ではない。** **★これも「1つの言い方が2つのことを指す」である（本日13回目）。**

### 1-1. ★設計が確かめること（★1つだけ・既存の口のみ）
```
★今回の依頼（TASK-2DER-0C458F38）について、★入力内容と受理時刻に到達できるか。
★到達できるなら Request は PASS のまま。★できないなら ★First FAIL は Request である。
★口を作らない。★連番や合成 id を当てに行かない。
★到達できないなら「到達できない」と書けばそれが結論である。
```

## 2. ★今回いちばん価値が在ったこと（★記録に残す）

> **★一箇所 直したら、★前に直した所が外れた。**
> **★D-140 で足した `SUBMIT-` の分岐は、★task が出来た途端に前提を失った。**
> **★これは Taka の規則「一度に一箇所だけ／修正後は必ず同じ検査を再実行」が★実際に効いた形である。**
> **★まとめて2箇所 直していたら、★どちらが原因か分からなかった。**

**★IMPL がこれを自分で原因まで特定して書いたことを名指しで評価する**（`webui.py:661` の `(tid or "SUBMIT")`）。

## 3. ★止まった所を直させない（★正典のまま）
```
★A-3 の修正は submit.py の外になる ∴ ★今回はやらない。★IMPL の判断は正しい。
★run_next は押さない（★runnable が true になったが、★今回の対象は Task の生成まで）
★選別が効いていない件（build_request が毎回 全件を要求）も★今回 直さない。★記録のみ
```

## 4. ★登記（★設計が内容で。★id は私が指定しない）
```
★/api/state の egl.current_claims に「GPU使用率: 両GPUとも約0.92 (92%)」が出るが、
  ★これは DE-0072/0170/0171 由来の claim であって★今回の実測ではない。
  ★実測に GPU 使用率は無い（取得不能のまま）。
★∴ 同じ応答の中に「実測でない値」と「実測」が並び、★読み手が取り違え得る。
★直さない。★登記して進む。★IMPL が自分から書いたものである。
```

## 5. 手番
```
★あなた: IMPL の BUILT を監査 ＋ §1-1 を確かめる → 判定案（★Taka の8点で PASS/FAIL）を1本
★私    : 確定 → Taka へ5行で報告
★Taka  : 手番なし
```

---
**決めたこと**: **①「既存 ID が不変」と「新しい依頼で通る」は別物 ∴ 今回の依頼で入力内容に到達できるかを確かめてから First FAIL を決める ②一箇所 直したら前に直した所が外れた——Taka の「一度に一箇所・毎回 再検査」が実際に効いた ③A-3・run_next・選別は今回 触らない ④実測でない GPU 使用率が応答に並ぶ件を登記する。**
