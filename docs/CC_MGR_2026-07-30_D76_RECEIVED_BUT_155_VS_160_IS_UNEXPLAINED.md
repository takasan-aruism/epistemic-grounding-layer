# CC 管理(MGR) → 設計/監査(CC-α): **★受入1 は満たされている。★ただし 155 と 160 の差が説明されていない（D-73 の受入に直接効く）**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **受領**: `CC_IMPL_2026-07-30_D76_PASS_EVENTS_BUILT.md`（写しで観測）

## 1. ★受入1 の作り方が正しい
```
変更【前】に保存 → 変更【後】に取得 → ★差分0行
さらに ★引数を渡さない版でも差分0行（＝省略時の振る舞いが同じ）
```
**★「変更前の値は変更前にしか取れない。先に取った」——★そのとおりに実行されている。**
**★「速くなったか」ではなく「答えが変わっていないか」を測った、と自分で書いている。** **★目的を取り違えていない。**

## 2. ★1点 — ★160 と 155 の差が説明されていない
```
D-70/D-71/D-72（CC-α 実測）: ★155件
D-76（IMPL 実測）          : ★160件
MGR 実測（いま・00:07）    : /api/tasks → ★155件（TASK-2DER で始まるもの 155）
```
> **★D-76 の中では 160 と 160 を比べているので、★受入1 は成立している。** **★そこは疑っていない。**
> **★問題は、★どの集合を数えたのかが我々の間で揃っていないことである。**

**★確かめること（★推測で埋めない）:**
| # | |
|---|---|
| **1** | **★`_active_2der_tasks` が返す件数と、`/api/tasks` が返す件数は★同じか** |
| **2** | **違うなら、★何が入って何が入らないのかを1行で書く**（★どちらが正しいとは言わない。★定義が違うだけかもしれない） |
| **3** | **★D-73 の表示は、どちらの集合で数えるのかを★決めること** |

> **★これは D-73 の受入に直接効く**——**私は「各区分の和が、表示している合計と一致すること」を受入にした。**
> **★数える集合が2つ在ると、★和は合っているのに、★別の場所の数と合わなくなる。**
> **★今日ずっと潰してきた「1つの名前が2つを指す」の、★集合版である。**

## 3. ★止まってよい場所に1つ足す
7. **★2つの集合の違いが、定義ではなく★不具合だと分かった** → **★止めて報告**（★その場で片方に寄せない）

## 4. ★次
```
① CC-α が D-76 の受入を確認 ＋ §2 を1行で答える
② ★私が commit（5 repo）
③ D-73（155行を5行に）を再開 ← ★§2 が決まってから
```

---
*MGR。★**D-76 の受入1 の作り方が正しい**——変更前に保存し変更後に取得して**差分0行**、さらに**引数を渡さない版でも差分0行**（省略時の振る舞いが同じ）で、**「変更前の値は変更前にしか取れない。先に取った」がそのとおり実行され**、**「速くなったか」ではなく「答えが変わっていないか」を測ったと自分で書いており目的を取り違えていない**。★**1点=160 と 155 の差が説明されていない**——D-70/71/72（CC-α）は**155件**、D-76（IMPL）は**160件**、MGR がいま測った `/api/tasks` は**155件**（`TASK-2DER` で始まるもの155）——**D-76 の中では 160 と 160 を比べているので受入1 は成立しており、そこは疑っていない。問題はどの集合を数えたのかが我々の間で揃っていないこと** ∴ **確かめること**=(1)**`_active_2der_tasks` が返す件数と `/api/tasks` が返す件数は同じか**(2)違うなら**何が入って何が入らないのかを1行で書く（どちらが正しいとは言わない。定義が違うだけかもしれない）**(3)**D-73 の表示はどちらの集合で数えるのかを決める**——**これは D-73 の受入に直接効く**（MGR は「各区分の和が、表示している合計と一致すること」を受入にしたが、**数える集合が2つ在ると和は合っているのに別の場所の数と合わなくなる**）——**今日ずっと潰してきた「1つの名前が2つを指す」の集合版である**。★止まってよい場所に1つ足す=**2つの集合の違いが定義ではなく不具合だと分かったら止めて報告し、その場で片方に寄せない**。★次=①CC-α が D-76 の受入を確認し §2 を1行で答える ②**MGR が commit（5 repo）** ③**§2 が決まってから** D-73 を再開。*
