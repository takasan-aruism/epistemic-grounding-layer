# CC 管理(MGR) → 設計/監査(CC-α): Build 2 BUILT の回付 — **交絡の扱いを間違えないこと**（HANDOFF）

- `BUILD_ROLE: 参照` / 宛: DESIGN/AUDIT(CC-α) / 写: IMPL / Taka / 発: MGR / 2026-07-26 21:20 / TYPE=HANDOFF
- 対象: `CC_IMPL_2026-07-26_BUILD2_BACK_THIN_SLICE_BUILT.md`

## 1. 受領（未監査ゆえ Taka へは上げていない）
遠隔の取り違え **11/11 で routing 反転**／近縁は **routing 不変だが検索先が入れ替わる（Jaccard 0.036）**／長文は実発話の **42% が400字超・平均3446字**。**本番コード変更ゼロ**（`preflight_gate` 未接触＝根の修正は今回不要）。

## 2. 監査で必ず見ること
1. **遠隔側の「問い合わせ規則に非依存」という主張**が本当に構造的か。**ここだけが確定できる結論**だと IMPL が言っている。**その線引きが妥当かを検証すること。**
2. **★§2-3 の交絡**: 近縁の 0.036 は **IMPL 自身の `build_query`（`CHOICE`→「選択肢 候補 比較」等の付け足し）の副産物である可能性が高い**、と IMPL 自ら申告している。**この数値を「0.71 で足りるか」の答えに使わないこと。** 結論にしたいなら **問い合わせ規則を変えた対照**が要る。**IMPL に自分で変えさせない**（計器を自分に有利にすることになる）——**規則の変更案は DESIGN が出すこと。**
3. **§3 の指摘（`acquire` は外部取得で RETRIEVE 定義と別物）** は SPEC の誤りに見える。**参考表を訂正すること。**
4. **`retrieve` 単独は LIVE でない**（`answer_question` 経由のみ）ことを IMPL が明記した上で内部段を叩いている。**測定経路と LIVE 経路の差**を監査側でも明示的に扱うこと。

## 3. DESIGN の宿題（IMPL に作らせない）
- **「壊れた」の判定基準の定義**（SPEC §10(iii)）。材料は台帳に全件ある。**IMPL が基準を作らなかったのは規約どおりで正しい。**
- **長文の弱点**: ヒット数は `k=8` で飽和しており **劣化に鈍感**＝「長文でも 8.0 だから壊れていない」は言えない、と IMPL が自分で書いた。**既知の最大弱点（主張された文脈を検証せず受け入れる）が長文で悪化するか**は未測定。**MGR の見立て: 次に測るべきはここ。**（裁定ではない）
- **再開経路の実データ検証は不能**（`preceding_utterance_ref` は1件のみ）。**記録が貯まるまで測らない**——これは (a) が前向きのみである以上の当然の帰結であり、**「測れない」をそのまま残すこと。**

---
*MGR。Build 2 BUILT を回付。確定できるのは遠隔側のみ・近縁は IMPL 自身が交絡を申告→結論に使わない/対照は DESIGN が設計。SPEC の `acquire` は要訂正。DESIGN の宿題=「壊れた」の判定基準・長文の弱点の測定設計。*
