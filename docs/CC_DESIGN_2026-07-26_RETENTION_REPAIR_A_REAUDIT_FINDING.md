# 設計/監査 → MGR/Taka: (a) retention 補修 独立再監査 — **通過。記録の範囲が初めて広がった**

- `BUILD_ROLE: 参照`
- 宛: MGR / Taka / 写: IMPL / 発: 設計/監査(CC-α) / 2026-07-26 / TYPE=FINDING
- 対象: `CC_IMPL_2026-07-26_RETENTION_REPAIR_A_BUILT.md`

## 0. 結論
**【監査:CC-α】通過。** LIVE 台帳で実記録を確認し、非回帰も私が走らせた。**IMPL の「言えること／言えないこと」の切り分けも正確である。**

## 1. 私が直接確認したこと【監査:CC-α】
| 項目 | 確認結果 |
|---|---|
| **LIVE 台帳の実記録** | **`UTT-0739` → `preceding_utterance_ref=UTT-0738` / `ts_source=CALLER` / `ts=2026-07-26T12:00:47Z`** ✓ |
| **実時刻であること** | **従来のハードコード既定 `2026-07-11T08:00:00` ではない** ✓ |
| **前向きのみ（遡及なし）** | UTTERANCE 総数 **739** のうち充足は **1件**、**738件は空のまま**＝**正常** ✓ |
| **非回帰（私が実行）** | `test_preflight_gate` **13/13** / `test_return_loop` **12/12** ✓ |
| **推測で埋めていない** | 先行発話が無ければ `None`、ts の出所が不明なら `UNSPECIFIED`（`CALLER`/`DEFAULT` を推測しない） ✓ |

## 2. ★意味 — 今日立てた原則に対する最初の一手
本日の原則:
> **自分を検査する能力は、自分が記録した範囲を超えない。**
> **散文は記録ではない。記述である。**

**この2つは「今ある記録では測れない」ことの説明だった。** 今回はその**範囲そのものを広げた**。
- **【監査:CC-α】これまで 739件の発話は、1件として「自分の前に何があったか」を知らなかった**（`preceding_utterance_ref` 0/459・`ts` はユニーク98/730）。
- **`UTT-0739` が、台帳で初めて前を指している発話である。**

**ただし過大にしない**: **貯まったのは1件。** P2 の束縛先条件が実データで効くかは**まだ何も示していない**。IMPL の断り書き（「記録が入る」と「P2 が効く」を混ぜない）は正確であり、**私も同じ扱いをする。**

## 3. IMPL の報告作法について【監査:CC-α】
- **「言える／言えない」を明示的に分けた。**「前向きのみ・現時点で 0/738 が正常」と**自分の成果の小ささを先に書いた。**
- **禁止事項（遡及書き換え・推測補完・P2 への配線）を全て守り、守ったことを実証した**（既存1160行の逐語比較で差分ゼロ）。
- **commit 規約の変更を受領し、自分では commit していない。**

## 4. 次（別スライス・今日はやらない）
1. **記録が貯まってから** P2 の束縛先条件を**実 context** で測り直す。**今日は1件しかないので測らない。**
2. Build 2（縦串・`context` 配線）は従来どおり別。
3. **`preflight_gate` の根の修正**（地位の照会を存在ゲートから分離）は、次にこの周辺を触る時に先に入れる（`..._PRECEDENCE_THRESHOLD_ADJUDICATION.md` §3b）。

## 5. commit 要求【CC-α → MGR】
触った repo: **`ds` / `twoder` / `egl`**。**本 FINDING を含む未 commit の DESIGN 文書も working tree にある。**
**新規 structure script `s_retention_repair_a.py` は commit 同梱必須**（meta self-heal hook）。

---
*DESIGN CC-α。(a) は通過。LIVE 台帳で UTT-0739 が preceding_utterance_ref と実時刻を持つことを確認。非回帰も実行。★これは今日立てた原則「記録した範囲を超えない」に対する最初の一手であり、範囲そのものを1件広げた。ただし貯まったのは1件で、P2 が効くことは何も示していない。*
