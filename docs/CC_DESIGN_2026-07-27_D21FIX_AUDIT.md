# 設計/監査 → MGR（写: Taka / IMPL）: **D-21 修正 監査 — 通過。ただし私が「追記のみの台帳の1行目を書き換えてよい」と許可したのは設計として弱い**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=FINDING
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_IMPL_2026-07-27_D21FIX_NORMALIZE_PATH_BUILT.md`

## 0. 判定
**通過。** 2表記が同じ id（`ART-845d63f15f`）を返し、`artifact_id_for` とも一致。`docs/` 以外は `ValueError`。既存14行は入れ直していない。列も行種も増えていない。`egl/docs/` 以外に書いていない。本番コード無変更。

---

## 1. ★私の設計が弱かった点（IMPL の申告が正しい）【設計:CC-α】
**IMPL は `_meta` の1文追加のために、追記のみの台帳の1行目を書き換えたと明記した。**
**それは私の SPEC §4-4 が許可した範囲である。** **∴ IMPL に落ち度は無い。**

**しかし設計として弱い:**
> **「追記のみ」と宣言した台帳に、書き換えてよい行を1つ作った。**
- **`_ensure()` は「既に在れば触らない」設計であり、定数を直しても反映されない。** **IMPL がそれを見つけて申告したのは正しい。**
- **∴ ヘッダを更新する必要が将来また出れば、また例外を切ることになる。**
- **【設計:CC-α】正しい形は「`_meta` も追記行にする」**（`{"kind":"META", ...}` を末尾に足し、最新を採る）。**ただし今は直さない。**
  **理由**: **本台帳は暫定であり、退役条件が既に書かれている。** **例外は1回で、その内容も記録に残っている。** **v1.9（膨らませない）に照らして、直す価値より変更のコストが大きい。**
  **★「弱いと分かった上で残す」ことを記録する。** **忘れて再発するのを防ぐ。**

## 2. ★試験行がまた運用台帳に入った（原因は私の SPEC）【監査:CC-α】
```
現在の未処理に残存: docs/X_D21FIX_A.md ×2
```
- **D-21 の監査で私は「次から試験は一時ファイルで行うこと」と書いた。**
- **しかし `D21FIX` の SPEC 受入①には「実行して両方貼る」としか書かず、一時台帳を使えと書かなかった。**
- **∴ 2回目の混入は、私の SPEC の欠落である。** **IMPL は SPEC どおりに実行した。**
- **★対処**: **入れ直さない・消さない**（追記のみ）。**`X_` 接頭辞で試験由来と読める。**
- **★次から**: **台帳を触る SPEC の受入には「試験は `CC_REGISTER_TEST.jsonl` 等の一時ファイルで行う」を必ず書く。**

## 3. `counts()` の母数が比較可能になった
```
counts() = {'doc_rows': 21, 'files_since_start': 22}
```
**∴ 母数が揃い、差が意味を持つようになった**（D-21 監査 §2 の指摘が解消）。**差 1 は「DOC 行の無いファイルが在る」という正しい信号である。**

## 4. ★別件申告への設計判断（IMPL は決めないのが正しい）【設計:CC-α】
```
状況表: MGR 未応答: ERR No module named 'cc_register'
```
**`cc_register.py` は `egl/docs/` に在る**（実装源どおり）。**呼ぶ側が `sys.path` にそのディレクトリを足す必要がある。**

**判断: 呼び出し側が解決する。** 理由:
1. **モジュールの配置は D-21 の実装源で決めており、変えると台帳と道具が離れる。**
2. **`sys.path` を `cc_register.py` 自身に書かせるのは、自分の居場所を自分で申告させる形であり、移設時に嘘になる。**
3. **∴ 呼び出し側（MGR の状況表・私・IMPL）が `sys.path.insert(0, "/home/takasan/egl/docs")` を行う。** **私も IMPL もそうしている。**

**★ただし、この事実がどこにも書かれていない。** **口伝になっている。**
**∴ 対処（1行・列ではない）**: **`cc_register.py` の docstring 冒頭に「利用側は `sys.path` に `egl/docs` を足すこと」と書く。** **これは膨張ではなく、口伝の解消である。**

---

## 5. 残（消さない）
| 件 | 状態 |
|---|---|
| `_meta` を追記行にする | **弱いと分かった上で残す**（§1） |
| 試験行の混入 ×2 | **残す。次から SPEC 側で防ぐ**（§2） |
| `sys.path` の口伝 | **docstring に1行**（§4・未着手） |
| **worker が4状態を分けられるか** | **未決。** 私は「分けられない」に賭けている。**held-out 未使用** |
| 受入オラクル | **未開封**（`77af566…`）。**`ids.resolve()` 未実行** |

---
*CC-α D-21 修正 監査。通過（2表記が同じ `ART-845d63f15f`・`artifact_id_for` と一致・`docs/` 以外は ValueError・既存14行は入れ直さず・列も行種も増やさず・本番無変更）。★私の設計が弱かった点=「追記のみ」と宣言した台帳に書き換えてよい行を1つ作った（`_ensure()` は既存なら触らないので定数変更が効かないことを IMPL が見つけて申告したのは正しい）。正しい形は `_meta` も追記行にすることだが、暫定台帳で退役条件も書かれており例外は1回で記録も残っているので**弱いと分かった上で残す**（v1.9・膨らませない）。★試験行がまた運用台帳に入ったのは私の SPEC の欠落（D-21 監査で「次から一時ファイルで」と書きながら D21FIX の受入に書かなかった）——入れ直さず残し、次から台帳を触る SPEC の受入に一時ファイル指定を必ず書く。`counts()` は母数が揃い差が意味を持つようになった（21/22 の差1は正しい信号）。★`No module named 'cc_register'` の設計判断=呼び出し側が `sys.path` を足す（配置を変えると台帳と道具が離れる／自分の居場所を自分で申告させると移設時に嘘になる）。ただし口伝になっているので docstring に1行書く（膨張でなく口伝の解消）。*
