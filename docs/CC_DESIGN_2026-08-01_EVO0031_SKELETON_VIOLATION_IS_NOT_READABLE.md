# 【調査回答 / `EVO-0031`】**★「どこが」は読めない — ★★作られてすらいない**（★同型8件目）

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-08-01 22:1x / TYPE=FINDING
- **開発者規律 確認済（版: v1.1 / 2026-08-01）**
- **★裁定の在り処**: `ITEM-2DER-EVO-0031` の `status_note`（★`.md` 無し・逐語「依頼=3値で1つ: SKELETON_VIOLATION が骨格の『どこが』違反かを front door から読めるか」）
- **★コードを1行も変えていない** ／ **★front door へ投入していない**

---

# 1. ★★3値の答え — **★「無い」**

```
★★★`verify_skeleton_preserved` は ★★★★`True` か `False` しか返さない（`generate_via_runner.py:69-80` 逐語・全文）:
   def verify_skeleton_preserved(skeleton, artifact):
       if not isinstance(artifact, str): ★return False
       pos = 0
       for seg in _skeleton_fixed_segments(skeleton):
           idx = artifact.find(seg, pos)
           if idx < 0: ★return False        ← ★★どの seg で落ちたかを ★捨てている
           pos = idx + len(seg)
       return True
★★★★∴ ★★「どこが」は ★★★front door に出ていないのではない。★★★★関数の中で ★作られてすらいない。
   ★★★★★★`EVO-0031` の①（★捨てている）とも ★違う形である: ★★あちらは ★作って捨てていた。
      ★★★★★★★★こちらは ★★★最初から ★作っていない。
★★実測（★front door・全欄）: `test_result` ＝ `['status','ok','reason','artifact_sha256','runner_exit','runner_stdout_tail']`
   ★骨格の差を示す欄 ＝ ★★0件 ／ ★`worker_run_ref` ＝ ★null
```

## 1-1. ★★∴ 同型8件目として書く（★裁定の逐語どおり）
```
★「★理由が読めない」の ★★8件目。★★ただし ★内訳が3通りに分かれてきた:
   ★A型（①〜⑥）: ★片側だけ在る（★読めるが書けない／書けるが読めない）
   ★B型（⑦）  : ★両側 在るのに ★渡す所で捨てている（★`webui.py:327` の4欄）
   ★★C型（⑧ 本件）: ★★★そもそも ★作っていない（★`True`/`False` しか無い）
★★★★★★C型は ★★最も直しにくい: ★A/B は ★繋げば出るが、★★C は ★★中で作る所から要る。
```

---

# 2. ★★何が違反されたかは、★★別の道から★推定できる（★★ただし★確定していない）

```
★`_skeleton_fixed_segments`（`:55-66` 逐語）は ★骨格を ★`<<<FILL` 行で割り、★残りを ★固定区間とする。
★★★★私が S-3 の契約に書いた skeleton には ★★`<<<FILL` が ★★1つも無い【★★私の実測・逐語】
   ∴ ★★★骨格★全文が ★★1つの固定区間になる。
★★★★★∴ ★worker は ★★docstring も `raise NotImplementedError` も ★★1文字も変えずに残したうえで
   ★実装を足さねば ★通らない。★★★`raise NotImplementedError` を消した時点で ★違反になる。
★★★★★★★∴ ★★★「実装せよ」と「骨格を1文字も変えるな」が ★★両立しない可能性が在る。
   ★★★★★★★★これは ★私の契約の書き方の問題である（★`<<<FILL` を置かなかった）。★★★本日7件目の私の誤り。
★★★★★★★★★★ただし ★★私は ★artifact を ★読んでいない ∴ ★★★「どこが違ったか」は ★★確定していない【未確認】。
   ★★★推定であって ★実測ではない。★★そう書く。
```

---

# 3. ★★∴ 直す先が2つに分かれる（★私は選ばない）

| | 直す先 | 中身 |
|---|---|---|
| **(A)** | **★我々の契約** | ★skeleton に ★`<<<FILL …>>>` を置き、★★埋めてよい場所を明示する（★検査は緩めない） |
| (B) | ★機構 | ★`verify_skeleton_preserved` が ★落ちた seg を返すようにする（★★C型を潰す） |

```
★★MGR の裁定は「★読めれば MGR が依頼文の骨格を直す（★我々の側を合わせる。★検査は緩めない）」である。
   ★★★★★読めなかった ∴ ★★裁定の前提が変わる。★★私は ★どちらとも書かない。
★★★★ただし ★1つ言える: ★★(A) だけを先にやると ★★次に骨格違反が起きた時も ★また読めない。
   ★★★★★★(B) は ★★C型の1件目を潰す作業である ∴ ★★他の C型にも効く形になりうる【★未確認】。
```

---

# 4. ★★私の誤り（★本日7件目・★先に書く）

```
★★私は S-3 の契約の skeleton に ★★`<<<FILL` を ★1つも置かなかった。
★★★`_skeleton_fixed_segments` は ★今日 私が読んだファイルの ★14行 上に在る。★★読んでいなかった。
★★★★★∴ ★★2DER が ★骨格違反で止まったのは ★★★私が ★埋めてよい場所を ★示さなかったからである可能性が在る。
   ★★★★★★★（★★確定していない。★§2 のとおり ★artifact を読んでいない）
★★★★★★★★∴ ★★★★本日 ★2件目の「★2DER の欠陥ではなく ★我々の依頼文の欠陥」候補である。
```

---

# 5. ★やっていないこと
```
★コードを1行も変えていない ／ ★契約を書き直していない ／ ★front door へ投入していない
★★★artifact（★worker が書いた `impl.py` の中身）を ★読んでいない【★未確認】
   ★理由: ★front door から読む口が ★無い（★`worker_run_ref` が null）。
   ★★★★読むには ★sandbox を直接 見るしかないが、★★それは ★2DER の外から中を覗く形である ∴ ★やらない。
★★commit していない ／ ★台帳を直読していない ／ ★`EVO-0033`（計器の偽陽性）に触れていない（★後回し・MGR 担当）
```

---
**決めたこと**: **①3値の答えは「無い」——`verify_skeleton_preserved` は `True`/`False` しか返さず、どの固定区間で落ちたかを関数の中で捨てている。front door に出ていないのではなく、作られてすらいない ②∴ 同型8件目だが内訳は3通りに分かれる——A型（片側だけ在る・①〜⑥）、B型（両側 在るのに渡す所で捨てる・⑦）、C型（そもそも作っていない・本件）。C型が最も直しにくく、A/B は繋げば出るが C は中で作る所から要る ③何が違反されたかは推定できる——`_skeleton_fixed_segments` は骨格を `<<<FILL` 行で割るが、私が書いた skeleton には `<<<FILL` が1つも無いので骨格全文が1つの固定区間になる。∴ worker は `raise NotImplementedError` を1文字も変えずに残したまま実装を足さねば通らず、「実装せよ」と「骨格を変えるな」が両立しない可能性が在る。ただし artifact を読んでいないので確定していない（推定であって実測ではない）④∴ 直す先が2つに分かれる——(A) 我々の契約に `<<<FILL` を置く／(B) 機構が落ちた seg を返すようにする。MGR の裁定は「読めれば我々の側を合わせる」だったが読めなかったので前提が変わる。私はどちらとも書かないが、(A) だけ先にやると次also読めず、(B) は C型の1件目を潰す作業になりうる ⑤私の誤り（本日7件目）——`_skeleton_fixed_segments` は今日 私が読んだファイルの14行 上に在るのに読んでいなかった。2DER が骨格違反で止まったのは私が埋めてよい場所を示さなかったからである可能性が在る（確定していない）⑥artifact は読んでいない。front door から読む口が無く、sandbox を直接 見るのは 2DER の外から中を覗く形なのでやらない。**
