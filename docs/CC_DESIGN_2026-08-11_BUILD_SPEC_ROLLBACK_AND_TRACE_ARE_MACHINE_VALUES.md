開発者規律 確認済(v1.0)

# 【BUILD SPEC】★rollback と ★trace_id を ★機械の値にする（★権限の規則④の 前提材料）

宛: IMPL ／ 発: DESIGN ／ 2026-08-11 07:45 ／ 台帳: `ITEM-2DER-EVO-0062`
出所: MGR 裁定 07:18（`ETR-d94dc2407a5c`）=「★材料を 先に 作る=★2つだけ (a) rollback を 機械の値にする ／ (b) evidence の `trace_id` を 実在の ETR に 揃える」

---

## 0. ★★私の訂正（★先に書く・★★MGR の裁定の前提が 1つ ずれている）

```
★★私が 07:15 に 台帳へ書いた「★rollback の材料 = ★0件」は ★★言い過ぎだった。
★★理由 = ★★`revert_scope` を ★『CHG に 保存される欄』だと 誤って 扱った。
★★実物 = ★`twoder/revert_scope.py:1` = ★★`revert_scope(changes, key)` = ★★導出する関数
        = ★★保存する欄ではない ∴ ★CHG に 欄が 無いのは ★★設計どおりで あって 欠落ではない。

★★∴ ★正しい言い方 = ★★『材料は 在る。★★★呼び手が 0件』。
★★∴ ★MGR 裁定(a) の「★revert_scope を ★CHG に 記録する」は ★★採らない
   ―― ★採ると ★★導出できる値を 保存して 二重に持つ＝★★食い違う口が もう1つ 増える
      （★★本日 実害が 出た EVO-0056『2つの口が 食い違う』と ★同じ形）。
★★代わりに = ★★★読む時に 導出して 載せる（★下の §3-A）。
```

## 1. ★★実測（★front door から・★2026-08-11 07:40・★母数を 先に書く）

```
★母数 = ★★CHG-0150〜CHG-0177 の ★28件（★`GET /api/resolve?id=CHG-…` で 引けた分）

★★【(a) rollback の材料 = ★★在る】
   ★`before_commit` 値あり        = ★27/28
   ★`after_commit`  値あり        = ★★28/28
   ★`affected_artifact_ids` 値あり = ★★28/28
   ★★★`revert_scope` が `complete=True` を 出せる行 = ★★★27/28
   ―― ★★∴ ★『rollback = verified』は ★★27件について ★今日 すでに 機械で 言える。
      ★言えていないのは ★★呼ぶ所が どこにも 無いから（★呼び手 = ★★0件・★全 repo を 走査）。

★★【(b) evidence の trace_id = ★★半分どころか ★★5/28】
   ★`trace_id` 値あり              = ★28/28（★★欄は 全部 埋まっている）
   ★★形が `ETR-<12桁の16進>`       = ★★★5/28
   ★★その5件が ★実在した（★resolve が True）= ★★5/5（★1件ずつ 引いて 確認）
   ★★残り 23件 = ★★手書きの文字列（★逐語例 = `ETR-blocker-ids-2026-08-09`）
      → ★★`resolve` = ★★False = ★★★ETR として 引けない
      → ★★同じ手書き文字列を ★2件が 共有している例も 在る
```

**★★∴ ★★欄が 埋まっていることを ★『証拠が 在る』と 読んではいけない実例が ★23/28。**
**―― ★[[absence-reads-as-compliance]] の 裏返し = ★★★存在が 遵守に 見える。**

## 2. ★★作る物（★2つ・★どちらも 新しい部品を 作らない）

### ★A. ★rollback を 読める形にする（★★保存しない・★導出して 載せる）

```
★場所   : ★`twoder/webui.py` の ★`GET /api/resolve` の ★★CHG を返す分岐
★やる事 : ★★返す `record` の 隣に ★`revert_scope` を ★1つ 足す
          ★★値 = ★`twoder/revert_scope.py` の ★`revert_scope(changes, key)` を
                ★★`key = その CHG の trace_id` で 呼んだ 戻り値 そのまま
                （★`{change_ids, before_commit, after_commit, artifact_ids, complete}`）
★★口   : ★★0増（★★既存の `/api/resolve` に 相乗り）
★★保存 : ★★0（★★台帳に 書き足さない = ★append-only を 汚さない）
★★関数 : ★★0増（★★既存の `revert_scope` を ★そのまま 呼ぶ・★★1行も 直さない）
```

### ★B. ★trace_id を fail-closed にする（★★新しい行だけ・★過去は 直さない）

```
★場所   : ★`twoder/artifact_registry.py:152` ★`record_change(...)`
★やる事 : ★★`trace_id` が ★★実在の ETR でない時に ★★★書かずに 止める
          ★実在の判定 = ★★`ds` の etrace から 引けること（★★形の正規表現だけで 判定しない
                       ―― ★形が 合っていても 実在しない文字列は 作れる）
★★過去の23件 : ★★★直さない（★append-only ／ ★★書き換えは 規律違反）
              → ★★`evidence` の 値は ★★`UNVERIFIED` として ★数える（★★0にしない・★消さない）
★★口 0増 ／ ★★台帳 0増 ／ ★★新語は ★1つだけ = ★`UNVERIFIED`
```

## 3. ★★受入（★数で書く・★★0件なら 0件と書く）

```
★① ★`GET /api/resolve?id=CHG-0176` の 応答に ★`revert_scope.complete` が ★在る（★1件 実物）
★② ★★28件を 引いて ★`complete=True` が ★★27件（★★今日の実測と 一致すること
      ―― ★★数が 変わったら ★どちらかが 間違い ∴ ★止めて 報告）
★③ ★★実在しない trace_id で ★`record_change` を 呼ぶと ★★書かれない（★1件 実物で示す）
★④ ★★書かれなかった時に ★★理由が 残る（★★黙って 落とさない）
★⑤ ★★過去23件が ★`UNVERIFIED` として ★★数えられる（★★消えない・★0にならない）
★⑥ ★★新しく足した関数で ★呼び手が 0件の物 = ★★0件（★★MGR が 本日 規律にした条件）
★⑦ ★★新台帳0 ／ ★★front door の口 0増 ／ ★★保存する欄 0増
```

## 4. ★注意（★実装の形）

```
★★本件は ★★契約封印（worker）を 使わない。
   ★理由 = ★★本番 repo への 配線であり ★worker には 書かせられない（★規律）
   → ★★Claude(IMPL) が 書く ／ ★★行数を 必ず 報告する ／ ★★2DER の実績に 数えない。

★★[[CC_DESIGN_2026-08-09_TAKA_PUT_IT_IN_THE_MACHINE_OR_DELETE_IT]]:
   ★A（★条件を満たせば 必ず走る）に できないなら ★足さない。
   → ★★A に なっている = ★`/api/resolve` は ★★毎回 通る道 ／ ★`record_change` は ★★唯一の書き口。

★★これが 済むまで ★★権限の規則(④)を 書かない（★MGR 裁定・★私も 同意）。
   ★理由 = ★★層1 の条件『rollback = verified』が ★★機械で 言えないまま だと
        ★★『不明 ∴ 通す』（★素通り）か ★★『不明 ∴ 全部 層3』（★★今夜の 全閉めの 再来）に なる。
```
