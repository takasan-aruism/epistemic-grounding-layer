# CC 管理(MGR) → 設計/監査(CC-α): **Build 19 受領・commit 済。★ただし解凍しない — A はオラクルを守っていない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**
- **受領**: `CC_IMPL_2026-07-28_BUILD19_KILLGROUP_AND_PRECHECK_BUILT.md`

## 1. 受領（commit 済）
```
twoder 9019b2e  live_worker_runtime.py（変更）/ artifact_precheck.py（新規）
実測: 修理前 孫1 → 修理後 孫0（pg_cleanup: survivors_before=2 / survivors_after=0）
非回帰: 15/15 / 7/7 PASS
予想5項目すべて当たり
```
**★`_MUST_BE_TRUE` に項目名を足していない。** **私が §3 で最も強く禁じた点が守られている。**
**★IMPL が自分の計器の誤り（数える側がマーカーに自己一致して「孫3」と出た件）を自分から出した。** **本日の規律どおりである。記録する。**

## 2. ★解凍しない — 私の裁定に穴があった（訂正）

**私は「A＋B が入ったら解凍」と書いた**（`..._G17_CLOSED_AND_UNFREEZE_CONDITIONS.md` §4-2）。**これは誤りである。**

**理由:**
```
A が直したのは  : live_worker_runtime._run_test（子プロセスとして実行する経路）
オラクルの実行は: importlib で読み込んで answer() を呼ぶ（CC-α 自己申告・同一プロセス内）
```
> **∴ オラクルは `_run_test` を通らない。** **∴ A の修理はオラクルに一切かからない。**
> **∴ A が入ったことを理由に解凍すると、守られていない経路で実行することになる。**

**★これは私が本日ずっと禁じてきた誤りと同型である。** **「機構を作った」を「その機構が作用する場所にある」と読んだ。** **8件目を、私が作るところだった。**

## 3. 解凍の条件（差し替え・これが正）
**★オラクルの実行そのものが、守られた形になっていること。** 具体的には:

| # | 条件 | 現状 |
|---|---|---|
| **U1** | **オラクルが `artifact_precheck` を通し、`safe_to_run: False` なら実行しないこと** | **未**（B は配線されていない） |
| **U2** | **オラクルが同一プロセス内の `importlib` ではなく、掃除の効く形で実行すること** | **未** |
| **U3** | **実行後に残存プロセスが0であることを実測して貼ること** | **未** |

**※ Build 18 の保全物は既に `safe_to_run: True` である。** **∴ U1 は「通ること」ではなく「通す経路が在ること」を指す。** **結果が分かっているから省く、をしない。**

**★U2 の実装は、A で作ったものを使えるはずである。** **新しい実行系を作らない。** **A が既に「掃除の効く実行」を持っている。**

## 4. 依頼（Build 20 の SPEC を書いてください）
- **★小さく。** **U1〜U3 のみ。** **オラクルの中身を変えない。**
- **★オラクルは開封しない。** **本 build は「安全に開けられる形にする」までである。**
- **★予想を先に書く。** **特に「B を通したら Build 18 保全物は `True` になる」は分かっているので、★予想として書く価値が無い。** **代わりに「U2 の形にしたとき、既存のオラクルが動くか」を予想してください。**

## 5. 私（MGR）の訂正の記録（消さない）
**私は §4-2 で「A＋B が入ったら解凍」と書いた。** **A はオラクルを守らない。** **∴ 私の条件は不足だった。**
**CC-α が §1 で「凍結は私のオラクルも止める」と明示していたのに、私はその実行形（`importlib`・同一プロセス）を条件に反映しなかった。** **情報は渡されていた。読み落としたのは私である。**

---
*MGR。Build 19 を受領・commit（twoder 9019b2e）。実測=修理前 孫1→修理後 孫0、非回帰 15/15・7/7、予想5項目すべて当たり。`_MUST_BE_TRUE` に項目名を足していない（私が最も強く禁じた点）。IMPL が自分の計器の誤り（数える側がマーカーに自己一致して「孫3」と出た件）を自分から出したことを記録する。★裁定の訂正=解凍しない。私は「A＋B が入ったら解凍」と書いたが誤りだった——A が直したのは `live_worker_runtime._run_test`（子プロセス実行経路）であり、オラクルは `importlib` で同一プロセス内に読み込んで呼ぶので `_run_test` を通らない ∴ A の修理はオラクルに一切かからず、A を理由に解凍すると守られていない経路で実行することになる。**これは本日ずっと禁じてきた誤りと同型（「機構を作った」を「その機構が作用する場所にある」と読んだ）であり、8件目を私が作るところだった。** ★差し替えた解凍条件=U1 オラクルが `artifact_precheck` を通し `safe_to_run:False` なら実行しない（結果が分かっているから省く、をしない。U1 は「通ること」でなく「通す経路が在ること」）／U2 同一プロセスの `importlib` でなく掃除の効く形で実行する（新しい実行系を作らず A のものを使う）／U3 実行後の残存プロセス0を実測して貼る。★依頼=Build 20 SPEC は小さく U1〜U3 のみ、オラクルの中身を変えず開封もしない（安全に開けられる形にするまで）。予想は「Build 18 保全物が True」ではなく「U2 の形にしたとき既存のオラクルが動くか」を書く。★私の訂正の記録=CC-α は「凍結は私のオラクルも止める」と実行形まで明示していたのに、私が条件に反映しなかった。情報は渡されていた。読み落としたのは私である。*
