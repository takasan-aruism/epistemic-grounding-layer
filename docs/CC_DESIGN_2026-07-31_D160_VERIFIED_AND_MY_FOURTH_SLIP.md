# 【監査】(a)(b)(c) を取り直した — **★3点とも一致。★ただし私の測り方に誤りが1つ（本日4回目）**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 09:5x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **受領**: `CC_MGR_2026-07-31_D160_A_REGRESSION_AND_A_SECOND_HYPOTHESIS.md`
- **★新しい名前で置いた**（`D-158` §2-2）

---

## 1. ★私の誤りを先に書く（★本日4回目）

```
★私は前の監査で「★packet に measured_state が載ったか: False」と測って書いた。
★★誤りである。★measured_state は ★provenance の中に★21件 入っていた。
★★私は knowledge_packet の★最上位キーだけを見て「無い」と読んだ。
★★本日4回目の同型である:
   ①docker ログに UTC を渡して0件 ②検査語のバッククォート漏れで受入を「×」
   ③英語の計画を日本語語で数えて全部0 ④★入れ子を見ずに最上位だけで「無い」
★★4回とも「機械的に照合したつもりで、★照合する側が間違っていた」。★4回とも自分で見つけた。
★★★私の SPEC には「キーを列挙して目視する」と★自分で書いてある。★自分が守れていなかった。
```
> **★結論への影響**: **★無い。** **★値は prompt に入っていた**（§2-c）。**★私の「値は入った」という判定は結果として正しかったが、★根拠が誤っていた。★根拠を差し替える。**

---

## 2. ★(a)(b)(c) の再検証

| | 依頼 | ★私の実測 | |
|---|---|---|---|
| **(a)** | 受入3 の反転 | `steps` に **「Implement process RSS collection using subprocess to run `ps`」「…`nvidia-smi`」「Implement Docker container status collection using subprocess」** ＝ **★取得を実装対象にしている** | **★一致** |
| **(b)** | 補の数え方 | **★私が観測値から独立にトークンを作り直した（42語・英語＋数値・打ち切り無し）→ ★本文に出たのは `container`/`memory`/`used` の3語のみ。★具体値は 0件** | **★一致** |
| **(c)** | 値が prompt に入っていたこと | **★コードで確認**: `build_planner.py:99-134` の `_observation_facts` が **2ブロック**を出す ①`ALREADY ACQUIRED FOR THIS TASK`（**★各行に取得コマンドが入る**）②`VALUES ALREADY OBSERVED`（**★値そのもの**）。**★値は `provenance["measured_state"]`（21件）から取られている** | **★一致** |

---

## 3. ★H1 と H2 について（★私の見立て。★断定しない）

**★H1（取得コマンドを列挙している）を支持する材料（★私が新たに確かめた）**
```
★観測ブロックの1つ目は「acquired by running ["ps","-eo",…] on … (result: OBSERVED)」の形である
∴ ★planner は「コマンドの一覧」を毎回 読んでいる。★H1 の前提は★実在する。
```
**★H2（依頼と無関係な観測まで見せている）を支持する材料**
```
★今回の依頼は「メモリを多く使っているプロセス」だけ。★しかし見せた観測は★4種すべて（GPU/コンテナ/プロセス/ポート）。
★★MGR の指摘のとおり、★計画も ps / nvidia-smi / docker の★3つを並べた。
★★`G-06`（毎回 全件要求）は既に登記済 ∴ ★H2 の前提も★実在する。
```
**★どちらとも決められない理由（★正直に）**
```
★前回（コンテナ）も★同じ形でコマンドを見せており、★それでも受入3 は ○ だった（★MGR の指摘）。
★★∴ 「コマンドを見せると必ず重複計画する」は★成り立たない。
★★かつ ★毎回 依頼文が変わる ∴ ★「変えたのは1つだけ」の対照が★構造的に作れない（★MGR §5 のとおり）。
★★∴ ★1手で決着はしない。★H1 を先に潰すという MGR の順番に同意する。
```

---

## 4. ★次の1件（★MGR `D-160` §4 に同意）
> **★観測ブロックから取得コマンドの列挙を外し、★値だけを見せる。**
**★私も「効く」と予告しない。** **★止まれば H1、★止まらなければ H1 が落ちる。★どちらでも結果である。**

---
**決めたこと**: **①(a)(b)(c) を独立に取り直して3点とも一致——`steps` は取得を実装対象にしており、観測値の具体トークンは本文に0件、値は `_observation_facts` の `VALUES ALREADY OBSERVED` ブロックで prompt に入っていた ②私の誤りを1つ訂正——`measured_state` は provenance の中に21件 入っており、最上位キーだけを見て「無い」と読んだのは本日4回目の同型（自分の SPEC に「キーを列挙して目視する」と書きながら守れていなかった）。結論への影響は無いが根拠を差し替える ③H1・H2 はどちらの前提も実在するが、前回は同じ形でコマンドを見せても受入3 が立っており、かつ対照が構造的に作れないため1手で決着しない——H1 を先に潰す順番に同意する。**
