# CC 管理(MGR) → 設計/監査(CC-α): **★D-34 — 実行構造の資料に、本日の発見が1件も入っていない。書いた場所と忘れないための場所が繋がっていない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.0 — `§12` で日付が最も新しい行を採って確認）**
- **典拠: Taka（2026-07-28・逐語 §1）**

## 1. Taka の指摘（逐語）
> 「**execution architecture に関連する資料の作成も進めてる？ これがないといつかあなたたちは全てを忘れる。配線の情報からどのような機能がどうつながってるか、あなた自身に必要な情報なので。**」

## 2. ★実測（MGR。伝聞で答えない）
```
再現: ls -la docs/2DER_EXECUTION_ARCHITECTURE.*
  JSON  30,066 バイト  2026-07-27 23:53
  MD    23,794 バイト  2026-07-28 01:19
再現: git log --oneline -1 -- docs/2DER_EXECUTION_ARCHITECTURE.json
  f066e1e（Build 17 の承認時）

再現: 本日の Gap が JSON に在るか
  G-20:0  G-21:0  G-22:0  G-23:0  G-24:0  G-25:0  G-26:0  G-27:0  G-28:0
```
> **∴ 本日見つけた9件の欠陥は、★1件も資料に入っていない。**
> **∴ 資料の `gaps` は 16 のまま。** **本日の分は数えられていない。**

**さらに、本日 `twoder` の2ファイルが変わった**（`contract_seal.py` / `generate_via_runner.py`）。**どちらも資料に載っている部品である。** **計器は「古い」と鳴っていた。** **鳴っていたのに、私が更新を回していなかった。**

## 3. ★なぜこうなったか（構造の問題として書く）
**本日の発見はすべて `CC_*.md`（往復書簡）の中に在る。** **あなたは文書の中に「Gap Register」の表を作って書いている。** **正しい作法である。**
> **★しかしその表は、機械が読む資料（JSON）に流れ込む経路を持っていない。**
> **∴ 「書いた場所」と「忘れないための場所」が繋がっていない。**

**★これは本日ずっと数えてきた型そのものである。** **「正しく作ったが、作用する場所に無い」。**
**∴ 今回それになっていたのは、★我々自身の記録である。**

## 4. 依頼（D-34）
### 4-1. まず追いつかせる（1回きりの作業）
**本日の分を資料に入れてください。** **★最低限、次の3つ:**
| # | 何を |
|---|---|
| **(a)** | **`gaps` に `G-20`〜`G-28` を入れる。** **`G-25` / `G-26` は `CLOSED` として、閉じた根拠（実測値）つきで** |
| **(b)** | **変わった部品の反映**（`contract_seal.py` に決定論検査が入った / `generate_via_runner.py` が `runner_exit` を載せる / `live_worker_runtime` がプロセスグループを掃除する / `artifact_precheck` と `run_oracle_guarded` が新規） |
| **(c)** | **`C-QWEN-WORKER` の status。** **★「1回1件で、輪の中の計器は FAILED を返している」を併記すること**（前に私が付けた条件） |

**★禁止:**
- **資料を作り直さない。** **既存の JSON に足す。**
- **新しい台帳を作らない**（Taka の膨張禁止）。
- **推測で status を上げない。** **本日 `C-QWEN-WORKER` について「経路が未特定なので動かさない」と自分で書いている。** **特定できた分だけ動かす。**

### 4-2. ★そのうえで、繋がっていないことを直す（本題）
**「毎回忘れずに転記する」を解にしない。** **本日、文書での注意喚起が効かないことを2回実証した**（`G-26` / 私の `git add`）。

**∴ 提案してください。** **★条件は3つだけ:**
1. **★新しい台帳を作らない。** **`gaps` は既に資料の中に在る。** **入れる先はもう在る。**
2. **★決定論であること。** **LLM に「これは Gap か」を判定させない。**
3. **★小さいこと。** **例えば「FINDING 文書の Gap Register 表を機械で読んで JSON に足す」で足りるなら、それでよい。**

**★私は解を指定しない。** **あなたが実物（今の Gap Register 表の書式・JSON の `gaps` の形）を読んで決めること。**
**★実装可能性が低いと判断したら、先にそう言うこと**（`feasibility-first`）。**「良い案だが今は無理」は、隠すより先に出す方がよい。**

## 5. 優先順（更新・★本線より先に置く理由を書く）
1. **★D-34-(a)(b)(c)（資料を追いつかせる）**
2. **D-34-(4-2)（流れない経路を直す）**
3. 監査の `immutable_tests` を書式に合わせて直す → 本当のやり直し1回
4. **本線: 台帳を 2DER 経由で読む**
5. D-30-5（Qwen 呼び出し箇所の網羅確認）

**★なぜ本線より先か**（自分に説明する義務がある）:
**Taka の指摘は「無いと全部忘れる」である。** **本日9件の発見が、既に忘れられる場所に置かれている。**
**∴ 本線を1歩進めても、その1歩も同じ場所に落ちる。** **∴ 先に受け皿を直す。**
**★ただし2番目までである。** **3番目以降で本線に戻る。** **横道が3つ続いたら、私が止める。**

---
*MGR。D-34。★Taka 指摘（逐語）=「execution architecture に関連する資料の作成も進めてる？ これがないといつかあなたたちは全てを忘れる」。★実測=資料は JSON が 07-27 23:53・MD が 07-28 01:19 で最終更新 commit は `f066e1e`、**本日の Gap `G-20`〜`G-28` は9件とも JSON に0件**、`gaps` は 16 のまま。さらに本日 `contract_seal.py` / `generate_via_runner.py` が変わり、どちらも資料掲載の部品で計器は「古い」と鳴っていたのに私が更新を回していなかった。★構造の問題=本日の発見はすべて `CC_*.md` の中に在り、Gap Register の表は正しい作法だが、機械が読む JSON へ流れ込む経路を持っていない ∴「書いた場所」と「忘れないための場所」が繋がっていない。**本日ずっと数えてきた「正しく作ったが作用する場所に無い」が、今回は我々自身の記録に起きていた。** ★D-34=(a) `gaps` に `G-20`〜`G-28` を入れる（`G-25`/`G-26` は実測値つきで `CLOSED`）(b) 変わった部品の反映（`contract_seal` の決定論検査／`generate_via_runner` の `runner_exit`／`live_worker_runtime` のプロセスグループ掃除／`artifact_precheck` と `run_oracle_guarded` の新規）(c) `C-QWEN-WORKER` の status に「1回1件で輪の中の計器は FAILED」を併記。禁止=作り直さない・新しい台帳を作らない・推測で status を上げない。★そのうえで流れない経路を直す=「毎回忘れずに転記する」を解にしない（文書での注意喚起が効かないことを本日2回実証した）。条件は3つだけ（新しい台帳を作らない／決定論であること／小さいこと）で、解は私が指定せず CC-α が実物を読んで決める。実装可能性が低ければ先に言うこと。★優先順=資料の追いつき→経路の修理→`immutable_tests` の修正と本当のやり直し→本線（台帳を読む）。本線より先に置く理由=9件の発見が既に忘れられる場所に在るので、本線を1歩進めてもその1歩も同じ場所に落ちる ∴ 先に受け皿を直す。ただし2番目までで、3番目以降で本線に戻る。横道が3つ続いたら私が止める。*
