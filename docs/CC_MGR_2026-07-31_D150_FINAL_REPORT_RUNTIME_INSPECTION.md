# 【最終報告】Runtime Inspection 共通基盤 — 正典の4項目

- **宛: TAKA** / 写: 設計/監査 / IMPL / 発: MGR / 2026-07-31 / TYPE=STATUS
- **正典**: `TAKA_2026-07-31_RUNTIME_INSPECTION_COMMON_BASE_ORDER.md` ／ **運用方針 確認済（版: v2.8）**

---

## 1. ★今回 接続できた配線

```
Request → RRI → Task → PLAN     ★ここまで通った（★第1〜3回は Task も PLAN も無かった）
```
| | 証拠 |
|---|---|
| **Task が出来る** | `TASK-2DER-67FE6548`（★観測の経路で初めて） |
| **PLAN が動く** | `dispatched=true` ／ `CREATED → READY_FOR_IMPLEMENTATION` ／ `plan_source=QWEN_BUILD_PLANNER` ／ `attempts=2`（★同区間の `:8005` も2件で一致） |
| **入口から辿れる** | `trace_id` / `rri_request_id` / `rri_intent_id` が packet に載った |

**★直したのは2箇所（★別々の回に1箇所ずつ）。★どちらも `twoder/submit.py`。★`gpu`/`nvidia` は diff に0件。**

---

## 2. ★まだ切れている配線

```
Runtime → Observation → Ledger → DW → Response
```
| | |
|---|---|
| **Ledger** | **★First FAIL。**生出力（`blob://…`）に既存の口から到達できない |
| **DW** | 要約が無い |
| **Response** | 要約ではなく生の配列。GPU 使用率は欠落のまま |

---

## 3. ★次回 修正すべき箇所（★1件のみ）

> ### **★`Ledger` — 生出力に、既存の口から到達できるようにする。**

**★新しい台帳も新しい口も作らない。★既に記録が持っている参照を1つ通すだけ**（★今回と同じ形）。

---

## 4. ★Runtime Inspection を他の監視項目へ再利用できるか

> ### **★再利用できる。★ただし「共通基盤として成立した」とは書かない。**

```
★再利用できる（実証）: ★待ち受けポートの依頼が、★同じ入口・同じ _CATALOG を通り、
                        ★PLAN の中身も port の話になった。★我々のコードに GPU 固有語は0件
★成立とは書かない理由（★2つとも残っている）:
   (1) ★選別が効いていない（依頼内容に関わらず毎回 全項目を要求＝既存 `G-06`）
   (2) ★観測が task の下流に無い（★§5）
```

---

## 5. ★あなたにしか決められないこと（★裁定要求。★我々は先へ進まない）

> ### **★あなたが描いた線は、★実装には存在しない。**

```
★あなたの図: Request → RRI → Task → ★Runtime → Observation → Ledger → DW → Response
★実装の形  : submit ─┬→ RRI → 観測 → 応答          （★ここで完結している）
                     └→ Task → PLAN → GENERATE     （★物を作る道）
```

**★決定的な証拠（設計が front door から引いた PLAN の中身）:**
```
★2DER は、たった今 `ss -ltn` を実行して OBS-00971〜00974 を取った。
★その直後の計画: 「★`ss -tulnp` を実行する port_checker.py を新しく作れ」
★cites_source_ids: ★空（★自分が取った観測を1件も引用していない）
```
> **★下流は「観測を実行する道」ではない。★「観測する能力を作り直す道」である。**
> **★これは配線の欠落ではない。★2本の道が別の目的を持っている、ということである。**

**★決めてほしいこと（★二択）**
| | |
|---|---|
| **(1)** | **★実装を図に合わせる** — 観測を task の下流に置く（★経路の設計変更になる） |
| **(2)** | **★図を実装に合わせる** — 「観測は submit で完結する道」と認め、★8点の並びを書き直す |

**★我々は裁定しない**（運用方針 §7-1 の1＝目的・優先順位そのもの）。**★「図が間違っている」とも書かない。★実装が図と違う、だけを書く。**

---

## 6. ★我々が今回 守ったこと・自分から出した誤り

```
★GENERATE を押していない（★物を作る道であり、押しても8点は進まない）
★止まった所を直していない・迂回していない・再投入していない
★GPU 取得を触っていない（★使用率は取得不能のまま。★正典どおり）
★A（同じ文）を省かなかった ∴「同じ文では届かない」が読みではなく★観測になった（★予告4点 的中）
★自己申告2件: ★IMPL が確認せず PID を kill（実サーバ無傷）／
              ★設計が docker ログに UTC を渡して「0件」を得た（★疑って取り直し2件＝逆の結論を回避）
★言えないことも書いた: ★`:8005` の2件が planner のものだと★名指しはできない（記録に要求 id が無い）
```

---
**決めたこと**: **①接続できたのは Request→RRI→Task→PLAN（修正2箇所・GPU 固有語0件） ②まだ切れているのは Runtime→Observation→Ledger→DW→Response で First FAIL は Ledger ③次回の1件は生出力への到達 ④再利用はできる（ポートで実証）が「共通基盤として成立」とは書かない ⑤★観測が task の下流に無いのは配線の欠落ではなく2本の道の目的が違うため、実装を図に合わせるか図を実装に合わせるかは Taka の裁定を待つ。**
