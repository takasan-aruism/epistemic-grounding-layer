# UI に何を求めるか、各担当が書き出す1枚

- **種別**: ★**規則ではない。収集シートである。**（運転規則は2本だけ＝`2DER_DEVELOPER_DISCIPLINE_v1.0.md` / `TAKA_2026-07-31_SELF_EVOLUTION_ARCH_v0_3.md`。★3本目を作らない。）
- **これは何か**: 開発者規律 **8番の逐語**「★進捗は台帳へ書く。.md は『台帳へ書く口が無くて書けなかった』時だけ。その .md には『どの口が無いのか』を1行書く」——**その .md を1枚にまとめた場所**。散らばると誰も読まない。
- **書く人**: **全担当の Claude**（Ledger / 設計・監査 / ESDE / RRI / DW / Inference Control / その他）
- **読む人・直す人**: 台帳・UI 担当（MGR）。**台帳すなわち UI。**
- **UI**: http://100.107.6.119:8770/control

> ## ★★このファイルは 台帳に 載せない（Taka 裁定 2026-08-26・逐語）
>
> 「**台帳に載せる正式なルートがないことが問題の本質で、そこを直すためにはUIを直さないといけない。
> どうせ直すなら今まで使われた無茶な使用方法をナレッジとして用意してから直した方がいいよね？
> なので今は外においておいて。今後ページからしか登録ー変更できないようにする。
> どうせチートしようとするのは目に見えているので、そこを権限で縛っていくというのが正規のプロセス。
> 文句がでたらUI直せばいい。というわけで、今は台帳の外に置く**」
>
> ★∴ **これを ARTIFACT 登記しないこと。** 「台帳に載っていない＝漏れ」ではなく **意図的に外**である。
> ★理由: 正式なルート（UI）が**まだ無い**ものを、無い経路で登記したら、それ自体が このファイルに書くべき迂回になる。

> ## ★★流れ（Taka 裁定 2026-08-26・逐語）
>
> 「**UIに何を求めるか各担当に聞くので、担当の立場でUIに不足している機能、UIでそもそも問題が発生している箇所を
> リスト化して、と。各担当自身が直さないといけないタスクも洗い出す。
> それを全てまとめてあなたがUI直った段階で登録する。この流れでいきましょ**」
>
> ```
> ① 各担当がここに書き出す（★下の3つ）        ← いまここ
> ② MGR がそれを見て UI を直す
> ③ ★UI が直った段階で、MGR が ここの全部を 台帳へ 登録する
> ④ 以後、ページからしか登録・変更できないようにする（権限で縛る）
> ⑤ 文句が出たら UI を直す
> ```
> ★**③であなたの項目は台帳の ITEM になります。** だから ★**書いておかないと台帳に載りません。**
> ★このファイルが役目を終えるのは ④が立った時。畳み先はその時に **Taka が決める**。

---

## ★書くのは3つ

### ① UI に不足している機能（＝やりたいのに UI に口が無い）

```
### <一行で「何がしたかったか」>
- 種別: 不足
- 担当:            (例) Ledger / 設計・監査 / ESDE / RRI / DW / Inference Control
- やりたかったこと: (何を台帳に入れたい・変えたい・見たいのか)
- 実際にやった経路: (例) register_item() を直接呼んだ / git commit した / /tmp に script を書いた
- UIで試したか:    はい（どのタブ・どのボタン） / いいえ（理由）
- 何が足りないか:  ★1行
- 実測の日付:      YYYY-MM-DD
```

### ② UI で問題が起きている箇所（＝口は在るのに、おかしい・使えない）

```
### <一行で「何が起きたか」>
- 種別: 不具合
- 担当:
- どこで:          (タブ名・ボタン名・URL)
- 何をしたか:      (押した順・入れた値)
- 何が起きたか:    (出た表示・出なかった物。★エラー文はそのまま貼る)
- 期待していたこと:
- 実測の日付:      YYYY-MM-DD
```

### ③ 自分の担当で直さないといけないこと（＝UI のせいではない、自分の宿題）

```
### <一行で「何を直すか」>
- 種別: 宿題
- 担当:
- 何が壊れている/足りないか:
- 実測（数字があれば）:  (例) 165族中138族が引けない / 明細1920件中 割れていないのが88%
- なぜ今できないか:      (例) 上流が先 / 承認待ち / 分母が無い
- 実測の日付:            YYYY-MM-DD
```

---

## ★守ってほしいこと

- **直し方を書かなくていい。** 「足りない物・壊れている物」だけ書いてください。設計は台帳・UI 担当がやります。
- **★推測で書かない。** 実際にやって・実際にできなかったことだけ。「たぶんできない」は書かない。
- **★数字があれば必ず入れる。** 「多い」「よくある」ではなく **件数と分母**。優先順位はここで決まります。
- **既に同じ項目が在れば、新しく足さずに「同じことがあった／担当◯◯」と1行足す**（何人が困ったかが分かるため）。
- **迂回したこと自体は責めません。** 迂回を隠されるほうが困ります。**やったなら書いてください。**
- **★「UIに無いから直接呼んだ」は、いま責められません。** ただし ④（権限で縛る）が立った後は **通らなくなります**。
  ★∴ **通らなくなる前に書いてください。書いていない使い方は、UI に載りません。**
- 既存項目に×印や削除をしない（追記のみ）。

---

## 集まったもの

### ITEM を新規に作りたい
- 種別: 不足
- 担当: Ledger (MGR)
- やりたかったこと: `ITEM-2DER-EVO-0108` / `0109` を新しく立てたい
- 実際にやった経路: `roadmap_registry.register_item()` を Python から直接呼んだ
- UIで試したか: はい（新規登録タブ）。★**TASK を作る口しか無く、ITEM を作る口が無い**
- 何が足りないか: ★UI に ITEM 新規登録の口が無い（★作成者を入れる欄も無い）
- 実測の日付: 2026-08-26

### 訂正記録（AMENDMENT）を足したい
- 種別: 不足
- 担当: Ledger (MGR)
- やりたかったこと: 承認範囲を超えた実行の訂正を、既存記録を書き換えずに追記したい
- 実際にやった経路: `roadmap_registry.register_amendment()` を直接呼んだ
- UIで試したか: いいえ（UI に該当する画面が見当たらなかった）
- 何が足りないか: ★UI に AMENDMENT を足す口が無い
- 実測の日付: 2026-08-26

### 明細を処分したい（RESOLVED / OPEN_GAP など）
- 種別: 不足
- 担当: Ledger (MGR)
- やりたかったこと: 根拠が付いた明細を処分して閉じたい
- 実際にやった経路: `domain_ledger.ledger_dispose_apply()` / `ledger_dispose_bulk()` を直接呼んだ
- UIで試したか: いいえ
- 何が足りないか: ★UI に明細の処分の口が無い（★処分は承認が要るのに、承認の母集団が画面で見えない）
- 実測の日付: 2026-08-26

### 一括承認したい（N件まとめて）
- 種別: 不足
- 担当: Ledger (MGR)
- やりたかったこと: 「この50件を処分してよい」という承認を取って実行したい
- 実際にやった経路: `authority.grant_approval()` を 184回 直接呼んだ → ★**承認は50件だったのに184件実行する事故を起こした**
- UIで試したか: いいえ（「裁定する(承認トークンを発行)」は在るが、1件ずつの形）
- 何が足りないか: ★UI に「対象の一覧を見せて N件まとめて承認する」口が無い（★件数だけでなく対象IDの集合が画面に出ないと母集団を取り違える）
- 実測の日付: 2026-08-25

### 台帳を数えたい・全件調べたい
- 種別: 不足
- 担当: Ledger (MGR)
- やりたかったこと: ID族の分布、明細の品質、根拠の粒度などを全件で数えたい
- 実際にやった経路: `/tmp` に使い捨て script を **20本以上** 書いて実行した
- UIで試したか: 一部（`/api/ledger_rows` `/api/resolve` は在るが、集計の粒度が足りない）
- 何が足りないか: ★UI に「数える」口が無い（1件を引く口はあるが、★分母つきで数える口が無い）
- 実測の日付: 2026-08-26

### 台帳の変更を commit / push したい
- 種別: 不足
- 担当: Ledger (MGR)
- やりたかったこと: 台帳ファイルの変更を履歴に残したい
- 実際にやった経路: `git add` / `git commit` / `git push` を直接叩いた
- UIで試したか: いいえ
- 何が足りないか: ★UI に commit の口が無い（★機構は在る＝`autonomous-git` が既存。UI から届いていないだけ）
- 実測の日付: 2026-08-26

### 明細の id から中身が引けない
- 種別: 宿題
- 担当: Ledger (MGR)
- 何が壊れている/足りないか: `ids.resolve` に `Q-` の枝は在るのに None が返る
- 実測: ID族 **165族中138族(84%)が引けない**。うち `Q`(明細) は **2,030件が全滅**。`APPROVAL` 847件・`QT` 1,499件・`QA` 677件は引き口そのものが無い
- なぜ今できないか: 直せる。UI より先に直すか後かを Taka 裁定待ち
- 実測の日付: 2026-08-26

### 明細化がほぼ機能していない
- 種別: 宿題
- 担当: Ledger (MGR)
- 何が壊れている/足りないか: 依頼文が1件の明細のまま割れない／依頼でないもの（記帳文・観測ダンプ）が明細になる／マーカーが本文に残る
- 実測: 明細 **1,920件**・依頼 **838本**。1依頼1明細が **740本(88%)**。1依頼から **502明細**出た例が1本。依頼でないものが **493件**（うち MGR 自身の記帳文 362件）。明細として成立し得るのは **約924件(48%)**
- なぜ今できないか: LLM管理マネージャが構築中（Taka 側）。`ITEM-2DER-EVO-0108` に登記済み・PLANNED
- 実測の日付: 2026-08-26

### ★MGR 宛の文書を「閉じる」口が front door に無い
- 種別: 不足
- 担当: General Manager（MGR・裁定と監視）
- 何が壊れている/足りないか: MGR 宛の文書に**裁定は書けるのに、閉じられない**。閉じる関数 `cc_register.record_done(doc_id, by, by_doc_id)` は在るが、**front door に口が無い**（`webui.py` に `cc_register` の語 **0件**）。直呼びは書き込みの門が止める（＝迂回）
- 実測（数字）: 手番 **7件 / 7件とも 2026-08-24 起票**＝**5日間 1件も減らなかった**。うち **1件（`TASK_THREAD_BULK_READ_HANDOFF`）は実装が完了していた**（`GET /api/task_threads` が 200 で返り、`domain_esde.py` に「★MGR が実装した」と書いてある）。★**済んだ仕事が一覧に残り続けていた** ∴ 「MGR が詰まっている」の一部は**私の遅さではなく口の欠落**
- なぜ今できないか: 直せる。★`record_done` は**既存機構**なので新しい仕組みは要らない。要るのは front door の口 1本だけ（★新台帳0）。★併せて `by_doc_id` は .md を前提にしているが、いま裁定は**台帳（ITEM）に書く**ので、**ITEM id を受けられるか**も要確認
- 実測の日付: 2026-08-29

### ★状況表の2つの山が1つに見えた／控えが古くても現在の顔で出た（★2件とも直した）
- 種別: 不具合
- 担当: General Manager（MGR・計器の維持）
- 何が壊れている/足りないか: ①`★Taka にしか出せない件` の一覧の直後に **MGR の手番一覧が見出し無しで並び**、別の山が1つに見えた。②台帳check の控えが**古くても「現在」として出て**いた（`SessionStart` の timeout **10秒** に対し所要 **30.9秒** ∴ 毎回打ち切られる）
- 実測（数字）: ①は **ESDE が 2026-08-24 に報告し、5日間 生きていた**。★起票者本人が「自分が渡し先を間違えた」と**誤読**している。②は **6日間** 誤った値を出し続けた（表示「1 mismatch / 56 ledgers」 vs 実測「0 mismatch / 42 ledgers」）
- なぜ今できないか: **直した**（2026-08-29）。①見出しを1行足した ②控えが**6時間超なら断面を添える**（`★古い（08-29 16:17 時点 / 420分前）`・★値は消さない）。★どちらも `.claude/hooks/` 配下で **どの repo にも入っていない** ∴ **次に消えても誰も気づかない**（★置き場は Taka の裁定が要る）
- 実測の日付: 2026-08-29

---

## ★担当ごとの欄（★自分の見出しの下に書いてください）

> ★見出しを分けているのは **同時に書いて消し合わないため**です。★他の担当の欄には書かないでください。
> ★自分の役割が分からないときは `~/.claude/hooks/2der_role.sh get` で出ます（MGR / DESIGN / IMPL）。
> ★ドメインの担当は `manager_v0.DOMAIN_OPERATIONS` の名前（ledger / esde / dw / route_table / sysops）。
> ★どこにも当てはまらないときは「その他」の下に、名乗りを1行書いてから書いてください。

### 【DESIGN（設計・監査）】
<!-- ここに書く。上の①②③の雛形をそのまま使う -->

### 【IMPL（実装）】
<!-- ここに書く -->

### 【ESDE（評価ドメイン）】

#### ① 不足

### 台帳に記帳したい（CHG 発行・ITEM の acceptance 追記・status 更新）
- 種別: 不足
- 担当: ESDE
- やりたかったこと: 変更・調査・事故を台帳に残したい（Taka 常設規則「今後全部変更は台帳に記帳する」）
- 実際にやった経路: `/tmp` の scratchpad に `record_*.py` を **13本**書き、`artifact_registry.record_change()` / `roadmap_registry.register_item()` / `set_status()` を直接呼んだ
- UIで試したか: いいえ（記帳の口が画面に見当たらなかった）
- 何が足りないか: ★UI に「CHG を発行して ITEM に紐づける」口が無い
- 実測: 2026-08-25〜26 に私が発行した CHG **23件（CHG-0283/0286/0287/0289/0290/0291/0292/0293/0295/0296/0298/0299/0300/0301/0302/0304/0305/0306/0307/0308/0309/0310/0311）／ UI 経由 0件（0/23）**
- 実測の日付: 2026-08-26

### relation の現行/保留/旧を画面で見たい
- 種別: 不足
- 担当: ESDE
- やりたかったこと: 明細に付いた relation が「今も正本か」を画面で分けて見たい
- 実際にやった経路: `domain_esde.esde_relation_status()` を Python から直接呼んだ（`DOMAIN_OPERATIONS` には登録済み）
- UIで試したか: はい（`/api/domain_esde`）。★**返りに relation の欄が無い**
- 何が足りないか: ★UI に relation の状態別の口が無い
- 実測: relation **49件 = CURRENT 16 / CANDIDATE 26 / DEPRECATED 7**。★**保留のはずの `STRUCTURAL_PARENT` 26件（49件中53%）が、素直に読むと正本の顔で混ざる**
- 実測の日付: 2026-08-26

### 封印試験の記録と本番の記録を画面で分けたい
- 種別: 不足
- 担当: ESDE
- やりたかったこと: 試験由来の実行記録を本番と区別して見たい（★消さずに分ける）
- 実際にやった経路: `ds.etrace.open_run()` で run を開き `run_id` を付けて emit した
- UIで試したか: はい（`/api/etrace`）。★run_id は返るが、その run が試験か本番かは **別途 `resolve_run` を引かないと分からない**
- 何が足りないか: ★UI に「この記録は試験由来」を示す面が無い
- 実測: 常駐の巡回記録は **15:55 以降 全件が同一 run（ETR-9fd62ec79e09・12,186件を抱える）** ∴ ★**run_id の有無では試験と本番を分けられない**
- 実測の日付: 2026-08-26

#### ② 不具合

### `/api/rthread` が明細の根拠（QE-）を返さない
- 種別: 不具合
- 担当: ESDE
- どこで: `GET /api/rthread?id=<TASK>`
- 何をしたか: ESDE Worker が書いた根拠を画面から確認しようとした
- 何が起きたか: 返りの鍵は `questions` / `typed` / `esde` / `open_gaps` 等で、★**`evidence` の鍵が無い**
- 期待していたこと: 「ESDE が何を根拠に書いたか」が画面から引けること
- 実測: `QE-` は **257件**実在する（`rri.list_evidence` で確認）。うち `/api/rthread` から見えるのは **0件（0/257）**
- 実測の日付: 2026-08-25

### 同じ TASK id で `/api/rthread` と `/api/resolve` の答えが食い違う
- 種別: 不具合
- 担当: ESDE
- どこで: `GET /api/rthread?id=TASK-...` と `GET /api/resolve?id=TASK-...`
- 何をしたか: 同じ TASK id を両方に入れた
- 何が起きたか: `/api/rthread` は `readable=false` / `why="no such task"` / `task_id=""`。同じ id で `/api/resolve` は `state` と `events` を返す
- 期待していたこと: 実在する TASK なら両方から引けること
- 実測: 試した **3件（TASK-2DER-9092B26A / 5A849467 / C6145813）で 3/3 とも "no such task"**。うち 9092B26A は `/api/resolve` で `state=DISPOSITION_REQUIRED`/events 7、5A849467 は `READY_FOR_UPPER_REVIEW`/events 8
- 実測の日付: 2026-08-25

### `/api/tasks` が state を返さない（1件ずつ引くしかない）
- 種別: 不具合
- 担当: ESDE
- どこで: `GET /api/tasks`
- 何をしたか: TASK の状態分布を数えようとした
- 何が起きたか: 返りは **id の文字列配列だけ**。state を知るには `/api/resolve` を1件ずつ叩く必要がある
- 期待していたこと: 一覧に state が付いていること
- 実測: TASK **647件**。60件を `/api/resolve` で引こうとして **2分でタイムアウト**（1件あたり約2秒）。★全件だと約21分
- 実測の日付: 2026-08-25

#### ③ 宿題

### packet が勘定科目へ辿れない（Top-Down の候補が0のまま）
- 種別: 宿題
- 担当: ESDE
- 何が壊れている/足りないか: packet → 親 ITEM までは引けるが、その ITEM に task_ids が無く TASK→明細→科目へ降りられない
- 実測: **ITEM 175件中 task_ids を持つのは 11件（6.3%）**。鎖そのものは成立する＝task_ids を持つ ITEM で **TASK 66件中 50件（75.8%）が科目まで届き、9 ITEM 中 6 ITEM で通った**。★実在 packet は **1本**で、その親 `ITEM-2DER-EVO-0104` の task_ids が **0件** ∴ 候補0
- なぜ今できないか: ★上流が先（ITEM→TASK は route_table 側の宿題）。★偽の fallback を作らない裁定のため候補0で止める
- 実測の日付: 2026-08-26

### Top-Down（上流）が巡回から一度も動いていない
- 種別: 宿題
- 担当: ESDE
- 何が壊れている/足りないか: 巡回が呼ぶのは `esde_one()` だけで、上流の5口は登録済みだが巡回に入っていない
- 実測: 上流の実績は **knowledge_leg 2回 / knowledge_verify 3回 / packet_applicable 3回 / packet_candidates 4回 = 全部 手動**。★**`esde_project_into_packet`（下流へ渡す段）は 0回**。★取得した知識は **2件とも同一の raw_content_hash**（固定の定型文）＝中身0
- なぜ今できないか: Taka 裁定＝実データで ITEM→TASK が埋まるまで一周しない（人工 packet で完了証拠にしない）
- 実測の日付: 2026-08-26

### 独立監査が missed を返す（手実装で機械経路を通っていない）
- 種別: 宿題
- 担当: ESDE
- 何が壊れている/足りないか: 私が手で実装したため、正規の implementation run / test_result が成立していない
- 実測: `TASK-2DER-D6EA0ABF` = **期待9 / 発見4**、`TASK-2DER-C6145813` = **期待9 / 発見5**。偽陰性率は **両方 rate=null（no_denominator）**。INTV-0004 / INTV-0005 へ流れた
- なぜ今できないか: 正規の implementation run が成立してから独立監査を再実行 → PASS 後に上級監査、という順序（Taka 裁定）。★偽の run を作らない
- 実測の日付: 2026-08-25

### AcquisitionRun が「取得できた」と「配線試験だけ」を区別していない（★発見者として記載・直す担当は EGL 側）
- 種別: 宿題
- 担当: ESDE
- 何が壊れている/足りないか: `content_status` の語彙に `PLACEHOLDER` / `EMPTY` が在るのに一度も使われていない
- 実測: **AcquisitionRun 6,793件が例外なく `transport=SUCCESS` / `content=OBSERVED`。`PLACEHOLDER`/`EMPTY` は 0件（0/6,793）**。adapter は **ACQ_MANUAL 6,784件（99.87%）/ ACQ_HTTP_STATIC 9件**。相異なる hash は 1,805 で最頻が 1,582回
- なぜ今できないか: ★ESDE の外（EGL の acquisition）∴ 私は直していない。ESDE 自身の分（2件）は `PLACEHOLDER` へ直した
- 実測の日付: 2026-08-26

### EGL の状態を数える口が2つあり答えが3桁違う（★発見者として記載・直す担当は EGL 側）
- 種別: 宿題
- 担当: ESDE
- 何が壊れている/足りないか: `core.by_type`（sqlite projection）と event log が同期していない
- 実測: `by_type` は **AcquisitionRun 0件 / RawObservation 1件**、event log は **AcquisitionRun 6,793件 / RawObservation 6,792件**。sqlite 側は Run **19件**で止まっている（log は 3,427件）。★私は「観測は1件だけ」と誤報する一歩手前だった
- なぜ今できないか: ★EGL の内部 ∴ 私は直していない
- 実測の日付: 2026-08-26

### 【RRI（依頼理解）】

> ★担当の名乗り: RRI（2026-08-25 に UI 担当から 役割変更）。
> ★下の ② のうち **UI-1〜UI-4 は UI 担当だった期間に 実測した物**。★測ったのは 私だが 直すのは UI 担当。
> ★MGR の判断で 「集まったもの」へ 移して 構いません。

#### ① UI に不足している機能

### HOLD で止まった依頼が UI から見えない
- 種別: 不足
- 担当: RRI
- やりたかったこと: 「いま人の回答待ちで止まっている依頼」を 一覧で 見たい
- 実際にやった経路: `/api/submit` の 応答を 自分で 保持した ／ 台帳側は `list_threads()` を Python から 直接呼んだ
- UIで試したか: はい（運転タブ）。★**HOLD は task を作らずに返るので TASK 一覧に 1件も 出ない**
- 何が足りないか: ★UI に「HOLD 中の依頼」の一覧が無い（★要求鍵・止めた理由・待っている回答が 画面に 出ない）
- 実測（数字）: HOLD された投入は **task_id=None** で返る ∴ ★TASK 647件 の どこにも 現れない
- 実測の日付: 2026-08-25

### HOLD への回答を UI から入れられない
- 種別: 不足
- 担当: RRI
- やりたかったこと: 止まった依頼に 人の回答を 入れて 再評価させたい
- 実際にやった経路: `<<<2DER:DETAIL>>>` マーカーを 手で 書いて `/api/submit` に POST した
- UIで試したか: はい（「この task の明細に足す」）。★**task が無いので ボタンが 押せない**
- 何が足りないか: ★UI に「HOLD への回答」の口が無い（★マーカーの書式を 人が 手で 覚える必要が ある）
- 実測（数字）: 回答は `HUMAN_DECLARATION` として 記録されるが **全 thread 782本中 2本** のみ ＝ ★私の実験2件だけ。人が UI から 入れた例は **0件**
- 実測の日付: 2026-08-25

### 明細の「根拠」を人が入れる口が無い
- 種別: 不足
- 担当: RRI
- やりたかったこと: 明細に 人の宣言（`HUMAN_DECLARATION`）を 根拠として 付けたい
- 実際にやった経路: `record_evidence()` を submit 経由で 呼ばせた
- UIで試したか: いいえ（画面に 該当が 見当たらない）
- 何が足りないか: ★UI に 根拠（evidence）を 足す口が 無い
- 実測（数字）: 証拠を持つ thread **219/782**。うち `recorded_by` が **人** の物は **0件**（全部 機械= ESDE_WORKER / MANAGER_V0 / DOMAIN_LEDGER / ESDE_PROBE）
- 実測の日付: 2026-08-25

#### ② UI で問題が起きている箇所

### UI-1: ドメイン画面のコックピットが ほぼ空
- 種別: 不具合
- 担当: **UI**（★測ったのは RRI＝私。UI 担当だった期間）
- どこで: ドメインタブ →「General Manager のコックピット」
- 何をしたか: 表示しただけ
- 何が起きたか: ★**30欄中 値が出ているのは 2欄だけ**
- 実測（数字）: **30欄（Domain 5 × 項目 6）中 —— 値あり 2 ／ ★未接続 22 ／ 未取得(重い) 5 ／ 数えていない 1**
  - `dw` **6/6 未接続** ／ `route_table` **6/6 未接続** ／ `esde` **6/6 未接続** ／ `sysops` 3/6 ／ `ledger` 1/6
- 期待していたこと: Domain / 現在状態 / backlog / 最後に動いた時刻 / 次に何を待っているか / 上申 / Worker稼働状況 が 埋まること
- ★UI 側は 表を 用意済み。**埋まるかは 各 Domain が 欄を返すかによる**（UI では 直せない）
- 実測の日付: 2026-08-26

### UI-2: front door に在るのに 画面が呼んでいない口が 5本
- 種別: 不具合
- 担当: **UI**（★測ったのは RRI＝私）
- どこで: front door 全体
- 何が起きたか: 口は 200 を返すのに ★画面から 1度も 呼ばれない
- 実測（数字）: **27本中 5本**（`/api/claude_packet` `/api/domain_esde` `/api/ingest` `/api/receipt` `/api/task_threads`）
  ★前回(08-24)は 4本。**`/api/task_threads` が 増えた**＝他セッションが 口を作って 繋いでいない
- 期待していたこと: 作った口が 画面から 使えること
- ★検査は 在る（`webui_ui_audit` の 検査F）。**止めない設計なので 増え続ける**
- 実測の日付: 2026-08-26

### UI-3: TASK の 88% が ROADMAP に繋がっていない
- 種別: 不具合
- 担当: **UI ＋ 上流**（★測ったのは RRI＝私）
- どこで: ロードマップタブ / TASK 詳細
- 何が起きたか: TASK を開いても ★「ROADMAP: 未接続」と 出るだけ
- 実測（数字）: **TASK 647件中 未接続 571件（88.3%）** ／ **ITEM 167件中 task_ids を持つのは 11件（★156件が どの TASK とも 繋がっていない）**
- 原因（実測）: ROADMAP へ 繋ぐ writer は **`twoder/submit.py` の 4箇所だけ**で、**全部 `<<<2DER:PROGRESS>>>` マーカーが 在る時だけ 走る**
- 期待していたこと: 画面から ITEM と TASK を 繋げること
- 実測の日付: 2026-08-26

### UI-4: 画面が「数えていない値」を 測った値のように 出していた
- 種別: 不具合（★2026-08-24 に UI 側で 表示は 直した。★元の欄は まだ 定数）
- 担当: **ESDE**（表示は UI で 直した／★測ったのは RRI＝私）
- どこで: ドメインタブ → esde の状態
- 何が起きたか: `worker` / `human_decision_waiting` / `blocking` が **定数を 返しているのに 数値として 出ていた**
- 実測（数字）: `domain_esde.py` の **3欄が 定数**（`worker=1` / `human_decision_waiting=0` / `blocking=False`）。★行番号は 176→392→613 と 移動した
- ★UI 側の 対処: 「数えていない」と 表示し、出どころの `file:line` を **実行時に導出**するようにした（手書きしない）
- 実測の日付: 2026-08-26

#### ③ 自分の担当で直さないといけないこと

### 手書きの注記4本が 古くなっても 誰も気づかない
- 種別: 宿題
- 担当: RRI（★UI 担当だった期間に **私が 書いた**）
- 何が壊れている/足りないか: `twoder/domains_view.py` と `twoder/task_dev_view.py` に **手書きの実測値・注記** が在り、**いつ測ったかの構造化された記録が無い**
- 実測（数字）: **4本** —— `HEAVY = {"ledger_summary": 71.4}` ／ `DOMAIN_NOTE`（note / confusable / still_blocked）／ `EXCLUDED`（母集団が違う台帳2本）／ sysops の `★未達` 固定リスト
  ★対照: 画面の 31箇所の 警告のうち **大半は 毎回 計算し直す**（直れば消える）。**この4本だけが 消えない**
- ★実例: 私が「`/api/pending_approvals` は 114秒」と 台帳(CHG-0254)と 資料に 書いた → **58分後に Sysops が 1.4秒に 修正** → ★私の記録は 嘘になったまま 残った
- なぜ今できないか: 正確化には `measured_at` / `measured_at_head` の 2欄が 要る。既存解（`2der_arch_fresh.sh` の 記録commit↔現在HEAD）と 同じ形で 書けるが、**どこに 持たせるかが 未裁定**
- 実測の日付: 2026-08-26

### `still_blocked` は live な出所が在るのに 手書きの写しを出している
- 種別: 宿題
- 担当: RRI（★私が 書いた）
- 何が壊れている/足りないか: 「まだ通らない（門が止めている）」を **手書きの散文**で 出しているが、`/api/domain_sysops` が **同じことを 機械可読で 返している**
- 実測（数字）: `kill_or_restart_gate` = `{"decision":"REQUIRES_APPROVAL","requires_approval":true,"authority_decision_id":"AUTHD:KILL_OR_RESTART:REQUIRES_APPROVAL"}` ―― **live で 引ける**のに 使っていない **1箇所**
- なぜ今できないか: 直せる。★UI 側の 小さな配線だが **UI 担当が MGR に 移った**ので 勝手に 触らない
- 実測の日付: 2026-08-26

### HOLD 判定が「HOLD が在ったこと」を観測していない（PARTIAL）
- 種別: 宿題
- 担当: RRI
- 何が壊れている/足りないか: 「HOLD への回答」の判定を **「宛先が要求鍵の形」＋「その task が まだ無い」** で 代用している。★HOLD そのものの 記録を 引いていない
- 実測（数字）: 誤りの向きは **過剰に拾う側**。現時点の実測では `HUMAN_DECLARATION` **2件とも 正しい HOLD 回答**・通常 DETAIL の混入 **0件**
- なぜ今できないか: 正確化には「HOLD が存在した」という **参照可能な記録** が要る ＝ **新 state**。★Taka 裁定で 今は作らない（known limitation として 残す）
- 実測の日付: 2026-08-25

### 依頼の種別を 決定論で 引ける情報が 無い
- 種別: 宿題
- 担当: RRI
- 何が壊れている/足りないか: 「対象が存在しない」ことが 依頼と **整合するか矛盾するか** は 種別で 決まるが、**決定論で かつ 実際に 埋まっている 種別情報が 存在しない**
- 実測（数字）:
  - `request_type`（6語）＝ 意味的には 合う（BUILD_CAPABILITY→CREATE 等）が **LLM 分類由来**。標本40件中 **30件** 埋まる
  - `work_kind`（2語）＝ **決定論**（マーカー由来）だが 標本40件中 **1件** しか 埋まらない
  - **DELETE に当たる語は どちらの語彙にも 無い**
  - ★影響: 現行は CREATE / MODIFY / DELETE すべてで **24/24 停止** ＝ 種別を 区別できていない（★CREATE での停止は **誤停止**）
- なぜ今できないか: 語彙を 増やすか LLM 判定を 足すかは **裁定事項**
- 実測の日付: 2026-08-25〜26


### 【DW（dev-workcell）】
<!-- ここに書く -->

### 【route_table（経路表）】

> ★名乗り: ITEM→TASK のリンク欠落を 2026-08-25 に ESDE から受領（Taka 逐語「担当は Route / Topology に寄せるのが自然」）。以下はその担当としての分。

#### ① 不足

### ITEM に TASK を結びたい（正規の起票から）
- 種別: 不足
- 担当: route_table
- やりたかったこと: 起票した TASK を、その作業が属する ITEM に結びたい
- 実際にやった経路: `roadmap_registry.append_task_id()` を直接呼ぶ経路しか無く、**`twoder/submit.py` にコードを足した**（`CLAUDE_IMPL=1` で commit）
- UIで試したか: はい（新規登録タブ）。★**TASK を作る口はあるが、作った TASK を ITEM に結ぶ欄が無い**
- 何が足りないか: ★UI に「この TASK はどの ITEM のためか」を選ぶ欄が無い（★書き手は `submit.py:1095`/`:1186` の1箇所だけで、進捗マーカー付きの投入しか動かない）
- 実測: **ITEM 175件中 task_ids を持つのは 11件（6.3%）**。1日前の同じ計器の記録は 162件中7件（4.3%）＝**1日で +4件しか増えていない**。TASK 側は **647件中 76件（11.7%）**しか ITEM に繋がっていない
- 実測の日付: 2026-08-26

### `next=`（次の手番）を画面から選びたい
- 種別: 不足
- 担当: route_table
- やりたかったこと: ITEM の次の手番を正しい語で記録したい
- 実際にやった経路: `set_status(note="… next=…")` に **自由文で書いた**
- UIで試したか: いいえ（手番を選ぶ欄が画面に無かった）
- 何が足りないか: ★UI に手番を **5語から選ぶ欄**が無い（自由文だから壊れる）。★正本は `twoder/effective_state.py:24` = 語彙 `DESIGN/IMPL/MGR/TAKA/NONE` の5語、かつ **位置は文字列の先頭か `note=` の直後だけ**
- 実測: ★**私が書いた status_note 49件のうち 49件（100%）が機械に拾われていなかった**。内訳＝語彙の外 19件（着弾待ち3／AUDIT2／Claude2／復旧1／裁定7 1／裁定5 1 ほか）、★**語彙は正しいが位置が違う `next=TAKA` が 30件**。MGR が後から `next=NONE` を正しい形で書いて拭っていた
- 実測の日付: 2026-08-26

#### ② 不具合

### `append_task_id(note=…)` が ITEM の `status_note` を上書きする
- 種別: 不具合
- 担当: route_table
- どこで: `roadmap_registry.py:224`（UI の下にある台帳の口）
- 何をしたか: リンクの由来を残そうとして `append_task_id(..., note="progress_marker")` を渡した
- 何が起きたか: `e["status_note"] = note` で **item の `status_note` が丸ごと置き換わる**。`status_note` は監査と状況表が actor / stage を読む欄
- 期待していたこと: リンクの由来だけが残り、既存の `status_note` は保たれること
- 実測: 私が入れて **約94秒**で気づき取り消した。★汚れた item は **0件（ROADMAP 175件を全件走査）**。★ただし**従来の marker 経路は元々 note を渡していなかった** ∴ 私が足したことで既存の経路まで壊すところだった
- 実測の日付: 2026-08-25

### リンクの由来を残せる場所が、読める場所に無かった
- 種別: 不具合
- 担当: route_table
- どこで: `twoder/runs/*.trace.json`（`_rec()` の行き先）
- 何をしたか: 「どの規則で ITEM→TASK を結んだか」を `_rec("ITEM_LINK_BY", …)` で残した
- 何が起きたか: そこは**横読みが禁止されている場所**で、★**自分で証拠を、自分が読めない所に置いた**
- 期待していたこと: 由来が正規の読み口から引けること
- 実測: front door の口 **28個**を当たり、由来を残せるのは **`/api/etrace?task_id=<TASK>` の1つだけ**だった（`/api/resolve?id=ITEM…` は `status_note` を上書きするので不可／`/api/rthread` は evidence の鍵が無く QE- が見えない／`/api/resolve?id=QE-` は RRI に行を足すので重い）
- 実測の日付: 2026-08-26

#### ③ 宿題

### 新経路（本文に ITEM が1件）に、まだ発火機会が来ていない
- 種別: 宿題
- 担当: route_table
- 何が壊れている/足りないか: 規則は載って動いているが、実データで一度も発火していない
- 実測: 再起動後に task を作った投入 **7件が7件ともマーカー付き**（`rule=progress_marker` 7件／`rule=body_single_item_mention` は **封印試験の1件のみ**）。★うち **2件は本文の ITEM 言及が1件**で、マーカーが無ければ新経路で結ばれていた。★過去データでの見込みは「新規に結べる候補 21件」だったが、**現在の投入傾向では出番が来ない可能性がある**
- なぜ今できないか: ★人工投入をしない裁定（`AUTONOMOUS_TASK_CREATION` は要承認）。自然投入待ち
- 実測の日付: 2026-08-26

### 本文の ITEM 言及が ref として抽出されない
- 種別: 宿題
- 担当: route_table
- 何が壊れている/足りないか: `twoder/detail_refs.py:30` の `ID_PREFIXES` に `ITEM-` が入っていない
- 実測: 抽出対象は **16族**（ETR/UTT/DEV/THREAD/TASK/ADM/DE/ART/CHG/INTV/SUBMIT/RTHREAD/ACC/AX/QE/Q）で ★`ITEM-` は無い。一方 **802 thread 中 337本（42.0%）の本文に `ITEM-` が書かれている**（43種）∴ ★**書いてあるのにどの口からも拾われていない**
- なぜ今できないか: ★「本文に出る＝その TASK の親」とは限らない（引用・言及がある）ので、採用の規則を決めずに増やせない
- 実測の日付: 2026-08-26

### TASK から明細（thread）へ辿れないものが半分以上ある
- 種別: 宿題
- 担当: route_table
- 何が壊れている/足りないか: TASK→thread が1つに定まらないと、本文も科目も引けない
- 実測: **TASK 647件中 thread が1つに定まるのは 284件（43.9%）**。定まらない 363件の内訳は `NO_TRACE` / `NO_RTHREAD_ID` / `AMBIGUOUS 36件`。★`task_threads` の summary は `with_thread=320` と答えるが**これは AMBIGUOUS 36 を含む別の鍵**（`rthread_id` が非 null なのは 284）＝★同じ画面に出すなら鍵を書かないと取り違える
- なぜ今できないか: ★AMBIGUOUS を推測で1件に縮約しない裁定。残りは trace file 側の欠落
- 実測の日付: 2026-08-26

> 名乗り: Route / Topology 担当（Taka 任命 2026-08-25）。実体は `twoder/domain_route_table.py`。
> 職責は「宣言と実測の突き合わせ・分類・起票」まで。修復コードは書かない。
> ★下の数字はすべて 2026-08-26 に実測したもの。分母は `manager_v0.DOMAIN_OPERATIONS` の全 operation **37件**（5 Domain）。

## ① UI に不足している機能

### Domain の operation を UI から呼べない
- 種別: 不足
- 担当: route_table
- やりたかったこと: 各 Domain の operation を UI から1件ずつ呼んで、接続しているか確かめたい
- 実際にやった経路: `/tmp` に probe script を書き、`manager_v0.DOMAIN_OPERATIONS` を実行時に読んで直接 import して呼んだ
- UIで試したか: はい（Domain / General / Worker 画面）。★**画面は「仕事を走らせる操作は名前だけ出します」と明記していて、押せない**
- 何が足りないか: ★**operation 37件のうち front door から呼べるのは 6件(16%)。31件(84%) は口が無い**（front door の `/api` endpoint は 28本在るのに、operation を呼ぶ口は 6本ぶんしか無い）
- 実測の日付: 2026-08-26

### 独立監査を回す口が無い
- 種別: 不足
- 担当: route_table
- やりたかったこと: 自分が実装した `TASK-2DER-8526357A` の独立監査を、別の担当に回したい（実装者は監査できない＝門が `generator=auditor` を independent と認めない）
- 実際にやった経路: `/api/submit` に「独立監査を1件回す」という依頼文を投げて TASK を1本立てた（`TASK-2DER-DE741FC1`）
- UIで試したか: はい。★**監査を依頼する口・監査担当を指名する口が画面に無い**
- 何が足りないか: ★UI に「この TASK の独立監査を回す」口が無い。★**依頼を TASK にしても、その TASK 自身が同じ門で止まる**（起票から 25時間、blockers 4件のまま state が付かない）
- 実測の日付: 2026-08-26

### 止まった依頼を画面から閉じられない
- 種別: 不足
- 担当: route_table
- やりたかったこと: `CLARIFY_FIRST` で保留になった依頼に、求められた事実の在り処を答えて先へ進めたい
- 実際にやった経路: `detail_seal` の封印マーカー（`<<<2DER:DETAIL>>>`）を手で組んで `/api/submit` に投げ、元の依頼文を1バイトも変えずに再投函した
- UIで試したか: はい。★**保留の一覧も、保留を閉じる口も画面に無い**。返り値の文中に `request_key=...` が出るだけ
- 何が足りないか: ★UI に保留（CLARIFY_FIRST）の一覧と、そこへ回答を綴じる口が無い。★**実測＝6件に回答を投函して、先へ進んだのは1件(17%)**。残り5件は同じ所で止まったままで、なぜ足りないのかが画面に出ない
- 実測の日付: 2026-08-26

### ITEM を作る口（★「集まったもの」の同名項目への追記）
- 種別: 不足
- 担当: route_table
- やりたかったこと: 親 ITEM を2本立てたい（`ITEM-2DER-EVO-0105` / `0106`）
- 実際にやった経路: **`/api/submit` に `<<<2DER:PROGRESS>>>` マーカー（item / phase / title / actor / stage）を付けて投げた。`register_item` を直接呼ばずに登記できた**（`submit.py:397` が呼ぶ）
- UIで試したか: はい。★**画面に ITEM を作るボタンは無い**が、front door 側には経路が在った
- 何が足りないか: ★UI に口が無いのは同じ。★ただし **front door には既に経路が在る**ので、足りないのは機構でなく画面と書式の案内。★書式（必須欄・許される actor / stage の語）はコードを読まないと分からない
- 実測の日付: 2026-08-26

## ② UI で問題が起きている箇所

### task の状態が引けない（state が空で返る）
- 種別: 不具合
- 担当: route_table
- どこで: `GET /api/state?task_id=TASK-2DER-8526357A`
- 何をしたか: 自分が起票した TASK の状態を引いた
- 何が起きたか: `state = None` / `next_legal_operation = None` が返る。`completion_blockers` は4件返る（`IMPLEMENTATION_RUN_MISSING` / `TEST_NOT_PASSED` / `INDEPENDENT_AUDIT_MISSING` / `UPPER_REVIEW_MISSING`）。★**thread_id も None**
- 期待していたこと: `CREATED` などの状態と、次の合法操作（`PLAN`）が返ること。★**止まっている理由は出るのに、いまどこに居るかが出ない**
- 実測: ★自分が起票した TASK **2本とも同じ**（`TASK-2DER-8526357A` / `TASK-2DER-DE741FC1`）
- 実測の日付: 2026-08-26

### 投函が通ったのか止まったのかが画面で分からない
- 種別: 不具合
- 担当: route_table
- どこで: `POST /api/submit`
- 何をしたか: 調査結果を TASK として **25件** 投函した
- 何が起きたか: ★**すべて HTTP 200 が返るが、`task_id` が `None` のものが 9件**。内訳は DW task 化 **16件(64%)** / `EGL_RESEARCH`（task を作らない）**3件(12%)** / `CLARIFY_FIRST` 保留 **6件(24%)**。止まった理由は `next_legal_operation` という欄の**長い文の中**に混ざって返る
- 期待していたこと: 「入った / 入らなかった」と、入らなかった理由が別の欄で分かること
- 実測の日付: 2026-08-26

### front door が応答なしで接続を切る
- 種別: 不具合
- 担当: route_table
- どこで: `POST /api/submit`
- 何をしたか: 独立監査の依頼文（約1,200字）を投函した
- 何が起きたか: `http.client.RemoteDisconnected: Remote end closed connection without response`。★**切れたのか入ったのかが分からない**ので、task 一覧（635件）を引いて最新が自分の直前の投函であることを確認してから再投函した（重複起票は避けられた）
- 期待していたこと: 応答が返るか、返らないなら「入っていない」と分かる手段が画面に在ること
- 実測: ★投函 **25件中 1件(4%)**。★直後の `GET /api/tasks` は 200 で、front door 自体は生きていた
- 実測の日付: 2026-08-25

### 台帳へ投函する経路の門が、投函する前のコマンドごと拒否する
- 種別: 不具合
- 担当: route_table
- どこで: `~/.claude/hooks/2der_note_form_guard.sh`（台帳へ投函する note の形式の門）
- 何をしたか: `<<<2DER:PROGRESS>>>` を含む投函 script を書こうとした
- 何が起きたか: ★**投函そのものではなく、script を作るコマンドが拒否された**（コマンド文字列に検査語が入るため）。★**2回**。1回目は必須欄（`AXIS=` / `判定=` / `止める=` / `測ったHEAD` / `形式=`）の不足、2回目はファイル作成ごと
- 期待していたこと: 投函の検査であって、投函文を書く作業は止まらないこと
- 実測: ★門のコメントに **「試験しにくい門は 後で試験されなくなる ∴ 記録する」** と既に書かれていた（2026-08-22 の監査の指摘）。★同じ穴を踏んだのは自分で2人目
- 実測の日付: 2026-08-25

## ③ 自分の担当で直さないといけないこと

### 経路表 Domain の唯一の operation が繋がっていない
- 種別: 宿題
- 担当: route_table
- 何が壊れている/足りないか: `precheck_names` に authority の宣言が無く、呼び手が無く、自分の実行を ETRACE に残さない
- 実測（数字があれば）: ★**route_table の operation 1件中 1件(100%) が UNWIRED**。実体は在る（`impl=YES`）。authority=無 / 呼び手=無 / 記録=無 / 実行痕跡=無。★5 Domain の中で **GREEN が 0件なのは route_table と dw の2つだけ**（ledger GREEN 4/14・sysops GREEN 6/8）
- なぜ今できないか: 分離TASK（`TASK-2DER-8526357A`）の独立監査が終わるまで着手しない（Taka 裁定 2026-08-25＝分離TASKと接続TASKを混ぜない）。authority 宣言は `ITEM-2DER-EVO-0106` の後続TASKに在る
- 実測の日付: 2026-08-26

### 分離TASKが閉じられない（上流待ち）
- 種別: 宿題
- 担当: route_table
- 何が壊れている/足りないか: `domain_route_table.py` への移設は実装・検証まで済んでいるが、DW の完了門を通せない
- 実測（数字があれば）: ★受入6条件のうち **4件は実測で確認済み**（返り値は分離前と1バイトも同一＝10ケースの sha256 が3経路で一致）。★残り2件（独立監査 PASS / 上級監査 PASS）は **25時間動いていない**。★原因は `dev-workcell/dw/dispatch.py:29` の逐語 `"CREATED": ("PLAN", "MANAGER", "GOAL+KNOWLEDGE_PACKET", True)` ＝ **PLAN の必要入力が KNOWLEDGE_PACKET**（RRI/EGL が作る）
- なぜ今できないか: ★上流（RRI/EGL）が調整中で、DW に担当が居ない（Taka 逐語 2026-08-25）。★**codegen を伴わない移設をどの経路で受け入れるかが未裁定**
- 実測の日付: 2026-08-26

### commit 済みの行が、git に無いファイルを指している
- 種別: 宿題
- 担当: route_table
- 何が壊れている/足りないか: `manager_v0.py:665` の `DOMAIN_MODULES["route_table"] = "twoder.domain_route_table"` は commit 済みだが、`twoder/domain_route_table.py` は git 未追跡。`domain_dw.py` の差分も未commit
- 実測（数字があれば）: ★いま動いているのは作業ツリーを読んでいるため。clone し直すと `to_domain("precheck_names")` が落ちる。★ただし twoder には **未追跡の .py が 22本**在り（`domains_view.py` も未追跡）、**追跡 464 / 作業ツリーの .py 321** ＝ この repo に元から在る状態でもある
- なぜ今できないか: `manager_v0.py` に他担当の未commit差分（`ledger_cycle` 周り 14+/3−）が同居しており、hunk 単位で切らないと自分の1行だけを commit できない。★Route が General の仕事を引き取らない（Taka 指示 2026-08-25）ため MGR へ「混在差分のため commit 境界不明」で返してある
- 実測の日付: 2026-08-26

### 自分の報告が「着手可能な穴」と「上流待ちの穴」を区別できない
- 種別: 宿題
- 担当: route_table
- 何が壊れている/足りないか: Route の接続報告が、担当が居て今すぐ直せる穴と、上流待ちで動かせない穴を同じ見た目で出す
- 実測（数字があれば）: ★いまの報告は **operation 37件中 UNWIRED 21件(57%) / PARTIAL 6件(16%) / GREEN 10件(27%) / MISSING 0件**。★このうち **dw の 6件は担当不在かつ上流待ち**なので、着手可能に見えるのは誤り。★実際に今すぐ着手できるのは route_table の 1件のみ。★語彙は新設しない＝`roadmap_registry.py:27 STATUSES` に既に `DEFERRED` が在る
- なぜ今できないか: 直せる。★ただし「dw を分母から外す」か「DEFERRED として別欄に出す」かは Taka 裁定待ち（分母の定義が変わるため）
- 実測の日付: 2026-08-26



### 【sysops（常駐・資源）】

## ① UI に不足している機能

### 常駐を再起動したい（承認を取ってから）
- 種別: 不足
- 担当: sysops
- やりたかったこと: 新しいコードを常駐へ反映するため `twoder-manager` / `twoder-webui` を再起動したい
- 実際にやった経路: `authority.grant_approval()` を直接呼び（**4回**）、`systemctl --user restart` を shell で直接叩いた（**4回**：02:28 manager / 13:50 webui / 13:50 manager / 23:45 manager）
- UIで試したか: いいえ（UI に「裁定する(承認トークンを発行)」は在る＝webui.py:3524。ただし **`KILL_OR_RESTART` を対象にできるか未確認**。再起動そのものの口は UI に無い）
- 何が足りないか: ★「承認を取る → 再起動する → 効いたか確かめる」が **UI で1本に繋がっていない**（承認だけ UI、実行は shell）
- 実測の日付: 2026-08-25 / 2026-08-26

### 常駐が古いコードを読んでいることを UI で気づきたい
- 種別: 不足
- 担当: sysops
- やりたかったこと: 「commit したのに常駐に届いていない」を画面で気づきたい
- 実際にやった経路: `actor_liveness.observe()` を Python から直接呼んだ（本日 **6回以上**）
- UIで試したか: はい（常駐の稼働状況タブ）。★`DEGRADED` と `flags` は出る。**ただし気づけたのは私が Python を叩いたときだけで、画面を見て気づいた回数は 0**
- 何が足りないか: ★出てはいるが **誰も見ていない**（通知・目立たせる仕組みが無い）。実測：`twoder-manager` が `古いコードを読んでいる` のまま **494.6分／238.7分** 放置された事例が2回
- 実測の日付: 2026-08-26

### 数える（全件走査）の口が UI に無い
- 種別: 不足
- 担当: sysops
- やりたかったこと: ETRACE・authority・operation を分母つきで数えたい
- 実際にやった経路: `/tmp` の scratchpad に使い捨て script を **50本** 書いて実行した
- UIで試したか: 一部（`/api/etrace` `/api/control` は在るが、集計の粒度が足りない）
- 何が足りないか: ★「数える」口が無い（★同じ項目が既に在る：**「台帳を数えたい・全件調べたい」／担当 Ledger(MGR)**。**同じことが sysops でもあった**）
- 実測の日付: 2026-08-26

## ② UI で問題が起きている箇所

### `/api/domain_sysops` の返り10欄のうち、UI は2欄しか描いていない
- 種別: 不具合
- 担当: sysops
- どこで: ドメインタブ →「常駐の稼働状況」（`webui.py` の `dmRuntime()`）
- 何をしたか: `GET /api/domain_sysops` の返りと、画面が描く欄を突き合わせた
- 何が起きたか: 返りは **10欄**（`domain` / `escalations` / `gate_slots` / `gate_slots_key` / `kill_or_restart_gate` / `read_only` / `runs_worker` / `runtime` / `runtime_gate` / `★この口が持たないもの`）。UI が描くのは **2欄**（`runtime` / `escalations`）＝ **8/10 が画面に出ない**
- 期待していたこと: 2026-08-25 に足した **`gate_slots`**（`waits` / `hits` / `misses` / `build_sec`）が画面に出ること。★これは「front door の詰まりが CPU か待ちか」を分ける唯一の計器だが、**画面からは 0回 見られていない**
- 実測の日付: 2026-08-26

## ③ 自分の担当で直さないといけないこと

### 受入条件（acceptance）が空のまま作業していた
- 種別: 宿題
- 担当: sysops
- 何が壊れている/足りないか: 自分の item に「何をもって終わりか」が書かれていない＝機械が完了を判定できない
- 実測: acceptance が空の item **7/11**（MGR 実測）のうち、**3件が私の Domain**（`EVO-0102` / `EVO-0101` / `EVO-0098`）。私は 2026-08-25 に **20件以上**記帳しながら、受入条件を **0件** しか書いていない
- なぜ今できないか: ★acceptance を書くのは「完了条件を作る」＝Taka 裁定境界により **Taka の手番**。材料（案）は EVO-0102 へ記帳済み
- 実測の日付: 2026-08-26

### front door の詰まりが「CPU か待ちか」まだ分けられていない
- 種別: 宿題
- 担当: sysops
- 何が壊れている/足りないか: 監視が数えた詰まり **累計17回** のうち、原因を説明できたのは **1回だけ**（2026-08-25 06:30 の self_check 窓）。残り **16回は未説明**
- 実測: 重い5口は **5/5 が別プロセスで走る**（`isolated_or`）／親が GIL を使う分は **0%**（JSON 814,455バイトでも 0.00秒）∴ **CPU 説は自分で棄却**。次に見るのは `gate_slots.waits` だが、`self_check` は **1日1回**・webui は本日 **4回**再起動されており基準線が毎回 0 に戻る
- なぜ今できないか: ★外部の出来事待ち（`next=External`）。故意に self_check を起こさない
- 実測の日付: 2026-08-26

### Domain 内の未達3件（W3対象選択 / Worker 実行状態 / 依存追跡の浅さ）
- 種別: 宿題
- 担当: sysops
- 何が壊れている/足りないか: ①W3（出力の同等性）の比較対を決定論で導けない ②Worker の実行状態が見えない（Worker が常駐でない）③依存追跡が import 1段で浅い
- 実測: 依存追跡の精度＝静的1段の**偽陰性 55.8%**／実ロード観測は**偽陽性 0** だが run 1 で飽和。CHANGE_LOG からの逆引きは **47.5%** ∴ W3 へ未接続（Taka 裁定①）。`sysops_*` は **8/8** が authority 宣言済み・role 付与済み（全体は authority **29/37**・role **37/37**）
- なぜ今できないか: ★W3 の coverage 拡張は「未宣言 operation の整理が終わるまで停止」（Taka 裁定）。未宣言 **8/37** は dw 6 / route_table 1 / ledger_dispose_bulk 1 ＝ **私の Domain には 0件**
- 実測の日付: 2026-08-26


### 【Inference Control（LLM 呼出の管理）】

## ① UI に不足している機能

### ITEM に付けた明細が front door から1件も引けない
- 種別: 不足
- 担当: Inference Control
- やりたかったこと: 自分の試験記録（明細）を引いて「入ったか」を確かめたい
- 実際にやった経路: `rri.request_thread.list_threads()` → `list_questions()` を Python から直接呼んだ
- UIで試したか: はい。`/api/rthread?task_id=ITEM-2DER-EVO-0107` → `{"readable": false, "why": "no such task"}`。`/api/task_threads` も試したが **TASK 639件しか見ない**
- 何が足りないか: ★ITEM 宛の明細を返す口が無い（**明細 86件中 0件** が front door から引けない）
- 実測の日付: 2026-08-26

### 知識 doc を artifact として後から紐づける口が無い
- 種別: 不足
- 担当: Inference Control
- やりたかったこと: 書いた知識 doc を ITEM の成果物として登記したい
- 実際にやった経路: 明細の `evidence:` 欄に doc 名を文字列で書いた／git commit で辿らせた
- UIで試したか: いいえ（`artifact_ids` は item 登記時にしか入らないため）
- 何が足りないか: ★artifact の後付け登記の口が無い（**doc 8本中 0本** が `ITEM-2DER-EVO-0107.artifact_ids` に入っている＝`[]`）
- 実測の日付: 2026-08-26

### Domain の状態を返す口が無い
- 種別: 不足
- 担当: Inference Control
- やりたかったこと: 「LLM 呼出の現在状態」を General / UI へ1画面で返したい（ESDE の `/api/domain_esde` と同じ形）
- 実際にやった経路: doc に手で書いた／自分の頭で保持した
- UIで試したか: はい。`/api/domain_esde` `/api/domain_sysops` は在るが Inference Control は無い
- 何が足りないか: ★Domain **5件（`DOMAIN_MODULES`）中 Inference Control は 0件**（未登録）。呼出点 38・呼び手 77・Knowledge 6件が UI から **0件** 引けない
- 実測の日付: 2026-08-26

### 依頼文の全文を一覧で取る口が無い（切片しか返らない）
- 種別: 不足
- 担当: Inference Control
- やりたかったこと: 実物の依頼文で LLM の門を測りたい
- 実際にやった経路: `/api/task_index` の `goal_head` を使った → **120字の切片**と気づき、`/api/state?task_id=` を **55回** 個別に叩いた（**85秒**）
- UIで試したか: はい（`/api/task_index`）
- 何が足りないか: ★全文を一覧で返す口が無い。**切片で測ると門の停止率が 14/55（25%）、全文では 2/55（4%）＝ 6倍ずれた**
- 実測の日付: 2026-08-26

### 試験結果を明細として入れる口が無い
- 種別: 不足
- 担当: Inference Control
- やりたかったこと: 走らせた試験を1件1明細で台帳へ入れたい（Taka 指示 2026-08-25）
- 実際にやった経路: `curl` で封印 DETAIL を `/api/submit` へ投函（**明細 86件すべて**）
- UIで試したか: いいえ（画面に該当の口が見当たらなかった）
- 何が足りないか: ★UI に「試験結果を明細として入れる」口が無い
- 実測の日付: 2026-08-26

## ② UI で問題が起きている箇所

### 記帳の形式ガードが、台帳投函でないコマンドを止める
- 種別: 不具合
- 担当: Inference Control
- どこで: `~/.claude/hooks/2der_note_form_guard.sh`
- 何をしたか: 計測スクリプトを実行した（標本から進捗投函を除外するため、コマンド本文に `2DER:PROGRESS` の文字列が含まれていた）
- 何が起きたか: `2DER 形式の門: 台帳へ投函する note に 欄が足りません -> AXIS= 判定= 止める= 測ったHEAD 形式=(範囲/R1/R2/R3/R4)` で **コマンド全体が拒否**。**実測 2回**（投函ではなく測定の実行が止まった）
- 期待していたこと: 投函の口を通るときだけ検査される
- 実測の日付: 2026-08-26

### front door が落ちて投函が 000 で返る
- 種別: 不具合
- 担当: Inference Control
- どこで: `POST /api/submit`（100.107.6.119:8770）
- 何をしたか: PROGRESS を投函した
- 何が起きたか: `PROGRESS=000`（接続不可）。**実測 1回**。webui の pid が 3701427 → 3731814 → 3750594 と入れ替わっていた。再投函で 200
- 期待していたこと: 落ちている間は投函が失敗したと分かる／★**200 も 000 も「入ったか」は別途引かないと分からない**
- 実測の日付: 2026-08-26

### CLARIFY_FIRST が、回答を記録しても閉じない
- 種別: 不具合
- 担当: Inference Control
- どこで: `POST /api/submit`（`RRI_INTENT_HOLD` / `request_key=TASK-2DER-B67BE7AB`）
- 何をしたか: ①依頼を投函 → HOLD ②封印 DETAIL で回答を投函 ③元の raw を **1バイトも変えず** 再投函
- 何が起きたか: `submit._recorded_answers(key)` は **n=1**（回答は記録されている）のに、**2/2 で `RRI_INTENT_HOLD` のまま**。`/api/rthread` では確認できず（`no such task`）、`runs/*.trace.json` は境界で直読禁止 ∴ **front door からは理由が引けなかった**
- 期待していたこと: 回答が記録されたら次へ進む／せめて「なぜまだ止まるか」が引ける
- 実測の日付: 2026-08-25

### 同じ依頼文で門の判定が走行ごとに変わる
- 種別: 不具合
- 担当: Inference Control
- どこで: front door の入口（`rri.intent_strategy.resolve_consensus` → `stops_before_action`）
- 何をしたか: 確実に依頼文である実物 **55本（全文）** を本番の合議へ通す。これを3回
- 何が起きたか: 走行ごとの停止が **3 / 1 / 1**。**3回とも止まった件は 0/55**。止まった件の確率 p を各10回で測ると **0.80 / 0.50 / 0.40 / 0.20 / 0.10 / 0.00 / 0.00**
- 期待していたこと: 同じ依頼文なら同じ判定
- 実測の日付: 2026-08-26

### `/api/control` は `route_table` を既定で返さない
- 種別: 不具合
- 担当: Inference Control
- どこで: `GET /api/control`
- 何をしたか: 経路表を見ようとして `/api/control` を叩いた
- 何が起きたか: `include.available` に `route_table` が在るのに `include.omitted` にも在り、**既定では 0件**。`?include=route_table` を付けて初めて 18行が返った（**S01〜S18・PRESENT 18/18**）
- 期待していたこと: 正本（経路表）が既定で見えるか、既定で出ないことが画面に出る
- 実測の日付: 2026-08-26

### LANDED が返るのに acceptance / title / phase が入らない
- 種別: 不具合
- 担当: Inference Control
- どこで: `python3 -m twoder.submit_client --file`（既知 ITEM への進捗投函）
- 何をしたか: `ITEM-2DER-EVO-0107` に `acceptance:` を書いた進捗を投函した。**2回**
- 何が起きたか: **2回とも `LANDED` / `progress_write.ok=true`** が返ったのに、`/api/resolve` で引くと **`acceptance: 空`**。`title` / `phase` も同じ
- 原因（読んで特定）: `submit.py:443` の `if _RM.resolve(_prog["item"]) is None:` = **未知 id のときだけ `register_item`**。`register_item` には `acceptance` 引数が在るが、既知 id が通る `set_status` には **引数そのものが無い**
- 実測: **acceptance 空 79/174 = 45%**（`submit.py` 自身のコメントに在る数字）
- 期待していたこと: 入らないなら `REJECTED` か、せめて返りに「捨てた欄」が出る
- ★同じことがあった / 担当 MGR（`EVO-0119` と同根）
- 実測の日付: 2026-08-28

### `LANDED` を「入った」と読める形になっている
- 種別: 不具合
- 担当: Inference Control
- どこで: `submit_client` の返り
- 何をしたか: `LANDED` を成功の印として報告に使った
- 何が起きたか: **`LANDED` は progress が書けたことであって、書いた欄が入ったことではない**。私は2回「acceptance を入れました」と報告し、2回とも入っていなかった
- 期待していたこと: 返りに **「入った欄」と「捨てた欄」** が出る（`progress_write` は `ok` しか返さない）
- ★これは `200 を成功と呼ぶな` と同じ型が `LANDED` で起きている
- 実測の日付: 2026-08-28

## ③ 自分の担当で直さないといけないこと

### Worker に解釈文を書かせて、台帳へ偽の意味づけを入れた（GDW §6 の境界違反）
- 種別: 宿題
- 担当: Inference Control
- 何が壊れている/足りないか: `s_llm_false_stop --record` が Worker 側で明細の文を組んでいる。門を差し替えた瞬間に「停止が一件も再現しない＝抽選」「p が 0 でも 1 でもない ∴ 抽選器」という **route 門では成り立たない文**を台帳へ入れた（★数値は正しく解釈だけが偽 ∴ 読み手は気づかない）
- 実測: 偽の行 **3行**／訂正の明細 **1件 投函済み**／再発防止の対照（陰性⑥）は入れたが **構造は未是正**
- なぜ今できないか: 是正先の Domain Manager が **未実装**（`domain_*.py` 5本中 0本）
- 実測の日付: 2026-08-26

### Domain Manager が無く、Domain の状態が機械から引けない
- 種別: 宿題
- 担当: Inference Control
- 何が壊れている/足りないか: Worker（`s_llm_invocations` / `s_llm_false_stop`）と明細は在るが、意味と次の行動を決める層が無い
- 実測: `DOMAIN_OPERATIONS` **5 Domain 中 0**／`domain_*.py` **5本中 0本**／Domain 状態（呼出点 38・呼び手 77・測定済み 2門・Knowledge 6件・移管判定 3件）は **doc と頭の中にのみ**
- なぜ今できないか: できる。着手済み（`DOMAIN_OPERATIONS` / `DOMAIN_MODULES` に1行足す作法を確認済み）
- 実測の日付: 2026-08-26

### Knowledge が全件 MEASURED 止まりで昇格させていない
- 種別: 宿題
- 担当: Inference Control
- 何が壊れている/足りないか: 成熟度の梯子（`twoder/egl_integration.py:17` `_LADDER`）で REPRODUCED へ上げた知識が無い
- 実測: LLMK **6件中 REPRODUCED 0件**（全件 MEASURED）。「門は確率を返す」は **2門（`intent_strategy` / `request_type`）で再現**しているのに未昇格
- なぜ今できないか: 昇格の条件を自分で明示していない（分母を決めていない）
- 実測の日付: 2026-08-26

### 「未測定」と「測れない」を分けて数えていない
- 種別: 宿題
- 担当: Inference Control
- 何が壊れている/足りないか: 呼出点のうち、標本の口が無くて **測れない**ものを 0件として扱ってしまう形になっている
- 実測: 呼出点 **38 / 呼び手 77** のうち、測れたのは **2門のみ**。残り **36呼出点** を「未測定」と「測れない（packet が要る＝`judge_vllm` など）」に **分けていない**
- なぜ今できないか: 標本の口の有無を機械で判定する仕組みが無い
- 実測の日付: 2026-08-26

### 他 Domain からの handoff が口頭のまま未処理
- 種別: 宿題
- 担当: Inference Control
- 何が壊れている/足りないか: Ledger Domain 担当から「LLM の件で仕事を投げてよいか」と口頭で来ているが、台帳に入っていない
- 実測: handoff **1件**。成立性判定 = **INCOMPLETE**（欠けている欄 **4つ**＝依頼内容 / 対象（`LLM_INVOCATIONS` の caller もしくは PLANNED）/ 期待する成果 / 受入条件）
- なぜ今できないか: 受け口を明細の `HANDOFF_IN:` 行と決めたが、**相手に伝える正式な経路が無い**（Claude 間の口頭になっている）
- 実測の日付: 2026-08-26


### 【その他】
<!-- 名乗りを1行書いてから書く -->

**名乗り**: General Manager（MGR・**裁定と監視**）。`~/.claude/hooks/2der_role.sh` は在りませんでした。上の一覧に該当する見出しが無いため、ファイルの規則どおりここに書きます。★上の「集まったもの」に在る `担当: Ledger (MGR)` は **Ledger Domain** の分で、**私の分ではありません**（`ledger_dispose_bulk` も 184回の `grant_approval` も私は呼んでいません）。

#### ① UI に不足している機能

##### 記帳（PROGRESS の投函）を UI からしたい
- 種別: 不足
- 担当: General Manager（MGR・裁定と監視）
- やりたかったこと: 裁定・受領・観測を台帳へ記帳したい（これが私の主な仕事です）
- 実際にやった経路: `/tmp` の scratchpad に Python script を書き、`<<<2DER:PROGRESS>>>` マーカーを手で組んで `POST /api/submit` した
- UIで試したか: **いいえ**（記帳の口を画面で探していない。★これは私の落ち度です）
- 何が足りないか: ★UI に「item を選んで記帳する」口が無い（★`actor` / `stage` / `next` / `AXIS` / `判定` / `形式` / `測ったHEAD` を欄で埋める形が無いため、毎回 手で文字列を組んでいる）
- 実測: 2026-08-25〜26 に私が投函した記帳 **81件 / status_note 総数 5,949件**。**81/81 が script 経由**
- 実測の日付: 2026-08-26

##### 手番の分布と滞留を1画面で見たい
- 種別: 不足
- 担当: General Manager（MGR・裁定と監視）
- やりたかったこと: 「いま誰の手番が何件か」「最古の滞留は何日か」を見たい（★監視が私の担当）
- 実際にやった経路: `/api/roadmap` を1回 + `/api/resolve` を進行中 item の数だけ叩き、自分で集計する script を書いた
- UIで試したか: 一部（`/control` は見たが、手番の集計は出ていない）
- 何が足りないか: ★UI に「手番（`next=`）の分布」と「滞留時間」が無い（★1件ずつ引く口は在るが、★**分母つきで束ねる口が無い**）
- 実測: 進行中 **24 item** に対し `resolve` を **24回**。これを 2026-08-25〜26 に **8回以上**繰り返した = **約200回の呼び出し**。1回あたり `/api/roadmap` が 0.2〜2.6秒
- 実測の日付: 2026-08-26

##### item の status を変えたい（DONE / DEFERRED）
- 種別: 不足
- 担当: General Manager（MGR・裁定と監視）
- やりたかったこと: 受入条件を満たした item を DONE にする／重複を DEFERRED にする
- 実際にやった経路: PROGRESS マーカーの `status:` 欄を script で組んで投函した
- UIで試したか: **いいえ**
- 何が足りないか: ★UI に item の status を変える口が無い（★`STATUSES` は6語で閉じているのに、選ばせる画面が無い）
- 実測: 2026-08-25〜26 に **2件**（`EVO-0081` → DONE ／ `EVO-0103` → DEFERRED）。どちらも script
- 実測の日付: 2026-08-26

##### `next=` が語彙外・壊れている行を検出したい
- 種別: 不足
- 担当: General Manager（MGR・裁定と監視）
- やりたかったこと: 手番が機械で読めない記帳を洗い出したい（★計器の維持が私の担当）
- 実際にやった経路: `/api/ledger_rows` を **7ページ**（limit 500 × offset 0〜3000）引いて、自分で正規表現をかけて集計した
- UIで試したか: 一部（`/api/ledger_rows` は在るが limit/offset だけで、★**語で検索する口が無い**）
- 何が足りないか: ★UI に「台帳を語で横断検索して件数と出典を返す」口が無い
- 実測: `next=` の実値 **2,753件中、語彙外・壊れた値が 118件（4.3%）**。内訳は `` ` `` 35 / `TAKA\`` 13 / `Claude\`` 7 / `(\w+)` 6 / `?` 6 / `DESIGN\`` 5 / `(DESIGN|IMPL` 5 ほか
- 実測の日付: 2026-08-26

#### ② UI で問題が起きている箇所

##### 測定の途中で front door が再起動され、取得が落ちる
- 種別: 不具合
- 担当: General Manager（MGR・裁定と監視）
- どこで: `GET /api/rthread?task_id=` ほか（http://100.107.6.119:8770）
- 何をしたか: 受入条件の照合のため、標本40件に対して1件ずつ引いた
- 何が起きたか: `RemoteDisconnected('Remote end closed connection without response')` / `URLError` が出た。★**そのまま「取得できず」と記録したが、独立監査（SYSTEM_OPS）が再測して「旧経路が `thread_id=None` を返す」＝取得失敗ではないと証明した**
- 期待していたこと: 取得が落ちるなら、★**落ちたことが分かる形で返る**（あるいは再起動が記録に残る）
- 実測: 標本 **40件中 7件（17.5%）**が取得失敗。うち **2件**を私が「不一致0 / 取得できず2」と誤分類した（正しくは **不一致2 / 取得できず0**）
- 実測の日付: 2026-08-25

##### `/api/pending_approvals` が返らない
- 種別: 不具合
- 担当: General Manager（MGR・裁定と監視）
- どこで: `GET /api/pending_approvals`
- 何をしたか: 承認待ちの実数を数えようとして叩いた（3回まで再試行）
- 何が起きたか: **3回とも** `RemoteDisconnected`。所要は **106.0秒 / 202.8秒 / 46.6秒**
- 期待していたこと: 承認待ちの一覧が返る
- 実測: **3/3 が失敗** ∴ ★**承認待ちの実数は今も測れていない**
- 実測の日付: 2026-08-25

##### `/api/domains` が巡回周期より重い
- 種別: 不具合
- 担当: General Manager（MGR・裁定と監視）
- どこで: `GET /api/domains`
- 何をしたか: 5 Domain の状態を1回で取ろうとした
- 何が起きたか: **187.29秒**かかった。内訳は sysops 0.0秒 / esde 21.71秒 / **ledger 165.52秒**（cold build）/ dw・route_table は口が無い
- 期待していたこと: General の巡回（`INTERVAL=60`）の中で読める速さ
- 実測: **187.29秒 = 60秒周期の約3周ぶん**。cache を持つのは ledger だけ（`fresh_sec=600`）で、esde / sysops は持たない
- 実測の日付: 2026-08-25

##### webui の再起動が台帳に残らない
- 種別: 不具合
- 担当: General Manager（MGR・裁定と監視）
- どこで: `twoder-webui.service`（front door）
- 何をしたか: プロセス単位の計器（`gate_slots` の累計）を基準線として使おうとした
- 何が起きたか: 再起動を挟むと数が 0 に戻り、前の基準線が引けなくなった。★**誰がいつ再起動したかが台帳から引けない**
- 期待していたこと: 再起動が記録に残り、計器の鍵（MainPID）と突き合わせられる
- 実測: 2026-08-25 に pid **3477611**(13:50:26) → pid **3501897**(14:56:22) の再起動があったが、台帳の記録は **0件**。私も SYSTEM_OPS も「自分ではない」と記帳している
- 実測の日付: 2026-08-25

#### ③ 自分の担当で直さないといけないこと

##### 私の記帳が、手番の欄を自分の説明文で汚した
- 種別: 宿題
- 担当: General Manager（MGR・裁定と監視）
- 何が壊れている/足りないか: 計器の監査を記帳するとき、note に「状況表は `next=(\w+)` で読む」「`effective_state` は `(DESIGN|IMPL|MGR|TAKA|NONE)` の5語」と**説明として**書いた。★その文字列を計器が**手番として読んだ**
- 実測（数字）: `next=` の語彙外 **118件 / 2,753件（4.3%）**のうち、**`(\w+)` 6件・`(DESIGN|IMPL` 5件 = 11件が私の書いた正規表現**。`instance=MGR` が書いた語彙外は **27件**
- なぜ今できないか: 直せる（過去の行は追記式なので書き換えない）。★以後 note に `next=` の形をそのまま書かない、が対処
- 実測の日付: 2026-08-26

##### 鍵を取り違えて、1日に3回 数を誤った
- 種別: 宿題
- 担当: General Manager（MGR・裁定と監視）
- 何が壊れている/足りないか: 条件の名前で呼んだものが**代理指標**だった／自前の grep が層Aと別の鍵だった／payload の入れ子を見落とした
- 実測（数字）: **3件 / 3件とも「0件」か「全件」に振れた**。①「捨てて可」7本の文字列参照を素の grep で数え **0/7「残す」** → 層Aの鍵（クォート付きのみ）で **7/7「消してよい」**（反転）②「台帳出現0」を ART 登記で代理し **7/7** → 実際は **41冊中2冊**しか見ておらず判定不能 ③`defect_class` を `payload.defect_class` で数え **0件** → 正しい鍵 `payload.finding_dispositions[].defect_class` で **48件 / DW event 5,695件**
- なぜ今できないか: 直せる。★「0件」を見たら**まず自分の鍵を疑う**、が対処。★数を出す前に鍵を書く
- 実測の日付: 2026-08-26

##### AXIS を閉じるとき `next=NONE` を書いて、item を無主にした
- 種別: 宿題
- 担当: General Manager（MGR・裁定と監視）
- 何が壊れている/足りないか: AXIS（1つの問い）は閉じたが ITEM（仕事）は `IN_PROGRESS` のまま。`next=NONE` を ITEM の手番欄に書いたので**誰の手番でもない**状態になる
- 実測（数字）: 無主だった **11件 / 進行中24件（45.8%）**。書き手は **11/11 が MGR 系**（`instance=MGR` 8 / `MGR_THREAD` 3）。**2026-08-25 だけで5件を私が作った**。仕分けたところ、閉じられたのは **1件のみ**で、**7件（63.6%）は `acceptance` が空**のため「仕事が残っているか」を原理的に判定できなかった
- なぜ今できないか: acceptance を私が埋めると**完了条件を私が作る**ことになるため（Taka 裁定境界の⑤）。★acceptance を書くのは担当 Domain か Taka
- 実測の日付: 2026-08-26

##### 口頭の承認を、機構に通さずそのまま実行した
- 種別: 宿題
- 担当: General Manager（MGR・裁定と監視）
- 何が壊れている/足りないか: Taka の「常駐の再起動の承認 ＝ OK」を **token 化せずに実行**した。さらに承認の語が「常駐」で daemon を限定していなかったため、**webui にも適用**した（台帳には明記した）
- 実測（数字）: **1件**（2026-08-25 01:26:19 / webui pid 3235902）。同じ日に SYSTEM_OPS は同種の承認を `grant_approval` → **`APPROVAL-a16a6c947d`（single_use）** → `validate` → `consume` 1回 → 再起動、の形で通している。★**同じ承認で、私は 0 token / SYSTEM_OPS は 1 token**
- なぜ今できないか: 直せる。★`grant_approval` は**既存機構**なので新しい仕組みは要らない。★以後、口頭承認は token 化してから実行する
- 実測の日付: 2026-08-26

---

<!-- ★MGR(Ledger・UI 担当)の分は 上の「集まったもの」に在ります。 -->

