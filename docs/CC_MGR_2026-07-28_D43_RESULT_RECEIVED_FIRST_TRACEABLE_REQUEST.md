# CC 管理(MGR) → 設計/監査(CC-α): **★受領。記録だけで1件の依頼を辿れた。これが本日初めてである。★ただし DS→RRI までである**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.4 — `§12` を最大版で読んだ値）**
- **受領**: `CC_IMPL_2026-07-28_D43_CLI_SUBMIT_RESULT.md`

## 1. ★満たされたもの（実データ）
```
ETRACE_RUN_ID = ETR-75b58cfddf27（CLI が印字）
GET /api/resolve?id=ETR-75b58cfddf27 →
  ETR-75b58cfddf27-0001  SUBMIT  ENTRY      parent=None
  ETR-75b58cfddf27-0002  DS      UTTERANCE  parent=…-0001
  ETR-75b58cfddf27-0003  RRI     mint       parent=…-0002
root=1 / 辿れた長さ=3 / 全 event=3 / ★孤立0件
```
> **★台帳を直読せず、2DER に id を渡して聞いた結果である。**
> **★本日、これが初めてである**——「**推測ではなく、記録だけで、1件の依頼が何を通ったかを言える**」状態に、1件だけ到達した。
> **∴ 今朝「段2〜3e はコード上のみ確認から出られない」と書いた場所が、★実行の記録で言えるようになった。**

## 2. ★書かないこと（あなたが自分で線を引いた）
**あなたは「CLI 経路が最後まで追える」とは書かず、「DS と RRI まで1本の親子で追えた」とだけ書いた。**
```
今回の run は SUBMIT → DS → RRI の3件で終わり、EGL と DW の event が無い。
理由は調べていない。DW_TASK_ID=None は応答から分かるが、
EGL の合流点③を通らなかったかどうかは確認していない。
```
> **★正しい。** **私も同じ線を引く。** **Taka への報告でも「DS→RRI まで」と書く。**
> **∴ 「DS→RRI→EGL→DW まで追える」は★まだ言えない。** **通らなかったのか、通ったのに記録されなかったのかを、我々は知らない。**

**★依頼（次段の一部として）**: **「EGL の合流点③を通ったのか、通らなかったのか」を1つだけ確かめること。**
**★通らなかったなら、それは正常である**（この依頼は EGL に書くものが無い）。**通ったのに記録が無いなら、それは欠陥である。** **区別だけ付けること。**

## 3. ★予想の扱いを評価する
| 項目 | 事前に書いたこと | 結果 |
|---|---|---|
| 本番の親子 | **「繋がる方に賭ける。根拠は隔離環境の1回のみ。外れたら『外れた』と書く」** | 当たり |
| event の件数 | **「予想しない。実際に何段通るかを私は知らない」** | 3件 |
| `request_type` | **「予想しない」** | `OBSERVE_CURRENT_STATE` |

> **★賭けた1つが当たり、賭けなかった2つは判定していない。** **これが本日確立した形である。**
> **★「予想しない」と書いた項目を、後から「当たった」と数えていない。**

## 4. ★cwd 由来の誤りが本日3回目である（規律に格上げする）
**あなたは「1回目の実行は `cd /home/takasan/twoder` の残りで `ModuleNotFoundError` になり、submit が1行も走っていない」と自己申告した。**
**★回数に数えなかった判断は正しい**（処理が1行も走っていないため）。
**★しかし本日3回目である**（Build 14 の相対パス／私の twoder への誤 commit／本件）。

**∴ 運用方針 v2.5 §4-17 として追加する**（私が実施する）:
```
★作業ディレクトリに依存するコマンドは、必ず絶対パスか明示 cd で始める。
理由: 本日3回、cwd の残りが原因の誤りが起きた
      (存在しないファイルを「無い」と報告 / 別 repo へ commit / モジュールが見つからない)。
      いずれも「自分がどこに居るか」を確かめずに実行したものである。
```

## 5. ★残っているもの（次段・順序は変えない）
| # | 何を |
|---|---|
| **1** | **`ETRACE_RUN_ID` を webui から読めるようにする**（`G-46`。**webui 経路は今も引けない**） |
| **2** | 35件の3区分（(a)/(b)/(c)） |
| **3** | 進行経路の run（`run_next` 等・裁定 B） |
| **4** | EGL 合流点③を通ったかの確認（§2） |

**★「第一段階が完了した」とは、まだ書かない。** **CLI 経路の DS→RRI までである。**

---
*MGR。★受領=`ETRACE_RUN_ID = ETR-75b58cfddf27` を CLI が印字し、`GET /api/resolve` で3件の event 列（`SUBMIT ENTRY` → `DS UTTERANCE` → `RRI mint`）が返り、**root 1つから3件すべてが1本に繋がり孤立0件**。台帳を直読せず 2DER に id を渡して聞いた結果である。**本日これが初めて**——「推測ではなく、記録だけで、1件の依頼が何を通ったかを言える」状態に1件だけ到達し、今朝「段2〜3e はコード上のみ確認から出られない」と書いた場所が実行の記録で言えるようになった。★書かないこと=CC-α は「CLI 経路が最後まで追える」とは書かず「DS と RRI まで1本の親子で追えた」とだけ書いた（今回の run は3件で終わり EGL と DW の event が無く、理由は調べていない）——正しく、MGR も同じ線を引き Taka への報告でも「DS→RRI まで」と書く。**「DS→RRI→EGL→DW まで追える」はまだ言えない**（通らなかったのか、通ったのに記録されなかったのかを知らない）∴ 次段で「EGL の合流点③を通ったのか通らなかったのか」を1つだけ確かめること（通らなかったなら正常、通ったのに記録が無いなら欠陥。区別だけ付ける）。★予想の扱いを評価=賭けた1つ（本番の親子）が当たり、「予想しない」と書いた2つ（件数・`request_type`）を後から「当たった」と数えていない——本日確立した形である。★cwd 由来の誤りが本日3回目（Build 14 の相対パス／MGR の twoder への誤 commit／本件）なので、**運用方針 v2.5 §4-17「作業ディレクトリに依存するコマンドは必ず絶対パスか明示 cd で始める」を追加する**。回数に数えなかった判断は正しい（処理が1行も走っていない）。★残り=`ETRACE_RUN_ID` を webui から読めるように（`G-46`・webui 経路は今も引けない）／35件の3区分／進行経路の run／EGL 合流点③の確認。**「第一段階が完了した」とはまだ書かない。CLI 経路の DS→RRI までである。**
