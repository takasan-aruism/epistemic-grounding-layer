# 【監査＋判定案】**★値を見せても効かなかった** — ★私の予告は2回連続で外れた

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 09:4x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **受領**: `CC_IMPL_2026-07-31_D158_PASS_THE_VALUES_BUILT.md`
- **2DER 優先原則**: ①読み出しの口のみ ②受入1〜4＋補を実データで取り直した ③該当なし ④実装しない ⑤該当なし ／ **`:8005` 未使用**

---

# 1. ★判定案（★1条件に1つの印。★確定は MGR）

| # | 受入（★Taka 逐語） | ★判定案 | ★実データ（★私が引いた・打ち切り無し） |
|---|---|---|---|
| **1** | `cites_source_ids` が空でない | **★立つ** | `["OBS-00983","OBS-00984","OBS-00985","OBS-00986"]` |
| **2** | 対象 Observation の ID を引用する | **★立つ** | **★この task の `egl.source_refs` と完全一致** |
| **3** | **既に観測済みの能力を、新規作成対象として重複計画しない** | **★立たない** | steps: **「Implement process RSS collection ★using subprocess to run `ps` command」／「Implement GPU memory collection ★using subprocess to run `nvidia-smi`」／「Implement Docker container status collection ★using subprocess」**。`files_expected` = `memory_monitor.py` ＝ **★2DER が直前に実行した3つのコマンドを、★全部 作り直す計画である** |
| **4** | 証拠から不足が判定された場合だけ改修 Task を作る | **★立たない** | `unresolved_assumptions` は「The environment is Linux」「Python 3.6 以上」「`ps` と `ss` を実行する権限が在る」＝**★環境の一般的な仮定のみ。★観測から不足を1つも述べていない** |
| **★補** | **★実際に観測された値が本文に出るか** | **★立たない** | **★`qwen36_vllm` / `703029` / `4022456` / `30520` / `32607` / `RTX` / `VLLM` / `Up 5 days` / `rss` ＝★すべて出ない。** 出たのは `container` / `memory` / `used` の**★一般語のみ** |

```
★Last PASS  : ★受入2      ★First FAIL : ★受入3
★2DER 担当工程数の前回差分: ★0
```

---

# 2. ★先に決めておいた言い方を、そのまま使う

> ### **★「値を見せても効かなかった」**（`D-158` §5 で先に固定した言い方）
> ### **★「値も届かなかった」**（`D-159` §3 で先に固定した言い方）

**★言い換えない。★「概ね」と書かない。★立たなかったものを立ったように書かない。**

---

# 3. ★予告の当否（★外れを先に書く）

| # | 予告 | 結果 |
|---|---|---|
| **Q-2** | **補（実際に観測された値が本文に出る）は立つ・確信 中** | **★外れた。★2回連続で外した**（前回は「概念は届いたが値が届かない」、今回は「値も届かない」） |
| Q-1 | 分類は `OBSERVE_CURRENT_STATE` | ★当たった |
| 予告 task_id `TASK-2DER-5ABBA4A1` | — | ★当たった（★投入前に sha1 から計算） |
| Q-3 | **受入4 が立つとは予告しない** | ★予告しなかった。★立たなかった |

**★Q-2 を2回続けて外したことの意味**: **★「入力に入れれば出力に出る」という私の見立てが、★2回とも支持されなかった。**
**★∴ 次に同じ形の予告をするなら、★確信を下げるか、★予告しない。**

---

# 4. ★前回との比較（★「後退した」と1語で書かない）

| | 前回（コンテナ） | **今回（メモリを多く使っているプロセス）** |
|---|---|---|
| 受入3 | **○**（観測データを JSON で読む設計だった） | **★×**（`ps`・`nvidia-smi`・`docker` を全部 作り直す） |

```
★★依頼文の形は同じ（MGR が意図してそう作った）。★しかし対象が違う。
★★∴ 「2DER が後退した」とは書かない。★両方の実データを並べるだけにする。
★★言えること: ★値を渡す前（前回）は立った受入3 が、★値を渡した後（今回）は立たなかった。
★★言えないこと: ★どちらが原因か（★依頼の対象か／★渡した値か／★揺れか）。★1回ずつしか見ていない。
```

---

# 5. ★構造として分かったこと（★これが今回の収穫）

```
★値は★prompt に入った（★IMPL の実装）。★しかし★計画の本文には★1つも現れなかった。
★★∴ 「planner に見せる」ことと「planner が使う」ことは★別である。
★★これは、★我々が受入1・2 と 3・4 を分けたのと★同じ形が、★もう一段 深い所で出たものである:
     「引用した」≠「使った」  ／  ★「見せた」≠「使った」
★★∴ 入力に足すやり方では、★ここから先は届かない可能性が在る。
   ★これは★私の見立てであって、★実証ではない。★1回の観測である。
```

---

# 6. ★次に直す1件（★私は1件を出さない。★理由を書く）

```
★★私は「次の1件」を出さない。★出せる根拠が無いためである。
★理由: ★入力を足す方向（★prompt に id → prompt に値）を★2回 試して、★2回とも受入3・4 に届かなかった。
       ★3回目も同じ方向を出すのは、★「効かなかった」を無視して同じことを繰り返すことになる。
★★∴ ここは方向の選択であり、★MGR の裁定（あるいは Taka の判断）である。★私は材料を出す:
   (a) ★Ledger 再開（★Taka の元々の1番。★保留のまま在る）
   (b) ★planner の指示文そのものを変える（★「観測を使え」と書く）——★ただし★これは規則の追加に近い
   (c) ★ここで止めて、★「入力に足す方向は効かなかった」を成果として報告する
★★私の推し: ★(c) → その上で (a)。★理由: ★2回の実測が同じ向きに出ており、★これは報告に値する結果である。
```

---
**決めたこと**: **①受入1・2 は立つが 3・4 は立たず、補（実際の観測値が本文に出るか）も立たない——`qwen36_vllm`/`703029`/`30520`/`RTX` 等はすべて本文に出ず、steps は `ps`・`nvidia-smi`・`docker` を全部 作り直す計画だった ②先に決めておいた言い方をそのまま使う＝「値を見せても効かなかった」「値も届かなかった」 ③私の予告 Q-2 は2回連続で外れた（「入力に入れれば出力に出る」という見立てが2回とも支持されなかった）④前回○→今回× を「後退」と1語で書かない（対象が違い、1回ずつしか見ていない）⑤次の1件を私は出さない——入力に足す方向を2回試して2回とも届かなかったため、方向の選択は MGR/Taka の判断であり、私の推しは「ここで止めて『効かなかった』を成果として報告し、その上で Ledger 再開」。**
