# 【A】実装 → 設計/監査: **「届いたか」を 2DER に聞ける口を1つ作りました（★「あの送信が届いたか」は答えられません）**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=BUILT
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-30_D102_P1_ASK_2DER_IF_IT_ARRIVED_SPEC_v1_0.md`
- **受領した文書**: 上記 / `CC_MGR_2026-07-30_D102_APPROVED_AND_THE_MISSING_ID_IS_MINE.md`（写しで観測。**★受入10 の文言はこちらに従いました**）
- **登記**: `ART-ae789b58f7`（`twoder/webui.py`・**内容一致 True**・`modified_by=D-102`）

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 0. ★2DER 優先原則の5点（★私が実際にやった順・★実測）
| # | |
|---|---|
| **① どの入口を使うか** | **★無かった。★それを作るのが本 build** |
| **② 2DER で何を試したか** | **★まず既存の4口に聞きました**——`/api/state` `/api/tasks` `/api/resolve?id=RECV` `/api/control` |
| **③ できなかったこと** | **★どの口にも受信記録が無い。`/api/receipt` は `{"error": "not found"}`** |
| **④ 何を実装したか** | **★「届いたか」だけを返す口を1つ**（`GET /api/receipt`） |
| **⑤ 再実行して成立したか** | **★同じことを口に聞き直して、★答えが返りました**（§1 受入1・2） |

```
再現（★2026-07-30 12:40 頃・★log は読んでいません）:
  GET /api/state?task_id=TASK-2DER-7089D8FC  → http 200 / 受信記録の語 なし
  GET /api/tasks                             → http 200 / 受信記録の語 なし
  GET /api/control                           → http 200 / 受信記録の語 なし
  GET /api/receipt                           → http 200 / {"error": "not found"}
```
### 0-1. ★自分の入力を証拠に数えないこと（`G-23`）
> **★`GET /api/resolve?id=RECV` の返りには `RECV` の語が出ます。**
> **★しかしそれは私が投げたクエリがそのまま返っているだけです**（`{"id": "RECV", "resolved": false, "record": null}`）。
> **★これを「記録が在った」と数えていません。**

---

## 1. ★受入（★1条件に1つの印・まとめていません）
| # | 条件 | 結果 |
|---|---|---|
| **1** | `GET /api/receipt` が 200 を返す | **★200** |
| **2** | 返る欄が5つだけ | **★5つ**（`last_recv_at` / `recv_count` / `last_sent_at` / `last_sent_status` / `readable`） |
| **3** | 依頼文・task_id・状態を返していない | **★返していません**（実測で該当欄0・ソースでも入れていません） |
| **4** | log の行そのものを返していない | **★返していません**（§2。行の変数は返り値に入りません） |
| **5** | `recv_count: 0` と `readable: false` が別の欄 | **★別**（さらに**読めないときの `recv_count` は `null`**・§2-1） |
| **6** | 読めないときも 500 にならない | **★済（関数で実測）／★端点では確かめていません**（§4-1） |
| **7** | 画面に「0件」と「引けなかった」が別の文で出る | **★済**（§3・配信物に2文とも在り） |
| **8** | 画面に「1件ごとには分かりません」が出る | **★済**（§3） |
| **9** | `/api/state`・`/api/submit`・`/api/resolve`・`/api/tasks` の返りを変えていない | **★済**（§5。**★ただし私は一度 計器を間違えました**・§5-1） |
| **10** | **MGR が log を読まずに「直近の受信がいつか」「何件 受けたか」を言えるか**（MGR 改訂版） | **★言えます**（§1-1）。**★私は本 build で log を1行も読んでいません** |
| **11** | 配信物の JS が `node --check` | **★OK**（`/command`・`/` とも） |
| **12** | 非回帰 91 passed / 7 failed | **★一致**（顔ぶれ diff 空・走らせ方は §6） |
| **13** | 色を足していない | **★済**（差分の追加行に `ok`/`bad` 0件） |

### 1-1. ★受入10 の実測（★口に聞いただけ）
```
GET /api/receipt →
{"last_recv_at": "2026-07-30T08:47:43.103799", "recv_count": 60,
 "last_sent_at": "2026-07-30T08:47:46.016987", "last_sent_status": "200", "readable": true}
```
| ★言えること | **★直近の受信は `2026-07-30T08:47:43`。★これまでに 60件。★口に聞いただけで言えました** |
|---|---|
| **★言えないこと** | **★その60件が誰の送信かは出せません**（★記録に id が無い）。**★Taka の分が何件かも出せません** |

### 1-2. ★追記（12:57 実測）——★値が動きました。★私が押したのではありません
```
12:47 の実測: last_recv_at 2026-07-30T08:47:43  /  recv_count 60
12:57 の実測: last_recv_at 2026-07-30T12:53:07  /  recv_count 67   ← ★+7件
```
| **★私が押したか** | **★押していません**（§8。★`/api/submit` を1回も押していません） |
|---|---|
| **★誰が押したか** | **★出せません。★口では答えられません**（★記録に id が無い。★これが本 build の限界そのものです） |
| **★同時に起きたこと** | `ds` / `rri` / `egl` / `dev-workcell` の台帳が未commit になりました（★`git status` の**ファイル名だけ**で見ました。**★中身は読んでいません**＝台帳の直読をしていません） |

> **★これは口が初めて実務で答えた例です。** **★私が知らなかった受信を、★口が数えて返しました。**
> **★従来なら log を読むしかなかった問いです。**
> **★ただし「7件 誰が押したか」は答えられません。** **★「あと1段で分かる」とも書きません**（★id を足すのは【C】）。
> **★§1-1 の 60件 は 12:47 の実測値です。** **★取り消しません。** **★動く値だと分かるように、両方 残します。**

## 2. 実装（★2箇所・追加のみ）
```python
# webui.py::receipt_view — ★数と時刻だけ。★行そのものは返さない
out = {"last_recv_at": None, "recv_count": None,
       "last_sent_at": None, "last_sent_status": None, "readable": False}
try:
    with open(RUNS / "submit_access.log") as f:
        lines = f.read().splitlines()
except Exception:
    return out                              # ★引けなかった。★件数を 0 と書かない
…
    if len(t) < 2:  continue                # ★読めない行は数えない（数え方を推測しない）
    elif t[1] == "SENT":
        out["last_sent_status"] = None      # ★この行の status だけを見る（前の行を持ち越さない）
out["recv_count"] = n                       # ★読めた ∴ 0 は「受けていない」を意味する
out["readable"] = True
```
```python
if u.path == "/api/receipt":                # Phase 3 P-1 (D-102): 「届いたか」だけを返す
    return self._send(receipt_view())
```
- **★認証は既存のまま**（`do_GET` の先頭で `_auth_ok`）。**★新しい認証を作っていません。**
- **★`_recv_log`（書く側）を触っていません。** **★読む口を作っただけです。**
- **★新しい台帳を作っていません。★EGL を触っていません。★勘定科目に触っていません。**
- **★LLM を使っていません。★キャッシュも並列化もしていません。**

### 2-1. ★`recv_count` を `null` にした理由（★実装源より1段 強くしました）
> **★実装源は「`recv_count: 0` と `readable: false` を別の欄にする」までを求めていました。**
> **★私はさらに、★読めなかったときの `recv_count` を `0` ではなく `null` にしました。**
> **★理由: `0` を残すと「受けていない」と「引けなかった」が同じ欄で同じ形になります。**
> **★これは今日ずっと潰してきた形です**（★「測って0」と「測れない」を同じに書かない）。
> **★欄は5つのままです。★増やしていません。**

## 3. 画面（★足したのは1行＋注記1行）
```
※ 2DER に聞いた受信の状態: 最後に受けたのは 2026-07-30T08:47:43.103799 ／ これまでに 60件
   1件ごとには分かりません（受信の記録に、どの依頼かを示す番号が無いためです）。
```
**配信物に在る3通りの文**（★実測・`grep`）:
```
※ 2DER に聞いた受信の状態: 最後に受けたのは …          ← 受けた記録が在るとき
これまでに受けたのは 0件 です。                          ← ★受けていないとき
受信の記録を引けませんでした（受けていないのか、記録を引けなかったのかは区別できません）。  ← ★引けなかったとき
```
- **★「0件」と「引けなかった」は別の文です。** **★同じ文にまとめていません。**
- **★色を使っていません。** **★「正常」「OK」と読める語を使っていません**（★0件は良い知らせではありません）。
- **★`last_sent_at` / `last_sent_status` は画面に出していません**（★実装源 §4 は「1行だけ」）。**★口からは返ります。**
- 呼ぶのは**起動時に1回**と**送信を押した後に1回**です。**★送信後は答えを待ちません**（★押した結果の表示を遅らせないため）。

## 4. ★確かめていないこと
1. **★返ってきた値が正しいかは確かめられません。** **★我々は log を読めません。**
   **★確かめられたのは「答えが返ること」「答えが2つの意味を持たないこと」までです。**
   **★「正しい値が返った」とは書きません**（★実装源 §6-1 と同じ形——★自分の成果を自分で確かめられない）。
2. **★ブラウザで画面を見ていません。** **★配信物に文言と道が在ることまでしか示していません。**
3. **★60件の内訳を見ていません**（★出せません。★id が無いため）。

### 4-1. ★受入6 を「関数では確かめた／端点では確かめていない」と分けて書きます
```
確かめた  : 別プロセスで RUNS を存在しないパスに向け receipt_view() を呼び、
            例外を出さず {"readable": false, "recv_count": null, …} を返した（★実ファイルには触っていません）
確かめていない: ★その状態のサーバに GET して 200 が返ること
            （★実 log を読めない状態にすれば確かめられますが、★計器を壊すのでやりません）
```

## 5. ★既存4口を変えていないこと（★2通りで示します）
```
① 同じ計器で改修前後を比べる（★文字数）:
   /api/state 5105 → 5105 ／ /api/tasks 3454 → 3454 ／ /api/control 3914 → 3914 ／ /api/resolve 68 → 68   ★全一致
② 旧コードと新コードを★同じデータ・同じ瞬間で走らせる:
   build_state  旧 5105 == 新 5105   ★一致
   build_report 旧 15752 == 新 15752 ★一致
```
- **★私の口の欄（`receipt`/`recv_count`/`last_recv`/`readable`）が4口に漏れていないことも実測しました**（★該当0）。

### 5-1. ★私が計器を間違えました（★先に書きます）
> **★最初の比較で「`/api/state` と `/api/control` に差が在る」と出ました。**
> **★原因は私の計器です。** **★改修前は `${#out}`（文字数）、改修後は `wc -c`（バイト数）で測っていました。**
> **★日本語が入るので一致しないのが当然でした**（★5105 文字 = 5623 バイト）。
> **★「差が在る」と報告する前に単位を揃え直し、★全一致を確かめました。**
> **★推測で「たぶんデータが変わっただけ」と書かず、★旧コードと新コードを同じ瞬間に走らせて決めました**（§5 ②）。

## 6. ★非回帰の走らせ方（`G-74`）
```bash
/home/takasan/.cc_tmp/d42/run_regression.sh /home/takasan/egl/docs/.cc_tmp/d49/regression_d102.txt
```
**中身**: `cd /home/takasan` してから `twoder/regression/*.py` の**98本**を1本ずつ
`timeout 600 python3 -m twoder.regression.<名前>` で走らせ、**終了コードで数える**（`pytest` は使っていません）。
```
結果: DONE 91 passed / 7 failed
基準（D-95）との diff: ★空（失敗7本の顔ぶれまで一致）
```

## 7. ★MGR が書かせたこと・書かせなかったこと（`CC_MGR_..._D102_APPROVED_...` §3）
| ★書きません | **「届いたかが分かるようになった」** |
|---|---|
| **★書きます** | **★「あの送信が届いたか」は★答えられません。★記録に id が無いためです** |

> **★本 build が満たすのは「受信状態」までです**——**★直近がいつか・何件か。**
> **★満たさないのは「あの依頼の受信状態」です。**

## 8. ★この build でやらなかったこと
- **`RECV` に依頼を特定する id を足す** → **★やっていません**（★【C】・MGR 裁定で件数 13→14）。
- **Phase 3 の Priority 2〜5** → **★手を出していません。**
- **同じ「無い」を6通りで言っている／注記が11〜12px** → **★持ち越しのまま。消していません。**
- **★投入していません**（★`/api/submit` を1回も押していません。**∴ `recv_count` を私が増やしていません**）。

### 8-1. ★口の限界（★別 build の材料として置きます。★掘りません）
- **★読めない行は数えません。** **★何行 読めなかったかを返す欄は在りません**（★実装源が欄を5つに固定しているため）。
- **★`recv_count` は `/api/submit` への POST の数です。** **★他の口の受信は数えていません。**

## 9. 触ったファイル / commit / 再起動
```
twoder/webui.py のみ（55 insertions / 1 deletion）
```
- **★再起動しました**（配信物で検査するため・訂正後の順番②）。**起動 12:44:07。**
- **commit していません**（MGR）。**資料の同期はしていません**（★MGR の commit 後に設計）。

---
*IMPL BUILT（D-102・Phase 3 P-1・【A】）。★**2DER 優先原則の5点を実測で埋めた**——**入口は無く（それを作るのが本 build）、まず既存4口（`/api/state`・`/api/tasks`・`/api/resolve`・`/api/control`）に聞き、どこにも受信記録が無く `/api/receipt` は `{"error": "not found"}` だったので、「届いたか」だけを返す口を1つ実装し、同じことを聞き直して答えが返った**（★log は読んでいない）。★**自分の入力を証拠に数えない**——`/api/resolve?id=RECV` の返りに `RECV` の語が出るのは**私が投げたクエリの反射**（`resolved: false`）であり記録ではない（`G-23`）。★実装は**2箇所・追加のみ**=`receipt_view()` と `GET /api/receipt` の1行で、**返すのは `last_recv_at`/`recv_count`/`last_sent_at`/`last_sent_status`/`readable` の5欄だけ**、**行そのものは返さず**、**認証は既存のまま**、**`_recv_log`（書く側）も EGL も新しい台帳も勘定科目も触らず**、**LLM もキャッシュも並列化も無し**。★**実装源より1段 強くした点**=**読めなかったときの `recv_count` を `0` ではなく `null` にした**——`0` を残すと「受けていない」と「引けなかった」が同じ欄で同じ形になり、**今日ずっと潰してきた形**になるため（**欄は5つのままで増やしていない**）。★画面は**1行＋注記1行**で、配信物に**3通りの文**（受けた記録が在るとき／**0件のとき**／**引けなかったとき**）が別々に在り、**色も「正常」「OK」と読める語も使わず**、**`last_sent_*` は画面に出さない**（実装源 §4 は1行だけ・口からは返る）。★受入13件を**1条件に1つの印**で判定し、**受入6 は「関数では確かめた／端点では確かめていない」と分けて書いた**（★実 log を読めない状態にすれば確かめられるが計器を壊すのでやらない）。★**受入10（MGR 改訂版）は満たした**——**`GET /api/receipt` に聞くだけで「直近の受信は 2026-07-30T08:47:43」「これまでに 60件」と言えた。★私は本 build で log を1行も読んでいない**。**ただしその60件が誰の送信かは出せない（記録に id が無い）**。★**確かめていないこと**=**返ってきた値が正しいかは確かめられない（我々は log を読めない）。確かめられたのは「答えが返ること」「答えが2つの意味を持たないこと」までであり「正しい値が返った」とは書かない**（実装源 §6-1 と同じ形＝自分の成果を自分で確かめられない）／**ブラウザを開いていない**／**60件の内訳を見ていない**。★**既存4口を変えていないことを2通りで示した**=同じ計器（文字数）で改修前後が全一致（5105/3454/3914/68）＋**旧コードと新コードを同じデータ・同じ瞬間で走らせて `build_state` 5105==5105・`build_report` 15752==15752**、加えて**私の欄が4口に漏れていないことも実測（該当0）**。★**私が計器を間違えたことを先に書いた**——最初は「差が在る」と出たが**原因は改修前 `${#out}`（文字数）／改修後 `wc -c`（バイト数）という自分の単位不統一**（5105 文字 = 5623 バイト）であり、**推測で「データが変わっただけ」と書かず単位を揃え直し、旧新を同じ瞬間に走らせて決めた**。★非回帰は **91 passed / 7 failed・顔ぶれ diff 空**（走らせ方を明記）。★**MGR が書かせたこと**=**「届いたかが分かるようになった」とは書かない／「あの送信が届いたか」は答えられない、記録に id が無いためであると書く**——**本 build が満たすのは「受信状態」（直近・件数）までで、「あの依頼の受信状態」は満たさない**。★やらなかったこと=**`RECV` に id を足す（【C】・件数13→14）**／Priority 2〜5／D-62 の持ち越し2件／**投入していない（`/api/submit` を1回も押しておらず `recv_count` を私が増やしていない）**。★**口の限界を別 build の材料として置いた（掘らない）**=**読めない行は数えず、何行 読めなかったかを返す欄が無い**（実装源が欄を5つに固定）／**`recv_count` は `/api/submit` への POST の数であり他の口の受信は数えていない**。触ったのは `twoder/webui.py` のみ（55 insertions / 1 deletion）、**再起動済（12:44:07）**、**登記 `ART-ae789b58f7`（内容一致 True・`modified_by=D-102`）**、commit・資料同期はしていない。*
