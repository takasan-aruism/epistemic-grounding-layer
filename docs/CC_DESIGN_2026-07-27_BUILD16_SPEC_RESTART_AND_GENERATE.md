# BUILD SPEC — Build 16: **再起動 → 修理を応答で確認 → `GENERATE` を1段 → その場で受け取る**

- **`BUILD_ROLE: ★実装源`**（本 build task の唯一の実装源）
- **宛: IMPL（coder）** / 写: MGR / Taka
- 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=BUILD_SPEC / v1.0
- **運用方針 確認済（版: v1.9）**
- 権限: `CC_MGR_2026-07-27_RESTART_IS_ROUTINE_NOW.md`（再起動は常設手順）/ `..._BUILD15_AUDIT_RECEIVED_PROCEED.md`

## 0. 差分は確認済（私が出した）
**前回の再起動（07-27 18:37:58）以降、`twoder` のコード commit は0件で、変わったのは Build 15 の修理2ファイルだけである**（`CC_DESIGN_2026-07-27_BUILD15_AUDIT_AND_RESTART_DIFF.md` §3）。
**∴ 今回の再起動で新たに効くのは Build 15 の修理だけ。** **前回のような「8 commit がまとめて効く」状況ではない。**

---

## 1. 手順（①〜⑦を1つの作業として続けて行う）

### 再起動（常設手順・省略しない）
| # | 手順 |
|---|---|
| **①** | **再起動前**: 旧 pid / 旧起動時刻 / `twoder/authority.py` と `twoder/generate_via_runner.py` の mtime を記録 |
| **②** | **停止 → 起動**: **既存の起動手順 `python3 -m twoder.webui 8770`**。**新しい起動方法を作らない**。停止時刻・起動時刻・新 pid・**使ったコマンドそのもの**を記録 |
| **③** | **起動時刻 > ソース mtime** を確認 |
| **④** | **★修理が入っていることを応答の形で確認する**（MGR §2-1-4「『起動した』だけでは足りない」）。**方法は §2** |

### 生成と受け取り
| # | 手順 |
|---|---|
| **⑤** | **投入前の `/tmp/2der_runner_*` の数を数える** |
| **⑥** | **`/api/submit` に Build 13/14 と同一の依頼文を1回**（機械抽出・1文字も変えない）→ **`/api/run_next` を1回だけ**（`{"task_id":"TASK-2DER-21F64D9D"}`）。**`run_until_barrier` を使わない** |
| **⑦** | **★その場で受け取る**: 増えた `2der_runner_*` を特定し、**全ファイルを `dev-workcell/contracts/out/SANDBOX_ARTIFACT-TASK-2DER-21F64D9D/` へ保全**（**既存の同名ディレクトリが在るなら別名にし、上書きしない**）、`MANIFEST.json`（各ファイルの**相対パス / sha256 / バイト数**、**元の sandbox 絶対パス**、`TASK_ID`、**受け取った時刻**、前後の sandbox 数）。**そして止まる** |

---

## 2. ★④ をどう確かめるか（新プロセスが修理を持つことの、応答による証拠）
**`GENERATE` を走らせる前に、次を1回実行する:**
```
新しい webui プロセス内で mint_token(1, task_id="PROBE-RESTART") を評価し、
その id を approval_registry._load_grant で引いて task_id を見る
```
- **修理が入っていれば `task_id` は `PROBE-RESTART`（素）** である。
- **入っていなければ `PROBE-RESTART#attempt-1`** になる。
- **★これは「起動した」ではなく「修理が効いている」の証拠である。**
- **【未確認】** **webui プロセス内で任意のコードを評価する手段があるかを、私は確認していない。** **無ければ「その手段が無い」と書いて、③のみで進めてよい。** **無理に作らない。**
- **★台帳に試験行を残さないため、`DS_DATA_DIR` を一時ディレクトリへ隔離して行うこと**（Build 15 で IMPL がやった形）。

---

## 3. 予想を先に書く（実測前に固定）
| 項目 | DESIGN の予想 |
|---|---|
| 再起動 | 成功する |
| ④ の `task_id` | **素（`PROBE-RESTART`）** |
| `dispatched` | **`true`** |
| token gate | **★通る**（Build 15 の修理が効くため） |
| 新しい `2der_runner_*` | **1つ増える** |
| **成果物** | **★出る方に賭ける**（`impl.py` ＋ `test_impl.py`） |
| **供給した T1〜T8** | **通る方に賭ける** |
| **held-out**（`[]` `""` `0` `False` / `known_prefixes` 空） | **★少なくとも1つ落ちる方に賭ける**（判定は設計/監査。IMPL は判定しない） |
| 進行後の状態 | **`READY_FOR_AUDIT`** |

**★外れたら「外れた」と書く。**

---

## 4. やってはいけないこと
1. **成果物の中身を評価しない。テストを足さない。修正しない。**
2. **★受入オラクルを開封しない**（場所も内容も見ない）。
3. **配置しない・登記しない・配線しない。**
4. **2段以上進めない**（`AUDIT` へ行かない）。
5. **失敗しても手で書かない。** **`problems` / `test_result` を逐語で記録して上げる。**
6. **`run_until_barrier` を使わない。token を迂回しない。**
7. **`B9B4DA3B` / `D6A93450` に触らない。**
8. **本番コードを変更しない**（本 build は再起動と実行のみ）。
9. **`twoder/runs/*.trace.json` を読まない。**
10. **`CC_REGISTER.jsonl` に試験行を書かない**（試験は隔離環境で）。
11. **既存の保全ディレクトリを上書きしない。**

## 5. BUILT に置く定型見出し（そのまま）
```
## 到達経路
- [ ] (A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。
- [ ] (B) 〇〇（経路名）を経て届いた。

## 再起動（常設手順）
- 旧 pid / 旧起動時刻 / ソース mtime:
- 停止時刻 / 起動時刻 / 新 pid / 使ったコマンド:
- [ ] 起動時刻 > ソース mtime
- ④ 応答による修理の確認: [ ] 素の task_id を確認 / [ ] 手段が無く未実施（③のみ）

## 結果の区分（1つに丸）
- [ ] SANDBOX_ARTIFACT_READY（成果物が出て、受け取れた）
- [ ] GENERATION_FAILED（worker が出せなかった／テストが通らなかった）
- [ ] SKELETON_VIOLATION（骨格の固定区間が保存されなかった）
- [ ] TOKEN_GATE_BLOCKED（token gate で再び止まった）
- [ ] ARTIFACT_LOST（生成されたが受け取る前に失われた）
```

## 6. そのほか出すもの
1. `/api/run_next` の応答全文。
2. `derive_state('TASK-2DER-21F64D9D')` と events。
3. **★`generate_runs` の payload を逐語で全文**（`test_result` / `problems` / `artifact_sha256` / `contract_source`）。**要約しない。**
4. **保全先パスと `MANIFEST.json` 全文。**
5. **成果物のファイル名と行数のみ**（**中身を貼らない・評価しない**）。
6. §3 の予想と実際の表。**外れに「外れた」と書く。**
7. 各操作1回ずつ・本番無変更・**commit しない**・冒頭に「運用方針 確認済（版: v1.9）」。

## 7. 位置づけ（緩めない）
- **成果物が出ても「2DER が作れるようになった」と書かない。** **1回・1件である。**
- **供給したテストが通っても、依頼を満たした証拠ではない**（**渡していない検査が別に在る**）。
- **1回の観測で常態を判定しない。**

---
*BUILD SPEC v1.0（★実装源）。Build 16=再起動→修理を応答で確認→`GENERATE` を1段→その場で受け取る。差分は確認済（前回の再起動以降 `twoder` のコード commit は0件・効くのは Build 15 の修理だけ）。再起動は常設手順で省略しない（旧 pid/起動時刻/mtime→既存コマンドで停止起動→起動時刻>mtime→★応答の形で修理を確認）。④の確認方法=新プロセスで `mint_token(1, task_id="PROBE-RESTART")` の id を `_load_grant` で引き `task_id` が素かを見る（`#attempt-1` なら修理が入っていない）。手段が無ければ「無い」と書いて③のみで進めてよい・無理に作らない・台帳に残さないため `DS_DATA_DIR` を隔離。予想を固定=再起動成功／④は素／token gate は通る／sandbox が1つ増える／**成果物は出る方に賭ける**／供給した T1〜T8 は通る／**held-out は少なくとも1つ落ちる**／`READY_FOR_AUDIT`。禁止=中身を評価しない・オラクル非開封・配置/登記/配線しない・2段以上進めない・手で書かない・gate 迂回なし・他 task に触らない・本番無変更・TRACE 横読み禁止・台帳に試験行を書かない・既存の保全ディレクトリを上書きしない。区分5択。成果物が出ても「作れるようになった」と書かない。*
