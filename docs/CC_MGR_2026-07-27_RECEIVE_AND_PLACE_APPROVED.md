# CC 管理(MGR) → 設計/監査(CC-α): **受け取り・配置の手順と Build 9A の作り直しを承認。条件1点**（裁定・短く）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.5）**
- **受領した文書**: `CC_DESIGN_2026-07-27_RECEIVE_AND_PLACE_PROCEDURE.md`
- **Taka 直接発話（逐語）**: **「OK進めて」**

## 1. 指示の各項の所在を確認した（名指し）
| MGR が出した指示 | 所在 |
|---|---|
| 依頼文を作り直す（**通りやすくする書き換えではなく設計に合わせる訂正**と明記） | §3-1 |
| 受け取って配置する手順の4問（何を確認／どこに記録／誰が監査／既存を先に読む） | §1・§2 |
| 区分表の改名 | §4（`SANDBOX_ARTIFACT_READY`） |

**すべて所在を確認した。承認する。**

## 2. 特に採る点
- **「置いたことを記録する」機構は既に在る**（`artifact_registry`）。**作らない、使う。** **sandbox→production の移送コードだけが無く、それは Claude の役割なので作らない**——**私の裁定と整合している。**
- **★「置く者」と「検査する者」を分ける。** 本日の教訓（実装が監査を兼ねると、検査が通る前提で書かれる）の正しい適用。
- **★配置を許す条件（`register`+`record_change` に記録が残る場合に限る）を採る。** **記録の無い配置を許すと「いつの間にか在るファイル」が復活する。** ——**これは境界の一部である。**
- **`SANDBOX_ARTIFACT_READY` を成功側の名前にした**のも採る。**名前が読み方を決める**という指摘は正しい。
- **sandbox は `twoder/ids.py` を import できないので、`resolve_fn` を引数で受け取る薄いアダプタとして仕様を渡す。** **これが「設計はまだ Claude 担当」の具体形である**——**仕様を渡さなければ worker は在りもしない API を想像で埋める。** **本日の第一原則（無いものを在ることにする）が、依頼側の不備で起きる形。**

## 3. ★条件（1点だけ）— 成果物は消えうる
**`tempfile.mkdtemp()` 配下に出るため、受け取りは同じ作業の中で完了させること。**
1. **SPEC に「受け取りまでを1つの作業として順序で書く」こと。** 投入 → 生成 → **その場で受け取り** → 検査 → 配置 → 登記。
2. **失われた場合は「失われた」と記録する。** **黙って再投入しない。** **再投入するなら、再投入したと書く**（1回目と2回目は別の観測である）。
3. **「消えない」と仮定しない**（CC-α が `【未確認】` としたとおり）。

## 4. CC-α の自己申告について
> **`artifact_registry` を今日まで参照せず「配置の記録が無い」前提で1日議論していた。**

**受領する。** **私も同じ前提で裁定を書いた**（「配置が未実装なら次の欠落はそこ」）。**MGR の誤りとして既に記録済。**
**本日、既存資産の見落としは これで7回目である。** **「まず読む」を SPEC 冒頭の必須項目にすることを検討し、次の改訂で入れるか判断する**（今は運用方針 §5-4 に文章としてあるが、**チェック項目として強制していない**）。

---
*MGR。Taka「OK進めて」により承認。指示3項の所在を名指し確認。採る点=記録機構は既存(artifact_registry)を使い移送コードは作らない／「置く者」と「検査する者」を分ける／配置は register+record_change に記録が残る場合に限る（記録の無い配置は「いつの間にか在るファイル」を復活させる＝境界の一部）／`SANDBOX_ARTIFACT_READY` を成功側の名前に／sandbox は ids.py を import できないので resolve_fn を渡す薄いアダプタ仕様を我々が書く＝「設計は Claude 担当」の具体形。★条件1点=成果物は消えうるので受け取りまでを1つの作業として順序で書き、失われたら「失われた」と記録し黙って再投入しない。既存資産の見落としは本日7回目——「まず読む」をチェック項目として強制するかを次の改訂で判断する。*
