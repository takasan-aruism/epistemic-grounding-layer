開発者規律 確認済(v1.0)

# 【起草】★Work Relay Protocol ―― ★★1本が **端から端まで 結合した**（★最小実証）

宛: MGR ／ 写: IMPL ／ 発: DESIGN ／ 2026-08-16 02:4x ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **MGR 02:35**（★起草=DESIGN ／ ★基本設計と受領=MGR ／ ★入れる数と ★書かない事を 指定）

---

## 0. ★★★先に 数の 訂正 2件（★あなたの 指定と 私の 実測が 違った）

**★引いた 時刻 = 2026-08-16 02:42:24 ／ ★引いた 口 = `GET /api/control?include=observed_edges`**

```
★★★訂正① ―― ★`identity` = ★あなたの 指定『★1〜2 / 87』 → ★★実測 ★★3 / 87
   ★名前 = ★`DS.reconstruct_snapshot` ／ ★`EGL.admit_design_evidence` ／ ★`WORKER.received_from_runner`

★★★訂正② ―― ★あなたの 指定『★出たのは S03 と S06 ／ ★S07 は まだ SEND_ONLY』
   → ★★実測は ★★★逆 = ★出たのは ★★S03 と ★★S07 ／ ★★★S06 は ★位置を 名乗っていない
   ★実測（★同じ 引きの 中）:
     ★`HANDOFF.S03 → DS.reconstruct_snapshot`   … `BOTH` 33 ／ ★locator ★在り
     ★`HANDOFF.S06 → RRI.mint`                  … `BOTH` 69 ／ ★★locator ★★無し
     ★`HANDOFF.S07 → EGL.admit_design_evidence` … `BOTH`  1 ／ ★locator ★在り
     ★`RUNNER.hand_to_worker → WORKER.received_from_runner` … `BOTH` 21 ／ ★locator ★在り

★★★食い違いの 正体 = ★★『両側に 出た（`BOTH`）』と ★★『位置を 名乗った（`locator`）』は ★別の 事
   ―― ★`S06` は ★69回 両側に 出ている が ★★1回も 位置を 名乗っていない
   ―― ★★これは ★不具合では なく ★★★計装していないだけ（★★S06 の 受け手に `at` が 無い）
   ―― ★★★∴ ★本文の 数は ★★実測を 採る（★あなたの 指定を そのまま 書くと ★盛った 事に なる）
```

## 1. ★★1本で 通った 型（★★3行・★Taka 指示＝★どこでも 同じ 形で 使う）

```
★★① ★送り手は ★★相手の 鍵を ★その場に 書く      （★経路表を 引かない＝★循環しない）
★★② ★受け手は ★★自分の 名前と 位置を 名乗る      （★後から 復元しない＝★14/86 で 成り立たなかった）
★★③ ★両方を ★★同じ 走行の 番号で 結ぶ            （★別々の 走行なら 揃ったと しない）
```

**★★★この3行が 揃った 時だけ ★`linked` と 書く。** ―― ★1つでも 欠けたら ★★欠けた 物の 名前が 出る。

## 2. ★★実証（★★数だけ・★盛らない）

**★対象 = `TASK-2DER-6DDDDCA9` ／ ★記録 16件 ／ ★走行 `ETR-ebff7e07f115`**

```
★★★`linked` = ★★1
   ★区間 = ★`RUNNER.hand_to_worker → WORKER.received_from_runner`
   ★`run_id` = ★`ETR-ebff7e07f115`（★送りと 受けが 同一）
   ★`locator` = ★`/home/takasan/twoder/qwen_worker.py::run`

★★同じ 走行の 他 2本 =
   ★`MANAGER_V0.submit_contract → FRONT_DOOR.submit` … ★`no_receive`
   ★`MANAGER_V0.tick → RUNGATE.receive`              … ★`no_locator`

★★★2人が 別々に 測って 一致（★MGR ／ ★DESIGN）
   ―― ★DESIGN 側の やり方 = ★生の 記録を front door から 引き ／ ★材料を 自分で 組み ／
      ★置かれた 部品（★`twoder/relay_chain.py` ／ sha `4c32bac5f00b6ef5`）を ★写して 外で 実行
   ―― ★★同じ 部品・★別の 材料の 組み方 で ★同じ 答え
```

## 3. ★★足りない 物（★★名前で・★★『残り』と 数えない）

```
★★`no_receive`  = ★★受け手が ★1行も 名乗っていない（★入口の 側）
★★`no_locator`  = ★★受け手は 名乗るが ★★位置を 書いていない（★例＝★`S06` `RRI.mint` ／ `BOTH` 69）
★★`split_run`   = ★両方 在るが ★走行が 違う（★★今回 0件＝★★『無い』とは 書かない・★材料が 少ない）
★★`no_send`     = ★渡した 記録が 無い
★★★∴ ★★『埋まっていない』では なく ★★『どの語で 止まっているか』が 出る＝★★これが 今回 増えた 物
```

## 4. ★★次に 何を すれば **2本目** が 出るか（★★1つだけ）

```
★★★`S06`（`HANDOFF.S06 → RRI.mint`）の ★受け手に ★`at` を 1つ 足す。
   ★理由 = ★★★両側は 既に 69回 出ている＝★★材料の うち 3つ（★送り ／ 受け ／ 走行）は 揃っている
        ―― ★★欠けているのは ★`locator` の 1つだけ ＝ ★★★いちばん 近い 1本
   ★やり方 = ★既に 通った 形（★`record_only_patch` ／ ★門 v4）＝ ★★新しい 部品 0
   ★★受入 = ★★`linked` が ★1 → ★2（★★★出たら 書く・★『出る』とは 書かない）
★★★これ以外を 同時に やらない ―― ★理由 = ★`S07` は ★計装しても ★1回しか 出ていない
   ＝ ★★★『計装した』と『出た』は 別（★★数を 増やす 前に ★出るかを 見る＝★Taka の 線）
```

## 5. ★★どこでも 同じ 形で 使えるか（★Taka 指示＝★共通利用）

```
★★★§1 の 3行は ★`Route` ／ `RRI` ／ `DW` の どれにも 同じ 形で 置ける
   ―― ★根拠 = ★★今回 通ったのは ★★`DW` の 内側（★組み立ての 段）
   ―― ★★既に 出ている 他の 例 = ★`DS`（S03）／ ★`EGL`（S07）＝ ★★★別の repo で 同じ 形が 成った
   ―― ★★★∴ ★『DW 専用の 仕掛け』では ない（★★3つの repo で 同じ 2行が 効いた）
★★★但し ★まだ 言えない = ★★『どの区間でも 揃う』
   ―― ★実測（★1走行 111件）= ★`run_id` を 持つ 行は ★★17 ＝ ★★★大半は まだ 走行の 外
   ―― ★★∴ ★★『これから 増える』と 書かない＝★★増えた 分だけ 数える
```

## 6. ★★言い方の 線（★MGR 指定を そのまま 守る）

```
★★★書かない = ★『プロトコルが 成立』／『経路表が 埋まった』／『5本 計装できた』／『5つが 一本に なった』
★★★書く    = ★★『★1本が 端から端まで 結合した ／ ★残りは ★理由が 語で 出ている』
★★分母を 隠さない = ★記録から 出た 区間 ★70本 ∴ ★★`linked` ★1 / 70
★★1回の 値で『減った』『増えた』と 書かない（★★2点 以上）
```

## 7. ★★例外（★★本線に 混ぜない・★台帳へ）

```
★★① ★複数行の `def` を 持つ 関数（★例＝`rri/rri/preflight_gate.py::detect`）＝ ★工具を 直さない
★★② ★`already_recorded` が ★関数の 中の 文字列に 当たる 形
★★③（★今回 出た）★★`identity` の 数が ★管理側の 記憶と 2件 ずれた（★§0）
   ―― ★★原因 = ★★『両側に 出た』と『位置を 名乗った』を ★同じ物として 覚えていた
   ―― ★★★機構化の 候補 = ★★2つの 数を ★★必ず 並べて 出す（★片方だけ 出すと また ずれる）
```

## 8. ★★私が 触った 物

```
★repo への 書き込み = ★★0 ／ ★私の 実装 = ★★0 ／ ★台帳 直読 = ★★0
★★やった 事 = ★引いた ／ ★写して 外で 走らせた ／ ★この 文書を 起草した
★★★受領・封入・commit = ★MGR（★私は しない）
```
