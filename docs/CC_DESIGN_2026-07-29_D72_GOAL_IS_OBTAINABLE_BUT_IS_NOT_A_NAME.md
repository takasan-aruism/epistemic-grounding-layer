# 設計/監査 → MGR（写: Taka / IMPL）: **D-72 — タスク名は★取れる。★ただし「名前」ではない（中央値3816字）。これで欠落5件が確定した**

- `BUILD_ROLE: 参照`（**調査のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-29_D71_RULING_THE_REMAINDER_IS_NOT_A_FIFTH_CATEGORY.md` §5

## 0. ★結論
> **① タスク名は★front door から取れる**（`/api/state` の `goal`）。**★`claude_packet` と `resolve` には無い。**
> **② ★しかし「名前」ではない。** **155件の goal は★中央値3816字・最長9144字**（＝依頼文そのもの）。
> **③ ∴ そのまま並べると★155件の ID の壁が「155件の長文の壁」になる。** **★問題が形を変えるだけである。**
> **④ ★これで Taka の欠落5件が確定した**（§3）。

---

## 1. ★実測（155件 全数・打ち切り無し・読み取りのみ）
```
再現: 3つの口で goal の有無
  /api/state         → ★在り（796字。TASK-2DER-B9B4DA3B）
  /api/claude_packet → ★キー無し
  /api/resolve       → ★キー無し

再現: 155件すべての goal の長さ
  取得 155 / 有効 155 / エラー 0 / ★空(0字) 0件
  最短 12字 / ★中央値 3816字 / 最長 9144字
  40字以下      ★5件
  41〜200字     ★20件
  ★201字以上   ★130件（84%）
```

### 1-1. ★私の計器が1つ壊れていた（自己申告）
```
最初のループ: URL に★空白を入れてしまい（"$B/api/$ep TASK-…"）、3つとも「読めず」と出た。
★もし個別確認をしていなければ、「goal は取れない」と書いていた。
正しい実行 : 空白を除く → ★/api/state では取れる。
```
> **★本日8回目の、私の計器の問題である。** **★今回は「読めず」が3つ並んだのに、★個別確認で覆った。**
> **★3つ揃って同じ答えでも、★計器が同じなら3つとも同じように間違える。**

## 2. ★「取れる」と「使える」は違う（★本件の核）
| | |
|---|---|
| **取れるか** | **★取れる**（`/api/state.goal`） |
| **★名前として使えるか** | **★使えない。** **中央値3816字は★名前ではなく依頼文である** |
| **∴** | **★155件を goal 付きで並べると、★「ID の壁」が「長文の壁」になる** |

> **★Taka の指摘は「タスク名なし」だった。** **★実測の答えは「名前が無い。依頼文なら在る」である。**
> **★これは今日ずっとやってきた形である**——**★「無い」と「別のものが在る」を分ける。**

### 2-1. ★短くできるか（★私は決めない）
| 案 | 判定 |
|---|---|
| **先頭N字を切る** | **★機械で決まる。** ただし**先頭が「宛: 設計/監査(CC-α)」で始まる**ものが在り、**★切ると全部同じに見える**（実データで確認） |
| **LLM に要約させる** | **★MGR が禁じている**（「LLM に分類させない」）。**★同じ理由で要約もさせない、と読む** |
| **名前を新しく持つ** | **★新しいデータを作る。** **★Taka の禁止（膨らませない）に触れうる** |
| **出さない** | **★「名前は無い。依頼文なら在る」と書く** |

> **★私は決めない。** **★MGR の裁定を仰ぐ。** **★ただし「先頭N字」は実データで壊れることを示した**（上表）。

## 3. ★Taka の欠落5件（★確定版・実測後）
| Taka の指摘 | **確定** | 根拠 |
|---|---|---|
| **タスク名なし** | **★「名前」は無い。依頼文なら取れる** | `/api/state.goal`・中央値3816字 |
| **状態なし** | **★取れる** | `dw_state`・155件 実測（8種類） |
| **開始日なし** | **★取れない** | 3つの口に ts 系が無い（D-71 §4） |
| **優先度なし** | **★取れない**（値そのものが無い） | `_active_2der_tasks` が持たない（D-70 §3） |
| **なぜ表示されたか不明** | **★取れる** | 「COMPLETE でも BLOCKED でもないから」 |

> **★取れるのは2件。取れないのは2件。★1件は「別のものなら取れる」。**
> **★D-70 では「4件が既に在る」、D-71 では「3件」と書いた。** **★確定は 2件＋1件（別物）である。** **★2回 訂正した。**

## 4. ★件数（★確定・MGR §6 の形）
```
停止中              2   （BLOCKED）
依存待ち           18   （claude_barrier=True）
背景保持          117   （機械が進められる: QWEN_*）
現在前面化すべき   ★出せない（決める値が無い）
どの区分にも入らない 18   （誰も進められない: barrier False かつ actor_role "-"）
────────────────────
合計              155   ★2+18+117+18 = 155（合う）
```

## 5. ★私は実装しない
- **★調査のみ。** **★コードを1行も変えていない。** **★投入していない。**
- **★区分を増やしていない。** **★LLM に分類も要約もさせていない。**
- **★MGR §5 の「これが出れば SPEC を書いてよい」に対し、★1つ裁定が要る**（§2-1・名前をどうするか）。

## 6. ★未確認
| # | | 誰が・いつ |
|---|---|---|
| 1 | **`JUDGE_REQUIRED` 18件が何を待っているのか** | **★分からない**（`G-72` 登記予定・§7） |
| 2 | **`goal` の先頭が何種類あるか**（先頭N字案の可否を決める材料） | **★数えていない。** MGR が要ると言えば |
| 3 | 画面をブラウザで見ていない | **★Taka にしか出せない**（未回答） |

## 7. ★Gap の登記（MGR 裁定③・id は私が採る）
```
G-72: 155件中18件（12%）について「次に誰が何をするか」の答えが「誰も」である。
      JUDGE_REQUIRED = claude_barrier False かつ actor_role "-"
      ∴ 機械も進められず、人も呼ばれていない。
      ★理由は分かっていない。★「あと1段で動く」と書かない。★今は直さない。
      根拠: 155件 全数実測（打ち切り無し・読み取りのみ）
```

---
*CC-α D-72（調査のみ）。★結論=①**タスク名は front door から取れる**（`/api/state.goal`。`claude_packet`/`resolve` には無い）②**しかし「名前」ではない**（155件の goal は**中央値3816字・最長9144字**＝依頼文そのもの。201字以上が130件＝84%）③**∴ そのまま並べると「155件の ID の壁」が「155件の長文の壁」になり、問題が形を変えるだけ** ④これで Taka の欠落5件が確定した。★**私の計器が1つ壊れていた**——最初のループは URL に空白を入れてしまい3つとも「読めず」と出たが、**個別確認をしていなければ「goal は取れない」と書いていた**。**本日8回目の計器の問題で、3つ揃って同じ答えでも計器が同じなら3つとも同じように間違える**。★**本件の核は「取れる」と「使える」は違う**——取れるが名前としては使えず、**Taka の指摘「タスク名なし」に対する実測の答えは「名前が無い。依頼文なら在る」**（今日ずっとやってきた「無い」と「別のものが在る」を分ける形）。短くする案は**先頭N字＝機械で決まるが、先頭が「宛: 設計/監査(CC-α)」で始まるものが在り切ると全部同じに見える**（実データで確認）／**LLM に要約させるのは MGR の「LLM に分類させない」と同じ理由で不可と読む**／名前を新しく持つのは**新しいデータを作る**／出さないなら「名前は無い。依頼文なら在る」と書く——**CC-α は決めず MGR の裁定を仰ぐ**。★**Taka の欠落5件の確定版**=タスク名は「名前は無い・依頼文なら取れる」／状態は取れる／開始日は取れない／優先度は取れない／なぜ表示されたかは取れる ∴ **取れるのは2件、取れないのは2件、1件は「別のものなら取れる」**——**D-70 で「4件」D-71 で「3件」と書き、確定は 2件＋1件（別物）で、2回 訂正した**。★件数は**2+18+117+18 = 155 で合う**。★CC-α は実装せず区分を増やさず LLM に分類も要約もさせておらず、**名前をどうするかの裁定が1つ要る**。★`G-72` を採番（**155件中18件（12%）について「次に誰が何をするか」の答えが「誰も」である**。理由は分かっておらず「あと1段で動く」と書かず今は直さない）。*
