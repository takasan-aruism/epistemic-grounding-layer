# 【D-195】★S-2（gate パッチ）を★取り下げる ／ ★C-1 を★戻す ／ ★C-4 の正典を★採る

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-08-01 00:3x / TYPE=STATUS
- **開発者規律 確認済（版: v1.0）** ／ **受領**: `CC_DESIGN_2026-08-01_S1_INVENTORY_AND_2DER_RAN_SIX_STEPS.md`（FINDING）／ `CC_DESIGN_2026-08-01_S3_REQUEST_WITH_A_CONTRACT.md`（BUILD_SPEC・写しとして受領）
- **この .md がまだ .md である理由**: 裁定を台帳へ書く口が front door に無いため（C-1）。**★その C-1 が本書で ACTIVE の次に戻る。**

## 1. ★裁定①：★S-2 を取り下げる（★私の宣言を、私が撤回する）
```
★私は `D-193` §3 で「★gate を予約から ACTIVE へ★合流させる」と★宣言した。
★★実測（★設計/監査）: ★同一依頼文の再投入で ★task_id は同じまま `runnable: True` になり、
   ★GENERATE / AUDIT / REGENERATE が ★6回 動いた ＝ ★正規の開け方が★既に在った。
★★★∴ ★S-2（gate の最小パッチ）は ★不要。★取り下げる。★「在るものは作らない」に該当する。
★★★★★私の側の誤りの形: ★D-190 の「進める経路が存在しない」を ★再検証せずに ★合流の根拠にした。
   ★開発者規律 第7章（★『無い』は最も高くつく主張・叩いて確かめる）を ★MGR が破った。
```
**★ただし「gate が閉じること」自体は残っている。** **1段ごとに再投入と押下が要る＝Claude の手が要る**（今回 押下8回）。
**∴ `D-191`（意図が記録に在るか）は ★止めたまま。★本線ではない。★C-1/C-2/C-3 の後に再判定する。**

## 2. ★裁定②：★C-4 の正典を採る（★設計案をそのまま採用）
```
★採用: ★「いまどこか」を出す面の正典は ★`/api/roadmap`（★絞る側・11フェーズ/78件/DONE 65）。
★★`PHASE-2DER-AC-00〜03` の AC 系列は ★消さず、★別のロードマップとして★並べて出す（★混ぜない）。
★★★理由は設計の §3-1 のとおり。★Taka には上げない（★調べれば分かることだった）。
```

## 3. ★順序（★ACTIVE は1件。★後回しにしたものは★書く）
| 件 | 扱い |
|---|---|
| **S-3 人間用UIの4つを 2DER に作らせる** | **★ACTIVE（★BUILD SPEC は IMPL へ渡り済）** |
| **C-1 進捗の書き込み口** | **★S-3 の直後に単独で戻す**（`D-194` の復帰条件を★前倒しする）。**繋ぐ先は `POST /api/submit` ↔ `set_status` の1本。★関数を新しく作らない** |
| C-2 入口を固定する / C-3 外の道を塞ぐ | **★後回しのまま**（★C-1 の後、この順） |
| C-4 | **★裁定済（§2）。★表示に反映するのは S-3 の受入試験5本目** |
| S-2 gate パッチ | **★取り下げ（§1）** |
| D-191 gate の意図調査 | **★止めたまま。★C-3 の後に再判定** |

## 4. ★S-3 について MGR が足す条件（★1つだけ）
```
★契約経路（`<<<2DER:...>>>`）は ★実投入で使われた形跡が無いと設計が実測している。
★★∴ ★マーカーで弾かれても ★それは失敗ではない。★止まった段と逐語を持ち帰ることが成果である。
★★★★ただし ★弾かれた時に ★依頼文を書き換えて通しにいかないこと。★書き換えたら ★「書き換えた」と書く。
★★★★★2DER が作れなかった場合に ★Claude が代わりに書くのは ★可。★ただし ★2DER の実績に数えない。
```

## 5. ★A（★実行事実から。★人が set した値は使わない）
```
★2DER だけで完了した工程: ★観測・PLAN の2種 → ★★GENERATE / AUDIT / REGENERATE を含む ★5種（実行6回）
★Claude が要る工程: ★再投入1回＋押下8回＋★JUDGE_REQUIRED の裁定 ／ ★生成した中身は ★0行
★★★これは ★本日 初めて ★2DER 側が「作る段」を動かした記録である。★ただし ★成果物は不合格（判定不能）。
★★★★∴ 「★動いた」と書く。「★できた」とは★書かない。
```

---
**決めたこと**: **①S-2（gate の最小パッチ）を取り下げる。同一依頼文の再投入で正規に開き、GENERATE/AUDIT/REGENERATE が6回 動いた——在るものを作ろうとしていた。合流を宣言した私の側の誤りで、再検証せずに D-190 を根拠にした ②ただし gate が閉じること自体は残り、1段ごとに Claude の押下が要る。D-191 は止めたまま、C-3 の後に再判定 ③C-4 は設計案を採用——「いまどこか」の正典は絞る側（DONE 65）、AC 系列は別ロードマップとして並べて出す。Taka には上げない ④順序は S-3（ACTIVE）→ C-1（前倒しで直後に戻す。繋ぐのは submit ↔ set_status の1本）→ C-2 → C-3 ⑤契約マーカーで弾かれても成果。ただし依頼文を書き換えて通しにいかない。Claude が代わりに書いた分は 2DER の実績に数えない ⑥今日 初めて 2DER 側が「作る段」を動かしたが、成果物は判定不能で不合格。「動いた」と書き「できた」とは書かない。**
