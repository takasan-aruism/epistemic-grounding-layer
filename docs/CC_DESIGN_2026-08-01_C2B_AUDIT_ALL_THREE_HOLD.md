# 【BUILT 監査 / C-2 訂正】**★(b-1)(b-2)(b-3) すべて成立** — ★3つとも★投入せずに確かめた

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-08-01 15:4x / TYPE=FINDING
- **開発者規律 確認済（版: v1.1 / 2026-08-01）** ／ **受領**: `CC_IMPL_2026-08-01_C2B_REJECT_TWO_CONTRACTS_BUILT.md`
- **★報告を読む前に自分で叩いた** ／ **★コードを1行も変えていない** ／ **★front door へ投入していない**

---

# 1. ★独立に確かめた（★3つとも）

| 受入 | 判定 | ★私の実測（逐語） |
|---|---|---|
| **(b-1)** 2組は弾かれる | **★○** | ★1組 → 通る ／ ★★2組 → `ValueError: multiple contracts: only one contract block is allowed` |
| **(b-2)** 雛形の契約は1組・中身は空 | **★○** | ★`<<<2DER:SKELETON>>>` **1** ／ `<<<2DER:IMMUTABLE_TESTS>>>` **1** ／ **`def test_` ★0行**（★前回は5行）／ 字数 3519 → **1329** |
| **(b-3)** 埋めた方が採られる | **★○** | ★★§2 |
| ★除いた跡の1行 | **★○** | 逐語:「（前回の契約はここに在りましたが、新しく書くために外しました）」 |

---

# 2. ★★(b-3) を ★投入せずに示した（★私の SPEC の測り方より軽い形が在った）

```
★私の SPEC は ★「★投入して ★採られた試験の名前を引く」と書いた。★★投入が要る形だった。
★★★実際には ★★純関数だけで示せる:
   ★① ★雛形の空欄に ★★`def test_written_by_cc_alpha_audit()` を入れる（★私が書いたと分かる名前）
   ★② ★`contract_seal.extract_contract` に通す
   ★③ ★採られた試験の名前を引く
★★★★実測（逐語）: ★採られた試験の名前 ＝ ★`['test_written_by_cc_alpha_audit']`
   ＝ ★★埋めた方が ★採られている。★★古い契約は ★1つも混ざっていない。
★★★★★∴ ★★front door へ ★1回も投入せずに ★(b-3) が示せた。★task も増えていない。
★★★★★★私の SPEC が ★余計に重かった。★★次から ★「純関数で示せないか」を先に見る。
```

---

# 3. ★私が今日 通した規律（★1つだけ書く）

```
★本日 ★私は ★4回「★その先を叩かずに SPEC を書いた」。
★★★今回（C-2 訂正）は ★★書く前に叩き（★278ファイル走査・0件）、★★監査でも ★報告を読む前に叩いた。
★★★★★5件目は ★起きていない。
★★★★★★ただし ★★「起きていない」は ★1回である。★★直ったとは書かない。
```

---

# 4. ★判定

> ### **★C-2 の受入 (b-1)(b-2)(b-3) は ★成立している。**

```
★★残り: ★(a)(c)(d)(e) は `D-209` のまま。
   ★(a) 雛形が返る ＝ ★○（★§1 で見えている）
   ★(c) ★空のまま投げたら弾かれる ＝ ★★測っていない（★★投入が要る。★★IMPL の手番）
   ★(d) 止まった理由が読める ＝ ★○（★`work.next_information_need` 7件・★既存）
   ★(e) 枠は日本語 ＝ ★○（★中身は英語のまま。★私が SPEC §4 で先に書いたとおり）
★★★★★∴ ★★`DONE` にしてよいかは ★(c) の実測後である。★私は ★投入しない（★実装の手番）。
```

---

# 5. ★やっていないこと
```
★front door へ投入していない ／ ★task を1件も増やしていない ／ ★コードを1行も変えていない
★commit していない ／ ★台帳を直読していない
★★`contract_seal.extract_contract` を ★私の作業領域で ★3回 呼んだ（★2DER の外・★純関数・★実績に数えない）
★★★測っていないこと（★前回と同じ・★消さない）: ★過去の依頼文に2組 在ったか【未確認】／
   ★`progress_seal` にも同じ問題が在るか【未確認】。★★どちらも今回 触っていない
```

---
**決めたこと**: **①(b-1)(b-2)(b-3) を独立に確かめ、3つとも成立した——2組は `ValueError: multiple contracts: only one contract block is allowed` で弾かれ、雛形の契約は1組で `def test_` は0行（前回は5行・字数も 3519→1329）、除いた跡の1行も入っている ②(b-3) は投入せずに示せた。雛形の空欄に私だと分かる名前の試験を入れて `extract_contract` に通すと、採られた試験の名前が `['test_written_by_cc_alpha_audit']` だけになり、古い契約は1つも混ざらなかった ③私の SPEC は「投入して確かめる」と書いており余計に重かった。純関数で示せる形が在ったので、次から「純関数で示せないか」を先に見る ④本日 私は4回「その先を叩かずに SPEC を書いた」が、今回は書く前にも監査でも先に叩いた。5件目は起きていない——ただし1回なので「直った」とは書かない ⑤C-2 の (b) 系は成立。(a)(d)(e) も成立しているが、(c)（空のまま投げたら弾かれる）は投入が要るので測っていない。`DONE` にしてよいかは (c) の実測後で、投入は実装の手番である ⑥測っていないことは前回と同じ2件（過去の依頼文の2組・`progress_seal` の同じ問題）で、消さずに残す。**
