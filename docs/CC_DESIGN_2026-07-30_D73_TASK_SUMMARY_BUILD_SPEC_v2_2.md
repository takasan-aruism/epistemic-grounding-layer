# BUILD SPEC — 継続中タスクを集約して出す **v2.2**（★実装源・v2.1 を置き換える）

- `BUILD_ROLE: ★実装源`（**本文書が実装の唯一の典拠**）／**★v1.0・v2.0・v2.1 は SUPERSEDED。★見ないこと**
- **★宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-30
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-30_D79_RULING_SPLIT_THE_EXCEPTION_AND_ONE_MARK_PER_ITEM.md`

> **★v2.1 の実装は★大部分そのまま使える。** **★直すのは §1 の2点だけである。**
> **★作り直さないこと。** **★`deferred_summary` に欄を1つ足し、内訳を1つ足すだけである。**

---

## 1. ★直す2点
### 1-1. ★例外を「決まっていない」から分ける（★今日7つ目）
```python
現在（webui.py:161-167・実読）:
  except Exception:
      out["unknown_state"] += 1     ← ★(b) 引けなかった
  if n.get("actor_id") == "-":
      out["unknown_state"] += 1     ← ★(a) 表に載っていない
★同じ欄に2つ入っている。
```
| ★分ける | 欄 | 画面の文言 |
|---|---|---|
| **(a) 表に載っていない** | `unknown_state` | **「誰がやるか決まっていない」** |
| **(b) 引けなかった** | **★`uncountable`（新規）** | **「数えられなかった」** |

**★守ること**
1. **★`uncountable` が0件でも行を消さない。**
2. **★ここでの「0件」は★本当に0である。** **★「Taka の手番」の「出せません」とは違う。**
   - **★理由: 例外は★数えられる。★人を指す担当者の値は★そもそも無い。** **★片方は測れて0、片方は測れない。**
3. **★行の下に1行だけ添える: 「『数えられなかった』は、機械が答えを出せなかったものです。」**
4. **★和に入れる。** **★合計＝背景＋Claude＋決まっていない＋数えられなかった。**

### 1-2. ★受入6 — ★内訳を数えて出す（★固定文にしない）
```
現在の配信物: 「『誰がやるか決まっていない』は、次に誰が何をするかを決める表に
              その状態が載っていないものです。」   ← ★固定の説明文。★数えていない
```
| ★出すもの | **`unknown_state` に入った task の★状態を数えた内訳** |
|---|---|
| 形 | **「└ 裁定が要る 18件」**（★今日はこの1種だけ。**★状態が2種以上なら全部 出す**） |
| **★固定しない** | **★「裁定が要る」と決め打ちしない。★実際の `state` を数えて出す** |
| **★出せないとき** | **★「出せません」＋理由と書く。★固定文を出して「出した」と扱わない** |
| 説明文 | **★残してよい**（★ただし★内訳の代わりにしない） |

## 2. ★画面（★直した後の形）
```
進行中の依頼  153件
  この一覧に、終わった依頼と止まった依頼は入っていません。

  背景で進んでいる（Qwen が動かす）    117件
  Claude の手番                        18件
  誰がやるか決まっていない              18件
      └ 裁定が要る  18件
  ★数えられなかった                     0件
  いま前に出すべきもの   出せません（前に出す順番を決める値が 2DER に無いため）
  ──────────────────────────
  合計                                153件

  ※ 止まっている依頼が 2件 ありますが、この一覧の対象外です。
  ※ Taka の手番かどうかはこの画面では出せません（2DER に人を指す担当者の値が無いためです）。
  「誰がやるか決まっていない」は、次に誰が何をするかを決める表にその状態が載っていないものです。
  ★「数えられなかった」は、機械が答えを出せなかったものです。
```

## 3. ★変えないもの（★v2.1 のまま。★作り直さない）
- **母数＝`_active_2der_tasks`／判定順 ①`actor_id == "-"` ②`actor_role in (…)` ③残り。**
- **ID を1件も出さない／色を使わない／LLM に分類も要約もさせない／名前を作らない。**
- **キャッシュを作らない／並列化しない／`ADJUDICATOR` を `DEFAULT_ACTORS` に足さない**（`G-72` は別 build）。
- **勘定科目 据え置き／★投入しない。**
- **★人称語（こちら・そちら・あなた）を使わない。**
- **★`/api/state` の返りを変えない**（`deferred_summary` は `/api/submit` 側・v2.1 のまま）。

## 4. ★受入（★MGR 裁定②: ★1条件に1つの印。★まとめて印を付けないこと）
| # | ★条件（★1行＝1条件） | 示し方 |
|---|---|---|
| **1** | `uncountable` の欄が在る | ソース |
| **2** | **★例外は `unknown_state` に入らない**（`except` の中が `uncountable` になっている） | ソース |
| **3** | **★`uncountable` が0件でも行が出る** | 配信物 |
| **4** | **★「『数えられなかった』は、機械が答えを出せなかったものです。」が出る** | 配信物 |
| **5** | **★合計＝背景＋Claude＋決まっていない＋数えられなかった** | 実測 |
| **6** | **★「誰がやるか決まっていない」の下に、★状態ごとの件数が出る** | 配信物 |
| **7** | **★その内訳が★固定文字列でない**（`state` を数えている） | ソース |
| **8** | **★内訳の件数の和が「誰がやるか決まっていない」の件数と一致** | 実測 |
| **9** | 合計が `_active_2der_tasks` が返す数と一致 | 実測 |
| **10** | ID を並べる箇所が無い | 差分 |
| **11** | 人称語（こちら／そちら／あなた）が0件 | 差分 |
| **12** | 色（`ok`／`bad`）を足していない | 差分 |
| **13** | 配信物の JS が `node --check` を通る | 配信物 |
| **14** | 非回帰 91 passed / 7 failed | **★走らせ方を BUILT に書く**（v2.1 で書かれた。★今回も書く） |
| **15** | 開いたときの所要 | **★数字だけ。「速い」と書かない** |

> **★MGR 裁定②: ★14行 それぞれに1つずつ印を付けること。** **★「4〜7 文言」のようにまとめない。**
> **★理由: v2.1 では「受入4〜7」とまとめたため、★6 が落ちたのに誰も気づかなかった。**

## 5. ★止まってよい場所
| # | |
|---|---|
| 1 | **例外を分けると、和が合計と一致しなくなる** → **★止めて報告** |
| 2 | **`unknown_state` の内訳が取れない**（`state` が引けない等）→ **★止めて報告**（★固定文を出さない） |
| 3 | **`uncountable` を足すと「区分を増やさない」に触れると判断した** → **★止めて報告**（★MGR は §1 で許している） |
| 4 | SPEC が2通りに読める |
| 5 | **重くて出せない** → **★止めて報告**（★区分を減らして軽くしない） |

## 6. ★この build でやらないこと（★横に広げない）
| # | | |
|---|---|---|
| 1 | **★同じ「無い」を6通りで言っている**（在りません5／ありません1／できません4／出せません2／分かりません3／区別できません3）／**最長80字の否定文**／**日本語の文に英語が残っている**（`authority boundary` `DW task`） | **★D-62（入力4件の文言）で扱う。★この build でやらない** |
| 2 | **★注記が12px・最小11px**（本文15px）——**★「必ず読んでほしい」注記ほど小さい** | **★別 build。★今日は文字の大きさを触らない** |
| 3 | `ADJUDICATOR` の割り当て | **`G-72`・★Taka の答え待ち** |
> **★1・2 は私が配信物で実測した。★消さずにここに残す。** **★「あとでやる」を口約束にしない。**

## 7. ★資料の同期
**★MGR の commit 後に私が同期する。**

---
*CC-α D-73 BUILD SPEC **v2.2**（実装源・宛 IMPL・**v1.0/v2.0/v2.1 を置き換え、見ないこと**）。★**v2.1 の実装は大部分そのまま使えるので作り直さず、`deferred_summary` に欄を1つ足し内訳を1つ足すだけ**。★直す2点=**①例外を「決まっていない」から分ける（今日7つ目）**——現在 `except Exception` の **(b) 引けなかった**と `actor_id == "-"` の **(a) 表に載っていない**が同じ欄に入っているので、**(a)=`unknown_state`「誰がやるか決まっていない」／(b)=新欄 `uncountable`「数えられなかった」**に分ける。守ることは**`uncountable` が0件でも行を消さない**／**ここでの「0件」は本当に0であり「Taka の手番」の「出せません」とは違う（例外は数えられるが、人を指す担当者の値はそもそも無い＝片方は測れて0、片方は測れない）**／**行の下に「『数えられなかった』は、機械が答えを出せなかったものです。」を1行 添える**／**和に入れる（合計＝背景＋Claude＋決まっていない＋数えられなかった）**。**②受入6=内訳を数えて出す**——現在は固定の説明文で数えていないので、**`unknown_state` に入った task の状態を数えた内訳（「└ 裁定が要る 18件」形式・状態が2種以上なら全部）を出し、「裁定が要る」と決め打ちせず実際の `state` を数える**、**出せないなら「出せません」＋理由と書き、固定文を出して「出した」と扱わない**（説明文は残してよいが内訳の代わりにしない）。★変えないもの=母数と判定順／ID を出さない・色・LLM・名前／キャッシュ・並列化・`ADJUDICATOR` 追加（`G-72` は別 build）／勘定科目・投入しない／**人称語を使わない**／`/api/state` の返りを変えない。★**受入は15行で MGR 裁定②により1条件に1つの印を付けさせる（まとめない）**——**v2.1 では「受入4〜7」とまとめたため 6 が落ちたのに誰も気づかなかった**。★止まってよい場所5件（**例外を分けて和が合わない**／**内訳が取れないとき固定文を出さない**／**`uncountable` が「区分を増やさない」に触れると判断したら止めて報告（MGR は許している）**／2通りに読める／重くて出せないとき区分を減らさない）。★**この build でやらないこと（横に広げない）**=**同じ「無い」を6通りで言っている（在りません5／ありません1／できません4／出せません2／分かりません3／区別できません3）・最長80字の否定文・日本語の文に英語が残っている（`authority boundary` `DW task`）→ D-62 で扱う**／**注記が12px・最小11px で「必ず読んでほしい」注記ほど小さい → 別 build。今日は文字の大きさを触らない**／`ADJUDICATOR` の割り当ては `G-72`・Taka の答え待ち——**1・2 は CC-α が配信物で実測したものであり、消さずに残して「あとでやる」を口約束にしない**。★資料の同期は MGR の commit 後に CC-α が行う。*
