# 設計/監査 → MGR（写: Taka / IMPL）: **D-56 の受入を確認し資料を同期した。★そして食い違いは「合計行が誤り」で確定した（表が正しい）**

- `BUILD_ROLE: 参照`（**監査＋資料同期。コードを1行も変えていない・投入していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_MGR_2026-07-29_D57B_APPROVED_ONE_BUILD_OF_NINE.md`

## 0. ★結論
> **① D-56 の受入は★私の実測でも満たされている。** **② 資料を同期した**（計器は「一致」に復帰）。
> **③ ★食い違いは「合計行が誤り」で確定した。** **★表が正しい。**
> **④ ★正しい内訳は 偽1 / 窓2 / まとめ6 = 9 である**（合計行の「窓3 / まとめ5」は誤り）。

---

## 1. ★食い違いの決着（MGR §3・先に片付ける）
```
私の §4 の表 : 偽(#12) 1 ＋ /command まとめ 5 ＋ / 窓(#2,#3) 2 ＋ / まとめ(#7) 1 ＝ 9
私の合計行   : 偽 1 ＋ ★窓 3 ＋ ★まとめ 5                                   ＝ 9
```
**★誤りの機構（自分で特定した）**
> **合計行は `#12` を★2回数えている。**
> **私は D-57b §2-1 で「`#12` は `#2`/`#3` と同じ class（窓）である」と書いた。**
> **その直後に MGR が「`#12` は★偽になる文として最優先」と裁定した。**
> **∴ 合計行を書くとき、`#12` を★偽にも窓にも入れ、代わりに `/` の `#7` を落とした。**

| | **正しい内訳** |
|---|---|
| **偽になる文** | **1**（`/command` #12） |
| **窓** | **2**（`/` #2・#3） |
| **まとめている** | **6**（`/command` #3・#4・#7・#10・#11 ＝5 ＋ `/` #7 ＝1） |
| **計** | **9** |

> **★総数9は最初から一致していた。1件も落ちていない。** **★分類だけが誤っていた。**
> **★MGR の指摘は細かくない。** **「種類ごとに直す」をやる以上、種類の数が違えば直し方の選び方が変わる。**

### 1-1. ★`#12` の扱いを1つに決める（二重に数えないため）
> **`#12` は★窓の性質を持つが、★分類は「偽になる文」1つとする**（MGR 裁定）。
> **理由: 「全 event」と書いて全 event を見ていないのは、★見た範囲の話を全体の話にした（窓）だけでなく、★文そのものが偽になる。**
> **∴ より重い方に分類する。** **★両方に入れない。**

---

## 2. ★D-56 の受入（私の実測）
```
再現: git diff --stat -- webui.py → 8 insertions / 5 deletions
再現: git diff -- webui.py | grep "^@@" → 766 / 797 / 821 行目（★PAGE=617 以降）
再現: git diff -- webui.py | grep "^+" | grep -cE "pill (ok|bad)|class='(ok|bad)'" → ★0
再現: GET /command → (c1)(c2) の文言が★どちらも在り
再現: GET /api/state → TASK-2DER-D84215A8 keys=19 / TASK-2DER-B9B4DA3B keys=19（★どちらも19で揃う）
```
| # | 受入 | 判定 |
|---|---|---|
| 1 | (c1) の文言が出る | **★満たされた**（`【実】`・配信画面に在り） |
| 2 | (c2) の文言が画面のソースに在る | **★満たされた**（`【実】`） |
| 3 | (b) が壊れていない | **★満たされた**（差分が `droppedBlock` の分岐のみ） |
| 4 | `ok`/`bad` を足していない | **★満たされた**（`【実】`・**差分で数えた**。ページ全体で数えていない） |
| 5 | 既存が不変 | **★満たされた**（`【実】`・両 task とも 19 キー） |
| 6 | 非回帰 91/7 | **`【伝聞】`**（IMPL 実測。私は再実行していない） |
| — | **(c2) は出たことが無い** | **★BUILT に書かれている。** **「効いた」と書いていない** |

### 2-1. ★変更の実体（実読・8行）
```diff
-function droppedBlock(rec){
-  if(!rec) return "…（この依頼の run_id が記録に無い）…";
+function droppedBlock(rec,hasRunId){
+  if(!rec) return "…"+(hasRunId ? "…（run_id は在るが、記録を引けなかった）"
+                                : "…（この依頼の run_id が記録に無い）")+"…";
-  …droppedBlock(rec);
+  …droppedBlock(rec,!!s.etrace_run_id);
```
> **★文言だけである。** **振る舞い（0件と出さない）を変えていない。** **新しい fetch も状態変数も無い。**

## 3. ★資料の同期（MGR 条件・完了）
```
再現: bash ~/.claude/hooks/2der_arch_fresh.sh
  → 実行構造の資料: 現在の HEAD と一致 (component 23 / edge 11 / gap 69)
```
- `EP-COMMAND-RUN` に D-55/D-56 の実測を追記。**5 repo の commit を現在の HEAD に更新。**
- **★`_meta` に「Phase 2 は完了していない（残件7件のうち減ったのは2件）」を明記した。**

## 4. ★MGR の「疑う条件は 0 ではない」について（受ける）
> **MGR:「今までは『0 が出たら自分を疑う』だった。今回は『少なすぎる、で気づいた』。∴ 疑う条件は『0』ではない。『思ったより少ない』も同じである。」**

**★受ける。** **★そして規則文書にしない**（MGR 明示・Taka の「規則作りへ広がると目的を失う」）。**この文書に残すだけにする。**

## 5. ★未確認
| # | 未確認 | 誰が・いつ |
|---|---|---|
| 1 | 受入6（非回帰）を私は再実行していない | CC-α / 要請時 |
| 2 | **画面をブラウザで見ていない**（HTML の文字列一致と差分で判定） | D-45 から継続 |
| 3 | **(c2) の実例** | **作らない** |

---
*CC-α。★結論=①D-56 の受入は実測でも満たされている ②資料を同期した（計器は「一致」に復帰）③**食い違いは「合計行が誤り」で確定**（表が正しい）④**正しい内訳は 偽1 / 窓2 / まとめ6 = 9**。★誤りの機構を自分で特定した=**合計行は `#12` を2回数えている**——D-57b §2-1 で「`#12` は `#2`/`#3` と同じ class（窓）」と書いた直後に MGR が「`#12` は偽になる文として最優先」と裁定したため、合計行を書くとき `#12` を偽にも窓にも入れ、代わりに `/` の `#7` を落とした。**総数9は最初から一致しており1件も落ちていない。分類だけが誤っていた**——MGR の指摘は細かくなく、「種類ごとに直す」以上、種類の数が違えば直し方の選び方が変わる。★`#12` は窓の性質を持つが**分類は「偽になる文」1つとする**（「全 event」と書いて全 event を見ていないのは窓であるだけでなく**文そのものが偽になる**ので、より重い方に分類し両方に入れない）。★D-56 の受入=差分は 8/5 で `@@` は 766/797/821 行（`PAGE`=617 以降）、`ok`/`bad` は**差分で数えて0**（ページ全体で数えていない）、`/command` に (c1)(c2) 両方の文言が在り、`/api/state` は両 task とも 19 キーで揃う ∴ 受入1〜5 は `【実】`、6 は `【伝聞】`。**BUILT に「(c2) は出たことが無い」が書かれ「効いた」と書いていない**。変更の実体は**文言だけ**で振る舞いを変えず新しい fetch も状態変数も無い。★資料を同期し `EP-COMMAND-RUN` に実測を追記、`_meta` に**「Phase 2 は完了していない（残件7件のうち減ったのは2件）」**を明記。★MGR の「疑う条件は 0 ではない。『思ったより少ない』も同じ」を受けるが**規則文書にせずこの文書に残すだけ**にする。★未確認=非回帰は再実行していない／画面をブラウザで見ていない／(c2) の実例は作らない。*
