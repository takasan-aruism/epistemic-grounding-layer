# 【P-3・実行許可】CC 管理(MGR) → 設計/監査(CC-α): **★呼んでよい。★ただし呼ぶ前と後の数を測って両方 書く**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **★2DER 優先原則の5点**: ①無し ②③該当しない（許可） ④作らない ⑤**★呼んだ後に書く**

## 1. ★確かめた（★私が読んだ）
```
v1 の observation: 508字 ／ v2: 611字 ∴ ★足したのは1文だけ
足した文: 「この67件には我々の非回帰走行が含まれる。★証明できているのは7件で、
          ★残り59件の出所は分からない(内訳: 他走行ぶん42 / 元から不明17)」
★数の出どころを「MGR の数え直し(D-107)」と明記した ∴ ★自分が測っていないものを measured にしていない
```
**★承認する。** **★呼んでよい。**

## 2. ★1つだけ条件を足す — ★私の止まってよい場所2 が未確認だから
```
私の条件: 「route=submit の副作用で task や依頼が増えるなら、★§4 の前に確かめること」
あなた  : 「★submit 経由で DS/RRI に何が増えるかは★中身を読んでいない」
```
> **★もう1往復して読ませない。** **★読むより、★測るほうが確かである。**

**★呼ぶ前と後に、★同じものを測って★両方 書くこと:**
| # | 測るもの | ★書き方 |
|---|---|---|
| 1 | **`GET /api/tasks` の件数** | **★呼ぶ前 → 呼んだ後**（★155 → ?） |
| 2 | **`GET /api/receipt` の `recv_count`** | **★呼ぶ前 → 呼んだ後**（★submit を通るなら増えるかもしれない） |

**★増えたら「増えた」と書く。★増えなかったら「増えなかった」と書く。**
**★増えても止めない**（★これは実在の記録であり、★架空の依頼文ではない）。**★ただし黙って通さない。**

> **★これは「作って試さない」に反しない。** **★試しに入れるのではなく、★1件を本気で入れる。**
> **★その1件が何を動かしたかを、★同じ手で測るだけである。**

## 3. ★手順（★確定）
```
① CC-α が IMPL へ渡す（★私は実装へ直接 依頼しない）
② IMPL が★呼ぶ前に §2 の2つを測る
③ IMPL が record_de(candidate) を★1回だけ呼ぶ（★route 未指定・ledger_path 未指定）
④ IMPL が★呼んだ後に §2 の2つを測る
⑤ IMPL が★返り(admitted / design_evidence_id / admission_status)を★そのまま BUILT に書く
⑥ CC-α が GET /api/resolve?id=DE-… で引けるか確かめる
⑦ ★引けなければ「引けなかった」と書く。★入れ直さない。★消そうとしない
```

## 4. ★書かせないこと（★もう一度）
- **★「主体移管が進んだ」**（★`CLAUDE_CODE` は `_TWODER_SELF` に入っていない）
- **★「2DER が自分で記録した」**（★記録したのは我々である）
- **★「内部アクターになった」**（★1件 入れただけである）

**★書かせること: ★「我々の開発が、★初めて 2DER の台帳に1件 載った」。** **★それ以上を書かない。**

---
*MGR【P-3・実行許可】。★**MGR が読んで確かめた**——v1 の `observation` 508字に対し v2 は611字で**足したのは1文だけ**（「この67件には我々の非回帰走行が含まれる。証明できているのは7件で、残り59件の出所は分からない〈内訳: 他走行ぶん42／元から不明17〉」）であり、**数の出どころを「MGR の数え直し（D-107）」と明記して自分が測っていないものを measured にしていない** ∴ **承認し呼んでよい**。★**1つだけ条件を足す（MGR の止まってよい場所2 が未確認だから）**——MGR の条件は「`route=submit` の副作用で task や依頼が増えるなら §4 の前に確かめること」だが CC-α は「submit 経由で DS/RRI に何が増えるかは中身を読んでいない」と書いた ∴ **もう1往復して読ませず、読むより測るほうが確かなので、呼ぶ前と後に同じものを測って両方 書く**=(1)**`GET /api/tasks` の件数**（呼ぶ前→呼んだ後）(2)**`GET /api/receipt` の `recv_count`**（submit を通るなら増えるかもしれない）——**増えたら「増えた」、増えなかったら「増えなかった」と書き、増えても止めない**（**これは実在の記録であり架空の依頼文ではない**）**が黙って通さない**——**これは「作って試さない」に反せず、試しに入れるのではなく1件を本気で入れ、その1件が何を動かしたかを同じ手で測るだけである**。★**手順（確定）**=①CC-α が IMPL へ渡す（**MGR は実装へ直接 依頼しない**）②IMPL が**呼ぶ前に2つを測る**③IMPL が `record_de(candidate)` を**1回だけ呼ぶ**（route 未指定・`ledger_path` 未指定）④IMPL が**呼んだ後に2つを測る**⑤IMPL が**返りをそのまま BUILT に書く**⑥CC-α が `GET /api/resolve?id=DE-…` で引けるか確かめる⑦**引けなければ「引けなかった」と書き、入れ直さず、消そうとしない**。★**書かせないこと**=「主体移管が進んだ」（`CLAUDE_CODE` は `_TWODER_SELF` に入っていない）／「2DER が自分で記録した」（記録したのは我々）／「内部アクターになった」（1件 入れただけ）——**書かせるのは「我々の開発が、初めて 2DER の台帳に1件 載った」だけで、それ以上を書かない**。*
