# 【実測 / `EVO-0034` #6】**`import human_view` は投入前に止まる**（機械が見るのは #6 だけ・そう明示する）

- **宛: 設計/監査(CC-α)** / 写: MGR / Taka / 発: 実装(IMPL) / 2026-08-02 12:2x / TYPE=BUILT
- **規律 v1.3 確認済 ／ 9項目 確認済（外れた番号なし）** ／ **実装源**: `CC_DESIGN_2026-08-02_EVO0034_BUILD_SPEC_MACHINE_CHECKS_ONE.md`

---

# 1. 変更（★Claude が書いた分。2DER の実績に数えない）

```
twoder/contract_seal.py のみ ★+17 / -0（検査 11行 ＋ 出典コメント 3行 ＋ 逐語メッセージ 3行）
既存3検査（マーカー / 契約2組 / ^def test_）は1文字も緩めていない。新しい関数・口・台帳・状態語なし。
★一覧・チェックボックス・確認欄は作っていない（裁定逐語「自己申告の再生産」を避ける）。
```

# 2. 受入

| # | 受入 | 判定 | 実測 |
|---|---|---|---|
| (1) | `import human_view` の契約が弾かれ、理由が逐語で出る | **○（純関数で確認）** | §3 の逐語。**front door への投入はしていない**（§4） |
| (2) | 偽陽性が無い | **○** | **`TASK-2DER-A64D0C6D` の実物の契約（`def test_` 10本）が通る**／`import impl` ＋ `json, os` ＋ `from collections import Counter` も通る |
| (3) | 既存3検査が壊れない | **○** | 名指し2本のみ `test_contract_read_spec.py` / `test_generate_via_runner_spec.py` → **23 passed in 0.17s**。61本は走らせていない |
| (4) | 「機械では見ない」の1行が出る／一覧を出さない | **○** | §3 の逐語に含まれる。チェック欄・一覧は無し |
| (5) | 戻せる | **○** | 足した17行を消すだけ（※ commit 後は `git checkout --` では戻らない） |
| (6) | 行数 | **○** | §1（Claude +17 / 2DER 0行） |

# 3. 逐語（弾いた時のメッセージ）

```
immutable_tests imports human_view; 2DER が作るのは impl.py です。試験は `import impl` と書いてください
（機械が見たのは #6(名前) だけです。#2 #4 #7 #8 #9 は 機械では見ていません）
```

```
許すもの: impl ／ 標準ライブラリ（sys.stdlib_module_names・python 3.13.5 で 290件と実測）
弾くもの: それ以外すべて（既存3検査と同じ ValueError の流儀・fail-closed）
```

# 4. 投入していない理由（★予告どおり）

```
受入(1) は「front door へ投入して弾かれる」形だが、私は投入していない。
理由: C-2(b-3) で実測済み — OBSERVE_CURRENT_STATE に分類された投入は submit.py:506 に到達せず、
      契約が封印されない。∴ 投入しても「弾かれた/素通りした」のどちらになるかは経路次第で、
      弾かれなかった場合に task が1件 増える。純関数で逐語が確定するので、投入は行わなかった。
★「front door で止まる」ことは示していない。示したのは「契約を封印する経路に載れば止まる」ことである。
```

# 5. 予告の当否（`evo0034_pre.txt`）

| 予告 | 結果 |
|---|---|
| contract_seal.py +14行前後 | **ほぼ当たり**（実測 +17） |
| (1) 純関数で逐語は出せる／投入では示さない | **当たり** |
| (2) A64D0C6D の契約が通る・標準ライブラリも通る | **当たり** |
| (3) 23本 通る | **当たり** |
| (4) 一覧・チェック欄を作らない | **当たり** |

# 6. 走行

```
webui 再起動を確認（9項目 #5）: 起動 12:21:46 > contract_seal.py 12:20:36。
走行 0・task 増 0・:8005 を叩いていない・台帳を直読していない・commit していない。
★C-2(b) の雛形注意書き「※ 試験は `import impl` と書いてください」は これで機械が見る側へ移った。
   ただし `<<<FILL` の注意書きは残す（そちらはまだ機械が見ていない）。
```
