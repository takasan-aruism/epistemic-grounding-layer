# 【監査＋事前調査】報告を確認した — **★「欄を足す」は検査を壊さない**（★次の1件の前提）

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 15:1x / TYPE=FINDING
- **運用方針 確認済（版: v2.9）** ／ **受領**: `CC_MGR_2026-07-31_D173_…`（★Taka 宛の報告）
- **★新しい名前で置いた** ／ **★実装していない・提案していない・着手していない**

---

## 1. ★Taka 宛の報告（`D-173`）を監査した — **★誤りは見つからなかった**
```
★§1 の様式・§3 の衝突3件・§4 の「3対1」・§5 の「却下したのは我々」——★すべて私の露出と一致する。
★★§2 の「あなたの仮説は方向が当たっており、程度が違った（強弱ではなく片方が存在しない）」も、
   ★17欄の列挙という★私の実測に基づいている。★過大ではない。
```

---

## 2. ★次の1件の前提を、先に確かめた（★着手ではない。★調査である）

**`D-173` §7 の案**: **「出力様式に『既に取得済だから作らない』を書ける欄を1つ足す」**

| # | 確かめたこと | ★結果 |
|---|---|---|
| **1** | **★欄を足すと `validate_plan` が壊れるか** | **★壊れない。**検査は「**★欄が無いこと**」だけを見る（逐語: `"malformed: missing structured key %r"` / `"missing executable key %r"`）。**★余分な欄を拒否する検査は無い** |
| **2** | **★worker が読む形式が壊れるか** | **★壊れない。**worker が消費するのは `EXECUTABLE_KEYS`（`requirement`/`target_file`/`test_file`/`test_body`/`test_command`/`allowed_files`）**★のみ**（`build_planner.py:49`） |
| **3** | **★足す場所は1箇所か** | **★★設計判断が要る（§3）** |

## 3. ★設計判断が要る1点（★私は決めない。★MGR の裁定を仰ぐ）
```
★(a) ★prompt にだけ足す  → ★1箇所。★ただし ★planner が書かないことが在りうる（★測れば分かる）
★(b) ★prompt ＋ `STRUCTURED_KEYS` に足す → ★2箇所。★書かないと★却下される（★fail-closed）
      ★★強いが、★既存の挙動を変える（★今まで通っていた形の計画が★落ちる可能性が在る）
★★私の見立て: ★(a) が「一度に一箇所」に合う。★(b) は「効かなかったら次に」でよい
★★★ただし決めるのは MGR である。★私は材料までを出す。
```

---

## 4. ★私が書きかけて止めた誤り（★7回目。★文書に入る前に止めた）

```
★★私は `DESTRUCTIVE_MARKERS` に `"docker "` が在るのに、★docker を使う計画が通っているのを見て、
   ★「★照合が大文字小文字を区別しており、`Docker`（大文字D）が★すり抜けている」と★書こうとした。
★★確かめた: ★`_serialized_body` は ★`.lower()` している（`build_planner.py:253` 逐語）
   ∴ ★照合は★大文字小文字を区別しない。★私の疑いは★外れた。
★★★通った本当の理由: ★計画の `authority_requirements` に
   逐語「Read access to system binaries (ps, nvidia-smi, ★docker)」が在り、
   ★`m.strip() not in auth` が偽になった ＝ **★権限を宣言したうえで使っている**
   ＝ ★fail-closed の仕組みが★設計どおり働いている。
★★★本日7回目の同型だが、★今回は★文書に入る前に止めた（★前6回は書いてから訂正した）。
```

---

## 5. ★やっていないこと
```
★PLAN を修正していない ／ ★欄を足していない ／ ★新規監査を作っていない ／ ★GENERATE を実行していない
★prompt を変更していない ／ ★コードを1行も変えていない ／ ★投入していない
★★Taka の「★PLANの修正、新規監査、GENERATE実行はその確認後です」を守っている
```

---
**決めたこと**: **①`D-173`（Taka 宛の報告）を監査し、誤りは見つからなかった ②次の1件「欄を1つ足す」の前提を先に確かめた——`validate_plan` は「欄が無いこと」しか見ず余分な欄を拒否しないので壊れず、worker が消費するのは `EXECUTABLE_KEYS` のみなので形式も壊れない ③ただし「prompt にだけ足す（1箇所・書かれないことが在りうる）」か「`STRUCTURED_KEYS` にも足す（2箇所・fail-closed だが既存の挙動を変える）」かは設計判断であり、私の見立ては (a) だが決めるのは MGR ④`DESTRUCTIVE_MARKERS` が大文字を素通りしているという指摘を書きかけたが、`_serialized_body` が `.lower()` しており誤りだった——通った理由は `authority_requirements` に docker を宣言していたからで、fail-closed は設計どおり働いている（本日7回目の同型だが、今回は文書に入る前に止めた）。**
