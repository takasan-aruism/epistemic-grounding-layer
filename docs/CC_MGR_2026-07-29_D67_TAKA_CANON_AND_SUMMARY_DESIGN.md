# CC 管理(MGR) → 設計/監査(CC-α): **★Taka 典拠が入った。主張文を固定する。★集約の形は Taka が指定した（発明しない）**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **典拠**: `docs/TAKA_2026-07-29_FIRST_CLOSED_LOOP_OBSERVATION.md`（★逐語・全文）

## 1. ★主張文を固定する（★これ以外で成果を書かない）
> **「2DERが初めて、Web UIから実環境を観測し、証拠をEGLに残して応答する一周を通した。ただし、完了追跡と人間向け要約は未完成」**

**★「一周した」を「動く」「使える」「自律した」に言い換えない。** **★Taka が6件を明示で禁じた**（典拠 §3）。

## 2. ★集約の形は Taka が指定した — ★我々が発明しない
```
（Taka 逐語）
継続中タスク 160件
・現在前面化すべきもの 0件
・停止中 12件
・依存待ち 8件
・背景保持 140件
のように集約し、必要な場合だけ開ける形が妥当です。
```
> **★この4区分をそのまま使う。** **★増やさない。★言い換えない。**
> **★Taka が挙げた欠落5件も逐語で持つ:「タスク名なし／状態なし／開始日なし／優先度なし／なぜ表示されたか不明」。**

**★調査を先にすること（実装ではない）:**
| # | |
|---|---|
| **1** | **★4区分に振り分けられるか。** **★振り分けの根拠が実データに在るかを実測する** |
| **2** | **★根拠が無い区分は「無い」と書く**（★埋めない。★推測で分類しない） |
| **3** | **★「なぜ表示されたか」を出せるか**（出せなければ「出せない」と書く） |
| **4** | **★件数を先に出す。** **実装はその後。私に返すこと** |

> **★これは今日ずっとやってきた形と同じである——★存在を先に決定論で確定してから、分岐へ渡す。**
> **★LLM に分類させない。★機械で決まるものだけを区分にする。**

## 3. ★Taka が言い当てた核（記録する）
> **「システムが持っている情報と、人間が判断に使える情報は違う」**

**★我々は D-57 で出力24件、D-62 で入力8件を数えた。** **★どちらも「1件ずつの言い方」だった。**
**★今回は「量」と「並べ方」である。** **★数え切りの述語に入っていなかった。**

## 4. ★私の指標の誤りについて（Taka の評価を採る）
> **「前回の結論自体は結果的に合っていたが、根拠は間違っていた」**
> **「★結果が合っていたことと、判断方法が正しかったことを分けられた」**

**★これを本日の到達点として記録する。** **★当たったことを実力と読み替えない。**

## 5. ★順番（変えない）
```
① SENT を成功時にも残す（小さい・目的の半分を埋める）
② 継続中タスクの集約（★調査 → 件数 → 私に返す → 実装）
③ D-62（入力4件の文言）
```

## 6. ★変えないもの
**勘定科目に触れない**（`G-62`/`G-64`〜`G-68` 据え置き）。**★投入しない。★規則文書を作らない。★`node --check` は配信物に対して。**

---
*MGR。★**主張文を固定**=「**2DERが初めて、Web UIから実環境を観測し、証拠をEGLに残して応答する一周を通した。ただし、完了追跡と人間向け要約は未完成**」——**これ以外で成果を書かず、「一周した」を「動く」「使える」「自律した」に言い換えない**（Taka が6件を明示で禁じた）。★**集約の形は Taka が指定したので我々が発明しない**=「継続中タスク 160件／現在前面化すべきもの 0件／停止中 12件／依存待ち 8件／背景保持 140件 のように集約し、必要な場合だけ開ける形が妥当」——**この4区分をそのまま使い増やさず言い換えず**、Taka が挙げた欠落5件（**タスク名なし／状態なし／開始日なし／優先度なし／なぜ表示されたか不明**）も逐語で持つ。**調査を先に**=(1)4区分に振り分けられるか・**振り分けの根拠が実データに在るかを実測**(2)**根拠が無い区分は「無い」と書く（埋めない・推測で分類しない）**(3)「なぜ表示されたか」を出せるか（出せなければ「出せない」と書く）(4)**件数を先に出し実装はその後・MGR に返す**——**今日ずっとやってきた形と同じで、存在を先に決定論で確定してから分岐へ渡す。LLM に分類させず機械で決まるものだけを区分にする**。★**Taka が言い当てた核を記録**=「**システムが持っている情報と、人間が判断に使える情報は違う**」——**我々は D-57 で出力24件・D-62 で入力8件を数えたがどちらも「1件ずつの言い方」であり、今回は「量」と「並べ方」で数え切りの述語に入っていなかった**。★**MGR の指標の誤りについて Taka の評価を採る**=「前回の結論自体は結果的に合っていたが根拠は間違っていた」「**結果が合っていたことと、判断方法が正しかったことを分けられた**」——**これを本日の到達点として記録し、当たったことを実力と読み替えない**。★順番=**①`SENT` を成功時にも残す（目的の半分を埋める）②継続中タスクの集約（調査→件数→MGR に返す→実装）③D-62**。★変えないもの=勘定科目・投入しない・規則文書を作らない・**`node --check` は配信物に対して**。*
