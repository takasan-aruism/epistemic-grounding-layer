# 【BUILT 監査 / C-1】**★書けている（独立に確認）** — ★ただし記録される「いつ」が★固定の偽日付

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-08-01 02:1x / TYPE=FINDING
- **開発者規律 確認済（版: v1.0 / 2026-07-31）** ／ **受領**: `CC_IMPL_2026-08-01_C1_PROGRESS_WRITE_WORKS_BUILT.md`
- **この .md がまだ .md である理由**: **★監査結果を台帳へ書く口はまだ無い**（★C-1 は ★進捗の口であって ★監査の口ではない）
- **★報告を読む前に自分で測った** ／ **★コードを1行も変えていない**

---

# 1. ★独立に確認した（★front door だけを使い、台帳を直読していない）

| 確認 | 結果 |
|---|---|
| `GET /api/resolve?id=ITEM-2DER-IMPL-PLATFORM-VLLM-RUNTIME-PROFILER` | **`resolved: true`** |
| `status_note`（逐語） | **`actor=Claude stage=VERIFY via=front_door note=口の試験で動かした値を元へ戻す（進捗の主張ではない）`** |
| ★∴ | **★front door から書いた記録が ★台帳に入り、★front door から読める。★受入①② は ★成立している** |
| 台帳の行数 | `audit/ROADMAP_REGISTRY.jsonl` **+2行**（★変更1・戻し1。★IMPL の申告と一致） |
| `status_counts` が私の観測時に動いていなかった理由 | **★IMPL が試験後に値を戻したため**（★§4 に申告済）。★食い違いではない |
| 差分 | `submit.py +8` / `webui.py +1` / `progress_seal.py` 新規。**`STRUCTURED_KEYS`/`EXECUTABLE_KEYS`/`validate_plan`/`_MAP` は diff 内 0件** ✓ |

## 1-1. ★作法として特筆する2件（★私が指示していないのに正しくやった）
```
★① ★例文の `actor: 2DER` を ★`actor: Claude` に★自分で直した。
   ★逐語:「★書かせているのは私である ∴ `2DER` と書けば主体を偽る」
   ★★★これは ★Taka の成功指標（★誰の仕事か）の ★根幹である。★私の SPEC の例文の方が★危なかった。
★② ★受入③ を ★「○」ではなく ★「部分的」と判定し、★理由に「★現に本報告は .md である」と書いた。
   ★★自分の成果を ★自分で割り引いた形。★正しい。
```

---

# 2. ★★私が見つけた欠陥1件（★IMPL は報告していない・★責めない）

> ### **★書き込まれる `registered_at` が ★`2026-07-11T08:00:00` という★固定値である。**

```
★実測（★`/api/resolve` の逐語）: `"registered_at": "2026-07-11T08:00:00"`
★★原因（★決定論・逐語）:
   ★`twoder/webui.py:657`  `tr = SUB.submit(b.get("raw", ""))`   ← ★`ts` を★渡していない
   ★`twoder/submit.py:94`  `ts = ts or "2026-07-11T08:00:00"`    ← ★既定値が★固定の日付
   ★`set_status(rid, status, ts=ts, …)` は ★その `ts` を ★`registered_at` に入れる（`roadmap_registry.py:94`）
★★★∴ ★front door から書く進捗は ★★すべて 2026-07-11 と記録される。
```

## 2-1. ★なぜこれが C-1 の目的に効くか
```
★★C-1 を ACTIVE にした理由（`D-201` §1・Taka 逐語）は
   「★ledger の更新が行われてないと ★モニター漏れするんかな」＝ ★★「いつ更新されたか」を機械で知ること。
★★★ところが ★書くほど ★`registered_at` は ★2026-07-11 になる。
   ★状況表が出している ★`roadmap-last-write 2026-07-22` は ★★これより新しい
   ∴ ★★書けば書くほど「最終更新」は ★古くなる／★動かない。
★★★★★∴ ★「書けた」は成立しているが、★★C-1 の目的（★いまどこかが分かる）は ★まだ達成していない。
★★★★★★私はこれを ★1回の観測で断定していない: ★`webui.py:657` が `ts` を渡していないことと、
   ★`submit.py:94` の既定値が固定であることは ★どちらも ★逐語のコードで確認済である。
```

## 2-2. ★これは誰の誤りか（★はっきり書く）
```
★★IMPL の誤りではない。★私の SPEC の誤りでもない（★どちらも `ts` に触れていない）。
★★★既存の造りである（★`submit.py:94` は ★C-1 より前から在る）。
★★★★∴ ★「直す」対象であって「差し戻す」対象ではない。★★BUILT は ★受け取ってよい。
```

---

# 3. ★次の1件（★私は決めない。★材料を出す）

| | 案 | 大きさ | 副作用 |
|---|---|---|---|
| **(1)** | **`webui.py:657` で ★実時刻を渡す**（`SUB.submit(raw, ts=<now>)`） | **★1行** | ★`submit` 全体の `ts` が実時刻になる ＝ ★DW event の時刻も変わる。**★広い** |
| **(2)** | **`progress_seal` の呼び出しだけ ★実時刻を渡す**（`set_status(..., ts=<now>)`） | **★1行** | **★進捗だけに閉じる。★他の記録の時刻は★変わらない** |
| (3) | 直さない | 0 | ★「最終更新」が永久に動かない ∴ ★C-1 の目的が立たない |

**★私の見立て**: **★(2)。** 理由: **★C-1 の目的は「進捗の最終更新が分かること」に閉じている**ので、
**★他の記録の時刻まで変える理由が無い**（★規律9: 一つ進めるために二つ動かさない）。
**★★ただし ★`2026-07-11` の固定値が ★他のどこで前提にされているかを ★私はまだ数えていない** ∴ **★(1) を選ぶなら先に数えること。**

---

# 4. ★判定
```
★★BUILT は ★受け取ってよい。★受入①② は ★私も独立に確認して ★成立している。
★★★受入③「.md を増やさずに現在地が言える」は ★IMPL の「部分的」を ★私も採る。
   ★理由: ★★本監査文書も .md である ∴ ★まだ言えていない。
★★★★廃止（★進捗を .md に書く運用／人が set する自己申告値）は ★★条件つきで進めてよい:
   ★「★進捗」の .md は廃止できる。★「★裁定・監査・調査」の .md は ★口がまだ無い ∴ ★残る。
   ★★★これを混ぜて「.md を廃止した」と書かないこと。
```

---

# 5. ★やっていないこと
```
★コードを1行も変えていない ／ ★`ts` を直していない ／ ★台帳へ書いていない（★試験もしていない）
★台帳を直読していない（★`/api/resolve` と `/api/roadmap` と `/api/control` だけ）
★`2026-07-11` の固定値が他のどこで前提にされているかを★数えていない（★§3 の但し書き）
★★S-3 に触っていない（★`JUDGE_REQUIRED` は依然 ★誰の手番でもない）／ ★commit していない
★★★後回しはそのまま: `D-199` ／ C-3 ／ `D-191`
```

---
**決めたこと**: **①独立に確認した——`/api/resolve` で `status_note` に `via=front_door` の記録が引け、front door から書いて front door から読めている。受入①② は成立する。私の観測時に counts が動いていなかったのは IMPL が試験後に値を戻したためで、食い違いではない ②IMPL が私の指示なく正しくやった2件を特筆する——例文の `actor: 2DER` を「書かせているのは私だから」と `Claude` へ直したこと（私の SPEC の例文の方が危なかった）と、受入③ を「○」でなく「部分的」と自分で割り引いたこと ③私が見つけた欠陥1件——書き込まれる `registered_at` が `2026-07-11T08:00:00` の固定値である。`webui.py:657` が `ts` を渡さず `submit.py:94` の既定値が固定日付だから ④これは C-1 の目的に直接 効く。Taka が求めたのは「いつ更新されたか」だが、書くほど `registered_at` は 2026-07-11 になり、いま表示中の `roadmap-last-write 2026-07-22` より古い ∴ 書くほど最終更新が動かない ⑤これは IMPL の誤りでも私の SPEC の誤りでもなく既存の造りである ∴ 直す対象であって差し戻す対象ではない。BUILT は受け取ってよい ⑥次の1件は「進捗の書き込みだけ実時刻を渡す」を推す（1行・進捗に閉じる）。`webui` 全体に渡す案は広すぎ、選ぶなら `2026-07-11` を前提にしている箇所を先に数えること——私はまだ数えていない ⑦廃止は条件つき。「進捗」の .md は廃止できるが「裁定・監査・調査」の .md は口がまだ無いので残る。混ぜて「.md を廃止した」と書かないこと。**
