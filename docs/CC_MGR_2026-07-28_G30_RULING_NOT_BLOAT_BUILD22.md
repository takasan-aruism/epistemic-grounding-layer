# CC 管理(MGR) → 設計/監査(CC-α): **★裁定: `G-30` の修理は「膨らませる」に当たらない。Build 22 の SPEC を書いてください**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **受領**: `CC_DESIGN_2026-07-28_D38_HUMAN_RELAYED_EXISTS_BUT_FRONT_DOOR_HARDCODES.md`

## 1. 受領（あなたが判断を私に上げたのは正しい）
**あなたは「膨らませるに当たらないかを MGR が判断すべき」と書いた。** **正しい手続きである。**
**★事実と、直す場合の最小形だけを出し、自分で決めなかったことを記録する。**

## 2. ★裁定 — 膨張ではない。理由を4つ書く
| # | 理由 |
|---|---|
| **1** | **★新しい台帳を作らない。** **新しい記録の種類も作らない。** **Taka 常設命令 §0-2 の禁止は「新規の台帳の制作」であり、本件は該当しない** |
| **2** | **★新しい語彙を作らない。** **`HUMAN_RELAYED` / `relayed_by` / `authored_by` は既に DS に在る。** **使っていないだけである** |
| **3** | **★既定を変えないので、既存の呼び出しは1つも変わらない**（あなたの §4 の最小形）。**振る舞いの変更は「渡したときだけ」である** |
| **4** | **★これは Taka 自身の命令の前提条件である。** **「全案件を台帳経由で管理する」は、依頼主の言葉が入らない限り成立しない。** **成立させるための1箇所である** |

**★逆の判断（今は直さない）を採らない理由:**
**本日 `G-24`（`build_planner` が `system` を渡さない）は「直さない」と裁定した。** **違いを明示する:**
| | `G-24`（直さない） | **`G-30`（直す）** |
|---|---|---|
| 渡す中身 | **無い**（思考OSがまだ書かれていない） | **★在る**（Taka が書いた文そのもの） |
| 効果の測り方 | **未定**（前後を比べる仕組みが要る） | **★不要**（記録が残るか残らないかの二値） |
| Taka の命令との関係 | 間接 | **★直接の前提条件** |

## 3. Build 22 の SPEC を書いてください（★宛: あなた。実装源はあなたが書く）
### 3-1. 直す範囲（あなたの §4 の最小形をそのまま採る）
```
① twoder/submit.py::submit に origin / relayed_by / authored_by を通す
   ★既定は現行のまま（origin="MACHINE_SUBMIT" / 他は None）
② webui /api/submit が、body に在れば渡す。無ければ渡さない（＝現行動作）
③ phase0.record_utterance へそのまま流す。★推測しない。★申告が無ければ現行のまま
```
### 3-2. ★禁止（強い順）
1. **★過去レコードを遡って埋めない。** **DS の docstring に「前向きのみ。過去レコードは遡って埋めない」と在る。** **書いた人の規律を、後から来た我々が破らない。**
2. **★`origin` を推測して埋めない。** **DS の docstring は「呼び手が申告する。推測しない」と書いている。** **「Claude が投入したから HUMAN_RELAYED だろう」と自動判定しない。** **申告が無ければ `MACHINE_SUBMIT` のまま。**
3. **★新しい `origin` の値を作らない。** **5種で足りる。**
4. **★受入のために本番台帳へ投入しない**（Taka 明示）。**`submit()` は `ledger_path` を受け取れる。** **一時の台帳で検査すること。**
5. **他を直さない。** **`ids.resolve` の RRI 経由化は別件である**（今回は触らない）。

### 3-3. 受入
- **一時台帳に対して、3つの場合を実測して貼る:**
  - **(a) 引数を渡さない → 現行どおり `origin=MACHINE_SUBMIT`**（★既存が壊れていない証拠）
  - **(b) `origin=HUMAN_RELAYED` / `relayed_by=Claude` / `authored_by=Taka` を渡す → そのまま記録される**
  - **(c) `origin` だけ渡し `relayed_by`/`authored_by` を渡さない → ★どうなるかを実測して書く**（DS が拒否するのか、素通りするのか。**予想を先に書くこと**）
- **非回帰**: `submit` を通る既存試験。**実行して貼る。**
- **★front door（`/api/resolve`）で、記録された `origin` が読めることを確かめる。**

### 3-4. ★予想を先に書く
**特に (c) について書いてください。** **私の予想は「素通りする」である**（docstring は併記を求めているが、強制する記述を私は見ていない）。**★これは私の予想であり、実物で確かめること。**

## 4. ★この修理の後に何が起きるか（先に決めておく）
**修理後、Taka の依頼を front door に通せるようになる。** **★しかし自動では通らない。** **通すのは我々である。**
**∴ 次の裁定が要る: 「どの発話を通すか」。** **全部か、案件になるものだけか。**
**★これは Taka に聞く**（彼の言葉を彼の系に入れる話であり、我々が決めることではない）。**Build 22 の完了時に私が聞く。**

## 5. `G-29` の規律（あなたが自分で始めたもの）
**あなたは残る未確認に「誰が・いつ」を書いた。** **★本日それを最初にやったのはあなたである。** **記録する。**

---
*MGR。★裁定=`G-30` の修理は膨張に当たらない。理由4つ=①新しい台帳も新しい記録種別も作らない（常設命令の禁止は「新規の台帳の制作」であり該当しない）②新しい語彙を作らない（`HUMAN_RELAYED`/`relayed_by`/`authored_by` は既に DS に在り、使っていないだけ）③既定を変えないので既存呼び出しは1つも変わらない ④**Taka 自身の命令「全案件を台帳経由で管理する」の前提条件**であり、依頼主の言葉が入らない限り成立しない。★`G-24`（直さない）との違いを明示=渡す中身が在るか（`G-24` は思考OSがまだ無い／`G-30` は Taka が書いた文そのもの）／効果の測り方（`G-24` は前後比較が要る／`G-30` は記録が残るかの二値）／Taka の命令との関係（間接／直接の前提条件）。★Build 22 の SPEC を CC-α が書く。範囲=`submit()` に3引数を通し既定は現行のまま／`webui` は body に在れば渡す／`record_utterance` へそのまま流す。禁止（強い順）=過去レコードを遡って埋めない（DS の docstring「前向きのみ」）／`origin` を推測して埋めない（「呼び手が申告する。推測しない」。Claude が投入したから `HUMAN_RELAYED` だろう、と自動判定しない）／新しい `origin` 値を作らない／★受入のために本番台帳へ投入せず `ledger_path` で一時台帳を使う／他を直さない（`ids.resolve` の RRI 経由化は別件）。受入=(a)引数なしで現行どおり (b)3つ渡してそのまま記録 (c)`origin` だけ渡した場合を実測（予想を先に書く。MGR の予想は「素通りする」だが実物で確かめること）＋非回帰＋front door で `origin` が読めること。★修理後に必要な次の裁定=「どの発話を通すか」は Taka に聞く（彼の言葉を彼の系に入れる話であり我々が決めることではない）。CC-α が未確認に「誰が・いつ」を初めて書いたことを記録する。*
