# 【BUILT / D-213 §2】監視を張り替えた — **★`status_note` は★実際に拾えた（逐語3件）**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: MGR / Taka / 発: 実装(IMPL) / 2026-08-01 19:4x / TYPE=BUILT
- **開発者規律 確認済（版: v1.1 / 2026-08-01）** ／ **実装源**: `CC_DESIGN_2026-08-01_D213_RELAY_WATCH_THE_LEDGER_NOTE.md`
- **★production コードを1行も変えていない**（★監視は 2DER の外・私の計器）／ **commit していない** ／ **`:8005` を叩いていない** ／ **`/api/control` を叩いていない** ／ **台帳を直読していない**

---

# 1. ★張り替えた監視の5項目

| | 中身 |
|---|---|
| **入力元** | ① `GET /api/roadmap`（★item と status だけ・**★note は返らない**＝SPEC §5 のとおり）／ ② `GET /api/resolve?id=ITEM-…` の **`record.status_note`** ／ ③ `egl/docs/CC_*.md`（**★`CC_MGR_*` を除く**・先頭14行の**★「宛」から次の `/` までの宛欄**に `IMPL` か `実装`。★★行全体で見ると `発: 実装(IMPL)` に当たる＝★§2-1） |
| **算出規則** | 見る ITEM ＝ **(status が `DONE`/`DROPPED` 以外の全 item) ∪ (`ITEM-2DER-EVO-0021`〜`0032` の12件)**。★後者を固定で足す理由: **★`DONE` の item にも裁定 note が載る**（★実例: `EVO-0022` は `DEFERRED`、`EVO-0021` は `DONE`）。署名は `(status, status_note)` の対。180秒ごと |
| **鳴らす条件** | ① note が変わった（★status が同じでも鳴る＝SPEC §2 の指摘）／ ② status が変わった ／ ③ 見ていなかった item が現れた ／ ④ **★`/api/resolve` が引けない**（`resolved` が偽・`record` が dict でない）／ ⑤ `/api/roadmap` が読めない ／ ⑥ 宛:IMPL の `.md` が新規・更新（★名前＋mtime＋size） |
| **廃止条件** | **★`ITEM-2DER-EVO-0032` の note に在るものをそのまま使う（逐語）**:「廃止条件=監視が note を拾い、MGR 発の指示が2回続けて .md 無しで届いたら本件完了し MGR は .md を出さないを既定にする。届かなければ .md に戻し「戻した」と書く」★→ .md に戻す時は **★`CC_MGR_*` を除く1行を戻す**（★戻し方が1行で書ける） |
| **既存計器との重複確認** | ★重複なし。★MGR の状況表フックは `status_note` を出していない（★出しているのは repo/台帳件数/A値）。★`/api/control` は叩いていない（★MGR と重複するため・SPEC §5 の禁止）。★私のもう1本（task の `dw_state`）は **ITEM ではなく task** を見ており対象が違う |

---

# 2. ★1本目に何が出たか（★機械が刻んでいる）

```
[基準値/機械が刻む] これは起動時の基準値であり、台帳が動いた証拠ではない（D-194 再発防止）。
ITEM 25 件を見ている（うち note 設定済 16 件・未設定 9 件）／ ★引けなかった 0 件 ／
宛:IMPL の .md 132 件（CC_MGR_* は捨てた）
```
（★これは ★直した後の本番＝`bcxldojwd` の1本目。★誤った版の1本目は ★§2-1 に逐語で残す）
★この1行は **★私が書いた文言ではなく ★スクリプトが毎回 起動時に印字する**（★人の申告ではない）。

## 2-1. ★★私は数え方を2度 変え、★1度目は ★誤りだった（★実際に誤報が鳴った・★直した）

| 版 | 規則 | 件数 | 判定 |
|---|---|---|---|
| 試走 | `宛: IMPL` の**正確一致** | **122** | ★狭い（`宛…実装` を落とす） |
| **1本目（★誤り）** | **★行全体**に「宛」かつ `IMPL`/`実装` | **★345** | **★★誤り** |
| **本番（★これが正）** | **★「宛」から次の `/` までの★宛欄だけ**（★旧監視 `grep -o '宛[^/]*'` と同じ） | **★132** | ★正 |

```
★★誤りの中身: ★行全体で見ると ★★`発: 実装(IMPL)` に当たる ＝ ★★自分が出した文書で鳴る。
★★★実際に鳴った（★逐語）: 「[.md 実装宛] CC_IMPL_2026-08-01_D213_LEDGER_WATCH_BUILT.md mtime=19:42:33」
   ＝ ★★本文書そのもの。★★私の BUILT の宛先は ★設計であって ★IMPL ではない。
★★★★★∴ ★345 は ★★宛先ではない文書を ★213件 抱き込んだ数である。★★使わない。
★★★★★★正しい基準値は ★★132 件（★うち ★`CC_IMPL_*` は ★4件＝★本当に IMPL 宛の古い文書）。
★★★★★★★★直し方: ★旧監視と同じ規則に戻しただけである（★私が勝手に広げたのが誤り）。
```

---

# 3. ★捨てた条件（★足して 捨てた・規律9）

```
★捨てた: ★`CC_MGR_*.md` を待つ条件（★逐語で1行）: `if b.startswith('CC_MGR_'): continue`
★残した: ★`CC_DESIGN_*.md` ほか 宛:IMPL の .md（★SPEC §2「私の BUILD SPEC は当面 .md のまま」）
★★増えた管理対象: ★0。★監視の本数は ★張り替え前後で ★変わっていない（★1本 止めて 1本 立てた・`bggggv131` → `bw9voxgzg`）
```

## 3-1. ★正直に書く: ★私の監視は元々 ★2本 在る
```
★SPEC §2 は「★あなたの監視は1本のまま」と書いているが、★私は ★2本 動かしている:
   ★① 文書＋（今回）台帳 note  ← ★今回 張り替えたのはこちら
   ★② 私が回している ★6 task の `dw_state`（★別物・★対象が task）
★★★∴ ★「1本」は ★①を指すと解した。★★3本目は作っていない。★合計は ★2本のままである。
★★★★食い違いが在るなら ★設計が直すこと（★私は解釈を勝手に確定しない）。
```

---

# 4. ★★`status_note` を実際に拾えたか → **★拾えた（★逐語3件）**

```
★① ITEM-2DER-EVO-0032 (IN_PROGRESS) 逐語（先頭）:
   「actor=MGR stage=PLAN via=front_door note=D-213。読む側を先に移す。設計/IMPL の front door 監視に
     ACTIVE ITEM の status_note 変化を足し CC_MGR_*.md 待ちを捨てる。廃止条件=…」
★② ITEM-2DER-EVO-0031 (IN_PROGRESS) 逐語（先頭）:
   「actor=MGR stage=ADJUDICATE via=front_door note=裁定(この回は .md を作らない)=原因確定。理由は
     generate_via_runner.py:213-216 で作られているのに webui.py:327-329 が4欄しか採らず
     runner_exit と runner_stdout_tail を捨てている。設計へ: 最小・可逆でこの2欄を test_result に足す BUILD SPEC を出すこと…」
★③ ITEM-2DER-EVO-0022 (DEFERRED) 逐語:
   「actor=MGR stage=ADJUDICATE via=front_door note=★後回しにした。受入(b)は成立済み。残る受入(c)(d)は
     EVO-0031 が通らないと示せない。戻す条件=EVO-0031 完了」
★★★★∴ ★「.md 無しの裁定」は ★★実際に届く。★②は ★宛が設計であり ★私の実装源ではない（★読めただけ）。
```

---

# 5. ★★私が1度 間違えた（★直した・隠さない）

```
★最初の版は ★`status_note` の ★★キーが無い item を ★「読めない9件」として鳴らした。
★★実測して直した: ★`/api/resolve` は ★`resolved: true` を返し ★record も返る。★★キーが無いだけである
   （★`ITEM-2DER-EVO-0010` / `ITEM-2DER-OFFRAMP` で確認）＝ ★★「読めない」ではなく ★「まだ note が付いていない」。
★★★そのまま出していたら ★毎回9件 鳴り、★★計器が狼少年になっていた。
★★★★直した後: ★note 未設定は `None` として保持し、★後で note が付けば ★`None → 文字列` の変化として1行 出る
   ＝ ★★取りこぼさずに ★黙る。★引けない場合だけ「読めない」と鳴らす（★沈黙しない・第6章）。
```

---

# 6. ★やっていないこと
```
★`/api/roadmap` で `status_note` を取ろうとしていない（★返らないことを実測で確認済）
★`/api/control` を叩いていない ／ ★台帳を直読していない ／ ★`:8005` を叩いていない
★production コードを1行も変えていない ／ ★commit していない ／ ★task を1件も増やしていない
★★「受け渡しが台帳へ移った」とは書かない（★★半分である。★我々→MGR は .md のまま）
★★★測っていない: ★MGR 発の指示が ★.md 無しで ★2回続けて届くか（★★これから起きること・★廃止条件そのもの）
```

---
*IMPL → 設計/監査（写: MGR / Taka）。D-213 §2。**監視を張り替えた（`bggggv131` を止めて立て直し、誤りを1件 直して `bcxldojwd` にした＝本数は増やしていない。production コードは1行も変えていない）。** 入力元は `/api/roadmap`（item と status のみ・note は返らないことを実測）＋`/api/resolve?id=ITEM-…` の `record.status_note`＋`egl/docs` の .md。見る ITEM は「`DONE`/`DROPPED` 以外の全 item」∪「`EVO-0021`〜`0032` の12件」——**`DONE` や `DEFERRED` の item にも裁定 note が載るため固定で足した**。鳴らすのは note 変化（status が同じでも鳴る）／status 変化／新規 item／resolve が引けない／roadmap が読めない／宛:IMPL の .md の新規・更新。**廃止条件は `EVO-0032` の note に在るものをそのまま使う（監視が note を拾い、MGR 発の指示が2回続けて .md 無しで届いたら完了。届かなければ .md に戻す）。** **捨てたのは `CC_MGR_*.md` を待つ条件（逐語1行 `if b.startswith('CC_MGR_'): continue`）で、`CC_DESIGN_*` は残した。** 1本目は機械が刻む形で出た——「これは起動時の基準値であり、台帳が動いた証拠ではない（D-194 再発防止）。ITEM 25 件（note 設定済 16・未設定 9）／引けなかった 0 件／宛:IMPL の .md 132 件」。**★ただし私は数え方を2度 変え、1度目は誤りだった——行全体で「宛」と `IMPL`/`実装` を見たため `発: 実装(IMPL)` に当たり、345 件に膨れ、★自分が出した本 BUILT 自身で誤報が鳴った（逐語「[.md 実装宛] CC_IMPL_2026-08-01_D213_LEDGER_WATCH_BUILT.md mtime=19:42:33」）。旧監視と同じ「宛欄だけ（`宛[^/]*`）」に戻して 132 件が正しい基準値。345 は宛先でない文書を213件 抱き込んだ数で、使わない。****★`status_note` は実際に拾えた——`EVO-0032`／`EVO-0031`／`EVO-0022` の3件を逐語で載せた。∴「.md 無しの裁定」は実際に届く。** **私は1度 間違えて直した——`status_note` のキーが無い item を「読めない9件」と鳴らしていたが、実測すると `resolved: true` でキーが無いだけ＝「まだ note が付いていない」。そのままなら毎回9件 鳴って計器が狼少年になっていた。いまは未設定を `None` で保持し、後で付けば変化として鳴り、引けない時だけ「読めない」と鳴る。** 正直に書くと**私の監視は元々2本ある**（文書＋台帳／task の `dw_state`）ので SPEC の「1本」は前者を指すと解した。3本目は作っていない。**「受け渡しが台帳へ移った」とは書かない——半分である。** `/api/control` は叩かず、台帳も直読していない。commit していない。*
