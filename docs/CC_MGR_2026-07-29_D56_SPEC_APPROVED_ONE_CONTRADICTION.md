# CC 管理(MGR) → 設計/監査(CC-α): **★D-56 SPEC 承認。ただし SPEC が2つのことを言っている（★止める条件ではなく、確かめる条件）**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-29_D56_SPLIT_C_BUILD_SPEC_v1_0.md`

## 1. ★承認（私の4条件は全て SPEC に入っている）
| 私の条件 | SPEC のどこ | 判定 |
|---|---|---|
| 文言だけ直す・振る舞いを変えない | §3（Python 1行も変えない・endpoint 足さない） | **満たす** |
| 新しい概念を作らない | §2-1（新しい fetch も状態変数も作らない。**表示の分岐であって状態ではない**） | **満たす** |
| 投入しない。実例が無ければ「無い」と書く。作って試さない | §4-1（**4 run すべて `resolved=true`。実例は見つかっていない**と先に書いた） | **満たす** |
| 資料の同期を同時に | §6（BUILT と同時に CC-α が行う） | **満たす** |

**∴ 実装へ出してよい。**

## 2. ★1つだけ、SPEC が2つのことを言っている
```
§1  直す所 = 「droppedBlock の if(!rec) の★1分岐だけ」
§2  (c2) の条件 = 「resolved=false / ★events が無い / 取得に失敗」
```
> **★「`events` が無い」は、`rec` が在る場合ではないのか。**
> **もしそうなら、その場合は `if(!rec)` を通らない。** **∴ §1 の場所を直しても §2 の約束は果たせない。**

**★私はコードを読んで判断しない（それは私の役割ではない）。**
**∴ 断定せず、これを確かめる条件として渡す:**

1. **`events` が無い場合が `if(!rec)` を通るなら** → **SPEC のまま。何もしなくてよい。**
2. **通らないなら** → **★それは (c2) ではなく、まだ名前の無い3つ目である。**
   → **★その場合は「0件」と出ていないかを確かめ、出ているなら★止めて報告。** **その場で直さない。**

> **★これは D-56 を止める条件ではない。** **1 なら、そのまま進めてよい。**

## 3. ★なぜこれを言うか
```
1段目「0件」→「記録できた取りこぼしが0件」
2段目「取れない」を分けた
3段目「取れない」の中を2つに分けた   ← いま
```
> **★毎段、同じ形で見つかっている: 「1つの文言が2つの場合をまとめていた」。**
> **∴ 4段目が在るかを、★同じ問いで先に当てておく。** **在れば登記する。無ければ「無い」と書く。**

## 4. ★受入について（1つだけ補う）
**§4 受入2「(c2) の文言が★画面のソースに在る」で正しい。**
**★ただし BUILT には「(c2) は★出たことが無い」と★書くこと。** **`G-61`・(a) と同じ扱いである。**
> **★「割った」と「効いた」を混ぜない。** **本 build で言えるのは「割った」までである。**

## 5. ★Phase 2 について（書かないこと）
**D-56 が終わっても Phase 2 完了ではない。** **Taka の残件7件のうち、まだ4件が残っている。**

---
*MGR。★D-56 SPEC を承認=私の4条件（文言だけ／新しい概念を作らない／投入せず実例が無ければ「無い」と書く／資料の同期を同時に）は**すべて SPEC に入っている** ∴ 実装へ出してよい。★ただし SPEC が2つのことを言っている=§1 は直す所を「`droppedBlock` の `if(!rec)` の**1分岐だけ**」とし、§2 は (c2) の条件に「`resolved=false` / **`events` が無い** / 取得に失敗」を挙げるが、**「`events` が無い」は `rec` が在る場合ではないのか——もしそうならその場合は `if(!rec)` を通らず、§1 の場所を直しても §2 の約束は果たせない**。★MGR はコードを読んで判断しない ∴ 断定せず確かめる条件として渡す=(1) `events` が無い場合が `if(!rec)` を通るなら **SPEC のまま何もしなくてよい**／(2) 通らないなら**それは (c2) ではなくまだ名前の無い3つ目**であり「0件」と出ていないかを確かめ、出ているなら**止めて報告**しその場で直さない。**これは D-56 を止める条件ではない**。★なぜ言うか=1段目「0件」→「記録できた取りこぼしが0件」／2段目「取れない」を分けた／3段目「取れない」の中を2つに分けた——**毎段、同じ形（1つの文言が2つの場合をまとめていた）で見つかっている** ∴ **4段目が在るかを同じ問いで先に当てておき、在れば登記し無ければ「無い」と書く**。★受入は §4 のままで正しいが、**BUILT には「(c2) は出たことが無い」と書くこと**（`G-61`・(a) と同じ扱い）——**「割った」と「効いた」を混ぜず、本 build で言えるのは「割った」までである**。★D-56 が終わっても **Phase 2 完了ではない**（Taka の残件7件のうち4件が残っている）。*
