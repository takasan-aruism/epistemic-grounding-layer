# BUILD SPEC — 継続中タスクを集約して出す **v2.1**（★実装源・v2.0 を置き換える）

- `BUILD_ROLE: ★実装源`（**本文書が実装の唯一の典拠**）／**★v1.0・v2.0 は SUPERSEDED。★見ないこと**
- **★宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-30
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-30_D73v2_APPROVED_BUT_WHO_IS_KOCHIRA.md`

> **★v2.0 との違いは §1 の1点だけである。** **★他は v2.0 のまま。★§2 以降は v2.0 の本文をそのまま再掲する。**

---

## 1. ★v2.0 から変わった所（★人称をやめ、★名指しにする）
### 1-1. ★MGR の指摘は正しい
```
v2.0 の文言: 「★こちらの手番  18件」
判定        : actor_role in ("MANAGER", "CLAUDE_SENIOR")
```
> **★この画面を読むのは Taka である。★Taka は「こちら＝自分」と読む。★しかし判定は我々の側である。**
> **★同じ語が、書き手と読み手で別のものを指していた。★私の欠陥である**（★今日6つ目・文言→記法→欄→名前→母数→★人称）。

### 1-2. ★実測（★MGR §2「書けるか確かめる」への答え・★推測しない）
```
再現（★私が実行・読み取りのみ・投入なし）:
  DEFAULT_ACTORS:  CODING_WORKER → QWEN_LIVECODER / INDEPENDENT_AUDITOR → QWEN_AUDITOR
                   MANAGER → CLAUDE / CLAUDE_SENIOR → CLAUDE
  ★Taka / HUMAN / 人 を指す actor の値: ★1つも無い
  ★宣言だけ在る役割: ADJUDICATOR（`workcell.ROLES` に在るが `DEFAULT_ACTORS` に★無い）
```
> **★∴ 2DER は「Taka の手番」を★出せない。** **★人を指す担当者の値が、そもそも無いからである。**
> **★「Taka の手番は0件」と書かない。** **★0件は「無い」だが、これは「★数えられない」である。**

### 1-3. ★「誰も名指しされない18件」の正体（★実測で分かった）
```
再現: 誰も名指しされない件数 ★18 / その状態の内訳 ★{"JUDGE_REQUIRED": 18}（★全件）
     _MAP に無い状態: AUDIT_FAILED / IMPLEMENTING / ★JUDGE_REQUIRED / PLANNING / REWORK
```
> **★18件はすべて `JUDGE_REQUIRED`＝「裁定が要る」である。**
> **★機械は「裁定が要る」と言っている。★しかし誰がやるかを決める表に、この状態が載っていない。**
> **★`ADJUDICATOR` という役割は宣言されているのに、★担当者が割り当てられていない**（`G-72` の原因が分かった）。
> **★ただし「これは Taka の手番である」とは書かない。★それは私の推測である。★機械は誰とも言っていない。**

### 1-4. ★直す文言（★「こちら」「そちら」を使わない）
```
  背景で進んでいる（Qwen が動かす）          117件
  ★Claude の手番                            18件
  ★誰がやるか決まっていない                  18件
      └ 今日の18件はすべて「裁定が要る」状態です。
        ★誰がやるかを決める表に、この状態が載っていません。
  いま前に出すべきもの                      ★出せません（前に出す順番を決める値が 2DER に無いため）
  ──────────────────────────────────────
  合計                                      153件

  ※ 止まっている依頼が 2件 ありますが、この一覧の対象外です。
  ※ ★Taka の手番かどうかは、この画面では出せません（2DER に人を指す担当者の値が無いためです）。
```
| # | ★守ること |
|---|---|
| 1 | **★「こちら」「そちら」「自分」を使わない。★名指しする** |
| 2 | **★「Taka の手番 0件」と書かない。★「出せません」＋理由**（★0件は「無い」、出せないは「数えられない」） |
| 3 | **★「誰がやるか決まっていない」の下に、★その状態の内訳を出す**（★今日は `JUDGE_REQUIRED` 18件だが**★固定しない**。**★実際の内訳を数えて出す**） |
| 4 | **★「保留」「担当者未定」「Taka 待ち」と書かない**（★分からないものを分かったように書かない・`G-72`） |
| 5 | **★「背景で進んでいる」に `(Qwen が動かす)` を添える**（★実測: `QWEN_LIVECODER` 50 / `QWEN_AUDITOR` 67） |

---

## 2. ★既定（開く前）— ★ID を1件も出さない
```
進行中の依頼  153件
  この一覧に、終わった依頼と止まった依頼は入っていません。
  （★以下 §1-4 の5行）
```
| # | 条件 |
|---|---|
| 1 | **★ID を1件も出さない**（いまは155行 出ている） |
| 2 | **★「いま前に出すべきもの」を消さない。「0件」とも書かない。★「出せません」＋理由** |
| 3 | **★「その他」と書かない**（中身を隠す） |
| 4 | **★合計を出し、★4区分の和が合計と一致すること**（§6-1） |
| 5 | **★停止中は「0件」と書かない。★「この一覧の対象外」と書く** |
| 6 | **★「終わった依頼と止まった依頼は入っていません」を必ず添える**（★数だけ置かない） |
| **7** | **★「Taka の手番かどうかは出せません」＋理由 を添える**（§1-4） |

### 2-1. ★区分の判定（★すべて決定論。★LLM を使わない）
```
母数        : ★submit._active_2der_tasks(W) が返す task（＝COMPLETE と BLOCKED を除いたもの）
各 task の値: ★dispatch.next_legal_operation(task_id, ★events=一度だけ読んだもの) の返り

① 誰がやるか決まっていない : nlo["actor_id"] == "-"                        ← ★先に判定する
② Claude の手番            : nlo["actor_role"] in ("MANAGER","CLAUDE_SENIOR")
③ 背景で進んでいる          : 上のどちらでもない
④ いま前に出すべきもの      : ★判定しない（値が無い）
⑤ 停止中                   : ★母数の外。★数えない
```
> **★この順でないと重複する。**

### 2-2. ★私の実測（★今日の値。★実装源に固定しない）
```
母数 _active_2der_tasks → ★153件
  INDEPENDENT_AUDITOR/QWEN_AUDITOR 67 ／ CODING_WORKER/QWEN_LIVECODER 50
  -/- 18 ／ MANAGER/CLAUDE 13 ／ CLAUDE_SENIOR/CLAUDE 5
∴ 背景で進んでいる 117 ／ Claude の手番 18 ／ 誰がやるか決まっていない 18
  ★和 153 = 母数 153  ★一致
```

### 2-3. ★言い換えないこと
> **★「誰がやるか決まっていない」を「人を待っている」と書かない。** **★機械は誰とも言っていない。**
> **★「Claude の手番」を「あなたの手番」と書かない。**

## 3. ★開いたとき — その区分だけを出す（v2.0 のまま）
| 出す | 出さない |
|---|---|
| **`task_id`** | **開始日**（★front door に無い） |
| **依頼文の先頭30字 ＋ ★「先頭30字です（全文ではありません）。先頭が同じでも別の依頼です」** | **優先度**（★決める値が無い） |
| **状態**（`dw_state`） | |
| **なぜ表示されたか: 「COMPLETE でも BLOCKED でもないため」** | |

> **★30字の根拠（実測・D-72）: 20字→66種 / ★30字→113種 / ★40字→113種（増えない）/ 120字→124種。**
> **★先頭は「区別」には使えない**——**全文が異なるのは154/155** ∴ **★併記を必ず書く**（`G-73`）。

## 4. ★出さないものの書き方（★空欄にしない）
| 項目 | **出す文言** |
|---|---|
| **開始日** | **「開始日は出せません（front door から取れないため）」** |
| **優先度** | **「優先度は出せません（前に出す順番を決める値が 2DER に無いため）」** |
| **停止中** | **「止まっている依頼が N件 ありますが、この一覧の対象外です」** |
| **★Taka の手番** | **「Taka の手番かどうかは、この画面では出せません（2DER に人を指す担当者の値が無いためです）」** |

## 5. ★変えないもの
- **★振る舞いを変えない。★色を使わない**（`ok` も `bad` も）。
- **★`/api/state` の返りを1つも変えない。★区分を減らさない・増やさない。**
- **★LLM に分類も要約もさせない。★名前を作らない**（`G-73`）。
- **★キャッシュを作らない。★並列化しない**（**★もう要らないはずである。0.004秒 で終わる**）。
- **勘定科目**（`G-62`/`G-64`〜`G-68` 据え置き）。**★投入しない。**
- **★`ADJUDICATOR` を `DEFAULT_ACTORS` に足さない**（**★この build でやらない。★`G-72` は別 build**）。
- **★新しい endpoint は必要なら可**（**★要らないなら作らない**）。

## 6. ★受入
| # | 条件 | 示し方 |
|---|---|---|
| **1a** | **★JS のソースに、全件を並べる箇所が無い** | **★差分で数える**（★ページ全体で数えない） |
| **1b** | 実際の画面から155行が消えたこと | **★我々には示せない。「確かめていない」と書く**（★Taka にしか出せない） |
| **2** | **★4区分の和が、表示している合計と一致する** | **★数を固定しない** |
| **3** | **★合計が `_active_2der_tasks` が返す数と一致する** | **★別に数えた数を出さない** |
| **4** | **★「こちら」「そちら」「あなた」が0件** | **★差分で数える** |
| **5** | **★「Taka の手番」の行が「0件」ではなく「出せません」＋理由** | ソース |
| **6** | **★「誰がやるか決まっていない」の下に、状態の内訳が★数えて出る**（★固定文字列でない） | ソース |
| **7** | **「いま前に出すべきもの」が「出せません」＋理由／停止中が「この一覧の対象外」** | ソース |
| **8** | **開いたとき、先頭30字＋併記／開始日・優先度が「出せません」＋理由** | ソース |
| **9** | **★開いたときの所要**（★数字だけ出す。「速い」と書かない） | 実測 |
| **10** | **配信物の JS が `node --check` を通る** | **★配信物に対して**（`G-71`） |
| **11** | 既存が不変・非回帰 91/7 | **★走らせ方を BUILT に書くこと**（`G-74`・§8） |

### 6-1. ★受入2 の書き方
> **★153 は今日の値である。★固定すると明日 嘘になる。**
> **∴ 「★各区分の和が、表示している合計と一致すること」とする。**

## 7. ★止まってよい場所
| # | |
|---|---|
| 1 | **区分の判定が §2-1 のとおりに書けない** → **★止めて報告** |
| 2 | **和が合計と一致しない** → **★止めて報告**（★合わせるために区分を動かさない） |
| 3 | **既定で ID を0件にすると、既存の機能が壊れる** → 止めて報告 |
| 4 | SPEC が2通りに読める |
| 5 | **既定の ID が、JS ではなくサーバ側で入っていた** → **★止めて報告** |
| 6 | **母数が `_active_2der_tasks` では取れない** → **★止めて報告**（★別の集合に寄せない） |
| 7 | **重くて出せない** → **★止めて報告**（★区分を減らして軽くしない） |
| **8** | **★「誰の手番か」を名指しできる値が無いと分かった** → **★止めて報告**（★「こちら」で誤魔化さない） |

## 8. ★`G-74` について（★この build で1つだけ埋める）
> **★非回帰の走らせ方が、どの文書にも書かれていない**（12文書が引用し、再現コマンドは0件・D-77）。
> **★BUILT には、★実際に走らせたコマンドをそのまま書くこと。** **★`G-74` 本体の解決は別 build。**

## 9. ★資料の同期
**★MGR の commit 後に私が同期する。** **★`G-72` の note に §1-3 の原因を書き足す**（★`ADJUDICATOR` は宣言だけ在り、担当者が割り当てられていない）。

---
*CC-α D-73 BUILD SPEC **v2.1**（実装源・宛 IMPL・**v1.0/v2.0 を置き換え、見ないこと**）。★**v2.0 との違いは1点だけ＝人称をやめて名指しにする**。★**MGR の指摘は正しい**——v2.0 の「こちらの手番 18件」は判定が `actor_role in ("MANAGER","CLAUDE_SENIOR")` ＝**我々の側**なのに、**画面を読む Taka は「こちら＝自分」と読む** ∴ **同じ語が書き手と読み手で別のものを指していた（CC-α の欠陥・今日6つ目: 文言→記法→欄→名前→母数→人称）**。★**実測=2DER は「Taka の手番」を出せない**——`DEFAULT_ACTORS` は `CODING_WORKER→QWEN_LIVECODER`／`INDEPENDENT_AUDITOR→QWEN_AUDITOR`／`MANAGER→CLAUDE`／`CLAUDE_SENIOR→CLAUDE` のみで、**Taka/HUMAN/人 を指す値は1つも無く**、**`ADJUDICATOR` は `workcell.ROLES` に在るのに `DEFAULT_ACTORS` に無い** ∴ **「Taka の手番は0件」と書かない。0件は「無い」だがこれは「数えられない」である**。★**「誰も名指しされない18件」の正体=全件が `JUDGE_REQUIRED`（裁定が要る）**——**機械は「裁定が要る」と言っているのに、誰がやるかを決める表にこの状態が載っておらず、`ADJUDICATOR` という役割は宣言されているのに担当者が割り当てられていない（`G-72` の原因が分かった）**——**ただし「これは Taka の手番である」とは書かない。それは推測であり機械は誰とも言っていない**。★直す文言=「背景で進んでいる（Qwen が動かす）117件／**Claude の手番** 18件／**誰がやるか決まっていない** 18件（└ 今日の18件はすべて「裁定が要る」状態で、誰がやるかを決める表にこの状態が載っていません）／いま前に出すべきもの＝出せません＋理由／合計153件／※止まっている依頼2件は対象外／**※Taka の手番かどうかはこの画面では出せません（2DER に人を指す担当者の値が無いためです）**」。守ること=**「こちら」「そちら」「自分」を使わず名指しする**／**「Taka の手番 0件」と書かず「出せません」＋理由**／**内訳は固定せず実際に数えて出す**／**「保留」「担当者未定」「Taka 待ち」と書かない**／**「背景で進んでいる」に `(Qwen が動かす)` を添える**。★判定順は ①`actor_id == "-"` ②`actor_role in (…)` ③残り（この順でないと重複する）、母数は `_active_2der_tasks`、値は `next_legal_operation(task_id, events=一度だけ読んだもの)`。実測は 背景117／Claude 18／決まっていない18、**和153＝母数153 一致**（★今日の値であり実装源に固定しない）。★**言い換えないこと**=「誰がやるか決まっていない」を「人を待っている」と書かない／「Claude の手番」を「あなたの手番」と書かない。★変えないものに **`ADJUDICATOR` を `DEFAULT_ACTORS` に足さない（この build でやらない。`G-72` は別 build）** を追加。★受入11件（**「こちら」「そちら」「あなた」が0件を差分で数える**／**「Taka の手番」が「0件」でなく「出せません」＋理由**／**内訳が固定文字列でなく数えて出る**／所要は数字だけ／配信物の `node --check`／**非回帰は走らせ方を BUILT に書く**）。★止まってよい場所に8番=**「誰の手番か」を名指しできる値が無いと分かったら止めて報告し「こちら」で誤魔化さない**。★資料同期時に **`G-72` の note へ「`ADJUDICATOR` は宣言だけ在り担当者が割り当てられていない」を書き足す**。*
