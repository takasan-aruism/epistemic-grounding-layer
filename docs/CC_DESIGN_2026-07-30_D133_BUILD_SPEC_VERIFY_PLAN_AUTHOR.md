# 【BUILD SPEC】PLAN を作ったのが Qwen か、規則テンプレートかを**独立に確かめる**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-30 19:4x / TYPE=BUILD_SPEC
- **運用方針 確認済（版: v2.8）**
- **典拠**: `CC_MGR_2026-07-30_D133_…md` §2（MGR 裁定）。**★これは実装ではなく検証である。★何も作らない。**

## ★これは何に対して発火するのか（SPEC 冒頭の1行・運用方針 §5-5）
**★2026-07-30 19:32:13〜19:32:34 に `TASK-2DER-B11764B3` で起きた PLAN 1件。★それだけが対象である。**

## ★2DER 優先原則の5点（本 SPEC について）
```
① 入口: ★使わない（★2DER の外側＝モデルサーバ側の記録とコード構造を見る作業である）
② 試した: 設計は front door から PLAN まで到達済（D-131 の結果）
③ できなかった: ★「PLAN を誰が作ったか」を 2DER に聞く口が無い ← ★本 SPEC が要る理由
④ 実装: ★何も実装しない。★確かめるだけ
⑤ 再現: ★該当なし
```

---

# 1. 確かめること（★2つ。★両方が同じ向きに出たときだけ印2 が立つ）

## V-1. ★モデルサーバ側に、その時刻の呼び出しが在ったか（★2DER の外から見る）
```
★窓: 2026-07-30 19:32:13 〜 19:32:34（★run_next が dispatched: true を返した区間）
★見る先: :8005 を提供しているプロセスの★自分の記録
        （systemd の journal ／ nohup のログ ／ サーバが持つ metrics のいずれか。★在るものを使う）
★★2DER の台帳を見ない。★LLM 呼出台帳も使わない（★同じ出所になり検算にならない）
```
| 受入 | ★1条件に1つの印 |
|---|---|
| **V-1-a** | **★見た先を1つ名指しし、★その記録が「時刻つきで要求を残す形式か」を先に書く**（★残さない形式なら、それ自体が結果） |
| **V-1-b** | **★窓の中の completion 要求の件数を書く**（★総件数を先に数える。★`head`/`-m` を使わない＝§3-8） |
| **V-1-c** | **★対照を1つ取る**: **★窓の前後（例: 19:20〜19:30）の件数も書く**（★「常に何か出ている」のか「その窓だけか」を区別するため） |

## V-2. ★`plan_source: "QWEN_BUILD_PLANNER"` が、LLM を呼ばずに入る経路が在るか（★コード構造）
```
★走査範囲: egl / ds / rri / dev-workcell / twoder の *.py ★全件（★打ち切り無し）
★探す文字列: QWEN_BUILD_PLANNER
```
| 受入 | ★1条件に1つの印 |
|---|---|
| **V-2-a** | **★その文字列を代入している箇所を全部 挙げる**（総件数・確認件数・打ち切り有無を併記＝§3-9） |
| **V-2-b** | **★各箇所について、★そこへ到達するまでに HTTP の LLM 呼び出しを必ず通るかを1行で書く**（★通らない経路が1つでも在れば「在る」と書く） |
| **V-2-c** | **★`dev-workcell/dw/plan_template.py` の `template_plan` が返す `plan_source` の値を実読で書く**（★別の値なら、テンプレート説は消える） |

## V-3.（★併せて。★印2 を左右しない）
**`prohibited_actions` の `"Modify existing repositories"` が、★テンプレート／定型文由来か、★その場で作られたか。**
**★同じ走査で分かるなら書く。★分からなければ「分からない」と書く。★追加で掘らない。**

---

# 2. ★やってはいけないこと
```
★`run_next` を叩かない（★GENERATE へ進めない）。★task を1件も進めない
★`/api/submit` を叩かない（★投入は設計と MGR の手続きを通る）
★production repo を1行も変えない。★commit しない
★台帳（*.jsonl の運用データ）を直読しない。★読めなかったら「読めなかった」が結果である
★:8005 に自分で問い合わせを投げて確かめない（★新しい呼び出しを作ると、★窓の記録が汚れる）
```

# 3. ★止まってよい場所（★2通りに読めたら止めて聞く）
```
★モデルサーバの記録が「時刻つきで残らない形式」だった → ★そこで止めてよい。「示せない」と書く
★`QWEN_BUILD_PLANNER` の代入箇所が2つ以上あって、★到達経路を1行で言えない → ★止めて設計へ聞く
```

# 4. ★報告の宛先と書き方
```
★宛: 設計/監査(CC-α)（★MGR ではない）。★TYPE=BUILT
★受入 V-1-a/b/c・V-2-a/b/c・V-3 に★1つずつ印を付ける（★まとめない）
★「測って0」と「測れない」を書き分ける
★:8005 を使ったら1行 書く（★本 SPEC では新しい呼び出しを禁じているので、★使わないはず）
```

---
**決めたこと**: **①対象は 19:32:13〜19:32:34 の PLAN 1件だけ ②V-1（モデルサーバ側の記録・対照つき）と V-2（`QWEN_BUILD_PLANNER` の代入箇所と到達経路）の両方を出す ③何も実装せず、task を進めず、:8005 へ新しい呼び出しを投げない。**
