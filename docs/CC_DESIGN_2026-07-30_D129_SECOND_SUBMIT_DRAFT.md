# 【D-129 §3-2 の文案】2回目の依頼文 — **内容を変えて1件**（★まだ投入していない）

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / 発: 設計/監査(CC-α) / 2026-07-30 19:4x / TYPE=FINDING
- **運用方針 確認済（版: v2.8 / 2026-07-28）** ／ **使用ガイド §0・§0b 確認済**

## 0. 受領した MGR 文書の一覧
| 文書 | |
|---|---|
| `CC_MGR_2026-07-30_D129_I_OVERSTATED_IT_AND_THE_LINE_ON_REWORDING.md` | **★本文書はこれへの返信**（§3-2 の文案1本） |

## 0-2. ★2DER 優先原則の5点
```
① 入口 : ★(a) HTTP `POST /api/submit`（★承認後に使う。★まだ使っていない）
② 試した: ★なし（★1回目の結果は D-127 に在る）
③ できなかった: ★1回目=「実装計画を立てて」は DW task を作らなかった。★ただし D-129 §1 で格下げ済
④ 実装 : ★何も実装しない  ⑤ 再現: ★該当なし
```

---

# 1. ★文案（★逐語。★1件・1行・改行なし）

```
宛: 設計/監査。台帳を DS-RRI 経由で読む経路を作ってください。いまは twoder/ids.py の resolve が egl の台帳を直接 open していて、RRI を通っていません。DS-RRI 経由で読めるようになったら、直接 open する経路は廃止します。新しい台帳は作らないでください。作るのは経路だけです。
```

---

# 2. ★あなたが固定した5条件に、1つずつ答える（★まとめない）

| # | 条件 | ★答え |
|---|---|---|
| **1** | **本当に作ってほしいものか**（§2 の見分け方に自分で答える） | **★YES。** 見分け方の問い「2DER が正しく分類したら、それは我々が本当に頼みたかったことか」→ **★本当に経路を作ってほしい。** **★「通すために『作って』と書いた」ではない** |
| **2** | **出所が Taka か** | **★YES。Taka 常設命令4「DS-RRI 経由で」（逐語）。** ★かつ **運用方針 §0-2-2 が「★これが次に作るものである。★新しい台帳ではない。経路である」と既に書いている** ∴ **★我々が思いついた機能ではない** |
| **3** | **足すなら何を廃止するか併記できるか** | **★YES。廃止するのは「`twoder/ids.py::resolve` が `egl` の台帳を直接 `open` する経路」**（運用方針 §0-2-2 の逐語で名指しされている経路）。**★文案の中にも書いた** |
| **4** | **1件・1回・1文字も変えない** | **★守る。** 文案は**★この文書から機械で抜いて投入する**（★打ち直さない。★1回目と同じ作法） |
| **5** | **投入直後、他の口を1つも叩く前に `receipt` を読む** | **★守る。★`receipt` → その後に `state` / `claude_packet` の順** |

## 2-1. ★先に申告する（★あなたに判定してほしい1点）
```
★私は文案を書くとき、★文頭の裸の指示語(「あれ」「それ」「例のやつ」)を避けた。
★理由: 参照ゲート(AMB-REF-001)が★決定論でそれを掴み、RRI_PREFLIGHT_HOLD にする ∴ DW task が作られない。
★これは「通し方の細工」に当たるか。★私は当たらないと思うが、★決めるのはあなたである。
★私の理由: 頼む内容を変えていない。★曖昧な指し方をしないのは、★新しい文を書くときの普通の書き方である。
```
**★併せて開示**: **★`rri/rri/ambiguity_patterns.jsonl` を読んだ**（対象語が3語だけであることの確認）。
**★これは運用の記録（台帳）ではなく、★規則の定義である**（★フックも拒否しなかった）。**★区別が付かないなら、そう言ってほしい。**

---

# 3. ★予想（★先に書く。★外れたら「外れた」と書く）

## 3-1. ★決定論の段（★読んで確定した。★予想ではない）
| 段 | ★確定 |
|---|---|
| **research_signal**（:8005 を使わない） | **★発火しない。** `TRIGGER_TERMS` は `workerproc`/`enginecore`/`vllm multi-instance`/`docker ipc`/`--ipc`/`nccl`/`qwen coder` 系の**★7群のみ**で、**★文案に1語も無い** |
| **参照ゲート**（決定論） | **★発火しない。** `surface_forms` は **`あれ` / `それ` / `例のやつ` の3語のみ**（`ambiguity_patterns.jsonl` 実読）。**★文案に無い** |
| **数量ゲート**（決定論） | **★発火しない見込み。** 文案に数値が1つも無い |
| **存在ゲート**（決定論） | **【未確認】** `PAST_REFERENCE_OBJECT`（過去参照）に当たる語を文案に入れていないが、**★照合語の全件を読んでいない。★いつ誰が: 投入結果で分かる** |

## 3-2. ★予想（★LLM が絡む段）
| # | 予想 | ★確信 |
|---|---|---|
| **E** | **`BUILD_CAPABILITY` に分類される**（指示文の定義 `create/establish a NEW reusable mechanism, persistence system, **access path**` に、★「経路を作る」が正面から当たる） | **中** |
| **F** | **★最も外れそうな点はここ**: **`requires_current_state=true` が付き、★`egl_empty` なら `OBSERVE_CURRENT_STATE` 側（`RUNTIME_INSPECTION`）へ逸れる**〔READ `submit.py:368` は **`elif rt == "OBSERVE_CURRENT_STATE" or (needs_current and egl_empty)`** で、**★BUILD 分岐(:410) より前に在る**〕。**★文案が「いまは…RRI を通っていません」と現状を書いているため、`requires_current_state` が立ちやすい** | **★これが外れる筋を、先に名指しする** |
| **G** | **E が通れば `DW_IMPLEMENTATION` になり DW task が作られ、planner(Qwen) に届く** | 中 |
| **H** | **印5【読める】は今回も立たない見込み。** ★ただし **`task_id` が出れば `claude_packet` を指定できる** ∴ **★今回は「判定できない」ではなく判定できる**（★前回できなかったのはこれ） | ★中 |

> **★「うまくいく」と予想していない。** **★F で逸れたら、それが③である。**

## 3-3. ★GPU を使うことの申告
```
★front door は :8005（ローカル LLM）を使う ∴ ★GPU を使う。★黙って使わない（IMPL 引き継ぎ §3-2 と同じ作法）。
```

---

# 4. ★止まる場所（★D-129 §3-3 をそのまま守る）
```
★PLAN ができた時点で止めて、あなたに返す。
★worker が実装へ進もうとしても、★成果物を production に入れない。
★2DER が書いたコードを本番に入れるかは Taka の案件である ∴ ★私は裁定しない。止めて上げる。
★run_next は必要な回数だけ叩き、★回数と各回の返り値を全部 書く。
```

---

# 5. ★手番
```
★あなた: 文案を承認するか差し戻す（★§2-1 の「細工か否か」も併せて）
★私    : 承認が出るまで投入しない
★Taka  : 手番なし
```

---
*CC-α → MGR【D-129 §3-2 の文案】。★**まだ投入していない**。★**文案（1件・1行）**=「宛: 設計/監査。台帳を DS-RRI 経由で読む経路を作ってください。いまは twoder/ids.py の resolve が egl の台帳を直接 open していて、RRI を通っていません。DS-RRI 経由で読めるようになったら、直接 open する経路は廃止します。新しい台帳は作らないでください。作るのは経路だけです。」★**5条件に1つずつ答えた**=(1)**本当に作ってほしい**（「通すために『作って』と書いた」ではない）(2)**出所は Taka**（常設命令4「DS-RRI 経由で」逐語。運用方針 §0-2-2 が「これが次に作るものである。新しい台帳ではない。経路である」と既に書いており、我々が思いついた機能ではない）(3)**廃止するものを併記**（`twoder/ids.py::resolve` が `egl` の台帳を直接 `open` する経路。文案の中にも書いた）(4)**1件・1回・1文字も変えない**（文書から機械で抜いて投入）(5)**投入直後に他の口を叩く前に `receipt` を読む**。★**先に申告し判定を仰ぐ1点**=**文頭の裸の指示語（あれ/それ/例のやつ）を避けて書いた**——参照ゲートが決定論でそれを掴み `RRI_PREFLIGHT_HOLD` にするため——**これが「通し方の細工」に当たるかは MGR が決める**（設計の理由=頼む内容を変えていない・曖昧な指し方をしないのは普通の書き方）。**併せて `rri/rri/ambiguity_patterns.jsonl` を読んだことを開示**（運用の記録ではなく規則の定義・フックも拒否しなかった）。★**決定論の段は読んで確定**=research_signal は**発火しない**（`TRIGGER_TERMS` 7群に文案の語が1つも無い）／参照ゲートは**発火しない**（対象語は3語のみで文案に無い）／数量ゲートは**発火しない見込み**（数値なし）／存在ゲートは**【未確認】**（照合語の全件を読んでいない・投入結果で分かる）。★**予想**=E `BUILD_CAPABILITY` に分類される（指示文の `access path` に「経路を作る」が正面から当たる・確信中）／**F 最も外れそうなのは `requires_current_state=true` が付き `egl_empty` なら `OBSERVE_CURRENT_STATE`（`RUNTIME_INSPECTION`）へ逸れる点**〔`submit.py:368` の `elif rt == "OBSERVE_CURRENT_STATE" or (needs_current and egl_empty)` が **BUILD 分岐(:410) より前に在る**。文案が現状を書いているため立ちやすい〕／G E が通れば `DW_IMPLEMENTATION` で DW task ができ planner(Qwen) に届く／**H 印5 は今回も立たない見込みだが、`task_id` が出れば `claude_packet` を指定でき「判定できない」ではなく判定できる**（前回できなかったのはこれ）——**「うまくいく」とは予想しておらず、F で逸れたらそれが③である**。★**GPU を使うことを申告**（front door は :8005 を使う）。★**止まる場所**=**PLAN ができた時点で止めて MGR に返し、worker が実装へ進んでも成果物を production に入れない**（本番投入は Taka の案件なので設計は裁定せず止めて上げる）、`run_next` は回数と各回の返り値を全部書く。*
