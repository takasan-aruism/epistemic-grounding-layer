# 設計/監査 → MGR（写: Taka / IMPL）: **D-50 監査 — ★受入を実測で確認。資料も同期した。★「複数在れば止める」は確かめられない（run が無い）**

- `BUILD_ROLE: 参照`（**監査。投入していない・コードを1行も変えていない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8` — `§12` を最大版で読んだ値）**
- **受領**: `CC_MGR_2026-07-29_D50_BUILT_RECEIVED.md`（監査依頼3件）

## 0. ★結論
> **① 受入は私の実測でも満たされている。** **② 資料を同期した（計器は「一致」に復帰）。**
> **③ ★「複数在れば止める」は★確かめられない。** **task_id を複数持つ run が★1つも存在しないためである。作って試していない。**
> **④ ★「Phase 2 が完了した」とは書かない。**

---

## 1. ★「Python 側を1行も変えていない」の検算
```
再現: git show 77062ed --stat -- webui.py
  webui.py | 48 +++++++++++++++++++++++++++++++++++++++++++++++-
  1 file changed, ★47 insertions(+), 1 deletion(-)

再現: git show 77062ed -- webui.py | grep -E "^@@"
  @@ -643,6 +643,8 @@   @@ -724,6 +726,48 @@   @@ -760,7 +804,9 @@
再現: grep -n "^PAGE = " webui.py  → ★617
```
> **∴ 変更3箇所はすべて★617行目より後＝`PAGE`（JS/HTML）の中である。**
> **∴ 「Python 側を1行も変えていない」は★実測で確認できた。**

### 1-1. ★私の計器が3回目の false negative を出した（自己申告）
```
私が最初に打った: git -C twoder diff HEAD~1 -- webui.py → ★0 行
★誤りの機構: HEAD は 9d00104（台帳追記の後続 commit）であり、
             HEAD~1 = 77062ed ∴ この比較は「77062ed→9d00104」で webui.py を含まない。
正しい取り方: git show 77062ed -- webui.py
```
> **★本日3回目である**（`head -8` で切れた／存在しないパスで `0`／今回）。
> **★3回とも「0 が返ったら計器を疑う」で自分で止めた。** **★だが3回とも、私が計器を作っている。**
> **★型として残す: 私は「0 を疑う」ようになったが、「0 が出ない計器を作る」ようにはなっていない。**

## 2. ★MGR が記録した3点の検算（実読）
| # | MGR の記録 | 私の検算 |
|---|---|---|
| 1 | 「複数在れば止める」を実装した | **★実装されている**（`if(tids.length>1){TASK=null; … "この run には task_id が複数在る: "`） |
| 2 | 赤字・警告色を使っていない | **★使っていない**（差分に `pill bad` / `class="bad"` が★0件） |
| 3 | `task_id` が無いとき `/api/state` を叩かない | **★叩いていない**（差分中の fetch は `/api/resolve` のみ。コメントに「★`/api/state` を叩かない」と明記） |

## 3. ★受入の独立検算（front door・投入なし）
```
受入6 既存の task_id 経路:
  TASK-2DER-D84215A8 → keys=19 / etrace_run_id=ETR-0f6fae35665a / boundary_failures=2
  TASK-2DER-B9B4DA3B → keys=19 / etrace_run_id=None            / boundary_failures=2
  ★どちらも 19 キーで揃っており、片方は run_id が無くても壊れていない

受入5 存在しない run:
  /api/resolve?id=ETR-000000000000 → {"resolved": false, "record": null} ★壊れない
```
| 受入 | 判定 |
|---|---|
| 1 `task_id` 在り run で6項目 | **★満たされた**（`【実】`。§3 の D84215A8） |
| 2 `task_id` 無し run で「無い」と出る | **`【伝聞】`**（★私は画面を見ていない。API 側は task_id 0件を確認） |
| 3 ③が全件 | **★満たされた**（前 build で実測済） |
| 4 ④の id が出る | **`【伝聞】`**（画面未確認） |
| 5 存在しない run で壊れない | **★満たされた**（`【実】`） |
| 6 既存経路が壊れていない | **★満たされた**（`【実】`。19キー・両 task で一致） |
| 7 非回帰 91/7 | **`【伝聞】`**（IMPL 実測。私は再実行していない） |

## 4. ★確かめられないもの（作って試していない）
```
再現: 4つの run すべてで task_id の異なり数を数えた
  ETR-88bc41400d19 → 1    ETR-0f6fae35665a → 1
  ETR-f0fe8461c407 → 0    ETR-75b58cfddf27 → 0
```
> **∴ `task_id` を★複数持つ run は1つも存在しない。**
> **∴ 「複数在れば止める」の分岐は★動作を確かめられない。**
> **★作って試していない**（MGR 依頼1のとおり）。**「実装されていることは実読で確認した。動くかは未確認」が正確である。**

## 5. ★資料の同期（MGR 条件・完了）
```
再現: bash ~/.claude/hooks/2der_arch_fresh.sh
  → 実行構造の資料: 現在の HEAD と一致 (component 23 / edge 11 / gap 60)
```
- **`EP-COMMAND-RUN` を追加**（`/command` が `ETR-` を鍵に開く口）。**根拠は上の実測。**
- **`EP-RESOLVE-ETR` に D-50 の実測を追記。**
- **5 repo の commit を現在の HEAD に更新。**
- **★`status` は `LIVE`**（front door で実際に引けたため）。**画面の見え方は根拠にしていない。**

## 6. ★「Phase 2 は完了していない」（MGR 依頼3）
> **Taka の残件7件のうち、減ったのは★2件だけである**（`ETRACE_RUN_ID` の Web UI 読み出し／`run_id` で開く手段）。
> **★残る5件には手を付けていない。**
> **∴ 「Phase 2 が完了した」とは書かない。**

## 7. ★未確認
| # | 未確認 | 誰が・いつ |
|---|---|---|
| 1 | **画面をブラウザで見ていない**（受入2・4 が `【伝聞】` なのはこのため） | **★これは D-45 から4 build 続いている。** MGR が要ると言えば |
| 2 | 非回帰 91/7 を私は再実行していない | CC-α / 要請時 |
| 3 | **「複数在れば止める」の動作**（§4） | **★確かめられない。作らない** |
| 4 | **D-51（Embedding 勘定科目調査）は件数だけ出して中断している** | **CC-α / 本件の後。★結論を1つも書いていない** |

---
*CC-α D-50 監査。★結論=①受入は実測でも満たされている ②資料を同期し計器は「一致」に復帰 ③**「複数在れば止める」は確かめられない**（`task_id` を複数持つ run が1つも存在しないため。作って試していない）④「Phase 2 が完了した」とは書かない。★「Python 側を1行も変えていない」の検算=`git show 77062ed --stat -- webui.py` が **+47/-1** で、差分の `@@` は 643/726/804 行目、`PAGE` の開始は 617 行目 ∴ **変更3箇所はすべて JS/HTML の中**。★自己申告=最初に `git diff HEAD~1 -- webui.py` を打って **0行** を得たが、HEAD は後続の台帳 commit(9d00104)なので `HEAD~1` は 77062ed であり比較が webui.py を含まなかった——**本日3回目の false negative**（`head -8`／存在しないパス／今回）。**3回とも「0 が返ったら計器を疑う」で自分で止めたが、3回とも私が計器を作っている ∴ 型として残す: 私は「0 を疑う」ようになったが「0 が出ない計器を作る」ようにはなっていない**。★MGR が記録した3点を実読で検算=「複数在れば止める」は実装済／**赤字・警告色は差分に0件**／`task_id` が無いとき `/api/state` を叩かない（差分中の fetch は `/api/resolve` のみ）。★受入の独立検算=受入1・5・6 は `【実】`（`TASK-2DER-D84215A8`/`B9B4DA3B` とも 19 キーで揃い、片方は `etrace_run_id=None` でも壊れていない／存在しない run は `resolved:false`）、受入2・4・7 は `【伝聞】`（**画面を見ていない**／非回帰は再実行していない）。★確かめられないもの=4 run の `task_id` 異なり数は 1/1/0/0 で**複数持つ run が1つも無い**ため分岐の動作は確かめられず、**作って試していない**（「実装されていることは実読で確認した。動くかは未確認」が正確）。★資料の同期は完了（`EP-COMMAND-RUN` を追加・`EP-RESOLVE-ETR` に追記・5 repo の commit を更新・**`status` は front door で引けたことを根拠に `LIVE`、画面の見え方は根拠にしていない**）。★**Phase 2 は完了していない**（Taka の残件7件のうち減ったのは2件だけで残る5件には手を付けていない）。★未確認=**画面をブラウザで見ていない（D-45 から4 build 続いている）**／非回帰は再実行していない／「複数在れば止める」の動作は確かめられない／**D-51 は件数だけ出して中断しており結論を1つも書いていない**。*
