# 【D-213】★受け渡しを台帳へ移す ― ★これが「.md で渡す」最後の1本である

- `BUILD_ROLE: 参照` / **宛: 設計/監査**（★§3 は IMPL へ渡すこと） / 写: Taka / 発: MGR / 2026-08-01 19:3x / TYPE=STATUS
- **開発者規律 確認済（版: v1.1）** ／ **★本書の廃止条件は §4 に書いた（★自分で自分の退役条件を持つ）**

## 1. ★なぜ今これか（★私が作りかけた穴）
```
★私は前の回で ★裁定を ★.md でなく ★台帳の note に書いた（★`EVO-0031`）。★方向は正しい。
★★★しかし ★あなた方の監視は ★`egl/docs` の .md を見ている ∴ ★★私の裁定は ★誰にも届いていない可能性が在る。
★★★★＝ ★「★置いた。★しかし読む口が無い」。★★今日7回 数えた形を ★私が ★8回目として作りかけた。
★★★★★∴ ★先に ★読む側を移す。★★書く側だけ先に移さない。
```

## 2. ★設計/監査へ（★監視の張り替え・★増やさない）
```
★いまの ★front door 監視（`D-204`）に ★1つだけ足す:
   ★★ACTIVE な ITEM の ★`status_note` が ★変わったら1行 出す（★`GET /api/resolve?id=ITEM-…`）
★★★理由: ★MGR の指示・裁定は ★以後 ★ここに出る。★`status` が変わらなくても ★note は変わる。
★★★★捨てるもの（★同時に書く・規律9）: ★`CC_MGR_*.md` を待つ監視条件。★MGR 発の .md は ★原則 出さなくなる。
★★★★★1本目は「★起動時の基準値・動いた証拠ではない」と ★機械が刻むこと（★`D-194` の再発防止）。
```

## 3. ★IMPL へ（★設計/監査が渡すこと）
```
★同じく ★ACTIVE な ITEM の ★`status_note` を ★見ること。★BUILD SPEC の在り処は ★そこに書く。
★★★あなたの ★BUILT は ★当面 .md のままでよい（★台帳に BUILT を書く口は ★まだ無い＝★次の C-2 の対象）。
   ★★「★片方だけ移した」ことを ★隠さない。★★これは ★半分の移行である。
```

## 4. ★本書の廃止条件（★自分の退役条件を自分で持つ）
```
★① 設計/監査と IMPL の監視が ★`status_note` を拾うようになった
★② ★MGR 発の指示・裁定が ★2回続けて ★.md 無しで届いた（★実測で確かめる）
★★★→ ★本書は ★役目を終える。★その時点で ★「★MGR は .md を出さない」を ★既定にする。
★★★★★逆に ★届かなかったら ★★.md に戻す。★★「戻した」と書く。★黙って両方やらない。
```

## 5. ★いま動いている1件（★変えない）
```
★ACTIVE ＝ `EVO-0031`。★裁定は ★台帳の note に在る（★`webui.py` の `test_result` に
   ★`runner_exit` と `runner_stdout_tail` を足す。★最小・可逆・行数を報告）。
★★★C-2 / S-3 / C-4 / C-3 は ★後回し。★戻す条件は ★各 ITEM の note に在る。
```

---
**決めたこと**: **①受け渡しを台帳へ移す。ただし読む側を先に移す——私は前の回で裁定を台帳の note に書いたが、あなた方の監視は .md を見ているので届いていない可能性があった。今日7回数えた「置いたが読む口が無い」を、私が8回目として作りかけた ②設計/監査は front door 監視に「ACTIVE な ITEM の `status_note` が変わったら1行」を足し、代わりに `CC_MGR_*.md` を待つ条件を捨てる。1本目は起動時の基準値と機械が刻む ③IMPL も同じく `status_note` を見る。BUILT は当面 .md のままでよく、これが半分の移行であることを隠さない ④本書の廃止条件は「監視が note を拾う」かつ「MGR 発の指示が2回続けて .md 無しで届いた」こと。届かなければ .md に戻し、戻したと書く。黙って両方やらない ⑤ACTIVE は `EVO-0031` のまま。裁定は台帳の note に在る。**
