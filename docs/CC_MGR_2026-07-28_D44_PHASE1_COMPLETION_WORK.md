# CC 管理(MGR) → 設計/監査(CC-α): **★D-44 — Phase 1 完了条件5件の SPEC を書いてください。★過去の走査を1つも流用しない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.7 — `§12` を最大版で読んだ値）**
- **正典**: `EVENT_TRACE_PHASE1_COMPLETION_CRITERIA_v1_0.md`（Taka 逐語）

## 1. ★Taka の5件が、これまでの依頼を置き換える
**私が出していた順序（`ETRACE_RUN_ID` → 合流点⑤ → 35件 → 進行経路）は、★Taka の5件の中に吸収される。**
**∴ 私の順序を優先しない。** **★Taka の①〜⑤を満たすことが Phase 1 である。**

| Taka | 我々の作業との対応 |
|---|---|
| ① `acquisition` を合流点へ | **合流点⑤（`core.append_event`）として実装**（正典 §2-1 の解釈。**★MGR が Taka に確認事項として明示済**） |
| ② 同一 CLI 依頼で DS→RRI→EGL を同一 `trace_id` で追跡 | **★これが本 build の合否である。** 今日は DS→RRI までしか出ていない |
| ③ 打ち切り出力を完全性の証拠に使わない | 監査の作法 |
| ④ 総件数・確認件数・打ち切り有無を必須記録 | 監査の作法（v2.7 §3-9） |
| ⑤ 12入口と全書き込み経路を**打ち切りなしで再走査** | **★過去の走査を1つも流用しない** |

## 2. ★⑤について（最も手間がかかる。先に条件を出す）
```
★過去の走査結果を1つも流用しない。
  本日の「12入口」も「65行の全分類」も「57個中35個」も、★全部やり直す。
理由: どれも head を付けていないとは限らず、我々はそれを確かめていない。
```
| # | 条件 |
|---|---|
| **1** | **★総件数を先に出す。** 「◯件出た」を書いてから中身を見る |
| **2** | **★確認件数を書く。** **総件数と一致しないなら、なぜ一致しないかを書く** |
| **3** | **★打ち切りを一度も使っていないことを、コマンドごと貼って示す** |
| **4** | **★件数が前回と違ったら、違ったと書く。** **前回に合わせない** |
| **5** | **★時間がかかることを理由に範囲を狭めない。** 狭めるなら「狭めた」と書き、私の裁定を仰ぐ |

## 3. ★②について（本 build の合否）
**今日の CLI 投入は `SUBMIT → DS → RRI` の3件で終わった。** **EGL には書いたが記録されなかった。**
> **∴ ①を実装すれば、同じ形の依頼で EGL の event が出るはずである。**
> **★「はず」を書かない。** **投入して確かめる。**

**★投入を1回だけ許可する**（②の実測のため）。**条件は前回と同じ:**
- **1回だけ。文面は事前に文書化する。** **★前回と同じ文面でよい**（`OBSERVE_CURRENT_STATE` に入り、EGL の取得系を通ることが分かっているため）
- **満たせなければ「満たせない」と書く。** **2回目を投入しない。**

## 4. ★禁止
1. **★「Phase 1 完了」と書かない。** **5件すべてが実測で満たされるまで。**
2. **★過去の走査を根拠に「変わっていないはず」と書かない**（⑤の趣旨に反する）。
3. **新しい RRI 判断ロジックを作らない。** **35件を全部出さない**（前回の裁定は生きている）。
4. **★`twoder` 自身の台帳9つに手を出さない**（`G-47`・据え置き）。
5. **★進行経路の run（裁定 B）は本 build に含めない。** **①②が先である。**

## 5. ★私（MGR）が Taka に確認していること（あなたは待たない）
```
①は「acquisition.py 自身に置く」のか「唯一の書き手 core.append_event に置く」のか。
私は後者を採ると明示して報告済。訂正が来れば私が伝える。
```
**★あなたは (A)（唯一の書き手）で進めてよい。** **訂正が来たら私が止める。**

---
*MGR。D-44。★Taka の5件がこれまでの依頼を置き換える（私が出していた順序は5件の中に吸収されるので優先しない）。対応=①`acquisition` を合流点へ＝合流点⑤（`core.append_event`）として実装（MGR が Taka に確認事項として明示済）／②同一 CLI 依頼で DS→RRI→EGL を同一 `trace_id` で追跡＝**本 build の合否**（今日は DS→RRI までしか出ていない）／③④監査の作法（v2.7 §3-9）／⑤12入口と全書き込み経路を打ち切りなしで再走査＝**過去の走査を1つも流用しない**。★⑤の条件=本日の「12入口」「65行の全分類」「57個中35個」も全部やり直す（どれも head を付けていないとは限らず、我々はそれを確かめていない）／総件数を先に出す／確認件数を書き総件数と一致しないなら理由を書く／打ち切りを一度も使っていないことをコマンドごと貼る／**件数が前回と違ったら違ったと書き、前回に合わせない**／時間を理由に範囲を狭めない（狭めるなら「狭めた」と書き MGR の裁定を仰ぐ）。★②について=今日の CLI 投入は3件で終わり EGL には書いたが記録されなかった ∴ ①を実装すれば EGL の event が出るはずだが**「はず」を書かず投入して確かめる**。②の実測のため**投入を1回だけ許可**（1回だけ・文面は事前に文書化・前回と同じ文面でよい・満たせなければ「満たせない」と書き2回目を投入しない）。★禁止=「Phase 1 完了」と書かない／過去の走査を根拠に「変わっていないはず」と書かない／新しい RRI 判断ロジックを作らず35件を全部出さない／`twoder` 自身の台帳9つ（`G-47`）に手を出さない／進行経路の run（裁定 B）は本 build に含めない。★MGR が Taka に確認中の1点（①は `acquisition.py` 自身か唯一の書き手か）について、CC-α は (A)（唯一の書き手）で進めてよく、訂正が来たら MGR が止める。*
