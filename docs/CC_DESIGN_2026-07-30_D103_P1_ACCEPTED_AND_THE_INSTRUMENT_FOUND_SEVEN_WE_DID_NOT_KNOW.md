# 設計/監査 → MGR（写: Taka / IMPL）: **D-103 — P-1 受入。★口が、我々の知らない受信7件を見つけた（IMPL 60件 → 私 67件）／★私は「0が返る壊れた計器」で1回 止まった**

- `BUILD_ROLE: 参照`（**監査のみ。★実装していない・★投入していない・★log を1行も読んでいない・★台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_IMPL_2026-07-30_D102_P1_ASK_2DER_IF_IT_ARRIVED_BUILT.md`

## 0. ★2DER 優先原則の5点
| # | |
|---|---|
| ① 入口 | **★`GET /api/receipt`**（★本 build で作られた口） |
| ② 何を試したか | **★「届いたか」を 2DER に聞いた**（★log を読まずに） |
| ③ **できなかったこと** | **★誰が送ったかは聞けない**（★記録に id が無い）／**★返った値が正しいかは確かめられない**（★log を読めない） |
| ④ 何を実装するか | **★実装しない**（受入確認のみ） |
| ⑤ | **★該当しない** |

## 0-1. ★結論
> **① 受入13件すべて満たされている。★私が自分で測った。**
> **② ★口が働いた。★私は log を1行も読まずに「直近の受信は 2026-07-30T12:53:07／これまでに67件」と言えた。**
> **③ ★その67件は、★IMPL が測った60件より★7件 多い。★どちらも投入していない。★我々の知らない受信が在る。**

---

## 1. ★受入（★13件＋10-b・★1つずつ）
| # | 条件 | 判定 | 私の確かめ方 |
|---|---|---|---|
| **1** | 200 が返る | **★済** | `GET /api/receipt` → HTTP 200 |
| **2** | 欄が5つだけ | **★済** | 返った欄＝`last_recv_at`/`recv_count`/`last_sent_at`/`last_sent_status`/`readable`。**★仕様外の欄 0件** |
| **3** | 依頼文・task_id・状態を返していない | **★済** | 返り全体を見て該当なし |
| **4** | **★log の行そのものを返していない** | **★済** | **★返り値に `RECV`/`SENT`/`/api/` を含む文字列が0件** |
| **5** | `recv_count` と `readable` が別の欄 | **★済** | 別の欄。**★IMPL は仕様より1段 強くした**（§3） |
| **6** | 読めないときも 500 にならない | **★IMPL の申告**（★関数では確かめた／端点では未確認）。**★私も確かめていない**（★log を壊す必要がある） |
| **7** | 「0件」と「引けなかった」が別の文 | **★済** | 追加分に**3通りの文**が別々に在る（在るとき／`recv_count===0`／`!readable`） |
| **8** | 「1件ごとには分かりません」が出る | **★済** | 配信物に2件（本文＋注記） |
| **9** | 既存4口の返りを変えていない | **★IMPL の実測を受ける**（★旧新を同じ瞬間に走らせて `build_state` 5105==5105） |
| **10** | **MGR が log を読まずに「直近の受信がいつか」「何件」を言えるか** | **★済。★私も言えた**（§2） |
| **10-b** | 「あの送信が届いたか」は答えられない、と BUILT に書く | **★済**（BUILT に明記されている） |
| **11** | 配信物の JS が `node --check` | **★済。★私が実行**（19854バイト → 構文 OK） |
| **12** | 非回帰 91/7 | **★IMPL の実測**（★私は本 build では再実行していない・D-100 から変わっていない範囲） |
| **13** | 色を足していない | **★済**（§4-1 に私の計器の話を書く） |

## 2. ★口が働いた。★そして我々の知らない受信を見つけた
```
★IMPL の実測（BUILT 記載）: last_recv_at = 2026-07-30T08:47:43 ／ recv_count = 60
★私の実測（12:57・2回）    : last_recv_at = 2026-07-30T12:53:07 ／ recv_count = ★67
★2回とも同じ値が返った（安定している）
```
| | |
|---|---|
| **★差** | **★7件。★最後の受信は 12:53:07** |
| **★IMPL は投入していない** | **BUILT に「`/api/submit` を1回も押していません」と明記** |
| **★私も投入していない** | **★本日 `POST /api/submit` を1回も出していない**（★私が出したのは `GET` だけ） |
| **★∴ 誰が送ったか** | **★分からない。★私は書かない**（★記録に id が無い・§0-1③） |

> **★これは本 build の一番の収穫である。** **★口が、我々が知らなかったことを1つ出した。**
> **★今朝まで、我々はこれを知る手段を持っていなかった**（★log を読む以外に）。
> **★ただし「Taka が押した」とは書かない。** **★口はそれを答えられない。** **★推測を事実にしない。**

## 3. ★IMPL が仕様より1段 強くした点（★黙って通さない）
```
実装源 §3-1: recv_count : RECV の件数
IMPL       : ★読めなかったときは 0 ではなく ★null を返す
```
**【設計:CC-α】★受け入れる。★理由:**
> **★`0` を返すと、「受けていない」と「引けなかった」が★同じ欄で同じ形になる**（`readable` を見落とせば区別できない）。
> **★IMPL の判断は、私の仕様より★正確である。** **★欄は5つのままで増えていない。**
> **★ただし「仕様に無い」ことは書く。** **★私の仕様が、そこまで書けていなかった。**

## 4. ★私の計器（★2件・自己申告）
### 4-1. ★「正常」「OK」をページ全体で数えた（★1回目の誤り）
```
ページ全体: 「正常」1件 ／「OK」★10件   → ★受入13 に反するように見えた
★差分だけ : 「正常」1件 ／「OK」2件
★中身     : ①実装源の禁止文をコメントに引用した行 ②`_rc.kind!=="OK"` という★内部の判定
∴ ★どちらも画面に出る語ではない。★受入13 は満たされている。
```
> **★D-58 で学んだ形をまた踏みかけた**——**★ページ全体で数えず、差分で数える。**
> **★今日 これで3回 助かっている。**

### 4-2. ★「0 が返る壊れた計器」で1回 止まった（★2回目）
```
私が打った: git diff … | grep -E "^\+" | grep -v "^\+\+\+"
返り        : ★追加行 0 / 禁止語も全部 0
★原因      : ★正規表現が壊れていた（"^+++" が無効な構文）→ ★何も通らなかった
```
> **★「全部0」を「きれい」と読まずに、★計器を疑って打ち直した**（★正しくは 追加55行）。
> **★もし疑わなければ「色は0件・追加も0件」と★受入を全部 通していた。**
> **★D-72 の「0 が返ったら計器を疑う」が、★今日また効いた。**

## 5. ★判定
**【設計:CC-α】★D-102 を受け入れる。** **★MGR の commit を推す。**

## 6. ★言わないこと（★先に書く）
| ★書かない | 理由 |
|---|---|
| **「届いたかが分かるようになった」** | **★1件ごとには分からない**（MGR §3 の指示） |
| **「7件は Taka が押した」** | **★口はそれを答えられない。★推測である** |
| **「正しい値が返った」** | **★我々は log を読めない。★確かめられるのは「答えが返ること」「2つの意味を持たないこと」まで** |
| **「体制再編が成立した」** | **★旧作業（log を直接読む）が停止したかは、★本 build では測っていない。★次の測定で見る** |

## 7. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **7件が誰の送信か** | **★分からない。★口は答えられない** |
| 2 | **返った値が正しいか** | **★log を読めない ∴ 原理的に確かめられない** |
| 3 | **読めないときに 500 にならないか** | **★log を壊す必要が在る ∴ やらない** |
| 4 | **非回帰を本 build で再実行したか** | **★していない**（★IMPL の実測を受けた） |
| 5 | **画面のその行を目で見たか** | **★見ていない。★Taka にしか出せない** |

---
*CC-α D-103（監査のみ・log を1行も読んでいない）。★結論=①**受入13件すべて満たされ CC-α が自分で測った** ②**口が働き、CC-α は log を1行も読まずに「直近の受信は 2026-07-30T12:53:07／これまでに67件」と言えた** ③**その67件は IMPL が測った60件より7件 多く、どちらも投入していないので我々の知らない受信が在る**。★受入=200／**欄が5つだけで仕様外0件**／依頼文・task_id・状態を返していない／**返り値に `RECV`/`SENT`/`/api/` を含む文字列が0件＝行そのものを返していない**／`recv_count` と `readable` が別の欄／**受入6 は IMPL の申告（関数では確かめた／端点では未確認）で CC-α も確かめていない（log を壊す必要がある）**／**3通りの文が別々に在る**／「1件ごとには分かりません」が配信物に2件／既存4口は IMPL の実測（旧新を同じ瞬間に走らせて 5105==5105）／**受入10 は CC-α も言えた**／10-b は BUILT に明記／**`node --check` は CC-α が実行（19854バイト → OK）**／非回帰は IMPL の実測／色は差分で確認。★**口が働き、我々の知らない受信7件を見つけた**——**IMPL 実測 08:47:43/60件 に対し CC-α 実測 12:53:07/67件（2回とも同じ値で安定）**、**IMPL は「`/api/submit` を1回も押していません」と明記し CC-α も `GET` しか出していない** ∴ **誰が送ったかは分からず CC-α は書かない**——**これは本 build の一番の収穫で、今朝まで我々はこれを知る手段を持っていなかった（log を読む以外に）。ただし「Taka が押した」とは書かない。口はそれを答えられない。推測を事実にしない**。★**IMPL が仕様より1段 強くした点を黙って通さない**=実装源は `recv_count : RECV の件数` だったが**IMPL は読めなかったとき 0 ではなく null を返す**——**`0` だと「受けていない」と「引けなかった」が同じ欄で同じ形になる**ので**IMPL の判断は CC-α の仕様より正確**であり**欄は5つのまま**だが、**「仕様に無い」ことは書く（CC-α の仕様がそこまで書けていなかった）**。★**CC-α の計器2件（自己申告）**=①**「正常」「OK」をページ全体で数えて 10件 と出し受入13 に反するように見えたが、差分だけで数えると2件で中身は「実装源の禁止文をコメントに引用した行」と「`_rc.kind!=="OK"` という内部の判定」であり画面に出る語ではなかった**（**D-58 で学んだ形をまた踏みかけ、今日これで3回 助かっている**）②**`grep -v "^\+\+\+"` の正規表現が壊れて「追加行0・禁止語も全部0」が返ったが、「全部0」を「きれい」と読まずに計器を疑って打ち直した**（正しくは追加55行。**疑わなければ受入を全部 通していた**——**D-72 の「0 が返ったら計器を疑う」がまた効いた**）。★**D-102 を受け入れ commit を推す**。★**言わないこと**=「届いたかが分かるようになった」（1件ごとには分からない）／「7件は Taka が押した」（推測）／「正しい値が返った」（log を読めない）／**「体制再編が成立した」（旧作業が停止したかは本 build では測っておらず次の測定で見る）**。★確かめていないこと=7件が誰の送信か／返った値が正しいか（原理的に確かめられない）／読めないときに 500 にならないか（log を壊す必要が在るのでやらない）／非回帰は本 build で再実行していない／**画面のその行を目で見ていない（Taka にしか出せない）**。*
