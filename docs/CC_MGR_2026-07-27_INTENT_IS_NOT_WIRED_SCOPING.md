# CC 管理(MGR): **意図調べは本番に繋がっていない** — Build 5 は続行、ただし主張の範囲を固定する（HANDOFF・短く）

- `BUILD_ROLE: 参照` / 宛: DESIGN(CC-α) / IMPL(coder) / 写: Taka / 発: MGR / 2026-07-27 05:15 / TYPE=HANDOFF
- 対象: `CC_IMPL_2026-07-27_BUILD5_PRECHECK_FACT_NOT_PASSED_STATUS.md`（LLM 不使用・grep と実 prompt による事実）

## 1. 受領した事実（未監査。**再現手順が全件付いているので検証は安い**）
1. **意図調べは本番コードから一度も呼ばれていない**（`twoder`/`rri`/`ds`/`dev-workcell` に参照 0 件）。**`egl/structure/` の研究スクリプトにしか存在しない。**
2. **決定論の判定（文脈の有無・存在の接地）は意図調べに一言も渡っていない。** `context` が空なら**記述が消えるだけ**で「空である」とは言っていない。
3. **`existence_grounding` は `preflight_gate` の内側で完結**しており、構造上意図調べに届かない。
4. `preflight_gate` は LIVE だが、**`context=` 引数は呼び出し側から渡されていない**（Build 1a で引数だけ足して未配線・既報）。

## 2. ★主張の範囲を固定する（今日いちばん効く訂正）
**今日われわれが動かそうとしていた精度は、本番の動作ではない。研究スクリプトの中の数字である。**
- **∴ 以後、意図調べの数字に「2DER が / システムが」と書かない。** **「研究スクリプト上の意図調べで」と書く。**
- **【設計:MGR】これは Taka の「いつになったら 2DER 主体に移行できるのやら」に直接効く事実である。** **精度の線と移行の線は別々ではなく、移行が済むまで精度の線は本番に影響しない。**

## 3. Build 5 は続行する（判断）
- **Taka が指示したテストであり、答えるのは「事実を渡せば選べるか」という設計の問い。** 研究スクリプト上でも答えられる。**続行。**
- **ただし結果の書き方を先に固定する**: **効いても「2DER が良くなった」と書かない。** **「事実を渡せば選べる、と研究スクリプト上で示された。本番に入れるかは別の作業」と書く。**

## 4. 順番の変更（MGR 判断）
**Build 5 の後、精度の線はいったん止める。次は配線と移行を本線にする。**
- 理由: **本番に繋がっていないものの精度を上げ続けても、Taka から見える動作は1ミリも変わらない。** 今日それを1日やった。
- **DESIGN へ**: D-6（1問い合わせ=複数明細）は**着手してよいが、「どう帳簿に登録されるか」の設計は、配線の設計と同じ場所で書くこと。** 別々に書くと、また繋がらないものが増える。
- **移行の線に、この事実を足す**: 直叩きの閉塞（書き5箇所）に加えて、**「front door → 意図調べ → 決定論の事実の受け渡し」が存在しない**ことを、棚卸しの結果に明記すること。

---
*MGR。事実受領: 意図調べは本番から一度も呼ばれておらず、決定論の判定も一言も渡っていない（存在の接地は preflight_gate の内側で完結）。★主張の範囲を固定=以後「2DER が」でなく「研究スクリプト上の意図調べで」と書く。Build 5 は Taka 指示のテストなので続行するが、効いても「2DER が良くなった」と書かない。★順番変更=Build 5 の後、精度の線を止めて配線と移行を本線にする（繋がっていないものの精度を上げても Taka から見える動作は変わらない）。*
