# BUILD SPEC 追補 — Build 17 v1.1: **再起動で消えた run-gate を、同一依頼文の1回投入で立て直す**

- **`BUILD_ROLE: ★実装源`**（**`CC_DESIGN_2026-07-28_BUILD17_SPEC_WALK_TO_REGENERATE.md` v1.0 への追補。本文書と v1.0 を併せて1本の実装源とする**）
- **宛: IMPL（coder）** / 写: MGR / Taka
- 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=BUILD_SPEC / v1.1
- **運用方針 確認済（版: v1.9）**
- 権限: `CC_MGR_2026-07-28_RESUBMIT_ONCE_AFTER_RESTART.md`

## 0. ★これは私の落ち度の修理である
**`_LAST` が webui プロセス内の変数であることは、私が Build 9C の監査で記録した。**
**それを Build 16 / 17 の SPEC の再起動手順に入れなかった。** **∴ IMPL は1段目から進めなかった。**
**★IMPL が迂回せず止めたのは正しい**（本日3回目の正しい停止）。

## 1. v1.0 の §1 の前に、次を1回だけ行う
```
① /api/submit に Build 13/14 と同一の依頼文を1回（機械抽出・1文字も変えない）
② 返った task_id が TASK-2DER-21F64D9D と同一であることを確認する
   → 異なれば ★止めて上げる（依頼文の sha1 で決まるという前提が崩れている）
③ 以降は v1.0 §1 のとおり（derive_state を確認 → run_next 1回 → …）
```
- **★これは2回目の投入である。** **BUILT に回数と理由を明記する**（例: 「再起動で run-gate が初期化されたため。task を新設しないので同一文を使う。通算2回目の投入」）。
- **依頼文を「通りやすくするために」変えない。** **1文字も変えない。**

## 2. 変わらないもの（v1.0 のまま）
1段ずつ／`run_until_barrier` を使わない／各段の前に `derive_state` を確認／`READY_FOR_REGENERATE` で止まる／`REGENERATE` を実行しない／新しい task を作らない／他の2 task に触らない／オラクル未開封／`/tmp` を消さない／本番無変更／commit しない。

## 3. 予想の追加（実測前に固定）
| 項目 | DESIGN の予想 |
|---|---|
| 再投入後の `task_id` | **`TASK-2DER-21F64D9D` と同一** |
| 再投入で状態が変わるか | **★変わらない**（`submit()` はループを進めない。`READY_FOR_AUDIT` のまま） |
| 再投入後の `run_next` | **通る**（gate が立つため） |

**v1.0 §3 の予想（findings 0件 → `READY_FOR_REGENERATE` 直行・1段）は変えない。**

## 4. BUILT の定型見出しに1行足す
```
## 投入回数
- 本 task への通算投入回数: ___ 回 / 今回の理由: ____
```

---
*BUILD SPEC v1.1 追補（★実装源・v1.0 と併せて1本）。再起動で run-gate（`_LAST`）が初期化され `refused: "no submit yet"` になったため、同一依頼文を1回だけ再投入して gate を立て直す。★私の落ち度の修理である——`_LAST` がプロセス内変数であることは Build 9C の監査で私が記録しながら、Build 16/17 の再起動手順に入れなかった。IMPL が迂回せず止めたのは正しい（本日3回目の正しい停止）。手順=同一文で submit 1回→返った task_id が `TASK-2DER-21F64D9D` と同一かを確認（異なれば止めて上げる。sha1 で決まる前提が崩れている）→以降は v1.0 どおり。2回目の投入であることと理由を BUILT に明記し、依頼文は1文字も変えない。予想の追加=task_id は同一／再投入で状態は変わらない（submit はループを進めない）／その後の run_next は通る。v1.0 の予想（findings 0件→READY_FOR_REGENERATE 直行・1段）は変えない。*
