# 【調査3 / 作業の一覧】`EVO-0040` — **★7行・優先順。★実装しない・接続しない**

- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-08-02 14:4x / TYPE=作業一覧
- **開発者規律 確認済（版: v1.3）** ／ 親: `ITEM-2DER-EVO-0035`
- **★この .md である理由**: 7行×7欄は `status_note` に入らない（**台帳へ格納する口が無い**＝親 `EVO-0035` の .md と同じ理由）。**台帳からは `ITEM-2DER-EVO-0040` の note が本書を指す。**
- **★入力**: 調査2 の**訂正後**の結論 —「**Q1 は 55本すべて言える**（在る33／無い=欠陥13／無い=設計どおり9）。**不足は B（到達性）だけ**、13本」
- **★走行 0・★task 増 0・★commit 0**（`s10.build()` を読み取り専用で呼んだ。`--apply` 不使用）

---

## ★先に言う：一覧を短くできた理由

```
★調査2 の訂正で ★不足が2種類 → ★1種類（到達性）に減った。
★★∴ 7行のうち ★1〜2行目だけが「調査2 の穴を塞ぐ」もので、
   ★3行目以降は ★親 EVO-0035 §1 の「できるようになること」6項目に対応する。
★★★行1 が通るまで 行3以降を始めない（★半移行を作らない・裁定#5）
```

---

## 1. ★実行の痕跡を読む口を1本 足す（**★最優先**）

| 欄 | 内容 |
|---|---|
| **目的** | 「その台帳に**実際に書かれたか**」を、静的解析でなく**実行の記録**から言えるようにする |
| **理由** | 到達性の13本は `readers`（**basename の部分一致**・`s10:181` 逐語）と `wired`（`REACHABILITY.jsonl` 依存）で決まる。**どちらも静的**。★★一方 `ds/data/event_trace.jsonl` は**投入のたびに書かれている**（`submit.py:99-101` が `ETRACE.open_run` を必ず通す）のに、**読み手が0本**（調査2 実測）＝**痕跡はあるのに誰も見ていない** |
| **具体的な方法** | `GET /api/etrace?run_id=…` を**1本**足す。中身は既存 `ds/ds/etrace.py` の `resolve_run` / `resolve_event`（`:160,:170`・**既に在る**）を呼ぶだけ。**新しい台帳を作らない・新しい記録を増やさない** |
| **2DER への影響** | 読み取り専用。既存の投入経路・状態遷移に**触れない**。`/api/state` の `etrace_run_id` が**初めて引ける値**になる（`webui.py:159` は既に出している） |
| **何がエラーになり / ならないか** | **なる**: `event_trace.jsonl` は `ds/.gitignore` 配下（`etrace.py:13` 逐語）∴ **消えていれば空**。`run_id` 不明なら 404 相当。**ならない**: 投入・生成・監査は一切通らない |
| **UI と 2DER 自身への影響範囲** | UI は**変えない**（口だけ）。2DER 自身は「自分が何を通ったか」を初めて外から言える |
| **★完了したら廃止できるもの** | **Claude が git log と .md を漁って「いつ・何が動いたか」を答える運用**（親 §1 の受入そのもの） |

## 2. 台帳一覧を front door から読めるようにする（`ITEM-2DER-EVO-0039` に既出）

| 欄 | 内容 |
|---|---|
| **目的** | 55本の一覧を 2DER に聞けるようにする |
| **理由** | 調査1 の未達1件（`webui.py`/`ids.py` に `LEDGER_REGISTRY` の参照 **0件**・実測） |
| **具体的な方法** | 既存 `LEDGER_REGISTRY.jsonl` を読む口を**1本**。**82本それぞれに API を作らない**（Taka §6③） |
| **2DER への影響** | 読み取り専用。**書かない** |
| **エラー** | **なる**: `--apply` を一度も走らせていなければ内容が古い（**再生成は別作業**）。**ならない**: 既存の投入経路 |
| **UI / 自身** | UI は任意（口だけでもよい）。自身への影響なし |
| **★廃止** | **Claude が記憶と .md から台帳を列挙する運用** |

## 3. 記録の時刻を実時刻にする（`ITEM-2DER-EVO-0036` に既出）

| 欄 | 内容 |
|---|---|
| **目的** | 「いつの記録か」を言えるようにする |
| **理由** | `webui.py:40` 逐語 `TS = "2026-07-11T09:00:00"`（固定）。**画面の「いつ時点」も この値**（本日 実測）。既登記 `G-84` と同型 |
| **具体的な方法** | `TS` を**呼ぶ側で**実時刻にする。**★決定論の試験が `TS` に依存していないかを先に測る**（依存していれば試験が壊れる） |
| **2DER への影響** | 記録の `ts` が実時刻になる。**過去の記録は変えない**（追記式） |
| **エラー** | **なる**: `TS` を固定値として比較している試験（**未確認・要調査**）。**ならない**: 追記済みの記録 |
| **UI / 自身** | 画面の「いつ時点」が動く。2DER 自身の順序判定に効く |
| **★廃止** | **時刻を git log から拾う運用** |

## 4. 過去の記録に主体（誰がやったか）が無い件

| 欄 | 内容 |
|---|---|
| **目的** | 「誰がやったか」を数えられるようにする |
| **理由** | 主体欄は C-1 で入ったが、**それ以前の記録には無い**（親 §1 の③） |
| **具体的な方法** | **遡って埋めない**（捏造になる）。**「主体が記録されている区間はここから」を1件 登記**し、それ以前は `UNKNOWN` と**言い切る** |
| **2DER への影響** | 記録を書き換えない。**境界を1件 足すだけ** |
| **エラー** | **なる**: 集計が「UNKNOWN 多数」を返す（**それが正しい**）。**ならない**: 既存の読み手 |
| **UI / 自身** | 画面に「ここから主体が取れる」が出る |
| **★廃止** | **主体不明を「Claude がやった」と推定する運用** |

## 5. 文書が台帳に載っていない（DOC 706 / 実ファイル 974）

| 欄 | 内容 |
|---|---|
| **目的** | 文書を台帳から辿れるようにする |
| **理由** | 親 §1 の⑥。**268本 未登記**（状況表・本日） |
| **具体的な方法** | **canonical 経路で投入**（裁定#4）。通せないものは **bypass を reason_code つきで記帳** |
| **2DER への影響** | 台帳の行が増える。**新しい台帳は作らない** |
| **エラー** | **なる**: 一括投入で run-gate に当たる／分類が BUILD に倒れて task が立つ（**本日 実測済みの挙動**）。**ならない**: 既存の記録 |
| **UI / 自身** | 一覧が増える。自身の検索対象が増える |
| **★廃止** | **「台帳のずれ N件」を状況表で毎回 眺めるだけの運用** |

## 6. 長文の内訳が取れない（1問い合わせ＝1明細）

| 欄 | 内容 |
|---|---|
| **目的** | 1回の問い合わせを**複数の明細**に分ける |
| **理由** | `submit.py:302` 逐語「**暫定の単純化: 1つの問い合わせ = 1つの明細**」 |
| **具体的な方法** | **★行1〜3 が通ってから**。先に実行の痕跡と時刻が要る |
| **2DER への影響** | 分類・集計の粒度が変わる。**影響が最も広い** |
| **エラー** | **なる**: 既存の集計が件数ベースで壊れる（**要調査**）。**ならない**: 投入そのもの |
| **UI / 自身** | 画面の件数が変わる。2DER 自身の分類軸に効く |
| **★廃止** | **「1発話1正解」で採点する運用** |

## 7. 勘定科目の再評価

| 欄 | 内容 |
|---|---|
| **目的** | 軸が立つかを**測り直す** |
| **理由** | 親 §2-1: corpus が 08-01 から**2倍のペース**。7/25 の結論（`n_frozen_axes=0`）は**古い可能性**。★但し「可能性」であって**まだ測っていない** |
| **具体的な方法** | **★行1〜2 の後**。判定材料は「その他 63%」がいくつになったか |
| **2DER への影響** | 分類の出力が変わりうる |
| **エラー** | **なる**: 軸が立たなければ**前と同じ結論**（**それも結果**）。**ならない**: 記録 |
| **UI / 自身** | 分類の見え方 |
| **★廃止** | **古い結論（軸は立たない）を根拠なく引き継ぐ運用** |

---

## ★MGR への1行（最初の1行の推薦）

```
★★推薦=行1（実行の痕跡を読む口）。★理由3つ:
   ① ★調査2 で残った唯一の不足（到達性）に ★直接 効く
   ② ★書く物を1つも増やさない（★痕跡は既に毎回 書かれている・読み手が0なだけ）
   ③ ★親の受入試験「この2日を台帳だけで説明できる」に ★最短で近づく
★★★行1 が通るまで 行3以降を始めない（★半移行を作らない）
★★★★★私は選ばない——★選ぶのは MGR（★完了条件の逐語「MGR が最初の1行を選べる状態」）
```

## ★禁止に触れていないことの確認
```
★82本 個別 API=無い（行1・行2 とも ★口は1本）／ ★新しい台帳=無い（★全行 既存を読む・書くのみ）
★地図の完成を成果にしていない=★各行の廃止欄が「Claude の手作業が減る」で書かれている
★半移行=行1→行2→行3 の順序を明記し、★古い口を消す指示を1つも書いていない
```
