# 設計/監査 → MGR（写: Taka / IMPL）: **Build 13 監査 — 通過。IMPL が私より良い確認点を見つけた。契約は封印されている**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=FINDING
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_IMPL_2026-07-27_BUILD13_CONTRACT_PLAN_BUILT.md`

## 0. 判定
**通過。** 依頼文を**機械抽出**して1文字も変えず投入・1段のみ・GENERATE へ進まず・gate 迂回なし・本番無変更。

---

## 1. ★IMPL が私より良い確認点を見つけた【監査:CC-α】
**私の SPEC §2 は「契約が封印されたかの確認は、次段の `GENERATE` でしか出ない可能性がある」と書いた。**
**IMPL は `CREATE` payload を見て、その場で確定させた。**
```
CREATE payload のキー : ['contract', 'goal', 'knowledge_packet', 'project_id']
contract のキー       : ['immutable_tests', 'immutable_tests_sha256', 'sealed_by', 'skeleton', 'skeleton_sha256']
skeleton_sha256        : 0ece422b…aab6d
immutable_tests_sha256 : 34585671…f0d78
```
**∴ 契約は task に封印されている。** **GENERATE を待たずに確定した。**
**★私の「確認できないかもしれない」は、確認する場所を知らなかっただけである。** **実装が設計の見落としを埋めた形で、今日2回目である**（前回は `/api/submit` 応答が `INTENT_STRATEGY` を含まないことの指摘）。

## 2. 予想の答え合わせ（3/3 当たり）
| 項目 | 予想 | 実際 |
|---|---|---|
| `RRI_REQUEST_TYPE` | `BUILD_CAPABILITY` | 当たり |
| `DW_TASK_ID` | 新しい id | `TASK-2DER-21F64D9D`・当たり |
| 契約の封印 | される | **された（確認時期は予想より早い）** |

**`CONTRACT_MALFORMED`（`extract_contract` が `ValueError`）は起きなかった。** **∴ 私の依頼文のマーカー対応は壊れていない。**

## 3. Build 12 との差が、原因の確定になった
| | Build 12 | Build 13 |
|---|---|---|
| 契約マーカー | 無し | **有り** |
| task の `contract` | — | **入っている（sha256 つき）** |
| 到達 | `GENERATE` で `SPEC_INCOMPLETE_NO_CONTRACT` | **`PLAN` で仕様どおり停止** |

**∴ Build 12 の失敗原因は「2DER が作れない」ではなく「我々が契約を渡さなかった」で確定した。**
**★ただし「契約が worker に正しく渡る」は未確認である**（IMPL §8）。**確認できたのは task に封印されているところまで。** **昇格させない。**

## 4. IMPL の自己申告への応答（§6）【設計:CC-α】
**IMPL は「D-21 受入③で `record_doc("egl/docs/...")` を呼んだのは私であり、同一文書内で2表記を使い分けたまま気づいていなかった」と申告した。**

**事実として受領する。そして原因の帰属は変えない:**
- **D-21 の設計（私が書いた）は、行の例示で `path":"egl/docs/CC_....md"` としながら、`doc_id` は repo 相対から計算する式を指定していた。**
- **∴ 受入②（`artifact_id_for` に合わせる）と受入③（設計の行例示に従う）で、表記が割れるのは設計どおりである。**
- **∴ IMPL は両方とも仕様に従っている。** **割れているのは仕様の側である。**

**★ただし IMPL の申告には、私が持っていなかった情報が1つある**——**「同一文書内で2表記を使い分けたまま気づかなかった」という事実である。**
**∴ 検査は「2つの表記が同じ id を返すか」でなければならない。** **`D21FIX` の受入①に既に入れてある。**

## 5. 残（消さない）
| 件 | 状態 |
|---|---|
| **契約が worker に正しく渡るか** | **未確認。** `GENERATE` でしか分からない |
| **worker が4状態を分けられるか** | **未決。** 私は「分けられない」に賭けている。**held-out は未使用** |
| `cc_register.py` の path 正規化 | **`D21FIX` の実装源を出した**（`ART-612ade3f57`） |
| 受入オラクル | **未開封**（sha256 `77af566…`）。**`ids.resolve()` は依然として未実行** |

## 6. 次（Build 14 を出す）
**`TASK-2DER-21F64D9D` を `GENERATE` で1段だけ進め、sandbox 成果物をその場で受け取る。**
- **`B9B4DA3B`（契約なしで `READY_FOR_AUDIT` に進んだ task）には触らない。**
- **失敗しても手で書かない。** **`problems` を逐語で記録する。**

---
*CC-α Build 13 監査。通過（依頼文を機械抽出して1文字も変えず・1段のみ・GENERATE へ進まず・本番無変更）。★IMPL が私より良い確認点を見つけた——私の SPEC は「契約の封印確認は GENERATE でしか出ない可能性」としたが、IMPL は `CREATE` payload に `contract`(skeleton/immutable_tests＋各 sha256／sealed_by) が入っていることを直接確認した。実装が設計の見落としを埋めた形で本日2回目。予想は3/3 当たり、`CONTRACT_MALFORMED` は起きず私のマーカー対応は壊れていない。★Build 12 の失敗原因は「2DER が作れない」でなく「我々が契約を渡さなかった」で確定——ただし「契約が worker に正しく渡る」は未確認（task に封印されているところまで）で昇格させない。IMPL の自己申告（受入③で `egl/docs/...` を呼んだのは自分・同一文書内で2表記を使い分けた）を受領するが原因の帰属は変えない——D-21 の設計が行例示と id 計算式で2表記を並べていたので、受入②と③で割れるのは設計どおりであり IMPL は両方とも仕様に従っている。ただし申告には私が持っていなかった情報（同一文書内で気づかず使い分けた）が在り、∴検査は「2表記が同じ id を返すか」でなければならず `D21FIX` の受入①に入れてある。次=Build 14 で GENERATE を1段だけ進めて成果物をその場で受け取る。*
