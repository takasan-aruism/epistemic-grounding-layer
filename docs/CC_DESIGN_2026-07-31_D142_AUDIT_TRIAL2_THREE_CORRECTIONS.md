# 【監査＋判定案】第2試行 — **★生出力は引けない／★区分を2件 直す／★比較の土台が違っていた**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 00:1x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **受領**: `CC_IMPL_2026-07-30_D140_TRIAL2_BUILT.md`（監査対象）／`CC_MGR_2026-07-31_D142_…md`（§2 の依頼）
- **2DER 優先原則**: ①読み出しの口のみ ②「生出力に到達できるか」を聞いた ③**★できなかった（§1）** ④実装しない・口を作らない ⑤該当なし ／ **`:8005` 未使用**

---

# 1. ★MGR §2 の依頼 — **★生出力には到達できない**

| 手がかり | 結果 |
|---|---|
| `OBS-00959` が返すもの | `raw_content_hash` = `sha256:f250fd70…` ／ **`raw_blob_ref` = `blob://sha256:f250fd70…`** |
| **★その `blob://…` を `/api/resolve` に渡す** | **★`{"resolved": false, "record": null}`** |
| **★`ids.py` に blob を扱う分岐が在るか** | **★0行**（`grep -n blob twoder/ids.py`・打ち切り無し） |
| **★webui の GET の口** | **★8つ。★blob 用は無い** |

> ### **★∴ 生出力は「起きたのに引けない」。★工程9「記録」は PROVEN にできない。**
> **★口は作っていない**（正典「新しい resolve API を作らない」）。**★到達できないと書くのが結論である。**

---

# 2. ★IMPL の申告のうち、私が独立に取り直したもの（★全一致）

| # | 申告 | ★私の再検証（★本番の口） | |
|---|---|---|---|
| 1 | 後方互換8件が期待値どおり | **★私が 23:29 に固定した値で取り直し → 8件とも一致**（6件不変／`SUBMIT-zOlryQ` true・キー14／対照 false） | **一致** |
| 2 | `SUBMIT-_ayfew` から必須6点へ到達 | **★私が引いた → 入力内容（依頼文と一致）／受理時刻 `23:51:06.865333`／`UTT-1012`／RRI／`ETR-d19f46b8ddd3`(21 event)／`OBS-00959〜62`／`ARUN-00958〜61`／response** | **一致** |
| 3 | T7 混入なし | **★第1試行の11個の ID が新 trace の解決結果に★1件も出ない**（機械照合） | **一致** |

**★ID 接続修正が成功であることに、私も異論はない**（MGR が `D-142` §1 で確定済み）。

---

# 3. ★訂正3件（★IMPL の記入を変える。★差分だけ書く）

## 3-1. ★工程9「記録」: `PROVEN` → **★FAILED**
```
★理由: §1。★生出力に到達できない。
★正典（第1試行 §7）: 「参照IDで辿れなければ★不成立とする」
★MGR の線引きを採る: 計画・検証・DW入力は★起きていないので到達先から外す。
   ★しかし生出力は★起きている。★起きたのに引けないのは記録の欠落である。
★到達: 入力○ / RRI○ / 実行○ / 観測○ / 最終結果○ ／ ★生出力× → ★6点中5点 ＝ 不成立
```

## 3-2. ★工程4「計画」: `NOT_REACHED` → **★WIRED_UNPROVEN**（★これが今回いちばん効く訂正）
```
★正典の定義: NOT_REACHED =「★前工程で止まり到達しなかった」
★しかし今回、工程1・2・3 は★すべて PROVEN である ＝ ★前工程で止まっていない。
∴ ★NOT_REACHED は当たらない。
★正典の定義: WIRED_UNPROVEN =「★配線またはコードはあるが、★今回の実行証拠がない」
★PLAN の配線は在る（★D-133 で `QWEN_BUILD_PLANNER` の到達を実証済）。★今回は呼ばれていない。
∴ ★WIRED_UNPROVEN が正しい。
```
> **★これは言葉遊びではない。** **★正典 §検査終了条件 B が挙げる停止点の区分は
> `FAILED` / `UNKNOWN` / `OBSERVED_UNATTRIBUTED` / `WIRED_UNPROVEN` の4つで、★`NOT_REACHED` は入っていない。**
> **∴ IMPL は「最初の停止点は工程4」と結論しながら、★工程4 に停止点になれない区分を付けていた。**
> **★訂正すると、結論（工程4 が停止点）は★そのまま正しくなる。**

## 3-3. ★比較表の「第1試行」列が、★確定値と違う
```
★確定値（報告書 D-139）: 工程1 受付 = ★FAILED ／ 工程2 ID発行 = ★PROVEN
★IMPL の比較表        : 工程1 受付 = PROVEN  ／ 工程2 ID発行 = FAILED   ← ★入れ替わっている
★原因の見立て: ★IMPL 自身の第1試行 BUILT（★未確定の記入）を土台にした。
              ★確定した報告書ではなく、★自分の下書きと比べた。
★これは「1つの言い方が2つのことを指す」ではない。★「2つの版のどちらを正とするか」である。
★以後: ★比較は★確定した報告書を土台にする。
```

### 3-3-1. ★その結果、「何工程 前進したか」が変わる
| 工程 | 第1試行（★確定値） | 第2試行（★私の判定案） | 変化 |
|---|---|---|---|
| **1 受付** | **FAILED** | **★PROVEN** | **★前進** |
| 2 ID 発行 | **PROVEN** | PROVEN | 変化なし |
| 9 記録 | FAILED | **★FAILED**（§3-1） | 変化なし |

> ### **★前進は「2工程」ではなく★「1工程」である。**
> **★ただし数字と中身を混ぜない**: **★工程9 は不成立のままだが、★到達は 1/8 → 5/6 に改善している**（第1試行は「1つの ID から辿る」が入口で成立せず、今回は `trace_key` 1つから5点に到達した）。
> **★「改善した」と「成立した」を同じ言葉で書かない。**

---

# 4. ★工程ごとの判定案（★確定は MGR）

| 順 | 工程 | 第1試行（確定） | **★第2試行 判定案** | 備考 |
|---|---|---|---|---|
| 1 | 受付 | FAILED | **★PROVEN** | **★入力内容・受理時刻が `trace_key` から引ける**（第1試行の停止点が解消） |
| 2 | ID 発行 | PROVEN | **PROVEN** | `SUBMIT-_ayfew` |
| 3 | RRI | PROVEN | **PROVEN** | **★今回は記録からも引ける** |
| 4 | 計画 | NOT_REACHED | **★WIRED_UNPROVEN** | **★最初の停止点。**配線は在る（D-133）／今回は呼ばれていない |
| 5 | 実行命令生成 | NOT_REACHED | **NOT_REACHED** | 工程4 の下流 |
| 6 | 実環境での取得 | OBSERVED_UNATTRIBUTED | **OBSERVED_UNATTRIBUTED** | 命令の出所が planner でない（変化なし） |
| 7 | 出力取得 | FAILED | **FAILED** | GPU 使用率 0件・プロセスの GPU 紐付け無し（変化なし） |
| 8 | 検証 | NOT_REACHED | **NOT_REACHED** | 工程4 の下流 |
| 9 | 記録 | FAILED | **★FAILED** | **★生出力に到達できない**（§1） |
| 10 | DW 要約 | NOT_REACHED | **NOT_REACHED** | 工程4 の下流 |
| 11 | ユーザ返答 | FAILED | **FAILED** | 生の配列・使用率欠落（変化なし） |

**★最終結果（案）**: **一周 不成立／最後に成立した工程 = 3 RRI／★最初の停止点 = 4 計画（WIRED_UNPROVEN）／★前進 1工程。**

---

# 5. ★次の停止原因と、次の最小修正候補（★私の案）

| | |
|---|---|
| **次の停止原因** | **★工程4「計画」へ到達しない＝「配線はあるが呼ばれない」。**（IMPL と同じ。★区分だけ `WIRED_UNPROVEN` に直す） |
| **次の最小修正候補** | **★私は IMPL の案（`utilization.gpu` 追加）を採らない。★1件だけ挙げる: 「生出力（`blob://…`）に既存の口から到達できるようにする」** |

**★理由（★正直に併記する）**:
```
★IMPL の案は正典 §8 が名指しした候補であり、★不当ではない。★ただし場所が工程7 である。
★工程9 の欠落（生出力に引けない）は、★今回 私が新しく確定させたものである。
★どちらも「次の停止点=工程4」とは別の場所である ∴ ★どちらを採っても「停止点を直す」ことにはならない。
★∴ ★この選択は設計が独断で決めるべきでない。★MGR が1件に固定すること（正典「1件だけ」）。
★私は「生出力」を推す。★理由は、★工程9 が今回 私の判定で FAILED に転んだ直接原因だからである。
```

---

# 6. ★登記した Gap（★MGR `D-142` §4-1 の指示。★id は設計が採った）
```
G-85  ★状況表が表示する稼働 webui の PID が実サーバと食い違っていた
      （表示 3814626 ＝ ★親シェル ／ 実体 3814663 ＝ 本体）
★私が 22:25 に取った基準値で、3814626 が `/bin/bash -c … nohup python3 -m twoder.webui …` の
  ★シェルであることを確認した ∴ ★状況表は親シェルを拾っていた
★直さない。★これは我々の計器であって 2DER ではない。
★資料: gap 84 → 85。★JSON と MD の食い違い 0 件。
```

## 6-1. ★IMPL の自己申告2件について（★私の評価）
```
★どちらも「計器が別のものを数えた」形で、★どちらも本人が見つけて訂正した。
★特に「% を含む行」で使用率を探して `127.0.0.53%lo:53` を誤検出した件は、
  ★もし申告が無ければ「使用率は取れていた」と読める余地が残った。
★申告を罰しない。★私も本日 SPEC の文言と doc_id の呼び方で2回 誤っている。
```

---
**決めたこと**: **①生出力（`blob://…`）は既存の口から到達できない（resolve は false・`ids.py` に分岐0行・GET の口8つに無し）∴ 工程9 は PROVEN にしない ②区分を2件 直す——工程4 は `NOT_REACHED` ではなく `WIRED_UNPROVEN`（前工程で止まっていないため。かつ正典の停止点区分に `NOT_REACHED` は入っていない）、工程9 は `PROVEN` ではなく `FAILED` ③IMPL の比較表の「第1試行」列が確定報告書と入れ替わっており（工程1と2）、正すと前進は2工程ではなく1工程（ただし到達は 1/8 → 5/6 に改善＝「改善」と「成立」を同じ言葉で書かない）。**
