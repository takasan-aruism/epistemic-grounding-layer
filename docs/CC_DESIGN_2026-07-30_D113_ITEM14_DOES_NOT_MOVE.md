# 設計/監査 → MGR（写: Taka / IMPL）: **D-113 — ⑭は★動かない。★散文依存は 6 のまま。★Taka 終了条件4 は成立しない**

- `BUILD_ROLE: 参照`（**測定のみ。★実装していない・★投入していない・★台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **裁定**: `CC_MGR_2026-07-30_D111_P2_NEEDS_NO_BUILD_MEASURE_IF_14_MOVES.md` §4（★⑭の1行だけを測る）

## 0. ★2DER 優先原則の5点
| ① 入口 | **`GET /api/resolve?id=DE-…`** ／ ② **対象の開発に対応する DE が在るかを 2DER に聞いた** ／ ③ **★無い**（★DE に時刻の欄も無い） ／ ④ **実装しない** ／ ⑤ **該当しない** |
|---|---|

## 1. ★答え（★先に。★測る前の見込みを書かない）
```
★⑭ は「散文依存」のままである。★動かない。
★散文依存: ★6 → ★6（★減っていない）
★Taka 終了条件4「散文依存が一項目以上 減る」: ★成立しない
```

## 2. ★測ったこと
```
再現（★front door だけ・★DE-0530〜0567 を★全数・打ち切り無し）: ★38件 引けた
  「受信の記録」の開発（D-63/D-64/D-68）を指す語で走査
  ★語が当たった: 2件 → ★読むと どちらも別件だった
     DE-0550 「Build 1a'(表層集合の拡張)…」  ← "SENT" が語の中に含まれただけ
     DE-0557 「Build 1c(P4 存在の前提)の最終独立監査…」 ← "届いた" が別の文脈
∴ ★対象の開発に対応する DE は ★0件
```
> **★これは D-86 で私が書いたのと同じ形である**——**★語の出現は、主題の一致ではない。**
> **★私は2件を「見つけた」と書かず、★開いて読んで0件にした。**

## 3. ★なぜ動かないか（★1行）
> **★`generated_by_principal` は在る。★値も入っている**（標本28件中10件が `CLAUDE_CODE`）。
> **★しかし対象の開発に DE が無い ∴ ★引く相手が無い。**

| ★在るもの | 読む口（`/api/resolve`）／機械の欄（`generated_by_principal`） |
|---|---|
| **★無いもの** | **★対象の開発の DE** |
| **∴** | **★⑭ は EGL から取れない。★散文（`CC_*.md`）にしか無いままである** |

## 4. ★言わないこと
| ★書かない | 理由 |
|---|---|
| **「6→5 になった」** | **★なっていない。★測った** |
| **「あと1段で動く」** | **★その1段は「我々の開発が EGL に載ること」＝★P-3 の主題であり、★1段ではない** |
| **「`generated_by_principal` は使えない」** | **★使える。★他の案件では10件 値が入っている。★使う相手が無いだけである** |

## 5. ★P-2 の結果（★まとめ）
| # | | |
|---|---|---|
| **条件2**（EGL から直接1件 証拠を取得） | **★成立**（D-111・`evidence_refs` を取得した） | **★実装0** |
| **条件4**（散文依存が1つ減る） | **★不成立**（本文書） | **★実装0** |
> **★P-2 で build を1つも作らず、★条件2 は成立し、★条件4 は成立しなかった。**
> **★これは「半分できた」ではない。** **★2つの別々の条件が、★別々の結果になった。**

## 6. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **DE-0530 より前に対象の DE が無いか** | **★見ていない。★対象の開発は 07-29〜07-30 なので★末尾側を見た** |
| 2 | **DE に欠番が在るか** | **★38件 引けたが、★0530〜0567 は38番ある ∴ 欠番0 と読めるが★確かめていない** |
| 3 | **`generated_by_principal` の欄が無い14件の理由** | **★実装を読んでいない** |

---
*CC-α D-113（測定のみ）。★**答え=⑭ は「散文依存」のままで動かず、散文依存は 6 → 6（減っていない）、Taka 終了条件4「散文依存が一項目以上 減る」は成立しない**。★測ったこと=**front door だけで DE-0530〜0567 を全数・打ち切り無しに走査して38件 引き**、「受信の記録」の開発（D-63/D-64/D-68）を指す語で照合したところ**語が当たったのは2件だが読むとどちらも別件**（DE-0550 は "SENT" が語の中に含まれただけ／DE-0557 は "届いた" が別の文脈）∴ **対象の開発に対応する DE は0件**——**これは D-86 で CC-α が書いたのと同じ形（語の出現は主題の一致ではない）であり、2件を「見つけた」と書かず開いて読んで0件にした**。★**なぜ動かないか**=**`generated_by_principal` は在り値も入っている（標本28件中10件が `CLAUDE_CODE`）が、対象の開発に DE が無いので引く相手が無い** ∴ **在るのは読む口と機械の欄、無いのは対象の開発の DE、⑭ は EGL から取れず散文（`CC_*.md`）にしか無いまま**。★**言わないこと**=「6→5 になった」（**なっていない。測った**）／「あと1段で動く」（**その1段は「我々の開発が EGL に載ること」＝P-3 の主題であり1段ではない**）／「`generated_by_principal` は使えない」（**使える。他の案件では10件 値が入っており、使う相手が無いだけ**）。★**P-2 の結果**=**条件2 は成立（`evidence_refs` を取得）／条件4 は不成立**、**どちらも実装0** ∴ **P-2 で build を1つも作らず、2つの別々の条件が別々の結果になった（「半分できた」ではない）**。★確かめていないこと=**DE-0530 より前は見ていない（対象の開発は 07-29〜07-30 なので末尾側を見た）**／**欠番は 38件/38番 で0と読めるが確かめていない**／**`generated_by_principal` の欄が無い14件の理由は実装を読んでいない**。*
