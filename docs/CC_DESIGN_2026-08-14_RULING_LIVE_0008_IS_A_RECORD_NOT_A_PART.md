開発者規律 確認済(v1.0)

# 【設計裁定・1件】★`live_0008.py` は **部品では なく 記録** ―― ★★繋がない ／ ★★消さない
## ＋ ★★★分類規則の 欠陥を 1つ 出す（★★残り43件に 効く）

宛: MGR ／ 発: DESIGN ／ 2026-08-14 03:2x ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **MGR 03:09**「★残るのは `live_0008` 1件 ＝ ★行き先を 1語（★★file を 消すのは 私 ／ ★あなたは 決めるだけ）」

---

## 1. ★★私が 引いた（★★決定論・★LLM 0回・★★推測 0）

```
★① ★★呼び手 = ★★★0件（★5 repo の `*.py` を `live_0008` で 引いた）
★② ★★入力 file = ★★★無い（★`/tmp/live0008.json` ／ 出力 `/tmp/live0008_result.json` ★どちらも 不在）
     ―― ★★∴ ★★★もう 走らせられない（★★『走っていない』では なく ★★『★入力が 消えている』）
★③ ★★入口 = ★★`__main__` ★0件（★★上から下へ 流れる 台本＝★呼び出せる 形では ない）
★④ ★★本番の 後継が 在る = ★★`twoder/operator.py:118 advance_dw_machine_ops`
     ―― ★★★この台本が 呼んでいた 物が ★★本番に 在る（★繋ぎ直す 相手が 既に 居る）
★⑤ ★★置き場ごと 1つの 実験 = ★兄弟 = ★`README.md` ／ `KEY_LOGS.md` ／ `ab_measure*.py` ／ `result_*.json`
★⑥ ★★file 自身の 逐語 = 「★approved, scoped, ★★single-use」／ ★時刻が ★`2026-07-13` で 固定
```

## 2. ★★裁定 = ★★★繋がない ／ ★★消さない

```
★★★繋がない（★A では ない）= ★理由 = ★§1 の ①②③④
   ―― ★★繋ぐ相手は ★★既に 本番に 在る ∴ ★★繋ぐと ★★★同じ物が 2本に なる

★★★消さない（★B では ない）= ★理由 = ★Taka 裁定 逐語
   ―― ★★『★試験の 記録も 帳簿に 残す 義務が 在る』（★[[ledger-records-tests-too-not-pollution]]）
   ―― ★これは ★★2DER が 初めて 本番で 一気通貫した 日の 記録（★★消すと 由来が 消える）

★★★∴ ★★行き先の 1語 = ★★★`RECORD`（★★部品では なく 記録）
```

## 3. ★★★私の 書き方の 欠陥（★先に 出す）

```
★★私は 02:0x の BUILD SPEC で ★★『★B = 捨てる』と 書いた
★★★その1語に ★★2つの 事を 混ぜていた
   ★(a) ★★宿題の 一覧から 外す（★★繋ぐ 相手では ない）
   ★(b) ★★file を 消す
★★★この件は ★(a) は 是 ／ ★(b) は 非 ＝ ★★★1語では 表せなかった
★★∴ ★★語を 3つに 直す = ★★`WIRE`（繋ぐ）／ ★`DELETE`（消す）／ ★★`RECORD`（残すが 繋がない）
   ―― ★★★語を 増やしたのは 私 ∴ ★★増やした 理由を ここに 書いた（★規律 §9）
   ―― ★★★『中間を 作らない』に 反しないか = ★★反しない
      （★★`RECORD` は ★★★宿題では ない と 確定させる 語＝★★保留では ない）
```

## 4. ★★★分類規則の 欠陥（★★これが 本体＝★残り43件に 効く）

```
★★いまの 規則（★逐語）= 「★★ONE_SHOT = ★★WIRE が 0 かつ ★demo_ / experiments/ / _proto」
★★★∴ ★★機構を 1つでも import した 試し物は ★★★必ず `WIRE_CANDIDATE` に 落ちる
   ―― ★★★実物が これ = ★`live_0008` は ★`authority` を import する ∴ ★WIRE の印が 立つ
      → ★★`experiments/` に 在るのに ★『繋ぐ宿題』に 数えられていた

★★★直す向き = ★★根拠を ★★『★その file 自身が 持つ 物』に する（★★★本日 4回 落とした 所）
   ★★候補（★決定論・★名前や 置き場に 頼らない）
      ★① ★★入力／出力が ★`/tmp` 配下（★★走行を またいで 残らない＝★再現できない）
      ★② ★★時刻・id が ★★ハードコード（★★1回きりの 台本）
      ★③ ★★`__main__` も 関数も 無い（★★呼び出せる 形では ない）
      ★④ ★★呼び手 0件
   ★★★但し ★★私は これを ★★★1件（live_0008）でしか 確かめていない
      ―― ★★∴ ★★★規則を 今 書き換えない（★1件で 規則を 変えない＝★本日の 教訓）
      ―― ★★★戻す条件 = ★★`ONE_SHOT` 31件 ／ `UNDECIDED` 12件 に ★同じ4条件を 当てて
         ★★★何件 動くかを 先に 数える（★★動かないなら 規則は そのまま）
```

## 5. ★★受入（★★MGR が 引く）

```
★★① ★`live_0008` の 行き先が ★★`RECORD` で ★台帳から 引ける（★★空欄 0）
★★② ★★file が ★消えていない（★★bytes 不変）／ ★★`WIRE_CANDIDATE` の 件数が ★11 → ★★10
★★③ ★★『11件 片づけた』と 書かれていない（★★正しくは ★10段＋★1件は RECORD）
★★④ ★★§4 の 4条件を ★43件に 当てた ★★件数（★★★当てるだけ＝★規則は 変えない）
★★⑤ ★LLM 0回 ／ ★新台帳 0 ／ ★口 0増 ／ ★★語は 3つ（★増やした 理由が 書いてある）
```

## 6. ★★もう1件（★`AUTONOMOUS_GIT_ENABLED`）への ★設計の 立場（★★1行）

```
★★★設計する物は ★無い = ★★線引き（★台帳は自動 ／ コードは人）は ★★3週間前に 実装済み
★★∴ ★要るのは ★★①記録（DE+CHG）1件 ＋ ★★②既定値 1行 ＝ ★★★新しい設計 0
★★Taka へ 上げるのは ★MGR の 担当 ―― ★★出す数は ★MGR が 既に 持っている
   （★★手で commit 337件 ／ ★台帳だけ 324件＝★★『入れたら 324件 が 人の手から 外れる』）
```
