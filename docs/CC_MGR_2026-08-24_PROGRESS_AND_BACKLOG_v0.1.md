# 進捗と宿題の整理 v0.1 — 台帳から出した現在地

**作成: Claude Code（MGR）／ 2026-08-24**
**ITEM: `ITEM-2DER-EVO-0094`（台帳の明細を完成させる）／ `IN_PROGRESS` / artifact 29件 / task 0件**
**★記憶で俯瞰していない。数はすべて台帳の所有API・front door から出した。**

---

## 1. 進捗 — 「機構は通った。母数には流していない」

| | 件数 | 割合 |
|---|---|---|
| 明細（分母） | **1,003** | — |
| 種別が付いた（構造化） | **33** | 3.3% |
| 勘定科目が付いた | **27** | 2.7% |
| 処分が付いた（verdict） | **5** | 0.5% |
| 根拠が付いた（evidence） | **5** | 0.5% |

依頼（thread）**659本**の状態: `SOFT 658` / `NARROWING 1`。

**通した機構（すべて1〜2本の task で実証済み）**

| 段 | 内容 | 状態 |
|---|---|---|
| 段0 | 種別 / 原文 / 原文中の位置 / 親request | ✅ |
| 段1 | 保存時の200字切断をやめた（新規のみ） | ✅ |
| 段2 | 明細が指す先の実在確認（✓/✗/?） | ✅ |
| 段3 | 依頼の形の不在検知（`REQUEST_GAPS`） | ✅ |
| 段① | 誰が書いたか（`recorded_by` / `recorded_via`） | ✅ |
| 段4 | 根拠（`QUESTION_EVIDENCE`・AEC の語彙を借用） | ✅ |
| 段5 | 科目割当（`assign_account`）＋ `dispose_question` の修理 | ✅ |
| — | chart 5→63（Taka裁定・命名未確定2件は事実を保持） | ✅ |
| — | thread 状態機械の**本番初回実行**（SOFT→NARROWING） | ✅ |
| — | 明細→PLAN の事実block（従来 prompt は1バイトも不変） | ✅ |
| — | 結果→明細の戻り（**常駐が手呼びなしで書いた**） | ✅ |

---

## 2. ★宿題 — 全記録から拾った未了（優先順）

### A. 本線の続き（Taka 裁定の①〜④のうち残り）

| # | 宿題 | 分母つきの実測 | 誰の権限 |
|---|---|---|---|
| A1 | **事実blockの効果が UNVERIFIED** | ED65242E では両群とも invented=0（対照にならない）。**EF6826DC（SPEC 0/TEST 0）で測り直す**必要 | 私 |
| A2 | **③既存への拡大** | 構造化 33/1,003 ／ 科目 27/1,003 ／ **割当可能なのは 644件（65%）** | 裁定待ち |
| A3 | **自動循環の拡大** | 未記帳の走行を持つ task **146件**。常駐は並びの先頭1件しか見ない | 裁定待ち |
| A4 | **④類似TASK統合・テンプレート学習** | 未着手。`REQUEST_KIND_COUNTS` は記録済みだが**横断consumer 0** | 未着手 |

### B. 構造的に詰まっているもの

| # | 宿題 | 実測 | 誰の権限 |
|---|---|---|---|
| B1 | **`present_gaps` の本番呼び手 0件** | ∴ **thread 全体は RESOLVED へ進めない**（明細単位は通る） | DS 管轄 |
| B2 | **`accept_thread`（人間の扉）の呼び手 0件** | authority の設計が要る | Taka |
| B3 | **`ds_delivery_receipt` の意味・schema・consumer が無い** | 空だけ弾く暫定。値の意味論は未定義 | 別AXIS |
| B4 | **`dispose_question` が二重処分を検出しない** | 既存 F-1 指摘。誤 RESOLVED の取り消し手順が無い | 別AXIS |
| B5 | **明細→封印試験の鍵が無い** | 生成側が刻む必要（DW/EGL 側の設計） | 別AXIS |

### C. 私が作った物の未完部分

| # | 宿題 | 実測 |
|---|---|---|
| C1 | 計器が `test_` 接頭辞を丸ごと捨てる | **試験名に紛れた本物の発明を取りこぼす**。取りこぼし量を測っていない |
| C2 | `PLAN が出なかった` 走行が 1/16 | parse 失敗と生成失敗を分けていない |
| C3 | `match_questions` 先頭40字一致が 3/4 | 外れた1件の原因は原文の先頭違い。閾値は動かしていない |
| C4 | ITEM の `task_ids` が空 | 事後に立てた ITEM の正しい形が未確認 |

### D. 別AXISへ送ったまま戻していないもの

| # | 宿題 |
|---|---|
| D1 | `audit_backlog` 4→1 の統合（`GAP-RRI-4` が同一ID・異なる状態） |
| D2 | ③保留20本（7月停止）の棚卸し |
| D3 | basename identity collision **22本** ／ reader検出の残 FN **18件** |
| D4 | `ambiguity_patterns.jsonl` の writer 欠損（残骸整理） |
| D5 | ★**id 空間の衝突**（`cc_register` と `artifact_registry` が同じ `ART-` を返す） |
| D6 | CC_REGISTER の重複行（`ART-e8dfcab57d` ×3 など） |
| D7 | 342件が2層モデルの corpus 外（提案が立っていない明細） |
| D8 | 最大の大分類 `LCAT-d23a39ff`（161件）が**命名未確定** |
| D9 | `account_gate.decide()` は旧 `ACCOUNT_AXES_v3.json` を見たまま |
| D10 | 選択肢 5→63 が `decide_with_llm` の精度に効くかを測っていない |

### E. 他担当・環境側（私は触らない）

| # | 内容 |
|---|---|
| E1 | front door のプロセス劣化（再起動直後 0.49秒 → 1時間後 88秒）。782MB丸読みとは**別原因** |
| E2 | `/api/rthread` 3回で +107MB ／ `/api/etrace` 3回で +194MB（原因未特定） |
| E3 | `event_trace.jsonl` 782MB 自体の扱い（分割・圧縮・保持期間）が未決 |
| E4 | `twoder-manager-v0` が inactive（★状況表の「誰も起こさない」next=2DER が2件） |

---

## 3. ★私が Taka の手番を増やしてしまった

登記を揃えた結果、**CC_REGISTER の「Taka 待ち」が 2件 → 23件**になった。
うち **21件が今回私が足したもの**（`to="TAKA"` で登記したため）。

以前からの待ちは **2件だけ**（`D188_WE_ARE_THE_TREADMILL` / `D190_PUSHING_DOES_NOT_HELP`）。

∴ **これは「Taka が読むべき物が21件増えた」のではなく「私の作業記録が可視化された」**である。
`record_done` を打つ主体を決めていないため、**放置すると数字だけが積む**。
`to` を `MGR` にすべきだったかもしれない ―― **この形が正しいかは未確認**（宿題 C4 と同根）。

---

## 4. 次の一手（私の推奨）

**A1（EF6826DC での効果測定）を先に片付ける。** 理由:

- ③の拡大は「事実blockが効くか分からないまま 990件へ広げる」ことになり、
  効かないなら**広げても意味が無い**
- A1 は**1 task・LLM 16走行・約20分**で決着する（計器は既にある）
- Taka 裁定⑦「2つが閉じてから③へ」の**残り1つ**がこれ
