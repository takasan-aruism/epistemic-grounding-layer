# 【D-165】確定 — **★B・C は「書けなかった」のではなく「書かなかった」／次は A' を足して各10本**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 10:2x / TYPE=HANDOFF
- **正典**: `TAKA_2026-07-31_INPUT_DESIGN_NOT_MODEL_RUN_THE_CONTROLLED_TEST.md` ／ **★新しい名前で置いた**

## 1. ★確定（★設計の訂正を採る）
```
★「現在の入力では分けられない」（A は3回中1回のみ・★割れた／B・C は基準② を1回も満たさない）
★「B にすれば／C にすれば分けられる」は★書けない
★「Qwen が使えない」は★書かない
★2DER 担当工程数の差分: ★0
```

## 2. ★私と IMPL の読みが1つ壊れた（★設計が壊した。★採る）
```
★私と IMPL: 「★削るほど①に書ける具体値の種類が減った」＝ ★B・C の0件を★入力の側で説明していた。
★★設計の実測: ★メモリの具体値（`703029` / `4022456`）は ★B にも C にも★入力として在った。
∴ ★B・C の②が0件だったのは「★書ける値が無かったから」ではない。★「★書かなかったから」である。
★★私はこれを訂正として採る。★言い方を変える。
```

## 3. ★測定設計の欠陥（★設計が自分から引き取った。★私も引き取る）
```
★A は「★PLAN を JSON で出せ」と「★3つに分けよ」を★同時に要求している。
∴ ★A の結果は「分けられるか」だけを測っていない。★「2つを同時にできるか」を測っている。
★★`finish_reason=length` が2回 出たのは、★max_tokens 不足ではなく★prompt 設計の側である可能性が高い
   （★我々の記録: budget を上げる前に prompt を見る）。★A は3条件で最長である。
★★私の側の落ち度: ★私は D-163 §3 で「A は実物を使え」と固定したが、★問いと指示の衝突を予見しなかった。
```

## 4. ★次の1件（★A' を足して、★各10本）

| 条件 | 中身 | ★何が分かるか |
|---|---|---|
| A | 現在のプロンプトそのまま | （既存） |
| **★A'** | **★A から「PLAN を JSON で出せ」の指示だけを外し、★問いだけにする** | **★A の割れが「2つ同時」のせいかが分かる** |
| B | メモリに関係する観測だけ | （既存） |
| C | メモリの値だけ・取得コマンド除外 | （既存） |

```
★各10本（計40本）。★設定は同一。★出力は全文 保存。★prompt は1文字も変えない（★A' は引き算のみ）
★★判定は D-163 §6 の3条件のまま。★1文字も緩めない
★★「分布で出す」を今度こそ守る（★3本と決めたのは私の誤りだった）
★GPU を使う ∴ 使うと書く。★2DER の外の測定 ∴ 投入も台帳書き込みもしない・担当工程に数えない
```

### 4-1. ★Taka の指示を1点だけ広げている（★隠さない）
```
★Taka: 「★実際のplanner入力をそのまま渡して」
★★A' は「そのまま」ではない ∴ ★指示の範囲を出る。
★★ただし A は残す（★置き換えない・★足すだけ）。★A' が無いと、★A の割れの原因を切り分けられない。
★★Taka が「A' は要らない」と言えば、★A/B/C の各10本に戻す。★それだけである。
```

## 5. ★今はやらないこと（★理由つき）
```
★出力契約に「値を出せ」と書く（★設計の材料2）: ★§2 の「書かなかった」に直接 効きそうである。
★★しかし今それをやると、★A の混在（§3）を抱えたまま★もう1つ変数を足すことになる。
★★分布を取ってからにする。★順番を守る。
```

## 6. ★設計の作法（★1つだけ名指しする）
```
★★自分の SPEC の欠陥を、★IMPL の「迷い」の1行から★自分で拾って引き取った。
★★IMPL は「迷い」として書いただけで、★止まらなかった（★Taka の線どおり）。
★★書いておいたから、★設計が拾えた。★これが「迷いを1行 書く」の効き方である。
```

---
**決めたこと**: **①確定は「現在の入力では分けられない」（A は割れ、B・C は②を1回も満たさない） ②B・C の0件は「書ける値が無かった」ではなく「書かなかった」——メモリの具体値は B・C にも入力として在った（設計の訂正を採る） ③A は「PLAN を出せ」と「3つに分けよ」を同時に要求しており、`length` 切断は prompt 設計の側の可能性が高い ④次は A'（PLAN 指示を外した版）を足して A/A'/B/C 各10本——Taka の「そのまま」を1点だけ広げていることを明記する ⑤出力契約に「値を出せ」と書くのは、分布を取ってから。**
