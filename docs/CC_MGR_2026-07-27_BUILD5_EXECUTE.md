# CC 管理(MGR): **Build 5 本体を実行せよ（Taka 直接）**（HANDOFF・短く）

- `BUILD_ROLE: 参照` / 宛: IMPL(coder) / 写: DESIGN(CC-α) / Taka / 発: MGR / 2026-07-27 05:30 / TYPE=HANDOFF

## 1. 権限
Taka 直接発話（2026-07-27・逐語）:
> **「Build 5実行させて」**

**前段（コード確認）は完了・受領済。本体を実行してよい。** 実装源は `CC_DESIGN_2026-07-27_BUILD5_SPEC_GIVE_THE_FACT.md` の1本。

## 2. 実行時の確認（既定の再掲・追加なし）
- **arm A / B / C**（可能なら D）。**C の事実文は DESIGN 指定のものを、全 fixture に一様適用**（cherry-pick 禁止・行動を示唆する語と戦略名を入れない）。
- **per-row を必ず書き出す**（前回の欠損の再発防止・実装済）。
- **paired・分布・最低 10 run × 2 batch。** run 間ノイズ ≈0.10。**単発で言わない。**
- **報告に3つの数字**（止まる/動くの取り違え率・別解も可の一致率・厳密一致率）+ **per-fixture**。
- **★核心**: `IP1`/`IP2`（「あれ、どこにあったっけ？」「それ、その後どうなった？」）が `INTENT_PROBE` に来るか。**来なければ来なかったと書く。**
- **★非回帰の方が重い**: **既に正しかった fixture を壊していないか**を per-fixture で示す。
- **誤りが「止まる側」と「動く側」のどちらへ移ったか**を出す（D-5 の反省）。
- **投げる前にコストが前回と大きく違うなら先に言うこと。** 同程度なら報告不要・そのまま実行。

## 3. 書き方の固定（効いた場合）
**「2DER が良くなった」と書かない。** **「研究スクリプト上の意図調べで、事実を渡せば選べることが示された。本番への配線は別の作業」** と書く。**事実文が戦略定義とほぼ 1:1 である旨も併記する**（DESIGN が SPEC §3 で先に申告済）。

---
*MGR。Taka 直接「Build 5実行させて」により本体実行。追加条件なし（既定の再掲のみ）。核心は IP1/IP2 が INTENT_PROBE に来るか、非回帰の方が重い、誤りがどちら側へ移ったかを出す。効いても「2DER が良くなった」と書かない。*
