# 実装 → 設計/監査: **153行の ID を、6行の件数にしました**（人称を使わず名指し）

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=BUILT
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-30_D73_TASK_SUMMARY_BUILD_SPEC_v2_1.md`（**v1.0 / v2.0 は見ていません**）
- **受領した文書**: 上記 / `CC_MGR_2026-07-30_D73v21_APPROVED_AND_TAKA_ITEM_2.md`（写しで観測）

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 結果（★2軸）
### 経路
- [x] **実装した**（サーバ側で集計 ＋ 画面を件数表示に）
- [x] **止まってよい場所には当たりませんでした**（§6）
### 実測
- [x] 受入1a **ID を並べる箇所が消えた**（`li(r.deferred_active_tasks)` が0件）
- [ ] **受入1b 画面から155行が消えたことは★確かめていません**（★Taka にしか出せない）
- [x] 受入2 **4区分の和が表示している合計と一致**（117+18+18 = 153）
- [x] 受入3 合計が `_active_2der_tasks` の数と一致（**別に数えた数を出していません**）
- [x] 受入4〜7 文言（「出せません」＋理由／「対象外」／注記）
- [x] 受入8 所要 **0.046 秒**（★数字だけ）
- [x] 受入9 **配信物の JS が `node --check` を通る**
- [x] 受入10 非回帰 91 passed / 7 failed（**★走らせ方を §7 に書きました**）
- [x] **人称語（こちら／そちら／あなた）が追加分に0件**

---

## 1. 画面に出る形（実装したもの）
```
進行中の依頼  153件
  この一覧に、終わった依頼と止まった依頼は入っていません。

  背景で進んでいる（Qwen が動かす）    117件
  Claude の手番                        18件
  誰がやるか決まっていない              18件
  いま前に出すべきもの   出せません（前に出す順番を決める値が 2DER に無いため）
  合計                                153件

  ※ 止まっている依頼が 2件 ありますが、この一覧の対象外です。
  ※ Taka の手番かどうかはこの画面では出せません（2DER に人を指す担当者の値が無いためです）。
  「誰がやるか決まっていない」は、次に誰が何をするかを決める表にその状態が載っていないものです。
```
- **★ID を1件も出していません。**
- **★「いま前に出すべきもの」を消さず、「0件」とも書いていません。**
- **★停止中を「0件」と書かず「この一覧の対象外」と書いています。**
- **★「Taka の手番 0件」と書かず「出せません」＋理由にしています。**
- **★「保留」「担当者未定」「Taka 待ち」と書いていません。**
- **★色を使っていません**（`ok` も `bad` も足していません）。

## 2. 実装（2箇所）
| 場所 | 内容 |
|---|---|
| `webui.py::deferred_summary`（新規） | **★サーバ側で数える。** 母数は `_active_2der_tasks` が返す task、値は `next_legal_operation(task_id, events=…)`。**★events は1回だけ読む**（D-76 で渡せるようにしたもの） |
| `/api/submit` の応答 | **`deferred_summary` を追加**（★既存の `deferred_active_tasks` は残しています・追加のみ） |
| `/command` の JS | `li(r.deferred_active_tasks)` を **`deferredCard(r.deferred_summary)` に置き換え** |

**判定の順番（★この順でないと重複します）**
```
① actor_id == "-"                                   → 誰がやるか決まっていない
② actor_role in ("MANAGER", "CLAUDE_SENIOR")        → Claude の手番
③ 残り                                              → 背景で進んでいる（Qwen が動かす）
★いま前に出すべきもの: 判定しない（値が無い）
★停止中: 母数の外。件数だけ別に数える
```
- **★LLM を使っていません。** **★キャッシュを作っていません。** **★並列化していません。**
- **★`ADJUDICATOR` を `DEFAULT_ACTORS` に足していません**（実装源 §5・別 build）。
- **★引けない task は「背景」に混ぜず「誰がやるか決まっていない」に入れています**（例外時）。

## 3. ★私も独立に数えました（設計と一致）
```
再現: cd /home/takasan && python3 -c "…"（sys.path に dev-workcell のみ・★twoder を入れない）
  母数 _active_2der_tasks = 153 件
  背景で進んでいる 117 / Claude の手番 18 / 誰がやるか決まっていない 18
  ★和 = 153 / 母数 = 153 → 一致
  止まっている（母数の外） = 2 件
  所要 0.057 秒
```
> **★設計/監査の実測（117 / 18 / 18・和153）と★完全に一致しました。**
> **★これは今日の値です。** **★実装に固定していません**（毎回数えています）。

## 4. 受入8（所要・★数字だけ）
```
再現: webui.deferred_summary(_active_2der_tasks(W)) を直接呼ぶ
  ★0.046 秒
  {'total': 153, 'background': 117, 'our_turn': 18, 'unknown_state': 18, 'blocked_outside': 2}
  和 153 = total 153 → 一致
```
**★「速い」と書きません。**

## 5. 受入9（配信物の構文検査）
```
再現: GET /command → <script> 抽出 → node --check   → ★構文 OK（17327 バイト）
再現: GET /        → <script> 抽出 → node --check   → ★構文 OK（  999 バイト）
★ソースではなく配信物に対して検査しました（G-71）。
```

## 6. 止まってよい場所（実装源 §7・いずれにも当たりませんでした）
| # | 条件 | 実際 |
|---|---|---|
| 1 | 判定が §2-1 のとおりに書けない | **書けました** |
| 2 | 和が合計と一致しない | **一致**（合わせるための操作をしていません） |
| 3 | ID を0件にすると既存が壊れる | **壊れていません**（非回帰・構文とも） |
| 4 | SPEC が2通りに読める | 無し |
| 5 | ID がサーバ側で入っていた | **JS 側でした**（`li(r.deferred_active_tasks)`） |
| 6 | 母数が `_active_2der_tasks` で取れない | **取れました**（153件） |
| 7 | 重くて出せない | **0.046 秒**。**★重くありません**（D-76 の下ごしらえが効いています） |
| 8 | 「誰の手番か」を名指しできない | **★名指ししました**（Qwen / Claude）。**★Taka については「出せません」と書きました** |

## 7. ★非回帰の走らせ方（`G-74`・実装源 §8）
```bash
/home/takasan/.cc_tmp/d42/run_regression.sh /home/takasan/egl/docs/.cc_tmp/d49/regression_d73.txt
```
**中身**: `cd /home/takasan` してから `twoder/regression/*.py` の**98本**を1本ずつ
`timeout 600 python3 -m twoder.regression.<名前>` で走らせ、**終了コードで数える**（`pytest` は使っていません）。
```
結果: DONE 91 passed / 7 failed
基準との diff: ★空（失敗7本の顔ぶれまで一致）
```
- **★このスクリプトは版管理の外（`.cc_tmp`）に在ります。** **★消えたら再現できません。** **置き場所は指示待ちです**（D-77 §4 で申告済）。

## 8. ★確かめていないこと
1. **★画面から153行が消えたことを見ていません**（受入1b）。**★私はブラウザを開いていません。** **★Taka にしか出せません。**
2. **★「Claude の手番 18件」が正しい担当かを、私は判断していません。** **★機械が返した `actor_role` をそのまま名指ししただけです。**
3. **★裁定を誰がやるかは Taka へ出ています**（`CC_TAKA_2026-07-30_WHO_ADJUDICATES.md`）。**★私は答えを推測していません。**

## 9. 触ったファイル / commit / 再起動
```
twoder/webui.py のみ
```
- **★再起動しました**（配信物で検査するため・訂正後の順番②）。
- **commit していません**（MGR）。**投入していません。** **資料の同期はしていません。**

---
*IMPL BUILT（D-73 v2.1）。**153行の ID を6行の件数にした**——`webui.deferred_summary` を新設して**サーバ側で数え**（母数=`_active_2der_tasks`、値=`next_legal_operation(task_id, events=…)`、**events は1回だけ読む**）、`/api/submit` の応答に `deferred_summary` を**追加のみ**で載せ、`/command` の `li(r.deferred_active_tasks)` を `deferredCard` に置き換えた。**判定順は ①`actor_id == "-"` ②`actor_role in ("MANAGER","CLAUDE_SENIOR")` ③残り**で、**LLM もキャッシュも並列化も使わず、`ADJUDICATOR` を `DEFAULT_ACTORS` に足していない**。★画面は **ID を1件も出さず**「背景で進んでいる（Qwen が動かす）117／**Claude の手番** 18／**誰がやるか決まっていない** 18／いま前に出すべきもの＝出せません＋理由／合計153／※止まっている2件は対象外／**※Taka の手番かどうかはこの画面では出せません**」——**「こちら」等の人称を使わず名指しし、「Taka の手番 0件」と書かず、「保留」「担当者未定」とも書かず、色も使っていない**。★**私も独立に数えて設計/監査の実測（117/18/18・和153・止まっている2件）と完全一致**し、**今日の値として毎回数える形にして実装に固定していない**。受入8=**0.046秒**（「速い」と書かない）、受入9=**配信物**の `node --check` が `/command`（17327バイト）`/`（999バイト）とも OK、受入10=**非回帰 91/7・顔ぶれ diff 空**で**走らせ方を BUILT に明記**（`/home/takasan/.cc_tmp/d42/run_regression.sh <出力>`／`cd /home/takasan` して `twoder/regression/*.py` の98本を `python3 -m` で1本ずつ・pytest 不使用／**このスクリプトは版管理の外に在り消えたら再現できないので置き場所は指示待ち**）。止まってよい場所8件はいずれにも当たらず、特に**重さは 0.046秒 で問題にならなかった（D-76 の下ごしらえが効いた）**。★**確かめていないこと**=**画面から153行が消えたことは見ておらず Taka にしか出せない**（受入1b）／**「Claude の手番」が正しい担当かは判断しておらず機械が返した `actor_role` をそのまま名指ししただけ**／**裁定を誰がやるかは Taka へ出ており私は推測していない**。触ったのは `twoder/webui.py` のみ、再起動は訂正後の順番②に従い実施、commit・投入・資料同期はしていない。*
