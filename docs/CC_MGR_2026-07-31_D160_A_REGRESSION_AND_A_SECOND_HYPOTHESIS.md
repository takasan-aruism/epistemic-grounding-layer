# 【D-160】受入3 が反転した — **★仮説は1つではない。★対立仮説を先に登記する**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 09:4x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** ／ **★新しい名前で置いた**（D-158 §2-2）

## 1. ★監査を依頼する（★3点）
```
(a) 受入3 の反転（`steps` が ps / nvidia-smi / docker の取得を実装対象にしている）
(b) ★補の数え方（観測値の具体トークン24個・英語と日本語の両方・打ち切り無し）
(c) ★値が prompt に入っていたこと（`VALUES ALREADY OBSERVED`・値25行・3526字）
```

## 2. ★私の確定案
```
受入1 ○／受入2 ○／★受入3 ×（★前回 ○ からの反転＝★悪化）／受入4 ×／★補 ×
Last PASS : 受入2      First FAIL : 受入3      担当工程数の差分: ★0
★言い方（D-159 で先に決めたまま）: 「★値を見せても効かなかった」「★値も届かなかった」
★★さらに: 「★悪化した」。★言い換えない。
```

## 3. ★IMPL の仮説と、★私が足す対立仮説（★どちらも断定しない）

| | 仮説 | ★これが正しいなら |
|---|---|---|
| **H1（IMPL）** | **観測ブロックが★取得コマンドを列挙している** ∴ 計画がそれを「実装すべきもの」として並べた | ★コマンドを外せば止まる |
| **★H2（私が足す）** | **★依頼と無関係な観測まで見せている** ∴ 計画が★見せられた全部を実装対象にした | ★依頼に関係する観測だけ見せれば止まる |

**★H2 の根拠（★私が確かめられる範囲）**
```
★今回の依頼は「★メモリを多く使っているプロセス」だけである。
★しかし計画は ★ps／nvidia-smi／docker の★3つを並べた ＝ ★依頼していない2つが入っている。
★★`build_request` が毎回 全件を要求することは★既に登記されている（`G-06`）。
∴ ★「見せた全部を実装対象にした」と読める。
```
**★H1 では説明しにくい点（★正直に書く）**
```
★前回（コンテナ）も★同じ形でコマンドを見せていた。★それでも 受入3 は ○ だった。
∴ ★「コマンドを見せると必ず重複計画する」とは言えない。
```

## 4. ★次の1件（★1つに絞る。★H1 を先に潰す）

> ### **★観測ブロックから取得コマンドの列挙を外し、★値だけを見せる。**

```
★理由: ★差分が小さく、★どちらに転んでも結果になる（★止まれば H1、★止まらなければ H1 は落ちる）。
★★H2 はこの1手では試せない（★選別は別の場所＝`G-06`）。★だから今 登記しておく。
★★後で「H2 が原因だった」と言い出せないように、★先に書いた。
★私は「効く」と予告しない（★IMPL と同じ）。
```

## 5. ★対照が取れないことを、★制約として記録する
```
★同じ依頼文では task_id が同じで CREATE 確定済 ∴ ★同じ文で再計画できない。
∴ ★毎回 依頼文が変わる ∴ ★「変えたのは1つだけ」が★構造的に作れない。
★★これは我々の測定の限界である。★「値を見せたから壊れた」と断定しない理由でもある。
★★直さない。★登記する（★id は設計が採る）。
```

## 6. ★止まらない（★Taka の線）
```
★これは「データ破壊・互換性破壊・ロールバック不能」ではない ∴ ★止まらない。
★悪化したことを理由に前の版へ戻さない（★戻す判断は Taka の案件・★今は聞かない）。
★GENERATE を押さない ／ ★受入は4条件のまま ／ ★図の修正は保留のまま。
```

---
**決めたこと**: **①受入3 が ○→× に反転＝悪化。Last PASS=受入2、First FAIL=受入3、差分0 ②仮説は1つではない——H1（コマンドの列挙が重複計画を誘発）に加え、★H2（依頼と無関係な観測まで見せている＝`G-06` の選別欠陥が計画に出た）を先に登記する。前回もコマンドを見せて ○ だったので H1 だけでは説明しにくい ③次の1件は H1 を潰す（コマンドを外し値だけ見せる）。H2 はこの1手では試せないので先に書いた ④同じ文では再計画できず「変えたのは1つだけ」が作れないことを、測定の限界として登記する。**
