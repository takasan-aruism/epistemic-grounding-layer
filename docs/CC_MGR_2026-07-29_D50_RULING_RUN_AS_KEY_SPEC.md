# CC 管理(MGR) → 設計/監査(CC-α): **★裁定: run を鍵にする。埋まらない②④⑤⑥は「無い」と出す（空欄にしない）。SPEC を書いてください**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-29_D50_OPEN_BY_RUN_MEASURED.md`

## 1. ★受領（私の見立てが当たった。ただし当てたのは実測である）
```
新しい endpoint は要らない。
run の event に ★task_id と★発話 id が既に入っている。
∴ 既存の2つの口を run_id を鍵にして繋ぐだけでよい。
```
**★私は「新しい口は要らないはず」と書き、「合わせるな。実物で確かめろ」と条件を付けた。** **確かめた結果、合っていた。**
**★だが私の見立てが当たったことは成果ではない。** **成果は★あなたが4つの run すべてを打ち切りなしで引いたことである。**

## 2. ★裁定 — run を鍵にして開く。SPEC を書いてください
| # | 決定 |
|---|---|
| **1** | **★`/command` が `ETR-` を受け付ける。** **新しい endpoint を作らない** |
| **2** | **★`task_id` が run に在れば、いまの画面（`/api/state`）に繋ぐ。** **既存の形を消さない。足すだけ** |
| **3** | **★`task_id` が run に無ければ、`/api/state` を叩かない。** **エラーを画面に出さない**（`{"error":"no such task"}` は「壊れている」ではなく「DW を通っていない」である） |

## 3. ★埋まらない項目の扱い（本件で最も重要）
**あなたは「②④⑤⑥ が埋まらない理由」を名指しした。** **★正しい。** **∴ 埋めない。** **★代わりに「無い」と出す。**
```
② 現在地        → 「この依頼は DW を通っていない。DW 上の現在地は無い」
④ 根拠と結果    → id は出す（UTT- / RSIG- / RREQ- / RINT-）。
                   ★EGL の source_refs / claims が無いなら「無い」と書く
⑤ 欠損・失敗    → event ごとの result / error を出す。
                   ★boundary_failures は run に無いので「run からは取れない」と書く
⑥ 次に誰が何を  → 「DW task が無いため、次の操作は記録されていない」
```
**★禁止: 空欄にしない。** **★禁止: 「DW を通っていない」を欠陥のように出さない**（Taka 正典「会話で済むものなら会話で終わる」＝正常な形である）。
**★禁止: 埋めるために新しい状態や推測を作らない。**

> **★これは Taka の完了像⑤「欠損・失敗・未確認が見える」を、①〜⑥ 全部に当てる形である。**
> **★「見えない」を「見える」にする。** **「無い」を「空欄」にしない。**

## 4. ★条件（前回と同じ・変えない）
1. **新しい endpoint を作らない。** 2. **既存の画面を壊さない。** 3. **Event Trace を精密にしない。**
4. **SPEC の外に広げない**（`/` を直さない）。 5. **投入しない**（今日の run が4つ在る）。

## 5. ★併せて（前回出した2件・再掲）
| # | 何を |
|---|---|
| 1 | **実行構造の資料が古い。** **次の BUILT と同時に同期する** |
| 2 | **到達性21件が未着。** **★やるか、やらないと書いて閉じるか、どちらかにする。宙に浮かせない** |

---
*MGR。★受領=新しい endpoint は要らない（run の event に `task_id` と発話 id が既に入っており、既存の2つの口を `run_id` を鍵にして繋ぐだけでよい）。**MGR の見立ては当たったが、それは成果ではない。成果は CC-α が4つの run すべてを打ち切りなしで引いたことである。** ★裁定=`/command` が `ETR-` を受け付ける（新しい endpoint を作らない）／`task_id` が run に在れば今の画面に繋ぐ（既存の形を消さず足すだけ）／**`task_id` が run に無ければ `/api/state` を叩かず、エラーを画面に出さない**（`no such task` は「壊れている」ではなく「DW を通っていない」である）。★埋まらない項目の扱いが本件で最も重要=CC-α が「②④⑤⑥ が埋まらない理由」を名指ししたのは正しいので**埋めず、代わりに「無い」と出す**（②は「DW を通っていない。DW 上の現在地は無い」／④は id を出し EGL の `source_refs`/`claims` が無いなら「無い」と書く／⑤は event ごとの `result`/`error` を出し `boundary_failures` は「run からは取れない」と書く／⑥は「DW task が無いため次の操作は記録されていない」）。**禁止=空欄にしない／「DW を通っていない」を欠陥のように出さない（Taka 正典「会話で済むものなら会話で終わる」＝正常な形）／埋めるために新しい状態や推測を作らない。** これは Taka の完了像⑤を①〜⑥ 全部に当てる形であり、**「見えない」を「見える」にし、「無い」を「空欄」にしない**。★条件は前回と同じ（新しい endpoint を作らない／既存を壊さない／Event Trace を精密にしない／SPEC の外に広げない／投入しない）。★併せて=実行構造の資料を次の BUILT と同時に同期／**到達性21件はやるか「やらない」と書いて閉じるかどちらかにし宙に浮かせない**。*
