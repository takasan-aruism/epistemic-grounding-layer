# 【D-208】`EVO-0030` を受領 ／ ★行き先は「判定」だけでなく「★どの入口から来たか」でも決める

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-08-01 13:1x / TYPE=STATUS
- **開発者規律 確認済（版: v1.1）** ／ **受領**: `CC_IMPL_2026-08-01_EVO0030_DESTINATION_BUILT.md`（BUILT）／ `CC_DESIGN_2026-08-01_EVO0030_AUDIT_I_PICKED_AN_UNMEASURABLE_TASK.md`（監査）

## 0. ★先に訂正（★私が Taka に伝えた前提が古かった）
```
★Taka:「★実装が動いていないので指示を出して」
★★実測（★指示を書く前に確かめた）: ★★動いていた。
   ★`CC_DESIGN_..._EVO0030_UPPER_REVIEW_NEEDS_A_DESTINATION.md`（SPEC）
   ★`CC_IMPL_..._EVO0030_DESTINATION_BUILT.md`（BUILT・★`workcell.py +11/-1`・`webui.py +1`）
   ★`CC_DESIGN_..._EVO0030_AUDIT_I_PICKED_AN_UNMEASURABLE_TASK.md`（★独立監査）
★★★∴ ★「動いていない」は ★私の報告の遅れである。★止まっていたのではない。
```

## 1. ★★Taka 追加指示（★逐語）— ★採る
> ### **「★UPPER_REVIEW の行き先は、★判定（PASS/FAIL）だけで決めず、★『どちらの入口から来たか』も見て決めること」**
```
★設計へ: ★`EVO-0030` の分岐に ★★入口の state を条件に足すこと。
   ★★例（★本数と条件は設計が決める。★私は指定しない）:
      ★`READY_FOR_UPPER_REVIEW` から来た `FAIL` と ★`JUDGE_REQUIRED` から来た `FAIL` は ★同じ行き先か。
      ★同じでよいなら「★同じでよい」と ★理由を書く。★違うなら ★分ける。
★★★理由（★Taka の言い方を私が補う）: ★判定は「★中身の良し悪し」しか表さない。
   ★★「★どこで詰まって上がってきたか」を捨てると、★同じ場所へ ★何度も戻す形になりうる。
★★★★受入に1つ足す: ★★(e) ★入口が違えば ★行き先が変わることを ★実測で示す（★1件では足りない）
```

## 2. ★`EVO-0030` の受領（★1つ閉じた。★1つは測れない）
```
★受入(c) ★★○ — ★`GET /api/state` に ★`upper_reviews` 欄が在り、★中身が逐語で読める（★3件）。
   ★★★∴ ★★「書けるが読めない」の ★同型5件目が ★閉じた。★これは ★本物の前進である。
★★設計の【未確認】も1つ解けた: ★仮の記録の `verdict` は ★`PLACEHOLDER` であって ★`PASS` ではない。
   ★★＝ ★仮の合格が本物として残る事故は ★起きていない。★IMPL の書き方が正しかった。★そう書く。
★受入(a)（FAIL で作り直しへ動く）は ★★「×」ではなく ★★この task では測れない（★既存の昇格規則のため）。
   ★★∴ ★`EVO-0030` は ★まだ `DONE` にしない。★§1 の (e) と合わせて ★測れる形で確かめること。
```

## 3. ★IMPL の問い（★「worker に書かせなかった」）への裁定 — ★差し戻さない
```
★IMPL: ★`build_planner.py:59-60` の `PROD_REPO_ROOTS` に `dev-workcell` が入っている
   ∴ ★worker は ★決定論で ★書けない。★投げれば task が1件 増えるだけ。★∴ 試さなかった。
★★★裁定: ★★採る。★差し戻さない。★★★理由: ★決定論の拒否が ★文書化されている以上、
   ★「試して確かめる」の価値より ★「詰まりを1件 増やす」費用が ★大きい。
★★★★★ただし ★これは ★恒久の構造制約である。★毎回 議論しない形にする:
   ★★★「★本番 repo（`egl` / `dev-workcell` / `twoder` …）への配線は ★worker には書かせられない。
      ★★∴ ★Claude が書く。★★行数を必ず報告し、★2DER の実績に数えない」——★これを既定とする。
   ★★★★★これを解くのは ★C-3（`EVO-0023`）の側の仕事である。★ここで解こうとしない。
```

## 4. ★設計への注記（★SPEC の行番号）
```
★SPEC が名指した `webui.py:221` は ★実際には `claude_packet()` の中だった（★IMPL が実測して訂正）。
★★行番号で名指すときは ★関数名も併記すること（★行はすぐずれる）。★責めない。★形だけ直す。
```

---
**決めたこと**: **①「実装が動いていない」は私の報告の遅れで、実測では SPEC・BUILT・独立監査の3本が揃っていた ②Taka 追加指示を採る——UPPER_REVIEW の行き先は判定だけでなく入口の state も見て決める。本数と条件は設計が決め、同じでよいなら理由を書く。受入に「入口が違えば行き先が変わることを実測で示す」を1つ足す ③受入(c) は ○。`upper_reviews` 欄が front door から逐語で読め、「書けるが読めない」の同型5件目が閉じた。仮記録の verdict は `PLACEHOLDER` で `PASS` ではなく、設計の懸念した事故は起きていない ④受入(a) は × ではなく「この task では測れない」なので `EVO-0030` はまだ `DONE` にしない ⑤IMPL が worker に書かせなかった判断は採り、差し戻さない。本番 repo への配線は worker には書かせられないという恒久の構造制約を既定とし、Claude が書いて行数を報告し 2DER の実績に数えない。解くのは C-3 の側の仕事である ⑥行番号で名指すときは関数名も併記する。**
