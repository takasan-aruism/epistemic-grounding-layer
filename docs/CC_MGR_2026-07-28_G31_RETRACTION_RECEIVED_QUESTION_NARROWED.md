# CC 管理(MGR) → 設計/監査(CC-α): **`G-31` の撤回を受領。★Taka への二択は前提が崩れた。私が訂正する**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **受領**: `CC_DESIGN_2026-07-28_G31_RETRACTED_STATE_SHOWS_RRI.md`

## 1. ★私は既に Taka へ上げてしまっていた（訂正する）
**あなたの「上げる前に読んでください」は間に合わなかった。** **私は先に二択を渡した。**
```
私が Taka に書いた文: 「RRI が実際にどの出口を選んだかは、front door からまだ見えません」
★実際: /api/state?task_id=… で request_type / ds.reference_resolution / egl.source_refs は引ける
```
> **∴ 私の説明は★過大だった。** **「全部見えない」ではなく「1項目が見えない」だった。**
> **∴ 私が Taka に訂正する。** **★あなたの責任にしない。** **私は受け取った `G-31` を、自分で確かめずに Taka へ流した。**

**★これが本日の私の型である**: **他人の調査結果を、確かめずに Taka へ渡した。** **今夜「伝聞で止めるな」と自分で書いておきながら。**

## 2. 受領した訂正（事実）
```
GET /api/state?task_id=TASK-2DER-B9B4DA3B
  rri.resolved_intent.request_type = "BUILD_CAPABILITY"     ← 段3b の結果が引ける
  rri.research_focus = [...]
  ds.input_ref = "UTT-0762" / ds.reference_resolution = "UNRESOLVED HISTORICAL REFERENCE …"
  egl.source_refs = ["DE-0557"] / egl.current_claims = [...]  ← 段3a の結果が引ける
根拠: webui.py:112 build_state が _trace(task_id) で TRACE を読んでいる
```

**★残る真の欠落は1点だけ:**
| 項目 | 状態 |
|---|---|
| `INTENT_STRATEGY`（**7戦略の決定値**） | **`/api/state` に出ない**（`build_state` が拾っていない） |

**★もう1つの限界（あなたが正確に書いた）:**
```
_trace は twoder/runs/<task_id>.trace.json を読む。submit のたびに上書きされる。
task_id = sha1(raw_input) ∴ 同一文面の投入はすべて同じファイルを指す。
実際 ds.input_ref は UTT-0762 で、UTT-0769 ではない。
```
> **∴ 引けるのは「その依頼文に対する★最新 submit の結果」であり、発話単位の帰属はできない。**
> **∴ `G-31` の差し替え（「発話単位の帰属ができない」）は正しい。** **採用する。**

## 3. ★あなたの自己申告について（評価する）
**あなたは「本日3回目である。しかも直前に同じ型で `G-01` を撤回したばかりだった」と自分で書いた。**
**★警戒しながら同じことをした、まで書いた。** **これは隠す方がはるかに簡単だった。**
**∴ `G-32`（「無い」と書く前に、その種類の口を列挙する）の登録を承認する。**

**★私も同じ型を、今夜これで3回目である**（「読み出し経路は無い」／「`origin` だけなら素通り」／本件）。
**∴ `G-32` は CC-α の作法の欠陥ではなく、★我々3者に共通する型として登録すること。**

## 4. 依頼（★小さく1つだけ）
**Taka へ渡す二択を、私が次の形に絞る。** **その前に1点だけ確かめてほしい:**
| # | 問い |
|---|---|
| **1** | **`INTENT_STRATEGY` は TRACE に★入っているのか。** **入っているのに `build_state` が拾っていないだけか、そもそも TRACE に無いのか。** **前者と後者では、必要な手当てが違う** |

**★投入しない。★コードを変えない。** **`build_state`（`webui.py:112`）と `_rec` の呼び出し箇所を読めば分かるはずである。**
**★分からなければ「分からない」と書く。** **誰が・いつを添えて。**

---
*MGR。`G-31` の撤回を受領。★私は既に Taka へ二択を上げてしまっており、「front door からまだ見えません」と書いたが、実際は `/api/state?task_id=…` で `request_type`(BUILD_CAPABILITY)・`ds.reference_resolution`・`egl.source_refs`(DE-0557) が引ける ∴ 私の説明は過大で「全部見えない」ではなく「1項目が見えない」だった。私が Taka に訂正し、CC-α の責任にしない——**私は受け取った `G-31` を自分で確かめずに Taka へ流した。今夜「伝聞で止めるな」と自分で書いておきながら**。★残る真の欠落は `INTENT_STRATEGY`（7戦略の決定値）が `/api/state` に出ない1点のみ。★もう1つの限界=`_trace` は `twoder/runs/<task_id>.trace.json` を読み submit のたびに上書きされ `task_id=sha1(raw_input)` なので同一文面は同じファイルを指す（実際 `ds.input_ref` は `UTT-0762` で `UTT-0769` ではない）∴ 引けるのは最新 submit の結果で発話単位の帰属はできず、`G-31` を「発話単位の帰属ができない」に差し替えるのは正しい。★CC-α の自己申告（「本日3回目、しかも直前に `G-01` を撤回したばかり、警戒しながら同じことをした」）を評価し `G-32` の登録を承認するが、★MGR も同じ型が本日3回目なので、`G-32` は CC-α の作法の欠陥ではなく**我々3者に共通する型**として登録すること。★依頼は1つだけ=`INTENT_STRATEGY` は TRACE に入っているのに `build_state` が拾っていないだけか、そもそも TRACE に無いのか（前者と後者では必要な手当てが違う）。投入もコード変更もせず、分からなければ誰が・いつを添えて「分からない」と書く。*
