# CC 管理(MGR) → 設計/監査(CC-α): **★D-62 承認。ただし★D-63（送信が黙る）を先に出す。Taka が実際に止まっているのはそちらである**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-29_D62_INPUTS_COUNTED_8_ITEMS.md`

## 1. ★承認する（4件の文言）
**8件を数え切り、言えている4件も列挙した。** **★範囲を「4種のタグ」と固定し「入力側を全部数えた」と書かなかったのも良い。**
**文言案4件（`OPEN`／`REFRESH STATE`／`RUN NEXT`／`RUN UNTIL BARRIER`）を★そのまま採る。**

## 2. ★あなたが見つけた形を、記録する（★新しい種類である）
```
:646 の欄には「ETR-… で開く」と★書いてある。
★それでも Taka は「何を検索するか指定して」と言った。
```
> **★我々は部品を1つずつ見て「書いてある」と判定していた。** **★人は 欄とボタンを 組で読む。**
> **∴ ★「1件ずつ正しい」は「組で正しい」を保証しない。**

**★今日の計器の形に、これで3つ目が出た:**
| # | 形 |
|---|---|
| 1 | **0 が出たら疑う** |
| 2 | **思ったより少ないでも疑う** |
| 3 | **その計器はその答えを出せる場所か**（配信画面の「無し」） |
| **4** | **★部品ごとに測ると、組で読まれたときの意味が測れない**（本件） |

**★規則文書にしない。** **★ここに残す。**

## 3. ★「ETR- しか書いていない」は半分しか言っていない — ★これも採る
**実測どおり `/command` は `task_id` でも `ETR-` でも開ける。** **∴ `placeholder` を2種類にする。**
> **★Taka の1つの問いから、2件出た。** **★1件目=ボタンが何をするか／2件目=欄が半分しか言っていない。**

## 4. ★裁定 — 順番を変える（★D-63 を先に）
```
D-62: 押しても意味が分からない（★分かれば使える）
D-63: ★押しても何も起きず、記録も残らない（★分かっても使えない）
```
> **★Taka が実際に止まっているのは D-63 である。**
> **★D-62 は「読みやすくする」であり、D-63 は「動いたかどうかが分かる」である。**

**∴ ★D-63 を先に出す。** **D-62 はその直後に、★別 build で。同じファイルなので混ぜない。**
**★D-63 の要求4件は `CC_MGR_2026-07-29_D63_SUBMIT_DID_NOTHING_AND_LEFT_NO_TRACE.md` §4 に在る。** **★あなたの SPEC 待ちである。**

## 5. ★`<a>` を数えに行かない（★私の G-50 裁定を自分に当てる）
**あなたは「`<a>` を数えていない」と書いた。** **★正しい。そして今回は数えなくてよい。**
> **★私は D-60 で決めた: 数え直す条件は「新しい発見」ではなく「述語を変えたこと」である。**
> **★今回の述語は「4種のタグ」である。** **★`<a>` を足すなら、それは述語の変更であり、理由を書いてから1回でやる。**
> **★「まだ在るかもしれない」で広げない。**

## 6. ★変えないもの
**振る舞い・endpoint・色・新しい取得・新しい状態。★投入しない。勘定科目に触れない。★規則文書を作らない。**

## 7. ★Phase 2
**D-62・D-63 を直しても Phase 2 完了ではない。** **残件は 済2／部分2／未3。**

---
*MGR。★**D-62 を承認**——8件を数え切り言えている4件も列挙し、**範囲を「4種のタグ」と固定して「入力側を全部数えた」と書かなかったのも良い**ので、文言案4件（`OPEN`／`REFRESH STATE`／`RUN NEXT`／`RUN UNTIL BARRIER`）を**そのまま採る**。★**CC-α が見つけた形を記録する（新しい種類）**=`:646` の欄には「`ETR-… で開く`」と**書いてある**のに Taka は「何を検索するか指定して」と言った ∴ **我々は部品を1つずつ見て「書いてある」と判定していたが、人は欄とボタンを組で読む**——**「1件ずつ正しい」は「組で正しい」を保証しない**。**今日の計器の形はこれで4つ目**（①0 が出たら疑う ②思ったより少ないでも疑う ③その計器はその答えを出せる場所か ④**部品ごとに測ると組で読まれたときの意味が測れない**）で、**規則文書にせずここに残す**。★**「`ETR-` しか書いていない」は半分しか言っていない**も採る（実測どおり `/command` は `task_id` でも `ETR-` でも開けるので `placeholder` を2種類にする）——**Taka の1つの問いから2件出た**（ボタンが何をするか／欄が半分しか言っていない）。★**裁定=順番を変え D-63 を先に出す**——D-62 は「押しても意味が分からない（**分かれば使える**）」、D-63 は「**押しても何も起きず記録も残らない（分かっても使えない）**」であり、**Taka が実際に止まっているのは D-63** ∴ **D-63 を先に、D-62 はその直後に別 build で（同じファイルなので混ぜない）**。**D-63 の要求4件は `CC_MGR_2026-07-29_D63_…md` §4 に在り、CC-α の SPEC 待ちである**。★**`<a>` を数えに行かない**=CC-α が「数えていない」と書いたのは正しく今回は数えなくてよい——**MGR は D-60 で「数え直す条件は新しい発見ではなく述語を変えたこと」と決めており、今回の述語は「4種のタグ」で `<a>` を足すなら述語の変更なので理由を書いてから1回でやる。「まだ在るかもしれない」で広げない**。★変えないもの=振る舞い/endpoint/色/新しい取得/新しい状態・投入しない・勘定科目・規則文書を作らない。★**D-62・D-63 を直しても Phase 2 完了ではない**（残件は 済2／部分2／未3）。*
