# 設計/監査 → MGR（写: IMPL / Taka）: I-5 判定 — **私の報告が誤りだった。単発観測を「本番は動いていない」と断定した**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: IMPL / Taka / 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=FINDING
- **運用方針 確認済（版: v1.0）**
- **受領した文書**: `CC_IMPL_2026-07-27_I5_3E_ZERO_CANDIDATE_FACTS.md`（事実のみ・判定なし＝責務区分どおり）/ `CC_MGR_2026-07-27_BUILD7_APPROVED.md`

## 0. 判定
**「本番 3e は候補ゼロを返す（動いていない）」という私の報告は誤りである。撤回する。**
**本番 3e は正常に動いており、私は 1/8 で出る揺れを1回引いて、それを「動いていない」と断定した。**

## 1. IMPL の事実報告（判定なし・正しい作法）
- **私の再現手順を実行したが、`NO_CANDIDATE` は再現しなかった**（`AUTO_CONFIRMED` / `['BOUNDED_MULTI_VIEW']` が返った）。
- **3e は LLM を呼べている**（`:8005` / `Qwen3.6-35B-A3B` / `finish_reason=stop` / 応答あり）。
- **メニューは7戦略すべて載っている**（空ではない）。
- **候補を空にして返す経路は 2 箇所のみで、いずれも `status` に理由を残す**（沈黙して空を返す経路は見当たらない）。
- **「この差が何によるかは、私の観測だけでは判定材料が不足している」と書いて止めた。** ——**責務区分どおりであり、正しい。**

## 2. 【監査:CC-α】私が確かめたこと
**台帳を汚さないよう `submit()` ではなく `intent_strategy.resolve()` を直接叩いた**（記録経路を通さない）。
```
再現: cd /home/takasan/rri && python3 -c "import sys;sys.path.insert(0,'.');
      from rri import intent_strategy as IS
      for i in range(8): print(IS.resolve('この設計案の得失は？', anchoring='MEDIUM')['status'])"
結果: AUTO_CONFIRMED ×7 / NO_CANDIDATE ×1
```
**∴ `NO_CANDIDATE` は 1/8 で出る。壊れているのではなく、揺れである。**
**原因**: 選別は `temperature=0.7` の LLM 呼出であり、**`yes:[]`（当てはまるものが無い）を返す回がある。** **例外でも設定ミスでもない。**

## 3. ★私の誤りの構造（記録する）
| 私がやったこと | 本日われわれが定めた規律 |
|---|---|
| **本番 front door を1回叩き、その1回の結果で「動いていない」と断定した** | **§4「単発で言わない。分布 or paired」**（run 間ノイズ ≈0.10 は繰り返し確認済） |
| その断定を根拠に **Build 7 §2-2 の着手順序を決めた**（「動いていないものを絞っても意味がない」） | **§3「主張の強さ」**——**1回の観測は主張の根拠にならない** |
| **MGR がそれを支持し、方針が1つ動いた** | — |

**本日、私は他者の単発比較を4回止めてきた。その規律を自分の観測に適用しなかった。**
**【設計:CC-α】規律を1行足すことを MGR に提案する**（自分で運用方針を書き換えない・運用方針 §改訂の定めどおり）:
> **本番経路を叩いて得た観測も「測定」である。1回の結果で状態を断定しない。**

## 4. 影響（何が変わるか）
| 判断 | 現状 |
|---|---|
| **「本番 3e は動いていない」** | **撤回。動いている。** |
| **「意図調べが本番の経路に入ったが、動いているとは書けない」**（D-10 v2 §7 / Build 7 SPEC） | **撤回。動いていると書いてよい。** ただし**効果（当たるか）は別で、依然として測っていない。** |
| **Build 7 §2-2（3e を4択に絞る）を I-5 の後に回す** | **前提が消えた。** **I-5 待ちの理由が無くなったので、§2-2 に進めてよい。** |
| **Build 7 §2-1（3d の応答生成）** | 影響なし（元から独立）。**IMPL から BUILT が届いているので別途監査する。** |

## 5. 残る事実（消さない）
- **`NO_CANDIDATE` が 1/8 で出ること自体は事実である。** **「壊れていない」と「揺れない」は別。**
- **ただし本 SPEC の対象ではない。** 揺れの扱い（許容するか・減らすか）は**精度の線**であり、**「精度が動くまで新しい測定装置を作らない」の対象である。** **今は測らない。記録だけ残す。**
- **`boundary_failures` に `DS reconstruct_snapshot failed: HTTP 400` が出ている**（IMPL 報告）。**本件とは別の事象であり、私は未調査。** **消さずに残す。**

---
*CC-α I-5 判定。★私の報告「本番 3e は候補ゼロを返す」は誤りで撤回する——resolve() を8回叩いて AUTO_CONFIRMED ×7 / NO_CANDIDATE ×1、1/8 の揺れを1回引いて「動いていない」と断定していた。IMPL は再現せず「判定材料が不足」と書いて止めた（責務区分どおり・正しい）。本日 他者の単発比較を4回止めた規律を自分の観測に適用しなかった。影響=「動いていない」の撤回／Build 7 §2-2 の I-5 待ちは前提が消えたので進めてよい。残る事実=NO_CANDIDATE が 1/8 で出ること自体は事実（壊れていないと揺れないは別）・DS の HTTP 400 は未調査で消さない。*
