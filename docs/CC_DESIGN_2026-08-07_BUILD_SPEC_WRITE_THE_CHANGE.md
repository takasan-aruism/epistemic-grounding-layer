# 【BUILD SPEC】`記録する側` — **★核0・★呼ばれていない所を 呼ぶ（★1本だけ）**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 監視 / 発: 設計/監査(CC-α) / 2026-08-07 23:5x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: ★v1.18）** ／ 台帳: `ITEM-2DER-EVO-0035` ／ 効く先＝「戻せる（先）」
- **★★核0 ／ ★新台帳0 ／ ★新しい欄0 ／ ★Claude の配線 ★上限10行**

---

## 1. ★★IMPL の指摘を ★私が 確かめた（★伝聞に 乗らない）

```
★① `record_change` の 呼び手 = ★★試験1件だけ（★全数 grep・逐語）:
     twoder/regression/test_artifact_registry.py:97  ← ★★本番からは 1度も 呼ばれていない
★② etrace の event に `trace_id` が 在るか（★実測・TASK-2DER-D28FF7E4 の 15件）:
     ★★trace_id が None = ★★15/15 ／ ★run_id 有 = ★15/15
★★∴ ★IMPL の指摘は ★2つとも 正しい。★私の「2段で引けば成立する」は ★★成立しません。
★★★∴ ★MGR の裁定（★`trace_id` に `run_id` を入れる規約）を ★採ります。
```

## 2. ★★核が 0 である 理由（★作らない物を 先に 書く）

```
★足りないのは ★★『書く行為』であって ★判定ではありません。
★判定の核は ★★もう在ります = ★`revert_scope`（★合格済・sha 51867b4d）。
★★∴ ★worker に 書かせる物が ありません。★でっち上げない（★本日 2回 そうし、★MGR が 採りました）。
★★★本件は ★★配線だけの1件です。
```

## 3. ★★配線（★上限10行）

```
★成果物を1本 置く その作業の中で、★次を 呼ぶ:
  (1) 置く前 = ★`before_commit` に ★その repo の 現在の HEAD を 入れる
  (2) ★`record_change(trace_id=★その走行の run_id（ETR-…）, de_id=…,
                      affected_artifact_ids=[置いた物の artifact_id], …,
                      before_commit=(1))`
  (3) commit の 後 = ★`update_change_after_commit(change_id, after_commit=新しい HEAD)`
★★★遡って 過去に 書かない（★本日の (A) と 同じ作法）。★★1本だけ（★Taka「全件直す、的な動きは不要」）。
```

## 4. ★★残る穴を 名前で 置く（★本件では 直さない）

```
★★★成果物を ★置いているのは ★★Claude(IMPL) であって ★2DER では ありません。
   ∴ ★この記録も ★★Claude が 呼ぶことに なります。
★★∴ ★『2DER 側で 完結する』には ★★置く行為そのものを 2DER の中へ 入れる必要が 在ります。
★★★本件では やりません（★Taka「戻せるのが先」・★順序）。★★但し ★満たしたとは 書きません。
★★★★行数は 報告し、★★2DER の実績に 数えません（★常設の作法）。
```

## 5. ★★受入（★口・欄・★id を 載せる物として）

```
★(1) ★口 = ★変更の記録 ／ ★欄 = `trace_id` / `before_commit` / `after_commit` / `affected_artifact_ids`
     ★id = ★★その走行の `run_id`（★ETR-…・★報告に 書く）
     ★読める物 = ★★『どの走行が どの commit から どの commit まで 何を変えたか』
★(2) ★★`revert_scope(その記録, その run_id)` の `complete` が ★★True
     ―― ★★合格済の核を そのまま 使う（★新しく作らない）
★★(3) ★★陰性 = ★★過去の記録（★196件・2026-07-22 以前）の `complete` が ★False の まま
     ―― ★★True に なったら ★遡って 書いています。
★(4) ★★引けなければ ★『引けなかった』と 書く（★0件 と 書かない・★規律 v1.17）
★(5) ★Claude の配線行数（★上限10行・★超えたら そう返す）
★(6) ★戻せる ／ ★(7) ★61本を走らせない ／ ★(8) ★commit は MGR
```

## 6. ★★私が 言っていないこと

```
★『これで 戻せるようになった』―― ★★1本だけです。★過去の196件は 戻せません。
★『2DER が 記録する』―― ★★Claude が 呼びます（★§4）。
★『task_id で 引ける』―― ★★引けません。★鍵は `run_id`（★§1 の実測）。
★『版の管理』―― ★★やりません（★Taka「明細管理と一緒・後の宿題」）。
```
