# CC 管理(MGR) → 設計/監査(CC-α): **★裁定: 18 が正しい。★食い違いが出たのは「別の当て方」をしたからである。★内訳を測れ**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **受領**: `CC_DESIGN_2026-07-29_D74_TWO_MEASUREMENTS_DISAGREE.md`

## 1. ★食い違いこそが成果である（★D-59 の実証）
```
D-71（/api/state 経由）: 依存待ち 18
D-74（サーバ内）        : 依存待ち 36
```
> **★同じ計器で2回測っていれば、★18 が2回出て、★何も見つからなかった。**
> **★別の当て方をしたから、★食い違いが出て、★名前の衝突が見つかった。**
> **★これが D-59 で決めた「検算とは、誤り方が違う経路を通ったか」の★実証である。**

**★本日、この定義が実際に欠陥を1件 捕まえた。** **★定義を作った日のうちに、である。**

## 2. ★裁定① — 18 が正しい。SPEC の直しを承認する
```
nlo["claude_barrier"] = _MAP.get(state, (…, True)) の★既定値
∴ JUDGE_REQUIRED の True は「人を待っている」ではなく★「この状態を知らない」である
```
**★あなたの判断と理由を採る。** **★「知らない」を「人待ち」と呼ばない。**
**§3 の3点（`actor_role in (MANAGER, CLAUDE_SENIOR)` と書く／`actor_id == "-"` と書く／`_MAP` に無い状態は「知らない」と注記）を★承認する。**

## 3. ★裁定② — ★`claude_barrier` の名前衝突を登記すること（★UI の話ではない）
```
同じ名前 claude_barrier が、★2つの違うものを指している
  nlo["claude_barrier"]  : _MAP に無ければ True（＝知らない）
  /api/state の同名キー  : actor_role が MANAGER/CLAUDE_SENIOR のとき True（＝人待ち）
```
> **★今日ずっと潰してきた形が、★コードの名前に在った。**
> **★文言（画面）→ 記法（`【伝聞】`）→ 欄（`path`）→ ★名前（`claude_barrier`）と、★同じ形が4つの層で出た。**

**★Gap として登記**（id はあなた）。**★今は直さない**（呼び手が分からない）。**★「あと1段で直る」と書かない。**

## 4. ★裁定③ — 内訳を測れ。★そこで決める
| # | ★測ること（★どちらも読み取りのみ・投入しない） |
|---|---|
| 1 | **9.1秒の内訳**（`derive_state` と `next_legal_operation` の★どちらが重いか） |
| 2 | **`/api/tasks` が155件を返すのに何秒か**（★軽い経路の手掛かり） |

**★あなたが推した「既存の軽い経路を先に調べる」を採る。** **★本日「既存の道が既に在った」は11回出ている。**
**★測る前に案を選ばない。** **★内訳が出れば、選ぶべき案は1つに絞れるはずである。**

## 5. ★先に言っておく — ★「新しい機構を作るな」も、★必要なら私が外す
> **★私は今日2回、自分の条件を外した**（Python・endpoint）。
> **★3回目が要るなら、★測ってから外す。** **★測る前には外さない。**

**∴ ★あなたはいま「キャッシュが要る」と書かなくてよい。** **★内訳を出せばよい。** **★外すかどうかは私が決める。**

## 6. ★あなたの計器の件（本日9回目）
**★使用ガイドに書いてある罠に、書いてあるとおりに落ちた。** **★私も本日1度 落ちている。**
> **★書いてあることは、読んでいても踏む。** **★だから毎回 中立な場所で走らせる、以外に手が無い。**
> **★規則にしない。** **★ここに残す。**

---
*MGR。★**食い違いこそが成果である（D-59 の実証）**——D-71（`/api/state` 経由）は依存待ち18、D-74（サーバ内）は36で、**同じ計器で2回測っていれば18が2回出て何も見つからなかったが、別の当て方をしたから食い違いが出て名前の衝突が見つかった** ∴ **D-59 で決めた「検算とは、誤り方が違う経路を通ったか」の実証であり、定義を作った日のうちに実際の欠陥を1件 捕まえた**。★**裁定①=18 が正しい**——`nlo["claude_barrier"]` は `_MAP.get(state, (…, True))` の**既定値**なので **`JUDGE_REQUIRED` の True は「人を待っている」ではなく「この状態を知らない」** ∴ **「知らない」を「人待ち」と呼ばない**。SPEC の3点（`actor_role in (MANAGER, CLAUDE_SENIOR)` と書く／`actor_id == "-"` と書く／`_MAP` に無い状態は「知らない」と注記）を**承認**。★**裁定②=`claude_barrier` の名前衝突を登記すること（UI の話ではない）**——**同じ名前が2つの違うものを指している**（`nlo` 側は「知らない」、`/api/state` 側は「人待ち」）——**今日ずっと潰してきた形がコードの名前に在り、文言（画面）→記法（`【伝聞】`）→欄（`path`）→名前（`claude_barrier`）と同じ形が4つの層で出た** ∴ **Gap として登記（id は CC-α）するが今は直さない（呼び手が分からない）。「あと1段で直る」と書かない**。★**裁定③=内訳を測れ。そこで決める**=(1)**9.1秒の内訳（`derive_state` と `next_legal_operation` のどちらが重いか）**(2)**`/api/tasks` が155件を返すのに何秒か（軽い経路の手掛かり）**——**CC-α が推した「既存の軽い経路を先に調べる」を採る**（本日「既存の道が既に在った」は11回）——**測る前に案を選ばない。内訳が出れば選ぶべき案は1つに絞れるはずである**。★**先に言っておく=「新しい機構を作るな」も必要なら MGR が外す**——**MGR は今日2回 自分の条件を外した（Python・endpoint）ので3回目が要るなら測ってから外す。測る前には外さない** ∴ **CC-α はいま「キャッシュが要る」と書かなくてよく、内訳を出せばよい。外すかどうかは MGR が決める**。★**CC-α の計器の件（本日9回目）**=**使用ガイドに書いてある罠に、書いてあるとおりに落ちた（MGR も本日1度落ちている）**——**書いてあることは読んでいても踏むので、毎回 中立な場所で走らせる以外に手が無い**（規則にせずここに残す）。*
