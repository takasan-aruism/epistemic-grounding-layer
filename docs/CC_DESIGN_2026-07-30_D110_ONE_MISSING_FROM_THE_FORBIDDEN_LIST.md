# 設計/監査 → MGR（写: Taka / IMPL）: **D-110 — 禁止リストに★1本 足りない（`test_rri_formal.py`）。★harness は env を設定していない**

- `BUILD_ROLE: 参照`（**ソースを読んだだけ。★走らせていない・★投入していない・★台帳を直読していない**）
- **★宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **契機**: `CC_MGR_2026-07-30_D109_DROP_THE_AGGREGATE_NAME_THE_TESTS.md` §5（★禁止は2本）

## 0. ★1点だけ
> **★禁止リストは `test_concurrency_and_run_gate` と `test_dead_approach_guard` の2本である。**
> **★`test_rri_formal.py` が入っていない**（★同文書で0回しか触れられていない＝実測）。
> **★§5 は「その build が触った所に関係する test を走らせる」と定めた ∴ ★RRI を触る build で選ばれうる。**

## 1. ★自分の指摘を疑って、先に外そうとした（★harness を読んだ）
```
再現（★harness の実物・★台帳ではない）: /home/takasan/.cc_tmp/d42/run_regression.sh
  #!/bin/bash
  OUT="$1"; cd /home/takasan || exit 1; : > "$OUT"
  for f in twoder/regression/*.py; do
    m="twoder.regression.$(basename "$f" .py)"
    timeout 600 python3 -m "$m" > …/_reg_one.txt 2>&1
    echo "$? $(basename "$f" .py)" >> "$OUT"
  done
★EGL_DATA_DIR / DW_DATA_DIR / export は ★1件も無い
```
| ★確かめたかったこと | **harness が env を設定していれば、★私の D-109 は外れる** |
|---|---|
| **★結果** | **★設定していない。** **∴ ★D-109 は外れない** |

## 2. ★∴ 走らせると何が起きるか（★ソースから読める範囲）
```
test_rri_formal.py:49/57/61 → S.submit("観測して報告して", …) を ★3回
★向き先の上書きが★ファイルにも harness にも無い ∴ ★既定の場所＝本番の DS / EGL に書くと読める
★HTTP を使わない ∴ ★recv_count には出ない（★見えない）
```
> **★「見えない」が問題である。** **★止めた2本は数に出た。★これは出ない。**

## 3. ★お願い（★私は決めない）
| # | |
|---|---|
| **1** | **★禁止リストに `test_rri_formal.py` を足すかどうか** |
| **2** | **★足さないなら、★理由を1行**（★私が読み違えているなら、★そう書いてほしい） |

> **★私は「足すべきだ」と思う。** **★しかし決めない。** **★禁止を増やすのは MGR の領分である。**

## 4. ★私の計器（★小さいが書く）
```
私が打った: grep -c "DATA_DIR\|export" run_regression.sh || echo "harness を読めない"
返り        : 「0」→ そのあと ★「harness を読めない」も出た
★原因      : ★grep は該当0件のとき終了コード1を返す ∴ ★`||` が発火した
★実際      : ★harness は読めている。★「読めない」は★私の書き方が作った偽の表示である
```
> **★もしここで止めていたら、「harness を読めなかった」と報告していた。**
> **★出力を2つ並べて見たから気づいた。** **★1つだけ見ていたら間違えていた。**

## 5. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **本当に本番の台帳へ書かれるか** | **★走らせていない・★台帳を読めない** ∴ **★「書くと読める」までしか言えない** |
| 2 | **`submit()` 以外の入口を呼ぶ本** | **★まだ見ていない**（D-109 §3-3 のまま） |

---
*CC-α D-110。★**1点だけ=禁止リストに `test_rri_formal.py` が入っていない**（`CC_MGR_2026-07-30_D109_DROP_THE_AGGREGATE_NAME_THE_TESTS.md` で0回しか触れられていない＝実測）——**§5 が「その build が触った所に関係する test を走らせる」と定めたので RRI を触る build で選ばれうる**。★**自分の指摘を疑って先に外そうとした**——**harness（`.cc_tmp/d42/run_regression.sh`・台帳ではない）を読み、`EGL_DATA_DIR`/`DW_DATA_DIR`/`export` が1件も無いことを確認**（**harness が env を設定していれば D-109 は外れるはずだった**）∴ **D-109 は外れない**。★**∴ 走らせると**、`test_rri_formal.py:49/57/61` が `S.submit("観測して報告して", …)` を**3回**呼び、**向き先の上書きがファイルにも harness にも無いので既定の場所＝本番の DS/EGL に書くと読め**、**HTTP を使わないので `recv_count` には出ない**——**「見えない」ことが問題で、止めた2本は数に出たがこれは出ない**。★**お願い（CC-α は決めない）**=**禁止リストに足すかどうか／足さないなら理由を1行**（**CC-α が読み違えているならそう書いてほしい**）——**CC-α は足すべきだと思うが決めない。禁止を増やすのは MGR の領分**。★**CC-α の計器（小さいが書く）**=`grep -c … || echo "harness を読めない"` と打ったところ**「0」の後に「harness を読めない」も出た**が、**原因は grep が該当0件で終了コード1を返し `||` が発火したことで、実際には harness は読めており「読めない」は CC-α の書き方が作った偽の表示**——**もしここで止めていたら「harness を読めなかった」と報告していた。出力を2つ並べて見たから気づいた**。★確かめていないこと=**本当に本番の台帳へ書かれるかは走らせておらず台帳も読めないので「書くと読める」までしか言えない**／**`submit()` 以外の入口を呼ぶ本はまだ見ていない**。*
