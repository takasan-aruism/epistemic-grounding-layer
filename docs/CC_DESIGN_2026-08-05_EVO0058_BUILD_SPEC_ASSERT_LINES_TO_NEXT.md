# 【BUILD SPEC】`EVO-0058` — **★assert の中身まで届ける（★`-q` は外さない）**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 監視 / 発: 設計/監査(CC-α) / 2026-08-05 20:3x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.14）** ／ 親: `ITEM-2DER-EVO-0035` ／ 台帳: `ITEM-2DER-EVO-0058`
- **★v1.8 の宣言**: **★核は無い・★2DER 工程 0**（★既存の1行で ★拾う行を1種類 増やすだけ）
- **★私の予告**: ★Claude **1〜3行** ／ ★worker **0件** ／ ★走行 **1回**（★同一 task の REGENERATE）
- **★新機構0・★新台帳0・★`-q` を外さない・★`stdout` の窓を ★広げない**

---

## 1. ★★`-q` を外す必要は ★無い（★条件(c)を ★先に測った）

```
★実測（★本物の欠陥を再現し ★`pytest -q` の ★実出力を測った）:
   ★全出力 = ★1407字
   ★★末尾500字（★= 記録に残っている窓）に ★★`E ` で始まる ★assert の詳細行が ★★3本 在る
   ★★★`FAILED ` 行も ★2本 在る（★現状 拾っているのは ★こちらだけ）
★★★★∴ ★★assert の中身は ★★★既に記録の中に在る。★拾っていないだけである。
★★★★★∴ ★`-q` を外さない ＝ ★他の読み手（★受入の数え方・★`runner_exit` 等）は ★1つも壊れない
   （★条件(c) は ★★『外さないので 壊れない』で ★満たされる）
```

## 2. ★★増分（★条件(a)・★実測）

| 案 | 拾う行 | 窓 | ★足す字数 | requirement |
|---|---|---|---|---|
| 現状 | `FAILED ` のみ | 500 | ★176字 | 7696字 |
| **★案A（★推薦）** | `FAILED ` ＋ `E ` | **500（★変えない）** | **★274字** | **7794字** |
| 案B | `FAILED ` ＋ `E ` | 1000（★広げる） | ★529字 | 8049字 |

```
★★推薦 = ★★案A。★理由 = ★★窓を ★1文字も広げない（★記録が太らない）／★増分は ★★+98字だけ
★★★案B は ★assert 行が ★3本→7本 に増えるが ★★記録の窓を広げる ＝ ★DW に積む量が ★増える
   ∴ ★★案A で ★足りるかを ★先に見る。★足りなければ ★その時に ★案B（★1件ずつ）
```

## 3. 変更（★`twoder/generate_via_runner.py` の ★`_ls` を作る1行）

```python
            _ls = [l[:200] for l in (_tr.get("runner_stdout_tail") or "").splitlines()
                   if l.startswith("FAILED ") or l.strip().startswith("E ")][:15]
```

```
★★上限(★条件(b))=★★1行200字 ／ ★合計15行（★現状は5行）。★本文を ★丸ごと入れない
★★★`E ` は ★左に空白が付くので ★`strip()` してから見る（★実測: pytest はインデントして出す）
★★★★拾った行は ★そのまま並べる（★整形しない・★要約しない）
★★★★★`runner_stdout_tail` の窓は ★★触らない（★`live_worker_runtime.py:79-80` の 500 は ★そのまま）
```

## 4. 受入

```
★(1) ★★同一 task で ★REGENERATE を1回 通し、★`requirement` の増分を ★逐語で書く
     ★★私の実測は ★★+274字（★7520 → 7794）。★違ってよい ―― ★違ったら ★『違った』と数字を書く
★(2) ★足された行に ★★`E ` で始まる行が ★1本以上 在ること（★assert の中身が ★届いていること）
★(3) ★★1行が ★200字を超えていないこと ／ ★合計が ★15行を超えていないこと
★(4) ★`test_command` に ★`-q` が ★残っていること（★外していない）
★(5) ★他の読み手が ★壊れていないこと = ★`runner_exit` と ★`passed` が ★従来どおり読めること
★(6) ★`live_worker_runtime.py` の ★500 が ★1文字も変わっていないこと
★(7) ★Claude の行数 ／★(8) ★戻せる ／★(9) ★61本を走らせない ／★(10) ★commit しない
★★★★★予告を投入前に書く: ★行数 ／ ★(1) の増分字数
```

## 5. ★★これで分からないこと（★先に言う）

```
★★`E ` 行は ★pytest が ★出す分だけ ―― ★★`assert x == y` の ★左右の値が ★長ければ ★pytest 自身が省略する。
   ∴ ★★『値が全部 見える』とは ★書かない。★見えるのは ★pytest が出した分だけ。
★★★窓が500字のままなので ★★落ちた試験が ★多いほど ★★古い方から ★溢れる（★実測: 7本のうち3本しか入らない）
   ∴ ★★『全部の失敗が届く』とも ★書かない。
★★★★これで ★通るようになるかは ★分からない（★1走行では ★言えない）。★測るのは ★★『届いたか』だけ。
```

## 6. 禁止

```
★`-q` を外す（★§1・★他の読み手が壊れる）／ ★`stdout` の窓(500)を ★広げる（★案B は ★別の1件）
★行を ★整形する・要約する・並べ替える（★そのまま並べる）
★上限(200字/15行)を ★外す ／ ★本文や diff を ★入れる
★★『assert の値が 全部 見えるようになった』『これで通る』と書く（★§5）
★61本を走らせる ／ ★commit する ／ ★`twoder` 配下で python を動かす
```
