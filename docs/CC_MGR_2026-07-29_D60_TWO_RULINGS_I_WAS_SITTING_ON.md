# CC 管理(MGR) → 設計/監査(CC-α): **★私が抱えたままにしていた裁定2件を出す（G-50 / G-46）。★残件の数も私が間違えていた**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **典拠**: `docs/2DER_EXECUTION_ARCHITECTURE.json` の `G-50` / `G-46`（**どちらも `note` に「MGR 裁定事項」と書かれていた**）

## 0. ★まず自分の誤りを2つ書く
| # | 誤り |
|---|---|
| **1** | **★残件を「4件→3件」と Taka に報告したが、誤りである**（§1 に実数） |
| **2** | **★裁定待ち2件を、資料に書かれたまま抱えていた。** **私が読みに行かなかった** |

> **★「今あなたで止まってる」は正しい。** **★止めていたのは私である。**

## 1. ★残件7件の実状（★資料から。記憶で数えない）
| # | Taka の7件 | 対応 | **状態** |
|---|---|---|---|
| 1 | 13番目の入口 | `G-50` | **OPEN（★MGR 裁定待ち）** |
| 2 | Web UI からの `ETRACE_RUN_ID` 読み出し | `G-46` | **PARTIALLY_CLOSED（★35件中1件。MGR 裁定待ち）** |
| 3 | DW 枝の到達性確認 | — | **未着手** |
| 4 | DW fail-open 時の欠損可視化 | `G-38` | **表示は済（D-55/56）。★実例は0件のまま** |
| 5 | 一件の依頼を最後まで読める | — | **1件について6/6（D-49/50）。★他の依頼では未確認** |
| 6 | `acquisition` 固有イベントの必要性判断 | `G-49` | **DEFERRED_TO_PHASE2（★判断が要る）** |
| 7 | 独立検算の正式な定義 | — | **★済（D-59）** |

**★はっきり済は2件（#4・#7）。部分が2件（#2・#5）。未が3件（#1・#3・#6）。**
**★「3件」は誤りだった。**

## 2. ★裁定① `G-50`「14番目が無いとは言えない」— ★止める基準
```
資料の note: 「13番目が出た以上14番目が無いとは言えず、Taka ⑤を『済』と言えない
             ——『もう1度やる』を無限に繰り返さない基準が要る(MGR 裁定事項)」
```
**★基準を1つだけ置く:**
> **★数え直す条件は「新しい発見が出たこと」ではない。** **★「入口の定義（述語）を変えたこと」である。**

| ★やること | |
|---|---|
| **1** | **「入口」を、★決定論で真偽が決まる述語として1つ書く**（例:「`create_task(` を直接呼ぶ箇所」） |
| **2** | **その述語で★全5 repo を打ち切り無しで1回走査する。総数を書く** |
| **3** | **★同じ述語での再走査はしない。** **★述語を変えたときだけ数え直し、変えた理由を書く** |

> **理由: 「まだ在るかもしれない」で数え直すと★終わらない。** **★終わらせるのは発見ではなく定義である。**
> **★13番目は、述語が変わった（`submit()` 経由 → `create_task` 直呼びも入口とみなす）から出た。** **★発見ではなく定義の変更が出したものである。**

**∴ `G-50` は「述語を書いて1回走査し、総数を記録したら CLOSED」とする。** **★14番目を探しに行かない。**
**★据え置き（CC-α 推奨の (a)）を採る**——**依頼経路でないなら `run_id` が無いのは正常であり、塞ぐと合流点が呼び手の数だけ増える。**

## 3. ★裁定② `G-46`「35件のうちどれを読めるようにするか」
```
資料の note: 「選別の基準は CC-α が決めず MGR へ: (a)Taka の問いに要る RRI 判断値だけ
              (b)別経路が無いものだけ【CC-α 推奨】 (c)全部」
```
**★(b) を採る。** **★ただし条件を1つ付ける。**

> **★選ぶ前に、35件それぞれに「別経路が在るか」を数え切ること。**
> **★数え切らずに (b) を選ぶと、本日 D-57 で止めた形（1件ずつ見つける）に戻る。**

| ★手順 | |
|---|---|
| **1** | **35件について「front door から読める別経路が在るか」を判定し、★総数・確認件数・打ち切り無しを書く** |
| **2** | **「在る」ものは★直さない**（既に読める） |
| **3** | **「無い」ものだけを対象とし、★件数を先に出す**（2件なのか30件なのかで進め方が変わる） |
| **4** | **★実装はまだしない。** **件数が出た時点で私に返すこと** |

**★これは調査であって変更ではない。** **★投入しない。★作って試さない。**

## 4. ★次に取るもの（私が決める）
```
① G-46 の35件を数え切る（★調査のみ）   ← ★これを先にやる
② G-50 の述語を1つ書いて1回走査する（★据え置きの記録）
```
**★①を先にする理由: 件数が出るまで、Phase 2 の残りの大きさが分からない。**

## 5. ★変えないもの
**勘定科目に触れない**（`G-62`/`G-64`〜`G-68` 据え置き）。**規則文書を作らない。★投入しない。**

---
*MGR。★**まず自分の誤り2つ**=①残件を「4件→3件」と Taka に報告したが誤り ②**裁定待ち2件を資料に書かれたまま抱えていた（読みに行かなかった）**——**「今あなたで止まってる」は正しく、止めていたのは私である**。★**残件7件の実状（資料から）**=①13番目の入口＝`G-50` OPEN（**MGR 裁定待ち**）②`ETRACE_RUN_ID` 読み出し＝`G-46` PARTIALLY_CLOSED（**35件中1件・MGR 裁定待ち**）③DW 枝の到達性確認＝未着手 ④DW fail-open の欠損可視化＝`G-38`・表示は済（D-55/56）だが**実例は0件のまま** ⑤一件を最後まで読める＝1件について6/6（D-49/50）だが**他の依頼では未確認** ⑥`acquisition` 固有イベント＝`G-49` DEFERRED（**判断が要る**）⑦独立検算＝**済（D-59）** ∴ **はっきり済2件・部分2件・未3件で「3件」は誤りだった**。★**裁定①`G-50`**=**数え直す条件は「新しい発見が出たこと」ではなく「入口の定義（述語）を変えたこと」**——(1)「入口」を決定論で真偽が決まる述語として1つ書く (2)その述語で全5 repo を打ち切り無しで1回走査し総数を書く (3)**同じ述語での再走査はせず、述語を変えたときだけ数え直し理由を書く**——**「まだ在るかもしれない」で数え直すと終わらず、終わらせるのは発見ではなく定義である。13番目は述語が変わった（`submit()` 経由→`create_task` 直呼びも入口とみなす）から出たのであって、発見ではなく定義の変更が出したものである** ∴ `G-50` は「述語を書いて1回走査し総数を記録したら CLOSED」とし**14番目を探しに行かず、CC-α 推奨の据え置き (a) を採る**（依頼経路でないなら `run_id` が無いのは正常で、塞ぐと合流点が呼び手の数だけ増える）。★**裁定②`G-46`**=**(b) 別経路が無いものだけ を採るが、選ぶ前に35件それぞれに「別経路が在るか」を数え切ること**——**数え切らずに (b) を選ぶと本日 D-57 で止めた形（1件ずつ見つける）に戻る**——手順は (1)35件について front door から読める別経路の有無を判定し**総数・確認件数・打ち切り無し**を書く (2)「在る」ものは直さない (3)「無い」ものだけを対象とし**件数を先に出す**（2件か30件かで進め方が変わる）(4)**実装はまだせず件数が出た時点で MGR に返す**——**調査であって変更ではなく、投入せず作って試さない**。★次に取るのは**①`G-46` の35件を数え切る（調査のみ）→②`G-50` の述語を1つ書いて1回走査**で、**①を先にする理由は件数が出るまで Phase 2 の残りの大きさが分からないから**。★勘定科目に触れず規則文書を作らず投入しない。*
