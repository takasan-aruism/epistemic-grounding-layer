# CC 管理(MGR): **D5-V 再実行を承認（10 run 維持）**（HANDOFF・短く）

- `BUILD_ROLE: 参照` / 宛: IMPL(coder) / 写: DESIGN(CC-α) / Taka / 発: MGR / 2026-07-27 03:35 / TYPE=HANDOFF

## 1. 承認
**再実行してよい。10 run を維持する**（IMPL の推奨どおり。5 run に削ると遠隔誤りが約10件しか出ないため判定材料が薄くなる＝**削って判定不能になるくらいなら回す**）。
- **投げる前にコストを出したのは正しい。** 2,400 呼出・ローカル GPU・16並列。
- **(a)/(b) の判定は IMPL がしない**（表と3つの数のみ）。**判定は DESIGN。**
- **判定基準（`STOP`/`ACT` の定義）は測定後に動かさない。**

## 2. 規律に反しないことの確認（自分で決めた規律を自分で破らない）
「**精度が動くまで新しい測定装置を作らない**」に対して、本件は**新しい計器ではなく、既に走らせた介入の結果を解釈するための欠損データの補完**である。**＝ 例外に定めた「修理」に当たる。**

## 3. 恒久（今回の原因への対処）
- **集計値だけを残して元データを捨てる実装を、今後作らない。** **per-row を必ず書き出す**（IMPL が実装済）。
- **これは「率を出したらヒットを目視する」規律の実装版である。** 本日4回刺された同じ形の5回目を防ぐ。

---
*MGR。D5-V 再実行を承認（10 run 維持・削って判定不能になるくらいなら回す）。投げる前にコストを出した作法は正しい。(a)/(b) の判定は DESIGN、IMPL は表と数のみ。判定基準は測定後に動かさない。本件は新しい計器でなく欠損データの補完＝「修理」の例外。恒久=集計値だけ残して元データを捨てる実装を今後作らない。*
