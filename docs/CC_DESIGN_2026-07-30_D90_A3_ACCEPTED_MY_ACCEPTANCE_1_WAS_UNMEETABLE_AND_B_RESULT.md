# 設計/監査 → MGR（写: Taka / IMPL）: **D-90 — A-3 受入。★私の受入1は満たせない形だった（3回目）／【B】残件7件のうち★2件は資料に登記されていなかった（私の漏れ）**

- `BUILD_ROLE: 参照`（**監査と登記のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_IMPL_2026-07-30_D89_STATE_NAME_JA_BUILT.md` ／ **裁定**: `CC_MGR_2026-07-30_D89_APPROVED_AS_IS_NEXT_IS_B.md` §4

## 0. ★2DER 優先原則の4点（D-88 §2）
| ① 入口 | **無し** ／ ② ③ **該当しない** ／ ④ 下記 |
|---|---|
> **★理由: 受入確認と登記は「2DER 自身の記録の維持」である**（D-88 §4）。

## 0-1. ★結論
> **① A-3 は満たされている。★ただし★私の受入1は「満たせない形」だった**（§2）。**★同じ形の3回目である。**
> **② 【B】: ★Taka の残件7件のうち、★2件が資料に登記されていなかった。★登記は私の責務である。**
> **③ ★登記した**（`G-75` / `G-76`）。**★中身は測っていない。★「済」とも「未」とも書いていない。**

---

# 【A-3】受入

## 1. ★満たされている（★私が実物で確認した）
| # | 条件 | 判定 |
|---|---|---|
| **2** | 「裁定が要る」が在る | **★済**（配信物） |
| **3** | 知らない状態はそのまま出す分岐 | **★済** `function stateJa(k){return (k in STATE_JA)?STATE_JA[k]:k;}` |
| **4** | 対訳が1件だけ | **★済** `const STATE_JA={"JUDGE_REQUIRED":"裁定が要る"};` |
| **5** | `/api/*` を変えていない | **★済**（差分は★JS の5行追加と1行の書き換えのみ） |
| **6** | 内訳の件数が変わっていない | **★済 18件**（★私の独立計算） |
| **7** | 和が合計と一致 | **★済 117+18+18 = 153**（★私の独立計算） |
| **8** | 配信物の JS が `node --check` | **★済。★今回は私が実行した**（配信 HTML から `<script>` を抽出＝17987バイト → 構文 OK） |
| **10** | 人称語 0件 | **★済**（差分に無い） |

> **★受入8 を私が自分で実行したのは初めてである。** **★これまでは `【伝聞】` で通していた。**

## 2. ★私の受入1が「満たせない形」だった（★3回目・自己申告）
```
私が書いた受入1: 「★配信物に JUDGE_REQUIRED が0件」
実際            : ★1件 在る —— ★対訳表そのものである（const STATE_JA={"JUDGE_REQUIRED":…}）
```
> **★対訳表を作れば、★英語の語は必ず1回 残る。** **★0件にはできない。**
> **★私は「計器が自分を数えている形」を、★また書いた。**

| 回 | 満たせない受入 | |
|---|---|---|
| 1 | **「`fetch(` の直呼びが0件」** | **`askJSON` 自身が `fetch` を呼ぶ** |
| 2 | **「`GET /command` の描画結果で155行が消えたと示す」** | **ID は JS が描くので HTML に元から無い** |
| **3** | **「配信物に `JUDGE_REQUIRED` が0件」** | **★対訳表に必ず残る** |

**★正しい受入はこうだった:**
> **「★画面に出る側（`unknownBreakdown` が出力する語）に `JUDGE_REQUIRED` が出ない」＝★`esc(stateJa(k))` を通っていること。**
> **★実際そうなっている ∴ ★受入1 の意図は満たされている。** **★書き方だけが誤っていた。**

**【設計:CC-α】★型として残す（★規則にしない）:**
> **★「0件」を受入にするときは、★その0が「作った物自身」を含まないか確かめる。**
> **★私は「0 を疑え」を身につけたが、★「0 にできない受入を書かない」はまだ身についていない。**

---

# 【B】Taka の残件7件の現況（★資料から。★記憶で数えない）

## 3. ★出典
- **正典**: `PHASE2_DIRECTION_MANAGEMENT_UI_v1_0.md` §1（**★Taka 逐語**・7件）
- **現況**: `2DER_EXECUTION_ARCHITECTURE.json` の `gaps`（★走査は打ち切り無し）

## 4. ★結果（★7件・総数と確認件数を明記）
| # | Taka 逐語 | 資料の Gap | status | **現況** |
|---|---|---|---|---|
| 1 | **13番目の入口** | `G-50` | `OPEN` | **★未** |
| 2 | **Web UIからの `ETRACE_RUN_ID` 読み出し** | `G-46` | `PARTIALLY_CLOSED` | **★部分**（webui 投入分は引ける。CLI 分と残り33件は未） |
| 3 | **DW枝の到達性確認** | **★無かった → `G-75` を登記** | `OPEN` | **★資料から出せない**（§5） |
| 4 | **DW fail-open時の欠損可視化** | `G-38` `OPEN` ／ `G-69` `OPEN` ／ `G-70` `PARTIALLY_CLOSED` | 混在 | **★部分**（画面に出るようになったが、fail-open 自体は残る） |
| 5 | **帳簿管理UIから一件の依頼を最後まで読める状態** | `G-46` `PARTIALLY_CLOSED` ／ `G-69` `OPEN` ／ `G-73` `OPEN` | 混在 | **★部分** |
| 6 | **`acquisition` 固有イベントの必要性判断** | `G-49` | `DEFERRED_TO_PHASE2` | **★未**（★判断していない） |
| 7 | **独立検算の正式な定義** | **★無かった → `G-76` を登記** | `DEFERRED` | **★据え置き**（D-45 §1-3 の裁定。★未着手ではない） |

```
★確認件数: 7 / 7（★1件も飛ばしていない）
★資料の Gap 総数: 74 → ★76（登記漏れ2件を足した）
★走査: gaps / planned_extensions / unknowns の全文を「到達性」「reachab」「17件」「独立検算」「正式な定義」で照合。打ち切り無し。
```

## 5. ★2件が登記されていなかった（★私の漏れ）
```
MGR `CC_MGR_2026-07-28_D45` §2-2:「★残件7件を、そのまま台帳へ（勝手に並べ替えない）」「★登記は設計/監査」
実際: ★③と⑦は、gaps にも planned_extensions にも unknowns にも★1件も無かった。
```
> **★決めたのに、やっていなかった。** **★登記は私の責務である。**
> **★2日 気づかなかった。** **★「資料から出す」と言われて初めて分かった。**
> **★計器（状況表）は「文書の Gap は全て資料に在る」と出し続けていた**——**★あれは文書に書いた `G-xx` を照合するもので、★★書かなかったものは検出しない。**

**★登記した内容（★中身は測っていない）**
| id | 中身 |
|---|---|
| **`G-75`** | 残件③が未登記だった ∴ **現況を資料から出せない**。**★DW の各枝が到達するかは★測っていない。「未着手」とも「済」とも書かない** |
| **`G-76`** | 残件⑦が未登記だった。**★status は `DEFERRED`**——**★D-45 §1-3「Phase 2 の中心ではないので作らない」＝裁定による据え置きであり、未着手ではない** |

> **★`G-76` について1つ添える**（★事実のみ）: **★実運用では D-59 の定義（「誤り方が違う経路を通ったか」）が既に使われており、★D-76/D-77 で実際に効いた**（私が git HEAD から独立に基準を作り差分0を示した）。
> **★しかし「正式な定義文書」は作っていない。** **★「実質できている」と書かない。**

## 6. ★資料の同期（★済）
```
JSON 76 / MD 76 / ★食い違い なし
component 23 / edge 11 / entrypoint 8
```
**★A-3 の commit 後に、`twoder` の commit を更新する**（★今はまだ未 commit）。

## 7. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **`G-75` の中身**（DW の各枝が到達するか） | **★測っていない** |
| 2 | **①③④⑤⑥ の中身が資料の status どおりか** | **★status を写しただけ。★1件も測り直していない** |
| 3 | **他にも登記漏れが無いか** | **★7件しか照合していない。★Taka の他の指示は照合していない** |

> **★3が重い。** **★私は「7件のうち2件が漏れていた」を見つけたが、★漏れが7件の中だけだと確かめていない。**

---
*CC-α D-90（監査と登記のみ）。★結論=①**A-3 は満たされているが、CC-α の受入1が「満たせない形」だった（同じ形の3回目）** ②**【B】Taka の残件7件のうち2件が資料に登記されていなかった（登記は CC-α の責務）** ③**登記した（`G-75`/`G-76`）が中身は測っておらず「済」とも「未」とも書いていない**。★**A-3 受入**=「裁定が要る」が在る／**知らない状態はそのまま出す分岐**（`(k in STATE_JA)?STATE_JA[k]:k`）／**対訳が1件だけ**／**`/api/*` 不変（差分は JS の5行追加と1行書き換えのみ）**／**内訳18件・和 117+18+18=153 は CC-α の独立計算**／**`node --check` は今回 CC-α が自分で実行（配信 HTML から `<script>` を抽出＝17987バイト → 構文 OK。これまでは `【伝聞】` で通していた）**／人称語0件。★**受入1 が満たせない形だった**——「配信物に `JUDGE_REQUIRED` が0件」と書いたが**対訳表そのもの（`const STATE_JA={"JUDGE_REQUIRED":…}`）に必ず1件 残るので0件にできない**——**「計器が自分を数えている形」を3回目に書いた**（1回目「`fetch(` の直呼びが0件」＝`askJSON` 自身が呼ぶ／2回目「描画結果で155行が消えたと示す」＝ID は JS が描くので HTML に元から無い／3回目が今回）。**正しくは「画面に出る側に `JUDGE_REQUIRED` が出ない＝`esc(stateJa(k))` を通っている」であり、実際そうなっているので意図は満たされ、書き方だけが誤っていた**——**型として残す（規則にしない）: 「0件」を受入にするときはその0が「作った物自身」を含まないか確かめる。CC-α は「0 を疑え」を身につけたが「0 にできない受入を書かない」はまだ身についていない**。★**【B】7件の現況（資料から・記憶で数えない）**=①13番目の入口 `G-50 OPEN` **未** ②`ETRACE_RUN_ID` 読み出し `G-46 PARTIALLY_CLOSED` **部分** ③DW枝の到達性確認 **資料に無し→`G-75` を登記・資料から出せない** ④DW fail-open 時の欠損可視化 `G-38 OPEN`/`G-69 OPEN`/`G-70 PARTIALLY_CLOSED` **部分** ⑤一件を最後まで読める `G-46`/`G-69`/`G-73` **部分** ⑥`acquisition` 固有イベント `G-49 DEFERRED_TO_PHASE2` **未（判断していない）** ⑦独立検算の正式な定義 **資料に無し→`G-76` を登記・`DEFERRED`（D-45 §1-3 の裁定による据え置きで未着手ではない）**——**確認件数 7/7・打ち切り無し・Gap 総数 74→76**。★**2件が登記されていなかったのは CC-α の漏れ**——**MGR D-45 §2-2 が「残件7件をそのまま台帳へ／登記は設計/監査」と決めたのに、③と⑦は gaps にも planned_extensions にも unknowns にも1件も無く、2日 気づかず「資料から出す」と言われて初めて分かった**——**計器（状況表）は「文書の Gap は全て資料に在る」と出し続けていたが、あれは文書に書いた `G-xx` を照合するものであり、書かなかったものは検出しない**。★`G-76` に事実のみ添える=**実運用では D-59 の定義（誤り方が違う経路）が既に使われ D-76/D-77 で実際に効いた**が、**「正式な定義文書」は作っておらず「実質できている」と書かない**。★資料は同期済（**JSON 76 / MD 76 / 食い違いなし**）で、**A-3 の commit 後に `twoder` の commit を更新する**。★確かめていないこと=`G-75` の中身は測っていない／**①③④⑤⑥ は status を写しただけで1件も測り直していない**／**他にも登記漏れが無いかは確かめておらず、7件の中だけを照合した（これが最も重い）**。*
