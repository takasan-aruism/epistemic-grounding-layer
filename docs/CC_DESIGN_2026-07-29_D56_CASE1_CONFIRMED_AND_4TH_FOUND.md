# 設計/監査 → MGR（写: Taka / IMPL）: **D-56 — ★MGR の問いはコードで決まった（case 1）。SPEC のままでよい。★そして4段目が実在した**

- `BUILD_ROLE: 参照`（**監査。コードを1行も変えていない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_MGR_2026-07-29_D56_SPEC_APPROVED_ONE_CONTRADICTION.md`

## 0. ★結論
> **① MGR の問い（`events` が無い場合は `if(!rec)` を通るか）→ ★通る。case 1 である。SPEC のままでよい。**
> **② ★D-56 は既に実装されている**（`droppedBlock(rec, hasRunId)` が実読で確認できる）。
> **③ ★4段目が実在した。** **「欠損・失敗の記録なし」が、★2つの場合を1つの文言でまとめている。**

---

## 1. ★MGR の問いへの答え（実読・断定の根拠を示す）
```javascript
async function passedCard(s){
  let body, out=null;
  …
    const rec=r.record||{}; const evs=rec.events||[];
    if(r.resolved&&evs.length) out=rec;      ← ★resolved かつ events が在るときだけ out に入る
  …
  return out;}
```
> **∴ `events` が無ければ `out` は `null` のままである。**
> **∴ `droppedBlock(null, …)` が呼ばれ、★`if(!rec)` を通る。**
> **∴ MGR の case 1 である。** **★3つ目の名前の無い場合は存在しない。** **SPEC は矛盾していなかった。**

**★私の §2 の書き方は不正確だった**（「`events` が無い」を `rec` が在る場合のように読ませた）。**MGR の疑いは正しく、答えが case 1 だっただけである。**

## 2. ★D-56 は既に実装されている（実読）
```javascript
function droppedBlock(rec,hasRunId){
  if(!rec) return "…"
    +(hasRunId ? "取りこぼしの件数は取得できない（run_id は在るが、記録を引けなかった）"
               : "取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）")+"</div>"+DROP_NOTE;
```
- **★(c1)/(c2) が文言だけで分かれている。** **振る舞いは変わっていない。**
- **★原因を書いていない**（「引けなかった」まで）。**SPEC §2 のとおり。**

## 3. ★4段目が実在した（MGR §3 の問いに答える）
> **MGR:「毎段、同じ形で見つかっている：1つの文言が2つの場合をまとめていた。4段目が在るかを同じ問いで先に当てておく。」**

**★在る。** **実読:**
```javascript
const body=(parts.length ? parts.join("")
                         : "<div class='empty'>欠損・失敗の記録なし</div>") + droppedBlock(...);
```
| 「欠損・失敗の記録なし」が意味しうる2つ | |
|---|---|
| **(1)** | **欠損・失敗が★起きなかった** |
| **(2)** | **起きたが★記録されなかった／`/api/state` に載っていない** |

> **★これは「取りこぼし 0件」と★完全に同じ形である。**
> **★そして併記文言が付いていない。** **`DROP_NOTE` は取りこぼしブロックにしか付かない。**
> **∴ 「記録なし」は、★安心の材料として読める。** **本 build 群が禁じてきたものである。**

### 3-1. ★根拠（この場合が実在するか）
```
再現（CC-α 実測）: /api/state?task_id=TASK-2DER-B9B4DA3B
  boundary_failures = 2件 / guard_block = None / failure_memory_match = None / block_source_refs = None
  ds_limitation = 在り
∴ この task では parts が空にならない。
★「parts が空になる依頼」を私は★まだ見つけていない。
```
> **∴ 4段目は★構造としては実在する。** **★実例は見つけていない。**
> **★作って試さない。** **`G-61`・(a)・(c2) と同じ扱いである。**

## 4. ★私は直さない（裁定を仰ぐ）
| # | 理由 |
|---|---|
| 1 | **D-56 の範囲は「(c) を2つに割る」であり、★「記録なし」は範囲外である** |
| 2 | **★MGR が「4段目が在れば登記する。無ければ『無い』と書く」と指示した。** **登記までが私の仕事である** |
| 3 | **★直すなら文言であり、文言は MGR 逐語の系列である。** **私が発明しない** |

**【設計:CC-α】★登記する。** **直すかどうかは MGR が決める。**

## 5. ★受入（MGR §4 の補足を反映）
- **受入2「(c2) の文言が画面のソースに在る」で判定する。**
- **★BUILT には「(c2) は★出たことが無い」と書かせる。** **`G-61`・(a) と同じ扱い。**
- **★私も同じことを書く: (c2) の実例は無い。** **4 run すべて `resolved=true` である。**

## 6. ★未確認
| # | 未確認 | 誰が・いつ |
|---|---|---|
| 1 | **「parts が空になる依頼」の実例** | **★作らない。** 自然に発生したら確かめる |
| 2 | **(c2) の実例** | **★作らない**（同上） |
| 3 | 受入5・6（既存不変・非回帰）を私は再実行していない | CC-α / 要請時 |
| 4 | **画面をブラウザで見ていない**（HTML と JS の実読で判定） | D-45 から継続 |

---
*CC-α D-56。★結論=①MGR の問い（`events` が無い場合 `if(!rec)` を通るか）は**通る＝case 1** で SPEC のままでよい ②**D-56 は既に実装されている** ③**4段目が実在した**。★①の根拠=`passedCard` は `let out=null` から始まり `if(r.resolved&&evs.length) out=rec;` なので **`events` が無ければ `out` は `null` のまま**であり `droppedBlock(null,…)` が `if(!rec)` を通る ∴ 3つ目の名前の無い場合は存在しない。**私の §2 の書き方が不正確だった**（「`events` が無い」を `rec` が在る場合のように読ませた）——MGR の疑いは正しく、答えが case 1 だっただけである。★②実読で `droppedBlock(rec,hasRunId)` が (c1)/(c2) を**文言だけ**で分け、振る舞いを変えず、**原因を書いていない**（「引けなかった」まで）ことを確認。★③**4段目は在る**=`missingCard` の `parts.length ? … : "欠損・失敗の記録なし"` が、**(1)欠損・失敗が起きなかった**と**(2)起きたが記録されなかった／`/api/state` に載っていない**の2つを1つの文言でまとめており、**「取りこぼし 0件」と完全に同じ形**で、**併記文言が付いていない**（`DROP_NOTE` は取りこぼしブロックにしか付かない）∴「記録なし」は安心の材料として読め、本 build 群が禁じてきたものである。**ただし実例は見つけていない**（`TASK-2DER-B9B4DA3B` は `boundary_failures` 2件・`ds_limitation` 在りで parts が空にならない）——**構造としては実在するが実例は無く、作って試さない**（`G-61`・(a)・(c2) と同じ扱い）。★**私は直さず登記する**（D-56 の範囲外／MGR が「4段目が在れば登記する」と指示した／直すなら文言であり MGR 逐語の系列なので私が発明しない）。★受入は (c2) の文言が画面のソースに在ることで判定し、**BUILT に「(c2) は出たことが無い」と書かせ、私も同じく「実例は無い。4 run すべて `resolved=true`」と書く**。★未確認=「parts が空になる依頼」の実例と (c2) の実例（**どちらも作らない**）／受入5・6 は再実行していない／画面をブラウザで見ていない。*
