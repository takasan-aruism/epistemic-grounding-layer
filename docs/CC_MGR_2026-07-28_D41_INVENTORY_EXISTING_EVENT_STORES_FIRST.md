# CC 管理(MGR) → 設計/監査(CC-α): **★D-41 — Event Trace 基盤に着手する前に、既存の記録機構を棚卸しする。10回目を作らない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **典拠**: `EVENT_TRACE_WORK_ORDER_v1_0.md`（Taka 逐語・本日受領）

## 1. Taka の作業指示書を受けた。★ただし最初の作業は実装ではない
**指示書は「既存処理へ最小限の Hook を追加する方式を優先する」と書いている。**
**★しかし「どこに記録するか」は指定されていない。** **∴ そこを我々が決める。**
**★そして本日、我々は「既存の道が既に在った」を9回数えた**（`ids.resolve` / `artifact_registry` / `contract_seal` / 再試行経路 / `HUMAN_RELAYED` / `/api/resolve` / `/api/state` ほか）。
> **∴ 10回目を我々が作る危険が、いま最も高い。**
> **∴ ★実装より先に、既存の記録機構を棚卸しする。**

## 2. 依頼（D-41・調査のみ。★作らない・投入しない）
**「イベントを記録する仕組み」として既に在るものを、★全部列挙してください。**

| # | 調べること | 出し方 |
|---|---|---|
| **1** | **既存の記録機構を全部列挙する** | **★私が知っている範囲でも次が在る: `TRACE`（`twoder/runs/*.trace.json`）／DW の `events`（`workcell._read_events`・`TASK-…` で `events=8`）／`dev-workcell/events.jsonl`／`ds_events.jsonl`／EGL の各台帳／`artifact_registry` の `record_change`。★これで全部かは私は知らない。網羅すること** |
| **2** | **各機構が、指示書の12項目のうち何を既に持っているか** | **★項目ごとに ○/× の表。** `Event ID` / `親 Event ID` / `Session ID` / `Request ID` / `Timestamp` / `Component 名` / `Function 名` / 入力 / 出力 / 次に渡した先 / 実行結果 / `Error` 有無 |
| **3** | **どれかが基盤になれるか。なれないなら何が足りないか** | **★「足りない」を項目名で書く。** 「不十分」で済ませない |
| **4** | **★親子関係（`親 Event ID`）を持つ機構が在るか** | **これが無いと「一本の系列」にならない。** 最重要項目である |
| **5** | **`Session ID` / `Request ID` に相当するものが既に在るか** | **`task_id`（`sha1(raw_input)`）／`conversation_id`／`run_id`／`SLICE-…` などが候補。★実物で確かめる** |

## 3. ★同時に答えること（指示書の最難項目・先送りしない）
> **完了条件に「★呼ばれなかった処理も識別できる」が在る。**

**Hook は「実行された」しか取れない。** **「呼ばれなかった」を出すには、★「呼ばれるはずだった処理の一覧」が要る。**
| # | 問い |
|---|---|
| **6** | **その一覧は、どこから作れるか。** **候補: `EXEC_ARCH` の `components`／`state_machines`／`submit()` の段構成／既存の `_rec()` 呼び出し箇所。★実物を見て答える** |
| **7** | **一覧が作れないなら、「呼ばれなかった」の代わりに何を出せるか**（例: 「この段に到達しなかった」だけを出す）。**★できないことを先に出す**（`feasibility-first`） |

## 4. ★禁止
1. **★何も作らない。** **Hook を1つも足さない。** **新しいファイルを作らない。**
2. **★「新しい台帳を作ればよい」を結論にしない。** **常設命令 §0-2 が生きている。** **既存で足りるかを先に出す。**
3. **★足りない場合も、あなたが決めない。** **「既存では足りない。理由は◯◯。新設が要る」まで書いて、★裁定を私に上げる。** **私が Taka に確認する**（新設は彼の禁止事項に触れるため）。
4. **投入しない。台帳に直接手を出さない。** **確認は front door（`/api/resolve` / `/api/state`）を使う。**
5. **★「全部見た」と書かない。** **未確認範囲を書く**（§3-7）。

## 5. ★私（MGR）が Taka に確認すること（あなたの答えを待って行う）
```
イベントを保存する先が、既存の機構では足りない場合:
  → 新しい保存先を1つ作ることになる。
  → それは「新規の台帳の制作は禁止」に触れる。
  → ★私が Taka に確認する。あなたは作らない。
```

## 6. 優先順（★組み直す）
1. **★D-41（本件・棚卸し）**
2. その結果を受けて、Event Trace 基盤の SPEC（**保存先が決まってから**）
3. D-39（`origin` の参照地図）— **★本基盤に吸収される可能性が在る。** 判断は D-41 の後
4. Build 22 の実装 — **止めたまま**
5. その他 — 止めたまま

---
*MGR。D-41。★Taka の Event Trace 作業指示書を受けたが、最初の作業は実装ではない——指示書は「既存処理へ最小限の Hook」を求めているが「どこに記録するか」は未指定であり、本日「既存の道が既に在った」を9回数えた以上、10回目を我々が作る危険が最も高い ∴ 実装より先に既存の記録機構を棚卸しする。★D-41（調査のみ・作らない・投入しない）=①既存の記録機構を全部列挙（`TRACE`/DW の `events`/`dev-workcell/events.jsonl`/`ds_events.jsonl`/EGL の各台帳/`artifact_registry.record_change` などが在るが網羅は不明）②各機構が指示書の12項目のうち何を持っているかを項目ごとに ○/× の表で ③基盤になれるか・なれないなら足りないものを**項目名で**（「不十分」で済ませない）④**親子関係（`親 Event ID`）を持つ機構が在るか**（無いと「一本の系列」にならない最重要項目）⑤`Session ID`/`Request ID` 相当が既に在るか（`task_id`/`conversation_id`/`run_id`/`SLICE-…` が候補・実物で確かめる）。★同時に答えること=完了条件の「呼ばれなかった処理も識別できる」は Hook では取れず「呼ばれるはずだった処理の一覧」が要るので、⑥その一覧をどこから作れるか（`EXEC_ARCH` の `components`/`state_machines`/`submit()` の段構成/既存の `_rec()` 呼び出し箇所が候補・実物を見て答える）⑦作れないなら代わりに何を出せるか（できないことを先に出す）。★禁止=何も作らない・Hook を1つも足さない・「新しい台帳を作ればよい」を結論にしない（常設命令 §0-2 が生きている）・足りない場合も CC-α が決めず「既存では足りない。理由は◯◯。新設が要る」まで書いて MGR に上げる（MGR が Taka に確認する）・投入しない・「全部見た」と書かない。★優先順=D-41 →保存先が決まってから Event Trace の SPEC →D-39（本基盤に吸収される可能性が在り判断は D-41 の後）→Build 22 は止めたまま。*
