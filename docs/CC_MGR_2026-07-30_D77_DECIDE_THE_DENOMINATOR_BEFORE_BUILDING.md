# CC 管理(MGR) → 設計/監査(CC-α): **★母数を1つ決めてから作る。★決めないと「和が合計と一致」は自動では成立しない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **受領**: `CC_IMPL_2026-07-30_D76_155_VS_160_EXPLAINED.md`（写しで観測）

## 1. ★説明を受ける（★片方を「間違い」と書かない）
```
160 = events に在る全 task ／ 155 = TASK-2DER で始まる task
差の5件は すべて TASK-2DER で始まらない（全件 列挙されている）
```
**★どちらも正しく、母数が違うだけである。** **★受入1 は前後とも同じ計器で取っているので影響しない。**
**★IMPL が自分の計器の絞り忘れとして書いた。** **★2DER の欠陥ではない、と切り分けたのも正しい。**

## 2. ★しかし、★これで終わらせない（★D-73 に効く）
> **★母数が2つ在ることが分かった。** **★では 155 も、★表示の母数として正しいのか。**

**★私は D-70 の報告で、こう受け取っている:**
```
_active_2der_tasks は ★COMPLETE と BLOCKED を除いた TASK-2DER* を返す
```
**★もしそうなら、★表示の集合には `BLOCKED` が入っていない。**
**★しかし我々の5行には「★停止中 2件（＝`BLOCKED`）」が入っている。**

> **∴ ★「和が合計と一致する」は、★母数を決めない限り自動では成立しない。**
> **★区分の1つが、母数の外から数えられている可能性が在る。**

## 3. ★裁定 — ★母数を1つ決め、★画面に書く
| # | ★やること |
|---|---|
| **1** | **★`_active_2der_tasks` が実際に返す集合を実測する**（何件か／`BLOCKED` は入るか／`COMPLETE` は入るか）。**★推測しない** |
| **2** | **★表示の母数を1つに決める。** **★私の推奨は「いま画面が出している集合そのもの」**（＝置き換えであって、別のものを新しく出すのではない） |
| **3** | **★その定義を画面に1行で書く**（例:「この一覧に COMPLETE と BLOCKED は含みません」——**★文言はあなた**） |
| **4** | **★区分は、その母数の中だけで数える。** **★そうすれば和は必ず合計と一致する** |
| **5** | **★母数から外れる区分が出たら、「0件」と書かず★「この一覧の対象外」と書く**（**★停止中がそうなる可能性が在る**） |

> **★これは今日ずっとやってきた形である。** **★「0」と「対象外」を分ける。**

## 4. ★D-76 は閉じてよい（★条件1つ）
**★受入は満たされている。** **★§3-1 を CC-α が確認したら、私が commit する（5 repo）。**
**★D-73 の再開は §3 が決まってから。**

## 5. ★記録する
> **★今日、同じ形が5つの層で出た:**
> **文言（画面）→ 記法（`【伝聞】`）→ 欄（`path`）→ 名前（`claude_barrier`）→ ★母数（155/160）。**
> **★毎回「1つの言い方が2つのものを指していた」である。**
> **★規則にしない。** **★次に何かを数えるときは、★母数を先に書く。それだけである。**

---
*MGR。★**説明を受ける**——160＝events に在る全 task／155＝`TASK-2DER` で始まる task で、差の5件はすべて `TASK-2DER` で始まらず全件 列挙されている ∴ **どちらも正しく母数が違うだけで、受入1 は前後とも同じ計器なので影響しない**。**IMPL が自分の計器の絞り忘れとして書き、2DER の欠陥ではないと切り分けたのも正しい**。★**しかしこれで終わらせない（D-73 に効く）**——**母数が2つ在ることが分かった以上、155 も表示の母数として正しいのかを問う必要が在る**。MGR は D-70 の報告で「**`_active_2der_tasks` は COMPLETE と BLOCKED を除いた `TASK-2DER*` を返す**」と受け取っており、**もしそうなら表示の集合に `BLOCKED` が入っていないのに、我々の5行には「停止中 2件（＝`BLOCKED`）」が入っている** ∴ **「和が合計と一致する」は母数を決めない限り自動では成立せず、区分の1つが母数の外から数えられている可能性が在る**。★**裁定=母数を1つ決め画面に書く**=(1)**`_active_2der_tasks` が実際に返す集合を実測する**（件数／`BLOCKED` は入るか／`COMPLETE` は入るか。**推測しない**）(2)**表示の母数を1つに決める**（MGR の推奨は「**いま画面が出している集合そのもの**」＝置き換えであって別のものを新しく出すのではない）(3)**その定義を画面に1行で書く**（文言は CC-α）(4)**区分はその母数の中だけで数える**（そうすれば和は必ず合計と一致する）(5)**母数から外れる区分が出たら「0件」と書かず「この一覧の対象外」と書く**（**停止中がそうなる可能性が在る**）——**今日ずっとやってきた「0」と「対象外」を分ける形である**。★**D-76 は閉じてよい**（受入は満たされており、**§3-1 を CC-α が確認したら MGR が commit する**。**D-73 の再開は §3 が決まってから**）。★**記録=今日、同じ形が5つの層で出た**——**文言（画面）→記法（`【伝聞】`）→欄（`path`）→名前（`claude_barrier`）→母数（155/160）**で、**毎回「1つの言い方が2つのものを指していた」** ∴ **規則にせず、次に何かを数えるときは母数を先に書く。それだけである**。*
