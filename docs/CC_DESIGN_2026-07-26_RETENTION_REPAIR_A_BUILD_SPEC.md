# BUILD SPEC — (a) retention 補修: 発話記録に「直前発話」と「実時刻」を書く

- **`BUILD_ROLE: ★実装源`**（本 build task の唯一の実装源。**IMPL はこの1本だけから作る**）
- 宛: IMPL（coder） / 写: MGR / Taka
- 発: 設計/監査(CC-α, DESIGN) / 2026-07-26 / TYPE=BUILD_SPEC / v1.0
- 典拠: **`CC_MGR_2026-07-26_RETENTION_REPAIR_A_GO.md`**（Taka「(a) に GO」・**この file が正典。memory は正典でない**）
- 統合元（`BUILD_ROLE: 参照`）: `..._BINDER_CONTEXT_ADJUDICATION_FINDING.md` §3(a) / `..._BINDER_REAL_CONTEXT_NOT_MEASURABLE_STATUS.md`

## 0. 表記規約
**【監査:CC-α】** / **【監査:IMPL】** / **【設計:○○】** / **【伝聞】** / **【未確認】**。**自分以外の名前が付いた【監査】は、自分にとっては【伝聞】。**

## 1. なぜやるのか（一行）
**P2（参照の前提）の束縛先チェックが、実データで検証できないから。** 【監査:CC-α】直前文脈を復元する手段は**4つとも存在しない**（`preceding_utterance_ref` 0/459 / ts ユニーク98/730 / `conversation_id` は `taka-main` に377件の粗い箱 / `DIALOGUE_EVENT` の `utterance_refs` は414件すべて長さ1）。
**これは実装漏れではなく記録の規律の穴である。** 本日立てた原則: **自分を検査する能力は、自分が記録した範囲を超えない。**

## 2. スコープ（★小さい。機能追加ではない）
### (1) 発話記録時に「直前発話 id」を書く
- 発話を記録する経路で、**同一 `conversation_id` の直前発話の id** を `preceding_utterance_ref` に入れる。
- **【監査:CC-α】新しい情報源は要らない**: `phase0.utterances()` が記録経路で既に使われている（`twoder/submit.py:151` を実読）。**その場で決定論的に求まる。**
- **会話の先頭なら `null`**（無理に埋めない）。

### (2) 実 ts を渡す・既定に落ちた事実を記録する
- **【監査:CC-α】配管は既存**: `submit(..., ts=None)` で受け取り、渡されなければ `"2026-07-11T08:00:00"` に既定する（`twoder/submit.py:85,90` を実読）。**実 ts を渡す口はある。**
- **既定値に落ちた場合、その事実を記録に残す**（例: `ts_source: "CALLER" | "DEFAULT"`）。**後から「実時刻か既定か」を見分けられるようにする。**
- **既存の ts 値は書き換えない。**

### (3) 前向きのみ
- **過去は救えない。** 既存 459 件は復元しない。**BUILT に明記すること。**

## 3. ★やってはいけないこと
- **既存レコードの遡及書き換え禁止。**
- **推測で `preceding_utterance_ref` を埋めない**（行順が近いだけの無関係な発話を入れない）。**求まらなければ `null`。**
- **P2 ゲートへの配線はしない**（`submit.py` が `preflight_gate.detect()` に `context` を渡す変更は **Build 2 の scope**）。本 SPEC は**記録を直すだけ**。
- **既存 LIVE 挙動を変えない**（記録フィールドの追加のみ）。

## 4. 受入
1. **【最優先】既存 LIVE の非回帰**: `twoder/regression/test_preflight_gate.py` **13/13 PASS**。他の既存テストも通ること。
2. **新規発話で `preceding_utterance_ref` が埋まることを実証**（会話の先頭は `null`・2件目以降は直前 id）。**decision は決定論**（同じ入力・同じ台帳状態なら同じ値）。
3. **`ts_source` が `CALLER` / `DEFAULT` を正しく区別することを実証。**
4. **既存 459 件が1件も変わっていないことを実証**（差分ゼロ）。
5. **母数の記録**（本日の裁定）: 生データ件数 / 機械生成除外件数 / dedup 後件数を報告。
6. `--check` GREEN（決定論再現）。
7. cross-repo: **`ds` / `twoder` に跨る可能性がある。触った repo をそれぞれ commit + push。**
8. commit=Taka。DE は front door `record_de` + `CLAUDE_CODE` 開示。

## 5. 測定（この SPEC で「効いた」と言えること／言えないこと）
- **言える**: 「今後の発話に直前 id と実時刻が入るようになった」。
- **言えない**: 「P2 が実データで効く」。**それは記録が貯まってから初めて測れる。** 本 SPEC 完了時点では **1件も貯まっていない**のが正常。
- **【設計:CC-α】** 貯まった後に P2 の束縛先条件を実 context で測り直す。**それは別スライス。**

## 6. 規律
- **前向きのみであることを隠さない。**
- **「動いた」と書かない**（記録が入ることと、P2 が効くことは別）。
- 数値を出す時は **dedup 済みか明記**。**LLM を挟む比較は対にする**（本件は決定論なので該当しないはずだが、混ぜたら明記）。
- ★3 本線は止めない。

---
*BUILD SPEC v1.0（★実装源）。記録の規律の補修であって機能追加ではない。前向きのみ・遡及書き換え禁止・推測で埋めない・P2 への配線はしない（Build 2）。「記録が入る」と「P2 が効く」を混ぜない。*
