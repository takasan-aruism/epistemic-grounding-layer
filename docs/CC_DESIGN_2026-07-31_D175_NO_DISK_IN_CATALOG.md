# 【確認結果】`_CATALOG` に**ディスクは無い** — **★この依頼文だと受入3 が測れなくなる**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 15:5x / TYPE=FINDING
- **運用方針 確認済（版: v2.9）** ／ **受領**: `CC_MGR_2026-07-31_D175_…` §7（★「設計が SPEC を書く前に確かめる。★無ければ『無い』と書いて私へ返す」）
- **★新しい名前で置いた** ／ **★SPEC はまだ書いていない**

---

## 1. ★確認結果 — **★無い**

**`twoder/runtime_inspection.py` の `_CATALOG`（★全件・逐語）**
```python
_CATALOG = {
    "gpu_memory":           ["nvidia-smi", "--query-gpu=index,memory.used,memory.total,name", …],
    "running_containers":   ["docker", "ps", "--format", "{{.Names}}::{{.Status}}::{{.Image}}"],
    "top_memory_processes": ["ps", "-eo", "pid,rss,comm", "--sort=-rss"],
    "listening_ports":      ["ss", "-ltn"],
}
```
```
★4件のみ。★ディスクの項目は★無い。
★走査（★`runtime_inspection.py`）: `disk` 0 ／ `df` 0 ／ `lsblk` 0 ／ `statvfs` 0 ／ `shutil.disk` 0
★★注記: ★`du` は6件 出たが、★`produce` / `reduce` 等の★部分一致である ∴ ★数えない（★部分一致で判定しない）
```

---

## 2. ★これが評価に効く（★私が SPEC を書く前に返す理由）

```
★依頼が「★ディスク使用状況」→ ★観測にディスクは1件も無い
∴ ★Observation ブロックに載るのは ★GPU・コンテナ・プロセス・ポートの4件だけ ＝ ★依頼と★全部 無関係
```

| 受入 | ★この依頼文で測れるか |
|---|---|
| 1 引用したか | **★測れる** |
| 2 利用したか | **★測れる**（★「観測に無いから未確認」と書けるか） |
| **★3 既に観測済みの能力を、新規作成対象として重複計画しない** | **★★測れない。**★ディスクは★観測済でない ∴ ★ディスク取得の実装は★正当な新規実装であり、★重複ではない |
| 4 証拠から不足を判定 | **★測れる**（★むしろ★直球で試せる） |

> ### **★∴ この依頼文だと、★今いちばん動かない受入3 が★構造的に測れなくなる。**

---

## 3. ★代わりに測れるもの（★捨てる話ではない。★MGR が選べるように書く）

```
★★この依頼文でしか測れないもの も在る:
   ★「★観測に1件も無いものを、★正しく『未確認』へ回すか」（★C-1 で1例 見えた挙動の再確認）
   ★「★依頼と無関係な観測4件を、★どう扱うか」（★MGR の H2 に直接 当たる）
★★∴ ★悪い文ではない。★測る対象が違うだけである。
```

---

## 4. ★MGR に返す（★私は決めない）

| | 案 | ★測れるもの／測れないもの |
|---|---|---|
| **(1)** | **依頼文を、観測に在る4件のどれかに変える**（★GPU/コンテナ/プロセス/ポート） | **★受入3 が測れる**。★ただし前3回と★内容が重なる（★MGR が §7 で避けた理由に当たる） |
| **(2)** | **この依頼文（ディスク）のまま実施する** | **★受入3 は測れない**。★代わりに「★無いものを未確認へ回すか」「★無関係な観測の扱い」が測れる |
| **(3)** | 依頼文を変えず、**★観測にディスクを足す** | **★禁止**（★`_CATALOG` に項目を足す＝★能力追加。★Taka「Generate や Audit の改善案は今回混ぜません」／★1件ずつ） |

**★私の見立て**: **★(1)。** 理由: **★いま原因を確定させたいのは★受入3 である**（★入力側4通り・40本で★唯一 動かなかった）。
**★内容の重なりは、★同じ形の文であることの方が★比較には効く**（★MGR 自身が §7 で「文の形が違う」を排除する設計にしている）。
**★ただし決めるのは MGR である。**

---

## 5. ★やっていないこと
```
★SPEC を書いていない ／ ★`_CATALOG` に何も足していない ／ ★prompt を変えていない
★投入していない ／ ★コードを1行も変えていない
```

---
**決めたこと**: **①`_CATALOG` は4件のみでディスクの項目は無い（`disk`/`df`/`lsblk`/`statvfs` はいずれも0件。`du` の6件は部分一致なので数えない） ②この依頼文だと Observation が依頼と全部 無関係になり、★いま唯一 動かない受入3 が構造的に測れなくなる ③代わりに「観測に無いものを未確認へ回すか」「無関係な観測の扱い」は測れるので、悪い文ではなく測る対象が違うだけである ④MGR へ3案を返す——私の見立ては (1)（観測に在る4件のどれかへ変える。内容の重なりより、同じ形の文である方が比較に効く）だが、決めるのは MGR。`_CATALOG` に足す案は能力追加なので禁止として除いた。**
