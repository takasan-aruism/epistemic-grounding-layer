# 【A】実装 → 設計/監査: **★訂正3件——①私の「届いている」の根拠が弱かった ②1走行は 8回でなく★7回 ③投入されているのは submit だけではない**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: v2.8）**
- **契機**: `CC_DESIGN_2026-07-30_D106_I_SUBMITTED_AND_I_SAID_I_DID_NOT.md` §1（★「1回の走行で8回以上」）
- **★実装していません。★投入していません**（★本文書のために1回も走らせていません）。**★log を読んでいません。★台帳を直読していません。**
- **★遡って D-105 を書き換えません。★この1本で前向きに記録します**（MGR D-105 §3 の作法）。

## 1. ★訂正①（★私の落ち度）——`exit 0` は「届いた」を意味しません
```
私が D-105 §3 に書いたこと:
  「★7走行すべてで両テストが exit 0 ∴ ★POST は届いている」
  「（test_dead_approach_guard は…skip では通りません）」   ← ★ここが誤りです
```
```
実測（★ソース）:
  test_concurrency_and_run_gate.py:31-33
      try: call("/api/tasks", timeout=5)
      except Exception: print("(skipped: server unreachable…)"); ★sys.exit(0)
  test_dead_approach_guard.py:36-37 相当
      except Exception: print("  (live skipped: …)")  → resp is None → ★live の判定を R に足さない
      :51  sys.exit(0 if all(R) else 1)   ← ★オフラインの判定だけで 0 になる
```
| ★∴ | **★両テストは、★サーバに届かなくても `exit 0` を返します** |
|---|---|
| **★私が書いた「skip では通りません」** | **★誤りです。★skip でも通ります** |

### 1-1. ★では何が証明できているか（★弱めずに書き直します）
| ★証明できている | **★7件**（★12:54 の1走行）。**根拠: `recv_count` 60 → 67 と `last_recv_at 12:53:07` が★走行の中に在る** |
|---|---|
| **★証明できていない** | **★残り 42件**（★他6走行ぶん）。**★到達したかを私は測っていません** |
| **★状況証拠は在る** | ★各 build で私は同じ時間帯に配信物を取得しています（★各 BUILT に記録）。**∴ サーバは動いていました。★しかし「その POST が届いた」の証明ではありません** |

```
★D-105 の「少なくとも49件」を、★次のように直します:
  ★証明できる    : ★7件
  ★計算上の見込み: ★49件（★7走行すべてが到達していれば）
  ★「少なくとも49件」とは★もう書きません
```
> **★これは「ソースに在る≠動く」を、★私が自分で破った形です。**
> **★私は exit 0 という★1つの数を、★2つの意味（★通った／★届かず skip した）で読みました。**
> **★今日ずっと潰してきた形です。**

## 2. ★訂正②——1走行の `/api/submit` は **8回でなく 7回**です
```
実測（★ソース・★行ごと）:
  :38  call("/api/submit", …)                       → ★1回
  :46  call("/api/submit", …)                       → ★1回
  :52  inputs = 4件
  :55  call("/api/submit", …)  ← ★worker の中。★スレッド4本 ∴ ★4回
  test_dead_approach_guard.py:31  Request(".../api/submit")  ← ★ループ無し ∴ ★1回
  ─────────────────────────────────────────────
  ★合計 1 + 1 + 4 + 1 = ★7回
```
| **★D-106 の「8回以上」** | **★`call("/api/submit"` の★箇所数3 に★スレッド4 を足した形と一致します**（★:55 が二重に入っています） |
|---|---|
| **★7回であることの裏付け** | **★`recv_count` の +7**（★1走行の前後で測った実測。★8 なら +8 になります） |

```
★D-106 の「私の POST は16回以上」→ ★14回（★2走行 × 7）が計算上の見込みです。
★ただし §1 と同じ理由で、★到達の証明は別です。
```
> **★私は数を下げに来たのではありません。** **★合わない数を残すと、次の判断が狂います。**

## 3. ★訂正③（★追加の事実）——投入されているのは `/api/submit` だけではありません
```
実測（★同ファイル・★call は body があれば POST）:
  :39  call("/api/run_next?task_id=TASK-2DER-GPU-SWITCH-001", {})        ← ★POST
  :43  call("/api/run_until_barrier?task_id=TASK-2DER-GPU-SWITCH-001",{})← ★POST
  :47  call("/api/run_next?task_id=TASK-2DER-GPU-SWITCH-001", {})        ← ★POST
```
| ★1走行の本番 POST | **★10回**（★`/api/submit` 7 ＋ ★実行系 3） |
|---|---|
| **★`recv_count` に入るのは** | **★7回だけ**（★`_recv_log` は `/api/submit` にしか置かれていません） |
| **★∴ `recv_count` は「本番へ出した POST の数」でもありません** | **★submit だけの数です** |

### 3-1. ★ただし実行系は「断られること」を確かめています（★過大に書きません）
```
:41  ck("2 GPU task NOT advanced by run_next (no state transition)", gpu_regen() == before)
:50  ck("5 GPU still not advanced", gpu_regen() == before)
```
> **★実行系3回は、★実在の task（`TASK-2DER-GPU-SWITCH-001`）に向いています。**
> **★しかしテストは「★断られること」「★task が進まないこと」を判定しています。**
> **★∴ 「非回帰が本番の task を進めている」とは★書きません。** **★判定が通っている限り、進んでいません。**
> **★何の記録が残るかは、★私は言えません**（★台帳を読めません。★D-106 §3 と同じ）。

## 4. ★私が確かめていないこと
1. **★他6走行が到達したか**（§1-1）。**★確かめる手段は log で、★読みません。**
2. **★残り17件以下が何か**（★D-105 のまま）。**★MGR は「見張るだけ」と裁定しました。★追っていません。**
3. **★DS / RRI / EGL / DW に何件の記録が増えたか。** **★台帳を読めません ∴ 出せません。**
   **★「増えていない」とも書きません。**

## 5. ★私が変えていないこと（★裁定に従います）
- **★非回帰の POST 先を変えていません**（MGR D-105 §4「★テストの POST 先を変えない」）。
- **★新しい欄も口も作っていません。**
- **★以後 `recv_count` を典拠に使うときは「★テスト走行が混ざる」と併記します**（MGR D-105 §4）。
  **★さらに「★submit だけの数である」も併記します**（§3）。

---
*IMPL FINDING（D-107・【A】。★実装せず・投入せず・log を読まず・台帳を直読せず。★遡って D-105 を書き換えず この1本で前向きに記録する）。★**訂正①（私の落ち度）＝`exit 0` は「届いた」を意味しない**——`test_concurrency_and_run_gate.py:31-33` は到達不能時に **`sys.exit(0)`**、`test_dead_approach_guard.py` は live 部を skip して **オフラインの判定だけで `sys.exit(0)`** になる ∴ **両テストはサーバに届かなくても exit 0 を返し、私が D-105 §3 に書いた「skip では通りません」は誤りである** ∴ **証明できているのは7件（12:54 の1走行。`recv_count` 60→67 と `last_recv_at 12:53:07` が走行の中に在る）／証明できていないのは残り42件（他6走行）／状況証拠として各 build で同時間帯に配信物を取得しているのでサーバは動いていたが「その POST が届いた」の証明ではない** ∴ **「少なくとも49件」とはもう書かず「証明7件／計算上の見込み49件」に直す**——**これは「ソースに在る≠動く」を私が自分で破った形であり、`exit 0` という1つの数を「通った／届かず skip した」の2つの意味で読んだ（今日ずっと潰してきた形）**。★**訂正②＝1走行の `/api/submit` は8回でなく7回**（`:38` 1回 ＋ `:46` 1回 ＋ `:55` が worker の中でスレッド4本ぶん4回 ＋ `test_dead_approach_guard.py:31` ループ無し1回 ＝ **7回**）——**D-106 の「8回以上」は `call("/api/submit"` の箇所数3にスレッド4を足した形と一致し `:55` が二重に入っている**、**7回である裏付けは `recv_count` の +7 実測（8なら +8 になる）** ∴ **D-106 の「16回以上」は計算上14回（2走行×7）だが到達の証明は別**——**数を下げに来たのではなく、合わない数を残すと次の判断が狂うため**。★**訂正③（追加の事実）＝投入されているのは `/api/submit` だけではない**——`call` は body があれば POST であり `:39`/`:47` が `/api/run_next`、`:43` が `/api/run_until_barrier` を**実在の task `TASK-2DER-GPU-SWITCH-001`** に対して POST する ∴ **1走行の本番 POST は10回（submit 7 ＋ 実行系 3）で、`recv_count` に入るのは submit の7回だけ（`_recv_log` は `/api/submit` にしか置かれていない）∴ `recv_count` は「本番へ出した POST の数」でもなく submit だけの数である**——**ただしテストは `:41`/`:50` で「断られること」「task が進まないこと」を判定しているので「非回帰が本番の task を進めている」とは書かない（判定が通っている限り進んでいない）。何の記録が残るかは台帳を読めないので言えない**。★**確かめていないこと**=他6走行が到達したか（手段は log であり読まない）／残り17件以下（MGR 裁定「見張るだけ」に従い追っていない）／**DS/RRI/EGL/DW に何件 記録が増えたか（台帳を読めないので出せず「増えていない」とも書かない）**。★**変えていないこと**=**非回帰の POST 先を変えていない**（MGR D-105 §4）／新しい欄も口も作っていない／**以後 `recv_count` を典拠に使うときは「テスト走行が混ざる」＋「submit だけの数である」を併記する**。*
