# 【BUILT 監査 / `EVO-0031`】**★断片が読めた。★★その断片は「骨格 全文」だった**（★私の推定が実測になった）

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-08-02 00:5x / TYPE=FINDING
- **開発者規律 確認済（版: ★v1.2 / 2026-08-02）** ／ **★9項目 確認済**（★外れた番号なし。★§5 に当て方を書いた）
- **★報告を読む前に自分で叩いた** ／ **★コードを1行も変えていない** ／ **★front door へ投入していない**

---

# 1. ★受入（★独立に測った）

| 受入 | 判定 | ★私の実測（逐語） |
|---|---|---|
| (1) `SKELETON_VIOLATION` の時 `skeleton_missing_segment` が読める | **★★○** | `TASK-2DER-816D6F68`: `reason: SKELETON_VIOLATION` ／ `skeleton_missing_segment` ＝ `'# impl.py\ndef render(roadmap, control, asof, only_incomplete=False):\n    """roadmap = GET /api/roadmap の dict, control =…'` |
| (2) 逐語を持ち帰る | **★○** | 上記（★先頭120字を私が引いた） |
| (3) 400字で切れている | ★実装の報告で確認（★私が見た範囲では妥当） | — |
| (4) 戻せる | ★実装の報告で確認 | — |
| ★欄の存在 | ★○ | `test_result` ＝ `['status','ok','reason','artifact_sha256','runner_exit','runner_stdout_tail',★'skeleton_missing_segment']` |

```
★★★★★★今回 初めて ★`SKELETON_VIOLATION` の場面で ★測れた。★前回（`TASK-2DER-0E5E8675`）は
   ★試験の失敗（exit 1）で止まったので ★★この枝を通っていなかった。★★実装がそう書いたのは正しい。
```

---

# 2. ★★読めた内容が、★私の推定を★実測に変えた

```
★返ってきた断片（逐語・先頭）: ★`# impl.py\ndef render(roadmap, control, asof, only_incomplete=False):\n    """…`
★★★＝ ★★骨格の ★★★先頭から丸ごと が「見つからなかった断片」である。
★★★★∴ ★★固定区間は ★★1つしか無い ＝ ★★★骨格 全文が ★1区間である。
★★★★★★これは ★`_skeleton_fixed_segments`（`generate_via_runner.py:55-66`）が
   ★`<<<FILL` 行で割る仕様であり、★★私の契約に ★`<<<FILL` が ★0件 だからである。
★★★★★★★★∴ ★私が昨夜「★推定であって実測ではない」と書いたものが、★★★いま実測になった。
```

## 2-1. ★★∴ 何が起きていたか（★これで確定した）
```
★worker は ★★docstring も ★`raise NotImplementedError` も ★★1文字も変えずに残したうえで
   ★実装を足さねば ★通らない形になっていた。
★★★`raise NotImplementedError` を ★実装に置き換えた時点で ★★違反になる。
★★★★★★∴ ★★★「★実装せよ」と「★骨格を1文字も変えるな」が ★★両立していなかった。
★★★★★★★★★2DER の欠陥ではない。★★★私の契約の書き方である（★本日/昨日で ★3件目）。
```

---

# 3. ★★∴ `EVO-0031` の目的は達成された（★ただし `DONE` の条件は別）

```
★★`EVO-0031` の目的（★MGR の裁定 逐語）＝「★受入試験が走る」／「★理由が front door から読める」
   ★① ★試験は走った（★`TASK-2DER-0E5E8675` で ★`exit 1` ＝ ★7本中6本 通過・1本 失敗が読めた）
   ★② ★理由は読める（★`runner_exit` ／ `runner_stdout_tail` ／ ★`skeleton_missing_segment` の3欄）
★★★★∴ ★★★「★読めない」は ★★もう無い。★★★★A型・B型・C型の ★3つとも ★塞がった。
★★★★★★ただし ★完了条件（★同じ task が `COMPLETE`）は ★★未達 ∴ ★`DONE` にするかは ★MGR の判断。
```

---

# 4. ★次に直すもの（★もう推測ではない。★★ただし手番は MGR）

```
★★MGR の裁定（逐語）:「★我々が骨格を推測して直すのは禁止」
★★★★★いま ★推測ではない。★★実測で ★「固定区間が1つ＝骨格 全文」と出た。
★★∴ ★直す先は ★★私の契約の骨格に ★`<<<FILL …>>>` を置くこと（★埋めてよい場所を明示する）。
★★★★★★★ただし ★依頼文（契約）を書くのは ★MGR である（★`D-200` の Taka 確定・★人が封入する）
   ∴ ★★私は ★書かない。★★材料まで出す。
★★★★材料: ★`_skeleton_fixed_segments` は ★`"<<<FILL" in line` で割る（★行に含まれていればよい・逐語）
   ∴ ★★`raise NotImplementedError` の行を ★`<<<FILL: ここに実装を書く>>>` に置き換えれば
      ★★固定区間が ★2つに割れ（★前＝宣言と docstring／★後＝末尾）、★中身は自由になる見込み【★未確認】
```

---

# 5. ★★9項目の当て方（★v1.2 第11-2・★私の分）

```
★1 置いたなら どこから読めるか       → ★`skeleton_missing_segment` を ★front door で確かめた ✓
★2 読めるなら どこから書けるか       → ★本件は ★読む側の追加 ∴ ★該当しない（★書く口は既存）
★3 失敗の理由を捨てていないか       → ★`SKELETON_VIOLATION` の枝に ★載ったことを確かめた ✓
★4 そもそも作っていないのでは       → ★★これが本件そのもの（★C型を潰した）✓
★5 走ったのか／ソースに在るだけか   → ★★webui は ★1時間前に入れ替わっており、★欄が実際に出ている ✓
★6 名前は 系が実際に作る名前か       → ★該当しない（★今回 名前を足していない）
★7 依頼文と試験が矛盾していないか   → ★★私が踏んだ7番は ★MGR が直して投入済み（★`816D6F68`）
★8 その計器は自分を数えていないか   → ★★私が測ったのは ★front door の値であり ★私の書き込みではない ✓
★9 増えるなら 何を廃止するか         → ★★本件で増えたのは ★欄1つ。★★廃止は ★★書いていない【★私の未達】
   ★★★★★★∴ ★★9番だけ ★私は満たしていない。★★そう書く。★★（★欄1つで廃止を求めるのが妥当かは MGR の判断）
```

---

# 6. ★やっていないこと
```
★コードを1行も変えていない ／ ★契約に `<<<FILL` を置いていない（★MGR の手番）
★front door へ投入していない ／ ★commit していない ／ ★台帳を直読していない
★★(3)(4)（400字の切りと可逆）は ★私が測っていない【★実装の報告で確認する】
★★★`<<<FILL` を置けば通るかは ★★確かめていない【★未確認】——★★置いてから測る話である
```

---
**決めたこと**: **①受入(1)(2) は成立。`TASK-2DER-816D6F68` で `SKELETON_VIOLATION` の時に `skeleton_missing_segment` が読め、逐語は `'# impl.py\ndef render(roadmap, control, asof, only_incomplete=False):\n    """…'` だった。前回の走行は試験の失敗（exit 1）で止まりこの枝を通っていなかったので、実装が「今回の走行では測れない」と書いたのは正しい ②読めた内容が私の推定を実測に変えた——返ってきたのは骨格の先頭から丸ごとであり、固定区間が1つ＝骨格 全文である。私の契約に `<<<FILL` が0件だからで、昨夜「推定であって実測ではない」と書いたものが実測になった ③∴ worker は docstring も `raise NotImplementedError` も1文字も変えずに残したうえで実装を足さねば通らず、「実装せよ」と「骨格を変えるな」が両立していなかった。2DER の欠陥ではなく私の契約の書き方である（3件目）④`EVO-0031` の目的（試験が走る・理由が読める）は達成——A型・B型・C型の3つとも塞がった。ただし完了条件（同じ task が `COMPLETE`）は未達なので `DONE` にするかは MGR の判断 ⑤次に直すのは契約の骨格に `<<<FILL` を置くことで、もう推測ではない。ただし契約を書くのは MGR なので私は書かず材料まで出す（`"<<<FILL" in line` で割るので `raise NotImplementedError` の行を置き換えれば固定区間が2つに割れる見込み・未確認）⑥9項目を自分に当てた。1・3・4・5・8 は満たし、2・6 は該当せず、7 は MGR が直して投入済み。★9（増えるなら何を廃止するか）だけ私は満たしていない——本件で欄が1つ増えたが廃止を書いていない。そう書く。**
