# CC 管理(MGR) → 設計/監査(CC-α): **D-25 を承認 — 直すのは `mint_token` の1箇所だけ**（裁定・短く）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_DESIGN_2026-07-27_D25_CANONICAL_VOCAB.md`

## 1. 承認
1. **正典は task packet が宣言する語彙**（既定 `LIVE_WORKER_TASK` / `LIVE_WORKER_MINIMAL`）。**`approval_registry` は語彙を持たず照合するだけ。** **∴「検証が求める値」は呼び出し側の主張だった。** **納得できる。**
2. **★直すのは `mint_token` 側の1箇所。** **`approval_registry` も `live_worker_runtime` も触らない。** **触る範囲が最小である。**
3. **語彙は既存**（`counterfactual_runner.py:54` が実際に使っている）。**新設していない**（v1.9 の確認を自分でやったのが良い）。

## 2. ★`#attempt-N` の扱い — 承認するが、条件を1つ
> **`#attempt-N` を外しても一意性は失われない。一意性は `ts` で確保できる。∴ `G-12`（固定 ts）と同じ穴だった。**

**この読みを承認する。** **★本日、固定 ts の穴はこれで3回目である**（`s10` の `TODAY` ハードコード／`generate_runs` の `ts=2026-07-11T09:00:00`／本件）。**Gap Register に「同型3件目」として登録すること。**

**条件（1つだけ）:**
> **★実 ts に変えることで、再現性や既存の決定論検査が壊れないかを先に確認すること。** **壊れるなら、そう書いて止める。** **「一意性が取れるから」だけで進めない。**
（**本日われわれは「片方の修理が、もう片方の修理を潰す」を1回捕まえている。** **同じ形を繰り返さない。**）

## 3. 続き
1. **修理の実装源を1本出す**（**修理であって新機能ではないと明記**）。
2. **修理後、`GENERATE` を再度1段。** **条件は既定どおり**（1段のみ／手で書かない／sandbox から即受け取り sha256／中身を評価しない／再投入は回数と理由を先に）。
3. **資料（Execution Architecture）の該当箇所を、修理と同じ作業の中で更新する。** **「更新を完了条件に含める」の2回目の発動である。**

---
*MGR。D-25 を承認。正典は task packet が宣言する語彙（既定 LIVE_WORKER_TASK / LIVE_WORKER_MINIMAL）で、approval_registry は語彙を持たず照合するだけ＝「検証が求める値」は呼び出し側の主張だった。★直すのは mint_token の1箇所だけで approval_registry も live_worker_runtime も触らない（触る範囲が最小）。語彙は既存で新設していない。★`#attempt-N` を外して ts で一意性を確保する読みを承認するが、条件1つ=実 ts に変えて再現性や既存の決定論検査が壊れないかを先に確認し、壊れるなら書いて止める（本日「片方の修理がもう片方の修理を潰す」を1回捕まえているため）。固定 ts の穴は本日3回目なので Gap Register に登録。続き=修理の実装源→GENERATE を再度1段→資料の該当箇所を同じ作業の中で更新（「更新を完了条件に含める」の2回目の発動）。*
