# 【BUILD SPEC】**★規則を prompt に1文 書く**（★1箇所）— ★物差しと語彙表は**先に**公開する

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 19:0x / TYPE=BUILD_SPEC
- **運用方針 確認済（版: v2.9）** ／ **裁定**: `D-180`（★受入3 の採点を止める／★prompt に書く／★文言は設計）
- **正典**: `TAKA_2026-07-31_NO_BLAMING_QWEN_AUDIT_OUR_OWN_QUESTION.md` §7・実施順序5
- **★新しい名前で置いた** ／ **★2DER の外ではない**（★本番経路への投入を含む）

## 0. ★これは何をする1件か（★取り違えない）
```
★★「★Qwen に効く指示を足して当てにいく」★ではない。
★★★「★我々が採点していた規則が prompt に無かった」ので、★★規則を書いて対等にする、である。
★★∴ ★書いた後の1回で「効いた/効かない」は★言わない。★言えるのは「★規則が渡った」までである。
```

---

# 1. ★やること（★1箇所・2行）

> ### **★`twoder/build_planner.py` の `_observation_facts` の返り値に、★規則を1文 足す。**

**★足す位置（★逐語で示す。★ここ以外に触らない）**
```python
    if not blocks:
        return ""
    blocks.append(<★§2 の文言>)      # ←★この1行だけ足す
    return "\n\n".join(blocks) + "\n\n"
```
```
★★この位置にする理由: ★観測が1件も無ければ `blocks` は空で ★早期 return する
   ∴ ★観測が無いときに規則だけが出ることが★構造的に起きない（★fail-safe）。
★★★`_plan_prompt` 側には足さない（★観測と規則を離すと、★また平叙文が孤立する）
```

| 条件 | |
|---|---|
| **★触らない** | **`STRUCTURED_KEYS` / `EXECUTABLE_KEYS` / `_plan_prompt` の欄定義 / `validate_plan`** |
| **★語の禁止** | **`gpu`/`nvidia`/`port`/`docker`/`disk`/`memory`/`ps`/`ss` を diff に0件**（★大小無視・打ち切り無しで示す） |
| **★差分の大きさ** | **★1ファイル・1 hunk・★挿入1行**（★§2 の文字列は1行に収める。★折り返しは可） |

---

# 2. ★文言（★逐語。★打ち直さない）

```
RE-ACQUISITION RULE: You MUST treat every item listed above as already obtained. Do NOT create work whose purpose is to obtain those items again. Obtaining something that is genuinely absent from the lists above is expected and is part of the task; if an already-obtained item is returned as a side effect of doing so, that is acceptable and MUST NOT be counted as new work.
```

## 2-1. ★要件との対応（★`D-180` §4）
| 要件 | 満たし方 |
|---|---|
| ① 目的とした再取得を禁じる | `Do NOT create work whose purpose is to obtain those items again` |
| ② **副作用として返るのは可** | `returned as a side effect of doing so, that is acceptable` |
| ③ 強制語 | **★MUST / Do NOT / MUST NOT ＝ ★3件**（★現在 Observation 側は 0件） |
| ④ 1箇所・最小差分 | ★§1 のとおり |
| ⑤ 主題語を入れない | ★§4 の走査で示す |

## 2-2. ★★指示からの逸脱を1件 申告する（★MGR の裁定を仰ぐ）
```
★★`D-180` §4-③ は「★強制語を★1つ 付ける」である。★私は★3つ にした。
★★理由: ★②に強制語を付けないと、★②は平叙文になる。
   ★`G-93` の実測では ★平叙文の Observation は★構造上いちばん弱く、★38本で値の引用は 2件だった。
   ∴ ★②が弱いままだと ★①だけが効き、★依頼文（「取得できるようにする方法を検討せよ」）と★正面衝突する
   ＝ ★`D-180` §4 が「★衝突を作らずに規則を足す」と定めた条件を★満たさなくなる。
★★★これは私の判断である。★却下されたら ★MUST を1つに削る（★②を平叙文にする）。★先に書いておく。
```

---

# 3. ★物差しと語彙表（★★測定より前に固定し、ここに公開する。★測定後に足さない）

## 3-1. ★物差し（★`D-180` §5-2 の案3）
```
★母数 = ★計画が自ら `already_satisfied` に挙げた★項目 × ★その項目に現れた★道具名 ＝ ★「対」
   ★★前回38本での母数は ★109対（★項目単位で数えると 82。★対で数える方を採る）
★判定 = ★成果物側（`files_expected` / `steps` / `objective` / `requirement`）で、
        ★その道具名の★前後160字に ★§3-2 の語が★1つも無ければ ★×
★★★「命令の重複」では判定しない（★D-180 §2 の裁定）
```

## 3-2. ★語彙表（★★これで確定。★英語と日本語の両方を必ず当てる）
```
★英: missing / additional / extend* / unmonitor* / unobserv* / not available / not captured /
     not observed / gap / beyond / deeper / granular / breakdown / correlat* / mapping /
     per-process / enrich / augment / supplement
★日: 不足 / 不十分 / 欠け / 未取得 / 取得できて / 追加 / 拡張 / 差分 / 詳細化 / 補完 / 対応付け
★★主題語（cmdline / vsz / ppid / -ltnp / compute-apps 等）は ★入れない
   ★理由: ★主題ごとに表を作り直すことになり、★共通基盤の線（⑤）に反する
```

## 3-3. ★語彙表の選び方で数が動く（★先に公開する）
| 語彙表 | 母数 | × | ×が出た本数 |
|---|---|---|---|
| **★一般語のみ（英＋日）★採用** | 109 | **14** | 6/38 |
| 一般語＋主題語 | 109 | 12 | 5/38 |
| **英語だけ（★禁じ手）** | 109 | **16** | 7/38 |
```
★★同じ38本でも ★×は 12〜16 に動く ∴ ★表を先に決めないと、★後からいくらでも動かせた。
★★★「英語だけ」は★私が本日2回 やった誤りである ∴ ★禁じ手として明記する。
```

## 3-4. ★★この数字を「前」として直接 比べない
```
★★×14/109 は ★:8005 単体・A/B/C1/C2 合算・38本 の値である。
★★本番の1回とは ★母数も経路も違う ∴ ★「14 → ?」という★引き算をしない。
★★★本番1回で言えるのは「★この1回、母数いくつのうち×がいくつだった」だけである。
```

---

# 4. ★手順
```
① 修正（★1箇所・挿入1行）
② ★禁止語の走査を★先に出す（★大小無視・打ち切り無し・0件を数字で）
③ webui 再起動（★全件 記録・run-gate 初期化も）
④ ★依頼文は ★MGR が固定する（★D-180 §5-1）。★あなたは打ち直さない
   ★★受領していなければ ★止まって設計へ聞く（★勝手に前回の文を使わない）
⑤ ★字数・sha1・★予告 task_id を★投入前に書く → POST /api/submit ★1回
⑥ ★直後に GET /api/receipt（★他の口を叩く前に）
⑦ POST /api/run_next?task_id=… ★1回 → ⑧ ★PLAN が出たら★止める（★GENERATE へ進まない）
```

---

# 5. ★予告（★投入前に固定。★外れたら「外れた」と書く）
```
★R-1: ★Observation 側の強制語は ★0件 → ★3件 になる                 …確信 高（★決定論）
★R-2: ★分類は `OBSERVE_CURRENT_STATE`                              …確信 高（★前4回と同型）
★R-3: ★`already_satisfied` は★埋まる                               …確信 中（★38/38 だった。★但し文が増えた分は未知）
★R-4: ★×が減るか ＝ ★予告しない                                     …★1回では言えない
★R-5: ★観測「値」の引用が増えるか ＝ ★予告しない                      …★今回の規則は値の引用を求めていない
★★★1回の結果で「直った/効かない」と書かない。★書けるのは「★この1回はこうだった」だけである。
```

---

# 6. ★やってはいけないこと
```
★`STRUCTURED_KEYS`/`EXECUTABLE_KEYS`/`validate_plan` を触る  ★欄を足す・削る
★★語彙表に語を足す（★§3-2 で確定済。★測定後に足すのは★禁止）
★GENERATE を押す  ★Generate/Audit の改善を混ぜる  ★観測の選別に触る（`G-06`）
★Ledger / 図 / (c) の patch に触る（★保留のまま）  ★commit する  ★61本の非回帰を走らせる
★★依頼文を打ち直す  ★同名で文書を差し替える  ★別の欠陥を見つけたことを理由に止まる
```

# 7. ★止まってよい場所
```
★MGR の依頼文が届いていない → ★止めて設計へ聞く
★1箇所・挿入1行で収まらないと分かった → ★止めて設計へ聞く
★★それ以外は止まらない。★2通りに読めたら狭い方を採り、★迷いを1行 書く
```

# 8. ★報告
```
1. 変更した1箇所（★ファイル/hunk/挿入行数・★禁止語0件の走査結果）
2. ★§3 の物差しでの判定（★母数と×を★両方 書く。★×だけ書かない）
3. ★Audit 6欄（★`修正候補のリスク` を空にしない）
4. ★予告 R-1〜R-5 の当否
5. ★2DER 担当工程数の前回差分（★0 なら「0」）
6. ★次に直す1件（★1件だけ）
★★`:8005` は 2DER が内部で呼ぶ。★あなたは直接 叩かない
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---
**決めたこと**: **①`_observation_facts` の `blocks` に規則1文を足す（挿入1行。観測が無ければ早期 return するので、観測なしで規則だけ出ることが構造的に起きない） ②文言は §2 の逐語で、①目的とした再取得の禁止と②副作用として返るのは可を同じ文に入れ、依頼文との衝突を作らない ③MGR の「強制語1つ」に対し私は3つにした——②に強制語が無いと平叙文になり `G-93` のとおり無視されて①だけが効き、依頼文と正面衝突するため。これは私の判断で、却下されたら1つに削る ④物差しは案3（母数＝`already_satisfied` の項目×道具＝対。前回38本で109対）で、語彙表を §3-2 で確定し測定前に公開した——同じ38本でも表の選び方で ×は 12〜16 に動くので、先に決めないと後から動かせた ⑤「英語だけで数える」は本日2回やった誤りなので禁じ手として明記した ⑥×14/109 は単体投入38本の値であり本番1回と母数も経路も違う ∴ 引き算をしない ⑦予告は R-1〜R-3 のみで、×が減るか・値の引用が増えるかは予告しない。**
