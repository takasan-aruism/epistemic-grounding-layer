# 【BUILD SPEC】対照試験 A/B/C — **★同じ Qwen に直接 聞く。★コードを1行も変えない**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 10:0x / TYPE=BUILD_SPEC
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-31_INPUT_DESIGN_NOT_MODEL_RUN_THE_CONTROLLED_TEST.md`（逐語）／**実施**: `CC_MGR_2026-07-31_D163_…`
- **★新しい名前で置いた**（`D-158` §2-2）

## 0. ★私の誤りを先に書く（★Taka の訂正を受ける）
```
★私は「★値を見せても効かなかった」と書いた。★これは Qwen を変数として扱った言い方である。
★★実際には入力側に3つの曖昧さが在り、★私はそれを固定しないまま結論の向きを決めていた。
★★私は `D-160` で MGR の H2（★依頼と無関係な観測まで見せている）を★自分で監査し、
   ★「H2 の前提も実在する」と書いていた。★それを★自分の結論に反映しなかった。
★★∴ 今回の言い方は取り下げる。★正しくは「★渡した情報の意味づけと選別が不十分だった」である。
```

---

# 1. ★これは何か（★位置づけを外さない）
```
★★2DER の外での測定である。★production の経路を通らない。
★∴ ★投入しない ／ ★run_next を押さない ／ ★台帳に書かない ／ ★コードを1行も変えない
★∴ ★2DER の担当工程に ★1件も数えない（★1/8 は動かない）
★★測るのは「モデルが使えたか」ではない。★「★どの入力なら、既知と不足を分けられるか」である。
★★`:8005` を★あなたが直接 叩く（★今回だけ許す。★理由＝2DER を通さない測定だから）。★使ったと1行 書く
★★新しいサーバを立てない（★既存の `:8005` をそのまま使う）
```

---

# 2. ★A の固定（★作り直さない）

```
★A ＝ ★`TASK-2DER-5ABBA4A1` に対して★実際に組まれた planner の prompt。
★作り方: ★`_plan_prompt(goal, provenance)` を、★記録された値で呼ぶ
   goal       = `GET /api/state?task_id=TASK-2DER-5ABBA4A1` の `goal`
   provenance = `GET /api/claude_packet?task_id=TASK-2DER-5ABBA4A1` の `knowledge_packet.provenance`
                （★`measured_state` 21件・`egl_source_refs` 4件を含む）
★★先に確かめること: ★`twoder/build_planner.py` が★あの実行時と同じ状態か
   （★`D-160` の変更は戻されている ＝ `git status` が clean であることを★自分で確認する）
★★再現できないなら「★再生成した」と書く（★正典 §3-(1)）
★★A の全文の★字数と sha1 を書く（★B・C との差が「引き算だけ」であることを後で示せるように）
```

---

# 3. ★B・C の作り方（★引くだけ。★足さない・言い換えない）

| | ★残すもの | ★除くもの |
|---|---|---|
| **B**（メモリに関係する観測だけ） | **★プロセス／RSS に関する観測行と値**（`pid … rss=…`） | **★GPU・コンテナ・ポートの行と値**（★依頼に無関係） |
| **C**（メモリに関係する値だけ・取得コマンドは除外） | **★B の値だけ** | **★B から さらに「★取得手段を示すもの」全部** |

## 3-1. ★C の「取得手段」には★2つ在る（★設計の判断。★これを外すと C にならない）
```
★① コマンド配列    : "acquired by running [\"ps\",\"-eo\",…]"
★② ★取得先(locator): "localhost-runtime://…/ps" ← ★★道具名が埋まっている
★★IMPL が実測で見つけた（`…/nvidia-smi`・`…/docker`・`…/ps`）。★私も引いて確認した。
★★∴ ★C は①と②の両方を除く。★①だけ外して「取得コマンドを除外した」と書かない。
★★これは Taka の曖昧さ②（★値と取得手段の混在）そのものである。
```
## 3-2. ★引き算であることの示し方
```
★A・B・C の★字数と sha1 を3つとも書く
★★B は A の部分集合、C は B の部分集合であることを★機械で示す
   （★B の各行が A に在る／C の各行が B に在る。★語順や語彙を変えていない）
★★1行でも「A に無い行」が B・C に在ったら ★止めて設計へ聞く（★足したことになる）
```

---

# 4. ★問い（★Taka 逐語。★3条件とも同一。★1文字も変えない）

```
この情報だけを使って、
1. 既に確認できていること
2. 未確認のこと
3. 新規実装が本当に必要なこと
を分けてください。
```

---

# 5. ★実行の条件（★MGR `D-163` §3。★増やさない）
```
★各条件 ★3回ずつ（★計9回）。★1回で断定しない
★★model / temperature / seed / max_tokens を★3条件で同一にし、★全部 記録する
   → ★planner が実際に使っている設定を★読み取って、それに合わせる（★あなたが選ばない）
★★出力は★全文 保存する（★要約しない・捨てない）。★保存先は scratchpad（★台帳に書かない）
★★BUILT には ★9本の出力を★全文 貼る（★要約しない）
★★GPU を使う ∴ ★使ったと1行 書く
```

---

# 6. ★判定（★MGR が測る前に固定した。★私は緩めない）
| | ★「分けられた」と書ける条件 |
|---|---|
| 共通 | **出力が ①既知 ②未確認 ③新規実装要 の★3区分になっている** |
| 共通 | **★①に、実際に観測された値が入っている**（★一般語でなく `qwen36_vllm` / `703029` / `30520` 等） |
| 共通 | **★③が空か、①に無いものだけになっている**（★既に取れているものを「新規実装要」に入れない） |

**★1つでも欠けたら「分けられた」と書かない。** **★3回のうち割れたら、★割れたこと自体を書く。**

## 6-1. ★言い方（★先に固定。★言い換えない）
```
★A で分けられなければ  → 「★現在の入力では分けられない」
★B か C で分けられたら → 「★入力を◯◯にすれば分けられる」（★どちらかを名指し）
★どれでも分けられなければ → 「★入力側の3条件では分けられない」
★★どの場合も「★Qwen が使えない」とは書かない
```

---

# 7. ★測り方（★私が本日4回 踏んだ形。★同じ穴に落ちない）
```
★★英語と日本語の両方で数える（★出力が英語で来ることが在る）
★★入れ子まで見る（★最上位キーだけで「無い」と読まない）
★★「全部0」を「きれい」と読まない  ★★部分一致で判定せず、★実物を目視する
★★時刻を渡すときは★現地時刻か UTC かを確かめる
```

# 8. ★やってはいけないこと
```
★コードを変える  ★投入する  ★run_next を押す  ★GENERATE を押す  ★台帳に書く  ★commit する
★新しいサーバを立てる  ★問いを条件ごとに変える  ★B・C に A に無いものを足す
★Ledger / (c) の SPEC と patch / 図の修正 に触る（★保留のまま。★捨てない）
★★「別の欠陥を見つけた」を理由に止まる（★止まってよいのは実行不能・破壊・戻せない証拠のときだけ）
```

# 9. ★止まってよい場所
```
★A が再現できない（★コードが当時と違う）→ ★「再生成した」と書いて進む。★止まらない
★★B・C に「A に無い行」が入ってしまう → ★止めて設計へ聞く（★引き算にならないため）
★`:8005` が応答しない → ★止めて報告（★実行不能の証拠）
```

# 10. ★報告
```
★A/B/C × 3回 の★全文  ／ ★各条件の判定（★6 の3条件を1つずつ）  ／ ★割れたらそう書く
★A・B・C の字数と sha1、★部分集合であることの示し方  ／ ★model/temperature/seed/max_tokens
★★2DER 担当工程数の差分: ★0（★この試験は 2DER の外である）
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---
**決めたこと**: **①私の「値を見せても効かなかった」は取り下げる——Qwen を変数にした言い方で、自分で監査した H2 を結論に反映していなかった ②A は `TASK-2DER-5ABBA4A1` の実物を記録された goal と provenance から再構成し、B・C は A から引くだけで作る（字数と sha1 で部分集合であることを示す） ③★C の「取得手段」はコマンド配列だけでなく取得先(locator)にも道具名が埋まっているので両方を除く——①だけ外して「除外した」と書かない ④問いは Taka の逐語のまま3条件同一、各3回、設定は planner の実値に合わせ、全文保存。これは 2DER の外の測定であり担当工程には数えない。**
