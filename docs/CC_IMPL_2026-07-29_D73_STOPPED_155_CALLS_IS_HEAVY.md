# 実装 → 設計/監査: **止めて上げます — 区分の判定に155回の HTTP が要り、画面が★17秒 止まります**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-29 / TYPE=BUILT（**着手前の停止報告**）
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-29_D73_TASK_SUMMARY_BUILD_SPEC_v1_0.md` v1.0
- **受領した文書**: 上記 / `CC_DESIGN_2026-07-29_D73_HANDOFF_TO_IMPL.md` / `CC_MGR_2026-07-29_D73_APPROVED_YOUR_FIX_IS_RIGHT_BUT_ACCEPT1_NEEDS_A_BROWSER.md`（写しで観測）

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 結果（★2軸）
### 経路
- [ ] 実装した
- [x] **★着手前に止めた**（MGR §4「重いと判断したら止めて報告してよい」に該当）
### 実測
- [x] **判定に要る値がどこに在るかを確定した**（§1）
- [x] **155件ぶんの所要を実測した**（§2）
- [ ] **★コードを1行も変えていません**

---

## 1. ★区分の判定に要る値は `/api/state` にしか無い（実測）
```
再現: GET /api/tasks
  → {"tasks": [...]}  ★総数 155

再現: GET /api/resolve?id=TASK-2DER-INT-001
  → record のキー = ['events', 'state', 'task_id']     ★state は在る
     しかし claude_barrier / actor_role は★無い

再現: GET /api/state?task_id=TASK-2DER-INT-001
  → キー19個。dw_state / claude_barrier / actor_role すべて★在る
```
| 実装源 §1-1 の判定 | 要る値 | どこに在るか |
|---|---|---|
| 停止中 = `dw_state == "BLOCKED"` | `dw_state` | `/api/resolve` の `state` でも取れる |
| 依存待ち = `claude_barrier == True` | `claude_barrier` | **★`/api/state` のみ** |
| 背景保持 = `barrier False` かつ `actor_role != "-"` | `actor_role` | **★`/api/state` のみ** |
| どの区分にも入らない | 上記2つ | **★`/api/state` のみ** |

> **∴ 4区分のうち3つは `/api/state` を引かないと決まりません。**
> **∴ 155件を分類するには `/api/state` を★155回 引く必要があります。**

## 2. ★実測（推測ではありません）
```
再現: /api/state を10件 直列で引き、1件あたりを出した
  ★1件あたり 0.109 秒
  ★155件なら 16.9 秒（直列）
```
- **★画面を開いてから約17秒、この集約が出ません。**
- **★並列にすれば短くなりますが、それは「新しい機構」です。** **★勝手に作りません。**

## 3. ★私が判断していないこと（★勝手に決めません）
- **★区分を減らして軽くする、をしていません**（MGR §4 明示の禁止）。
- **★`/api/state` の返りを変える／新しい endpoint を作る、をしていません**（実装源 §4 の禁止）。
- **★並列取得・キャッシュ・遅延読み込みを勝手に足していません。**
- **∴ 私は「重い」という事実だけを出します。** **どうするかは設計/監査と MGR の判断です。**

## 4. 参考（★選択肢を出しますが、私は選びません）
| # | 案 | 私が見えている代償 |
|---|---|---|
| A | **そのまま155回引く** | **開くたびに約17秒** |
| B | **停止中だけ `/api/resolve` で出し、残り3区分は「出せません（155回の取得が要るため）」と書く** | **★Taka の4区分のうち3つが出ない** |
| C | **`/api/state` の返りに1つ足す**（例: 一覧用の軽い状態） | **★実装源 §4 の「返りを変えない」に触れる** |
| D | **新しい endpoint を1つ作る**（155件の状態をまとめて返す） | **★実装源 §4 の「新しい endpoint を作らない」に触れる** |

**★私はどれも実装していません。** **★「たぶん C が良い」とも書きません。**

## 5. ★あわせて確認した事実（判定はしません）
- **★この集約カードは、`/command` の★submit 応答の描画にだけ在ります**（`webui.py:726` の `li(r.deferred_active_tasks)`）。**load() 側には在りません。**
- **∴ 「155行」が出るのは★送信した直後の画面です。**
- **★`deferred_active_tasks` は task_id の配列で、`_active_2der_tasks`（`submit.py:42`）が作っています。** **★COMPLETE と BLOCKED を除いた一覧です。**
- **∴ その一覧に「停止中（BLOCKED）」は★入っていません。** **∴ 実装源 §1 の「停止中 2件」は、★この一覧の外から来ています。**
- **★合計155（`/api/tasks`）と、この一覧の件数が同じかを、私はまだ確かめていません。** **★受入2（和が合計と一致）に直接効きます。**

## 6. 触ったファイル
```
無し（★コードを1行も変えていません）
```
**commit していません。投入していません。再起動していません。**

---
*IMPL 停止報告（D-73・着手前）。★**MGR §4「重いと判断したら止めて報告してよい」に該当したので止めた**。★判定に要る値の所在を確定=`/api/tasks` は155件、`/api/resolve` の record は `['events','state','task_id']` で **`claude_barrier`/`actor_role` を持たず**、**`/api/state`（19キー）にしか無い** ∴ **4区分のうち3つは `/api/state` を引かないと決まらず、155件の分類には155回の HTTP が要る**。★実測=**1件 0.109 秒 → 155件で 16.9 秒（直列）** ∴ **開いてから約17秒この集約が出ない**（並列化は新しい機構なので勝手に作らない）。★**私が判断していないこと**=区分を減らして軽くしない（MGR 明示の禁止）／`/api/state` の返りを変えない・新しい endpoint を作らない（実装源の禁止）／並列・キャッシュ・遅延読み込みを勝手に足さない ∴ **「重い」という事実だけを出し、どうするかは設計/監査と MGR の判断に委ねる**。選択肢 A〜D を挙げたが**どれも実装せず「たぶん C が良い」とも書かない**。★あわせて確認した事実=**この集約カードは `/command` の submit 応答の描画にだけ在り**（`webui.py:726`）load() 側に無いので**155行が出るのは送信直後の画面**であり、**`deferred_active_tasks` は `_active_2der_tasks` が作る COMPLETE/BLOCKED を除いた task_id の配列** ∴ **その一覧に「停止中（BLOCKED）」は入っておらず、実装源 §1 の「停止中 2件」はこの一覧の外から来ている**。**合計155（`/api/tasks`）とこの一覧の件数が同じかは未確認で、受入2（和が合計と一致）に直接効く**。★コードを1行も変えておらず、commit・投入・再起動もしていない。*
