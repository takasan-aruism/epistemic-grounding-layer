# 設計/監査 → MGR（写: Taka / IMPL）: **D-54 調査 — `dropped_before` は★実例が0件。載る条件は「emit が例外で落ちること」であり、★正常時は永久に現れない**

- `BUILD_ROLE: 参照`（**調査のみ。★実装していない・`G-38` を塞いでいない・投入なし・台帳直読なし・勘定科目に触れていない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_MGR_2026-07-29_D53_REGISTER_6_AND_RETURN_TO_BACKLOG.md` §4

## 0. ★先に — 登記（Taka 6件）は完了した
| Taka の番号 | 内容（★文言を変えていない） | 採った id |
|---|---|---|
| **1** | ACCOUNT_AXESからRThread chartへの正規変換仕様 | **`G-64`** |
| **2** | chartのversioning・生成責任者・保存場所 | **`G-65`** |
| **3** | request_threadが読むchartとの同一性 | **`G-66`** |
| **4** | 初回chartが存在しない状態の起動手順 | **`G-67`** |
| **5** | 取り下げ済みmass保存則の実装残存 | **`G-62`**（★既登記。重複させず注記で繋いだ） |
| **6** | MINING→chart→RRI→LedgerまでのExecution Architecture | **`G-68`** |
- **全件 `OPEN` / 種別 `design_gap` / ★「あと1段で動く」と書かない・実装しない・配線しない を全件の注記に入れた。**
- **★禁じる理由も注記に残した**（その1段が何をする段かを設計が書いていないため、「1段だけ」と書くと中身を我々が発明することになる）。
- **★以降、本文書で勘定科目に触れない**（Taka 明示）。

---

## 1. 問① — `dropped_before` は実際にどこに載るか。載った実例が在るか
### 1-1. 載る場所（実読・全出現4件・打ち切り無し）
```
再現: grep -rn "dropped_before" --include=*.py 全5repo  → ★4件 / 確認4 / 打ち切り無し

ds/ds/etrace.py:32   （コメント）合流点④(DW)は fail-open。落ちた件数をプロセス内に持ち、次に成功した emit が載せる
ds/ds/etrace.py:117  rec["dropped_before"] = _dropped   ← ★載る場所はここ1箇所のみ
ds/ds/etrace.py:126  _dropped += 1                       ← ★増える場所
dev-workcell/dw/workcell.py:84 （コメント）黙らせない旨
```
> **∴ 載る先は★event レコードそのもの**（`event_trace.jsonl` の1行に `dropped_before` キーが増える）。
> **∴ ★別の台帳も別のカードも無い。** **既存の event に足されるだけである。**

### 1-2. ★実例は0件（front door で全走査）
```
再現: 既存4 run すべてを GET /api/resolve で引き、全 event を数えた（打ち切り無し）

ETR-88bc41400d19  event=6  / dropped_before を持つ event=★0 / truncated=0
ETR-0f6fae35665a  event=6  / dropped_before を持つ event=★0 / truncated=0
ETR-f0fe8461c407  event=21 / dropped_before を持つ event=★0 / truncated=0
ETR-75b58cfddf27  event=3  / dropped_before を持つ event=★0 / truncated=0
                  ─────────────────────────
                  総 event 36 / 確認 36 / 打ち切り無し / ★該当 0
```
> **∴ 載った実例は★1件も無い。**

---

## 2. 問② — 載っていないなら「載る条件」（★作らない）
**実読（`ds/ds/etrace.py:114-126`）**
```python
if _dropped:
    rec["dropped_before"] = _dropped     # ← ★_dropped が 0 でないときだけ
    _dropped = 0
…
except Exception as ex:
    _dropped += 1                        # ← ★emit が例外で落ちたときだけ増える
```
**★載る条件（3つが順に成立したときのみ）**
| # | 条件 |
|---|---|
| **1** | **`emit` が★例外で落ちる**（`event_trace.jsonl` に書けない等） |
| **2** | **その後★同じプロセス内で `emit` が成功する** |
| **3** | 2 の event に `dropped_before` が載る |

> **★∴ 正常時は永久に現れない。** **これは欠陥ではなく★設計どおりである。**
> **★`_dropped` はプロセス内変数である。** **∴ プロセスが落ちれば件数も消える**（そのために `stderr` が置かれている＝`workcell.py:84` のコメント）。
> **★2 が起きないまま終われば、落ちた事実は★どこにも残らない。** **これが `G-38` の穴の実体である。**

### 2-1. ★実例を作らない（禁止の遵守）
- **★`emit` をわざと失敗させれば実例は作れる。** **やっていない。**
- **理由: MGR の禁止「実装しない」「`G-38` を塞ごうとしない」に加え、★記録を汚すからである。**
- **∴ 「実例が0件である」を、★実例を作って埋めない。**

---

## 3. 問③ — UI に出すとしたら、既存のどのカードに入るか（★場所だけ）
```
再現: /command の既存カード構成（実読）
  「Current Thread / Focus」「2DER が捉えた Taka の問い」「分かっていること」
  ★「欠損・失敗・未確認」  ← D-49 で追加済
  ★「通過した処理」        ← D-49/D-50 で追加済
```
| 候補 | 判定 |
|---|---|
| **「欠損・失敗・未確認」カード** | **★ここである。** `boundary_failures` / `ds_limitation` と同じ性格（＝**取れなかったもの**） |
| 「通過した処理」カード | **★違う。** そこは「起きたこと」を並べる場所であり、`dropped_before` は**「起きたのに記録されなかった件数」**である |

> **★場所だけ書く。** **実装案を書かない**（MGR 禁止）。
> **★ただし1つだけ設計上の事実を添える**: **`dropped_before` は event に載る。** **∴ UI は run を引いた時点で既に持っている。** **新しい取得は要らない。**

---

## 4. ★私が言えないこと
| # | 未確認 | 誰が・いつ |
|---|---|---|
| 1 | **`dropped_before` が実際に載ったときの見え方**（実例が0件のため） | **★作らない。自然に発生したら確かめる** |
| 2 | **`stderr` に出た実例が在るか**（プロセスの標準エラーを見ていない） | CC-α / MGR が要ると言えば |
| 3 | **`_dropped` がプロセス跨ぎで失われた実例が在るか** | **★確かめる手段を持たない**（プロセス内変数のため） |

## 5. ★禁止事項の遵守
- **実装していない。** **`G-38` を塞いでいない**（fail-open は Taka の撤回により現状が正）。
- **実例を作っていない**（`emit` をわざと落としていない）。
- **UI の実装案を書いていない**（場所だけ）。
- **勘定科目に1文字も触れていない**（Taka 明示）。
- **`head`/`tail`/`-m` を使っていない。** 各走査に総件数・確認件数・打ち切り有無を書いた。
- **台帳を直読していない**（front door の `/api/resolve` のみ）。

---
*CC-α D-54（調査のみ）。★先に登記完了=Taka の未完案件6項目を**番号も文言も変えず**登記し（1=`G-64` / 2=`G-65` / 3=`G-66` / 4=`G-67` / 6=`G-68`、**5 は既登記の `G-62` に重複させず注記で繋いだ**）、全件 `OPEN`・種別 `design_gap`・**「あと1段で動く」と書かない/実装しない/配線しない**を全件の注記に入れ、禁じる理由（その1段が何をする段かを設計が書いていないため「1段だけ」と書くと中身を我々が発明することになる）も残した。以降 本文書で勘定科目に触れていない。★問①=`dropped_before` の全出現は**4件**で、載る場所は **`ds/ds/etrace.py:117` の1箇所のみ**（event レコードそのものにキーが増える。別の台帳も別のカードも無い）。**載った実例は front door で既存4 run・総 event 36 を打ち切りなしで全走査して 0件**。★問②=載る条件は3つが順に成立したときのみ（①`emit` が例外で落ちる ②その後**同じプロセス内で** `emit` が成功する ③その event に載る）∴ **正常時は永久に現れず、これは欠陥ではなく設計どおり**。**`_dropped` はプロセス内変数なのでプロセスが落ちれば件数も消え**（そのために `stderr` が置かれている）、**②が起きないまま終われば落ちた事実はどこにも残らない——これが `G-38` の穴の実体**。★実例を作っていない（`emit` をわざと失敗させれば作れるが、禁止の遵守に加え記録を汚すため。**「実例が0件である」を実例を作って埋めない**）。★問③=出すなら**「欠損・失敗・未確認」カード**（`boundary_failures`/`ds_limitation` と同じ「取れなかったもの」の性格）で、「通過した処理」カードは違う（そこは「起きたこと」を並べる場所で `dropped_before` は「起きたのに記録されなかった件数」）。**場所だけ書き実装案を書かない**が、設計上の事実を1つ添える=**`dropped_before` は event に載るので UI は run を引いた時点で既に持っており新しい取得は要らない**。★言えないこと=実際に載ったときの見え方（実例0件・**作らない**）／`stderr` に出た実例（プロセスの標準エラーを見ていない）／`_dropped` がプロセス跨ぎで失われた実例（**確かめる手段を持たない**）。*
