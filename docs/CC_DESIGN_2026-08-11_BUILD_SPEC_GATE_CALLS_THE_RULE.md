開発者規律 確認済(v1.0)

# 【BUILD SPEC】★門が 規則を 呼ぶ（★表示から ★支配へ）

宛: IMPL ／ 発: DESIGN ／ 2026-08-11 19:05 ／ 台帳: `ITEM-2DER-EVO-0062`
出所: MGR 裁定 18:42（`ETR-772ba5bc709c`）＝「★`gate_for_item` が `decide()` を 呼ぶ＝★層1/層2 は 止めない ／ ★層3 は 止める」

---

## 1. ★★先に 危険を 出す（★★このまま 素直に 繋ぐと ★2DER が 止まる）

```
★★`decide()` を ★門の 判定として そのまま 使うと ―― ★★いま 通っている行為が 止まる。

★理由 = ★★`rollback` は ★`revert_scope.complete`（★commit の 前後）で 出している。
       ★★行為を ★これから やる 時点では ★★commit は まだ 無い ∴ ★★`incomplete`
       → ★★層3 → ★★★止まる。
★★対象 = ★段 REVERSIBLE の ★3行為（★POLICY より・★名指し）
       ★`DW_MACHINE_DISPATCH`（★DW の task を Qwen worker で 進める）
       ★`AUTONOMOUS_TASK_CREATION`（★2DER が 自分で task を 立てる）
       ★`ROLLBACK_RESTORE`（★承認済み実験の 後始末）
★★これらは ★★commit を 伴わない ∴ ★★★`revert_scope` では 永久に `incomplete`
       = ★★★畳めない条件（★[[absence-reads-as-compliance]]:★鳴らない条件は 願望）。

★★∴ ★★『層2 は 0件』どころか ★★『2DER が 自分で task を 立てられない』に なる
   ―― ★★★今夜 承認で やった『全部 閉め切る』を ★★門で もう一度 やる形。
```

## 2. ★★作る物（★★規則は ★支配する所だけ 支配する・★★残りは 触らない）

```
★★`gate_for_item(action_type, item_id, …)` の 中で ★`decide()` を 呼ぶ。
★★判定の 合成は ★3つに 分ける（★★上書きしない所を はっきり 書く）

★★(1) ★`decision == REQUIRES_TAKA`（★層3）    → ★★止める（★★既存の判定より 厳しい側にのみ 動く）
★★(2) ★`decision == AUTO_APPROVED`（★層1）    → ★★止めない（★★既存の門が 通す物を そのまま 通す）
★★(3) ★★`rollback` が ★行為の時点で 言えない → ★★★規則は 判定しない
       = ★★既存の門（★段 ≦ 上限）の 結果を ★そのまま 使う
       = ★`rule` に ★★『規則は まだ この行為を 支配しない（★理由）』と 書く

★★∴ ★★★既存の通り道を 1本も 塞がない ／ ★層3 だけが 新しく 止まる。
★★evidence は ★★`ds.etrace.current_run_id()`（★★いま 通ってきた1本）が 実在の ETR か
   ―― ★★`ds/ds/etrace.py:69` に 既に 在る（★新しい部品を 作らない）。
```

## 3. ★★『規則が どこまで 支配しているか』を 数にする（★★これが 本体）

```
★★POLICY 22行 の うち
   ★★規則が 支配する = ★OBSERVE 9（→層1）＋ ★IRREVERSIBLE 10（→層3）= ★★★19/22
   ★★規則が 支配しない = ★★★3/22（★上の 3行為・★★名指しで 出す）

★★この数が ★★『憲法が どこまで 効いているか』の 唯一の 正直な 答え。
★★★『権限の規則を 入れた』と 書かない = ★★★『22 のうち 19 を 支配する』と 書く。

★★3件が 開く条件（★★併記する・★鳴る口を 同じ行に）
   ★★① `record_change` の ★自動の呼び手が 在る（★★本日 実測 = ★★0件）
      → ★★行為が commit を 伴うなら ★`revert_scope` が そのまま 使える
   ★★② commit を 伴わない行為の ★『戻し方』の 定義（★★裁定が 要る = ★我々は 決めない）
      → ★案: ★台帳は 足すだけ ∴ ★★『打ち消す行を 足せる』= 戻せる、と 読むか
   ★★観測される口 = ★`/api/control` の ★★支配率（★19/22 が 動いたら 効いた）
```

## 4. ★★受入（★数で・★★走らせる前に 宣言する）

```
★★【★直す前の値=★2026-08-11 18:50 実測】
   ★止まった行為（★`/api/pending_approvals`）= ★★20件
   ★規則を 呼ぶ 門 = ★★★0箇所（★呼び手は 読み口2つだけ）

★① ★OBSERVE の行為（★例 `REGRESSION_TEST`）を ★1本 通し ★★止まらない
     ＋ ★★`decision=AUTO_APPROVED` が ★記録に 残る（★実物1件）
★★② ★IRREVERSIBLE の行為（★例 `COMMIT_PUSH`）を ★1本 通し ★★止まる ＋ ★`rule` が 付く
★★③ ★`CHANGE_AUTHORITY_CEILING` は ★段・上限に 関わらず ★★止まる（★★自己昇格の錠）
★★④ ★★★止まった行為の件数が ★★20件から ★増えも 減りも しない
     ―― ★★★既存の通り道を 塞いでいない、の 検査（★★増えたら 未達=★止めて 報告）
★★⑤ ★★支配率が ★`/api/control` から 引ける = ★★`19/22` ＋ ★★支配しない3件の ★名前
★★⑥ ★`rule` が 空の行 = ★★0件（★★『規則は まだ 支配しない』も ★理由として 書く）
★★⑦ ★★LLM 0回 ／ ★人が 書いた文を 読む所 0箇所 ／ ★新台帳0 ／ ★口0増 ／ ★保存欄0増
★★⑧ ★★引けなかった件数を ★★別立てで 書く（★★本日 私と MGR が 2人とも 落ちた所）
★★⑨ ★新しい名前（★定数・関数・欄）の 数を ★報告する
★★⑩ ★退行 = ★`test_command_surface` ／ ★`test_authority*` を 走らせ ★★走らせた名前を 書く
```

## 5. ★注意

```
★★『規則を 門に 入れた』＝ ★★『全部を 規則が 決める』では ない。
   ―― ★★支配しない3件を ★★★隠さない（★★数と 名前で 出す）。
★★層3 は 常に Taka ／ ★材料が 無い物は 層3（★fail-closed）―― ★1文字も 緩めない。
★★但し ★★『材料が 無い』と ★★『規則の 対象外』を ★★分ける
   ―― ★前者は 層3へ 落とす ／ ★後者は ★既存の門に 任せる（★★勝手に 締めない）。
★★本番配線 ∴ ★Claude(IMPL) が 書く ／ ★行数を 報告 ／ ★2DER の実績に 数えない。
```
