# 【BUILT / C-2 受入(c)】**★止まった。★しかし★止めたのは契約の検査ではない** — ★(c) は★示せない

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: MGR / Taka / 発: 実装(IMPL) / 2026-08-01 18:5x / TYPE=BUILT
- **開発者規律 確認済（版: v1.1 / 2026-08-01）** ／ **実装源**: `CC_DESIGN_2026-08-01_C2B_REJECT_TWO_CONTRACTS.md` §3 受入(c)（`D-209` のまま）
- **手番の根拠**: `CC_DESIGN_2026-08-01_C2B_AUDIT_ALL_THREE_HOLD.md` §4 逐語「★(c) ★空のまま投げたら弾かれる ＝ ★★測っていない（★★投入が要る。★★IMPL の手番）」
- **★コードを1行も変えていない** ／ **commit していない** ／ **`:8005` を叩いていない** ／ **`twoder` 配下で python を動かしていない**

---

# 1. ★やったこと（★1回だけ・★投入は1件）

```
★投入したもの: ★`GET /api/state` の ★`next_request_template`（★`TASK-2DER-GPU-SWITCH-001` の雛形・★342字）
★★投入前に決定論で実測（★予告に固定済み・`c_pre.txt`）: ★`def test_` = ★0行 ／ ★SK/IT/END = ★1/1/2
★★★＝ ★★これは ★受入(c) が言う「★空のまま」そのものである（★人が1文字も埋めていない）
★変更行数: ★★0行（★今回は測るだけ・★何も書いていない）
```

---

# 2. ★★実測（★逐語・`POST /api/submit` の応答）

```json
{"task_id": null,
 "request_type": "MODIFY_EXISTING",
 "acquisition_method": "RRI_PREFLIGHT_HOLD",
 "next_legal_operation": "CLARIFY_FIRST (gate RRI-GATE-UNBOUND-REFERENT-001): 「前の件」が指す対象は記録から特定できない。 RRI holds before DW/acquisition. required to proceed: 参照している案件の一意な識別子（DE番号/タスクID/ファイル名等）",
 "trace_key": "SUBMIT-Fp73Jw"}
```

| 見えたこと | 値 |
|---|---|
| 進んだか | **★進んでいない**（`task_id` = **`null`**） |
| task は増えたか | **★増えていない**（★前回 (c)-1 は ★1件 増えた。★今回は0件） |
| **★止めたのは何か** | **★★`RRI` の preflight gate（`RRI-GATE-UNBOUND-REFERENT-001`）** |
| **★契約の検査か** | **★★違う** |

---

# 3. ★★∴ ★受入(c) は「★示せない」（★「弾かれた」と書かない）

```
★受入(c) の逐語:「★空のまま投入 → ★`ValueError` で ★弾かれる」
★★実測は ★「★止まった」だが ★★止めた門が ★★違う。

★なぜ届かなかったか（★決定論・行番号つき）:
  ★① 分類は ★`MODIFY_EXISTING` ＝ ★`submit.py:466` の ★DW 分岐に入る資格は ★在った
  ★② ★しかし ★その手前の ★preflight（`:289` 付近）で ★`RRI_PREFLIGHT_HOLD` になり ★分岐に入っていない
  ★③ ∴ ★`:506` の ★`contract_seal.extract_contract` は ★★呼ばれていない
★★★★★＝ ★★「空の契約だから止まった」のではない。★★「★『前の件』が特定できないから止まった」である。
★★★★★★★★この2つを ★同じ「止まった」として ★★○ にはしない。

★★★★★★★★★正直に書く: ★★★もし私が ★「(c) ○ ＝ 弾かれた」と書いていたら、
   ★それは ★★★★「通らなかった」を ★「機構が効いた」と ★読み替える★嘘である。
```

## 3-1. ★前回 (c)-1 との違い（★「3回目の同型」とは書かない）

| | 前回 (c)-1（14:4x） | ★今回 |
|---|---|---|
| 分類 | `OBSERVE_CURRENT_STATE` | **`MODIFY_EXISTING`** |
| 結果 | **素通り**・`TASK-2DER-670DA828` が**作られた** | **止まった**・task **0件** |
| 止めた門 | ★無い | **`RRI-GATE-UNBOUND-REFERENT-001`** |

```
★★∴ ★★「機構は経路による」が ★★★3回目として再現したとは ★★書けない（★経路も結果も違う）。
★★★今回 分かったのは ★別のことである: ★★★契約より ★手前に ★もう一つ門が在り、★そちらが先に閉じる。
```

---

# 4. ★予告の当否（★投入前に固定・`c_pre.txt`）

| 予告 | 結果 |
|---|---|
| 判定 = **○（弾かれる）** | **★★外れた**（★止まったが ★契約の検査ではない＝★「示せない」） |
| 逐語 `immutable_tests has no pytest-collectable test` が出る | **★外れた**（★出ていない。★`extract_contract` に届いていない） |
| 外れ方の予告 = 「OBSERVE に落ちて `TASK-2DER-4C179886` が作られる」 | **★★これも外れた**（★★私が挙げた2つの外れ方の ★どちらでもなかった） |
| DW 分岐に入る（`MODIFY_EXISTING`）| **★当たり**（★分類は当たったが ★手前で止まった） |

```
★★★私の予告は ★「通る／OBSERVE で素通り」の ★2択で書いていた。
★★★★実際は ★★3つ目（★preflight で止まる）だった ＝ ★★私は ★門を1つ 見落としていた。
```

---

# 5. ★★止めたこと（★隠さない）

```
★★2回目の投入を ★用意したが ★★投入していない。
   ★内容: ★`TASK-2DER-CCCAEAA8` の雛形（★2128字・★`def test_` 0行・★不定の参照語 無し）を投入し、
          ★`:506` に ★到達させて ★(c) を測り直す、というもの。★予告も先に固定した（`c_pre.txt` 末尾）。
   ★★止めた理由: ★★実行の許可が出なかった ∴ ★★止めた（★押していない）。
   ★★★∴ ★(c) は ★★未測定のままである。★★「測ったが示せなかった」ではなく ★★「1つの経路でしか測れていない」。
★★★★S-3（`TASK-2DER-B37727E3`）の雛形は ★使っていない（★SPEC §4 禁止「S-3 の依頼文を書き換えない」）。
```

---

# 6. ★副作用 ／ ★戻し方

```
★副作用: ★★無い。★task 0件増 ／ ★コード0行 ／ ★台帳へ書いていない ／ ★commit していない
★戻し方: ★★戻すものが無い（★何も変えていない）
```

---

# 7. ★設計へ渡すもの（★私は決めない）

```
★① ★受入(c) は ★★現状の書き方だと ★★測れない場合が在る。
   ★「★止まった」には ★★少なくとも3種類 在る: ★契約で弾かれた ／ ★preflight で止まった ／ ★素通りした
   ★★★どれなのかは ★front door から ★読めた（★`acquisition_method` と ★`next_legal_operation`）＝ ★ここは在る
   ★★★★∴ ★受入(c) を ★★「止まったこと」ではなく ★★「★契約の検査で止まったこと」に ★書き直すかは ★設計の判断。
★② ★(c) を測るには ★★preflight を通る依頼文が要る ＝ ★どの task の雛形で測るかも ★設計の判断。
★★★私は ★どちらも決めていない（★規律: 設計判断をしない）。
```

---
*IMPL → 設計/監査（写: MGR / Taka）。C-2 受入(c)。**コードは1行も変えていない（今回は測るだけ）。** 監査文書が「(c) は投入が要る・IMPL の手番」と名指ししたので、`TASK-2DER-GPU-SWITCH-001` の雛形（342字・投入前に `def test_` 0行・SK/IT/END 1/1/2 を実測）を1回だけ front door へ投入した。**結果は `task_id: null` / `acquisition_method: "RRI_PREFLIGHT_HOLD"` / gate `RRI-GATE-UNBOUND-REFERENT-001`「『前の件』が指す対象は記録から特定できない」。★進まなかったが、止めたのは契約の検査ではない。** 分類は `MODIFY_EXISTING` で DW 分岐に入る資格は在ったが、その手前の preflight で止まったため `submit.py:506` の `extract_contract` は呼ばれていない。**∴ 受入(c) は「示せない」。「弾かれた」とは書かない——「通らなかった」を「機構が効いた」と読み替えるのは嘘だからである。** 前回 (c)-1（OBSERVE で素通りし task が1件 増えた）とは経路も結果も違うので「機構は経路による」の3回目とは書かない。今回分かったのは別のこと——**契約より手前にもう一つ門が在り、そちらが先に閉じる**。予告は外れた。私は「通る／OBSERVE で素通り」の2択で書いており、実際は3つ目（preflight で止まる）で、**門を1つ見落としていた**。**2回目の投入（`CCCAEAA8` の雛形で `:506` に到達させる）は用意したが、実行の許可が出なかったので押していない ∴ (c) は「測ったが示せなかった」ではなく「1つの経路でしか測れていない」。** S-3 の雛形は禁止に当たるので使っていない。副作用は無い（task 0件増・コード0行・commit なし）。設計へ渡すのは2件——受入(c) を「止まったこと」ではなく「契約の検査で止まったこと」に書き直すか、どの task の雛形で測るか。どちらも私は決めていない。*
