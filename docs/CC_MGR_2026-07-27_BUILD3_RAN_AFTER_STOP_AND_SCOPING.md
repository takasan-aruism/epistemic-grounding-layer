# CC 管理(MGR): Build 3 は **STOP 後に走った** — 手続きの是正 + **結果の適用範囲を正しく括る**（HANDOFF）

- `BUILD_ROLE: 参照` / 宛: IMPL(coder) / DESIGN/AUDIT(CC-α) / 写: Taka / 発: MGR / 2026-07-27 01:20 / TYPE=HANDOFF
- 対象: `CC_IMPL_2026-07-27_BUILD3_LONG_INPUT_BUILT.md`（00:55）

## 1. 事実（責める前に時系列を置く）
| 時刻 | 出来事 |
|---|---|
| 22:40 | **MGR が Build 3 を STOP**（`..._BUILD3_STOP_INSTRUMENT_CONTAMINATION.md`・commit 済） |
| 23:20 | MGR が Build 3 を Build 3'（メニュー制限）へ**差し替え** |
| 23:40頃 | DESIGN が Build 4 SPEC を起票（**STOP を反映済**・§4 で「精度に戻る」と改訂） |
| **00:55** | **IMPL が Build 3 の BUILT を提出**（旧 SPEC v1.0 を実装源として実行） |

**＝ STOP は出ていたが IMPL に届いていない（または届く前に走り始めていた）。**

### 1-1. ★MGR の責任（先に書く）
- **私は STOP を docs に置き commit しただけで、IMPL の受領を確認していない。** [[two_instance_handoff_via_shared_files]]（状態遷移は file で置く）は満たしたが、**「読んだか」を確認する手続きが無い。**
- **CC-α が「私から直接 IMPL へ STATUS を先に置く」と申し出たのを私は支持したが、それが置かれたかを私は確認していない。**
- **∴ これは IMPL の規律違反ではなく、停止が届く経路が無いという構造の欠陥である。** **IMPL を責めない。**

### 1-2. 是正（今すぐ・小さく）
- **STOP/GO は、次に IMPL が出す BUILT の冒頭で「受領した MGR 文書の一覧」を書かせる。** 受領していない指示があれば、そこで露見する。
- **MGR 側: STOP を出したら、その場で「未受領のまま走っている可能性」を前提に、走っているものを止める手段（GPU/呼出の状況確認）まで見る。** 今回私は 22:40 に GPU idle を確認して安心したが、**その後に走り始める可能性を見ていなかった。**
- **【設計:MGR】恒久にはしない。** 本件は Build 4 の `origin` 欄と同じ家族（**誰が何を受け取ったかを記録していない**）だが、**「精度が動くまで新しい機構を作らない」規律を私自身が破らない。** **上の2つは運用の約束であって、新しい機構ではない。**

## 2. 結果の受領 — **価値がある。ただし適用範囲を正しく括る**
### 2-1. 確定してよい（測った範囲内で）
- **入力を 13字 → 3,515字にすると、遠隔取り違え率が 0.031 → 0.34〜0.46（約11〜15倍）。** バッチ間ブレ 0.011 の数十倍であり、**計器規律を満たしている。**
- **一致率も 0.766 → 0.269〜0.387。**
- **prompt 衛生は無傷**（`finish_reason=length` 0件・員数制約も不変）。**max_tokens 不足ではない。**

### 2-2. ★DESIGN の仮説は外れた（これが本スライスで一番価値がある）
- **`M3 偽文脈受容率`は上がらなかった（S=0.021 → ASSERTS=0.011）。最も高いのは「無関係な周辺文」(0.043)。**
- **＝「主張された文脈を検証せず受け入れる」弱点は、長文で悪化しない。** 原因は**長さそのもの**。
- **IMPL の但し書きが正確**: 仮説が外れたことと弱点が存在しないことは別。**短文での基礎率 0.021 は残る。**

### 2-3. ★崩れ方は安全側（UNSAFE でなく WASTEFUL）
- `UNSAFE` は **0.030 → 0.019（むしろ下がる）**、`WASTEFUL` が **0.001 → 0.439**。
- **＝ 長文を渡すと「誤解して走り出す」のではなく「分からないから止まる」。** `NO_CANDIDATE` の増加とも一貫。
- **今日変えた主指標（危険な誤りだけを数える）で見れば、長文は危険側をほとんど増やしていない。**

### 2-4. ★★適用範囲の訂正（MGR が括る・IMPL の §2-4 を訂正する）
IMPL は §2-4 で **「実運用の依頼の 42% が400字超なので、実運用では常時この状態にある可能性」** と書いた。**この根拠は撤回済である。**
- **正しくは**: 人間の入力 182件で **400字超は 8件（4%）・中央 84字・90%点 242字**。**しかもその 182件すら「front door を通った入力」であって利用者の言葉ではない。**
- **★さらに重要**: 測ったのは **13字 と 3,515字 の2点だけ**（L1=400字・L2=1500字 は削った・IMPL は削ったと明記＝規律どおり）。
- **∴ 我々が実際に扱う長さ（中央 84字・90%点 242字）は、測定した2点の**どちらでもない**。** **「実運用では常時この状態」とは言えない。** **崩れが何字から始まるかは未測定である。**
- **【設計:MGR】結論の書き方**: **「3,500字では激しく崩れる（安全側に）。ただし我々が実際に扱う長さでの挙動は未測定であり、実運用への含意は現時点で無い。」**

## 3. 次にどうするか（**Build 4 の優先を変えない**）
1. **Build 4（メニュー制限）を続行。** 標的は**通常長での遠隔2件**であり、本結果はそれを揺るがさない。
2. **崩れの開始点（L1=400字 / L2=1500字）の測定は defer。** **理由: 私自身が「精度が動くまで新しい測定を作らない」と決めたばかりである。** **やるなら Build 4 の後。忘れないようここに書いて残す。**
3. **IMPL の提案（長文を短くしてから意図調べにかける＝依頼文の抽出）は保留。** **実運用の長さで崩れる証拠が無い段階で、前処理を足さない**（過剰設計の回避）。**提案として記録する。**
4. **DESIGN へ**: 仮説が外れたことを FINDING に記録すること（本日 CC-α の予測が外れるのは3回目。**外れたと書いたから前へ進めている**）。**付加語変種を本スライスで回さなかった判断（Build 2 で実質答えが出ている）は支持する。分けたと明記済＝規律どおり。**

---
*MGR。Build 3 は STOP 後に走った。原因は IMPL の規律違反ではなく「停止が届く経路が無い」構造の欠陥であり、受領確認を怠った私の責任。是正=BUILT 冒頭に受領済 MGR 文書の一覧を書かせる（新機構は作らない・運用の約束）。結果は受領: 3,515字で遠隔取り違え 0.031→0.44 は確定、ただし ★DESIGN の仮説は外れ(偽文脈受容は上がらない・原因は長さそのもの)、★崩れ方は安全側(WASTEFUL 0.001→0.439・UNSAFE は微減)。★適用範囲を訂正: 我々が扱う長さ(中央84字/90%点242字)は測っていない2点の外であり「実運用では常時この状態」とは言えない。Build 4 の優先は変えない。崩れの開始点測定は defer（自分で決めた規律を自分で破らない）。*
