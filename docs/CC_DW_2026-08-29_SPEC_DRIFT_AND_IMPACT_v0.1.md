# DW — 旧仕様から現仕様への必然的変更 と 影響範囲 v0.1

- 親: `ITEM-2DER-EVO-0149` / 担当: DW（`DW_CLAUDE`） / 2026-08-29
- 測ったHEAD: twoder `a3207da` / dev-workcell `b003368` / egl `083e549`
- 指示（Taka）: 「DW の全面的な調査、**影響範囲**、その他**旧仕様から現在の仕様にあたって発生する必然的な変更**ならびに配線等を徹底的に」
- ★実装 0行。★TASK を1件も動かしていない。

---

## 0. ★いちばん大きい1つ（先に）

> **DW の状態機械は「実装する仕事」しか完了できない。**
> 現行仕様（`TAKA_SPEC_2026-08-20` §20）は **非IMPLEMENT の仕事がコード生成なしで正常完了できること**を要求している。

**実測:**
```
dev-workcell 配下に work_kind …… ★0 file
                 STOP_AT   …… ★0 file
                 DELIVERABLE …… ★0 file
DW の PHASES 10語 = CREATE/PLAN/GENERATE/AUDIT/DISPOSE/REGENERATE/UPPER_REVIEW/COMPLETE/BLOCK/PROCESS_EVENT
   → ★10語すべて IMPLEMENT の段。★調査で終わる段が 1つも 無い
CREATE payload に work_kind を持つ = 20 / 713 = 2.8%
```
∴ **DW は「今回は調べるだけ」という仕事の種類を、概念として持っていない。**
∴ 調査依頼を入れると **GENERATE へ吸い込まれる**（`TAKA_SPEC` §21 が実測で記録した現象と同じ）。

★**既存 item が在る**: `ITEM-2DER-EVO-0082`（RRI-DW 18段 P1＝INVESTIGATE 最小成立・IN_PROGRESS）。
★**新規起票しない。** 本書は **DW 側から見た影響範囲**を足すだけ。

---

## 1. 必然的変更の一覧（★影響範囲つき・全 .py 846本を走査）

| # | 旧仕様のまま残っている物 | 実測（分母つき） | 影響範囲（文脈一致 file） | 出所 |
|---|---|---|---|---|
| **C0** | **非IMPLEMENT の完了経路が無い** | `work_kind`/`STOP_AT`/`DELIVERABLE` が dev-workcell に **0 file**／CREATE の 2.8% | — | `TAKA_SPEC_2026-08-20` §20 / `EVO-0082` |
| **C1** | **PLAN に `run_id` が付かない** | `run_id` の**欄は 6391件 100%**在るが**値は 1442件 22.6%**（GENERATE/AUDIT/REGENERATE のみ）。PLAN/CREATE/DISPOSE/UPPER_REVIEW/COMPLETE/BLOCK は **0** | **38 file**（dw 12 / twoder 16 / 他 10） | `CC_MGR_2026-08-23_EGL_DW_BOUNDARY_SURVEY` §3 の申し送り |
| **C2** | **`event_id` / 親 `event_id` が無い** | **0 件**（event の欄は8つで、どれも id を持たない） | **5 file**（twoder のみ） | `CC_MGR_2026-07-28_D41` の**裁定①**（★裁定から1ヶ月 未実装） |
| **C3** | **`_MAP` が 13 state 中 9 しか持たない** | `PLANNING` 51 ＋ `BLOCKED` 4 = **55 / 710 (7.7%)** が dispatch 対象外 | **4 file** | `workcell.STATES` と `dispatch._MAP` の差 |
| **C4** | **`GATE` が actors に登記されていない** | 閉じられる **109件** が動かず、08-28 の DW `COMPLETE` は **1日 0件** | **5 file** | `EVO-0086` |
| **C5** | **受領 queue が減らない** | 105件を毎周 **387秒**かけて見て受領 **0**。閉じられる109件との**重なり 0** | **5 file** | `EVO-0144` |
| **C6** | **明細への鍵が通っていない** | `ds_thread_id` **3 / 697 = 0.4%**／dev-workcell に `rthread`/`question_id` **0回** | **15 file**（dw 0 / twoder 12 / 他 3） | `CC_DESIGN_2026-08-23_DW_OBSTACLES` |
| **C7** | **契約を持たない TASK が7割** | `contract` を持つ CREATE **217 / 708 = 30.6%** | **19 file** | ― |
| **C8** | **`related_failure_patterns` が packet schema の外** | EMPTY **665 / 709 = 93.8%**／DW が空を監視する欄は**この1つだけ** | **4 file** | `EVO-0124` |
| **C9** | **経路表 S09–S18 の受け手鍵が区間を分けない** | 6区間が同一の `DW._append_event`（6区間とも 45964回）。**S10 は宣言そのものが無い** | **3 file** | 本 census W4 |

★`run_id` は **「欄が在る」と「値が入っている」を分けた**。分けないと 100% に見える。

---

## 2. ★影響範囲の読み方（★数字の意味を書く）

- 「文脈一致 file」= その語に触れ、**かつ** 変更の文脈語も持つ .py の数（分母 846本）。
- ★**上限であって工数ではない。** 実際に直す必要が在るかは各 file を開くまで分からない。
- ★**小さい方が安全とは限らない**: `C4`(5 file) は 109件を止めており、`C1`(38 file) より効き目が大きい。

### ★影響が DW の外へ出るもの（★他担当の確認が要る）
- **C1**（他 repo 10 file）／**C6**（twoder 12 + egl 3）／**C2**（twoder 5）
- ∴ この3つは **DW 単独で決められない**。

### ★DW 内で閉じるもの（★私が持てる）
- **C3**（4 file・うち dw 3）／**C5**（5 file・全部 twoder の DW 面）／**C9**（3 file）

---

## 3. ★順序の提案（★決めるのは MGR）

```
① C5（受領 queue）      … 私1人で閉じる。毎周387秒が戻る。他を待たない
② C4（GATE 登記）       … 効き目 最大(109件)。★裁定が要る
③ C3（_MAP の4語）      … 55件が dispatch に乗る。★状態機械に触れるので裁定が要る
④ C1 / C2（id と系列）  … 系列が畳めない・親子が引けない。★他 repo に出る
⑤ C0（非IMPLEMENT完了） … ★最大の設計変更。`EVO-0082` が正本 ∴ 私は材料を出すだけ
⑥ C6 / C8 / C9          … 相手 Domain の確認待ち（W2 / W1 / W4）
```

★**C7（契約 30.6%）は順序に入れない。** 「調査系は契約を持たないのが正しい」可能性が在り、
★**それが正常かを先に決めないと、直すこと自体が誤り**になる（MGR へ問いとして出す）。

---

## 4. ★私が過去に出した数の訂正（★同じ文書に置く）

`EVO-0089` について「JUDGE_REQUIRED 145件中 83件(57.2%) が今も上流」と書いたのは **誤り**。
DW の events は追記式 ∴ **修正前の記録が古い文字列を持ち続ける**のを、現在と読んだ。

```
no neutral runner result を含む DISPOSE の割合(日ごと):
  08-19 32/33(97%) → 08-20 38/39(97%) → ★08-23 12/36(33%) → 08-27 1/18(5.6%) → 08-29 0/1
JUDGE_REQUIRED 145件の内訳: 修正前 66 / ★修正後 17 / DISPOSE無し 10
★生きている残存 = 17/145 = 11.7%（57.2% ではない）
```
★修正（`adjudicator._rr_exit` / `_rr_stdout` の fallback）は**効いている**。★但し **0 ではない**（修正後13件）。
★残差の原因が①か別経路かは **未測定**。

---

## 5. していないこと

- 実装 0行 ／ TASK を1件も動かしていない ／ `dev-workcell` を1文字も触っていない
- C0 を新規起票していない（`EVO-0082` が在る）
- C6 / C8 / C9 を起票していない（相手 Domain の確認前・W2/W1/W4 のまま）
- 影響範囲の数を「工数」と言い換えていない
