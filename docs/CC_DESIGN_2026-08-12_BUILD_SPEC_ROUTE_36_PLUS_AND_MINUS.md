開発者規律 確認済(v1.0)

# 【BUILD SPEC】★『渡した』と『受け取った』を ★別々に 3値で 出す（★母数 18→36）

宛: IMPL ／ 発: DESIGN ／ 2026-08-12 00:15 ／ 台帳: `ITEM-2DER-EVO-0058`
出所: Taka 逐語（★『A は B に対する ＋ を持ち、B は A に対して − を持つ』）＋ MGR 条件8つ（23:50）

---

## 1. ★★鍵（★★これを 最初に 書く＝★数は 鍵で 動く）

```
★★− ＝ ★受け取った側の 記録 ＝ ★★`(component, function[, phase])`
   ―― ★★これは ★★★昨日 出した 18/18 PRESENT ★そのもの
      （★ROUTE の component/function は ★★受け手が emit する名前。★例 S04 = `RRI/request_type`）
★★＋ ＝ ★渡した側の 記録 ＝ ★★`handoff` の `(component, function)`
   ―― ★実測 = ★★★`handoff` が 在る区間 = ★★1/18（★S14 のみ = `RUNNER/hand_to_worker`）
      ★`receipt` が 在る区間 も ★★1/18（★S14 のみ = `WORKER/received_from_runner`）

★★∴ ★★★昨日の『渡した記録 1/18』は ★★この鍵。★★『18/18』とは ★★別の物を 数えている。
```

## 2. ★★ケース表（★★走らせる前に 出す・★語は 3値のまま＝★新語0）

```
★★C1 ★記録に 鍵の行が 1件以上                          → ★`PRESENT`
★★C2 ★記録0件 ＋ ★源に その鍵の emit 呼び手が 在る      → ★`EMPTY`
★★C3 ★記録0件 ＋ ★源に その鍵の emit 呼び手が 無い      → ★`NOT_OBSERVED`
★★C9 ★★★鍵そのものが ROUTE に 無い（★`handoff` が None）→ ★`NOT_OBSERVED`
      ―― ★★理由 = ★★観測する口が ★定義されていない ＝ ★C3 と 同じ意味
      ―― ★★但し ★`rule` に ★★『C9(鍵が ROUTE に無い)』と 書き分ける（★★C3 と 混ぜない）
★★C7 ★記録が 引けない → ★★3値に しない ＝ ★『引けなかった』を 別立て（★0として 数えない）
★★C6 ★`phase` が None なら phase を 条件に しない（★−側のみ）
```

## 3. ★★出す物（★＋と − を ★★1つの数に しない＝★Taka の 非対称）

```
★★`/api/control` の `route_table` に ★2つ 足す（★口0増・★新台帳0）
   ★`minus`: ★{total:18, by_value:{PRESENT/EMPTY/NOT_OBSERVED:{count, ids}}, unreadable:{...}}
   ★`plus` : ★{total:18, by_value:{…同じ3語…}, unreadable:{…}}
   ★★`pairs`: ★★区間ごとに ★{id, plus, minus, verdict}

★★`verdict`（★★Taka の言葉を そのまま 機械の語に する・★2語だけ）
   ★★`SURPLUS_PLUS`（★余った＋）= ★＋が PRESENT ＋ ★−が PRESENT でない
      ―― ★★出しているのに ★受け手の記録が 無い ＝ ★★★止まらない ∴ ★気づかれない
      ―― ★★これが ★本日 9回 出た『部品は在るが 呼び手が無い』の ★★機械の姿
   ★★`UNMET_MINUS`（★満たされない−）= ★−が PRESENT でない ＋ ★＋が PRESENT
      ―― ★★止まる ＝ ★エラーとして 出る
   ★★どちらでもない区間は ★★`verdict` を 付けない（★★空欄にせず ★`null` と 書く）
★★★2つを 足し合わせた『合計』を ★出さない（★★非対称を 潰さない＝Taka 逐語）
```

## 4. ★★走らせる前の 宣言（★★源から 決定論で 出した値・★予想では ない）

```
★★−側 = ★`PRESENT 18` ／ `EMPTY 0` ／ `NOT_OBSERVED 0`（★昨日と 同じ＝★★変わったら どちらかが 誤り）
★★＋側 = ★★`PRESENT 1`（★S14）／ ★★`NOT_OBSERVED 17`（★★★全て C9 ＝ 鍵が ROUTE に 無い）
★★`SURPLUS_PLUS` = ★★0件 ／ ★★`UNMET_MINUS` = ★★0件
   ―― ★★理由 = ★★S14 は ＋も −も 在る ／ ★他17区間は ＋が 観測されない
   ―― ★★∴ ★★★この段で 出る 本当の答えは ★★『17区間で ＋を 観測する口が 無い』

★★★これは ★Taka の問い①（★どこが抜けると 止まるか）に 対する ★★正直な答え ＝
   ★★★『18のうち 17は ★まだ 答えられない』（★★答えられる と 書かない）
```

## 5. ★★受入（★MGR の条件8つ ＋ ★私から1つ）

```
★★① ★36件（★18×2）すべてに 3値の どれかが 付く（★★母数を 明記）
★★② ★★3語とも ★欄が 在る（★★0件でも 欄を 消さない＝★前回の 欠陥1）
★★③ ★`NOT_OBSERVED` の 判定が ★★区間ごと（★★C3 と C9 を `rule` で 書き分ける）
★★④ ★★`SURPLUS_PLUS` と `UNMET_MINUS` を ★★別々に 出す（★★合計を 出さない）
★★⑤ ★★宣言（§4）と ★実測が 一致（★★ずれたら 止めて 報告＝★どちらかが 誤り）
★★⑥ ★『引けなかった』を ★別立て（★0として 数えない）
★★⑦ ★★決定論（★LLM 0回・★人が 書いた文を 読む所 0箇所）／ ★口0増 ／ ★新台帳0 ／ ★新語0
★★⑧ ★`/api/control` が ★1秒以内（★退行の検査）
★★⑨（★私）★★『経路表が 埋まった』と 書かない ＝ ★★『17区間で ＋を 観測する口が 無い』を
     ★★★同じ記録に 1行 書く（★★これが この段の 成果物）
```

## 6. ★★やらないこと

```
★★口を 足して ＋を 記録し始めない（★★★この段は『いま どうなっているか』を 出すだけ）
★★`handoff` を 17区間に 書き足さない（★★埋める作業は 次の段）
★★`sends` の 名前が 一致するか（★Taka の `X'`）は ★★★この段では 見ない（★別の数・★次の段）
```
