# CC 管理(MGR) → 設計/監査(CC-α): **★Taka が送信ボタンを押した。★入っていない。★どこで消えたかも分からない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **契機**: Taka（逐語）「**送信ボタンを押したけど何も動かないよ**」

## 1. ★実測（打ち切り無し・投入していない）
```
19:48 /api/tasks 総数 = 155
19:51 /api/tasks 総数 = ★155（Taka が押した後）  ∴ ★task は増えていない

webui プロセス pid 3565640 / 起動 15:26:43 / webui.py の mtime 15:26:21
  ∴ ★プロセスは現行ソースで動いている（D-58 の変更を含む）

/proc/3565640/fd/1,2 → /home/takasan/webui_8770.log
  ★サイズ 0 バイト（4時間25分 稼働・GET も多数在ったのに★1行も無い）
```

## 2. ★分かったこと / ★分からないこと（★混ぜない）
| ★分かった | **押した結果は 2DER に入っていない** |
|---|---|
| **★分からない** | **★どこで消えたか。** ブラウザから出なかったのか／サーバが受けて断ったのか |
| **★理由** | **★サーバが何も記録していない**（ログ 0 バイト）。**∴ 我々の側に手掛かりが無い** |

**★確かめる手段が「もう1度 投入する」しかないので、★確かめない**（Taka 常設の禁止・架空の依頼を本番経路へ入れない）。
**★「たぶん◯◯だろう」を書かない。**

## 3. ★これは Phase 2 の中心そのものである
> **Taka の完了像:「欠損・失敗・未確認が★見える」**
> **★押した結果が入らず、画面は黙り、記録も残らない。** **★完了像の真逆である。**

**★そして今日ずっと直してきたのと同じ形である:**
| 今日直した | 今回 |
|---|---|
| 「無い」とだけ書いて何が無いのか書かない | **★何も書かない**（成功も失敗も出さない） |
> **★「何も出ない」は「まとめている」より重い。** **★0 件でも「取れない」でもなく、★沈黙である。**

## 4. ★要求（基本設計。詳細はあなたが作る）
| # | ★満たすこと |
|---|---|
| **1** | **押した結果が★必ず画面に出る。** **成功なら何が出来たか（`TASK-…`）／失敗なら失敗と出る。★黙らない** |
| **2** | **失敗のとき、★どこで止まったかが分かる**（最低限「サーバに届いていない」と「サーバが断った」を分ける） |
| **3** | **★入力が空なら「空である」と出す**（空を黙って捨てない） |
| **4** | **★サーバ側に、押されたことが残る**（我々が後から「届いていたか」を言えるようにする。★形はあなたが決める） |

**★1〜3 は画面。4 は記録。** **★4 が無いと、次に同じことが起きても我々はまた「分からない」と書くことになる。**

## 5. ★止まってよい場所
| # | |
|---|---|
| 1 | **原因の特定に「投入」が要ると分かった** → **★止めて報告。投入しない** |
| 2 | **4（記録）が Python の変更を要する** → **★止めて報告**（今回は文言では済まない可能性が高い） |
| 3 | **既に同じ Gap が登記されていた** → **★新規に採らず、それを指す** |

## 6. ★私の未確認
| # | |
|---|---|
| 1 | **Taka が入力欄に何を入れたか（空だったか）を私は知らない。** **★聞かない**（Taka に切り分けをさせない。★どちらでも §4-1〜3 は要る） |
| 2 | **`/api/submit` を私は叩いていない**（叩く＝投入のため） |

---
*MGR。★**実測（打ち切り無し・投入していない）**=19:48 の `/api/tasks` 総数 155 に対し Taka が押した後の 19:51 も**155 で task は増えていない**／webui プロセス pid 3565640 は 15:26:43 起動で `webui.py` の mtime 15:26:21 なので**現行ソース（D-58 を含む）で動いている**／`/proc/3565640/fd/1,2` の実体 `/home/takasan/webui_8770.log` は**0 バイト**（4時間25分 稼働し GET も多数在ったのに1行も無い）。★**分かったこと=押した結果は 2DER に入っていない**／**分からないこと=どこで消えたか**（ブラウザから出なかったのか、サーバが受けて断ったのか）／**理由=サーバが何も記録していないので我々の側に手掛かりが無い**——**確かめる手段が「もう1度 投入する」しかないので確かめず、「たぶん◯◯だろう」を書かない**。★**これは Phase 2 の中心そのもの**=Taka の完了像「**欠損・失敗・未確認が見える**」に対し**押した結果が入らず画面は黙り記録も残らない＝完了像の真逆**であり、**今日ずっと直してきた形（「無い」とだけ書いて何が無いのか書かない）より重い——0 件でも「取れない」でもなく沈黙である**。★**要求（基本設計）**=(1)**押した結果が必ず画面に出る**（成功なら `TASK-…`／失敗なら失敗。**黙らない**）(2)**失敗のときどこで止まったかが分かる**（最低限「サーバに届いていない」と「サーバが断った」を分ける）(3)**入力が空なら「空である」と出す**（空を黙って捨てない）(4)**サーバ側に押されたことが残る**（後から「届いていたか」を言えるように。形は CC-α が決める）——**1〜3 は画面、4 は記録で、4 が無いと次に同じことが起きてもまた「分からない」と書くことになる**。★止まってよい場所=原因特定に投入が要ると分かった（**止めて報告。投入しない**）／(4) が Python の変更を要する（**止めて報告**。今回は文言では済まない可能性が高い）／**既に同じ Gap が登記されていたら新規に採らずそれを指す**。★私の未確認=**Taka が入力欄に何を入れたか（空だったか）を私は知らず、聞かない**（Taka に切り分けをさせない。**どちらでも §4-1〜3 は要る**）／**`/api/submit` を叩いていない**（叩く＝投入のため）。*
