# ★Taka 裁定 — Event Trace Phase 1 完了条件 v1.0（これを満たすまで未完了）

- **典拠: Taka（2026-07-28）。★§1 は逐語である。要約・整形・補筆をしていない。**
- **本文書は正典である。改訂できるのは Taka のみ。**
- 親文書: `EVENT_TRACE_PHASE1_WORK_ORDER_v1_0.md` / `EVENT_TRACE_WORK_ORDER_v1_0.md`

---

## 1. 本文（★Taka 逐語）

今回の対応は、egl/acquisition.py の記録追加だけでは不十分です。

1. **egl/acquisition.py を共通 Event Trace の合流点へ接続する**
2. **同一 CLI 依頼で DS→RRI→EGL まで同一 trace_id で追跡する**
3. **全走査監査では head/tail/件数制限付き出力を完全性の証拠に使わない**
4. **監査結果に総件数・確認件数・打ち切り有無を必須記録する**
5. **既存12入口と全書き込み経路を、打ち切りなしで再走査する**

これを満たすまで Phase 1 は未完了とする。

---

## 2. MGR の付記（★Taka の本文ではない）

### 2-1. ★1点だけ、勝手に解釈せずに確認事項として書く
**§1-1 は「`egl/acquisition.py` を合流点へ接続する」である。**
**★本日の実測では、`egl/data/events.jsonl` の★唯一の書き手は `egl/egl/core.py::append_event`（`:119`）であり、`acquisition.py` はその呼び手の1つである。**

| 読み方 | 帰結 |
|---|---|
| **(A) 唯一の書き手（`core.append_event`）に置く** | **`acquisition` 経由の書き込みは★すべて記録される。** **加えて他の呼び手（`runtime_inspection` / `research_acquisition` / `gpu_inspection`）も記録される** |
| (B) `acquisition.py` 自身に置く | `acquisition` は記録されるが、**★他の呼び手が記録されない** |

> **★私は (A) を採る。** **理由: (A) は §1-1 の要求を満たしたうえで、より広く満たす。** **(B) は §1-5「全書き込み経路」と矛盾する。**
> **★ただしこれは私の解釈である。** **「`acquisition.py` 自身の境界にも1件イベントが要る」という意図であれば、一言で足す。**
> **★勝手に範囲を広げないための明示である**（本日、Taka の一行を私が全合流点へ広げて撤回した経緯が在る）。

### 2-2. ★3〜5 は、本日 v2.6 §3-8 で足した規則の完成形である
**私は本日「『存在しない』ことを示す走査に `head` を付けない」を足した。**
**★Taka の 3〜5 は、それを★証拠の要件にまで進めている:**
| Taka | 我々の規則との関係 |
|---|---|
| 3 打ち切り付き出力を**完全性の証拠に使わない** | §3-8 の言い換えだが、**★「使わない」と明示することで、付けたかどうかではなく「証拠として通用するか」の基準になった** |
| 4 **総件数・確認件数・打ち切り有無を必須記録** | **★これは我々に無かった。** **数えたことを書かせる。** 「全部見た」を書かせない |
| 5 **12入口と全書き込み経路を打ち切りなしで再走査** | **★過去の走査を信用しない。** **本日3回、切り詰めた走査が誤りを生んだ** |

**∴ 運用方針 v2.7 §3-9 として 4 を追加する**（私が実施する）。

### 2-3. ★「未完了」の扱い
**★「配線は入った」と「Phase 1 が完了した」を、私は分けて書いてきた。** **その線を Taka が引き直した。**
**∴ 5件すべてが実測で満たされるまで、★どの文書にも「Phase 1 完了」と書かない。**

---
*Taka 裁定 v1.0（Event Trace Phase 1 完了条件）。★§1 は逐語=①`egl/acquisition.py` を共通 Event Trace の合流点へ接続する ②同一 CLI 依頼で DS→RRI→EGL まで同一 `trace_id` で追跡する ③全走査監査では head/tail/件数制限付き出力を完全性の証拠に使わない ④監査結果に総件数・確認件数・打ち切り有無を必須記録する ⑤既存12入口と全書き込み経路を打ち切りなしで再走査する——これを満たすまで Phase 1 は未完了とする。★§2 は MGR の付記=①§1-1 について、本日の実測では `egl/data/events.jsonl` の唯一の書き手は `core.append_event` で `acquisition.py` はその呼び手の1つなので、**唯一の書き手に置く (A) を採る**（(A) は §1-1 を満たしたうえで他の呼び手も記録し、(B) は §1-5「全書き込み経路」と矛盾する）——**ただしこれは MGR の解釈であり、`acquisition.py` 自身の境界にも1件要る意図なら一言で足す。本日 Taka の一行を全合流点へ広げて撤回した経緯があるので、勝手に範囲を広げないための明示である**。②3〜5 は本日 v2.6 §3-8 で足した規則の完成形であり、特に④「総件数・確認件数・打ち切り有無を必須記録」は我々に無かったもので「全部見た」を書かせない基準になる ∴ 運用方針 v2.7 §3-9 として追加する。③「配線は入った」と「Phase 1 完了」を分けて書いてきた線を Taka が引き直した ∴ 5件すべてが実測で満たされるまで、どの文書にも「Phase 1 完了」と書かない。*
