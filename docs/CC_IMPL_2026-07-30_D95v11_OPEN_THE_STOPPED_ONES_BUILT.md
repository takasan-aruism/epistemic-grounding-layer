# 【B】実装 → 設計/監査: **対象外の2件を「開いて読める」ようにしました（集計は1ミリも動かしていません）**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=BUILT
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-30_D95_OPEN_THE_STOPPED_ONES_SPEC_v1_1.md`（**v1.0 は見ていません**）
- **受領した文書**: 上記 / `CC_MGR_2026-07-30_D95v11_APPROVED_NO_CONDITIONS.md`（写しで観測）

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 0. ★2DER 優先原則の4点（★私が実際にやった順）
| # | |
|---|---|
| **① どの入口を使ったか** | **`GET /api/state?task_id=…`**（front door） |
| **② 2DER で何を試したか** | **★まず 2DER に聞きました。** 止まった2件を front door から読めるか |
| **③ 2DER では何ができなかったか** | **★読めました。** `dw_state=BLOCKED` も依頼文も返りました。**★できなかったのは「画面から辿り着くこと」だけです** |
| **④ 何を実装したか** | **★その1点だけ**（画面に開く手段を足す） |

```
再現: GET /api/state?task_id=TASK-2DER-7089D8FC → dw_state=BLOCKED / goal 先頭30字='既存RRI分類結果からBUILD taskを生成する前に、e'
再現: GET /api/state?task_id=TASK-2DER-D6346F79 → dw_state=BLOCKED / 同上
```
> **★私は外側で代行していません。** **★2DER が答えられることは 2DER に聞き、答えられなかった1点だけを実装しました。**

---

## 1. ★受入（★1条件に1つの印・まとめていません）
| # | 条件 | 結果 |
|---|---|---|
| **1** | 止まった2件が開いて読める | **★済**（§0 で front door から `BLOCKED` を実測／画面に開く手段を追加） |
| **2** | 終わった依頼を開く経路が在る | **★済（経路は在る）／★実例0件**（`TASK-2DER` の完了は0件・実測） |
| **3** | `total` が153のまま | **★153** |
| **4** | 和＝合計が変わっていない | **★153 = 153**（117+18+18+0） |
| **5** | `blocked_outside` が和に入っていない | **★入っていません**（和が153のまま） |
| **6** | 「対象外ですが、開いて読めます」 | **★済**（旧「この一覧の対象外です」は0件） |
| **7** | 対象外の内訳が数えた結果と一致（括弧に固定文を置かない） | **★済**（`kinds.join("／")` で組み立て・§2） |
| **8** | 0件のものは語ごと出ない | **★済**（「終わった 0件」は出ません） |
| **9** | 先頭30字＋併記が出る | **★済**（2件でも省いていません） |
| **10** | ID がこの2件だけ | **★済**（差分で確認・153件の一覧は戻していません） |
| **11** | 色を足していない | **★済**（差分で該当0行） |
| **12** | `/api/state` の返りを変えていない | **★済**（変えたのは `/api/submit` の `deferred_summary` のみ・追加） |
| **13** | 配信物の JS が `node --check` | **★OK**（`/command`・`/` とも） |
| **14** | 非回帰 91 passed / 7 failed | **★一致**（顔ぶれ diff 空・走らせ方は §4） |
| **15** | 所要 | **★0.097 秒**（数字だけ） |

## 2. 実装（★3箇所・追加のみ）
```python
# webui.py::deferred_summary — ★集計の母数は1ミリも動かさない
blocked, completed = [], []
for t in seen:
    st = W.derive_state(t, events=evs)[0]
    if st == "BLOCKED":     blocked.append(t)
    elif st == "COMPLETE":  completed.append(t)
out["blocked_outside"]      = len(blocked)              # ★件数はそのまま残す
out["blocked_outside_ids"]  = [{"task_id": t, "head30": _head30(t)} for t in blocked]
out["completed_outside"]    = 0 相当                     # ★今日は0件。完了が出た日に同じ道で出る
out["completed_outside_ids"] = […]
```
```javascript
// ★括弧に中身を固定しない。★出ているものだけを数えた結果のまま名指しする
const kinds=[];
if(b.length) kinds.push("止まった "+esc(b.length)+"件");
if(c.length) kinds.push("終わった "+esc(c.length)+"件");
if(!kinds.length) return "";
… "（"+kinds.join("／")+"）。開いて読めます:" …
// ★新しい描画系を作らない。既存の load() をそのまま呼ぶ
async function openTask(tid){TASK=tid;RUNNABLE=false;setRunControls();$("#tid").textContent=tid;await load();}
```
- **★`_head30` は依頼文の先頭30字を切るだけです。** **★要約していません。★名前を作っていません**（`G-73`）。
- **★LLM を使っていません。** **★キャッシュも並列化もしていません**（`events` は既に1回だけ読んだものを使っています）。
- **★新しい endpoint を作っていません。**

## 3. ★「ID を出さない」との区別（実装源が書くよう求めた点）
> **D-73 の「ID を1件も出さない」は★集計一覧の話です**（153件を並べないこと）。
> **★対象外の2件は、集計一覧ではありません。** **★集計に入っていないものを、開くために名指ししています。**
> **∴ 私はこれを D-73 に反するとは判断しませんでした。** **∴ 止めていません。**
> **★もしこの区別が誤りなら、そう指示してください。** **★私が勝手に広げた区別ではなく、実装源 §3-4 の区別に従っています。**

## 4. ★非回帰の走らせ方（`G-74`）
```bash
/home/takasan/.cc_tmp/d42/run_regression.sh /home/takasan/egl/docs/.cc_tmp/d49/regression_d95.txt
```
**中身**: `cd /home/takasan` してから `twoder/regression/*.py` の**98本**を1本ずつ
`timeout 600 python3 -m twoder.regression.<名前>` で走らせ、**終了コードで数える**（`pytest` は使っていません）。
```
結果: DONE 91 passed / 7 failed
基準との diff: ★空（失敗7本の顔ぶれまで一致）
```

## 5. ★確かめていないこと
1. **★画面をブラウザで開いていません。** **★リンクを押して開けることは、配信物に `openTask` と `load()` の道が在ることまでしか示していません。**
2. **★完了が出た日の表示を見ていません**（今日は0件）。**★作って試していません。**
3. **★止まった2件の先頭30字が同じです**（両方 `既存RRI分類結果からBUILD taskを生成する前に、e`）。**★併記文（「先頭が同じでも別の依頼です」）が、まさにこの形に当たっています。** **★同じ依頼だとは書きません。**

## 6. ★この build でやらなかったこと
- **⑤(b) 通過した処理が読めること** → **★作りに行かず「待つ」**（実装源 §7）。
- **`/api/tasks` の絞り込み拡大** → **★採っていません**（別の問題を混ぜるため）。
- **同じ「無い」を6通りで言っている／注記が11〜12px** → **★持ち越しのまま。消していません。**

## 7. 触ったファイル / commit / 再起動
```
twoder/webui.py のみ（37 insertions / 3 deletions）
```
- **★再起動しました**（配信物で検査するため・訂正後の順番②）。
- **commit していません**（MGR）。**投入していません。** **資料の同期はしていません。**

---
*IMPL BUILT（D-95 v1.1・【B】）。★**2DER 優先原則を守った**——**まず front door（`GET /api/state`）で止まった2件（`TASK-2DER-7089D8FC`/`TASK-2DER-D6346F79`）を聞き、`dw_state=BLOCKED` も依頼文も読めた**ので、**できなかった「画面から辿り着くこと」の1点だけを実装した**（外側で代行していない）。★実装は3箇所・追加のみ=`deferred_summary` に `blocked_outside_ids`/`completed_outside`/`completed_outside_ids` を足し（**件数 `blocked_outside` はそのまま残し、和にも入れない**）、`deferredCard` の注記を「開ける一覧」にし、**開く手段は既存の `load()` を呼ぶ `openTask` だけ**（新しい描画系・endpoint を作らない・LLM 不使用・キャッシュも並列化もなし・`_head30` は先頭30字を切るだけで要約も命名もしない）。★**括弧に中身を固定していない**——`kinds` を数えた結果から組み立てるので**今日は「（止まった 2件）」、完了が出た日は自動で「（止まった 2件／終わった 1件）」になり、0件のものは語ごと出ない（「終わった 0件」と書かない）**。★受入15件を**1条件に1つの印**で判定し全て満たした（`total` 153のまま／和 153=153／`blocked_outside` は和に入っていない／「開いて読めます」／内訳が数えた結果と一致／0件の語が出ない／先頭30字＋併記／**ID はこの2件だけで153件の一覧は戻していない**／色なし／`/api/state` 不変／`node --check` OK／非回帰 91/7・顔ぶれ diff 空・**走らせ方を明記**／所要 0.097秒）。**受入2 は「経路は在る／実例0件」と書いた**（`TASK-2DER` の完了は0件）。★**「ID を出さない」との区別**=D-73 の禁止は**集計一覧（153件を並べないこと）**の話であり、**対象外の2件は集計一覧ではなく、集計に入っていないものを開くために名指ししている** ∴ **D-73 に反するとは判断せず止めなかった**（実装源 §3-4 の区別に従った。誤りなら指示を仰ぐ）。★**確かめていないこと**=**ブラウザを開いておらず、リンクで開けることは配信物に道が在ることまでしか示していない**／**完了が出た日の表示は見ていない（作って試していない）**／**止まった2件の先頭30字が同じで、併記文がまさにこの形に当たっているが「同じ依頼だ」とは書かない**。★やらなかったこと=⑤(b) は作らず待つ／`/api/tasks` の絞り込み拡大は採らない／**「無い」の6通りと注記の文字の小ささは持ち越しのまま消していない**。触ったのは `twoder/webui.py` のみ、再起動は訂正後の順番②に従い実施、commit・投入・資料同期はしていない。*
