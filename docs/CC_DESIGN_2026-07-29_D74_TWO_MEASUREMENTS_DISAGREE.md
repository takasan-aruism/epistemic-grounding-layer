# 設計/監査 → MGR（写: Taka / IMPL）: **D-74 — サーバ内でも★9.1秒 かかる。★そして私の2つの実測が食い違った（原因を特定した）**

- `BUILD_ROLE: 参照`（**調査のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-29_D74_RULING_MOVE_IT_SERVER_SIDE_I_LIFT_MY_CONSTRAINT.md` §4

## 0. ★結論
> **① サーバ内でも★9.118秒 かかる**（HTTP 16.9秒 → 9.1秒。**★速くならない**）。
> **② ★私の2つの実測が食い違った**（依存待ち 36 vs 18）。**★原因を特定した**（§2）。
> **③ ★正しいのは 18 の方である。** **★理由を示す。**
> **④ ★「測ってから進む」が2回とも効いた。** **★どちらも「速いはず」「同じはず」で進めていたら間違えていた。**

---

## 1. ★サーバ内の実測（MGR §4「先に測ること」）
```
再現（★読み取りのみ・投入なし）:
  sys.path に dev-workcell だけを入れる（★twoder を入れると operator.py が stdlib を隠す＝既知の罠に落ちた）
  import          : 0.008 秒
  event 全読み    : 0.045 秒 / task 155件
  ★155件の分類   : ★9.118 秒
```
| | |
|---|---|
| **HTTP 経由**（IMPL 実測） | 16.9 秒 |
| **★サーバ内** | **★9.118 秒** |
| **∴** | **★通信と認証を外しても、★9秒 かかる。** **★「中で計算するだけなら速い」は成り立たなかった** |

> **★重いのは `derive_state` と `next_legal_operation` を★155回 呼ぶこと自体である。**
> **★event の全読みは 0.045秒 で終わっている。** **★遅いのは読み込みではない。**

### 1-1. ★私の計器の問題（★本日9回目・自己申告）
```
最初の実行: sys.path に twoder を入れた → ★ImportError（operator.py が stdlib の operator を隠す）
★これは使用ガイド §4 に書かれている既知の罠である。★MGR も本日1度落ちている。
```
> **★書いてある罠に、★書いてあるとおりに落ちた。**

---

## 2. ★2つの実測が食い違った（★原因を特定した）
```
D-71（/api/state 経由・155件）: 依存待ち 18 / どの区分にも入らない 18
本日（サーバ内・155件）      : 依存待ち ★36 / どの区分にも入らない ★0
```
**★原因（実読で特定）**
```
webui.py:113  is_claude = nlo["actor_role"] in ("MANAGER", "CLAUDE_SENIOR")
webui.py:127  "claude_barrier": ★is_claude          ← ★nlo["claude_barrier"] ではない
webui.py:126  "actor_role": ★nlo["actor_id"]        ← ★nlo["actor_role"] ではない
```
| | |
|---|---|
| **`nlo["claude_barrier"]`** | `_MAP` の第4要素（`CREATED`/`DISPOSITION_REQUIRED`/`READY_FOR_UPPER_REVIEW`/**`BLOCKED`**/**未知状態** が True） |
| **`/api/state` の `claude_barrier`** | **★別物。** `actor_role` が `MANAGER` か `CLAUDE_SENIOR` のときだけ True |
| **∴ `JUDGE_REQUIRED`** | **`_MAP` に無い ∴ `nlo["claude_barrier"]=True`（既定）／★しかし `actor_role="-"` なので `/api/state` では False** |

### 2-1. ★どちらが正しいか（★判断の根拠を示す）
> **★`/api/state` の 18 が正しい。**
> **理由: `nlo["claude_barrier"]` は★`_MAP` に無い状態すべてに True を付ける既定値である**（`dispatch.py:51` の `_MAP.get(state, ("BLOCKED","-","-",True))`）。
> **∴ `JUDGE_REQUIRED` の True は「人を待っている」ではなく★「この状態を知らない」である。**
> **∴ それを「依存待ち」に入れると、★知らないものを人待ちと呼ぶことになる。**

**★私が SPEC §1-1 に書いた判定は、この点で★曖昧だった:**
```
SPEC §1-1: 依存待ち : claude_barrier == True
★どちらの claude_barrier かを書いていない。★2通りに読める。
```
> **★私の SPEC の欠陥である。** **★止まってよい場所④「SPEC が2通りに読める」に自分で当たった。**

## 3. ★SPEC を直す（意味だけ・MGR の裁定を仰ぐ）
| # | 直すこと |
|---|---|
| **1** | **★「`claude_barrier`」と書かず、★`actor_role in ("MANAGER","CLAUDE_SENIOR")` と書く**（`/api/state` と同じ判定） |
| **2** | **★「どの区分にも入らない」は `actor_id == "-"`**（`/api/state` の `actor_role` 欄が返しているもの） |
| **3** | **★`_MAP` に無い状態（`JUDGE_REQUIRED`）は「どの区分にも入らない」に入る。** **★これは「知らない」であって「人待ち」ではない、と★注記する** |

## 4. ★9.1秒 をどうするか（★私は決めない）
| 案 | 判定 |
|---|---|
| **そのまま出す** | **★画面が9秒 止まる。** **★D-63 で直した「押しても黙る」に近い形に戻る** |
| **数えるのをやめ、「出せません（重いため）」と出す** | **★今日ずっとやってきた形。** **★ただし Taka の要求（集約して出す）を満たさない** |
| **既存の `/api/tasks` の作り方を見て、軽い経路が在るか調べる** | **★まだ調べていない。** **★「既存の道が既に在った」が本日10回出ている** |
| 並列化・キャッシュ | **★MGR が「新しい機構を作らない」と禁じた** |

**【設計:CC-α】★3番目を先に調べることを推す。** **★9秒の内訳（どこが遅いか）も測っていない。**
**★ただし推すだけで決めない。** **★MGR §4「測って、なお重ければ止めて報告」に従い、★ここで止める。**

## 5. ★未確認
| # | | 誰が・いつ |
|---|---|---|
| 1 | **9.1秒の内訳**（`derive_state` と `next_legal_operation` のどちらが重いか） | **★測っていない。** CC-α / 指示が在れば |
| 2 | **`/api/tasks` が155件を返すのに何秒かかるか**（★軽い経路の手掛かり） | **★測っていない** |
| 3 | **`JUDGE_REQUIRED` が `_MAP` に無い理由** | **★分からない**（`G-72` と同じ） |

---
*CC-α D-74（調査のみ）。★結論=①**サーバ内でも 9.118秒**（HTTP 16.9秒 → 9.1秒で**速くならない**）②**私の2つの実測が食い違った**（依存待ち 36 vs 18）が**原因を特定した** ③**正しいのは 18 の方**で理由を示す ④**「測ってから進む」が2回とも効いた**（どちらも「速いはず」「同じはず」で進めていたら間違えていた）。★サーバ内の実測=import 0.008秒／event 全読み 0.045秒／**155件の分類 9.118秒** ∴ **通信と認証を外しても9秒かかり「中で計算するだけなら速い」は成り立たなかった**——**重いのは `derive_state` と `next_legal_operation` を155回呼ぶこと自体で、event の全読みは 0.045秒 で終わっている**。★**私の計器の問題（本日9回目）**=`sys.path` に `twoder` を入れて `operator.py` が stdlib を隠す既知の罠に落ちた（**使用ガイド §4 に書かれている罠に、書いてあるとおりに落ちた**）。★**食い違いの原因を実読で特定**=`webui.py:127` の `claude_barrier` は **`nlo["claude_barrier"]` ではなく `is_claude`**（`actor_role` が `MANAGER`/`CLAUDE_SENIOR` のときだけ True）、`webui.py:126` の `actor_role` は **`nlo["actor_id"]`** ∴ `JUDGE_REQUIRED` は `_MAP` に無いので `nlo["claude_barrier"]=True`（既定）だが `/api/state` では False。★**正しいのは 18**——`nlo["claude_barrier"]` は `_MAP.get(state, (...,True))` の**既定値で `_MAP` に無い状態すべてに True を付ける** ∴ **`JUDGE_REQUIRED` の True は「人を待っている」ではなく「この状態を知らない」であり、依存待ちに入れると知らないものを人待ちと呼ぶことになる**。★**私の SPEC §1-1 は「どちらの `claude_barrier` か」を書いておらず2通りに読めた**——**自分が書いた「止まってよい場所④」に自分で当たった** ∴ SPEC を直す（`claude_barrier` と書かず `actor_role in ("MANAGER","CLAUDE_SENIOR")` と書く／「どの区分にも入らない」は `actor_id == "-"`／**`_MAP` に無い状態は「知らない」であって「人待ち」ではないと注記**）。★**9.1秒をどうするかは決めない**——そのまま出せば画面が9秒止まり「押しても黙る」に近い形に戻る／「出せません（重いため）」は Taka の要求を満たさない／**既存の `/api/tasks` の作り方に軽い経路が在るかを先に調べることを推す**（「既存の道が既に在った」が本日10回出ている）／並列化・キャッシュは MGR が禁じた——**MGR §4「測って、なお重ければ止めて報告」に従いここで止める**。★未確認=**9.1秒の内訳を測っていない**／`/api/tasks` の所要時間を測っていない／`JUDGE_REQUIRED` が `_MAP` に無い理由は分からない。*
