# 【実測 / `EVO-0022`】`resolve` が履歴を返す — **裁定は front door から全部 読める（6件）**

- **宛: 設計/監査(CC-α)** / 写: MGR / Taka / 発: 実装(IMPL) / 2026-08-02 03:4x / TYPE=BUILT
- **規律 v1.3 確認済 ／ 9項目 確認済（外れた番号なし）** ／ **実装源**: `CC_DESIGN_2026-08-02_EVO0022_BUILD_SPEC_RESOLVE_RETURNS_HISTORY.md`

---

# 1. 変更

```
twoder/roadmap_registry.py  +7 / -0（history 関数。docstring 2行含む）
twoder/webui.py             +6 / -1（resolve_view に分岐4行 ＋ 欄1行）
合計 13挿入 1削除。ids.py・台帳(_append)・record の中身は触っていない。TASK-/DE- へは広げていない。
```

# 2. 受入

| # | 受入 | 判定 | 実測 |
|---|---|---|---|
| (1) | `EVO-0031` の裁定5件が全部 読める | **○** | `history` **21件**／`status_note` に「裁定」を含むもの **6件**（裁定1〜5＋受領1）。逐語で読める |
| (2) | `record` は従来どおり最新1件 | **○** | 変更前後の `record` が**完全一致** |
| (3) | 戻せる | **○** | 手で2箇所を戻した版が `HEAD` と**バイト一致**（両ファイル） |
| (4) | `limit=50` が効く／応答が大きすぎない | **○（但し実データでは切っていない）** | `limit=3 → 3件` / `limit=1 → 1件` / `limit=1000 → 21件`。実データは 21件 < 50 ∴ **切られていない** |

# 3. 逐語（`GET /api/resolve?id=ITEM-2DER-EVO-0031` の `history` にある裁定・先頭44字）

```
裁定(この回は .md を作らない)=原因確定。理由は generate_via_run…
裁定2(.md を作らない・これが .md 無し伝達の2回目)=受入「理由が front…
裁定3(.md 無し)=原因確定。2DER は成果物を ws/impl.py として作る…
裁定4(.md 無し)=★試験が初めて走り通った(独立監査・test_result.st…
裁定5(.md 無し)=同型8件目を受領。今回は「作って捨てた」ではなく★最初から作って…
設計の回答を受領。①JUDGE_REQUIRED からの正規の呼び方は無い(私が2回試し…
```

# 4. 応答の大きさ

```
変更前 1,188字 (1,608 bytes) → 変更後 20,684字 (30,240 bytes)。21件ぶん。
★予告（15,000〜40,000 bytes）の範囲内。40,000 は超えていない。
```

# 5. 測って分かった別のこと（直していない）

```
history の全21件の registered_at が すべて `2026-07-11T08:00:00` で同一。
∴ 履歴の順序は timestamp ではなく 台帳の追記順に依存している。
本件では順序は正しく出ている（裁定1→5が並ぶ）が、時刻では並べ替えられない。
★私は直していない（実装源の範囲外）。
```

# 6. 走行・戻し方

```
webui 再起動を確認（9項目 #5）: 起動 03:33:01 > webui.py 03:32:36 / roadmap_registry.py 03:32:31。
:8005 を叩いていない。task を増やしていない。commit していない。
戻し方: ① roadmap_registry.py の history 関数を消す ② webui.py の resolve_view から分岐4行と history 欄を消す。
        ※ commit 後は `git checkout --` では戻らない。
```
