# 【BUILD SPEC】`Ledger` — **★生出力に既存の口から到達できるようにする（1箇所）**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 03:0x / TYPE=BUILD_SPEC
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-31_RULING_TWO_ROUTES_CONNECTED_BY_EVIDENCE.md`（逐語）／**実施**: `CC_MGR_2026-07-31_D151_…`
- **★これは何に対して発火するのか**: **★`RUNTIME_INSPECTION` が取った観測の生出力。★GPU に限らない。**
- **★2DER 優先原則の例外**（IMPL が書く）。**★2DER の担当工程に数えない。★担当工程数の差分は 0 になる見込み。★0 なら「0」と書く。**

## ★止まり方（★D-151 §1。★今日 引き直された線）
```
★止まってよい : ★この1件が実行不能である証拠／データ破壊・既存互換性破壊・ロールバック不能の具体的証拠
★止まらない   : ★別の欠陥を見つけた ／ もっと良い案を思いついた ／ 2通りに読めると感じた
★2通りに読めたら ★狭い方を採って進み、★迷いを報告に1行 書く（★私に聞かない）
★見つけた欠陥は★登記して進む
```

---

# 1. ★設計の調査（★私が read-only で読んだ。★あなたは確かめ直してよい。★再調査は広げない）

| # | 事実 | 根拠 |
|---|---|---|
| 1 | **`blob://…` は★保管庫ではない。★ハッシュから合成された文字列である** | **`egl/egl/acquisition.py:150`** `"raw_blob_ref": f"blob://{arun.get('raw_content_hash')}"` |
| 2 | **★`blob://` を読む箇所は★0件**（作る箇所1・読む箇所0。5 repo `*.py` 全走査・打ち切り無し） | 同上 |
| 3 | **★生出力は実際に保存されている** | **`twoder/runs/<RIREQ-…>.snapshot.json`** = `{"host":…, "raw": {kind: {"command","stdout","stderr","exit_code"}}}`（`runtime_inspection.py:129-130`） |
| 4 | **★kind ↔ ハッシュの対応も記録されている** | `runtime_inspection.py:119` `stdout_refs.append({"observation": kind, "stdout_hash": _h(out)})` |
| 5 | **★OBS 記録は `raw_content_hash` を持つ** | `acquisition.py:148`／実測（`OBS-00955` などの record） |
| 6 | **★`ARUN` 記録に `RIREQ` への参照は無い**（実測: record のキー13個に無い） | `GET /api/resolve?id=ARUN-00954` |

> **★∴ 既存記録が持っている接続は「★ハッシュ」である。** **★新しい参照を作る必要はない。**

---

# 2. ★やること（★1箇所だけ）

```
★観測の生出力に、★既存の口から到達できるようにする。
★★どの形にするかは あなたが選ぶ。★選んだ理由を1行 書く。★狭い方を採ること。
```
| 条件（★正典のまま。★増やさない） |
|---|
| **★新しい台帳を作らない ／ ★新しい API・読み出し口を作らない**（★`/api/*` を増やさない） |
| **★既存記録が保持する参照を通すだけ**（★§1 の「ハッシュ」がそれである） |
| **★一度に直す箇所は1件** ／ **★GPU 固有実装を入れない**（`gpu`/`nvidia` を diff に0件） |
| **★fail-closed**: ★ハッシュが一致しない／実体が無い／複数に当たる ときは**★返さない**（★部分的に返さない） |

---

# 3. ★受入（★1条件に1つの印。★まとめない）

| # | 受入 | ★示し方 |
|---|---|---|
| **L-1** | **★生出力に到達できる** | **★既存の口を叩いて、★実データ（`ss` や `nvidia-smi` の実際の出力文字列）が返ることを示す** |
| **L-2** | **★fail-closed** | **★ハッシュ不一致／実体なし の場合に★返さないことを1つ示す**（★作った偽データではなく、★存在しないハッシュで叩く） |
| **L-3** | 後方互換 | **★下の基準値11件を修正後に取り直し、1件ずつ突き合わせる** |
| **L-4** | 1箇所・GPU 固有語なし | 変更ファイル／hunk 数／挿入削除行数 ／ **diff に `gpu`/`nvidia` が0件**（大小無視・打ち切り無し） |
| **L-5** | **★観測経路7段階**（正典 §次の作業 2） | **★ID と実データで PASS/FAIL**: Request / RRI / Runtime Inspection / Observation / **Ledger から生出力取得** / DW 要約 / Response |

## 3-1. ★後方互換 基準値（★設計が修正前に取った・2026-07-31 02:55:08）
```
ARUN-00954 true/13   OBS-00955 true/10   DE-0525 true/10   TASK-2DER-B11764B3 true/3
ETR-fdf52322e5bf true/5   UTT-1010 true/12   SUBMIT-zOlryQ true/14   SUBMIT-_ayfew true/14
TASK-2DER-67FE6548 true/3   OBS-00971 true/10   NO-SUCH-ID-XYZ false/0
★修正前の現状（★到達できないことの証拠）:
   GET /api/resolve?id=blob://sha256:f250fd70… → ★resolved:false
```
> **★L-3 の期待値**: **★11件は不変**。**★`OBS-…` のキー数が増えるのは可**（★生出力を載せる形を選んだ場合）。**★増えたら「増えた」と書き、★他の10件が不変であることを示す。**

---

# 4. ★修正後の確認（★正典 §次の作業 2）
```
★同一依頼で E2E を1回（★依頼文は第2試行の指示書から機械で抜く。★sha1 0c458f38… を確認）
★POST /api/submit ★1回 → ★直後に GET /api/receipt → 7段階を ID と実データで判定
★★予告を先に書く: ★「Ledger から生出力を取れるか」を投入前に書き、★外れたら「外れた」と書く
★★観測経路は task を経由しない ∴ ★同じ文でも観測は毎回 新しく走る（★D-151 §4）
★run_next は押さない ／ ★GENERATE は押さない（★正典で禁止）
★webui 再起動が要るなら★全件 記録する（操作内容/操作者/理由/既存運用か/主体判定への影響＋run-gate 初期化）
```

# 5. ★やってはいけないこと
```
★観測を task の下流へ移す設計変更（★正典で禁止）  ★GENERATE を押す  ★新しい配線総点検
★EXEC_ARCH 全体の再調査  ★新しい台帳・API・ID 族の追加
★★Ledger 修正と PLAN 証拠接続（`G-87`）の同時実装  ★commit しない  ★61本の非回帰は走らせない
★:8005 を自分で叩かない（★2DER が内部で呼ぶのは可。★呼ばれたら1行 書く）
```

# 6. ★報告（★正典の6項目。★増やさない）
```
1. 変更した1箇所  2. 観測経路7段階の PASS/FAIL  3. Last PASS  4. First FAIL
5. ★2DER 担当工程数の前回差分（★0 なら「0」）  6. 次に直す1件
★受入 L-1〜L-5 は別表で1つずつ  ★予告の当否を書く
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---
**決めたこと**: **①既存記録が持っている接続は「ハッシュ」である——`blob://` は保管庫ではなく合成文字列（読む箇所0）、生出力は `runs/<RIREQ>.snapshot.json` に在り、`stdout_refs` が kind↔ハッシュを、OBS が `raw_content_hash` を持つ ②直すのは1箇所、形は IMPL が狭い方を選んで理由を1行書く、fail-closed（不一致・実体なし・複数該当なら返さない） ③確認は観測経路7段階を ID と実データで、予告を投入前に書く。`G-87`（PLAN の証拠接続）は同時に実装しない。**
