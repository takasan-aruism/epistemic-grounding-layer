# 実装 → 設計/監査: **取りこぼし件数を出しました。0 を「問題なし」と読ませない形で**（投入なし）

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-29 / TYPE=BUILT
- **運用方針 確認済（版: v2.8）**
- 実装源: `CC_DESIGN_2026-07-29_D55_DROPPED_BEFORE_UI_BUILD_SPEC_v1_0.md` v1.0
- **受領した文書**: 上記 / `CC_DESIGN_2026-07-29_D55_HANDOFF_TO_IMPL.md`

## 到達経路
- [x] **(A) IMPL が自分で読んで転記した。設計/監査へ自動で届く経路は通っていない。**

## 結果（★2軸）
### 経路
- [x] **実装した**（`/command` の JS のみ・**Python 側は1行も変えていない**）
- [x] **止まってよい場所には当たりませんでした**（§5）
### 実測
- [x] 受入1 (b) 0件の表示（`ETR-0f6fae35665a`・Σ=0）
- [x] 受入2 **併記文言が MGR 逐語と一致**
- [x] 受入3 (c)「取得できない」（`TASK-2DER-B9B4DA3B`・**0件と出さない**）
- [x] 受入4 **`ok` / `bad` クラスを使っていない**（差分の検算で0行）
- [x] 受入5 既存が不変（sha256 一致）
- [x] 受入6 非回帰 91 passed / 7 failed（顔ぶれ diff 空）
- [ ] **(a)（非0）は★検証していません**（実例が無く、作りません）

---

## 1. 実装（既存カードの中・新しいカードも新しい取得も作っていません）
```
場所: 既存の「欠損・失敗・未確認」カードの中（★「通過した処理」には入れていません）
データ: /api/resolve を引いた時点で既に持っている events を数えるだけ
        N = Σ (event.dropped_before or 0)      ★新しい fetch を1つも足していません
```
| 場合 | 出す文言 |
|---|---|
| (a) 1件以上 | **「記録できた取りこぼし: N 件」** |
| (b) run は引けたが0件 | **「記録できた取りこぼし: 0 件」** |
| (c) run を引けない | **「取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）」** |
| `truncated` のとき | **「取得した範囲での合計である」を併記** |

**★併記文言（どの場合でも必ず出します・1文字も変えていません）:**
```
これは★記録できた取りこぼしの件数である。取りこぼしの後に成功した記録が無ければ、件数そのものが残らない（G-38）
```

### 1-1. ★実装源に無い判断を1つしました（申告）
**実装源の (c) は「run_id が無く run を引けない」です。**
**`run_id` は在るが `/api/resolve` が引けない場合も、私は (c) と同じ表示にしました。**
- **理由: どちらも「取得できない」であり、0件と出すべきでないのは同じだからです。**
- **★ただし文言は「この依頼の run_id が記録に無い」のままです。** **`run_id` が在るのにこの文言が出る余地があります。**
- **★これは私の判断です。** **別の文言が要るなら指示してください。**

### 1-2. ★既存の表示を壊していないこと
**「欠損・失敗・未確認」カードは D-49 で「1件も無ければ『欠損・失敗の記録なし』と出す」形にしていました。**
**取りこぼしを常時表示にすると、その分岐が二度と成立しなくなります。** **∴ 組み立てを分け、`記録なし` の表示を残しています:**
```
body = (他の項目が1件も無ければ「欠損・失敗の記録なし」) + 取りこぼしブロック
```
**★一度は壊す書き方をしましたが、投入前に気づいて直しています。**

## 2. 受入（★投入なし。既存の run と task で足りました）
### 2-1. 受入2（併記文言・画面 HTML を実取得して確認）
```
再現: GET /command （認証つき）
  併記文言（逐語）              ★在り
  (a)(b) の文言「記録できた取りこぼし: 」  在り
  (c) の文言「取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）」 在り
  truncated 時の「取得した範囲での合計である」 在り
  droppedBlock 本体             在り
```

### 2-2. 受入1（(b) 0件）
```
再現: GET /api/resolve?id=ETR-0f6fae35665a
  Σ dropped_before = 0 / truncated = False
  → 「記録できた取りこぼし: 0 件」＋併記文言
```

### 2-3. 受入3（(c) 取得できない）
```
再現: GET /api/state?task_id=TASK-2DER-B9B4DA3B
  etrace_run_id = None
  → rec が無い ∴「取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）」
  ★「0 件」とは出しません。
```

### 2-4. 受入4（色を使っていないこと）
```
再現: git diff webui.py | grep "^+" | grep -E "pill (ok|bad)|class='(ok|bad)'|class=\"(ok|bad)\""
結果: ★該当0行
```
**使ったのは既存の `k` / `v` / `mut` のみです。** **緑にも赤にもしていません。**

### 2-5. 受入5・6
```
受入5: GET /api/state?task_id=TASK-2DER-D84215A8
       before acc9ddba5f0f4bec / after acc9ddba5f0f4bec  ★一致
受入6: 非回帰98本 91 passed / 7 failed（基準と★顔ぶれ diff 空）
差分:  webui.py | 22 insertions(+), 6 deletions(-)   ★すべて HTML/JS 文字列の中
```

## 3. 守った禁止事項（実装源 §1-3）
- **「取りこぼしなし」「問題なし」と読める文言を1つも使っていません。**
- **`ok` 色を使っていません**（0 は良い知らせではないため）。
- **`bad` 色も使っていません**（0 でも非0でも事実であって評価ではないため）。
- **`G-38` を塞いでいません**（`emit` に触っていません）。
- **実例を作っていません**（`emit` をわざと失敗させていません）。
- **Python 側・endpoint・`ids.py` を1行も変えていません。既存カードを1つも消していません。勘定科目に触れていません。**
- **投入していません。**

## 4. 予想と実際（実装源 §4-1）
| 項目 | 設計の予測 | **実際** | 判定 |
|---|---|---|---|
| 受入1 が通る | 通る方に賭ける | **通った**（Σ=0） | 当たり |
| 受入3 が通る | 通る方に賭ける | **通った**（`etrace_run_id=None`） | 当たり |
| **(a) の表示** | **確かめられない** | **★確かめていません**（実例0件・作りません） | **未検証のまま** |

## 5. ★未確認（引き継ぎます）
1. **(a)（非0）の表示は検証していません。** **実例が無く、作らない指示です。** **`G-61` と同じ扱いで、未検証のまま残ります。**
2. **★私はブラウザで画面を見ていません。** **示したのは「HTML にその文言と関数が在ること」と「それが読むデータが実際に返ること」の2点です。**
3. **`stderr` に落ちた実例が在るかを見ていません。**

## 6. 触ったファイル / commit
```
twoder/webui.py のみ（HTML/JS 部分）
```
**commit していません**（MGR）。

---
*IMPL BUILT（D-55）。取りこぼし件数を**既存の「欠損・失敗・未確認」カードの中**に出した——`/command` の JS のみで **Python 側は1行も変えず**（22 insertions / 6 deletions・すべて HTML/JS 文字列内）、`/api/resolve` を引いた時点で持っている events を `N = Σ(dropped_before or 0)` で数えるだけで**新しい fetch を1つも足していない**。3つの場合を別々の文言で出し（(a)「記録できた取りこぼし: N 件」／(b)「0 件」／**(c)「取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）」**）、**併記文言は MGR 逐語のまま・どの場合でも必ず出す**、`truncated` なら「取得した範囲での合計である」を併記。★実装源に無い判断を1つ申告=**`run_id` は在るが引けない場合も (c) と同じ表示にした**（どちらも「取得できない」で0件と出すべきでないため。ただし文言は「run_id が記録に無い」のままなので、`run_id` が在るのにこの文言が出る余地があり、別文言が要るなら指示を仰ぐ）。★既存を壊していないことも申告=取りこぼしを常時表示にすると D-49 の「1件も無ければ『欠損・失敗の記録なし』」の分岐が成立しなくなるため組み立てを分けて残した（**一度は壊す書き方をしたが投入前に気づいて直した**）。受入=**投入なし**で6件（(b) は `ETR-0f6fae35665a` の Σ=0／併記文言の逐語一致を HTML 実取得で確認／(c) は `TASK-2DER-B9B4DA3B` の `etrace_run_id=None` で**0件と出さない**／**`ok`/`bad` クラスは差分検算で該当0行**／`/api/state` の sha256 が `acc9ddba5f0f4bec` で前後一致／非回帰 91/7・顔ぶれ diff 空）。禁止事項（「問題なし」と読める文言・緑・赤・`G-38` を塞ぐ・実例を作る・Python 変更・既存カード削除・勘定科目・投入）はすべて守った。予測は受入1・3 とも当たり、**(a) の表示は確かめられないという予測どおり未検証のまま**（`G-61` と同じ扱い）。★未確認=(a) 非0の表示は未検証／**ブラウザで画面を見ていない**（示したのは HTML に在ることとデータが返ることの2点）／`stderr` の実例は見ていない。commit していない。*
