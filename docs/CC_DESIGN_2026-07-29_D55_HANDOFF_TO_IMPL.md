# 設計/監査 → **実装(IMPL)**: **取りこぼし件数を出してください。★ただし「0件」を安心の材料にさせない**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**送付状。★実装源は本文書ではない**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.8`）** / **承認**: `CC_MGR_2026-07-29_D55_SPEC_APPROVED.md`

## 1. ★実装源（これ1本）
```
CC_DESIGN_2026-07-29_D55_DROPPED_BEFORE_UI_BUILD_SPEC_v1_0.md   ← BUILD_ROLE: ★実装源
```

## 2. ★この build の一行
> **数字を出すのではない。★数字の限界を出す。**
> **「取りこぼし 0件」は「失われていない」ではない。★「失われたことを★記録できた件数が0」である。**

## 3. ★いちばん外しやすい1点（先に言う）
**3つの場合を、★それぞれ違う文言で出す。** **★2つにまとめない。**
| 場合 | 文言 |
|---|---|
| (a) 1件以上 | 「記録できた取りこぼし: N 件」 |
| (b) run は引けたが0件 | 「記録できた取りこぼし: 0 件」 |
| **(c) ★run_id が無く引けない** | **「取りこぼしの件数は取得できない（この依頼の run_id が記録に無い）」** |

> **★(c) を「0件」と出さない。** **「取れない」と「0である」は違う。**
> **★(c) は実在する**: `TASK-2DER-B9B4DA3B` は `etrace_run_id = None`（CLI 投入・`G-36`）。**架空の場合分けではない。**

## 4. ★併記文言（1文字も変えない・どの場合でも必ず出す）
```
これは★記録できた取りこぼしの件数である。
取りこぼしの後に成功した記録が無ければ、件数そのものが残らない（G-38）
```

## 5. ★色（両方とも禁止）
| # | |
|---|---|
| 1 | **緑・OK 色にしない**（`ok` を使わない）。**0 は良い知らせではない** |
| 2 | **赤・警告色にもしない**（`bad` を使わない）。**★0 でも非0でも、これは事実であって評価ではない** |

## 6. ★確かめられないものが1つ在る（先に決めてある）
- **(a)（1件以上）の表示は★検証できない。** **実例が0件であり、★作らない**（`emit` をわざと失敗させない）。
- **∴ 「通った」と書かない。** **★`G-61` と同じく、未検証のまま登記する。**
- **★これは MGR が承認済である。** **あなたが「確かめられなかった」と書いても、不合格にならない。**

## 7. ★止まってよい場所
1. **`/api/resolve` の返りに `dropped_before` を数える経路が無かった** → **★私の見立てが外れた。報告**
2. **JS だけでは足りず Python を変える必要が出た** → **★止めて報告**（新しい口に当たりうる）
3. **(c) の判定が `etrace_run_id` だけでは足りなかった** → 報告
4. SPEC が2通りに読める

## 8. ★投入は1回も要らない
```
受入(b) → ETR-0f6fae35665a（実測: dropped_before 0件）
受入(c) → TASK-2DER-B9B4DA3B（実測: etrace_run_id = None）
```
**★今日すでに投入は6回ある。** **7回目を作らずに検証できる。**

---
*CC-α → IMPL 送付状（D-55）。★実装源は `CC_DESIGN_2026-07-29_D55_DROPPED_BEFORE_UI_BUILD_SPEC_v1_0.md` の1本。★一行=**数字を出すのではなく数字の限界を出す**——「取りこぼし 0件」は「失われていない」ではなく「失われたことを**記録できた**件数が0」。★いちばん外しやすい1点=**3つの場合をそれぞれ違う文言で出し2つにまとめない**（(a)1件以上／(b)run は引けたが0件／**(c)run_id が無く引けない=「取りこぼしの件数は取得できない」**）——**(c) を「0件」と出さない。「取れない」と「0である」は違う。(c) は実在する**（`TASK-2DER-B9B4DA3B` は `etrace_run_id=None`・CLI 投入・`G-36`）ので架空の場合分けではない。★併記文言は1文字も変えず、どの場合でも必ず出す。★色は両方禁止（**緑にしない=0 は良い知らせではない／赤にもしない=0 でも非0でも事実であって評価ではない**）。★確かめられないものが1つ在り先に決めてある=**(a) の表示は検証できない**（実例0件で作らない）ので「通った」と書かず `G-61` と同じく未検証のまま登記する——**MGR 承認済なので「確かめられなかった」と書いても不合格にならない**。★止まってよい場所4件。★**投入は1回も要らない**（(b)=`ETR-0f6fae35665a`／(c)=`TASK-2DER-B9B4DA3B`。今日すでに6回投入しており7回目を作らずに検証できる）。*
