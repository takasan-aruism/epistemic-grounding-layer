# 設計/監査 → MGR（写: Taka / IMPL）: **D-75 — 9.1秒の正体は★同じファイルを155回 読み直していること。1回なら 0.032秒。★ただし注ぎ口が無い**

- `BUILD_ROLE: 参照`（**調査のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-29_D74_RULING_18_IS_RIGHT_AND_MEASURE_THE_BREAKDOWN.md`

## 0. ★結論
> **① 9.1秒の正体は★`_read_events(t)` を155回 呼ぶことである**（4.56秒）。
> **② ★同じファイルを155回 読み直している。** **1回読めば★0.032秒 で全734 event が取れる。** **★142倍の無駄である。**
> **③ ★しかし「読み込み済みを渡す口」が★無い。** **`derive_state(task_id)` は中で必ず自分で読む。**
> **④ ∴ 速くするには★DW を変えることになる。** **★私は変えない。裁定を仰ぐ。**

---

## 1. ★内訳の実測（MGR §依頼・打ち切り無し）
```
再現（★読み取りのみ・投入なし・sys.path は dev-workcell だけ）:
  derive_state ×155         : 4.600 秒
  next_legal_operation ×155 : 4.502 秒   （★内部で derive_state を呼ぶ）
  _read_events(t) ×155      : 4.560 秒   ← ★これが本体
  ★_read_events(None) 1回   : ★0.032 秒 / 734 event
```
| | |
|---|---|
| **★遅さの正体** | **`_read_events(t)` が★毎回 events.jsonl 全体を読み、`task_id` で絞っている**（実読・`workcell.py:56-69`） |
| **★倍率** | **0.032秒 × 155 ≒ 5秒。** **★1回で済むものを155回 読んでいる** |
| **★event 総数** | **734件。** **★155件の task に対して 734 event しか無い**（1件あたり約5 event） |

> **★データが多いのではない。** **★読み方が155回なだけである。**

## 2. ★「既存の道が既に在った」が、★半分だけ在る（11回目）
```
再現（実読）: _active_2der_tasks は ★_read_events(None) を1回だけ呼んでいる（正しい）
             その後 ★derive_state(t) を task ごとに呼ぶ → ★そこで155回 読み直す
```
> **★同じ関数の中で、★1回読みと155回読みが同居している。**
> **★1回読む道は在る。** **★それを使い続ける道が無い。**

## 3. ★注ぎ口が無い（★実読で確認）
```
再現: grep -n "def derive_state\|def _read_events\|def next_legal_operation"
  workcell.py:56  def _read_events(task_id=None)      ← ★引数は task_id だけ
  workcell.py:114 def derive_state(task_id)           ← ★events を受け取れない
  dispatch.py:47  def next_legal_operation(task_id)   ← ★同上
```
| | |
|---|---|
| **★読み込み済みを渡せるか** | **★渡せない。** 3つとも `task_id` しか受け取らない |
| **∴ 速くするには** | **★`derive_state` の署名を変える**（`events=None` を足す等）**＝DW を変える** |
| **★MGR の禁止** | **「DW 仕様を書き換えない」**（D-42 から一貫）／**「新しい機構を作らない」**（D-74） |

> **★∴ 私は作れない。** **★「あと1段で速くなる」とも書かない**——**★その1段が DW の変更だからである。**

## 4. ★選択肢（★私は決めない。代償をそのまま出す）
| # | 案 | 代償 |
|---|---|---|
| **A** | **そのまま9秒 出す** | **★画面が9秒 止まる。** **★D-63 で直した「押しても黙る」に近い形に戻る** |
| **B** | **「出せません（数えるのに9秒 かかるため）」と出す** | **★Taka の要求（集約して出す）を満たさない。** **★ただし嘘は言わない** |
| **C** | **`derive_state` に `events` を渡せるようにする**（既定 `None` で現状と同じ） | **★DW を変える。** **★MGR の禁止に触れる。** **★ただし追加のみで既存の呼び手は1つも変わらない** |
| **D** | **件数を先に出し、区分は開いたときに数える** | **★開いたときに9秒 待つ。** **★問題が移動するだけ** |

**【設計:CC-α】★私は推さない。** **★理由: A〜D はどれも、★私が今日「やめよう」と言ってきたもののどれかに当たる。**
- **A は「黙る」に戻る。** **B は要求を満たさない。** **C は禁止に触れる。** **D は問題を移す。**
> **★4つとも欠点が在る、と★そのまま出す。** **★1つを推して欠点を小さく書かない。**

## 5. ★1つだけ、事実として添える（★推奨ではない）
```
C の「追加のみ」がどういう意味か:
  derive_state(task_id, events=None) にすると、★既存の155箇所の呼び出しは1文字も変わらない。
  ★渡した時だけ、読み直しを飛ばす。
```
> **★これは「DW を変えない」に触れるかどうかの判断材料である。** **★判断は MGR がする。**
> **★私は「触れない」とは書かない。** **★署名が変われば、それは変更である。**

## 6. ★未確認
| # | | 誰が・いつ |
|---|---|---|
| 1 | **`/api/tasks` が155件を返す所要時間** | **★測っていない**（`_read_events(None)` 1回のはずだが★確かめていない） |
| 2 | **9秒が「毎回」か「初回だけ」か**（OS のファイルキャッシュ） | **★測っていない。** **★2回目が速い可能性が在る** |
| 3 | `JUDGE_REQUIRED` が `_MAP` に無い理由 | `G-72` と同じ・不明 |

---
*CC-α D-75（調査のみ）。★結論=①**9.1秒の正体は `_read_events(t)` を155回 呼ぶこと**（4.56秒）②**同じファイルを155回 読み直しており、1回読めば 0.032秒 で全734 event が取れる＝142倍の無駄** ③**しかし「読み込み済みを渡す口」が無く `derive_state(task_id)` は中で必ず自分で読む** ④∴ 速くするには DW を変えることになり、**CC-α は変えず裁定を仰ぐ**。★内訳の実測=`derive_state`×155 4.600秒／`next_legal_operation`×155 4.502秒（内部で `derive_state` を呼ぶ）／**`_read_events(t)`×155 4.560秒**／**`_read_events(None)` 1回 0.032秒・734 event** ∴ **データが多いのではなく読み方が155回なだけ**（155 task に対し 734 event しかなく1件あたり約5 event）。★**「既存の道が既に在った」が半分だけ在る（11回目）**=`_active_2der_tasks` は `_read_events(None)` を**1回だけ**呼んでいる（正しい）が、その後 `derive_state(t)` を task ごとに呼んで**155回 読み直す** ∴ **同じ関数の中で1回読みと155回読みが同居しており、1回読む道は在るがそれを使い続ける道が無い**。★**注ぎ口が無い**（`_read_events(task_id=None)`／`derive_state(task_id)`／`next_legal_operation(task_id)` の3つとも `task_id` しか受け取らない）∴ 速くするには `derive_state` の署名を変える＝**DW を変える**ことになり、**MGR の「DW 仕様を書き換えない」「新しい機構を作らない」に触れる** ∴ **CC-α は作れず「あと1段で速くなる」とも書かない（その1段が DW の変更だから）**。★選択肢4つの代償をそのまま出す=A そのまま9秒（**「押しても黙る」に近い形に戻る**）／B「出せません」（**要求を満たさないが嘘は言わない**）／C `events` を渡せるようにする（**DW を変えるが追加のみで既存の呼び手は1つも変わらない**）／D 開いたときに数える（**問題が移動するだけ**）——**CC-α は推さない。4つとも欠点が在るとそのまま出し、1つを推して欠点を小さく書かない**。★C の「追加のみ」の意味だけ事実として添える（`derive_state(task_id, events=None)` にすれば既存155箇所の呼び出しは1文字も変わらず、渡した時だけ読み直しを飛ばす）——**判断材料であって推奨ではなく、「触れない」とは書かない。署名が変われば変更である**。★未確認=`/api/tasks` の所要時間を測っていない／**9秒が「毎回」か「初回だけ」か（OS のファイルキャッシュ）を測っておらず2回目が速い可能性が在る**／`JUDGE_REQUIRED` が `_MAP` に無い理由は不明。*
