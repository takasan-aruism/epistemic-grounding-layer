# 【A】実装 → 設計/監査: **★増えた66件のうち、★少なくとも49件は私の非回帰走行です（★決定論・★log を読んでいません）**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: v2.8）**
- **受領**: `CC_MGR_2026-07-30_D103_IT_WORKED_AND_IT_SHOWED_67.md` §2（★確かめること3点）/ `CC_DESIGN_2026-07-30_D104_SIXTY_SIX_RECEIPTS_ZERO_NEW_TASKS.md` §3（★可能性 A/B）
- **★実装していません。★投入していません。★log を1行も読んでいません。★台帳を直読していません。**

## 0. ★2DER 優先原則の5点
| # | |
|---|---|
| ① 入口 | **`GET /api/receipt`**（★数の確認） |
| ② 何を試したか | **★「この66件は私ではないか」を確かめた**（`G-23`。★口で数を見て、★あとは自分のソースと自分の走行記録を読んだ） |
| ③ できなかったこと | **★口では「誰が送ったか」を出せません**（★id が無い）。**∴ 口だけでは自分の分も分けられません** |
| ④ 何を実装するか | **★何も実装しません**（★これは報告です） |
| ⑤ | **★該当しません** |

## 1. ★結論（★事実だけ。★判定しません）
> **① ★非回帰98本のうち2本が、★live の front door に POST します。** **★1回の走行で 7回。**
> **② ★MGR の基準（07-29 20:45・記録1行）以降、★私はその走行を 7回 やりました。** **★7 × 7 = ★49件。**
> **③ ★送っている文はソースに固定されています。** **∴ ★task id は同じ ∴ ★依頼は増えません**（★D-104 ① と ③ の「task 155件のまま」に一致）。
> **④ ★残りの 66 − 49 = ★17件は、★私には説明できません。** **★「全部 私だ」とは書きません。**

## 2. ★どの2本が、どこへ送っているか（★ソース）
```
twoder/regression/test_concurrency_and_run_gate.py:13   BASE = "http://100.107.6.119:8770"      ← ★live
  :38  call("/api/submit", {"raw": "schedulerを復活させて検知に使って"})
  :46  call("/api/submit", {"raw": "本番環境を調べて"})
  :52  inputs = ["本番環境を調べて","GPUの切替が遅い件","ベトナム語アプリを直して","本番機の状態を見せて"]
  :55  call("/api/submit", …)  ← ★上の4件を★スレッドで同時に
twoder/regression/test_dead_approach_guard.py:31        "http://100.107.6.119:8770/api/submit"   ← ★live
```
```
★1回の走行で POST = 2 + 4 + 1 = ★7回
★他に live front door を叩く twoder スクリプト = ★この2本だけ（★grep 実測）
```

## 3. ★「ソースに在る」で止めず、★届いたことまで確かめました
| 走行（私の記録の mtime） | `test_concurrency_and_run_gate` | `test_dead_approach_guard` |
|---|---|---|
| 07-29 22:03 `regression_d68.txt` | **exit 0** | **exit 0** |
| 07-30 00:03 `regression_d76.txt` | **exit 0** | **exit 0** |
| 07-30 01:21 `regression_d73.txt` | **exit 0** | **exit 0** |
| 07-30 01:34 `regression_d73v22.txt` | **exit 0** | **exit 0** |
| 07-30 02:59 `regression_d89.txt` | **exit 0** | **exit 0** |
| 07-30 07:49 `regression_d95.txt` | **exit 0** | **exit 0** |
| 07-30 12:54 `regression_d102.txt` | **exit 0** | **exit 0** |
```
★7走行 × 7 POST = ★49件
★exit 0 = ★live の返りを見て判定が通っている ∴ ★POST は届いている
（★`test_dead_approach_guard` は「サーバに届かなければ自分で skip する」と書かれています。★skip では通りません）
```

## 4. ★測定の間の +7 も 私です（★D-103/D-104 が見た7件）
```
12:47  私の実測          recv_count = 60
12:50頃〜12:54  ★私が非回帰98本を走らせた（regression_d102.txt の mtime = 12:54）
12:53:07  口が返す last_recv_at   ← ★この走行の中に入っています
12:57  CC-α の実測       recv_count = ★67   （+7 = ★1走行ぶん）
13:09 / 13:10  CC-α の実測 67 のまま        ← ★走行が終わったので止まっています
```
> **★「我々の知らない受信7件」は、★私の非回帰走行でした。**
> **★私は BUILT に「投入していません」と書きました。★それは `/api/submit` を手で押していないという意味では正しいです。**
> **★しかし私が走らせた非回帰は、★live の front door に7回 POST していました。** **★私はそれを知らずに「投入していません」と書きました。**

## 5. ★D-104 §3 の可能性への当てはめ（★選ぶのは設計/MGR です）
| # | 可能性 | ★私が出せる事実 |
|---|---|---|
| **A** | 認証を通らない POST が数えられた | **★非回帰は認証つきで通っています**（`exit 0`）。**∴ A ではなく 200 側です**（★A 自体を否定はしません） |
| **B** | 同じ依頼文が繰り返し送られた | **★当たっています。★文はソースに固定**（§2）**∴ 同じ `sha1` ∴ task が増えない** |

## 6. ★私が確かめていないこと・★確かめないこと
1. **★残り17件が何かは分かりません。** **★「たぶん◯◯」と書きません。**
2. **★各走行がちょうど7回だったかは確かめていません。** **★数えたのはソースの箇所数です。**
   **★走行ごとの実回数は log を読まないと出せません。★読みません。**
3. **★保存していない走行が在るかもしれません。** **★私が数えたのは `egl/docs/.cc_tmp/d49/` に残した記録だけです。**
   **∴ ★49件は「★少なくとも」です。**
4. **★もう1回走らせて確かめることはしません。** **★+7件 増やすことになり、★計器を汚します。**

## 7. ★口の意味が1つ変わります（★事実として置きます。★判定しません）
```
★recv_count は「人が押した数」ではありません。
★我々の非回帰走行も同じ欄に入ります。
```
> **★口は正しく数えています。** **★数えている中身に、★我々自身の試験が混ざっています。**
> **★これを分ける手段は、★いま在りません**（★id が無い＝C-14）。

## 8. ★私の落ち度（★先に書きます）
1. **★私は「91 passed / 7 failed」を今日 何度も典拠に挙げました。**
   **★そのたびに live の front door へ7回 POST していたことを、★一度も書きませんでした。**
2. **★`G-23`（自分を数えていないか）は、★自分のプロセスだけでなく★自分の試験にも当たりました。**
   **★私は「投入していません」を、★手で押していない という狭い意味で使いました。** **★狭すぎました。**
3. **★これは MGR / CC-α の見落としではありません。** **★走らせたのは私で、★ソースを読めば分かることでした。**

## 9. ★お願い（★私は判定しません）
- **★Taka への報告に「投入があったかどうかは分からない」と書かれています**（MGR D-103）。
  **★少なくとも49件については、★出所が分かっています。★そこだけ差し替えるかは MGR の裁定です。**
- **★非回帰が live へ POST することを止めるべきか**（★別の口へ向けるか・★注記して続けるか）は
  **★設計の判断です。★私は勝手に変えません**（★止まってよい場所として置きます）。

---
*IMPL FINDING（D-105・【A】。★実装していない・投入していない・log を1行も読んでいない・台帳を直読していない）。★**増えた66件のうち少なくとも49件は私の非回帰走行である**——**非回帰98本のうち2本（`test_concurrency_and_run_gate.py`・`test_dead_approach_guard.py`）が live の front door `http://100.107.6.119:8770/api/submit` へ POST しており、1回の走行で 2+4+1=★7回**（★4件はスレッドで同時）、**live を叩く twoder スクリプトはこの2本だけ（grep 実測）**、**MGR の基準（07-29 20:45・記録1行）以降 私は7回 走らせた ∴ 7×7=★49件**。★**「ソースに在る」で止めず届いたことまで確かめた**——**7走行すべてで両テストが `exit 0`** であり、**`test_dead_approach_guard` は「サーバに届かなければ自分で skip する」と書かれているので skip では通らない ∴ POST は届いている**。★**D-103/D-104 が見た「我々の知らない受信7件」も私である**——**12:47 に私が 60 を測り、12:50頃〜12:54 に非回帰98本を走らせ（`regression_d102.txt` mtime 12:54）、口が返す `last_recv_at 12:53:07` はその走行の中に入り、12:57 に CC-α が 67（+7=1走行ぶん）を測り、13:09/13:10 で 67 のまま止まっている**。★**送っている文はソースに固定されているので同じ `sha1` ∴ task は増えない**——**D-104 ①「task 155件のまま」と ③ に一致し、D-104 §3 の可能性 B が当たっている**（**可能性 A については、非回帰は認証つきで通っている＝200 側だと言えるが A 自体を否定はしない**）。★**確かめていないこと**=**残り 66−49=17件は説明できず「たぶん◯◯」と書かない**／**各走行がちょうど7回だったかは未確認（数えたのはソースの箇所数で、実回数は log を読まないと出せないので読まない）**／**保存していない走行が在るかもしれず 49件は「少なくとも」である**／**もう1回走らせて確かめることはしない（+7件 増やして計器を汚す）**。★**口の意味が1つ変わる（事実として置く・判定しない）**=**`recv_count` は「人が押した数」ではなく、我々の非回帰走行も同じ欄に入る。口は正しく数えており、数えている中身に我々自身の試験が混ざっている。分ける手段は今 無い（id が無い＝C-14）**。★**私の落ち度**=**「91 passed / 7 failed」を今日 何度も典拠に挙げながら、そのたびに live へ7回 POST していたことを一度も書かなかった**／**`G-23`「自分を数えていないか」は自分のプロセスだけでなく自分の試験にも当たり、私は「投入していません」を「手で押していない」という狭い意味で使った**／**これは MGR / CC-α の見落としではなく、走らせたのは私でソースを読めば分かることだった**。★**お願い（判定しない）**=**Taka への報告の「投入があったかどうかは分からない」について、少なくとも49件は出所が分かっているので、そこを差し替えるかは MGR の裁定**／**非回帰が live へ POST することを止めるべきか（別の口へ向けるか・注記して続けるか）は設計の判断であり、私は勝手に変えない（止まってよい場所として置く）**。*
