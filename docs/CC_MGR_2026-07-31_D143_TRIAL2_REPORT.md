# 【報告書】Phase 4 Inspection #001 第2試行 — ID 接続修正と同一 E2E 再検査

- **宛: TAKA** / 写: 設計/監査 / IMPL / 発: MGR / 2026-07-31 / TYPE=STATUS
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-30_PHASE4_INSPECTION_001_TRIAL2_ORDER.md`
- **★第1試行の報告書（`D-139`）は1文字も変更していない。**

---

## ★比較表（★正典 §8・説明より先に）

| 工程 | 第1試行（確定値） | 第2試行 | 変化 | 主な証拠ID |
|---|---|---|---|---|
| **1 受付** | **FAILED** | **★PROVEN** | **★前進** | `SUBMIT-_ayfew` → 入力内容・受理時刻 `23:51:06.865333`（receipt と 198µs 差） |
| **2 ID 発行** | PROVEN | **PROVEN** | 変化なし | `SUBMIT-_ayfew`（★今回は `resolved=true`） |
| **3 RRI** | PROVEN | **PROVEN** | ★記録からも引けるようになった | `OBSERVE_CURRENT_STATE` / `ETR-d19f46b8ddd3`(21 event) |
| **4 計画** | NOT_REACHED | **★WIRED_UNPROVEN** | **★ここが新しい停止点** | `dw_task_ref=None` |
| **5 実行命令生成** | NOT_REACHED | **NOT_REACHED** | 変化なし | — |
| **6 実環境での取得** | OBSERVED_UNATTRIBUTED | **OBSERVED_UNATTRIBUTED** | 変化なし | `ARUN-00958`〜`00961` |
| **7 出力取得** | FAILED | **FAILED** | 変化なし | `OBS-00959`〜`00962` |
| **8 検証** | NOT_REACHED | **NOT_REACHED** | 変化なし | — |
| **9 記録** | FAILED | **FAILED** | **★不成立のまま（★ただし到達は改善）** | `blob://sha256:f250fd70…` が `resolved=false` |
| **10 DW 要約** | NOT_REACHED | **NOT_REACHED** | 変化なし | — |
| **11 ユーザ返答** | FAILED | **FAILED** | 変化なし | `measured_state`（21行） |

**★判定区分は正典の6つだけ。★新しい判定語を追加していない。**

---

## 1. 修正結果 → **★成功**

```
変更ファイル : twoder/ids.py ★1本のみ
変更行       : ★59行 追加のみ（削除0）
採った方式   : (b) trace_key から既存の正典 record ID 群へ★決定論的に解決する
選んだ理由   : (a) は trace_key を台帳へ登録する＝★台帳に手を出すことになる
★作っていない: 新しい台帳／新しい resolve API／今回専用の GPU 分岐／
               DW task 生成方式の変更／Planner の新設・強制起動／要約処理
fail-closed  : `return None` ★9箇所。★raw_input を Event Trace の ENTRY と★完全一致で照合
後方互換     : ★基準値8件を★本番の口で取り直し、★期待値と1件ずつ一致
```
> **★正典 §7「第2試行の成功条件」（逐語）**: 「新規試行の trace_key を既存 /api/resolve で解決でき、**入力から実行・観測・応答までを同一 trace から辿れる**」
> **★4点とも満たした ∴ 修正は成功である。**

---

## 2. trace 解決結果

```
trace_key      : SUBMIT-_ayfew   → /api/resolve  resolved=true
入力参照       : UTT-1012（★入力内容は依頼文と完全一致）
受理時刻       : 2026-07-30T23:51:06.865333（★receipt と 198µs 差）
RRI 参照       : OBSERVE_CURRENT_STATE / RUNTIME_INSPECTION ／ ETR-d19f46b8ddd3（21 event）
ARUN 参照      : ARUN-00958 / 00959 / 00960 / 00961
OBS 参照       : OBS-00959 / 00960 / 00961 / 00962
response 参照  : runs/SUBMIT-_ayfew.trace.json
★解決不能な参照:
   ★生出力（blob://sha256:f250fd70…）→ resolved=false（★ids.py に blob 分岐0行・GET の口8つに無し）
   RRI の record ID（RREQ-/RINT-）→ TRACE に入っていない（★今回の対象外）
   DW task → 無い（★この経路は task を作らない）
```

---

## 3. E2E 結果

```
★一周            : ★不成立
★最後に成立した工程: ★3（RRI）
★最初に成立しなかった工程: ★4（計画）  ← ★第1試行は「1（受付）」
★第1試行から何工程 前進したか: ★1工程（工程1 が FAILED → PROVEN）
```

### 3-1. ★数字を混ぜないための注記（★私が母数を直した）
```
★「前進 1工程」と「最初の停止点が 1 → 4 へ動いた」は★別のことである。両方 書く。
★到達の比較は、★同じ母数（正典 §7 記録の8項目）で書く:
   起きた項目は5つ（入力／RRI／実行／生出力／最終結果）。★計画・検証・DW入力は起きていない
   第1試行: ★5項目中 1 到達（実行のみ）
   第2試行: ★5項目中 4 到達（★生出力だけ×）
★設計は「1/8 → 5/6」と書いたが、★母数が違う2つを並べている ∴ ★私が上の形に直した。
```

---

## 4. 2DER が担当した工程（★証拠で主体を示せるものだけ）

| 工程 | 証拠 |
|---|---|
| **3 RRI** | `request_type` / `resolved_intent`（分類の根拠文つき）＋ 同区間の `:8005` 呼び出し4件 |
| **6 取得の実行** | `ARUN-00958`〜`00961`（`started_at 14:51:11.592907Z`＝**★投入の内側**。★保存済みではない） |
| **9 記録の書き込み** | `ds_events.jsonl` / `rri_records.jsonl` / `egl data/events.jsonl` が更新 |

**★今回の修正（`ids.py`）は IMPL が書いた ∴ ★2DER の担当に数えない。★主体移管は 1/8 のまま動かない。**

---

## 5. 人間・Claude の操作（★区分して全件）

| 区分 | 操作 | 時刻 |
|---|---|---|
| **実装** | `twoder/ids.py` に `SUBMIT-` 解決を追加（59行 追加のみ） | 23:36 |
| **運用操作** | **★webui 再起動**（旧 3814663 → 新 3923096） | 23:49:07 |
| **★誤操作（自己申告）** | **★確認せずに PID 3814626 を kill した。**★実サーバは無傷、★1回目の再起動は空振り | 23:48 |
| **監査** | 設計が後方互換8件・必須6点・混入なしを★独立に取り直し（全一致）／★生出力の到達可否を確認 | 23:29・00:1x |
| **テスト** | T1〜T8（★本番の口で。★走らせた test の名前と結果を記載） | 23:49〜23:52 |
| **開始操作** | **`POST /api/submit` ★1回** | 23:51:06 |

**★していないこと**: `run_next` を押していない（`task_id` が無い）／**★GPU を自分で測っていない**／コマンドを選んでいない／出力を整形・要約していない／再投入していない／**★第1試行の報告書を変更していない**。

**★再起動の記録**（正典 §実施条件）: 操作者=IMPL ／ 理由=`ids.py` を本番の口へ反映（**★再起動前は `resolved=false` だった＝「ソースに在る≠動く」の実測**）／**既存運用** ／ **★2DER の担当工程として数えない** ／ **★run-gate は初期化された**（★本件は `runnable=false` のため結果に影響なし）。

---

## 6. ★次の停止原因（★1件に固定）

> ### **★工程4「計画」へ到達しない。区分は「配線はあるが呼ばれない」。**

```
★根拠: RUNTIME_INSPECTION 経路は DW task を作らない（dw_task_ref=None）。
       ★PLAN／GENERATE／AUDIT／DW要約 は DW task の工程であり、task が無い以上 起動しない。
★「配線が無い」とは書かない（★PLAN の配線は D-133 で実証済）。
★区分の訂正: IMPL は NOT_REACHED と記入したが、★前工程(1・2・3)はすべて PROVEN ＝ 止まっていない。
             ★かつ正典 §終了条件B の停止点区分に NOT_REACHED は入っていない ∴ WIRED_UNPROVEN が正しい。
```

### 6-1. ★あなたに1つだけ渡す事実（★提案ではない）
> **★工程4 に到達しないのは「壊れている」からではない。★`submit.py:377` が★意図して DW task を作らない。**
> **∴ ★これは「直す欠陥」なのか「そういう経路」なのかが、★まだ決まっていない。**
> **★経路の設計そのものの話になるので、★私は裁定しない。★あなたの判断である。**

---

## 7. ★次の最小修正候補（★1件だけ。★実施していない）

> ### **★生出力（`blob://…`）に、既存の口から到達できるようにする。**

```
★理由: ★工程9 が不成立である直接原因である（★起きたのに引けない）。
★形は今回と同じ: ★既存の resolve に、★既に記録が持っている参照を1つ通すだけ。
       ★新しい台帳・新しい API を作らない。
★正直に併記: ★これは「次の停止点(工程4)」とは★場所が違う。★停止点を直す修正ではない。
```

**★正典 §8 が名指しした候補（`utilization.gpu` の追加）は、★今回は挙げない。**
```
★理由: 正典の条件は「★第2試行でも GPU 使用率が取得不能で、★それ以前の工程が成立している場合」。
★工程4 は未成立、工程6 は OBSERVED_UNATTRIBUTED ∴ ★「それ以前の工程が成立している」を満たさない。
★あなたの条件どおりに、★今回は出さない。
```

---

## 8. ★検査前後の状態

| | 検査前 23:24 | 検査後 |
|---|---|---|
| 5 repo | 全て未commit 0 | ★ds / rri / egl(data) が更新（2DER が書いた）→ **★commit + push 済** |
| **dev-workcell** | 未commit 0 | **★無変化**（★DW は今回も動いていない） |
| 稼働プロセス | webui 3814663 | **★3923096（★再起動した。全件 記録済）** |
| 資料 | gap 84 | **gap 85**（★`G-85` を登記） |

**★`G-85`**: **状況表が表示していた webui の PID は、実サーバではなく★親シェルだった**（設計が実体を確認）。**★我々の計器であって 2DER ではない。★直していない。**

---
**決めたこと**: **①ID 接続修正は成功（正典 §7 の成功条件4点を満たす） ②一周は不成立、前進は1工程、最初の停止点が 工程1 → 工程4 へ移動 ③次の停止原因は工程4「配線はあるが呼ばれない」1件、次の最小修正候補は「生出力に到達できるようにする」1件（`utilization.gpu` は正典の条件を満たさないので出さない）。**
