# 設計/監査 → MGR（写: Taka / IMPL）: **D-91 —【B】残件7件の現況（★測った）。★裁定済みで未実行の作業が2件 見つかった／★⑦は資料と D-60 が食い違う（私の登記が誤り）**

- `BUILD_ROLE: 参照`（**測定と報告のみ。★実装していない・投入していない・台帳を直読していない・★1件も直していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-30_D90_A_COMPLETE_AND_THE_LEDGER_ITSELF_WAS_INCOMPLETE.md` §4

## 0. ★2DER 優先原則の5点（D-90 §0）
| ① 入口 | **`GET /api/tasks` / `GET /api/state` / `GET /api/resolve`**（★項目②④⑤の実測に使った） |
|---|---|
| ② 2DER で何を試したか | **1件の依頼を front door だけでどこまで読めるか／`etrace_run_id` を持つ task が何件か／その run を引けるか** |
| ③ **2DER では何ができなかったか** | **★作成時刻が取れない／状態遷移が取れない／完了記録が取れない（★完了した task が1件も無い）** |
| ④ 何を実装するか | **★何も実装しない**（本文書は測定のみ） |
| ⑤ | **★該当しない**（実装していない） |

## 0-1. ★結論
> **① 総数7・確認7・打ち切り無し。** **★済0 / 部分3 / 未3 / 食い違い1。**
> **② ★裁定済みなのに実行されていない作業が2件 在る**（`G-50` の述語・`G-46` の3区分）。**★どちらも私宛である。**
> **③ ★⑦は、私が登記した status（`DEFERRED`）と D-60 の判定（済）が★食い違う。★私が D-60 を読まずに登記した。**

---

## 1. ★7件（★1件ずつ・根拠つき）

### ① 13番目の入口
| | |
|---|---|
| **Taka 逐語** | 「13番目の入口」 |
| **Gap** | `G-50` / `OPEN` |
| **判定** | **★未** |
| **根拠** | `G-50` evidence（`EXEC` 打ち切り無し: `create_task(` を全5repo で走査 → 総10件。`dev-workcell/dw/workflow.py:37` が12入口に漏れていた） |
| **★裁定は出ている** | **D-60 裁定①**: 「入口」を決定論の述語として1つ書き、**その述語で全5repo を1回走査して総数を書く**。同じ述語で再走査しない。**据え置き (a) を採る** |
| **★未実行** | **★述語を書いた文書は無い**（`egl/docs` を「述語」で走査 → `G-50` に関する記述は D-60 と D-62 の言及のみ。★実行文書0件） |

### ② Web UI からの `ETRACE_RUN_ID` 読み出し
| | |
|---|---|
| **Taka 逐語** | 「Web UIからの ETRACE_RUN_ID 読み出し」 |
| **Gap** | `G-46` / `PARTIALLY_CLOSED` |
| **判定** | **★部分** |
| **★済んでいる分（★私の実測・今日）** | **`etrace_run_id` は front door から取れる — ★155件中3件**。**その run を `GET /api/resolve?id=ETR-…` で引ける（`resolved:true`・event 5件が読めた。例 `TASK-2DER-C032596E` → `ETR-e2f048d955fd`）** |
| **★残っている分** | **★152件は空**（★理由は測っていない。★推測しない）／`G-46` 本体＝**置いたのに読めない35件**は未処理 |
| **★裁定は出ている** | **D-60 裁定②**: **(b) 別経路が無いものだけ** を採る。**ただし選ぶ前に35件それぞれに「別経路が在るか」を数え切る**。件数を先に出して MGR に返す |
| **★未実行** | **★35件の3区分は行われていない**——2026-07-28 の走査文書が自ら「**別経路の有無を35件すべてについて調べていない。『私が知らない』であって『無い』ではない**」と書いており、**その後 数え切った文書は無い** |

### ③ DW枝の到達性確認
| | |
|---|---|
| **Taka 逐語** | 「DW枝の到達性確認」 |
| **Gap** | **`G-75`**（★本日 私が登記。それまで資料に無かった）/ `OPEN` |
| **判定** | **★未** |
| **根拠** | **★私の実測は無い。** 根拠は `CC_MGR_2026-07-29_D60` の「③DW 枝の到達性確認＝**未着手**」——**★これは MGR の読みであって、私が測ったものではない** |
| **★正直に書く** | **★「未」の根拠は伝聞である**（D-90 §5-3 に従い、そう書く） |

### ④ DW fail-open 時の欠損可視化
| | |
|---|---|
| **Taka 逐語** | 「DW fail-open時の欠損可視化」 |
| **Gap** | `G-38` `OPEN` ／ `G-69` `OPEN` ／ `G-70` `PARTIALLY_CLOSED` |
| **判定** | **★部分** |
| **★済んでいる分（★私の実測・配信物）** | **画面に「取りこぼし」の表示が在る（★10箇所）／`dropped_before` を★同じ返りから数えている（新しい取得を足していない）／「この画面では区別できません」の併記が在る** |
| **★残っている分** | **(a) `G-38` の fail-open 自体**——**★これは欠陥ではなく裁定により正である**（誤送信で撤回された「全合流点 fail-closed」の件）。**★事実の登記として残る** ／ **(b) `G-69`「欠損・失敗の記録なし」が2つの場合を1文言でまとめている——★未修正** ／ **(c) ★実例が0件**（取りこぼしが実際に出たところを誰も見ていない） |

### ⑤ 帳簿管理UIから一件の依頼を最後まで読める状態
| | |
|---|---|
| **Taka 逐語** | 「帳簿管理UIから一件の依頼を最後まで読める状態」 |
| **Gap** | `G-46` `PARTIALLY_CLOSED` ／ `G-69` `OPEN` ／ `G-73` `OPEN` |
| **判定** | **★部分** |
| **★済んでいる分（★私の実測・1件を front door だけで）** | `TASK-2DER-C032596E` について **依頼全文／現在の状態／直前に終わった操作／次の操作／誰がやるか／EGL 出典／取得方法／`run_id`／境界の失敗／★通過した処理5件** が**すべて取れた** |
| **★残っている分** | **★成果物・完了記録が取れない**——**★完了した task が1件も無い**（この件は `CREATED`）∴ **「最後まで」の「最後」を、まだ誰も見ていない** ／ **★作成時刻が取れない**（欄が無い）／ **★短い名前が無い**（`G-73`） |
| **★言い過ぎない** | **★「1件は読める」とは書ける。「最後まで読める」とは書けない。** |

### ⑥ `acquisition` 固有イベントの必要性判断
| | |
|---|---|
| **Taka 逐語** | 「acquisition固有イベントの必要性判断」 |
| **Gap** | `G-49` / `DEFERRED_TO_PHASE2` |
| **判定** | **★未（判断していない）** |
| **根拠** | `G-49` note「**Phase 2 で★必要性を判断すること**の一覧」／**★`G-49` に触れた文書5件を走査したが、「必要」「不要」を判断した文書は★0件** |

### ⑦ 独立検算の正式な定義
| | |
|---|---|
| **Taka 逐語** | 「独立検算の正式な定義」 |
| **Gap** | **`G-76`**（★本日 私が登記）/ **私が付けた status `DEFERRED`** |
| **判定** | **★食い違いが在る。私は決めない**（§2） |

---

## 2. ★⑦の食い違い（★私の登記が誤っている可能性）
```
私が今日 登記した G-76: status=DEFERRED
  根拠: CC_MGR_2026-07-28_D45 §1-3「独立検算の正式な定義を作らない（中心ではない）」

しかし CC_MGR_2026-07-29_D60: 「⑦独立検算＝★済（D-59）」
  ★D-59 は D-45 より★後である。
```
| | |
|---|---|
| **★私の落ち度** | **★D-60 を読まずに登記した。** **★7件の現況は D-60 に既に書かれていた** |
| **★どちらが正か** | **★私は決めない。** 「作らない」と決めた翌日に定義を書いた ∴ **★裁定が上書きされたのか、別のものを指すのかは MGR の領分** |
| **★事実として在るもの** | **D-59 の定義（「検算とは『同じ答えが出るか』ではなく『誤り方が違う経路を通ったか』」）は★実際に効いた**——D-76 で私が git HEAD から独立に基準を作り差分0を示した |
| **★書かないこと** | **★「実質できている」と書かない。** **★`G-76` の status を私の判断で変えない** |

## 3. ★総数と確認件数
```
★総数 7 / ★確認 7 / ★打ち切り無し
  済   0件
  部分 3件（② ④ ⑤）
  未   3件（① ③ ⑥）
  食い違い 1件（⑦）
★D-60（2026-07-29）の判定は「済2 / 部分2 / 未3」だった。
★私の今日の判定と違う。★違いは ⑦（済→食い違い）と ⑤（部分のまま）と ②（部分のまま）。
★★私は D-60 を否定していない。★D-60 の ⑦「済」の根拠を、私が確かめられていない。
```

## 4. ★C に入れる（★登記だけ。★直さない）
| # | 見つけたもの | |
|---|---|---|
| **1** | **★裁定済みで未実行の作業が2件**（`G-50` の述語を書く／`G-46` の35件を3区分に分ける） | **★1日 経っている。★どちらも私宛である** |
| **2** | **★私が D-60 を読まずに `G-76` を登記した** | **★資料に書いてあるものを読まずに、資料へ書いた** |

> **★2 は今日いちばん悪い形である。** **★MGR が D-60 §0 で「裁定待ち2件を資料に書かれたまま抱えていた（読みに行かなかった）」と自己申告したのと、★同じ形を私がやった。**
> **★D-83 に従い、今は直さない。★登記して進む。**

## 5. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **③の中身**（DW の各枝が到達するか） | **★測っていない。★根拠は伝聞** |
| 2 | **②の152件がなぜ空か** | **★測っていない。★推測しない** |
| 3 | **④の取りこぼしが実際に出るところ** | **★実例0件。★作って試していない** |
| 4 | **⑤の「最後」** | **★完了した task が1件も無いので、★原理的に見ていない** |
| 5 | **他に裁定済み未実行が無いか** | **★2件しか見ていない。★全裁定を照合していない** |

> **★5が重い。** **★「7件のうち2件が未実行」を見つけたが、★7件の外は見ていない。**
> **★D-90 §7-3 で書いた「登記漏れが7件の中だけか確かめていない」と、★同じ形である。**

---
*CC-α D-91（測定と報告のみ・1件も直していない）。★**5点**=入口は `GET /api/tasks`/`/api/state`/`/api/resolve`、試したのは「1件を front door だけでどこまで読めるか」「`etrace_run_id` を持つ task が何件か」「その run を引けるか」、**できなかったのは作成時刻・状態遷移・完了記録（完了した task が1件も無い）**、**何も実装しない**。★**結論**=①**総数7・確認7・打ち切り無しで 済0/部分3/未3/食い違い1** ②**裁定済みなのに実行されていない作業が2件（`G-50` の述語・`G-46` の3区分）でどちらも CC-α 宛** ③**⑦は CC-α が登記した `DEFERRED` と D-60 の「済」が食い違い、CC-α が D-60 を読まずに登記した**。★**①13番目の入口=`G-50` OPEN・未**（根拠は `create_task(` の全5repo 走査 総10件で `dw/workflow.py:37` が漏れていたこと）——**D-60 裁定①「入口を決定論の述語として1つ書き、その述語で1回走査して総数を書く／据え置き (a) を採る」が出ているが、述語を書いた文書は0件＝未実行**。★**②`ETRACE_RUN_ID` 読み出し=`G-46` PARTIALLY_CLOSED・部分**——**済んでいる分は CC-α の実測で「`etrace_run_id` は front door から取れる（155件中3件）」「その run を `/api/resolve` で引ける（`resolved:true`・event 5件。例 `TASK-2DER-C032596E` → `ETR-e2f048d955fd`）」**、**残る分は152件が空（理由は測っていない・推測しない）と `G-46` 本体の35件**——**D-60 裁定②「(b) 別経路が無いものだけ。ただし選ぶ前に35件を数え切る」が出ているが、2026-07-28 の走査文書が自ら「別経路の有無を35件すべてについて調べていない」と書いており、その後 数え切った文書は無い＝未実行**。★**③DW枝の到達性確認=`G-75`（本日 登記）OPEN・未**——**CC-α の実測は無く、根拠は D-60 の「未着手」という MGR の読み＝伝聞である**（D-90 §5-3 に従いそう書く）。★**④fail-open の欠損可視化=`G-38`/`G-69`/`G-70`・部分**——**済んでいる分は配信物の実測で「取りこぼしの表示が10箇所」「`dropped_before` を同じ返りから数えている」「『この画面では区別できません』の併記」**、**残る分は (a)`G-38` の fail-open 自体（★欠陥ではなく裁定により正であり事実の登記として残る）(b)`G-69` の1文言2場合が未修正 (c) 実例が0件**。★**⑤一件を最後まで読める=`G-46`/`G-69`/`G-73`・部分**——**1件について依頼全文・現在の状態・直前の操作・次の操作・誰がやるか・EGL 出典・取得方法・`run_id`・境界の失敗・通過した処理5件 がすべて取れた**が、**成果物と完了記録が取れず（完了した task が1件も無い）「最後まで」の「最後」をまだ誰も見ていない**ほか作成時刻と短い名前が無い ∴ **「1件は読める」とは書けるが「最後まで読める」とは書けない**。★**⑥`acquisition` 固有イベント=`G-49` DEFERRED_TO_PHASE2・未**（`G-49` に触れた文書5件を走査したが必要・不要を判断した文書は0件）。★**⑦独立検算の正式な定義=`G-76`・食い違い**——**CC-α は D-45 §1-3「作らない」を根拠に `DEFERRED` で登記したが、D-60 は「済（D-59）」と判定しており D-59 は D-45 より後である** ∴ **CC-α の落ち度は D-60 を読まずに登記したこと（7件の現況は D-60 に既に書かれていた）**、**どちらが正かは決めず**（裁定が上書きされたのか別物を指すのかは MGR の領分）、**D-59 の定義が実際に効いた事実は在る**（D-76 で CC-α が git HEAD から独立に基準を作り差分0を示した）が**「実質できている」と書かず `G-76` の status を独断で変えない**。★**総数7/確認7/打ち切り無し・済0/部分3/未3/食い違い1**——**D-60 の「済2/部分2/未3」と違うが D-60 を否定しているのではなく、D-60 の⑦「済」の根拠を CC-α が確かめられていない**。★**【C】に登記だけ（直さない）**=**裁定済みで未実行の作業が2件（1日 経過・どちらも CC-α 宛）**／**CC-α が D-60 を読まずに `G-76` を登記した＝資料に書いてあるものを読まずに資料へ書いた**——**これは MGR が D-60 §0 で自己申告した「裁定待ちを資料に書かれたまま抱えていた（読みに行かなかった）」と同じ形である**。★確かめていないこと=③の中身は測っておらず根拠は伝聞／②の152件がなぜ空かは測っていない／④の実例は0件で作って試していない／⑤の「最後」は完了 task が無いので原理的に見ていない／**他に裁定済み未実行が無いかは2件しか見ておらず全裁定を照合していない（これが最も重く、D-90 §7-3 の「登記漏れが7件の中だけか確かめていない」と同じ形）**。*
