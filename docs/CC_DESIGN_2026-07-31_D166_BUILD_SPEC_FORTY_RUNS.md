# 【BUILD SPEC】A / A' / B / C を**各10本（計40本）** — ★判定の切り出し方まで設計が固定する

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 10:4x / TYPE=BUILD_SPEC
- **運用方針 確認済（版: v2.8）** ／ **裁定**: `D-165` §4（計画）／`D-166`（★厳格に読む・0/9 確定）
- **★新しい名前で置いた** ／ **★2DER の外の測定**（★投入しない・台帳に書かない・コードを変えない・**★担当工程に数えない**）

## 0. ★前回からの変更点（★これだけ）
```
★① A' を足す（★4条件）  ★② 各10本（★計40本）  ★③ 判定の切り出し方を★設計が固定する（★§4）
★★他は D-163 SPEC のまま。★問いも判定基準も★1文字も変えない。
★★`:8005` を直接 叩く（★40回）。★GPU を使う ∴ ★使ったと1行 書く。★新しいサーバを立てない
```

---

# 1. ★A' の作り方（★引き算だけ。★足さない）

```
★A' ＝ ★A から「★PLAN を JSON で出せ」の指示だけを外す。★問いは残す。
★★具体的に外すもの: ★出力 JSON のキー仕様（"objective" … "allowed_files" の列挙）と
   ★「Produce an IMPLEMENTATION PLAN … as a SINGLE JSON object inside one ```json code block」の指示
★★残すもの: ★TASK（依頼文の逐語）／★観測ブロック（取得記録＋値）／★provenance trace 行／★§3 の問い
★★1文字も言い換えない。★語順を変えない。★足さない。
```
## 1-1. ★引き算であることの示し方（★A/B/C と同じ）
```
★A' の★字数と sha1 を書く
★★A' の各行が ★A に在ることを機械で確認する（★「A' に在って A に無い行」＝0件）
★★1行でも足されていたら ★止めて設計へ聞く
★★A は残す（★置き換えない）
```
## 1-2. ★正直に併記すること（★`D-165` §4-1）
```
★★A' は Taka の「★実際のplanner入力をそのまま渡して」の範囲を出る。★報告に1行 書く。
★★Taka が「A' は要らない」と言えば ★A/B/C の各10本に戻す。
```

---

# 2. ★実行（★設定は同一。★あなたが選ばない）
```
★model=Qwen3.6-35B-A3B ／ temperature=0 ／ ★seed は送らない（★planner も送っていない）
★max_tokens=8192（★前回と同じ。★変えない＝★`G-88` を「budget で解決」しない）
★endpoint=既存の http://127.0.0.1:8005/v1/chat/completions
★各条件 ★10本（★計40本）。★出力は★全文 保存（★要約しない）
★★`finish_reason` を★1本ずつ記録する（★length で切れた本数を条件別に出す）
```

---

# 3. ★問い（★Taka 逐語。★4条件とも同一。★1文字も変えない）
```
この情報だけを使って、
1. 既に確認できていること
2. 未確認のこと
3. 新規実装が本当に必要なこと
を分けてください。
```

---

# 4. ★判定の切り出し方（★設計が固定する。★40本を目で数えない）

## 4-1. ★①「3区分になっているか」— ★機械で判定してよい
```
★日本語 と 英語の★両方のパターンで探す（★前回 B-2・C-2 は英語だった。★私はこれで誤った）
   日本語: 「既に確認」「未確認」「新規実装」
   英語  : "already (been )?confirmed|already known" / "unconfirmed|not (yet )?confirmed|unknown" / "new implementation|needs? to be (newly )?implemented"
★★3つとも見つからなければ ★その本は「3区分でない」。★見つからない本は★全文を目視して確かめる
```

## 4-2. ★②「①に観測値が入っているか」— ★★条件ごとに照合対象を作る（★ここが要）
```
★★★照合する具体値は「★その条件の prompt に実在するトークン」だけにする。
   ★理由: ★B・C には GPU・コンテナの値が無い ∴ ★A と同じリストで測ると★B・C に不利になる。
   ★★これは母数を条件ごとに揃える作業である（★私が本日 危うく踏みかけた形）。
★作り方（★機械）: ★その条件の prompt から `\d{4,}` と `[A-Za-z][A-Za-z0-9_:.\-]{5,}` を抜き、
   ★一般語（container / memory / used / process / GPU / Docker 等）を除いたものを★照合リストにする
★★判定: ★①の範囲の中に、そのリストのトークンが★1つでも出れば ○
★★★①の範囲外（例: test_plan）に出ても ★× である（★前回 C-2 がこれだった）
★★リストと、★条件ごとの件数を★報告に書く（★述語を書く）
```

## 4-3. ★③「①に無いものだけか」— ★★機械で「候補」を出し、★目視で確定する
```
★機械: ★①に現れた名詞（RSS / PID / GPU / コンテナ / ポート 等）が ★③にも現れたら ★「要確認」に立てる
★★目視: ★要確認の本だけを開き、★「①で『取得できている』と書いたものを、★③の実装対象に入れているか」を見る
★★★これが `D-166` の厳格な線である（★A-3 は ★③の出力に RSS を含んでいたので ×）
★★「新しいものと一緒に出力するだけ」も ★× とする（★A-3 と同じ扱い。★物差しを変えない）
★★40本すべてを目視しない。★要確認に立った本だけ。★立った本数も書く
```

## 4-4. ★総合
```
★★3条件すべて満たした本だけを「分けられた」と数える。★1つでも欠けたら数えない
★★条件ごとに ★成立/10 を出す（★分布）。★平均だけを書かない
★★失敗の型を条件別に書く（★前回: A は③で落ち、B・C は②で落ちた。★「同じように失敗」と書かない）
```

---

# 5. ★やってはいけないこと
```
★コードを変える ★投入する ★run_next / GENERATE ★台帳に書く ★commit する ★新しいサーバを立てる
★max_tokens を上げる（★`G-88` を budget で解決しない）  ★問いを条件ごとに変える
★A' に A に無いものを足す  ★Ledger / (c) の SPEC と patch / 図の修正 に触る
★★「概ね」と書く  ★★「Qwen が使えない」と書く  ★★別の欠陥を見つけたことを理由に止まる
```

# 6. ★止まってよい場所
```
★`:8005` が応答しない（★実行不能の証拠）  ★A' に「A に無い行」が入る（★引き算にならない）
★★それ以外は止まらない。★2通りに読めたら狭い方を採り、★迷いを1行 書く
```

# 7. ★報告
```
★条件別の ★成立/10（★分布）  ★失敗の型を条件別に  ★`finish_reason=length` の本数を条件別に
★A/A'/B/C の★字数・sha1・部分集合の確認  ★②の照合リストと件数（★条件ごと）
★③で「要確認」に立った本数と、★目視の結果  ★設定（model/temperature/seed/max_tokens）
★★40本の全文（★保存先を書く。★BUILT には★条件ごとに代表2本＋★成立した本は全文）
★★A' が Taka の指示の範囲を出ることを1行  ★★担当工程数の差分: ★0
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---
**決めたこと**: **①A' は A から「PLAN を JSON で出せ」の指示だけを引き、問いと観測ブロックは残す（引き算であることを sha1 と行の包含で示す。A は置き換えず残す。Taka の指示の範囲を出ることを報告に1行） ②各10本・計40本、設定は前回と同一で `max_tokens` を上げない（`G-88` を budget で解決しない）、`finish_reason` を1本ずつ記録 ③判定の切り出し方を設計が固定した——①は日英両方のパターンで機械判定、②は★条件ごとの prompt から作った照合リストで測る（A と同じリストで B・C を測らない＝母数を揃える）、③は機械で「要確認」を立てて要確認の本だけ目視（`D-166` の厳格な線をそのまま当てる） ④成立は3条件すべて満たした本だけ、条件ごとに成立/10 の分布を出し、失敗の型を条件別に書く。**
