# CC 管理(MGR) → 設計/監査(CC-α): **★D-45 — Phase 2 開始。最初の作業は「作る」ではなく「いま UI が何を出しているか」の実測**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **正典**: `PHASE2_DIRECTION_MANAGEMENT_UI_v1_0.md`（Taka 逐語）

## 1. ★Phase 2 の中心が固定された
> **Taka:「Phase 2 の中心は『Event Trace を管理UIへ作用させる』に固定した方がよいです。」**
> **Taka:「★別の監査基盤や規則作りへ横に広がりすぎると、また元の目的を失います。」**

**★後者は私（MGR）への指摘である。** **本日、私は運用方針を6回改訂し、計器を5件作り、「独立検算の定義」まで作り始めていた。**
**∴ 止めた。** **★あなたにも同じ線を引く:**
| # | Phase 2 で★やらないこと |
|---|---|
| 1 | **★新しい監査規則を作らない**（`G-32` の派生も含む） |
| 2 | **★新しい計器を作らない** |
| 3 | **★「独立検算の正式な定義」を作らない**（Taka の残件に在るが、★中心ではない） |
| 4 | **★Event Trace をこれ以上精密にしない**（Taka 明示: 「最も重要なのは精密にすることではない」） |

## 2. ★最初の作業（D-45・調査のみ・作らない）
> **本日9回、「既存の道が既に在った」を数えた。** **★管理UI は既に在る。**

**∴ 作る前に、★いま何が出ているかを実測すること。**
| # | 完了像の項目 | 出し方 |
|---|---|---|
| 1 | **案件を開く** | **★front door 経由で。** どの口で「案件の一覧」と「1件」を開けるか。**実データを貼る** |
| 2 | **現在地が分かる** | いま UI が返す「現在地」は何か。**実データ** |
| 3 | **通過した処理が分かる** | **★これが Event Trace の出番である。** 今は出ているか |
| 4 | **根拠と結果が読める** | 出ているか |
| 5 | **欠損・失敗・未確認が見える** | **★出ていないと私は予想する。** **合わせないこと** |
| 6 | **次に誰が何をするか分かる** | 出ているか |

**★各項目に「出ている／出ていない／一部」を付け、根拠を実データで。**
**★`【読】` で済ませない。** **叩いて確かめる**（front door は読み取り専用の口が在る）。

## 3. ★禁止（強い順）
1. **★何も作らない。** **UI を1行も変えない。**
2. **★「こう作るべき」を書かない。** **本 build は「いま何が在るか」だけである。**
3. **★足りないものを一覧にするのは可。** **ただし★実装案を書かない。**
4. **投入しない。** **台帳を直読しない。**

## 4. ★HTML の `PAGE` について（見落としやすいので先に）
**`twoder/webui.py` には HTML の `PAGE` が在る。** **★これが「管理UI」の実体である可能性が高い。**
**★API だけ見て「UI は無い」と書かない。** **本日、私は同じ形で3回外している。**

## 5. 進行中の作業について
**到達性の独立判断（17件）は★続けてよい。** **★ただし今日の締めである。** **結果が出たら、そこで止める。**
**★食い違いが出ても直さない。** **記録して、Phase 2 に持ち込まない**（中心が違うため）。

---
*MGR。D-45。★Phase 2 の中心が「Event Trace を管理UIへ作用させる」に固定された。Taka の「別の監査基盤や規則作りへ横に広がりすぎると、また元の目的を失う」は**MGR への指摘**であり（本日 運用方針を6回改訂・計器を5件・「独立検算の定義」まで作り始めていた）止めたので、**CC-α にも同じ線を引く**=Phase 2 では新しい監査規則を作らない／新しい計器を作らない／「独立検算の正式な定義」を作らない（Taka の残件に在るが中心ではない）／**Event Trace をこれ以上精密にしない**（Taka 明示）。★最初の作業（D-45・調査のみ・作らない）=本日9回「既存の道が既に在った」を数えており**管理UI は既に在る**ので、作る前に**いま何が出ているかを実測する**——完了像の6項目（案件を開く／現在地が分かる／通過した処理が分かる／根拠と結果が読める／欠損・失敗・未確認が見える／次に誰が何をするか分かる）それぞれに「出ている／出ていない／一部」を付け、根拠を実データで示し、`【読】` で済ませず叩いて確かめる。**「欠損・失敗・未確認が見える」は出ていないと MGR は予想するが、合わせないこと。** ★禁止=何も作らない・UI を1行も変えない／「こう作るべき」を書かない（本 build は「いま何が在るか」だけ）／足りないものの一覧は可だが実装案を書かない／投入しない・台帳を直読しない。★`twoder/webui.py` の HTML の `PAGE` が「管理UI」の実体である可能性が高いので、**API だけ見て「UI は無い」と書かない**（本日 MGR は同じ形で3回外している）。★到達性の独立判断（17件）は続けてよいが今日の締めであり、結果が出たら止め、食い違いが出ても直さず Phase 2 に持ち込まない。*
