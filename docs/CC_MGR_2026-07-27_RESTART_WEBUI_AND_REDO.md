# CC 管理(MGR) → 設計/監査(CC-α): **webui を再起動して Build 10 の受入をやり直す（Taka 直接）**（HANDOFF）

- `BUILD_ROLE: 参照`（実装源でない。**詳細は設計/監査が SPEC にする**）
- **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.7）**

## 0. 権限
Taka 直接発話（2026-07-27・逐語）:
> **「webui 再起動して受入やり直して」**

**★これは Taka が見ている実サービス（`:8770`）を止めて起こす操作である。** **許可はこの発話による。** **範囲は「再起動と受入のやり直し」に限る。**

## 1. 手順（この順で・SPEC に落とすこと）
| # | 手順 | 記録すること |
|---|---|---|
| 1 | **再起動前の状態を採る** | **旧 pid / 旧起動時刻 / `webui.py`・`dispatch.py` の mtime** |
| 2 | **停止 → 起動**（既存の起動手順を使う。**新しい起動方法を作らない**） | **停止時刻・起動時刻・新 pid**・使った**コマンドそのもの** |
| 3 | **★新プロセスが修理を持っていることを確認** | **新 pid の起動時刻 > ソース mtime** であること。**加えて応答に `planner_outcome` キーが出ること**（`None` でもよい。**キーの有無が判定**） |
| 4 | **受入のやり直し**: **同じ依頼文**を webui `/api/submit` へ1回 → `/api/run_next` を1回 | 依頼文は**1文字も変えない**。**`run_until_barrier` を使わない** |
| 5 | **`planner_outcome` を逐語全文で貼る** | **`stage`・`reason` を1文字も省略しない。要約しない** |

## 2. ★併せて確かめること（1回の観測で断定しない）
**Build 9C（`CLAUDE_BARRIER`）と Build 10（`dispatched: true`）で、同じ入力に対し結果が違った。**
- **どちらも同じ古いプロセスだったので、コードの違いでは説明できない。**
- **∴ 新プロセスで同じ入力を回した結果が、どちらに一致するか（あるいは第三の結果か）を記録する。**
- **★1回では常態を判定しない。** **判定材料が不足するなら「不足する」と書く。** **繰り返す必要があると判断したら、回数と理由を先に出して MGR に上げること**（勝手に何度も回さない）。

## 3. 守ること
- **run-gate を回避しない。** **assert を書き換えない。** **依頼文を通りやすく変えない。**
- **本番コードを新たに変更しない**（本件は再起動と観測のみ）。
- **トークンを文書・ログ・argv に出さない。**
- **理由が出ても「作れるようになった」と書かない。** **見えるようになっただけである。**

## 4. MGR 側の機械的確認
**状況表に「実行中プロセスの鮮度」を入れた。** **再起動が成功すれば、次のターンで**
`実行中プロセス: ソースより新しい起動 or 該当なし` **に変わる。**
**∴ 再起動の成否は、報告を待たずに私の側でも機械的に見える。** **食い違えば私が指摘する。**

---
*MGR。Taka 直接「webui 再起動して受入やり直して」により、実サービスの再起動を許可（範囲は再起動と受入のやり直しに限る）。手順=再起動前の pid/起動時刻/mtime を採る→既存の起動手順で停止・起動（新しい起動方法を作らない）→★新プロセスが修理を持つことを確認（起動時刻 > ソース mtime、かつ応答に planner_outcome キーが出ること。None でも可、キーの有無が判定）→同じ依頼文で submit 1回・run_next 1回→planner_outcome を逐語全文。併せて 9C と 10 の食い違いが新プロセスでどちらに一致するかを記録し、1回では常態を判定しない（繰り返すなら回数と理由を先に MGR へ）。MGR 側は状況表のプロセス鮮度行で再起動の成否を機械的に確認する。*
