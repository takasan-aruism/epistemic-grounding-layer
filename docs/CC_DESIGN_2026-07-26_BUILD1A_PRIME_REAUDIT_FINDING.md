# 設計/監査 → MGR/Taka: Build 1a' 独立再監査 — 裁定は正しく実装され、束縛先条件が実データで初めて分岐した

- `BUILD_ROLE: 参照`
- 宛: MGR / Taka / 写: IMPL / 発: 設計/監査(CC-α) / 2026-07-26 / TYPE=FINDING
- 対象: `CC_IMPL_2026-07-26_BUILD1A_PRIME_SURFACE_EXPANSION_BUILT.md` + `rri/rri/{preflight_gate.py,ambiguity_patterns.jsonl}` + `egl/structure/s_ambiguity_stage_build1.py`
- 表記規約: **【監査】**/**【設計】**/**【伝聞】**

## 0. 結論
**【監査】裁定どおりに実装され、私が指摘した偽陽性経路は塞がれている。** 1a' の新しい証拠は **M6 の1点のみ**（束縛先条件が実データで初めて分岐した）で、IMPL 自身がそう限定して報告している。**呼称の訂正（2/298 は誤発火ではない）も IMPL が正しい。**

## 1. 独立に検証したこと【監査】
| 項目 | 監査側の実行結果 |
|---|---|
| 既存 HBB-30 非回帰（1a' 後） | **私が実行し 13/13 PASS** ✓ |
| `--check` | **私が実行し GREEN**（パターン2件・抑制5/5・決定論再現・非回帰7項目・fixture） ✓ |
| **★裸の指示語ガード** | **私が `PG.detect()` を直接叩いて確認** ✓ |
| DE 記録 | **DE-0549 / DE-0550 とも front door 経由・`CLAUDE_CODE` 開示済**（私が ledger を実読） ✓ |

### 裸の指示語ガードの直接検証（私の 6/298 誤測定の原因を塞げているか）
| 入力 | 結果 | 判定 |
|---|---|---|
| `この処理を直して` | **非発火** | ✓ **私の偽陽性4件の原因（裸の `この`）が塞がれている** |
| `そのまま進めて` | **非発火** | ✓ 同上（裸の `その`） |
| `その件を進めて` | **発火** | ✓ 名詞束縛形は撃つ |
| `この設計案の得失は？`(BV3) | **非発火** | ✓ 自己解決は撃たない |
| `あれ、どこにあったっけ？`(IP1) | 発火 | ✓ |
| `前の件を優先して進めて`(実データ) | 発火 | ✓ |

**データ側（`surface_forms` に裸の指示語を入れない）とコード側（`_noun_bound_reference()` のガード）の二重防御**になっており、**データ事故で偽陽性が復活しない**。これは私が要求した以上の実装。

## 2. IMPL の呼称訂正を受け入れる【監査】
- 1a で私も IMPL も M3 を「**誤発火率**」と呼んでいたが、**1a' の2件は誤発火ではない。**
- この2件（`前の件を優先して進めて` / `前の件のQwenとCoderの切替タスクをそのまま進めて`）は、**私自身の裁定で「文脈があれば解決でき、無ければ聞き返すべき」と認定したケース**である。**context が空である以上、発火が正しい挙動。**
- **∴ M3 は「発火率」であり、偽陽性と判定できるものは 0件。** 以後この呼称を使う。
- **【設計・未測定】**「実運用でこの2件に聞き返すのが妥当か」は**運用判断であり、誰も測っていない。**

## 3. 1a' の正味の新規性（過大にも過小にもしない）
- **【監査】新しいのは M6 だけ**: 1a では束縛先条件が実データを1件も判定していなかった。1a' で**実発話2件が AND 条件を通過し、束縛先つき context で 2/2 非発火・空 context で発火**＝**機構が実データ上で初めて分岐した。**
- **【監査】ただし context は合成**（IMPL の自己申告どおり）。**「実 context で判別できる」ことはまだ示していない。**
- 捕捉 3/3・BV3 非発火・非回帰は 1a から**維持**（新規ではない）。

## 4. ★IMPL が見つけた計器の性質（重要・私が見落としていた）
> コーパス母数が **478 → 479**、機械生成除外が **19 → 20** に増えた。原因は **IMPL 自身の DE-0549 記録が front door を通ったため**。除外+dedup 後の正典 **298 は不変**。

- ＝ **我々が DE を記録するたび、回帰コーパスの生データが増える。計器が自分の活動を食っている。**
- **【監査】現時点では正典298は安定**しているが、**これは「除外規則が効いている」という条件つきの安定**であり、保証ではない。
- **裁定**: **測定のたびに母数（生データ件数・除外件数・dedup 後件数）を必ず記録・報告すること。** 母数が黙って動く計器は、本日4回起きた事故（重複水増し・部分一致・帰属誤り・degenerate 指標）と同型のリスク源である。**IMPL がこれを自分で見つけて報告したのは good catch。**

## 5. IMPL の提案への裁定
> `preceding_utterance_ref` を使った**実 context** での M6 再測定を、Build 2 の配線前に単独実施できる。

**承認【設計】。ただし条件つき:**
1. **測定のみ。本番配線（`submit.py` が `context` を渡す）は行わない。** Build 2 の scope を先食いしない。
2. **実 context で分岐しなかった場合も報告する**（合成 context で成立したものが実 context で崩れる可能性は十分ある）。
3. **`preceding_utterance_ref` が欠損している発話がどれだけあるかを先に数える**（欠損だらけなら測定自体が成立しない）。

## 6. 残る限界（BUILT と一致・確認済）
- **【監査】`twoder/submit.py` は `context` を渡していない** → 実 front door では常に「束縛先なし」判定。**「動いた」とは言えない。**
- `AMB-REF-001`（あれ/それ/例のやつ）は**実発話で 0 件**のまま。**fixture 専用の機構である状態は変わっていない。**

---
*DESIGN CC-α 独立再監査。裁定は二重防御で実装され、私の偽陽性経路は塞がれた。新規性は M6 のみで IMPL 自身が限定して報告している。呼称訂正を受け入れる。計器が自分の活動を食う性質は今後毎回 母数を記録すること。*
