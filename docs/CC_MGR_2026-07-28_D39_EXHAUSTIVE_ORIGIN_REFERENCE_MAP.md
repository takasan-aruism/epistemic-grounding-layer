# CC 管理(MGR) → 設計/監査(CC-α): **★D-39 — `origin`/`relayed_by`/`authored_by` の参照箇所を網羅で出す。私の答えは根拠が薄かった**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **典拠: Taka（2026-07-28・逐語 §1）**

## 1. Taka の指摘と要求（逐語）
> 「**今回分かったのは、HUMAN_RELAYED化によって変わるのは発話の出所情報と入力検証だけであり、RRI・EGL・DW・台帳処理の動作変更はまだ確認されていない、という理解です。「変わらなかったものも全部列挙する」という問いにはまだ答えられていません。**」
> 「**次は、同じ入力について以下を明示してください。1. HUMAN_RELAYED化によって変わらない関数・分岐・保存項目 2. origin / relayed_by / authored_by を参照している全箇所 3. その3項目がRRIの4軸7カテゴリ、次アクション決定、EGL、DW、勘定科目、Sub-IDに影響するか 4. 影響しない場合は「影響しない」と実測または参照箇所を根拠に明記 5. 確認したファイル、関数、検索語、未確認範囲**」
> 「**Build 22の修理と、RRI全体の実態調査を混同しないでください。**」

## 2. ★私（MGR）の答えの欠陥（先に出す）
**私は「変わらない」を10項目の表で出した。** **根拠は grep 1本である。** **しかもその grep には★除外フィルタが掛かっていた:**
```
私が使った検索: grep -rn '\borigin\b' --include=*.py …
  | grep -v "/test\|_test\|/tests/\|regression/\|/structure/\|/docs/"
  | grep -vE 'origin\s*=\s*"|origin\s*=\s*None|def .*origin|origin: |#'
```
> **★2本目の `grep -vE` は「代入っぽい行」を落としている。** **`origin` を読んでいる行が、その形に一致すれば消えている。**
> **∴ 私の一覧は網羅ではない。** **「変わらない」と断定できる根拠を、私は持っていなかった。**
> **★Taka の指摘が正しい。** **私はこの話題で今夜2回、探索範囲を誤っている**（「読み出し経路は無い」／「`origin` だけなら素通り」）。**3回目を自分で作らない。**

## 3. 依頼（D-39・調査のみ。★投入しない・直さない）
**★Taka の5項目を、そのままの番号で答えてください。**

| # | 要求 | 出し方 |
|---|---|---|
| **1** | **`HUMAN_RELAYED` 化で変わらない関数・分岐・保存項目** | **★関数名で列挙する。** 「RRI は変わらない」ではなく「`request_type.classify` は変わらない」の粒度で |
| **2** | **`origin` / `relayed_by` / `authored_by` を参照している★全箇所** | **★除外フィルタを掛けない生の結果を貼る。** **テスト・`structure/` も含めて出し、区分を付ける**（本番／テスト／研究） |
| **3** | **3項目が 4軸7カテゴリ / 次アクション決定 / EGL / DW / 勘定科目 / Sub-ID に影響するか** | **★項目ごとに ○/× と、その根拠の行** |
| **4** | **影響しない場合の根拠** | **★「参照箇所が無い」か「参照しているが分岐に使っていない」かを区別する。** 後者なら該当行を貼る |
| **5** | **確認したファイル・関数・検索語・★未確認範囲** | **★未確認範囲を必ず書く**（§3-7）。「全部見た」と書かない |

### 3-1. ★検索語について（私の失敗を繰り返さないため）
- **`origin` だけで探さない。** **辞書アクセスの形（`["origin"]` / `.get("origin")` / `rec["origin"]`）も探すこと。**
- **★`utterance` を受け取る関数が、`origin` を素通しで別の場所へ渡していないかを見ること**（間接参照）。
- **同名の別物を区別すること**: `experiment_candidate.py` の `origin`（出所 ID の辞書）／`conformance_probe.py` の `origin`（モジュール名）。**私は区別したが、区別したことを書くこと。**

### 3-2. ★混同しない（Taka 明示）
> **「Build 22 の修理と、RRI 全体の実態調査を混同しないでください。」**

**∴ 本文書は D-39（`origin` 3項目の参照地図）だけである。**
**∴ RRI が実際に何を走らせるかの実態調査（Taka の元の8問）は★別件であり、`UTT-0772` を起点に別文書で行う。**
**∴ 1つの文書に混ぜない。** **混ぜたら私が差し戻す。**

## 4. ★禁止
- **投入しない。直さない。新しい台帳を作らない。台帳に直接手を出さない。**
- **★「影響しない」を、参照箇所を出さずに書かない。**
- **★「全部見た」と書かない。** **見ていない範囲を書く。**

---
*MGR。D-39。★Taka の指摘=`HUMAN_RELAYED` 化で変わるのは発話の出所情報と入力検証だけで、RRI・EGL・DW・台帳処理の動作変更は未確認であり、「変わらなかったものも全部列挙」にはまだ答えていない。★MGR の答えの欠陥を先に出す=私は「変わらない」を10項目の表で出したが根拠は grep 1本で、しかも2本目の `grep -vE` が「代入っぽい行」を落としていた ∴ 私の一覧は網羅ではなく、断定する根拠を持っていなかった。私はこの話題で今夜2回探索範囲を誤っており（「読み出し経路は無い」「`origin` だけなら素通り」）、3回目を自分で作らない。★D-39（調査のみ・投入も修正もしない）=Taka の5項目をそのままの番号で答える。①変わらない関数・分岐・保存項目を**関数名の粒度**で列挙 ②3項目を参照している**全箇所**を除外フィルタ無しの生の結果で貼り、本番／テスト／研究の区分を付ける ③4軸7カテゴリ・次アクション決定・EGL・DW・勘定科目・Sub-ID への影響を項目ごとに ○/× と根拠行で ④影響しない場合は「参照箇所が無い」か「参照しているが分岐に使っていない」かを区別し、後者なら該当行を貼る ⑤確認したファイル・関数・検索語・★未確認範囲を必ず書く。検索語の注意=`origin` だけで探さず辞書アクセスの形も探す／`utterance` を素通しで渡す間接参照を見る／同名の別物（`experiment_candidate` の出所 ID 辞書、`conformance_probe` のモジュール名）を区別し、区別したことを書く。★Taka 明示「Build 22 の修理と RRI 全体の実態調査を混同しないでください」に従い、本件は `origin` 3項目の参照地図だけとし、RRI の実態調査（元の8問）は `UTT-0772` を起点に別文書で行う。混ぜたら MGR が差し戻す。*
