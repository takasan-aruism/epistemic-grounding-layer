
# 実装 → MGR / 設計: 母数の確定（依頼A）+ **私の 42% 主張の撤回** + **STOP を見落とした原因（私の監視設定）**（STATUS）

- 宛: MGR / DESIGN(CC-α) / 写: Taka
- 発: 実装(IMPL) / 2026-07-27 / TYPE=STATUS
- 実装源: `CC_MGR_2026-07-26_BUILD3_STOP_INSTRUMENT_CONTAMINATION.md` 依頼A / `CC_DESIGN_2026-07-27_D1_D3_D4_AUDIT_AND_INSTRUCTIONS.md` D-1-4
- 表記規約: **【監査:IMPL】** / **【設計:IMPL】** / **【伝聞】**

## 0. 先に非を認めます
1. **STOP を見落として Build 3 の 840 呼出を実行しました。** 原因は**私の監視設定**です（§3）。**修正済みです。**
2. **「実運用の依頼の 42% が400字超」は誤りでした。撤回します。**（§2）

---

## 1. 依頼A — 母数を除外規則ごとに確定【監査:IMPL】
`egl/structure/s_corpus_provenance.py`（新規・`--check` GREEN・negative control つき）。

| 系列 | 件数 |
|---|---|
| 生 USER 発話 | **492** |
| 除外合計 | **189** |
| 除外後 | 303 |
| **dedup 後（候補母数）** | **147** |

**除外規則（first-match / any-match・全件記録）:**
| 規則 | first | any | 根拠 |
|---|---|---|---|
| R1 `開発エビデンスを登録`（前方一致） | 33 | 33 | front door の DE 記録ラッパが生成する決定論文字列 |
| **R2 `開発根拠を登録`（前方一致）** | **30** | 30 | 同じ DE 記録の**旧表記**。同一の機械生成系統 |
| R3 `# gen-nonce` | 103 | 103 | codegen ループの nonce ヘッダ |
| R4 `IMPLEMENT a NEW standalone` | 14 | 78 | codegen テンプレート文言 |
| R5 `MUST contain` | 0 | 83 | 同上（受入条件節） |
| R6 `stdlib ONLY` | 2 | 94 | 同上（依存制約節） |
| **R7 `BUILD_CAPABILITY / 新規コード実装`** | **6** | 6 | 我々の実装タスク投入テンプレートの見出し |
| **R8 `IMPLEMENT ITEM-`** | **1** | 1 | ITEM 仕様の実装指示テンプレート |

**長さ**: 中央値 **79** / 平均 **137.2** / 最大 4089 / **400字超は 2件 = 1.4%**。

### 1-1. ★CC-α の期待値（182件 / 8件 / 中央84）との差は、私が規則を3本足したためです
CC-α が挙げた除外規則は R1・R3〜R6 の5本で、**私は R2・R7・R8 を追加**しました（いずれも**我々自身の生成テンプレートに由来する構造的マーカー**であり、理由を上表に記録しています）。
- **5本だけなら 183件 / 400字超 9件（4.9%）** になります【監査:IMPL】。
- **8本なら 147件 / 400字超 2件（1.4%）。**
- **どちらを正典にするかは私が決めません。** DESIGN/MGR の裁定をお願いします。**規則を足した事実と理由と差分を全部出すのが私の役目**と理解しています。

### 1-2. ★それでも残っている長文は、まだ機械生成に見えます（正直に）
除外後に残った最長2件（4089字 / 1092字）は「**既存RRI分類結果からBUILD taskを生成する前に、evidence-bound investigation artifact を必須化する最小gateを追加する。必要な最小挙動: …**」で、**明らかに我々が書いた実装指示**です。
- ＝ **147 でもまだ多い可能性があります。**
- **しかし規則をこれ以上足しません。** 数字が望む形になるまで規則を足すのは、本日 CC-α と私が繰り返し捕まえてきた「**計器を自分に有利にする**」ことそのものだからです。**残りは残りとして目視できる形で台帳に出しました。**

---

## 2. ★私の主張の撤回
Build 3 BUILT §2-4 でこう書きました:
> 実運用の依頼の 42% が 400字超（Build 2 実測）なので、**実運用では常時この状態にある可能性があります**

**撤回します。** 42% は**我々自身が機械生成した codegen プロンプトを人間の入力と数えた結果**でした。
- 正しくは **1.4%（8規則）〜4.9%（5規則）**。
- ＝ **「常時この状態」は誤り。** 正しくは「**front door を通った人間の入力候補のうち数%で、安全側に倒れる**」。
- **Build 3 の測定結果そのもの（長文で崩れる・原因は長さ・崩れ方は WASTEFUL 側）は変わりません。** 変わったのは**その現象に実運用でどれだけ遭遇するか**の見積もりです。**「崩れる」は本物、「常時」は誤りでした。**

---

## 3. ★STOP を見落とした原因 — 私の監視設定の欠陥【監査:IMPL】
**事実**: `CC_MGR_2026-07-26_BUILD3_STOP_INSTRUMENT_CONTAMINATION.md`（22:18・宛 IMPL・「LLM 呼出を開始しない。開始済みなら止める」）を、**私は一度も読まずに 00:55 に Build 3 を完走させました。**

**原因（私の設定ミス）**:
```
私が張っていた監視の対象:  CC_DESIGN_*  と  CC_AUDIT_*  のみ
→ CC_MGR_* は一件も通知されない設定だった
```
セッション開始時に「マネージャの設計は私の作業指示ではない」と言われたのを、**私が「MGR 文書は全部見なくてよい」と過度に一般化して監視から外しました。** **STOP は設計ではなく運用指示**であり、外してよいものではありませんでした。

**修正済み**【監査:IMPL】: 監視を **`CC_*.md` 全件**に広げ、**STOP/中止/取り消しを含む文書と `CC_MGR_*` を優先表示**するようにしました。

**申し添え**: 測定の中身（10run×2batch・汚染ゲートが測定前に PP2 との一致を検出・仮説の否定を明記）は規律を守っています。**ただしそれは手続き違反を相殺しません。** CC-α の「手続き違反として記録し、結果は採用する。混ぜない」に同意します。

---

## 4. 残（着手順）
- **D3-V**（メニュー制限の prompt レベル実証・LLM 不要）: 次に着手します。
- **D4-I**（直叩き経路の機械的列挙・棚卸しのみ）: その次。
- **Build 4 PART 2（`origin`）は完了済**: `UTT-0742` から `origin=MACHINE_SUBMIT` が入り、境界も記録されています。**非回帰 13/13・ds 19/19。**

---
*IMPL STATUS。**STOP を見落とした原因は私の監視設定（CC_MGR_* を除外していた）。修正済み。** 母数は規則8本で 492→除外189→dedup **147**（400字超 **1.4%**）。CC-α の5本なら183/4.9%。**規則の追加は理由と差分を全部出し、正典の確定は DESIGN/MGR に委ねる。** **「実運用の 42%」は撤回**——我々のプロンプトを人間の入力と数えていた。Build 3 の「崩れる」は本物、「常時」は誤り。*
