# ITEM 記帳の actor 欠損 全件調査（★補完0・★推測0・★書き込み0）

作成: Claude Code (MGR) / 2026-08-25 / ITEM: `ITEM-2DER-EVO-0100`

Taka 裁定 逐語:
「actor欠損339件を全件調査する。補完はまだしない。
　各件を『既存情報から決定論で主体を引ける / 情報は在るが未接続 / 本当に記録が無い /
　複数候補で一意に決まらない』に分け、分母と保存則を出す。
　principal_of、ETRACE、DW event、request_thread actor記録、front door認証主体など
　既存の主体情報源を先に全件検索する。推測でactorを書かない。
　その上で、配線で消える欠損と本当に復元不能な欠損を分けて報告する。」

---

## 1. ★分母（★裁定の「339件」を実測で数え直した）

```
ITEM 記帳 総数        2,654
 ├─ ★actor 有り       2,310   (Claude 1,123 / MGR 1,118 / Taka 69)
 └─ ★actor 欠損       ★344     ← ★裁定の339より5件多い（★記帳が進んだため）
```

★**「339件」は測った時点の値**。同じ鍵で数え直すと **344件**。以下すべて 344 を分母にする。

## 2. ★先に全件検索した主体情報源

| 出所 | 実在 | 引けたか |
|---|---|---|
| `principal_of`（`twoder/principal_attribution.py:22`） | ○ | ★**推測しない作り**（未知は `UNKNOWN_PRINCIPAL`）。**写像には使えるが情報源ではない** |
| note 本文の別表記（`instance=` / `actor:` / 「MGR から」/ `recorded_by`） | ○ | **3件** |
| ETRACE の `ETR-` 参照 → run の component | ○（索引 4,397件） | **0件**（欠損 note に `ETR-` が無い） |
| ETRACE の**同時刻**突合 | ○ | ★**一意に決まったのは 0件**（複数 48 / 無し 292） |
| DW event | ○ | item 単位で 35件が重なるが ★**記帳1行に対応づかない** |
| `request_thread.record_actor` | ○ | ★**明細の actor**であって **ITEM 記帳の actor ではない**（別の対象） |
| front door の `actor_role` | ○ | ★逐語「**`actor_role` は宣言上の担当であって実際に供した主体ではない**」＝**使えない** |

## 3. ★★4分類（★保存則 成立）

| 分類 | 件数 |
|---|---:|
| **★決定論で主体を引ける** | **3** |
| 情報は在るが未接続 | **0** |
| 複数候補で一意に決まらない | **0** |
| **★本当に記録が無い** | **★341** |
| **合計** | **344 / 344 ★一致 True** |

引けた3件の出所：`actor=Claude(MGR)` が本文の途中に在る2件 ＋ 「Taka から」が本文に在る1件。

## 4. ★「無い」と言う前に探した範囲（★§15）

- ROADMAP 行の**全欄**を数えた（`kind` / `authority` / `wiring_state` 等 ―― **主体を持つ欄は無い**）
- **時刻**が引けるか確認（★`ts` が無いのは **4件**のみ ∴ 突合できないのが理由ではない）
- ETRACE を**分単位で突合**（同時刻の component が**一意なのは 0件**・複数48・無し292）
- DW event を **ITEM 参照で全走査**（49 ITEM が現れ、欠損行の item と 35 重なるが**行単位に対応づかない**）

★∴ **341件は、既存のどの出所からも主体を決定論で引けない。**

## 5. ★配線で消える欠損 と 復元不能な欠損の切り分け

### 5-1 ★復元不能（★過去の在庫）＝ **341件**

```
時期: 2026-07 → 171件 / 2026-08 → 169件 / ts無し 4件
形 :  その他の散文 187 / 短い一文 124 / ★AXIS= 形式 19 / 状態語で始まる(初期登録) 14
```

★**書いた時に主体を残さなかった**もの。追記式の台帳ゆえ**後から真値を復元する出所が無い**。
★**推測で埋めれば偽の主体が台帳に入る** ∴ **補完しない**（裁定どおり）。

### 5-2 ★配線で消える（★これから発生する分）＝ **書き手側の問題**

★**`AXIS=` 形式なのに actor 欄だけ欠けているものが 19件**在る。
これは**書式は守られているのに先頭の `actor=` だけ落ちた**もの＝**書き手の作法の穴**。

★**同型の欠損は、記帳の入口で `actor=` を必須にすれば発生しなくなる**。
★既存機構が在る ―― `progress_seal` の逐語「**UNKNOWN は値として書けるが、欄を欠くことは許さない**」。
★**同じ門を ITEM 記帳にも当てれば、5-2 は 0 になる**（★実装していない）。

## 6. ★私の見立て

| 何を | 件数 | どうするか |
|---|---:|---|
| **復元不能** | **341** | ★**補完しない**。★`UNKNOWN` と**書く**か、★**欠損のまま残す**かは裁定事項 |
| **配線で消える** | ★**これから分**（既存19件は復元不能側） | ★記帳の入口で `actor=` を必須にする（★既存の `progress_seal` と同じ作法） |

★**「補完する/しない」より先に、入口を締める方が効きます** ―― 締めなければ 344 は増え続けます。
★実測でも **2026-07 に 171 / 2026-08 に 169** と**均等に増えて**います。

## 7. ★やっていないこと

- **補完 0件**（1件も `actor` を書いていない）
- **推測 0**（`principal_of` は未知を `UNKNOWN_PRINCIPAL` にする作り ∴ 埋めていない）
- **書き込み 0**（調査のみ）
- 入口の門を**実装していない**（★裁定待ち）
