# LLMK-0010 答えが返るかを決めているのは max_tokens ではなく enable_thinking

- knowledge_id: LLMK-0010
- maturity: MEASURED
- 測ったHEAD: egl df4b45b ／ 実走 vLLM Qwen3.6-35B-A3B（max-num-seqs 32 / max-model-len 65536 / TP2 / gpu-util 0.92）
- call_sites: `twoder/runs/gpu_experiment/experiment_full_swap.py:gen`（★空が実在することを確かめた点）
- applies_when: reasoning モデル（Qwen3 系）を :8005 経由で呼ぶとき
- 出所: ITEM-2DER-EVO-0020（役割③トークン数・並列数 ／ ④信頼度）

## 1. 実測（同一設問・seed=0・max_tokens=512・n=5・実走）

| | 答えが返った | completion_tokens | finish_reason |
|---|---|---|---|
| thinking 既定（有） | **3/5 = 60%** | 437 / 512 / 512 / 396 / 486 | stop 3・**length 2** |
| `enable_thinking=False` | **5/5 = 100%** | **2 / 2 / 2 / 2 / 2** | stop 5 |

★**答えは同じ**（どちらも「問題」）。★**払ったトークンは 470 対 2 ＝ 約235倍。**

## 2. max_tokens を上げても解決しない

| max_tokens | thinking 既定 | `False` |
|---|---|---|
| 32 | 0/2 | **2/2** |
| 128 | 0/2 | **2/2** |
| 512 | 1/2 → 別機会に 3/5 | **2/2** |

★**thinking を切れば 32 tokens で足りる。**★切らなければ 512 でも 4割が空で返る。

## 3. ★危うく「再現性100%」と報告するところだった

最初の実測は max_tokens=32・seed 3条件・各5回＝**15回とも完全一致**だった。
★**中身が空**（finish_reason=length）だっただけである。
★**一致したのではなく、15回とも答えを返さなかった。**
∴ ★**自己一致は、答えが在ることを確かめてからでないと意味を持たない。**

## 4. 呼出点の分母（2026-08-29 / 38件）

| enable_thinking | 件数 |
|---|---|
| `False` を書いている | 15 |
| **ABSENT（書いていない＝既定＝有）** | **19** |
| UNRESOLVED（引数で決まる） | 4 |

★**19件は、この設問と同じ形なら 4割が空で返り、235倍のトークンを払う側に居る。**
★ただし **19件それぞれで測ってはいない**（★0件ではなく未測定）。

## 5. ★「全部切れ」とは言えない

測ったのは **1語で答える分類**である。★**長文の生成や判断で thinking が品質に効くかは測っていない。**
∴ 使える形はこう：

- ★**答えが短い（分類・1語・ラベル）呼出点は `enable_thinking=False` を書く。**★測った範囲はここまで。
- ★**呼出点ごとに測る。**「モデルの設定」ではなく「★その呼出点の設問の形」で決まる。
- ★**自己一致を出す前に、答えが空でないかを先に数える。**
