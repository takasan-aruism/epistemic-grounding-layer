# 設計/監査 → MGR（写: Taka / IMPL）: **Build 12 監査 — 原因は私の依頼文。契約マーカーが無い依頼は、構造上ぜったいに成果物を生まない**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=FINDING
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_IMPL_2026-07-27_BUILD12_RUN_WORKER_BUILT.md`

## 0. 判定
**Build 12 は通過。** 鮮度確認・1段のみ・保全対象なしの申告・オラクル非開封・手で書かない、すべて守られている。
**★失敗の原因は worker ではない。私の依頼文である。** **先に書く。**

---

## 1. ★原因 — 依頼文に契約マーカーが無かった【監査:CC-α】

```
再現: sed -n '1,45p' twoder/contract_seal.py
  BEGIN_SK = "<<<2DER:SKELETON>>>"
  BEGIN_IT = "<<<2DER:IMMUTABLE_TESTS>>>"
  END      = "<<<2DER:END>>>"
  「BEGIN_SK も BEGIN_IT も無ければ None(契約無し)」
再現: sed -n '140,165p' twoder/generate_via_runner.py
  「contract 無 → reason="SPEC_INCOMPLETE_NO_CONTRACT"」（ledger fail-closed。run_runner を呼ばない）
  「契約が揃えば mint_token → run_runner(skeleton=, immutable_tests=, ...)」
```

**流れ:**
```
私の依頼文（マーカー無し）
  → submit.py:430  contract_seal.extract_contract(raw_input) → None
  → W.create_task(..., contract=None)
  → GENERATE 段  generate() が CREATE の contract を引く → 無い
  → fail-closed  reason="SPEC_INCOMPLETE_NO_CONTRACT" / diff=null / artifact_sha256=null
```

**∴ worker は正しく振る舞った。** **仕様が無いのに書き始めなかった。** **これは本日ずっと求めてきた挙動そのものである**（無いものを在ることにしない／失敗を正常に見える結果に置き換えない）。

### 1-1. ★一般化すると重い
> **front door から投げた依頼は、契約マーカーが無い限り、構造上ぜったいに成果物を生まない。**

**∴ Build 9B・9C・11・12 の依頼文は、いずれもマーカーを持っていない。** **∴ どれも成果物には到達し得なかった。**
**∴「2DER に作らせる」は、今日ずっと、生成の手前で止まる形の依頼を投げ続けていた。**

### 1-2. これは本日8回目の「既存を読まずに使った」である
**私は worker に作らせる依頼文を、worker が入力として何を要求するかを読まずに書いた。**
- 3回目まで: 作ったものが配線されていない
- 7回目: 動いているプロセスが古い
- **8回目（本件）: 呼ぶ側が、呼ばれる側の入力要件を読んでいない**

---

## 2. ★これが「設計は Claude 担当」の機構的な正体【設計:CC-α】

**Taka の発話**（2026-07-27）:
> 「**DW側は問題ないけど設計はまだClaude担当になると思う**」

**契約の構造がそれをそのまま実装している:**
| 誰が出すか | 何を |
|---|---|
| **依頼者（Claude）** | **`skeleton`**（関数の骨格・シグネチャ・契約の記述）と **`immutable_tests`**（変更不可の受入テスト） |
| **worker（Qwen）** | **body だけ**（`contract_seal.py` 逐語: 「**worker(Qwen)は下部の extract_contract body だけを埋める。骨格・マーカー定数・_sha256 は変更禁止**」） |

**∴ 「設計は Claude 担当」は比喩ではない。** **skeleton と immutable_tests を書くことが、この系での「設計」である。**
**∴ 私はそれを書かずに「作れ」と言っていた。**

### 2-1. ★私の受入オラクルは、そのまま `immutable_tests` である
**私は成果物を見る前に受入テストを固定し、封緘した**（sha256 `77af566…`）。
**この系は、まさにそれを依頼時に渡すことを求めている。**

**∴ 私の「オラクルは worker に渡さない（渡せばそれに合わせて書かれる）」という方針は、この機構とは合わない。**
**★ただし全部を渡すべきではないと考える。理由と対処:**
- **渡したテストに合わせて書かれるのは、この系では正しい**（テストが仕様である）。
- **しかし「テストだけ通る実装」を弾く手段が無くなる。**
- **∴ `immutable_tests` には MUST の中核（4状態の分離・`resolve_fn` を呼ばない・falsy と None の区別）を入れ、`M6` の一部（`0` / `False` / `""`）と別接頭辞の組み合わせを**held-out** として私の手元に残す。**
- **★held-out を残すことを、依頼文にも書く**（隠さない。「渡したテストが全てではない」と明示する）。

---

## 3. 併せて見つけた小さな欠陥（提案のみ・作らない）【設計:CC-α】
**契約が無いことは `submit()` の時点で決定論的に分かる**（`extract_contract` が `None` を返す）。
**しかし front door は何も言わない。** **失敗は3段あと（GENERATE）に出る。**

> **提案**: `submit.py` が `contract=None` のとき、`TRACE` に1行記録する（例: `CONTRACT: NONE`）。
> **効果**: **投入した時点で「これは成果物に到達しない依頼だ」と分かる。**
> **v1.9 の判定**: **足すなら何を廃止するか** → **廃止するものは無い。∴ 今は作らない。MGR が要ると判断したら起票する。**

**★私はこれを「作りたい」と思ったが、止めた。** **止めたことを記録する。**

---

## 4. 次にやること（提案・裁定を待つ）
| # | 内容 |
|---|---|
| **B12R** | **契約マーカー付きの依頼文で、もう一度1段動かす。** 依頼文は DESIGN が確定（`skeleton` ＋ `immutable_tests` を書く） |
| 注意 | **task id は依頼文の sha1 なので、文面が変われば新しい task になる。** **`B9B4DA3B` は `READY_FOR_AUDIT` のまま残る**（触らない） |
| 注意 | **新しい task は `CREATED` から始まり、PLAN → GENERATE の2段が要る。** **1段ずつ2回に分けるか、例外的に2段進めるかは MGR の裁定** |

**【設計:CC-α】私は「1段ずつ2回」を推す。** **PLAN と GENERATE は別の失敗をする。** **混ぜると、どちらで落ちたか分からなくなる。**

---

## 5. 受入の確認【監査:CC-α】
| # | 項目 | 判定 |
|---|---|---|
| 定型見出し（到達経路 / 持ち越し） | **満たす** |
| プロセス鮮度 | **★満たす**（起動 > 全ソース mtime を先に確認して続行）。**定型見出しに移した効果** |
| 結果の区分 | **★4択に丸が無い。** ただし本文で「worker は走ったが成果物なし」と明確に書かれており、**4択のどれにも当てはまらない**（`GENERATION_FAILED` に近いが「worker が出せなかった」ではなく「契約が無く着手しなかった」） |
| 中身の評価をしない / オラクル非開封 / 配置・登記・配線なし / 1段のみ / 手で書かない | **満たす** |
| `SPEC_INCOMPLETE_NO_CONTRACT` の出所を `file:line` で示した | **満たす**（**「なぜ contract が無いかは調べていない」と限界も明記。正しい**） |

### 5-1. 区分に5つ目が要る
> **`REFUSED_NO_CONTRACT` — 契約（skeleton / immutable_tests）が無いため、worker が着手せず fail-closed した。**
**`GENERATION_FAILED`（作れなかった）と混ぜない。** **前者は依頼側の欠陥、後者は生成側の限界である。**

---

## 6. 私の誤り（消さない）
1. **worker の入力要件を読まずに依頼文を書いた**（§1-2）。**本日8回目。**
2. **`skeleton` と `immutable_tests` を書くことが、この系での「設計」だと気づいていなかった**（§2）。**Taka は「設計はまだ Claude 担当」と明言していた。**
3. **オラクルの扱いを、この機構と噛み合わない前提で設計していた**（§2-1）。
4. **オラクルは未開封のまま**（sha256 `77af566…`）。**`ids.resolve()` は未実行。**

---
*CC-α Build 12 監査。通過（鮮度確認・1段のみ・保全対象なしの申告・オラクル非開封・手で書かない）。★失敗の原因は worker でなく私の依頼文——`contract_seal` は `<<<2DER:SKELETON>>>` と `<<<2DER:IMMUTABLE_TESTS>>>` のマーカーを要求し、無ければ `extract_contract` が `None` を返し、`generate()` が fail-closed で `SPEC_INCOMPLETE_NO_CONTRACT` を返す。worker は仕様が無いのに書き始めなかった＝本日ずっと求めてきた挙動そのもの。★一般化すると重い: front door から投げた依頼は契約マーカーが無い限り構造上ぜったいに成果物を生まない ∴ Build 9B/9C/11/12 の依頼文はどれも生成に到達し得なかった。本日8回目の「既存を読まずに使った」（呼ぶ側が呼ばれる側の入力要件を読んでいない）。★これが「設計はまだ Claude 担当」の機構的な正体——依頼者が `skeleton` と `immutable_tests` を出し、worker は body だけを埋める（`contract_seal.py` 逐語）。∴ 私の受入オラクルはそのまま `immutable_tests` であり、「worker に渡さない」方針はこの機構と合わない。ただし全部は渡さず MUST の中核を渡して一部を held-out に残し、held-out を残すこと自体を依頼文に明示する（隠さない）。★小さな欠陥を1件見つけたが作らない=契約の不在は submit 時点で決定論的に分かるのに front door は何も言わず失敗は3段あとに出る（v1.9「足すなら何を廃止するか」を満たせないので止めた。止めたことを記録する）。次=B12R として契約マーカー付きの依頼文で再投入、ただし文面が変われば新 task なので PLAN と GENERATE の2段が要り、**1段ずつ2回**を推す（混ぜるとどちらで落ちたか分からない）。区分に5つ目 `REFUSED_NO_CONTRACT` が要る（依頼側の欠陥であり生成側の限界ではない）。*
