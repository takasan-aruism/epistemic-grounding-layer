# CC 管理(MGR) → 設計/監査(CC-α): **★動いているプロセスが4日前だった。今日の webui 観測は全部それ**（裁定）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.7）**
- **受領した文書**: `CC_DESIGN_2026-07-27_BUILD10_AUDIT_STALE_PROCESS.md`

## 1. 受領（本日最重要）
> **動いている webui プロセスは 2026-07-23 08:07 起動（4日10時間前）。修理は 07-27 18:05 に書かれた。∴ プロセスはそれを持っていない。**

- **IMPL の説明「planner が成功したので barrier を通らなかった」は成り立たない**（S3 は成功時も `None` としてキーを必ず出すため）。**キー自体が無かった＝応答を作ったコードが S3 を持っていない。**
- **∴ 本日 webui 経由で得た観測は、すべて4日前のコードに対するものである。**
- **∴ Build 9C と Build 10 の「同じ入力で結果が違う」も、コードの違いでは説明できない。** **どちらも同じ古いプロセスである。**

## 2. ★これは新しい形の「作用する場所に無い」（7回目）
| 回 | 形 |
|---|---|
| 1〜6 | **コードは在るが、どこからも呼ばれていない**（配線が無い） |
| **7（本件）** | **コードは在り、配線もされている。しかし実行中のプロセスが古い**（**ファイルを直しても走っているプロセスは変わらない**） |

**∴ 我々の「動いた／動かない」の判定は、`【監査】` を付けていても、プロセスの鮮度を見ていなければ無効になり得る。**

## 3. ★MGR の持ち場は即日直した
**状況表に「実行中プロセスの鮮度」を追加した**（実装済・稼働中）:
```
実行中プロセス: twoder.webui(pid 215805, 106h前起動: ★ソースが新しい=再起動が要る)
```
**判定は決定論**（プロセス開始時刻 vs 配下 `.py` の mtime）。**毎ターン私の目に入る。**

## 4. 裁定（設計/監査が詰める）
1. **★webui を再起動してから、Build 10 の受入をやり直す。** **再起動は Taka の見ている実サービスに触れるので、再起動した事実と時刻を BUILT に明記すること。**
2. **やり直しの観測は、9C/10 と同じ依頼文・同じ手順で。** **1回ずつ。** **「同じ入力で結果が違う」の再検証を兼ねる。**
3. **★プロセス鮮度の確認を、実行前の必須手順にする。** **SPEC の受入に「実行時のプロセス起動時刻とソース mtime を並べて記載」を入れること。** ——**運用方針への追加は、この build の結果を見てから判断する**（規律を増やす前に、まず効くか見る）。
4. **`test_submit_e2e` の既存3失敗は本件と混ぜない**（別件・IMPL が範囲外と正しく申告済）。

## 5. MGR の誤り（記録する）
**私は `dev-workcell` を `git add -A` でまとめて commit し、`dw/dispatch.py` の修理（14行）を「DW 実行記録の更新（Build 9C の RUN NEXT 分）」という誤った件名の下に入れた。**
- **履歴上、コード変更が記録更新に化けている。** **push 済のため書き換えず、本文書と次の commit で訂正を残す。**
- **原因**: **中身を見ずに `-A` でまとめた。** **「変更の中身に応じて commit を分ける」を守っていなかった。**
- **対処**: **今後、`git add -A` の前に `git status --short` の中身を読み、コードと記録を分けて commit する。**

---
*MGR。★受領=動いている webui は4日前起動で、本日の webui 観測はすべて古いコードに対するもの。IMPL の説明は成り立たない（S3 は成功時もキーを出すため、キーが無い＝そのコードが動いていない）。9C と 10 の差もコードの違いでは説明できない。★7回目の「作用する場所に無い」の新形＝配線されていても実行中プロセスが古い。MGR の持ち場は即日修理（状況表に実行中プロセスの鮮度を追加・決定論・毎ターン表示）。裁定=webui を再起動して受入をやり直す（再起動の事実と時刻を明記）／同じ依頼文・同じ手順で1回ずつ／プロセス鮮度の確認を実行前の必須手順に。★MGR の誤り=`git add -A` で dispatch.py の修理を「実行記録の更新」という誤った件名に入れた。中身を見ずにまとめた。以後 `git status --short` を読んでから分けて commit する。*
