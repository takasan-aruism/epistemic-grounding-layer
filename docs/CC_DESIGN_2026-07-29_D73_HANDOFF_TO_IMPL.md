# 設計/監査 → **実装(IMPL)**: **155行の ID を5行の件数にしてください（承認済）。★受入1 を訂正しました**（写: MGR / Taka）

- `BUILD_ROLE: 参照`（**送付状。★実装源は本文書ではない**）
- **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: `v2.8`）** / **承認**: `CC_MGR_2026-07-29_D73_APPROVED_YOUR_FIX_IS_RIGHT_BUT_ACCEPT1_NEEDS_A_BROWSER.md`

## 1. ★実装源（これ1本・★改訂1 入り）
```
CC_DESIGN_2026-07-29_D73_TASK_SUMMARY_BUILD_SPEC_v1_0.md
```

## 2. ★改訂1（★これだけ読めば足りる）
| | |
|---|---|
| **旧** | 受入1「既定で ID が0件」の示し方＝**「`GET /command` の描画結果」** |
| **★新** | **1a**「JS のソースに全件を並べる箇所が無い」→ **★差分で数える**／**1b**「実際の画面から155行が消えた」→ **★我々には示せない。「確かめていない」と書く** |

> **★理由: 155件の ID は JS が★実行時に描いている。** **`curl` の HTML には★元から入っていない。**
> **∴ 「消えた」も「在る」も curl では言えない。**
> **★これは今日の事故の裏返しである**——**あの時は「文字は在るのにページが死んでいた」。今回は「消えたことを HTML では見られない」。**
> **★私は「配信物で検査する」を学んだ直後に、「配信物で見えないもの」を受入に書いた。**

## 3. ★作るもの（既定＝5行 ＋ 合計）
```
継続中の別タスク  合計 155件
  停止中                  2件
  依存待ち               18件
  背景保持              117件
  現在前面化すべきもの   ★出せません（前面化を決める値が 2DER に無いため）
  どの区分にも入らない   18件（誰も進められない状態）
```
**★区分の判定はすべて決定論**（SPEC §1-1）。**★LLM を使わない。**

## 4. ★外しやすい4点
| # | |
|---|---|
| **1** | **★「その他」と書かない。「どの区分にも入らない」と書く**（「その他」は中身を隠す） |
| **2** | **★「現在前面化すべきもの」を消さない。「0件」とも書かない。** **「出せません」＋理由** |
| **3** | **★先頭30字には必ず併記する**:「**先頭30字です（全文ではありません）。先頭が同じでも別の依頼です**」——**★42件が同じ先頭を持つが、全文は154/155 が異なる。書かないと同じものが並んで見える** |
| **4** | **★合計は固定値にしない。** **★「各区分の和が、表示している合計と一致すること」**（155 は今日の値。**固定すると明日 嘘になる**） |

## 5. ★止まってよい場所（★5番が増えた）
1. 区分の判定が SPEC §1-1 のとおりに書けない（`actor_role` が取れない等）→ 止めて報告
2. **和が合計と一致しない** → **★止めて報告。合わせるために区分を動かさない**
3. 既定で ID を0件にすると既存の機能が壊れる → 止めて報告
4. SPEC が2通りに読める
5. **★既定の ID が、JS ではなくサーバ側で入っていた** → **★止めて報告**（MGR の前提が誤りだったことになる）

## 6. ★投入は1回も要らない
**★既定の表示は、いま在るデータで確かめられる**（155件は実在する）。**★受入1b だけが Taka 待ちである。**

## 7. ★実行前後
- **★再起動 → 配信物で `node --check` → 私が受入確認 → MGR が commit → 私が資料同期**（訂正後の順番）。
- **★再起動はあなたがやってよい**（D-69 で訂正済。配信物を新しくするため）。

---
*CC-α → IMPL 送付状（D-73）。★実装源は `CC_DESIGN_2026-07-29_D73_TASK_SUMMARY_BUILD_SPEC_v1_0.md`（改訂1 入り）。★**改訂1**=受入1 の示し方を **1a「JS のソースに全件を並べる箇所が無い」（差分で数える）** と **1b「実際の画面から155行が消えた」（我々には示せない・「確かめていない」と書く・Taka にしか出せない）** に分けた——**155件の ID は JS が実行時に描いており `curl` の HTML には元から入っていないので「消えた」も「在る」も curl では言えない**。**これは今日の事故の裏返し**（あの時は「文字は在るのにページが死んでいた」、今回は「消えたことを HTML では見られない」）で、**CC-α は「配信物で検査する」を学んだ直後に「配信物で見えないもの」を受入に書いた**。★作るものは既定5行＋合計（停止中2／依存待ち18／背景保持117／**現在前面化すべきもの＝出せません＋理由**／**どの区分にも入らない18**）で、**区分の判定はすべて決定論・LLM を使わない**。★外しやすい4点=**「その他」と書かず「どの区分にも入らない」と書く**（「その他」は中身を隠す）／**「現在前面化すべきもの」を消さず「0件」とも書かず「出せません」＋理由**／**先頭30字には「先頭30字です（全文ではありません）。先頭が同じでも別の依頼です」を必ず併記**（42件が同じ先頭だが全文は154/155 が異なるので、書かないと同じものが並んで見える）／**合計を固定値にせず「各区分の和が、表示している合計と一致すること」**（155 は今日の値で固定すると明日 嘘になる）。★止まってよい場所5件（**和が合計と一致しないとき合わせるために区分を動かさない**／**既定の ID が JS ではなくサーバ側で入っていたら MGR の前提が誤り**）。★**投入は1回も要らない**（既定の表示はいま在るデータで確かめられ、受入1b だけが Taka 待ち）。★順番=再起動 → 配信物で `node --check` → CC-α が受入確認 → MGR が commit → CC-α が資料同期。**再起動は IMPL がやってよい**（D-69 で訂正済）。*
