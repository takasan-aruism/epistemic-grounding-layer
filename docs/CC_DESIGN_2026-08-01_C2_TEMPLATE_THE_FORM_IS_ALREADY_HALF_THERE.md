# 【BUILD SPEC / C-2 `EVO-0022`】申請書を 2DER に出させる — **★空欄で出すことは★機構が保証している**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-01 14:3x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.1 / 2026-08-01）** ／ **裁定**: `D-209`（ACTIVE＝C-2・受入5つ）
- **この .md がまだ .md である理由**: 指示を台帳へ渡す口が無いため（★本件が通れば ★§6-② が廃止できる）
- **★私はコードを1行も変えていない**

---

# 1. ★3値で確かめた（★`D-209` §2「在れば作らない」）

| 部品 | 3値 | 実測 |
|---|---|---|
| **不足の名指し** | **★在って動く** | `TASK-2DER-B37727E3` の `next_information_need` に **★7件**（`GET /api/state` の `work` 欄で読める）。逐語例:「Explicit mapping table or function for English-to-Japanese status translation」 |
| **契約マーカーの定数** | **★在って動く** | `contract_seal.BEGIN_SK/BEGIN_IT/END` ／ `progress_seal.BEGIN_PR` |
| **★雛形を組み立てて返す口** | **★★無い** | ★マーカーを**出力する側**は ★全数走査で **0件**（★`submit.py:241-242` の2件は `EVO-0028` の**★除去側**であり出力ではない。★中身を見て確かめた） |

```
★★∴ ★作るのは ★★1つだけ: ★「★既に在る不足の名指し」を ★「★次に投げる依頼文の形」に★組み立てて返す口。
★★★不足を★検出する仕組みは ★作らない（★既に在る）。
```

---

# 2. ★★設計思想は ★機構が既に守っている（★`D-209` §3・★feasibility-first で先に書く）

> ### **★空欄の契約を入れた雛形は、★そのまま投げても★通らない。**

```
★`contract_seal.py:66-67` 逐語:
   `if not any(ln.startswith("def test_") for ln in immutable_tests.splitlines()):
        raise ValueError("immutable_tests has no pytest-collectable test (^def test_)")`
★★∴ ★契約の枠だけ在って中身が空の雛形は ★★投入時に ★`ValueError` で弾かれる。
★★★★＝ ★★「★人が埋めるまで進めない」は ★★規律ではなく ★機構である。
★★★★★★∴ ★Taka 確定「★LLM が自分で書いた合格条件で自分の成果物を採点させない」は
   ★★本件で★壊れない。★2DER が契約を埋めたくても ★空欄で出す限り ★埋めた人が誰かは★投入時に分かる。
★★★★★★★これは ★私が作る保証ではない。★★既に在る。★★∴ ★作らない。
```

---

# 3. ★やること（★1つ。★新しい入口を作らない）

## 3-1. ★`twoder/request_template.py` を1本（★`progress_seal.py` と同じ大きさの見込み）
```python
def build(goal, next_information_need):
    """止まっている task の goal と不足の名指しから、次に投げる依頼文の雛形を1つの文字列で返す。
    ★契約の中身は空で出す（★人が埋める）。★LLM を呼ばない（★決定論）。"""
```
**★返す雛形（★固定部分は日本語）**
```
【次に投げる依頼文の雛形】この雛形は 2DER が出しました。★空欄は人が埋めてください。

■ 依頼（★前回の依頼をそのまま置いています。★必要なら書き換えてください）
<goal>

■ 2DER が「足りない」と言っている項目（★埋めてください）
  1. <next_information_need[0]>
  …

■ 契約（★合格条件。★★ここは人が書きます。★2DER は埋めません）
<<<2DER:SKELETON>>>
（★ここに骨格を書く）
<<<2DER:END>>>

<<<2DER:IMMUTABLE_TESTS>>>
（★ここに def test_ で始まる試験を書く。★空のままだと投入時に弾かれます）
<<<2DER:END>>>
```

## 3-2. ★読む口（★v1.1。★対で名指しする）
```
★`GET /api/state?task_id=…` に ★`next_request_template` を ★1欄 足す
   （★`upper_reviews` を足したのと ★同じやり方・★同じ場所）
★★★★書く口は ★作らない（★本件は★読むだけの機能である）。★v1.1 の「書く口には読む口を」は
   ★★逆向き ∴ ★該当しない。★そう書いておく。
```

---

# 4. ★★正直に書くこと（★通ってから言わない）

```
★★受入(e)「★人間が読める日本語で出る」は ★★半分しか満たせない。
   ★★枠（見出し・注意書き）は ★日本語で書ける。
   ★★★しかし ★不足の名指し（`next_information_need`）の中身は ★★Qwen が英語で書いたものである
      （★実測・逐語:「Explicit mapping table or function for English-to-Japanese status translation」）
★★★★∴ ★雛形は ★★「日本語の枠 ＋ 英語の中身」になる。
★★★★★訳すなら ★表示時に Qwen で訳す（★`D-193` の方針）が、★★それは ★別件である（★規律9・一度に2つ増やさない）。
   ★★★★★私は ★今回 ★訳さない設計にした。★★通ってから「日本語で出ました」と★書かないこと。
```

---

# 5. ★受入（★`D-209` §4。★(e) は §4 の但し書きつき）
```
★(a) ★`GET /api/state?task_id=TASK-2DER-B37727E3` に ★`next_request_template` が★在り、★雛形が返る
★(b) ★雛形に ★契約マーカーの枠が在り、★★中身が空である（★`def test_` が ★0行）
★(c) ★★空のまま投入すると ★★`ValueError` で弾かれる（★★§2 の機構が効いていることを★実測で示す）
     ★★★埋めて投入すると ★通る（★★別 task になる。★★そう書く）
★(d) ★止まった理由が front door から読める → ★★既に成立している（★`work.next_information_need` 7件）。
     ★★★再確認だけでよい
★(e) ★枠は日本語で出る（★★中身は英語のまま。★★§4 のとおり ★そう書く）
★★★★★予告を投入前に書く: ★(a)〜(e) の予想 ／ ★変更行数 ／ ★埋めて投入したときの ★新しい task の id（★sha1 から先に）
```

# 6. ★同時に廃止するもの（★規律9・★通った時だけ）
```
★① ★止まった理由を .md に書いて人が読む運用 → ★`work.next_information_need` を読む形へ
★② ★Claude が依頼文の★様式部分を手で書く運用 → ★`next_request_template` を使う形へ
★★★通らなければ ★廃止しない（★嘘を書かない）
```

# 7. ★禁止 ／ 8. ★報告
```
【禁止】★新しい台帳・新しいマーカー・新しい状態語を作る ／ ★新しいエンドポイントを作る（★既存の欄に足す）
        ★`contract_seal` の検査を緩める（★★§2 の `ValueError` は ★守るべき機構である）
        ★2DER に ★契約の中身を書かせる ／ ★不足の検出を作り直す（★既に在る）
        ★S-3 の依頼文を書き換える（★別 task になる。★C-2 が通ってから投げ直す）／ ★commit する
【報告】1 ★変更行数（★`twoder` 配下 ∴ ★Claude が書く。★2DER の実績に数えない）
        2 ★受入 (a)〜(e) ／ 3 ★予告の当否 ／ 4 ★戻し方（★可逆）
        5 ★★(c) で ★実際に弾かれたか（★逐語のエラー文）
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---
**決めたこと**: **①3値で確かめた——不足の名指し（`next_information_need`・7件が `/api/state` の `work` 欄で読める）と契約マーカーの定数は「在って動く」、雛形を組み立てて返す口だけが「無い」。マーカーを出力する側は全数走査で0件で、`submit.py` の2件は `EVO-0028` の除去側だと中身を見て確かめた ②∴ 作るのは1つだけ。不足を検出する仕組みは既に在るので作らない ③設計思想は機構が既に守っている——契約の中身が空だと `contract_seal` が投入時に `ValueError` で弾く。「人が埋めるまで進めない」は規律ではなく機構であり、Taka の「LLM が自分の合格条件で自分を採点しない」は本件で壊れない。これは私が作る保証ではなく既に在るので作らない ④`twoder/request_template.py` を1本と、`GET /api/state` に `next_request_template` を1欄。新しいエンドポイントは作らない。本件は読むだけの機能なので書く口は作らず、v1.1 は逆向きなので該当しないと明記した ⑤正直に書くこと——受入(e) は半分しか満たせない。枠は日本語だが、不足の名指しの中身は Qwen が英語で書いたものなので「日本語の枠＋英語の中身」になる。訳すのは別件（規律9）で今回はやらない。通ってから「日本語で出ました」と書かないこと ⑥受入(c) は「空のまま投げると弾かれる」ことを実測で示させる——機構が効いている証拠になる ⑦廃止は「止まった理由を .md で読む運用」と「Claude が様式部分を手で書く運用」で、通らなければ廃止しない。**
