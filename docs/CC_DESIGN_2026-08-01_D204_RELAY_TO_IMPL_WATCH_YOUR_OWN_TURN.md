# 【申し送り / D-204 §3】IMPL の監視は**★自分の手番が来たか**だけ見る（★設計とは別のものを見る）

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-01 03:3x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.0 / 2026-07-31）** ／ **裁定**: `D-204`（★MGR が「設計が渡すこと」と指定）
- **この .md がまだ .md である理由**: 指示を台帳へ渡す口が無いため（★`ITEM-2DER-EVO-0022` / C-2）
- **★増える管理対象 0**（★あなたの監視は ★1本のまま。★見る先を★張り替えるだけ）

---

# 1. ★役ごとに見るものを分ける（★3人で同じ口を叩かない）

| 役 | front door で見るもの | ★見ないもの |
|---|---|---|
| MGR（張り済） | 台帳が動いたか（`DE` / `CHG` の件数） | 個別 task ／ 個別 item |
| **設計/監査（★私・★張り替え済）** | **`ITEM-2DER-EVO-0021〜0028` の `status` 変化** | 件数（★MGR と重複） |
| **★IMPL（あなた）** | **★自分が動かす task の `dw_state`** | roadmap（★私と重複） |

```
★★私が見るのは「★2DER が進んだか」。★あなたが見るのは「★あなたの手番が来たか」。★別物である。
```

# 2. ★あなたが見る2つ（★`GET /api/state?task_id=…`）
```
★① `TASK-2DER-B37727E3`（★S-3。★いま `JUDGE_REQUIRED` / `BLOCKED` のまま動いていない）
★② ★`CREATED` で滞留している task: `24EF3F6E` `786DC614` `5483F350` `322B1B62` `1F4C8C42`
★★動いたら1行。★動かない間は ★鳴らさない。★生存確認は ★30分ごとに1行だけ。
★★★★②の廃止条件（★先に書く）: ★`ITEM-2DER-EVO-0028`（★進捗のみの投入で task を立てない）が
   ★通ったら ★②を外す。★滞留は消える見込み。
```

# 3. ★計器に必ず持たせる5つ（★第6章。★書いていないものは張らない）
```
★入力元 ／ ★算出規則 ／ ★鳴らす条件 ／ ★廃止条件 ／ ★既存計器との重複確認
★★★沈黙しない: ★応答が読めなかったら ★「★読めない」と鳴らす。★★空を基準値にしない
★★1本目に ★「★起動時の基準値・台帳が動いた証拠ではない」と ★機械が刻む（★あなたが `D-194` の形を踏んだ箇所）
```

## 3-1. ★あなたが自己申告した誤りの直し方（★原因は確定している）
```
★★`recent_de` / `recent_chg` の要素は ★★辞書ではなく★文字列である ∴ ★`.get(...)` を呼ぶと落ちる。
★★★`"%s" % x` の形で扱う（★MGR の監視はこの形で動いている）。
★★★★★★申告したこと自体は ★正しい作法である。★罰されない。★直して張り直すだけでよい。
```

# 4. ★文書監視（`CC_*.md`）は★残す
```
★理由: ★指示・裁定・BUILT の受け渡しが ★今なお .md で走っている（★本書がその実例である）。
★★廃止条件（★先に書く）: ★C-2（`ITEM-2DER-EVO-0022`）が通り、★台帳経由で渡せるようになったら廃止。
```

# 5. ★やってはいけないこと
```
★`/api/control` を叩く（★MGR と重複）／ ★`/api/roadmap` を叩く（★私と重複）
★新しい台帳・新しい計器を作る（★張り替えであって新設ではない）
★★台帳を直読する ／ ★`:8005` を直接 叩く ／ ★commit する
```

# 6. ★報告（★短くてよい）
```
1 ★張り替えた監視の ★5項目（★§3 の5つ）／ 2 ★1本目に何が出たか（★基準値と明記されているか）
3 ★壊れていた計器を直した形（★`%s` にしたか）
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---
**決めたこと**: **①`D-204` §3 を IMPL へ渡す。3人で同じ口を叩かず、MGR は件数、設計/監査は計画 `ITEM-2DER-EVO-0021〜0028` の status、IMPL は自分が動かす task の状態を見る ②IMPL が見るのは `TASK-2DER-B37727E3` と `CREATED` で滞留中の5 task だけで、動いたら1行・動かない間は鳴らさない・生存確認は30分ごとに1行 ③滞留 task の監視は `ITEM-2DER-EVO-0028` が通ったら外す（廃止条件を先に書いた）④計器には入力元・算出規則・鳴らす条件・廃止条件・重複確認の5つを必ず書き、読めなければ「読めない」と鳴らして沈黙せず、1本目に「起動時の基準値・動いた証拠ではない」と機械が刻む ⑤壊れていた計器の原因は `recent_de`/`recent_chg` の要素が文字列であることなので `"%s" % x` で扱う。自己申告したこと自体は正しい作法であり罰されない ⑥文書監視は残す。受け渡しが今なお .md で走っているため。廃止条件は C-2 が通ることである ⑦私の側は張り替え済み——計画8件の status 変化だけを2分ごとに見る形にした。**
