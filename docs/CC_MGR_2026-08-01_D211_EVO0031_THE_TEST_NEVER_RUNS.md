# 【D-211】`EVO-0031` ― ★受入試験が走らない（★前は出ていた。★今は null）

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-08-01 17:0x / TYPE=STATUS
- **開発者規律 確認済（版: v1.1）** ／ **ACTIVE を ★`EVO-0031` へ移す**（★C-2 は受入(b) を満たして待機）

## 1. ★実測（★私が front door から回した。★中身は1文字も書いていない）
```
★`TASK-2DER-CCCAEAA8`（★雛形を埋めて投入・★予告した task_id と★一致）
★走った工程: ★PLAN(CLAUDE) → GENERATE(QWEN_LIVECODER) → AUDIT(QWEN_AUDITOR) → REGENERATE → AUDIT
★★押下 ★4回 ／ ★同一依頼文の再投入 ★4回（★毎回 gate が閉じるため。★1文字も変えていない）
★★★毎回:  ★`test_result` = ★null ／ ★`findings` = ★0件 ／ ★作り直し回数の昇格も ★発火せず
★★★★∴ ★合否が出ない ∴ ★生成と監査が ★永遠に往復できる。★v0.3 §12.3 により ★私が止めた。
```

## 2. ★★手がかり（★これが一番安い。★先に見ること）
```
★★前の task（`TASK-2DER-B37727E3`）では ★`test_result` が ★出ていた。★逐語:
   `{"status":"FAILED","ok":false,"reason":"RUNNER_FAILED",
     "artifact_sha256":"479629fd29c949aff…"}`
★★★今の task では ★null である。★★∴ ★「試験を走らせる仕組みが無い」のではない。
   ★★★「★出ていたものが ★出なくなった」である。★★差分を見るのが最短である。
★★★★候補【いずれも未確認・★設計が確かめること】:
   ★`request_type` が違う（★前=？ ／ ★今=`MODIFY_EXISTING`）→ ★経路が違う可能性
   ★本日の変更（`submit.py` / `contract_seal.py` / `webui.py` / `workcell.py` / `dispatch.py`）の影響
```

## 3. ★依頼（★3値で返す。★作る前に確かめる）
```
★(a) ★試験を走らせる仕組みは ★在って動くか／在るが未配線か／無いか
★(b) ★★`RUNNER_FAILED` が出ていた頃と ★今とで ★何が違うか（★差分で示す。★推測で書かない）
★★★直し方は ★(a)(b) の答えが出てから。★★ここで先に実装しない（★原因未確定のまま実装しない）
★★★★規律 v1.1: ★試験結果は ★「★走る」だけでなく ★「★front door から読める」ところまで受入に入れること
```

## 4. ★受入（★`EVO-0031` を通す条件）
```
★(1) ★`TASK-2DER-CCCAEAA8` を進めると ★`test_result` に ★`status` と `ok` が ★入る（★null でない）
★(2) ★不合格なら ★不合格として ★次の段が決まる（★永遠に往復しない）
★(3) ★合格なら ★成果物の ★`artifact_sha256` が ★front door から読める
★★★★(4) ★★7本の試験のうち ★どれが通り ★どれが落ちたかが ★分かる（★合計だけにしない）
```

## 5. ★状態（★台帳から。★記憶で書いていない）
| 件 | 状態 |
|---|---|
| **`EVO-0031` 試験が走らない** | **★ACTIVE（本書）** |
| `EVO-0022` C-2 申請書 | ★IN_PROGRESS（★受入(b) は成立。★(c)(d) は `EVO-0031` が通らないと示せない） |
| `EVO-0027` S-3 | ★IN_PROGRESS（★`TASK-2DER-CCCAEAA8` へ引き継いだ。★前の task は記録として残す） |
| `EVO-0024` C-4 表示反映 ／ `EVO-0023` C-3 | ★予約 |
| 後回し | `D-199` ／ `G-84` ／ 滞留6件（`BLOCK` の書く口） |

---
**決めたこと**: **①ACTIVE を `EVO-0031`（受入試験が走らない）へ移す ②実測——雛形を埋めた `TASK-2DER-CCCAEAA8` で PLAN→GENERATE→AUDIT→REGENERATE→AUDIT が走ったが、`test_result` は毎回 null、`findings` は0件、作り直しの昇格も発火せず、合否が出ないまま永遠に往復できる状態だった。押下4回・再投入4回で私が止めた（v0.3 §12.3）③一番安い手がかりは差分である。前の task では `RUNNER_FAILED` と `artifact_sha256` が出ていた ∴「仕組みが無い」のではなく「出ていたものが出なくなった」。`request_type` の違いと本日の5ファイルの変更が候補（いずれも未確認）④依頼は3値で2つ——試験を走らせる仕組みの有無と、出ていた頃との差分。原因が確定するまで実装しない ⑤受入は4つ——`test_result` が null でない／不合格なら次の段が決まる／合格なら `artifact_sha256` が front door から読める／7本のうちどれが通りどれが落ちたかが分かる ⑥C-2 の残り受入は `EVO-0031` が通らないと示せない。S-3 は新しい task へ引き継ぎ、前の task は記録として残す。**
