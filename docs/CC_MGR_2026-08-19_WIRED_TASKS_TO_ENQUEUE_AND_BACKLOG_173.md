# 宛: Taka ―― `tasks_to_enqueue` 配線 完了 ／ **★起動前に 1点だけ 上げます（★上申条件 ④⑦）**

**2026-08-19 ／ commit `e2a876d`（`[Claude実装]` ・足場のみ・★判断ロジック 0行）**

---

## 1. 配線した中身（★Taka 許可の範囲そのまま）

```
`twoder/manager_v0.py::_last_task()` の 末尾に 1ブロック(★pick が 無い ときだけ 走る)

・★何が 候補か   → ★入口 `/api/tasks`(★DW の event log の ★初出順)を ★順の まま 渡す
・★state は 何か → ★DW の 正本 `dw.workcell.derive_state`
                    (★既存の 引数 `events=` に 全 event を 1回 読んで 渡す)
・★何を 足すか   → ★2DER が 契約経路で 書いた `tasks_to_enqueue` の 返りを ★そのまま
                    (★逐語「並べ替えも 優先順位も 付けない」／★私は 1行も 書いていない)
・★失敗は 握り潰さない → stderr へ 出す(★`DONE_INDEX` の NameError 前例)
```

**★所要の実測**: 528件 → **0.2秒**（`events=` を渡さず 1件ずつ引くと **53秒相当**＝常駐の周期を壊す）。

## 2. 直した欠落（★2026-08-19 F14 実測）

```
自己修復が 見る 一覧 = `domain_dw._append_index("submitted")` が 書いた 行だけ
その 呼び手      = `domain_dw.py:241`(★契約経路の 中だけ)
∴ ★front door へ 直接 投げた goal は ★常駐に ★一度も 届かなかった。
```

## 3. 隔離 ―― **★手を触れずに 成立**

| TASK | state | `to_add` に入るか |
|---|---|---|
| `9F26BF5F`（★Claude 設計の契約由来） | `JUDGE_REQUIRED` | **★入らない** |
| `32EDB6C4`（★暴走・BLOCKED） | `BLOCKED` | **★入らない** |

**∴ 停止操作を1つも打っていません**（`CREATED` 以外は部品が自動で外すため）。
**通常経路の証拠には数えません。**

## 4. ★★起動前に上げる1点 ―― **配線した瞬間に 173件が待ち行列へ入る**

```
★to_add = ★173件 ／ not_created = 355 ／ duplicates = 0（★実測・待ち行列は 書いていない）
★うち ★159件 は ★今日より前（07-22 ／ 08-09 に22 ／ 08-10 に31 ／ 08-18 に35 …）
★今夜の goal `TASK-2DER-7D461717`(20:29:44) は ★★172番目＝★最後
```

**★これが意味すること**: いま常駐を再起動すると、manager は **2026-07-22 の task から順に**掴み、
**172件を Qwen で PLAN/GENERATE してから**、ようやく今夜の goal に届きます。

- **★ご指示の受入試験（1件実走）が今夜は終わりません**
- **★Qwen 172件分の資源**（上申条件 ⑦）／ **★append-only の記録が 172件分 増える**（④）

**★私が決めない理由**: 部品は逐語で **「並べ替えも 優先順位も 付けない」**。
∴ **どれを先に流すかは 呼び手の決定**であり、**許可されたのは「配線」だけ**です。

### 選択肢（★私の見立ては (い)）

```
(あ) そのまま 起動する
     ＋ 規則を 1つも 作らない ／ 溜まっていた goal が すべて 通常経路に 乗る
     − ★今夜の 受入試験は 成立しない ／ Qwen 172件分 ／ 記録が 172件分 増える

(い) ★待ち行列を 空に したまま、★今日以降(08-19)の 14件だけを 対象に 起動する
     ＋ ★受入試験が 今夜 終わる ／ 古い 実験の 残骸を 通常経路の 証拠に 混ぜない
     − ★『今日以降』は ★Taka の 一回限りの 運用判断（★コードに 規則は 足さない）

(う) 古い 159件の 扱いを 先に 決める（★別件として 棚卸し）
     ＋ 取りこぼしを 作らない
     − ★受入試験が さらに 後ろへ
```

**(い) を推す理由**: **コードに規則を1行も足さずに済み**、かつ **ご指示の「1件実走」が今夜成立する**からです。
159件は消えません（`CREATED` のまま残り、後でいつでも同じ配線が拾います）。

## 5. 併せて記録する運転規則（★Taka 指示・逐語）

```
・★Claude 設計契約の 新規作成を ★通常経路で 禁止する
・★今後 Claude MGR は ★観測・調査・goal投入・監視・上申 のみ
・★skeleton / test_body / contract / implementation を Claude MGR が 生成した場合は
  ★2DER が 受理しない
・★Claude Senior(`claude -p`) は ★高度判断 actor として ★別枠で 維持
・★Taka が 例外を 認める場合のみ ★scope付き 単発 approval で 解除
```

**★機械での強制はまだ在りません**（＝いまは私の遵守のみ）。
**強制する部品が要る場合、それ自体を goal として 2DER に投げます**（★私は書きません）。

## 6. していないこと

```
★常駐の 再起動 0 ／ 待ち行列への 書き込み 0 ／ 停止操作 0
★判断ロジック 0行 ／ 新台帳 0 ／ 新しい front door 0
★Claude 設計の 契約 新規作成 0（★以後 出しません）
★`9F26BF5F` は 触っていない ／ `32EDB6C4` は BLOCKED の まま
```
