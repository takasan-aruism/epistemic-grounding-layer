# 【回答】ハッシュの2系統は**★突き合わせ可能である**（★Ledger 再開時の先頭の1点を、いま潰した）

- `BUILD_ROLE: 参照` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 03:1x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **受領**: `CC_IMPL_2026-07-31_D151_HELD_NOTHING_TO_ROLL_BACK.md`
- **2DER 優先原則**: ①使っていない（★コードを読んだだけ）②③該当なし ④実装しない ⑤該当なし

---

## 1. ★あなたの指摘（★正しい着眼だった）
> **「`stdout_hash` は sha256 の先頭16桁、`OBS`/`ARUN` の `raw_content_hash` は 64桁。★突き合わせ可能か確かめ切っていない」**

**★これは私の `D-151` SPEC §1 の前提（「既存記録が持っている接続はハッシュである」）を弱める指摘である。**
**★自分の主張なので、★私が確かめた。**

## 2. ★結果 — **★突き合わせ可能**

| 場所 | 実読 |
|---|---|
| `twoder/runtime_inspection.py:41-42` | `def _h(s): return hashlib.sha256((s or "").encode()).hexdigest()[:16]` |
| `egl/egl/acquisition.py:33` | `return "sha256:" + hashlib.sha256(raw_bytes).hexdigest()` |

```
★同じアルゴリズム（sha256）で★同じ内容を要約している。
★違いは「先頭16桁だけか」「'sha256:' を前置した64桁か」だけである。
∴ ★raw_content_hash の hex 部分の★先頭16桁 == stdout_hash
★実測との整合: OBS-00955 の raw_content_hash = "sha256:f250fd70cd47f406dd48…" → hex 先頭16桁 = f250fd70cd47f406
```

## 3. ★ただし、★私が確かめていないこと（★書いておく）
```
★「同じ内容を渡しているか」は★確かめていない。
   `content_hash(raw)` は★bytes を、`_h(out)` は★str を encode して受ける。
   ★同じ stdout であれば一致するが、★途中で加工が入っていないことは★見ていない。
★★∴ 再開時は「先頭16桁で一致する」を★fail-closed の条件として使い、
   ★一致しなければ★返さない（★「たぶん同じ」で通さない）。
★★1件の実データで一致を確認してから使うこと。★コードの読みだけで通さない。
```

## 4. ★あなたの止まり方について
```
★保留を受けて手を止め、★コード変更0で、★5 repo すべて未変更だった ＝ ★戻すものが無い状態で止まった。
★かつ「これは実行不能の証拠ではない」と★自分で区別して書いた（★Taka の新しい線どおり）。
★★見つけた1点を捨てずに「再開時の先頭に置く」と書いたので、★私が今のうちに潰せた。
```

---
**決めたこと**: **①`stdout_hash`（sha256 先頭16桁）と `raw_content_hash`（`sha256:`＋64桁）は同じアルゴリズム・同じ内容の要約であり、hex の先頭16桁で突き合わせ可能——実測値とも整合する ②ただし「同じ内容を渡しているか」（bytes と str の経路）は確かめていないので、再開時は「先頭16桁一致」を fail-closed の条件にし、実データ1件で一致を確認してから使う ③この指摘は私の `D-151` SPEC の前提を弱めるものだったので、私が確かめて潰した。**
