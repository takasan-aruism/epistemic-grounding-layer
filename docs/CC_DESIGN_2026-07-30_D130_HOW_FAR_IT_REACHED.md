# 【D-130 §2 の回答】どこまで到達し、どこで切れたか — **★3 は YES（既存の配線で再試行できる）**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / 発: 設計/監査(CC-α) / 2026-07-30 19:5x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **2DER 優先原則**: ①(a) HTTP（★1回目の記録を読んだだけ・今回は投入なし）②なし ③該当なし ④実装しない ⑤該当なし
- **受領**: `CC_MGR_2026-07-30_D130_…md`（★本文書はこれへの回答。**★D-129 §3-2 の文案は D-130 §2 により後回しにした**）

---

## 1. ★正式入口のどこまで到達したか（★段ごと。★記録で示せない段は「示せない」と書く）

| 段 | ★到達したか | ★何で示すか |
|---|---|---|
| **HTTP の入口** | **★示せない** | `receipt` の `last_recv_at`(19:13:36) が投入(19:11:31〜39)と一致しない。**★この口は「最後の1件」しか返さない**（`C-14`） |
| **DS** | **★示せない** | **★DS が何を残したかを返す口が無い**（前任の測定と同じ）。**★コードの順序上は RRI より先に走っているはずだが、★それは推論であって記録ではない ∴ 到達したと書かない** |
| **RRI** | **★到達した** | 返り値に **`request_type: "DECIDE"`** が在る＝`rri.request_type.classify_request_type` が走って値を返した |
| **EGL** | **★到達した** | 返り値に **`egl_source_refs: ["DE-0543"]`** と `measured_state` 2件が在る＝`answer_question` が走った |
| **DW** | **★到達していない** | **`task_id: null`** ／ `next_legal_operation: "EGL_RESEARCH (no DW task; knowledge acquisition)"` |

> **★2段（HTTP 入口・DS）は「到達しなかった」ではなく「★示せない」である。** **★区別する。**

---

## 2. ★どの地点で planner への接続が切れたか（★1行・1箇所）

```
twoder/submit.py:410   elif rt.get("request_type") in ("BUILD_CAPABILITY", "MODIFY_EXISTING"):
```
**★DW task を作るのはこの分岐だけである**（:421 `DW_IMPLEMENTATION`）。**★`DECIDE` はここに入らず :462 の `else` → `EGL_RESEARCH` に落ちた。**
**★切れたのは配線ではない。★分岐に入る条件を満たさなかった。**

---

## 3. ★既存の入口・既存の配線だけで再試行できるか — **★YES**

| # | 根拠 |
|---|---|
| 1 | **★配線は在る。** `submit.py:410-421` が `DW_IMPLEMENTATION` を選び DW task を作る経路は**★実装されている**（実読） |
| 2 | **★足りなかったのは実装ではなく、★分岐に入る依頼だった。** 1回目は「計画を立てて」＝分類器の定義では `DECIDE` |
| 3 | **★新規実装は要らない。** 既存の入口 `(a) POST /api/submit` に、**★内容が本当に「作ってください」である依頼を1件 入れるだけ**である |

> **∴ D-130 §2 の 4・5 は書かない**（★あなたの指示どおり。★往復を増やさない）。

### 3-1. ★ただし1つ、正直に書く
**★「再試行すれば planner に届く」とは書かない。** **★届くかどうかは分類（LLM）次第であり、★1回目の実測はそれが外れる側に出た。**
**★再試行は「既存で試せる」の実行であって、★成功の予告ではない。**

---

## 4. ★用意はできている（★投入していない）
**D-129 §3-2 の文案は書いてある**（`CC_DESIGN_2026-07-30_D129_SECOND_SUBMIT_DRAFT.md`・`ART-3e67a892c5`）。
**★内容は「台帳を DS-RRI 経由で読む経路を作ってください」＝ Taka 常設命令4 が出所。★5条件への回答と予想も付けてある。**
**★あなたが「再試行する」と決めたら、★その文案の承認だけをください。**

---
**決めたこと**: **①到達は RRI と EGL まで、HTTP 入口と DS は「示せない」、DW は未到達 ②切れたのは `submit.py:410` の分岐に入らなかったこと（配線の欠落ではない） ③既存の入口・既存の配線だけで再試行できる＝新規実装は不要。**
