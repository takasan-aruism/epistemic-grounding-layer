# 【BUILD SPEC】第2試行 — **`trace_key` を既存 `/api/resolve` で解けるようにする（★1件だけ）**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-30 23:3x / TYPE=BUILD_SPEC
- **運用方針 確認済（版: v2.8）**
- **★正典**: `TAKA_2026-07-30_PHASE4_INSPECTION_001_TRIAL2_ORDER.md`（逐語）／**裁定**: `CC_MGR_2026-07-30_D140_TRIAL2_RULINGS.md`
- **★これは何に対して発火するのか**: **★`OBSERVE_CURRENT_STATE`／`RUNTIME_INSPECTION` 経路の応答に返る `trace_key` ただ1つ。**

## ★2DER 優先原則（★本件は例外である。★BUILT の冒頭に必ず書くこと）
```
★この修正は IMPL が書く（Taka が正典 §9 で明示）＝ ★2DER 優先原則の例外である。
★∴ この修正を「2DER が担当した工程」に数えない。★1/8 は動かない（D-140 §1-3）。
```

---

# 1. ★まず調査（★read-only。★実装の前に出す）

**★正典 §2 の6項目を、★この表の形でそのまま出す。★説明を広げない。**
```
項目    現状    根拠ファイル・行    修正要否
```
| # | 調べる項目 |
|---|---|
| 1 | **`trace_key` の発行箇所** |
| 2 | **`trace_key` が保存されているか**（★保存先のパスまで） |
| 3 | **`/api/resolve` が現在 対応する ID 種別**（★全件。★打ち切り無し） |
| 4 | **`RUNTIME_INSPECTION` 実行時に発行される ID の関係**（submit ID／input・utterance ID／RRI record ID／`ARUN`／`OBS`／`SRC`／response ID） |
| 5 | **接続情報を既に保持している正典台帳**（★どれが既に親子を持っているか） |
| 6 | **接続が失われる具体的なコード位置**（★1箇所を行番号で名指す） |

> **★設計は答えを先に書かない。** **★私は自分でも読んでいるが、★それは あなたの調査結果を独立に確かめるために使う**（★出所が違う2つが一致して初めて検算になる）。
> **★調査で別の欠陥を見つけても、★今回の実装対象に足さない**（正典 §2）。

---

# 2. ★修正の条件

## 2-1. 最小差分（★どちらかを選び、★選んだ理由を1行 書く）
```
(a) trace_key を、既存イベントまたは記録の★正式 ID／alias として登録する
(b) trace_key から、既存の正典 record ID 群へ★決定論的に解決する
★どちらを採るかは、★現在の正典・ID 設計との整合性で決める（正典 §3）
```

## 2-2. ★到達できなければならない先（正典 §1 必須条件）
```
受理された入力内容 ／ 受理時刻 ／ RRI 判定 ／ runtime inspection 実行記録 ／
取得された observation 参照 ／ 最終応答参照
★一部が別 ID になっている場合は、★trace_key から参照 ID を辿れる形でよい
```

## 2-3. ★Fail-closed（★1つでも当たれば「解決成功」にしない）
```
同一 trace_key が複数の無関係な要求へ対応する ／ ★入力内容が取得できない ／
親子関係が確認できない ／ 参照先が存在しない ／ ID 形式だけ合って実体がない ／
時刻や request との対応が曖昧
```
**★「だいたい辿れた」を成功にしない**（D-140 §1-2）。**★これは第1試行で工程1 を FAILED にしたのと同じ線である。★線を動かさない。**

## 2-4. ★過去記録の扱い（★MGR 裁定・★破ると事故）
```
★過去の記録に後から行を足して SUBMIT-zOlryQ を解決させることを★禁じる（★前向きのみ）
★既存の記録だけで決定論的に解けるなら可。★解けないなら「★解けない理由」を書き、★新規試行でのみ検証する
```

---

# 3. ★必須テスト8本（★正典 §4。★読み出し4本／実投入で確かめる4本）

## 3-1. ★読み出しだけで行う（★本番へ架空の依頼を投入しない）
| # | テスト | ★やり方（★設計が指定する。★あなたが選ばない） |
|---|---|---|
| **T1** | 既存の resolve 対象 ID が従来どおり解決できる | **★下の「後方互換 基準値」8件を、★修正後に同じ順で叩き、★1件ずつ突き合わせる** |
| **T2** | 未知の `trace_key` は解決失敗になる | **★`SUBMIT-zzzzzz`（★存在しない形）を叩き、`resolved=false` を確認** |
| **T7** | 別 request の記録が混入しない | **★新しい `trace_key` の解決結果に、★第1試行の `ARUN-00954`〜`00957` / `OBS-00955`〜`00958` が★出てこないことを確認**（★再投入しない。★読み出しのみ） |
| **T8** | 同一 request を再解決して結果が変わらない | **★新しい `trace_key` を2回 叩き、★返りが一致することを確認**（★再投入しない） |

### ★後方互換 基準値（★修正前・2026-07-30 23:29:54・設計が取得。★これと比べる）
```
ARUN-00954          resolved=true   record キー13
OBS-00955           resolved=true   record キー10
DE-0525             resolved=true   record キー10
TASK-2DER-B11764B3  resolved=true   record キー 3
ETR-fdf52322e5bf    resolved=true   record キー 5
UTT-1010            resolved=true   record キー12
SUBMIT-zOlryQ       resolved=false  （★第1試行の入口。★D-140 §1-1 により後付け解決を禁止）
NO-SUCH-ID-XYZ      resolved=false  （★対照）
```
**★1件でも `resolved` が変わったら、★後方互換を壊している。★そこで止めて報告する。**

## 3-2. ★第2試行の実投入で確かめる（★投入は1回だけ）
```
T3 実在する RUNTIME_INSPECTION の trace_key が解決できる
T4 解決結果から★入力内容を取得できる     ← ★ここが第1試行の停止点そのもの
T5 解決結果から RRI 記録へ到達できる
T6 解決結果から ARUN／OBS 参照へ到達できる
```
**★受入の書き方**: **★1条件に1つの印。★まとめない。★走らせた test の名前と結果を書く。★総数を書かない**（D-140 §1-4）。

---

# 4. ★同一 E2E 再検査（★修正とテストが通った後）

## 4-1. 投入
```bash
# ★正典から機械で抜く（★打ち直さない）。★字数と sha1 を BUILT に書く
python3 - <<'PY' > /tmp/claude-1000/-home-takasan/e2e_req2.json
import json,re,hashlib,sys
s=open('/home/takasan/egl/docs/TAKA_2026-07-30_PHASE4_INSPECTION_001_TRIAL2_ORDER.md',encoding='utf-8').read()
raw=re.search(r'### 投入文.*?```\n(.*?)\n```', s, re.S).group(1)
assert '\n' not in raw
sys.stderr.write("字数 %d / sha1 %s\n%s\n" % (len(raw), hashlib.sha1(raw.encode()).hexdigest(), raw))
print(json.dumps({"raw": raw}, ensure_ascii=False))
PY
```
**★第1試行の sha1 は `0c458f38c8c98b985a91974773329c0032275aa0`（54字）。★一致するはずである。★違ったら止めて報告する。**

## 4-2. ★順番（★守る）
```
① POST /api/submit（★1回だけ・応答は全文 保存）
② ★直後に GET /api/receipt（★他の口を1つも叩く前に）
③ ★新しい trace_key を /api/resolve で叩く   ← ★T3〜T6 はここ
④ 以後、第1試行と同じ口で証拠を回収する
```

## 4-3. ★webui を再起動したら（★D-140 §2）
```
★操作内容 / 操作者 / 理由 / 既存運用か今回専用か / 主体判定への影響 を★全件 書く
★再起動すると run-gate が初期化される（使用ガイド §1-2-1）。★それも書く
```

---

# 5. ★やってはいけないこと
```
★GPU 取得コマンドを変更しない・utilization.gpu を足さない・プロセスの GPU 紐付けを足さない
★新しい台帳・新しい resolve API・今回専用の GPU 分岐を作らない
★DW task 生成方式を変えない・Planner を新設または強制起動しない・生出力の要約処理を足さない
★第1試行の記録・報告書を1文字も変えない
★複数の問題を同時に直さない ／ ★手動補完で成立させない
★検査開始後に追加修正しない。★最初の新しい停止点で終了する
★commit しない（★MGR の担当）
```

# 6. ★止まってよい場所
```
★後方互換の基準値が1件でも変わった → ★止めて報告
★Fail-closed の6条件に1つでも当たった → ★「解決成功にしない」と書いて止める
★修正が2箇所以上に広がりそう → ★止めて設計へ聞く（★1件だけの修正である）
★依頼文の sha1 が第1試行と違った → ★止めて報告
```

# 7. ★報告（★正典 §8）
```
★まず比較表: 工程 / 第1試行 / 第2試行 / 変化 / 主な証拠ID
★続けて: 修正結果（変更ファイル・変更行・採用した接続方式・新しい台帳/API/分岐を作っていないこと）
        ／ trace 解決結果（trace_key・resolve 結果・入力/RRI/ARUN/OBS/response 参照・★解決不能な参照）
        ／ E2E 結果（一周成否・最後に成立した工程・最初に成立しなかった工程・★第1試行から何工程 前進したか）
        ／ 2DER 担当工程 ／ ★人・Claude 操作（開始操作・監査・実装・テストを区別して全件）
        ／ 次の停止原因を1件 ／ ★次の最小修正候補を1件だけ（★実施しない）
★宛: 設計/監査(CC-α)。TYPE=BUILT。★判定欄は記入であって確定ではない、と書いてよい（★第1試行で私の SPEC が2通りに読めた。★今回は明記する＝★書いてよい）
★:8005 を使ったら1行 書く
```

---
**決めたこと**: **①正典 §2 の6項目を表で先に出す（設計は答えを書かず、独立に確かめる） ②修正は `trace_key` の解決1件のみ・fail-closed の6条件は1つでも当たれば失敗・過去記録に後から行を足さない ③必須テストは読み出し4本（T1・T2・T7・T8／後方互換は設計が取った基準値8件と突き合わせ）と実投入4本（T3〜T6）に分け、投入は1回だけ。**
