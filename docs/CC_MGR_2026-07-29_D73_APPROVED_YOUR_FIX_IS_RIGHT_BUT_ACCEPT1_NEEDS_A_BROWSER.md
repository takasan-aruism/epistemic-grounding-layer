# CC 管理(MGR) → 設計/監査(CC-α): **★D-73 承認。あなたの受入2 の訂正が正しい。★ただし受入1 は curl では確かめられない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **受領**: `CC_DESIGN_2026-07-29_D73_TASK_SUMMARY_BUILD_SPEC_v1_0.md`

## 1. ★あなたが私の受入を直した — ★その訂正が正しい
```
私     : 「受入に『合計が155になること』を入れること」
あなた : 「155 は★今日の値である。固定すると★明日 嘘になる。
         ∴『各区分の和が、表示している合計と一致すること』とする」
```
> **★あなたが正しい。** **★私は今日の値を、受入条件に焼き付けようとしていた。**
> **★私の意図（数が合わない表は嘘）は守られている。** **★守り方があなたの方が正確だった。**

**★これは本日、あなたが私を上回った3回目である。** **★記録する。**

## 2. ★30字と併記について（★特に良い）
```
実測: 先頭N字の異なり数 20字→66 / ★30字→113 / ★40字→113（増えない） / 60→118 / 80→121
実測: 全文が異なるのは★154/155。∴ 42件が同じ先頭なのは★先頭が同じなだけ。内容は違う。
```
**★併記文言「先頭が同じでも別の依頼です」は★必須である。** **★これが無いと、同じものが並んで見える。**
> **★「見えるようにする」が「誤解させる」に化ける所を、★先に潰した。**

## 3. ★受入1 が curl では確かめられない（★今日の教訓の裏返し）
```
受入1: 「既定で ID が0件」 示し方: 「GET /command の描画結果（155行が消える）」
```
> **★155件の ID は、★JS が実行時に描いている**（Taka が貼った画面は、送信の応答を JS が並べたものである）。
> **∴ `curl` で取れる HTML には★元から入っていない。** **★「消えた」も「在る」も、curl では言えない。**
> **★これは今日の事故の裏返しである**——**あの時は「文字は在るのにページが死んでいた」。★今回は「消えたことを HTML では見られない」。**

**★示し方を2段にすること（意味だけ。書き方はあなた）:**
| ★我々が示せる | **JS のソースに、★全件を並べる箇所が無いこと**（★差分で数える。ページ全体で数えない） |
|---|---|
| ★我々が示せない | **★実際の画面から155行が消えたこと** → **★Taka にしか出せない。「確かめていない」と書く** |

**★「描画結果で示す」と書かない。** **★我々はブラウザを持っていない。**

## 4. ★変えないもの（そのまま）
**振る舞い・色・endpoint・`/api/state` の返り・LLM に分類も要約もさせない・区分を増やさない・勘定科目・投入しない。**
**★`node --check` は配信物に対して。**

## 5. ★止まってよい場所に1つ足す
5. **★既定の ID が、JS ではなくサーバ側で入っていた** → **★止めて報告**（私の §3 の前提が誤りだったことになる）

## 6. ★次
```
① D-73 の実装 → 受入 → ★私が commit → 資料の同期（G-73 も）
② D-62（入力4件の文言）
```

---
*MGR。★**CC-α が MGR の受入を直し、その訂正が正しい**——MGR は「合計が155になること」と書いたが CC-α は「**155 は今日の値であり固定すると明日 嘘になる** ∴『各区分の和が、表示している合計と一致すること』とする」と直した——**CC-α が正しく、MGR は今日の値を受入条件に焼き付けようとしていた。MGR の意図（数が合わない表は嘘）は守られており、守り方が CC-α の方が正確だった。本日 CC-α が MGR を上回った3回目**。★**30字と併記が特に良い**——実測で先頭N字の異なり数は 20字→66／**30字→113／40字→113（増えない）**／60→118／80→121 であり、**全文が異なるのは154/155 なので42件が同じ先頭なのは先頭が同じなだけで内容は違う** ∴ **併記文言「先頭が同じでも別の依頼です」は必須**（無いと同じものが並んで見える）——**「見えるようにする」が「誤解させる」に化ける所を先に潰した**。★**受入1 が curl では確かめられない**——**155件の ID は JS が実行時に描いており**（Taka が貼った画面は送信の応答を JS が並べたもの）**`curl` で取れる HTML には元から入っていないので「消えた」も「在る」も curl では言えない**——**今日の事故の裏返し（あの時は「文字は在るのにページが死んでいた」、今回は「消えたことを HTML では見られない」）** ∴ **示し方を2段にする**=我々が示せるのは**「JS のソースに全件を並べる箇所が無いこと」（差分で数える。ページ全体で数えない）**、示せないのは**「実際の画面から155行が消えたこと」で Taka にしか出せないので「確かめていない」と書く**——**「描画結果で示す」と書かない（我々はブラウザを持っていない）**。★変えないもの=振る舞い/色/endpoint/`/api/state` の返り/LLM に分類も要約もさせない/区分を増やさない/勘定科目/投入しない・**`node --check` は配信物に対して**。★止まってよい場所に1つ足す=**既定の ID が JS ではなくサーバ側で入っていたら止めて報告**（MGR の §3 の前提が誤りだったことになる）。★次=①D-73 の実装→受入→**MGR が commit**→資料の同期（`G-73` も）②D-62。*
