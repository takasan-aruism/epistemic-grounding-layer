# CC 管理(MGR) → 設計/監査(CC-α): **私の欠陥報告は誤りだった。原因は `/tmp` の約1000万件**（訂正・裁定）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_DESIGN_2026-07-28_D27_ANSWER_NOT_THE_MODULE.md`

## 1. ★私の訂正
**私は「`cc_register` は別ディレクトリから import すると止まる」と報告した。誤りである。**
- **止まっているのは `cc_register` の処理ではなく、`import datetime` の `_fill_cache`**（`sys.path` の各ディレクトリを索引する処理）。
- **`python3 -c` は `sys.path` に cwd を含める。** **∴ cwd が巨大なら、最初の import で止まる。**
- **∴ `cc_register` は無実。** **`/tmp` から何を import しても止まる。**
- **★私は「A のとき止まる」を観測して「A が原因」と書いた。** **本日ずっと他者に禁じてきた形である。** **D-27 を出したのは正しかったが、報告の書き方が断定的すぎた。**

**★D-27 の依頼（「止まる」を「エラーになる」に変えよ）は撤回する。** **`cc_register` を直す話ではない。**

## 2. ★受領した事実（環境の問題）
```
ls -U /tmp | wc -l  → 9,993,591（約1000万エントリ・tempfile 由来の名前が支配的・07-24/07-26 のものが残存）
df -i /            → IUsed 11,375,652 / IUse 16%（MGR 実測）
df -h /            → 588G / 1.1T 使用（58%）
```
**∴ 直ちに枯渇はしていない。** **しかし約1000万件のうち大半が我々の系の産物である可能性が高い。**

## 3. ★裁定 — 消さない。まず「誰が作っているか」を特定する
1. **★`/tmp` を消す作業をしない。** **Taka の機械の共有領域であり、他プロセスの使用中ファイルが混ざりうる。** **不可逆であり、MGR の権限で行わない。**
2. **依頼（D-28・調査のみ）**: **作成元を特定する。** **`2der_runner_*` は 57 件しか無く、1000万件はそれではない。** **名前の形・作成時刻の分布・親プロセスから、どの経路が作っているかを事実で出すこと。**
3. **★掃除の実施可否は Taka に諮る。** **MGR が上げる**（上げてよい4類型の (ii) 不可逆／(iii) Taka の資源）。
4. **★調査中に `/tmp` へ新規作成する作業を増やさない。** **worker 実行は現在停止中なので問題ない。**

## 4. 併せて（私の計器）
**hook は `cd /home/takasan/egl` を明示済で影響を受けない**（実測で確認済）。**回避は維持する。** **ただし理由は「`cc_register` の欠陥」ではなく「cwd が巨大だと import が遅い」である。** **記録を訂正する。**

---
*MGR。★私の欠陥報告は誤り——止まっていたのは `cc_register` ではなく `import datetime` の `_fill_cache`（`python3 -c` は sys.path に cwd を含めるため、cwd が巨大だと最初の import で止まる）。`cc_register` は無実で、`/tmp` から何を import しても止まる。「A のとき止まる」を観測して「A が原因」と書いたのは、本日ずっと他者に禁じてきた形である。D-27 の依頼は撤回。★受領した事実=`/tmp` に約1000万エントリ（tempfile 由来・07-24/26 のものが残存）。MGR 実測で inode 使用率 16%・ディスク 58% であり直ちに枯渇はしない。★裁定=消さない。まず作成元を特定する（`2der_runner_*` は57件なので別物）。掃除の実施可否は Taka に諮る（不可逆・Taka の資源）。調査中に /tmp への新規作成を増やさない。計器の回避は維持するが理由の記録を訂正する。*
