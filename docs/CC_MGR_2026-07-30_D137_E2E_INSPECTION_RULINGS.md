# 【D-137】E2E 検査 — **裁定と手番。★事前状態は本文書に保存した**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-30 22:2x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）**
- **★正典**: `TAKA_2026-07-30_PHASE4_E2E_INSPECTION_ORDER.md`（★逐語・全文）。**★本文書はその裁定であって、指示の言い換えではない。**
- **2DER 優先原則**: ①**★この検査そのものが「まず 2DER を使う」である** ②③④⑤ は**★検査結果に書く**

---

## 1. ★曖昧な点への裁定（★検査前に固定する。★測ってから動かさない）

| # | 論点 | ★裁定 |
|---|---|---|
| **1** | **開始操作の範囲** | **★`POST /api/submit` と `POST /api/run_next` は「不可避な開始操作」として認める。★ただし押した回数を全部 記録し、★工程「実行」の actor に 2DER を入れない。**（★2DER が自分で次へ進んだ事実が在れば、そのときだけ別に書く） |
| **2** | **★GPU の外部実測** | **★我々は `nvidia-smi` 等を叩かない。** 要約の一致は **★2DER 自身の生出力と突き合わせて**確かめる。**★禁止事項「手動での GPU 情報取得と結果注入」を、★狭くではなく広く読む** |
| **3** | **証拠回収の道具** | **★既存の口を叩いて記録するだけ。★スクリプトをファイルとして作らない**（★コマンドを打つのは可）。**★「補助スクリプトを追加しない」に当たる** |
| **4** | **止まったとき** | **★その場で終了。★直さない・迂回しない・再投入しない。★自動 retry が既存仕様として動いた場合のみ、回数と各試行結果を記録する** |
| **5** | **判定の主体** | **★工程ごとの判定案は設計が出す。★確定は MGR。★設計は自分の判定を自分で承認しない** |
| **6** | **★工程名** | **★実装上の正式名称に置き換えてよい**（Taka 明示）。**★存在しない工程を成立扱いしない。★無いなら「無い」と書く** |
| **7** | **★事後の外部照合** | **★検査を閉じた後に、GPU の実測を別途 取って照合するかは MGR が決める。★検査の中ではやらない**（★やるなら「検査後の別作業」と明記する） |

---

## 2. ★事前状態（★Claude Code の役割1。★MGR が保存した）

```
保存時刻: 2026-07-30T22:22:37+0900
5 repo HEAD:
  ds           main   6f93f8a  dirty=0
  rri          main   564d0f4  dirty=0
  egl          main   3c0abcd  dirty=0
  dev-workcell master 450566f  dirty=0
  twoder       master 760820c  dirty=0
稼働プロセス（★絶対パス照合・自分と先祖を除外＝§4-15）:
  700957   2026-07-26 03:54:10 起動  /usr/local/bin/vllm serve --served-model-name Qwen3.6-35B-A3B …
  3814663  2026-07-30 12:44:07 起動  python3 -m twoder.webui 8770
資料: component=23 / edge=11 / gap=83 ／ CC_REGISTER doc_rows=566
```
**★検査後に同じ形で取り直し、差分を報告に付けること。**

---

## 3. ★手番（★Claude Code の3インスタンスは全員 作業主体から降りる）

| 誰 | ★やること | ★やらないこと |
|---|---|---|
| **設計(CC-α)** | **★証拠回収の設計**（各工程で「何を証拠にするか」を先に決める）／**★IMPL の BUILT を監査**／**★工程ごとの判定案** | **★実行しない。★判定を確定しない** |
| **IMPL** | **★投入1回**（正典の依頼文を1文字も変えず）／**★`run_next` を押す（★回数を全部 記録）**／**★既存の口から証拠を回収** | **★計画・コマンド選択・修正・整形・検証・台帳登録・DW入力・要約の補筆を一切しない。★止まったら直さない** |
| **MGR** | **★事前/事後の状態保存**／**★actor 判定と裁定の確定**／**★報告書**／**★5 repo の確認と commit** | **★実装しない。★数字を自分で出さない** |

**★証拠が無い工程は `UNKNOWN` にする。★「できるはず」を書かない。**

---

## 4. ★先に決めておく（★測ってから決めない）

```
★判定区分は正典の6つだけを使う。★中間の言い方（「概ね成功」等）を作らない。
★「一周成立」でなくても、★最初の停止工程を証拠付きで確定できれば★完了である（Taka 明示・B）。
★停止したら、★次の最小修正候補を1件だけ挙げる。★検査中に実施しない。★2件以上 挙げない。
★複数の問題をまとめて直す提案をしない。
```

## 4-1. ★特に注意する2点（★今日の実測から）
```
★(a) 工程「実行」は、★人が run_next を押している限り 2DER の担当にならない
      （★D-136 で既に判明している形）。★それが出ても「失敗」ではない。★そう書けばよい。
★(b) 返ってきた要約が、★投入文の言い換えに過ぎない場合が在る
      （★1回目の投入で実際に起きた）。★そのときは「DW が最後の手入力文だけを要約した」と★正典の言い方で書く。
```

---

## 5. 手番
```
★設計: 証拠回収の設計を1本 → IMPL へ（★SPEC は設計が書く。★私は書かない）
★私  : 検査後に判定を確定し、報告書を作る
★Taka: ★手番なし（★報告まで待つ）
```

---
**決めたこと**: **①`submit`/`run_next` は開始操作として認めるが回数を全部記録し工程「実行」の actor に 2DER を入れない ②我々は GPU を自分で測らない（要約は 2DER 自身の生出力と突き合わせる） ③証拠回収でスクリプトをファイルとして作らない ④止まったらその場で終了・直さない ⑤事前状態は本文書に保存した。**
