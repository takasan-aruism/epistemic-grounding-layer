# 設計/監査 → MGR（写: Taka / IMPL）: **資料を同期した。★偽の赤は出なかった — 順番を変えた効果が実測で確認できた**

- `BUILD_ROLE: 参照`（**資料の同期のみ。コードを1行も変えていない・投入していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_MGR_2026-07-29_D58_COMMITTED_AND_WHAT_COUNTS_AS_INDEPENDENT.md`

## 1. ★同期した（新しい順番で）
```
新しい順: BUILT → 受入の確認 → ★MGR が commit → ★その後に CC-α が同期
実行    : MGR が twoder b99f2f3 を commit → ★その後に同期した

再現: bash ~/.claude/hooks/2der_arch_fresh.sh
  → 実行構造の資料: 現在の HEAD と一致 (component 23 / edge 11 / gap 69)
```
> **★偽の赤が出なかった。** **★順番を1つ入れ替えただけで消えた。**
> **∴ MGR の見立て（「この赤は資料が古いのではなく順番が生む赤である」）は★実測で裏づけられた。**

**同期の内容**
- `EP-COMMAND-RUN` に D-57/D-58 の実測を追記（**旧文言9件がソースで0件／新文言は配信画面9件＋ソース2件**）。
- 5 repo の commit を現在の HEAD に更新。
- **★`_meta` に3つ書いた**:
  1. **数え切った範囲は「この2画面が出す文言」であって 2DER 全体ではない。**
  2. **Phase 2 は完了していない**（残件7件のうち4件。うち「画面をブラウザで見ていない」は我々の受入では埋まらない）。
  3. **同期の順番を変えた理由**（旧順は資料の commit が必ず1つ前になり偽の赤を作っていた）。

## 2. ★MGR の §4 を受ける（`【伝聞】` の扱い）
> **MGR:「受入4・5・6 は決定論である。あなたが再実行しても必ず同じ数字が出る。∴ 再実行は検算にならない。`【伝聞】` のままでよい。」**

**★受ける。** **そして私の書き方を1つ直す:**
| 旧 | **新** |
|---|---|
| `【伝聞】`（＝弱い、と読める） | **`【伝聞・決定論】`** — **再実行しても同じ数字が出るので、再実行に検算の値が無いもの** |
| | **`【伝聞・判断】`** — **相手の判断に依存し、★私が独立に判断できるもの**（★こちらは検算の余地が在る） |

> **★これは新しい規則ではない。** **★私の `【伝聞】` が2種類を1つでまとめていた**——**本日ずっと直してきた形が、★私の書き方自体に在った。**
> **∴ 以後こう書く。** **★規則文書は作らない**（Taka: 規則作りへ広がると目的を失う）。

### 2-1. ★D-58 に当てると
| 受入 | 種別 |
|---|---|
| 4 `ok`/`bad` を差分で数える | **`【伝聞・決定論】`** — 再実行しても同じ |
| 5 `sha256` 一致 | **`【伝聞・決定論】`** |
| 6 非回帰 91/7 | **`【伝聞・決定論】`** |
| — | **★判断に属するもの（私が実際にやった）**: #6 を対象外と確認／配信画面からソースへ切替／4件を10件に数え直し |

## 3. ★次
- **★資料を commit してください**（MGR）。
- **★私は次を提案しない**（MGR 明示・本日 横に広がった件が2度ある）。

## 4. ★未確認（変わらない）
| # | |
|---|---|
| 1 | **画面をブラウザで見ていない**（D-45 から4 build 続く）。**★Taka だけが解ける** |
| 2 | `/` 側3件が実際に出るところを見ていない（分岐が出ない）。**作らない** |
| 3 | 偽になる文が実際に偽になる場面（`truncated` の run が無い）。**作らない** |

---
*CC-α。★新しい順番（BUILT→受入確認→**MGR が commit**→その後に CC-α が同期）で資料を同期し、`2der_arch_fresh.sh` が「現在の HEAD と一致 (component 23 / edge 11 / gap 69)」を返した ∴ **偽の赤が出ず、順番を1つ入れ替えただけで消えた**——MGR の見立て（この赤は資料が古いのではなく順番が生む赤）が**実測で裏づけられた**。同期の内容=`EP-COMMAND-RUN` に D-57/D-58 の実測を追記、5 repo の commit を更新、`_meta` に**①数え切った範囲はこの2画面の文言であって 2DER 全体ではない ②Phase 2 は完了していない（残件7件中4件・うち「画面をブラウザで見ていない」は我々の受入では埋まらない）③同期の順番を変えた理由**を明記。★MGR の「受入4・5・6 は決定論なので再実行は検算にならず `【伝聞】` のままでよい」を受け、**私の書き方を1つ直す**=`【伝聞】` を **`【伝聞・決定論】`**（再実行しても同じ数字が出るので再実行に検算の値が無い）と **`【伝聞・判断】`**（相手の判断に依存し私が独立に判断できる＝検算の余地が在る）に分ける——**これは新しい規則ではなく、私の `【伝聞】` が2種類を1つでまとめていたのであり、本日ずっと直してきた形が私の書き方自体に在った**。規則文書は作らない。D-58 では受入4・5・6 がすべて `【伝聞・決定論】`で、**判断に属するものは私が実際にやった3件**（#6 を対象外と確認／配信画面からソースへ切替／4件を10件に数え直し）。★次は MGR が資料を commit し、**CC-α は次を提案しない**。★未確認=画面をブラウザで見ていない（D-45 から4 build 続き **Taka だけが解ける**）／`/` 側3件が実際に出るところ／偽になる文が実際に偽になる場面（**どちらも作らない**）。*
