# 【BUILT】D-158 — **★値は prompt に入った。★本文には届かず、★受入3 が ○→× に反転した**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: MGR / Taka / 発: 実装(IMPL) / 2026-07-31 09:3x / TYPE=BUILT
- **運用方針 確認済（版: v2.8）** ／ **実装源**: `CC_DESIGN_2026-07-31_D158_BUILD_SPEC_PASS_THE_VALUES.md`（依頼文は `D-159`）
- **`:8005` が呼ばれた**（★私は0件。**投入4件／`run_next` 3件＝`attempts=3` と一致**）

---

# 1. ★変更した1箇所
```
twoder/submit.py        1行  ： provenance に measured_state（既に取れている値）を載せる
twoder/build_planner.py 数行 ： _observation_facts が その値も prompt に出す
合計 3 hunk ／ 13挿入・3削除 ／ ★禁止語 gpu・nvidia・port・docker は diff に ★0件
```
> **★迷いを1行（狭い方を採った）**: **「1箇所」だが、値を★載せる所（`submit.py`）と★読む所（`build_planner.py`）は別ファイルであり、★1ファイルでは成立しない**（`knowledge_packet` に値が無いことが届かない理由だと設計が特定している）。**★各ファイル最小差分（1行＋数行）を「1つの経路」と読んで進んだ。**

**★後方互換**: 基準値**11件 不変**。値も参照も無ければ**空文字＝従来と同じ prompt**（単体で確認）。

## 1-1. ★値が prompt に入ったことの確認（★実行して確かめた）
```
packet の provenance.measured_state = ★21件（例: "GPU 0 memory used = 30520 / 32607 MiB…"）
_plan_prompt(...) に "ALREADY ACQUIRED"      → ★含む
_plan_prompt(...) に "VALUES ALREADY OBSERVED" → ★含む   ／ 値の行 ★25行 ／ prompt 3526字
★∴ 「見せていない」ではない。★見せた上での結果である。
```

---

# 2. ★受入（★1条件に1つの印）

| # | 受入 | 印 | 実測 |
|---|---|---|---|
| 1 | `cites_source_ids` が空でない | **○** | `['OBS-00983','OBS-00984','OBS-00985','OBS-00986']` |
| 2 | 対象 Observation の ID を引用する | **○** | この投入の `egl.source_refs` と完全一致 |
| **3** | 既に観測済みの能力を、新規作成対象として重複計画しない | **★×（前回 ○ から反転）** | `steps` = **"Implement process RSS collection using subprocess to run ps command." / "Implement GPU memory collection using subprocess to run nvidia-smi." / "Implement Docker container status collection using subprocess."** ＝**★既に取得済みの3つを、そのまま実装対象にしている** |
| **4** | 証拠から不足が判定された場合だけ改修 Task を作る | **★×** | `requirement` は「無ければ warning を出して null を入れる」という**★一般的な堅牢化**。**★観測の値に基づく「何が取れていないか」は無い**。`unresolved_assumptions` も OS/Python/権限の一般前提のみ |
| **★補** | 値が本文に届いたか | **★×** | 観測値から取った具体トークン**24個**のうち本文に出たのは **`container`・`memory` の2語だけ**（★どちらも一般語）。**`VLLM` 0／`rss`(小文字) 0／`703029` 0／`30520` 0／`qwen36` 0／`pid` 0／`observed` 0／「観測」0／「既に」0**（★英語・日本語の両方で数えた） |
| 補 | 後方互換 | **○** | 11件 不変 |
| 補 | 1箇所・語の禁止 | **○** | 3 hunk／13挿入3削除／禁止語 0件 |

---

# 3. Last PASS
> **★受入2**（ID の引用は今回も正しい）。

# 4. First FAIL
> **★受入3。★前回 ○ だったものが × に戻った。**

## 4-1. ★言い方（★`D-159` が先に決めた形のまま）
```
★受入4 が立たなかった → 「★値を見せても効かなかった」
★補 が立たなかった    → 「★値も届かなかった」。★設計の予告は★2回続けて外れた
★★さらに今回は「効かなかった」では足りない。★受入3 が ○→× へ★反転した。★悪化と書く。
```

## 4-2. ★ただし断定しない（★1回の観測・★対照が無い）
```
★依頼文が前回と違う（前回=コンテナ／今回=メモリを多く使っているプロセス）。
∴ ★「値を見せたから 受入3 が壊れた」とは言えない。★同一条件の対照を取っていない。
★★仮説（★断定しない）: 観測ブロックが★4つの取得コマンドを列挙している ∴ 計画が
   それらを「実装すべきもの」として並べた可能性が在る（★今回 並んだ3つは、★ブロックに載せた命令と同じ3つ）。
★★対照が難しい理由も書く: ★同じ依頼文では task_id が同じで CREATE 確定済 ∴ ★同じ文で再計画できない。
```

---

# 5. ★予告の当否
| # | 予告 | 結果 |
|---|---|---|
| **Q-1** | 分類は `OBSERVE_CURRENT_STATE` | **★当たり** |
| **Q-2** | 補（値が本文に出る）は立つ | **★外れた**（★設計の予告は2回連続 外れ） |
| **Q-3** | 受入4 が立つとは予告しない | **★立たなかった** |
| **Q-4** | 観測は `top_memory_processes` が主に効く | **★判定できない**（★4種とも走り、計画は3種を等しく並べた。★「主に効く」を測る物差しが無い） |
| 私 | `TASK-2DER-5ABBA4A1` | **★当たり**（63字 / sha1 `5abba4a1…`・機械抽出） |

# 6. ★2DER 担当工程数の前回差分
```
★0
```

# 7. ★次に直す1件（★候補・★実施しない・★採否は設計）
> **★観測ブロックから「取得コマンドの列挙」を外し、★値だけを見せる。**
> **理由**: **★4-2 の仮説を検証できる唯一の1手である**（★命令を見せることが重複計画を誘発しているなら、外せば止まる。★止まらなければ仮説が外れたと分かる）。
> **★私は「効く」と予告しない。** **★どちらに転んでも結果になる形として出す。**

---
*IMPL → 設計/監査（写: MGR / Taka）。D-158＝観測の値を planner に渡す修正（`submit.py` 1行で `provenance.measured_state` を載せ、`build_planner.py` 数行で prompt に出す／3 hunk・13挿入3削除・**禁止語 `gpu`/`nvidia`/`port`/`docker` は 0件**・後方互換11件 不変）。**値が prompt に入ったことは実行して確認済み（`VALUES ALREADY OBSERVED` を含み値25行・prompt 3526字）＝「見せていない」ではない。** **受入は 1○ 2○ ★3×（前回 ○ から反転——`steps` が ps / nvidia-smi / docker の取得をそのまま実装対象にしている）／4×（一般的な堅牢化のみで、観測の値に基づく不足の判定が無い）／★補×（観測値の具体トークン24個のうち本文に出たのは一般語2つだけ。英語・日本語の両方で数えた）。** **Last PASS=受入2、First FAIL=受入3。`D-159` が先に決めた言い方どおり「値を見せても効かなかった」「値も届かなかった（設計の予告は2回連続 外れ）」と書き、さらに今回は★受入3 が反転したので「悪化」と書く。** **ただし依頼文が前回と違い対照が無いので「値を見せたから壊れた」とは断定しない**（仮説＝観測ブロックが取得コマンドを列挙していることが重複計画を誘発した可能性。並んだ3つはブロックに載せた命令と同じ3つ）。**同じ文では task_id が同じで再計画できないため対照が取りにくいことも記録する。** 予告は Q-1 当たり／Q-2 外れ／Q-4 判定できない。**2DER 担当工程数の差分は 0。GENERATE は押していない。commit していない。** 次の1件の候補は**観測ブロックから取得コマンドの列挙を外し、値だけを見せる**（仮説を検証できる唯一の1手・効くとは予告しない）。*
