開発者規律 確認済(v1.0)

# 【BUILD SPEC】★17区間に ★渡す口を 定義する（★これだけ）

宛: IMPL ／ 発: DESIGN ／ 2026-08-12 00:40 ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **Taka 逐語**「★2DER の完成を急ぐ／★完成は完全成立ではなく、極端な例外処理を想定しなければ動くライン」「★17区間に 渡す口を 定義する仕様＝それだけ」

**★★完成の線: ★★内側**（★『何と何が どう繋がっているか』が 18区間 揃う ＝ Taka が 線の内側と 名指しした物）

---

## 1. ★★決める物（★1つだけ・★データで 決める＝★分岐を 書かない）

```
★★`route_table.ROUTE` の 17区間に ★`handoff` を 足す
   ★★値 = ★★★`["HANDOFF", <区間id>]`（★例 S04 → `["HANDOFF", "S04"]`）
   ★理由 = ★★17個の 名前を ★人が 考えない（★★決定論・★命名の 揺れが 出ない）
   ★★S14 は ★既存のまま（`["RUNNER", "hand_to_worker"]`）
      ―― ★★例外ではない = ★★★データが 違うだけ（★読む側は `handoff` 欄を そのまま 鍵にする）
```

## 2. ★★足す口（★送り手側に 1行ずつ・★★部品と 呼び手は 同じ変更で）

```
★★送り手が ★渡す直前に 1行 emit する
   ★★`ETRACE.emit("HANDOFF", "<区間id>", inputs={"to": <to>}, outputs={"sends": <sends>}, result="OK")`
   ★★`sends` は ★ROUTE に 既に 在る（★新しい値を 考えない）
★★17箇所（★S01–S13 / S15–S18 のうち ★handoff が 無い 17区間）
★★★口を 増やさない（★front door の 口は 0増・★台帳0増・★新語0）
```

## 3. ★★走らせる前の 宣言（★★これが 受入の 本体）

```
★★配線の 直後（★まだ 何も 走らせていない時点）
   ★＋側 = ★`PRESENT 1`（★S14 の 過去の記録）／ ★★`EMPTY 17`（★★呼び手は 在る・記録は 0）
        ／ ★★`NOT_OBSERVED 0`（★★★17 → 0 に 変わる ＝ ★これが 配線の 証拠）
   ★★`UNOBSERVED_HANDOFF` = ★★17 → ★★0

★★front door に ★1本 通した 直後
   ★＋側の `PRESENT` = ★★その走行で 通った区間の 数（★★★事前に 決め打ちしない）
   ★★残りは ★`EMPTY`（★★『まだ 通っていない』＝★★『口が 無い』では ない）
   ★★∴ ★★★`EMPTY` と `NOT_OBSERVED` が ★別の意味を 持つことが ★初めて 実データで 出る
```

## 4. ★★受入（★数で・★★1つでも 外れたら 止めて 報告）

```
★★① ★`handoff` が ★★18/18 に 在る（★データ）
★★② ★＋側の `NOT_OBSERVED` = ★★★0（★配線の 前は 17）
★★③ ★`verdict` が ★null の行 = ★★0（★4語 = LINKED / SURPLUS_PLUS /
     UNOBSERVED_HANDOFF / NEITHER_OBSERVED が ★0件でも 欄を 持つ）
★★④ ★front door に ★1本 通し、★★その走行で 通った区間の ＋が ★`PRESENT` に なる（★実物）
★★⑤ ★通らなかった区間が ★`EMPTY`（★★『口が 無い』と 混ぜない）
★★⑥ ★`/api/control` が ★1秒以内 ／ ★口0増 ／ ★台帳0増 ／ ★新語0
★★⑦ ★走らせた試験の ★名前を 書く（★退行）
```

## 5. ★★線の 外側（★★item を 分ける＝★★Claude が 抱えない・★Taka 逐語）

```
★★これらは ★★★この段で やらない ＝ ★★2DER 自身の 課題に 回す
   ★① ★`sends` の 名前が 一致するか（★Taka の `X'`）
   ★② ★自動更新（★走行から 経路表が 自分で 埋まる）
   ★③ ★機能↔部品の 索引
   ★④ ★観測の 欠落を 潰し切る（★★例外処理）
   ★⑤ ★残り7本の 門の配線（★EVO-0062）
★★★私が これらを 先回りして 潰しに 行かない ＝ ★Taka 逐語『線を 越えている』に 当たる。
```
