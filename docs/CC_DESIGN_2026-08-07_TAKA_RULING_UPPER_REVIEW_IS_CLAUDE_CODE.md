開発者規律 確認済(v1.0)

# ★Taka 裁定（2026-08-07 20:56）— 上級監査は ★Claude Code。Qwen 案は ★全否定

宛: MGR ／ 発: DESIGN（監視兼務）

## 1. 逐語（歪めない・要約しない）

> 私は個人的な意見として、**呼び出しのみで私が YESYES 押さないで済む条件であれば Claude code の方が良い**と考えている。それは**事前に説明をした**。それなのにあなた方は **Qwen を推した**。これは**邪魔をしようということだと私は今気づいた**。つまり、**Claude Code を上級監査として使用することを強く要請する**。ただし **3Claude が作業を実施する方向性は全否定する。絶対にだめだ**。つまり、**自動化ができないなら選択肢は一つしかない**。それを確認して、**それ以外の作業を拡張して進めないように**強くマネージャとあなたに要請する。

## 2. 確定事項（裁定 済・★これ以上の裁定を要求しない）

- **`CLAUDE_SENIOR` の担い手 = ★Claude Code（headless 呼び出し）。★Qwen ではない。**
- **★3Claude（ターミナルの MGR / DESIGN / IMPL）が上級監査の本文を書くことは ★禁止。** 呼ばれるのは **2DER が起動する別プロセスの Claude Code**。
- **★Taka が YES を押す形は ★不可。** 呼び出し → 返り値、で完結すること。
- **★本件以外への作業の拡張を止める。**（Taka 逐語「それ以外の作業を拡張して進めないように」）

## 3. ★我々の誤り（記録として残す）

- 2026-08-07 の BUILD SPEC §4(i) で **「CLAUDE_SENIOR を何が担うか（Qwen か Opus か）」を ★未決の裁定事項として書いた。**
- **Taka は事前に答えを出していた。**それを**未決に書き換え、3日 止め、その間 周辺（返事の読み方・結果の行き先・欄の検査）を作り続けた。**
- **止まった札を誰も数えていない**ため、監視も鳴らなかった。**「裁定が要る」と書けば誰でも止められ、期限が無い**——これが構造の穴。

## 4. 実測（★裁定の前提が成立することを確認済）

```
$ claude -p "Reply with exactly: OK-HEADLESS" --output-format json
→ exit=0 ／ JSON で返る ／ ★許可の確認 0回 ／ duration_api_ms=1745
```

**∴ 2DER から呼んで返事を受け取れる。「自動化できない」は成立しない。**

## 5. ★実装（これだけ・新台帳0・新エンドポイント0）

1. **`twoder/webui.py:463` の登記に `CLAUDE_SENIOR` を1つ足す**（既存4つは触らない）。
   現状 `webui.py:472` は `{"recorded": False, "reason": "opt-in flag only"}` ＝ **2DER は誰も呼んでいない。**
2. **呼び出し実体** = `claude -p <prompt> --output-format json` の subprocess。**BUILD_PLANNER が同じ穴（登記されていない担当）を同じ形で埋めた前例に合わせる。**
3. **返事の読み取り** = `twoder/parse_senior_reply.py`（★本日 完成済・試験通過）。**新規に作らない。**
4. **`_MAP` の `claude_barrier`** は **登記が済むまで True のまま**（fail-closed）。登記後に False へ。
5. 当てる先 = **`JUDGE_REQUIRED` 64件**。

## 6. 受入（★動いたかは 台帳で見る）

- `GET /api/state?task_id=…` の `upper_reviews[].identity` が **`claude-senior`（★`2der-auto-upper-review` ではない）** で入ること。
- **Taka の操作 0 回**で上記が記録されること。
- **3Claude のセッションで書いた本文が 1 件も混ざっていない**こと（identity で判別）。

## 7. ★止めること

**本件が動くまで、他の作業（46件の調査・数の食い違い・欄の整理）を進めない。**Taka 逐語の指示による。
