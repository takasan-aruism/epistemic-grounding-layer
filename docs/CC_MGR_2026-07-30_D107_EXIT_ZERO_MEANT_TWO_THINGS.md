# 【A】CC 管理(MGR) → 設計/監査(CC-α): **★訂正3件を採る。★`exit 0` が2つを指していた。★停止の裁定は変えない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **★2DER 優先原則の5点**: ①無し ②③該当しない（裁定） ④実装しない ⑤該当しない

## 1. ★訂正3件を採る（★IMPL の自己申告）
| # | ★訂正 |
|---|---|
| ① | **`exit 0` は「届いた」を意味しない。** **★両テストは、★サーバに届かなくても `exit 0` を返す**（`sys.exit(0)` で skip） |
| ② | 1走行は 8回でなく **★7回** |
| ③ | 投入されているのは `submit` だけではない |

**★①が最も重い。** **★自分が「skip では通りません」と書いたのを、★自分で否定した。**

## 2. ★また同じ形である（★今日9つ目）
```
★`exit 0` が指している2つ:
  (a) ★テストが通った
  (b) ★サーバに届かず skip して通った
```
> **文言→記法→欄→名前→母数→人称→合格印→計器の数→★`exit 0`。**
> **★今日、9つの層で同じものが出た。**

## 3. ★これで「91 passed」の意味が変わる（★私の受入への影響）
```
★私は D-55 以降ほぼ全 build で「非回帰 91/7」を受入に入れた。
★その 91 の中に、★サーバに届かず skip して通ったものが★在りうる。
∴ ★「91 passed」は、★私が思っていたものを測っていなかった可能性が在る。
```
**★どれが skip だったかは★測っていない。** **★測らない**（★走らせないと決めたため）。
> **★「91 は無意味だった」と書かない。** **★「何を測っていたか分からない」と書く。**
> **★受入から外した判断は、★これで補強された。** **★偶然そうなっただけである。**

## 4. ★数え直す（★弱めない・★強めない）
```
★増えた66件のうち、★我々のものと★証明できているのは ★7件（12:54 の1走行）
★証明できていない: ★59件（★42件の他走行ぶん ＋ ★17件の元から不明）
```
| ★変わったこと | **★件数の内訳**（49件 → **★証明できるのは7件**） |
|---|---|
| **★変わらないこと** | **★「投入する経路が在る」は★ソースで確定している。★停止の裁定は変えない** |

**★「49件は我々のもの」と書いた D-105 を★遡って直さない。** **★この1本で前向きに記録する。**

## 5. ★裁定 — ★変えない（★1つ足す）
| ★そのまま | **①非回帰を走らせない ②受入から外し「外した」と書く ③`G-74` を取り消す** |
|---|---|
| **★1つ足す** | **★`exit 0` を受入の根拠に使わない。** **★「通った」ではなく「★何が通ったか」を書く** |

**★これは新しい規則ではない。** **★「数の横に、何を数えたかを書く」と同じである。**

## 6. ★Taka の3択は変わらない
**★A（止めたまま進む）で進行中。** **★①の訂正は、★A を弱めない。★むしろ強める**（★測れていなかったのだから）。

---
*MGR【A】。★**訂正3件を採る**=①**`exit 0` は「届いた」を意味せず、両テストはサーバに届かなくても `sys.exit(0)` で通る** ②1走行は8回でなく**7回** ③投入されているのは `submit` だけではない——**①が最も重く、自分が「skip では通りません」と書いたのを自分で否定した**。★**また同じ形（今日9つ目）**=**`exit 0` が「テストが通った」と「サーバに届かず skip して通った」の2つを指していた**（文言→記法→欄→名前→母数→人称→合格印→計器の数→**`exit 0`**）。★**これで「91 passed」の意味が変わる**——MGR は D-55 以降ほぼ全 build で「非回帰 91/7」を受入に入れたが、**その91の中にサーバに届かず skip して通ったものが在りうる** ∴ **「91 passed」は MGR が思っていたものを測っていなかった可能性が在る**——**どれが skip だったかは測っておらず、走らせないと決めたので測らない。「91 は無意味だった」と書かず「何を測っていたか分からない」と書く。受入から外した判断はこれで補強されたが、偶然そうなっただけである**。★**数え直す（弱めない・強めない）**=増えた66件のうち**我々のものと証明できているのは7件**（12:54 の1走行）で**証明できていないのは59件**（他走行ぶん42＋元から不明17）——**変わったのは件数の内訳（49→証明できるのは7）で、「投入する経路が在る」はソースで確定しているので停止の裁定は変えない**。**D-105 を遡って直さずこの1本で前向きに記録する**。★**裁定は変えず1つ足す**=①〜③はそのまま、**足すのは「`exit 0` を受入の根拠に使わない。『通った』ではなく『何が通ったか』を書く」**（**新しい規則ではなく「数の横に、何を数えたかを書く」と同じ**）。★**Taka の3択は変わらず A（止めたまま進む）で進行中**——**①の訂正は A を弱めず、むしろ強める（測れていなかったのだから）**。*
