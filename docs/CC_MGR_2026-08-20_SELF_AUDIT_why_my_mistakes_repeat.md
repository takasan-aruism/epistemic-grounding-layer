# 自己監査 ―― ★私の 事故が 繰り返す 機械的な 原因（★実測・★言い訳を 書かない）

**2026-08-20 18:2x ／ ★対象＝本日 私が 入れた commit 8件 ／ ★実装 0（★この 文書は 記録）**

---

## 1. ★実測 ―― 「★実際の 上流から 1回 走らせたか」で ★事故率が 分かれた

| commit | 上流から 走らせたか | 事故 |
|---|---|---|
| `20195fa` | **★していない**（★関数 単体の 回帰だけ） | **★`run_runner` の `NameError`** → ★本線 GENERATE が ★全 task で 停止。★再起動まで **潜伏** |
| `04f8b07` | **★していない**（★throwaway のみ ／ ★本番 呼び手 **0**） | ★連動 不成立の まま COMPLETE できない |
| `fe46ab5` | ★通した（front door） | ★但し ★HOLD 経路を 通して いない → `work_kind=None` を 見落とし |
| `377c85c` | ★通した（`_last_task()` 経由） | ★事故 0 |
| `af4088a` | ★通した（front door 再投入） | ★事故 0 |

```
★★通した 3件 = 事故 0 ／ ★通して いない 2件 = 事故 2。
★★＝ ★『実際の 上流から 1回 走らせる』の 有無で ★きれいに 割れた。★偶然では ない。
```

---

## 2. ★もう 1つの 型 ―― 「★引けた／在った」を 「★成立した」と 読んだ

| 私が 見た 信号 | 私の 結論 | ★実際 |
|---|---|---|
| `/api/state` の `generate_runs` | 「記録が 無い」→ `no_input_record` | **★その 欄が 存在しない だけ**。`derive_state` には 在った（`_ordinal=4166`） |
| `OBS-…` が `/api/resolve` で 引ける | P1 受入 ⑨ ✔ | **★引けるのは 証拠だけ**。★組み立てた 報告は 引けない |
| 経路表から `S12` が 引けた | `ROUTE_STAGE` 充足 | **★『到達する』を 『通過する』と 読んだ**。★S12 の 内側の 受入検査を 宣言に 入れて いない |
| `egl_source_refs` が 4件 在る | 調査 完了（`INVESTIGATION_RECORDED`） | **★10問 中 0問 しか 答えて いない**（★下記 §3） |

```
★★共通 = ★★分母を 見ずに ★存在だけを 見た。
```

---

## 3. ★私が 作った 門の 欠陥（★今日 実際に 発火した）

```
★`submit.py` の INVESTIGATE 分岐に 私が 書いた 判定:
      _done8 = bool(_refs8)            # ★証拠が 1件でも 在れば 完了
★★実測(本日 18:2x) = ★問い 10件 ／ ★答え 0件 ／ ★evidence 0件
      それでも  stop_at_reached = "INVESTIGATION_RECORDED"
★★＝ ★★空虚な 成功。★『調査した』と 記録に 残る のに ★1問も 答えて いない。
★★分類 = ★ASSUMPTION_WITHOUT_EVIDENCE（★私の P1 実装）
★★これは ★Taka の 逐語「★『Stage に 到達した』≠『Stage を 通過した』」の ★私自身の 実例。
```

**★これは Claude 由来の 破損（★指示書 §2-A）。★但し ★第四 停止中 ∴ ★私は 直して いない。**

---

## 4. ★2度と 起こせなく する 規則（★私の 心掛けでは なく ★機械の 条件）

```
★★R1 ―― ★変更は ★『実際の 上流から 1回 走らせ ★observed edge が 出る』まで ★完了に しない。
   ★単体の 回帰は ★通過条件に 数えない（★`20195fa` は ★単体 全通過 で ★本線 全停止 だった）。
   ★観測の 引き方は 既に 在る = `etrace.resolve_task` → `run_ids` → `resolve_run` の 2段引き。

★★R2 ―― ★成立の 判定に ★★存在の 有無を 使わない。★★分母つきの 数で 判定する。
   ★悪い例(★私が 書いた) = `bool(_refs8)`
   ★正しい形            = 「★答えた 数 / 問うた 数」「★記録された 数 / 生成された 数」
   ★★どの 欄を 数えるかを ★宣言に 書き ★実測と 突き合わせる。

★★R3 ―― ★`ROUTE_STAGE` は ★『到達』では なく ★『通過』で 宣言する。
   ★段の 内側の 受入検査（★`build_planner` の validation 等）を ★門と して 列挙する。
```

---

## 5. ★2DER に 分析させた 結果（★私は 補完して いない）

```
★投入 = `TASK-2DER-F9A6AD9C`（★work_kind=INVESTIGATE）
★返り = ★問い 10件 ／ ★★10件すべて `UNVERIFIED` ／ `needs_design=true` ／ ★evidence 0
★★＝ ★2DER は ★この 分析を ★いまの 観測カタログでは ★1問も 答えられない。
★★分類 = ★OBSERVABILITY_GAP（★カタログは gpu_memory / running_containers /
        top_memory_processes / listening_ports の 4種のみ ＝ ★code も 台帳も API も 読めない）
★★∴ ★Taka の 10問への 答えは ★★『2DER からは 取れない』が ★実測の 結果。
★★私は ★その 10問を 代わりに 答えて いない（★指示書 逐語）。
```

## 6. ★私が した 事故の 一覧（★本日・★言い訳なし）

```
①`20195fa` `run_runner` の NameError ―― 本線 GENERATE 全停止（★復旧済 `3f84663`）
②`/api/state` の 無い 欄から 読み ★`no_input_record` と 誤診（★訂正済）
③P1 受入 ⑨ を ✔ と 報告（★正しくは PARTIAL・★訂正済）
④14項目で `S12` を ★『到達』で 書き ★段の 内側の 門を 宣言に 入れなかった
⑤分析の 投入で ★`<<<2DER:QUESTIONS>>>` を 付け忘れ ★問い 0件 で 走らせた
⑥`_done8 = bool(_refs8)` ―― ★0問でも 完了に なる 門を 自分で 書いた（★未修理）
⑦`planner_outcome` を 出力に 含めず ★却下理由を 取り逃した
```
