# 【D-202】★C-1 を受け取る（★廃止2件を実行する）／ ★S-3 は「誰の手番でもない」— ★次の ACTIVE はこれ

- `BUILD_ROLE: 参照` / **宛: 設計/監査** / 写: Taka / 発: MGR / 2026-08-01 01:1x / TYPE=STATUS
- **開発者規律 確認済（版: v1.0）** ／ **受領**: `CC_IMPL_2026-08-01_C1_PROGRESS_WRITE_WORKS_BUILT.md`（BUILT・★未監査）／ `CC_DESIGN_2026-08-01_C1_BUILD_SPEC_AND_S3_CANNOT_BE_INGESTED.md`

## 1. ★C-1 の受領（★「通った」と書く。★「完了」とはまだ書かない）
```
★受入①② ○（★front door から書けた ／ `/api/roadmap` で読めた。★counts と status と note が動いた）
★受入③ ★部分的（★1件は言えた。★全部は言えていない。★現に報告は .md である）
★★★BUILT は ★未監査である（★第10章）∴ ★設計/監査が ★独立に確かめるまで ★Taka へ成果として上げない。
★★★★IMPL が書いた ★63行は ★SPEC が認めた例外であり、★行数が報告されている。★隠していない。
```

## 2. ★裁定①：★廃止2件を★実行する（★IMPL が「廃止は私が決めない」と書いた分）
```
★① ★進捗を .md に書く運用 → ★★廃止する。
   ★以後、★進捗は ★front door の `<<<2DER:PROGRESS>>>` で書く。★.md に進捗を書かない。
   ★★残るのは ★裁定・調査の往復だけである（★別問題。★C-2 で減らす）
★② ★人が set する自己申告値（★状況表の `A` 行） → ★★廃止する。
   ★`~/.claude/hooks/2der_a_metric.sh` を止め、★`GET /api/roadmap` から★機械で出す形へ置換する。
   ★★これは ★計器の維持＝MGR の担当だが ★実装は IMPL。★設計が BUILD SPEC に落とすこと。
   ★★★置換までの間、★A 行は ★「自己申告・非証拠」の表示のまま ★据え置く（★消してから作らない）
★★★★★廃止したものを ★後で黙って復活させない。★復活させるなら「復活させた」と書く。
```

## 3. ★裁定②：★S-3 は「誰の手番でもない」— ★設計の訂正を採る
```
★私は `D-201` §4 で「★設計/監査が `/api/ingest` で戻す」と書いた。★★誤りである。
★★実測（★設計が2回 叩いた）: 逐語 `{"error": "ValueError: no Claude ingest for op=BLOCKED"}`
★★★原因: ★`dispatch.py:_MAP` に `JUDGE_REQUIRED` が無く ★既定の `BLOCKED` に落ちる ／
   ★`webui.ingest` は `PLAN` / `DISPOSE` / `UPPER_REVIEW` しか受け取らない。
★★★★∴ ★S-3 は ★止まっているのではなく ★★渡す先が無い。★C-1 と ★同じ形の欠落である。
```

## 4. ★次の ACTIVE（★1件）
> ### **★`JUDGE_REQUIRED` に ★手番を与える（★最小・可逆）。★S-3 を先へ通すため。**
```
★設計へ: ★案を ★2つ出すこと。★どちらも ★最小差分で ★戻せる形にすること。
   ★案A: ★`_MAP` に `JUDGE_REQUIRED` の行を足す（★誰の段かを決める）
   ★案B: ★`UPPER_REVIEW` へ寄せる（★既に ingest が受け取れる口を使う＝★新しい口を作らない）
★★★同時に ★1回だけ確認: ★「`JUDGE_REQUIRED` に口を置かなかったのは ★意図か」を ★記録の中で探す
   （★`D-191` の縮小版。★別件として増やさない。★見つからなければ「記録に無い」で終わり）
★★★★★私の傾き（★決めるのは設計案を見てから）: ★案B。★理由＝★新しい口を作らないから。
★★★★★★どちらでも ★`UPPER_REVIEW` の実体は ★`CLAUDE_SENIOR`＝★Claude である。
   ∴ ★★これは ★「Claude の仕事を増やす」方向である。★★★増やすと書いた上で入れる（★隠さない）。
   ★★★★減らす道（★2DER 自身に判定させる／★単発 Claude）は ★`D-199` に保留してある。★そこへ戻す前提。
```

## 5. ★見つけて直していないもの（★1件だけ持っていく）
```
★`registered_at` が ★`submit()` の既定 ts（`2026-07-11T08:00:00`）に落ちる
   ＝ ★front door から書いた進捗に ★「いつ書かれたか」が残らない。★既登記 `G-84` と同型。
★★★直していない。★C-1 の対象に足していない（★規律9）。★★C-2 で扱うか、★単独で戻す。
```

## 6. ★順序（★更新。★後回しは全部 書く）
| # | 件 | 扱い |
|---|---|---|
| **ACTIVE** | **`JUDGE_REQUIRED` に手番を与える（案A/案B・可逆）** | **本裁定** |
| 直後 | S-3 を先へ通す（★判定 → 再生成 → 合否） | 待ち |
| 次 | 状況表 `A` を `/api/roadmap` から機械算出へ置換（★廃止②の実体） | 予約 |
| その次 | (2) 停止時に `next_information_need` を上書き ／ (1) 契約 None を止める ／ C-2 申請書 | 予約 |
| **後回し** | `D-199` 単発 Claude ／ C-3 外の道を塞ぐ ／ `G-84` 系（`registered_at`） | **後回し** |

---
**決めたこと**: **①C-1 は受入①②が ○ で通った（front door から書け、`/api/roadmap` で読め、counts と status と note が動いた）。③は部分的で、BUILT は未監査だから Taka へ成果として上げない。IMPL が書いた 63行は SPEC が認めた例外で、行数は報告されている ②廃止2件を実行する——進捗を .md に書く運用を廃止し、以後は front door の `<<<2DER:PROGRESS>>>` で書く。人が set する自己申告値（状況表の A）も廃止し、`/api/roadmap` からの機械算出へ置換する（置換までは非証拠表示のまま据え置く。消してから作らない）③S-3 は誰の手番でもない——`/api/ingest` は `no Claude ingest for op=BLOCKED` を返す。`_MAP` に `JUDGE_REQUIRED` が無く BLOCKED に落ち、ingest は PLAN/DISPOSE/UPPER_REVIEW しか受け取らない。私の「設計が ingest で戻す」は誤りだった ④次の ACTIVE は `JUDGE_REQUIRED` に手番を与えること。案A（`_MAP` に足す）と案B（`UPPER_REVIEW` へ寄せる）を最小差分・可逆で出させ、同時に「口を置かなかったのは意図か」を記録の中で1回だけ確認する。私の傾きは案B（新しい口を作らないから）⑤ただしどちらも実体は Claude なので「Claude の仕事を増やす」方向である。増やすと書いた上で入れ、減らす道は `D-199` に保留してある ⑥`registered_at` が既定 ts に落ちる件は直さず、後回しとして持っていく。**
