# 設計/監査 → IMPL（写: MGR / Taka）: D-5 は現記録では答えられない — **必要な記録を指定する**

- `BUILD_ROLE: 参照`
- 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=STATUS
- **受領した MGR 文書**: `CC_MGR_2026-07-27_BUILD4_RECEIVED_MY_PREDICTION_FAILED.md`（設問 D-5）

## 0. 結論
**D-5（旧アームの遠隔誤りは新アームで何になったか）は、現在の記録では答えられない。** 憶測で答えない。**必要な記録を指定するので、IMPL が出すこと。**

## 1. 【監査:CC-α】記録に何が在り、何が無いか
```
再現: cd /home/takasan/egl/structure
      python3 -c "import json; d=json.load(open('MENU_RESTRICTION_BUILD4.json')); print(sorted(d['llm_compare']['batches'][0].keys()))"
```
| 在るもの | 無いもの |
|---|---|
| 各アームの集約（`M1_far` / `M1a_unsafe` / `M2_match` / `NO_CANDIDATE`） | **遠隔誤りの per-fixture 一覧（A / B それぞれ）** |
| **`NO_CANDIDATE` の振り直し per-case**（fixture/seed/run/B_choice/正解か） | **A の遠隔誤りが B で何になったかの対応** |

**＝ `reassignment.cases` は「A で `NO_CANDIDATE` だったもの」の追跡であり、「A で遠隔誤りだったもの」の追跡ではない。** 別の集合である。**D-5 は後者を要求している。**

## 2. 【監査:CC-α】現記録から言えること（部分的・ここまで）
- **構造的事実**: `context` 空の 17 fixture で `CONTEXT_RESOLVE` は B のメニューに無い（17/17・prompt 記録で検証可能）。**∴「IP1/IP2 が `CONTEXT_RESOLVE` を選ぶ」という形の遠隔誤りは、B では起こり得ない。**
- **にもかかわらず遠隔取り違え率は動いていない**（batch0: 0.0167 → 0.0167）。
- **∴ 論理的には (a) か (b) のどちらかしかない**が、**どちらかは記録から判定できない。**
- **参考（別集合だが示唆的）**: 振り直し事例に `IP2 seed0 → PREMISE_PROBE（不正解）` がある。`PREMISE_PROBE` は **STOP 側**であり `INTENT_PROBE`（期待）と**同じ側**＝**近縁**。**この1件に限れば「遠隔に移った」のではない。** **ただしこれは `NO_CANDIDATE` だった事例であり、遠隔誤りだった事例ではない。混同しないこと。**

## 3. 【指示】IMPL へ — D5-V（**新規の LLM 呼出は不要。既存 run の記録から出せるはず**）
```
依頼 D5-V: 遠隔誤りの per-fixture 前後対応を出す

前提: 既に走らせた Build 4 の A/B 両アームの per-(fixture,seed,run) の選択結果が
      手元にあるなら、そこから再集計するだけでよい。
      ★無ければ「記録していなかった」と書くこと。再実行の要否は MGR に諮る。
      ★LLM を新たに呼ぶ必要があるなら、呼出数と並列の有無を先に出すこと（IMPL 自身の申し送り）。

出すもの（表1つ）:
  行 = (fixture_id, seed, run) のうち、A または B のいずれかで遠隔誤りだったもの 全件
  列 = A_choice / A_far(真偽) / B_choice / B_far(真偽) / 期待戦略 / A側STOP-ACT / B側STOP-ACT

  併せて次の3つの数を出す:
   (1) A で遠隔誤り → B で正解      … 何件
   (2) A で遠隔誤り → B でも遠隔誤り（別の選択肢に移った） … 何件・移り先
   (3) A では遠隔誤りでない → B で新たに遠隔誤り … 何件・どの fixture

判定基準は既定のまま（STOP={INTENT_PROBE,PREMISE_PROBE,DEFER} / ACT=残り4つ）。
★IMPL は (a)/(b) の判定をしない。表と3つの数だけ出す。判定は DESIGN が行う。
```

## 4. 【設計:CC-α】なぜこの設問が重いか（先に書いておく・後から意味づけない）
- **(b) だった場合の含意**: **不正な選択肢を消しても、誤った判断そのものは消えず隣へ移る。** ＝ **「選べなくする」は必要条件だが十分条件ではない。**
  - **その時は、今日2回起きた「構造的に正しくなるが精度は動かない」（P3・Build 4）の共通原因が特定されたことになる。**
- **(a) だった場合の含意**: 介入は効いており、残る遠隔誤りは**別の原因**。**次に潰すべき対象が変わる。**
- **どちらでも「精度が動かなかった」事実は変わらない。** **意味づけを先に固定しておく。**

---
*CC-α。D-5 は現記録では答えられない（`reassignment.cases` は NO_CANDIDATE の追跡であり遠隔誤りの追跡ではない）。憶測で答えず、必要な記録を D5-V として指定した。新規 LLM 呼出が要るなら呼出数を先に出させる。(a)/(b) の判定は DESIGN が行い、IMPL は表と3つの数のみ出す。含意は先に固定した。*
