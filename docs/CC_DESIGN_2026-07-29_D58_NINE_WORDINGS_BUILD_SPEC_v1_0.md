# BUILD SPEC — 「無い」と出す9件を1回で直す v1.0（★実装源）

- `BUILD_ROLE: ★実装源`（**本文書が実装の唯一の典拠**）
- **★宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-29_D56_CLOSED_GO_FOR_NINE.md`
- **数え切りの根拠**: `CC_DESIGN_2026-07-29_D57_ALL_ABSENCE_WORDINGS_COUNTED.md` / `..._D57B_ROOT_PAGE_COUNTED.md`

---

## 0. ★本 build の一行
> **「無い」と言い切っている所を、★「何が無いのか」まで書く形に直す。**
> **★文言だけ。振る舞いを1つも変えない。**

**★対象は 2画面 24件のうち★9件**（内訳: **偽になる文1 / 窓2 / まとめている6**）。**残り15件は直さない**（まとめていないため）。

---

## 1. ★偽になる文（1件・最優先）
| | |
|---|---|
| **場所** | `twoder/webui.py` の `openByRun` 側「欠損・失敗・未確認」カード（`全 event の result は OK` の行） |
| **現在** | `全 event の result は OK（失敗の記録なし）` |
| **★なぜ最重要か** | **`truncated` の run では全 event を見ていない。** **∴ 文そのものが偽になる。** **しかも同じブロックに「`boundary_failures` → run からは取れない」が在り、その隣で「全 event は OK」と言えば★取れない範囲まで OK に見える** |

**★直した後**
```
取得した event の中に、失敗の記録は在りません
  ＋ rec.truncated が true のときは併記: 「★全 event を見ていません（打ち切られています）」
```
- **★「全 event」と書かない。** **「取得した event」に限定する。**
- **★「OK」と書かない。** **評価語を使わない。**

## 2. ★窓（2件）
| # | 場所 | 現在 | **直した後** |
|---|---|---|---|
| **`/` :396** | 直近 DE | `なし` | **「直近6件の中には在りません」** |
| **`/` :397** | 直近 CHG | `なし` | **「直近6件の中には在りません」** |

**★根拠（実読）**: `de = rep.get("recent_de", [])[-6:]` / `chg = rep.get("recent_chg", [])[-6:]`
> **∴ 「無い」ではなく★「この窓に無い」である。** **★窓の大きさ（6）を文言に出す。**

## 3. ★まとめている（6件）
| # | 場所 | 現在 | **直した後** |
|---|---|---|---|
| **A** | `/command` 起動時 | `現在 runnable な DW task はありません。` | **「進められる DW task は在りません。まだ投入していない／投入したが DW task にならなかった／BLOCK された、のいずれかです。この画面では区別できません」** |
| **B** | `/command` `openByRun` | `その run は記録に無い: <id>` | **「その run は引けませんでした: `<id>`（存在しないのか、記録を引けなかったのかは区別できません）」** |
| **C** | `/command` `:701` | `authority boundary なし` | **「記録された authority boundary は在りません（記録されなかったものが在るかは、この画面では分かりません）」** |
| **D** | `/command` `missingCard` | `欠損・失敗の記録なし` | **「記録された欠損・失敗は在りません」＋併記「記録されなかった欠損・失敗が在るかどうかは、この画面では分かりません」** |
| **E** | `/command` `openByRun` | `EGL の source_refs / claims` → `無い` | **「この経路では取れません（EGL に根拠が無いのか、この経路に載らないのかは区別できません）」** |
| **F** | `/` `:445` | off-ramp フラグ → `なし` | **「記録された off-ramp フラグは在りません」** |

## 4. ★書き方の規律（4つ。★全件に当てる）
1. **★「無い」と言い切らない。** **「記録された◯◯は在りません」**（記録されたもの、に限定する）。
2. **★区別できないなら「区別できません」と書く。** **★原因を推測しない。**
3. **★「この画面では」を付ける。** **画面の限界であって、世界の事実ではない。**
4. **★色を使わない**（`ok` も `bad` も足さない）。**評価を色で与えない。**

## 5. ★変えないもの
- **振る舞いを1つも変えない**（分岐の条件・出す/出さないの判定）。
- **`webui.py` の Python 側**——**★ただし §2 の2件（`/`）は `render_report_page` の中に在る。** **★そこは Python である。**
  > **★文字列リテラルだけを変える。** **ロジックを1行も変えない。** **`[-6:]` を変えない。**
- **endpoint を足さない。** **新しい fetch・新しい状態変数を作らない。**
- **残り15件を触らない**（まとめていないため）。
- **勘定科目に触れない**（`G-62`/`G-64`〜`G-68`・据え置き）。

## 6. 受入（★投入なし。既存の run と task で足りる）
| # | 条件 | 使うもの |
|---|---|---|
| **1** | **9件の新文言が★配信画面のソースに在る** | `GET /command` と `GET /`（**★両方**） |
| **2** | **旧文言が★1つも残っていない** | **★9件の旧文言を1つずつ検索して0件を示す** |
| **3** | **実例が在るものは実際に出る** | **`/command` #C**（`TASK-2DER-B9B4DA3B` の `taka_authority` は `[]`） |
| **4** | **`ok`/`bad` を足していない** | **★差分で数える**（ページ全体で数えない） |
| **5** | **既存が不変** | `/api/state` の sha256 が実装前後で一致（**★文言変更なので Python 側の返りは変わらないはず**） |
| **6** | 非回帰 | 基準 91 passed / 7 failed・顔ぶれ diff |

### 6-1. ★実例が在るもの / 無いもの（★混ぜて書かない）
| 実例が**在る** | `/command` #C（`authority boundary`・`taka_errors` が `[]` で実際に出る） |
| 実例を**見つけていない** | `/command` #A・#B・#D・#E ／ `/` #2・#3・#F ／ **偽になる文（`truncated` の run が無い）** |
> **★作って試さない。** **★「実例が無い」を「実例が在る」と混ぜて書かない**（MGR 明示）。

## 7. ★止まってよい場所
| # | 条件 |
|---|---|
| 1 | **`/` 側の2件（`render_report_page`）で、文字列だけを変えられなかった** → **★止めて報告**（ロジックに触れる） |
| 2 | **旧文言を消すと別の分岐が壊れた** → 止めて報告 |
| 3 | **9件のどれかが、私の指定した場所に無かった** → **★私の数え間違い。報告** |
| 4 | SPEC が2通りに読める |

## 8. ★資料の同期
**`webui.py` が変わるため資料が古くなる。** **★同期は BUILT と同時に私が行う。** **あなたは触らなくてよい。**

---
*CC-α D-58 BUILD SPEC v1.0（実装源・宛 IMPL）。★一行=**「無い」と言い切っている所を「何が無いのか」まで書く形に直す。文言だけで振る舞いを1つも変えない**。対象は2画面24件のうち**9件**（**偽になる文1／窓2／まとめている6**）で、残り15件は直さない。★偽になる文（最優先）=`全 event の result は OK（失敗の記録なし）` → **「取得した event の中に、失敗の記録は在りません」＋`truncated` なら「全 event を見ていません（打ち切られています）」を併記**（「全 event」と書かず「取得した event」に限定し、「OK」という評価語を使わない）——`truncated` の run では全 event を見ておらず**文そのものが偽**になり、しかも同じブロックの「`boundary_failures` → run からは取れない」の隣で「全 event は OK」と言えば取れない範囲まで OK に見えるため最重要。★窓2件=`/` の直近 DE・CHG が `[-6:]` で切られているので `なし` → **「直近6件の中には在りません」**（窓の大きさを文言に出す）。★まとめている6件=A「進められる DW task は在りません。まだ投入していない／DW task にならなかった／BLOCK された、のいずれかです。この画面では区別できません」／B「その run は引けませんでした（存在しないのか、記録を引けなかったのかは区別できません）」／C「記録された authority boundary は在りません（記録されなかったものが在るかは、この画面では分かりません）」／D「記録された欠損・失敗は在りません」＋併記／E「この経路では取れません（EGL に根拠が無いのか、この経路に載らないのかは区別できません）」／F「記録された off-ramp フラグは在りません」。★書き方の規律4つを全件に当てる（「無い」と言い切らず「記録された◯◯は在りません」と限定／区別できないなら「区別できません」と書き原因を推測しない／「この画面では」を付ける／色を使わない）。★変えないもの=振る舞い・endpoint・新しい fetch/状態・残り15件・勘定科目。**ただし窓2件は `render_report_page`＝Python の中に在るので、文字列リテラルだけを変えロジックと `[-6:]` を1行も変えない**。★受入6件は**投入なし**（9件の新文言が `/command` と `/` の**両方**のソースに在る／**旧文言を1つずつ検索して0件**／実例が在る #C は実際に出る／`ok`/`bad` は**差分で数える**／既存の sha256 一致／非回帰 91/7）。★**実例が在るもの（#C のみ）と見つけていないもの（他8件）を混ぜて書かない**——作って試さない。★止まってよい場所=`/` の2件で文字列だけを変えられなかった（ロジックに触れる）／旧文言を消すと別の分岐が壊れた／**9件のどれかが指定した場所に無かった＝私の数え間違い**／2通りに読める。★資料の同期は BUILT と同時に CC-α が行う。*
