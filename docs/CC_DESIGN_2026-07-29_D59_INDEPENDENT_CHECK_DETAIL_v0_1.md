# 詳細設計 — 「独立に検算した」とは何か v0.1（D-59）

- `BUILD_ROLE: 参照`（**詳細設計。★規則文書にしない・実装に入れない・投入しない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **基本設計**: `CC_MGR_2026-07-29_D59_INDEPENDENT_CHECK_BASIC_DESIGN_DRAFT_v0_1.md`

## 0. ★成立条件への答え（先に）
> **MGR §0:「この定義を入れた結果、検査の総量が減っていなければ失敗である。」**

**★減る。** **測り方と実数を §3 に書いた。** **本日 私が無駄に流した検査は★6件である。**

---

## 1. ★MGR 未確認①：「別の当て方」をどこまで具体に書くか
> **★手順を書かない。★問いを3つ書く。** **手順は規則になる。問いは規則にならない。**

```
検算する前に、自分に3つだけ聞く:

  ① ★どこを測ったか — 相手と同じ場所か。同じなら、それは検算ではない。
  ② ★この計器は、この答えを出せる場所か — 出ない分岐を測って「無し」と言っていないか。
  ③ ★自分の前提に戻ったか — 対象外にしたはずのものを、欠陥として数えていないか。
```
| なぜ3つか | |
|---|---|
| **①** | **本日7回のうち、経路の違いが効いたのが最多**（同じ場所を測っても何も出ない） |
| **②** | **本日いちばん重い1件**（配信画面の「無し」は、その分岐が出ないだけだった） |
| **③** | **本日、報告する寸前で止まった1件**（「なし」1件は対象外の #6 だった） |

**★4つ目を作らない。** **★「本日の7回」を全部規則にしない**——**★残り4回は①②③のどれかで捕まる**（実測: 型4=②、型5=③、型1=③、型6=①）。

## 2. ★MGR 未確認②：過去の受入に当てると、どれが書けなくなるか（★実例1件）
### **★書けなくなる実例: `D-44` 受入②（Phase 1 の合否条件）**
```
私が書いたこと（逐語）:
  「Taka ② … ★満たされた。『【実】』（私が自分で引いた）」
  再現: GET /api/resolve?id=ETR-f0fe8461c407 → count=21 / EGL:18 / 孤立0

★新しい定義を当てると:
  ・IMPL が流したのと★同じコマンドである
  ・`/api/resolve` は★決定論である（同じ id に同じ答えしか返さない）
  ∴ ★これは「独立に検算した」ではない。★決定論の再実行である。
```
| | |
|---|---|
| **★データは正しい** | **21件・EGL18・孤立0 は事実である。★取り消さない** |
| **★取り消すのは「独立検算した」という★書き方である** | **私の再実行は、確からしさを1つも足していない** |
| **∴ 正しい書き方** | **`【伝聞・決定論】`。** **「私も同じ数字を見た」までで、「独立に確かめた」と書かない** |

> **★これは Phase 1 の結論を変えない。** **★変えるのは、その結論に私がどれだけ寄与したかの記述である。**
> **★区別して書く。** **本日ずっとやってきた「1つの言葉が2つを指す」の、★私の側の最後の1件である。**

### 2-1. ★同じ判定になるもの（実測・打ち切り無し）
```
再現: grep -l "独立検算|私自身で|私が自分で引いた|独立に検算" CC_DESIGN_2026-07-2*.md
総件数 6 / 確認 6 / 打ち切り無し
  D-44 監査（2本）／D-49 監査／D-50 監査／D-50 実測／到達性21件
```
> **★6件すべてに、決定論の再実行が含まれている。** **★6件とも「独立検算した」とは書けなくなる。**
> **★ただし6件のうち、★判断を当てた部分は残る**（§2-2）。

### 2-2. ★残るもの（★これが本物の検算である）
| 何を | どの文書 |
|---|---|
| **`egl/core.append_event` の到達性を、呼び手の実測で覆した** | D-42 監査 |
| **`sha1(文面+改行)` を自分で計算し、前提の崩壊を特定した** | D-49 監査 |
| **`head -8` の切り捨てを見つけ、27行を数え直した** | 撤回文書 |
| **配信画面からソースへ計器を切り替えた** | D-58 監査 |
| **対象外の #6 を、直し漏れとして報告する前に確認した** | D-58 監査 |
> **★どれも「相手と違う場所を、違う当て方で測った」ものである。** **★同じコマンドの再実行は1件も入っていない。**

## 3. ★MGR 未確認③：総量が減ったかの示し方（★実数を出す）
```
★測り方（1つだけ。新しい計器を作らない）:
  「実装が流したのと★同じコマンドを、監査が流した回数」を数える。

本日の実数: ★6件（§2-1）
この定義を入れた後の目標値: ★0件
```
| なぜこれで足りるか | |
|---|---|
| **1** | **★数えるのは私自身の文書だけである**（`grep -l`）。**新しい台帳も計器も要らない** |
| **2** | **★0 に近づいたかどうかだけを見る。** 比率も指標も作らない |
| **3** | **★増えたら失敗が分かる。** **減っても「良くなった」とは書かない**——**★減った分の時間が判断に回ったかは、別の話である** |

> **★§0 の成立条件はこれで測れる。** **★ただし「減った＝良い」と書かない。** **★測れるのは「無駄が減ったか」だけである。**

## 4. ★私が決めていないこと（MGR へ返す）
| # | |
|---|---|
| **1** | **★過去の6件を書き換えるか。** **【設計:CC-α】書き換えない方を推す**——**記録を遡って直さない**（本日ずっと守ってきた原則）。**代わりに本文書1本で「6件は決定論の再実行だった」と★前向きに記録する** |
| **2** | **★「人しか出せない」を Taka に出す形**（MGR §3 の3区分目）。**★私は形を持っていない。** 現状は「未確認」に書くだけである |

## 5. ★範囲（広げない・MGR §7 のまま）
- **我々3者の受入の書き方だけ。** **2DER の実装・台帳・UI に入れない。**
- **勘定科目に触れない**（`G-62`/`G-64`〜`G-68` 据え置き）。**投入しない。**
- **★規則文書を作らない。** **★この文書に在れば足りる。**

---
*CC-α D-59 詳細設計 v0.1。★成立条件（検査の総量が減らなければ失敗）への答え=**減る。本日 私が無駄に流した検査は6件**。★未確認①「別の当て方」は**手順を書かず問いを3つだけ**書く（手順は規則になり問いは規則にならない）=**①どこを測ったか（相手と同じ場所なら検算ではない）②この計器はこの答えを出せる場所か（出ない分岐を測って「無し」と言っていないか）③自分の前提に戻ったか（対象外にしたはずのものを欠陥として数えていないか）**——**4つ目を作らず、本日の7回の残り4回も①②③のどれかで捕まる**（実測）。★未確認②の実例=**`D-44` 受入②（Phase 1 の合否条件）**で「私が自分で引いた `【実】`」と書いたが、**IMPL が流したのと同じコマンドであり `/api/resolve` は決定論** ∴ **「独立に検算した」ではなく決定論の再実行**——**データ（21件・EGL18・孤立0）は正しく取り消さないが、取り消すのは「独立検算した」という書き方**であり、正しくは `【伝聞・決定論】`（「私も同じ数字を見た」までで「独立に確かめた」と書かない）。**Phase 1 の結論は変わらず、変わるのはその結論に私がどれだけ寄与したかの記述である**。同じ判定になる文書は実測で**6件**あり6件とも「独立検算した」と書けなくなるが、**判断を当てた部分は残る**（`egl/core.append_event` の到達性を呼び手の実測で覆した／`sha1(文面+改行)` を自分で計算して前提の崩壊を特定した／`head -8` の切り捨てを見つけ27行を数え直した／配信画面からソースへ計器を切り替えた／対象外の #6 を報告前に確認した）——**どれも相手と違う場所を違う当て方で測ったもので、同じコマンドの再実行は1件も入っていない**。★未確認③=測り方は**「実装が流したのと同じコマンドを監査が流した回数」を数える**1つだけで、**本日6件・目標0件**（数えるのは自分の文書だけなので新しい台帳も計器も要らない／比率も指標も作らない／**増えたら失敗が分かるが、減っても「良くなった」とは書かない——減った分の時間が判断に回ったかは別の話**）。★MGR へ返す=**過去6件を書き換えるかは「書き換えない」を推す**（記録を遡って直さない原則。代わりに本文書1本で前向きに記録する）／**「人しか出せない」を Taka に出す形を私は持っていない**（現状は「未確認」に書くだけ）。★範囲は我々3者の受入の書き方だけで実装・台帳・UI に入れず、勘定科目に触れず、投入せず、**規則文書を作らない**。*
