開発者規律 確認済(v1.0)

# 【指示・★Taka】★ノウハウは ★保存ではなく ★★呼び出し規則 ／ ★★新台帳0・★集計は 保存せず 導出する

宛: MGR ／ 発: DESIGN（監視兼務）／ 2026-08-13 07:52 ／ 台帳: ★★MGR が 決める（★★本線＝EVO-0058 ∴ ★本件は ★後回し で 登録が 妥当）

## 1. ★Taka 逐語（★歪めない）

> **新台帳0を前提に、既存記録へ「参照した失敗パターン」「適用した方法」「結果」を残せるか調査する。集計値 used/worked/failed は可能なら正本として保存せず、実績から導出する。**

**★背景（★Taka が 外部助言と 議論した中身の 逐語・要点）:**
> **資産化で一番危ないのは「保存したから資産になった」と思うこと…本当に必要なのは、保存ではなく★呼び出し規則**
> **参照はLLMの気分でやらせない…★この種類の仕事を始めるには、必ずこの台帳を通る にしてしまえばいい**
> **機械側は どの候補をLLMへ見せるか を担当する…LLMの仕事は 今回の問題と候補ノウハウが本当に似ているか の判断**

## 2. ★★調査の結果（★私が 源で 直接 確かめた・2026-08-13 07:49）

```
★★(a) ★『参照した失敗パターン』の 器 = ★★★既に 在る
   ★`dev-workcell/dw/workflow.py:47` 逐語=`"relevant_failure_patterns": knowledge_packet.get("related_failure_patterns", [])`
   ★`dev-workcell/dw/adapters.py:139` 逐語=`RELEVANT_FAILURE_PATTERNS:` ＝ ★監査役の prompt へ 渡している
   ★`twoder/submit.py:701` が 投入時に 埋めている
   ★★∴ ★新しい欄を 作らない。
   ★★足りない = ★★①中身が ★直近1件だけ（逐語 `recs[-1]`）／ ★★②『採用したか／しなかったか』が 無い

★★(b) ★『結果』の 器 = ★★★既に 在る
   ★`dev-workcell/dw/workcell.py:456 record_disposition` 逐語=
     `verdict∈{ACCEPTED,PARTIAL,REJECTED,REMAINS}` ／ ★PARTIAL は `accepted_portion` 必須（fail-closed）
   ★指摘の 分類語 = ★13種 固定（★`failure_pattern_recurrence`＝★同じ形の再発 も 語として 在る）
   ★★∴ ★『効いた／効かなかった』の 元データは ★既に 記録されている。

★★(c) ★『適用した方法』= ★★★無い
   ★変更の記録（`artifact_registry.record_change`）が 持つのは
     ★触った成果物 ／ ★走らせた試験 ／ ★実測 ／ ★権限 ／ ★commit の前後
   ★★『どの失敗パターンを見て どう直したか』は ★入っていない。

★★(d) ★成績（used / worked / failed）= ★★★どこにも 無い（0）

★★(e) ★★『強制参照』の 器 = ★★★既に 在る（★★これが 一番 大きい）
   ★`completion_blockers`（★条件を満たさないと 完了できない）
   ★`workcell.py:347` 逐語=`EMPTY_WATCHED_FIELDS = ["related_failure_patterns"]`
   ★★∴ ★★『過去の失敗を 参照したか（★欄が空か）』は ★既に 見張られている。
```

## 3. ★★∴ 足すのは ★1つだけ

```
★★『見た失敗パターンの id』と『★採用したか／しなかった理由』を
   ★★★記録の1行として 残す（★★既存の 記録の器へ emit）
   ―― ★票（menu_vote）や ★採用（ADOPT_ROUTE_EDGE）と ★同じやり方
   ★★新台帳0 ／ ★front door の口 0増 ／ ★新しい file 0（★見込み）
```

## 4. ★★集計は 保存しない（★Taka 明示）

```
★used   = ★その失敗パターンが ★包みに 入っていた task の数
★worked = ★その task が ★完了に 至った数
★failed = ★その後 ★同じ分類の指摘が ★また 出た数
★★∴ ★★読む時に 数える。★★正本として 保存しない。
★★理由（★本日の実測）= ★保存した集計は ★★実態と ずれる（★本日 2件・欄は在るが 中身が空）
```

## 5. ★★受入（★数で書く・★案）

```
★① ★『見た／採った／採らなかった』が ★1件 実物で 記録に 残る
★② ★★集計を ★保存していないこと（★★保存欄の 増分 = ★★0）
★★③ ★used / worked / failed が ★★読む時に 導出できる（★1件で 実演）
★★④ ★母数を 明記（★★分母が 無い割合を 出さない）
★★⑤ ★『採らなかった理由』が ★空でないこと（★★0件で 素通りしない）
★★⑥ ★渡す件数が ★1件のままでないこと（★★いま `recs[-1]`＝1件）
   ―― ★★『これは N 回目です』が 出るのが 狙い（★本日 9回 同じ型が 出た）
★⑦ ★新台帳0 ／ ★口 0増 ／ ★新語は 足すなら 1つだけ
★★⑧ ★★LLM に 検索させないこと（★★機械が 候補を 削り、★LLM は『似ているか』だけ）
```

## 6. ★★順序（★本線を 止めない）

```
★★★本線 = ★経路表（EVO-0058）＝ ★止めない（★Taka 逐語『経路表が完成しないと 永遠に Claude 主体になる』）
★★∴ ★本件は ★★後回し で 登録する ／ ★★戻す条件を 併記する
   ★案 = ★『経路表が 閉じた時』
★★但し ★★(c)『適用した方法』の1欄は ★★安い ∴ ★★経路表の中で 先に 入れられるかを MGR が 判断してよい。
```

## 7. ★注意

```
★★新台帳を 作らない（★Taka 明示・★規律§9）。
★★[[in-the-machine-or-delete-it]]: ★A（★その工程を 始めたら 必ず 通る）に できないなら ★足さない
   ―― ★★『必ず通る』の 器は ★★既に 在る（★完了の門・★空欄の見張り）＝ ★★そこに 相乗りする。
★★[[absence-reads-as-compliance]]: ★★『参照 0件』を ★『参照する物が 無い』と 読まない。
★★外部助言の 案は ★新しい台帳を 立てる形に 読める ∴ ★★その部分は 採らない（★既存へ 相乗り）。
```
