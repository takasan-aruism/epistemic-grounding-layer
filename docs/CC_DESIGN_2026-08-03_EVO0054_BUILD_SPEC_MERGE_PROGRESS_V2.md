# 【BUILD SPEC v2】`EVO-0054` — **★`claims_turn` を捨てる。★手番は運用で守っていると台帳に残す**

- `BUILD_ROLE: ★実装源` / **宛: IMPL**（★封入は MGR） / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-03 19:4x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.10）** ／ **★9項目 確認済（★§6）** ／ 親: `ITEM-2DER-EVO-0035`
- **★v1 との関係**: ★差し替えない・追記しない（規律）。**★本書が実装源**
- **★走行 0・★task 増 0・★commit 0**

---

## 1. ★判断: **(a) 捨てる**（★私が決める・★理由を書く）

**★まず独立に確かめた（★指摘を鵜呑みにしない）**
```
★`twoder/progress_seal.py` に ★`next` の抽出は ★★0件
   ★`REQUIRED = ("item","status","actor","stage")` ／ ★`OPTIONAL = ("phase","title","note")`
★∴ ★`progress` dict に ★`next` キーは ★決して入らない ＝ ★`claims_turn` は ★★常に False
★★MGR の指摘は ★正しい。★不活性である。
```

**★(a) を選ぶ理由 3つ**
```
★① ★規律9=増やさない。★(b) は ★`progress_seal` に欄を1つ増やす
★② ★★不活性な欄を残すのは ★『在るが呼ばれない』を ★我々が新しく作る行為である
   ★本日3例目としてMGR が数えたばかりの病気を、★自分で1つ足すことになる
★③ ★★手番は ★既に `next=` を ★note 本文の先頭に書く規約(EVO-0046)で運用しており、★二重になる
   ★同じことを2つの場所で言う形は ★食い違いの種（★本日 何度も踏んだ）
★★★★★★★手番の乗っ取り(記録だけのつもりで next= を書いてしまう)の実害は ★本日 0件【★但し ★測っていない=未確認】
```

## 2. やること

**★契約から `claims_turn` を外す**（★依頼文・試験・骨格を1本の走行で作り直す）
```
★依頼文: ★「claims_turn」に関する2行を ★削る。★他は1文字も変えない
★戻り値: ★{"status": str|None, "status_kept": bool}   ← ★2欄になる
★封印試験: ★8本 → ★★6本（★`test_claims_turn_only_when_next_present` と
          ★`test_empty_next_does_not_claim` の ★2本を ★外す。★残り6本は ★1文字も変えない）
★骨格: ★不変（`def merge_progress(prev, progress):`）
```

**★配線（★Claude・★行数を申告）**
```
★`submit.py:229` の呼び手は ★`claims_turn` を ★読まない（★既に読んでいないなら ★0行）
★★`note_of` は ★従来どおり（★`next=` は ★書き手が note 本文に書く＝★運用のまま）
```

## 3. ★★台帳に残すこと（★裁定の指定・★隠して閉じない）

```
★★「★手番の保護は ★機構ではない。★運用（★書き手が `next=` を書かない）で守っている」
★★★これを ★`EVO-0054` の note に ★1行 残す（★DONE にする時も ★消さない）
★★★★★戻す条件も書く=★『★手番の乗っ取りが ★実測で1件でも出たら ★(b) を検討する』
   ＝ ★畳んだのではなく ★★測っていないから今は運用、と ★正直に書く
```

## 4. 受入

```
★(1) ★worker が書く（★Claude は本文0行）／★(2) ★6本 全通
★(3) ★★戻り値が ★2欄（★`claims_turn` が ★無い）——★逐語で示す
★(4) ★★本日(1)(2) の形が再現しない: ★`DONE` の item に ★status を書かない追記 → ★`DONE` のまま
★(5) ★★本日(3)(4) の形が再現しない: ★その追記で ★`effective` の ★手番の出所が ★前のまま
     ★★★★これは ★運用（★`next=` を書かない）で成り立つ ∴ ★★「機構で守った」と ★書かない
★(6) ★sha256 一致 ／ ★(7) ★行数を分ける ／ ★(8) ★戻せる ／ ★(9) ★61本を走らせない
★★★★★予告を投入前に書く: ★worker の行数 ／ ★Claude の配線の行数（★0 と見込むなら 0 と書く）
```

## 5. ★★予告が外れた件（★私の分も数える）

```
★MGR: worker 20〜35行 / 配線 5〜10行 → ★実測 19 / 11 ＝ ★両方 外れ
★実装: 20〜30 / 6〜10 → ★両方 外れ
★★★私(設計)は ★v1 で ★行数の予告を ★書いていない ∴ ★当否以前に ★出していない。
   ★★これは ★私の不備である（★予告は ★投入前に書くと ★自分の SPEC に書いておきながら ★自分は書かなかった）
★★★★本書では ★§4 の末尾で ★実装に予告を求めている。★私も次から ★SPEC に ★自分の見込みを書く
```

## 6. ★9項目（私の分）
```
1 置いたなら読めるか＝★受入(3) は ★成果物の逐語で見る
2 読めるなら書けるか＝★★本件はまさに ★書く口を ★確かめ切っていなかった件（★MGR の指摘どおり）
3 理由を捨てない＝★★§3 で「機構ではない」を ★残す（★DONE でも消さない）
4 作っていないのでは＝★★`claims_turn` は ★作ったが ★呼ばれていなかった ∴ ★捨てる
5 走ったか＝★受入(4)(5) は ★実投入で測る／6 名前＝★不変
7 依頼と試験の矛盾＝★★2本を外すので ★依頼文の該当2行も ★同時に削る（★片方だけ直さない）
8 計器が自分を数えないか＝★受入(5) は ★台帳の値
★9 増える代わりに廃止＝★★★増やさない。★★逆に ★契約から1欄 ★減らす。
   ★★★★畳むのは ★『不活性な欄を持ったまま DONE にする』こと
```

## 7. 禁止
```
★`progress_seal` に ★`next` 欄を足す（★＝(b)。★実害が1件 出るまで ★やらない）
★残す6本の試験を変える ／ ★骨格を変える ／ ★`set_status` を触る ／ ★勘定科目に触る
★「手番も機構で守った」と書く ／ ★§3 の1行を ★DONE 時に消す
★新しい台帳・エンドポイントを作る ／ ★61本を走らせる ／ ★commit する
★`twoder` 配下で python を動かす（★`operator.py` の罠）
```
