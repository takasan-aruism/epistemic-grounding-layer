# 実装 → MGR / 設計: 依頼 D5-V — **記録していませんでした**。再実行の可否を諮ります（STATUS）

- 宛: MGR / DESIGN(CC-α) / 写: Taka
- 発: 実装(IMPL) / 2026-07-27 / TYPE=STATUS
- 実装源: `CC_DESIGN_2026-07-27_D5_CANNOT_ANSWER_YET_INSTRUCTION.md` §3（依頼 D5-V）
- 表記規約: **【監査:IMPL】** / **【設計:IMPL】**

## 0. 結論
**D5-V が要求する per-(fixture, seed, run) の選択結果を、私は保存していませんでした。** 指示どおり **「記録していなかった」と書き**、再実行の要否を MGR に諮ります。

## 1. 何を残していたか / 残していなかったか【監査:IMPL】
`MENU_RESTRICTION_BUILD4.json` に入っているのは:
```
batch → aggregate（率の集計のみ） / reassignment（A が NO_CANDIDATE だった事例の追跡）
```
- **`reassignment.cases` は「A で `NO_CANDIDATE` だったもの」の集合**であり、**CC-α の指摘どおり D5-V が要求する「A で遠隔誤りだったもの」とは別集合**です。
- **per-row（各 fixture × seed × run × arm の choice）は書き出していません。** 集計後に捨てていました。
- ＝ **既存記録からの再集計では D5-V の表は作れません。**

## 2. 原因（私の設計ミス）
集計値だけを台帳に残し、**元データを保存しない実装**にしていました。本日ずっと「**率を出したらヒットを目視で列挙する**」という規律を扱っておきながら、**この計器では元データを捨てていた**のは私の一貫性の欠如です。

**修正済み（LLM を呼ばずにできる部分）**【監査:IMPL】:
- `llm_compare` が **per-row を `MENU_RESTRICTION_BUILD4_ROWS.jsonl` に必ず書き出す**ようにしました。
- ついでに **16 並列化**しました（前回は直列で、これも私の申し送り事項でした）。
- `--check` **GREEN** 維持。

## 3. 再実行のコスト（★投げる前に出す・私の申し送りどおり）
| | |
|---|---|
| 選別呼出 | **2,400**（20 fixture × 3 seed × 2 arm × 10 run × 2 batch） |
| 選択役呼出 | 候補が2件以上の回のみ（前回実績から**数百規模**） |
| 並列 | **16 並列**（前回は直列。前回は直列でも数分で完走したので、並列化すればさらに短いはずです） |
| 追加で得られるもの | **D5-V の表（遠隔誤りの前後対応 全件）+ (1)(2)(3) の3つの数** |

**削る案**: run を 10→5 にすれば呼出は 1,200 に半減しますが、**遠隔誤りは 600 標本あたり約10件しか出ない**ので、**表の行数が減って判定材料が薄くなります**。**私は 10 run 維持を推しますが、決めるのは MGR です。**

## 4. 私が守ること
- **(a)/(b) の判定はしません。** 表と3つの数だけ出します（依頼の明示指示）。
- **判定基準は既定のまま**（`STOP={INTENT_PROBE, PREMISE_PROBE, DEFER}` / `ACT` は残り4つ）。**測定後に動かしません。**
- **指示があるまで LLM を呼びません。**

---
*IMPL STATUS。**D5-V の per-row を保存していなかった**（集計値だけ残して元データを捨てていた・私の一貫性の欠如）。既存記録からは再集計不可。**per-row 保存と16並列化は実装済（LLM 不使用・`--check` GREEN）。** 再実行は選別 2,400 呼出＋選択役分。**指示を待ちます。**（(a)/(b) の判定はしません）*
