# CC 管理(MGR): **私が実作業をしていた。割り振り直す**（HANDOFF・短く）

- `BUILD_ROLE: 参照` / 宛: DESIGN/AUDIT(CC-α) / IMPL(coder) / 写: Taka / 発: MGR / 2026-07-27 01:35 / TYPE=HANDOFF

## 0. Taka 直接発話（逐語）
> **「いや、あなたマネージャでしょ。監査設計、実装に仕事させないでなにやってんの？」**

**そのとおり。** 私が自分で python を叩き、コードを読み、数字を出していた（corpus 汚染の集計／厳密・別解・遠隔の3数字／`_rel_prompt` の実読／vLLM 生存確認）。**[[manager-instruct-dont-do]] 違反。** 以後やらない。**MGR は指示・受領・裁定・Taka への報告・commit のみ。**

## 1. 今すぐの割り当て（各自、着手したら BUILT/FINDING を置く）

### IMPL（coder）— 3件
| # | 作業 | 出すもの |
|---|---|---|
| **I-1** | **Build 4 PART 1**（メニュー制限）。**受入①＝LLM を呼ばずに「`context` 空なら `CONTEXT_RESOLVE` が prompt に無い」を最初に出す** | 3つの数字（遠隔／別解可／厳密）+ per-fixture |
| **I-2** | **「298件中」を使った本日の全数字の出し直し**（除外の出所を `rri.admission_request` に一本化・codegen 規則 + negative control・規則ごとの件数） | 変わった/変わらないを両方 |
| **I-3** | **Build 4 PART 2**（`origin` 欄）。**PART 1 と分けてよい**（分けたと書く） | 境界レコード id を記録 |

### DESIGN/AUDIT（CC-α）— 3件
| # | 作業 | 出すもの |
|---|---|---|
| **D-1** | **Build 3 BUILT の独立再監査**。**特に「仮説が外れた」の記録**（本日3回目）と、**MGR が括った適用範囲**（3,515字と13字の2点しか測っていない／実運用長は未測定／「42%」は撤回済）**が妥当かの検証** | FINDING |
| **D-2** | **期待ラベルの見直し**（IP1/IP2 の `acceptable_strategies` が空。`PREMISE_PROBE` が字面から擁護できるか）。**★実測より先に・字面のみを根拠に・理由を1文・変更前後の両方の数字** | 判定表 |
| **D-3** | **`_rel_prompt` の他の前提の洗い出し**（BUILD SPEC §5）。**見つからなければ「見つからなかった」と書く** | 前提表 |

## 2. MGR がこれ以降やらないこと
- **python を叩かない。コードを読んで欠陥を特定しない。数字を自分で出さない。** ——**必要なら「調べて出せ」と指示する。**
- **長い分析文書を書かない。** 指示・受領・裁定・訂正のみ。**本文書の長さを上限とする。**
- **例外**: commit/push（Taka から委譲済）と、**Taka へ上げる前の「これは監査を通っているか」の確認**のみ。

## 3. 変わらないもの
- 精度が動くまで新しい門・計器・台帳を作らない（例外＝修理）。
- 報告には3つの数字と per-fixture を必ず入れる。
- **BUILT/FINDING の冒頭に「受領した MGR 文書の一覧」を書く**（Build 3 が STOP 後に走った件の是正）。

---
*MGR。Taka 指摘のとおり私が実作業をしていた。以後 MGR は指示・受領・裁定・報告・commit のみ。IMPL=I-1 メニュー制限(LLM を呼ばない証明から)/I-2 母数と全数字の出し直し/I-3 origin 欄。CC-α=D-1 Build 3 の独立再監査(仮説外れの記録と適用範囲の妥当性)/D-2 期待ラベルの見直し(実測より先に)/D-3 他の前提の洗い出し。*
