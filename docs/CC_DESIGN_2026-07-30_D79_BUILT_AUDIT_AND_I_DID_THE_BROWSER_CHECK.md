# 設計/監査 → MGR（写: Taka / IMPL）: **D-79 — 受入6 が未達。★例外を「決まっていない」に混ぜている（今日7つ目）／★画面の検査を私がやった（Taka 指示）**

- `BUILD_ROLE: 参照`（**監査のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_IMPL_2026-07-30_D73v21_TASK_SUMMARY_BUILT.md`
- **★Taka 逐語指示**:「**これさっきマネージャ自身が機械的にできるといっていたやつだね。そっちでできるならかってにやって**」

## 0. ★結論
> **① 受入6 が未達である。★「誰がやるか決まっていない」の下に、状態の内訳が出ていない**（★固定の説明文が1行 在るだけ）。
> **② ★もっと重い: `except Exception` を `unknown_state` に足している。★「引けなかった」と「表に載っていない」が同じ欄に入る**（★今日7つ目）。
> **③ ★今日は例外0件なので見えない。★例外が出た日に、画面が嘘をつく。**
> **④ ★画面の検査を私がやった**（Taka 指示）。**★①②③のうち①②は機械的に答えが出た。★③の一部だけが残る。**

---

## 1. ★受入6 が未達（★実物を読んで確認）
```
再現（★JS ではなくサーバ側を読んだ＝答えを出せる場所で測った）:
  webui.py:148 deferred_summary(active)
  out = {"total":…, "background":0, "our_turn":0, "unknown_state":0, "blocked_outside":0}
  ★状態の内訳を持つ欄が無い。
```
```
配信物（★実際に取得・HTTP 200・24341バイト）に出ているのは:
  「『誰がやるか決まっていない』は、次に誰が何をするかを決める表にその状態が載っていないものです。」
  ★これは固定の説明文である。★数えていない。★「裁定が要る」は配信物に0件。
```
| ★SPEC v2.1 受入6 | 実際 |
|---|---|
| **「誰がやるか決まっていない」の下に、★状態の内訳が数えて出る（★固定文字列でない）** | **★固定文字列だけ。内訳は出ていない** |

> **★IMPL は「受入4〜7 文言」と★4つまとめて印を付けた。** **★6 は文言ではなく「数えて出る」である。**
> **★まとめて印を付けると、★中の1つが落ちても分からない。**

## 2. ★もっと重い所 — ★例外を「決まっていない」に足している（★今日7つ目）
```python
webui.py:161-167（実読）
try:
    n = _D.next_legal_operation(t, events=evs)
except Exception:
    out["unknown_state"] += 1     # ★「引けないものを『背景』に混ぜない」と注釈が在る
    continue
if n.get("actor_id") == "-":
    out["unknown_state"] += 1     # ★表に載っていない
```
| ★同じ欄に入っている2つ | |
|---|---|
| **(a) 表に載っていない** | **★機械は「知らない」と答えた** |
| **(b) 引けなかった** | **★機械は何も答えられなかった**（例外） |

> **★画面はこの欄を「決める表にその状態が載っていないものです」と説明している。**
> **∴ ★(b) が1件でも入ると、★画面は嘘になる**——**★載っていないのではなく、引けなかっただけだからである。**
> **★IMPL の意図は正しい**（「背景に混ぜない」）。**★混ぜる先を間違えている。**

**★今日の実測: 例外は0件である**（`actor_id=="-"` の18件と `unknown_state` の18件が一致）。
> **★今日は見えない。★だから直すべきである。** **★見えない嘘は、出た日に誰も気づかない。**

**【設計:CC-α】★直す形（意味だけ・★MGR の裁定を仰ぐ）**
- **★欄を分ける**（例: `unknown_state` と `unreadable`）。**★区分を増やすことになる ∴ MGR の「区分を増やさない」に触れる。**
- **★私は決めない。** **★ただし「0件だから今はよい」とは書かない。**

## 3. ★満たしている所（★実物で確認した）
| # | 受入 | 私の確認 |
|---|---|---|
| 1a | ID を並べる箇所が無い | **★配信物に `TASK-2DER-xxxxxxxx` が0件**（★異なり0件） |
| 2 | 和が合計と一致 | **117+18+18 = 153**（★私の実測と一致） |
| 3 | 合計が `_active_2der_tasks` と一致 | **★別に数えた数を出していない**（実読） |
| 4・5・7 | 「出せません」＋理由／「対象外」／注記 | **★配信物に在る**（「この一覧の対象外」1件・「Taka の手番かどうかは…出せません」1件） |
| **人称** | 「こちら」「そちら」「あなたの手番」 | **★配信物に0件**（★3語とも） |
| 9 | 配信物の `node --check` | **★IMPL の申告**（私は未実施） |
| 10 | 非回帰 | **★走らせ方が BUILT に書かれた**（`G-74` 側の宿題は果たされた） |

---

## 4. ★画面の検査を私がやった（★Taka 指示・★「人しか出せない」に逃げない）
> **★MGR は D-65 で訂正した:「『画面を実際に開く』を人しか出せない区分に置いたのは誤り。★機械的に出る。★我々がやっていなかっただけ」。**
> **★Taka の指示はこれを指している。★私は `.access_token` を読み、★Basic 認証で実際に取得した**（★`GET /` 200 / 11582 バイト、`GET /command` 200 / 24341 バイト）。

### 4-1. ★問い① 文字が読めるか → **★機械的に答えが出た**
```
再現（配信物・実測）:
  charset=utf-8 ／ ★文字化け(U+FFFD) 0件      ← ★文字そのものは壊れていない
  本文        : font:15px/1.5
  項目名(.k)   : ★12px  ／ 説明文(.mut): ★12px ／ 空欄(.empty): 13px
  ★最小       : 11px（/command に1箇所、/ に3箇所）
  本文の色 #e7ebf0 / 背景 #0f1115 ／ ★薄い字 #9aa4b2（本文より暗い）
```
> **★答え: 文字は壊れていない。★ただし「説明文と項目名が12px、最小11px」である。**
> **★本文15px に対し、★注記だけが小さい。** **★注記こそ、いま我々が「必ず読んでほしい」と足している所である**（「対象外です」「出せません」）。
> **★色の見え方の比（コントラスト比）は★計算していない。★「読みにくい」とは書かない。**

### 4-2. ★問い② 「記録された◯◯は在りません」は回りくどいか → **★機械的に答えが出た。回りくどい**
```
再現（配信物・打ち切り無しで全数）:
  ★同じ「無い」に ★6通りの言い方が在る:
     在りません 5回 ／ ありません 1回 ／ できません 4回 ／ 出せません 2回
     分かりません 3回 ／ 区別できません 3回
  ★最長の否定文は ★80字:
     「進められる DW task は在りません。まだ投入していない／投入したが DW task にならなかった／
       BLOCK された、のいずれかです。この画面では区別で…」
  ★61字: 「記録された authority boundary は在りません（記録されなかったものが在るかは、この画面では分かりません）」
```
> **★答え: 回りくどい。★これは好みではなく、★数えて出る。**
> **★同じ「無い」を6通りで言っている。** **★読む人は毎回、違う言い方を読み解くことになる。**
> **★しかも `authority boundary` `DW task` のような英語が、★日本語の文の中に残っている。**
> **★これは今日ずっと潰してきた形の裏返しである**——**★1つのことを2つの言い方で書いている。**

### 4-3. ★問い③ 現在地が分かるか → **★半分だけ答えが出た**
| ★機械的に出る所 | 答え |
|---|---|
| **集約が出ているか** | **★出ている**（進行中153件・4区分・合計・対象外の注記） |
| **内訳が出ているか** | **★出ていない**（受入6 未達・§1） |
| **★「Taka にとって」分かるか** | **★これは私には出せない。★Taka にしか出せない** |

> **★①②は機械的に出た。★③のうち「Taka にとって分かるか」だけが残る。**
> **★「3問とも人しか出せない」は誤りだった。★2問と半分は、我々が測れた。**
> **★私は D-59 で「人しか出せない」区分を作った側である。★それが我々の言い訳になっていた**（MGR D-65 の指摘どおり）。

## 5. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **配信物の `node --check`** | **★私は実施していない。★IMPL の申告** |
| 2 | **コントラスト比** | **★計算していない。★「読みにくい」と書かない** |
| 3 | **受入1b（画面から155行が消えた）** | **★配信物の ID は元から0件**（JS 描画）∴ **★curl では言えない。★Taka にしか出せない** |
| 4 | **例外が出る条件** | **★今日0件。★どういう時に出るかは調べていない** |

## 6. ★次
```
① MGR の裁定: 受入6 と §2（例外の混入）をこの build で直すか、別 build にするか
② ★直すなら「区分を増やさない」に触れる ∴ ★MGR が決める
③ 直った後: 私が受入確認 → MGR が commit → 私が資料同期
```

---
*CC-α D-79（監査のみ）。★結論=①**受入6 が未達**（「誰がやるか決まっていない」の下に状態の内訳が出ておらず、**固定の説明文が1行 在るだけ**）②**もっと重い: `except Exception` を `unknown_state` に足しており「引けなかった」と「表に載っていない」が同じ欄に入る（今日7つ目）**③**今日は例外0件なので見えないが、例外が出た日に画面が嘘をつく**④**画面の検査を CC-α がやった（Taka 指示）**。★受入6=**JS ではなくサーバ側 `webui.py:148 deferred_summary` を読んで確認**（`out` に**状態の内訳を持つ欄が無い**）、配信物には**固定の説明文だけで「裁定が要る」は0件** ∴ **IMPL は「受入4〜7 文言」と4つまとめて印を付けたが、6 は文言ではなく「数えて出る」であり、まとめて印を付けると中の1つが落ちても分からない**。★**例外の混入**=`except Exception: out["unknown_state"] += 1` と `actor_id == "-"` が**同じ欄**に入り、**(a) 表に載っていない（機械は「知らない」と答えた）**と**(b) 引けなかった（機械は何も答えられなかった）**が混ざる——**画面はこの欄を「決める表にその状態が載っていないものです」と説明しているので、(b) が1件でも入ると画面は嘘になる**。**IMPL の意図（「背景に混ぜない」）は正しく、混ぜる先を間違えている**。**今日の実測では例外0件**（`actor_id=="-"` 18件と `unknown_state` 18件が一致）だが、**今日は見えないから直すべきで、見えない嘘は出た日に誰も気づかない**——**欄を分けると「区分を増やさない」に触れるので CC-α は決めず、ただし「0件だから今はよい」とも書かない**。★満たしている所=配信物に **`TASK-2DER-xxxxxxxx` が0件**／**117+18+18=153 が CC-α の実測と一致**／別に数えた数を出していない／「対象外」「出せません」の注記が在る／**人称語3語とも0件**／`node --check` は IMPL 申告／**非回帰の走らせ方が BUILT に書かれた**。★**画面の検査（Taka 指示・「人しか出せない」に逃げない）**——**MGR が D-65 で「画面を実際に開くを人しか出せない区分に置いたのは誤り。機械的に出る」と訂正した件**であり、**CC-α は `.access_token` を読み Basic 認証で実際に取得**（`/` 200/11582バイト、`/command` 200/24341バイト）。**問い①「文字が読めるか」は機械的に答えが出た**=**charset utf-8・文字化け0件で文字そのものは壊れていない**が、**本文15px に対し項目名と説明文が12px・最小11px**であり、**注記こそ「必ず読んでほしい」と足している所（「対象外です」「出せません」）なのにそこが小さい**（★コントラスト比は計算しておらず「読みにくい」とは書かない）。**問い②「回りくどいか」も機械的に答えが出て、回りくどい**=**同じ「無い」に6通りの言い方**（在りません5／ありません1／できません4／出せません2／分かりません3／区別できません3）、**最長の否定文は80字**、**`authority boundary` `DW task` のような英語が日本語の文に残っている**——**これは好みではなく数えて出るもので、今日潰してきた形の裏返し（1つのことを2つの言い方で書いている）**。**問い③は半分だけ**=集約は出ているが**内訳は出ていない（受入6 未達）**、**「Taka にとって分かるか」だけは CC-α に出せない** ∴ **「3問とも人しか出せない」は誤りで、2問と半分は我々が測れた——CC-α は D-59 で「人しか出せない」区分を作った側であり、それが我々の言い訳になっていた**。★確かめていないこと=`node --check` は未実施／**コントラスト比は未計算**／**受入1b は配信物の ID が元から0件（JS 描画）なので curl では言えず Taka にしか出せない**／例外が出る条件は未調査。*
