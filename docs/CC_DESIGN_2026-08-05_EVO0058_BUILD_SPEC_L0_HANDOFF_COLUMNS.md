# 【BUILD SPEC】`EVO-0058` — **★L0 に受け渡しの2欄を足す（★データだけ・★行は増やさない）**

- `BUILD_ROLE: ★実装源` / **宛: IMPL**（★封入不要＝★純データ・契約なし） / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-05 02:4x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.13）** ／ 親: `ITEM-2DER-EVO-0035` ／ 裁定: MGR 02:3x「★次は本線＝L0 の S14 行に handoff/receipt の2欄」
- **★v1.8 の宣言**: **★核は無い・★2DER 工程 0**（★データ。★判断が無い）
- **★私の予告**: ★worker **0件** ／ ★Claude **定数の2欄 × 18行**（★論理 0行）／ ★走行 **0**（★MGR が次に呼ぶ）
- **★新台帳0・★新計器0・★新エンドポイント0・★行を増やさない・★2枚目の表を作らない**

---

## 1. 変更（★`twoder/route_table.py` の ★定数だけ）

**★S14 の行に2欄。★他の17行は `None`。**

```python
 # S14 の行にだけ 値を入れる
 {"id": "S14", ...,
  "handoff": ["RUNNER", "hand_to_worker"],          # ★渡した側の (component, function)
  "receipt": ["WORKER", "received_from_runner"]},   # ★受け取った側の (component, function)

 # 他の17行（S01..S13, S15..S18）は 一律
  "handoff": None, "receipt": None,
```

```
★値の出どころ = ★★推測ではない ―― ★(A') で ★実際に emit した component/function（★MGR 実測で ★両側が出ている）
★★`None` = ★★『まだ配線していない』であって ★『切れている』ではない（★`reconcile_handoff` が ★NOT_WIRED と返す）
```

## 2. ★★段3 が変わらないことを ★私が先に測った（★実測）

```
★`locate_failure` に ★2欄を足した route を渡し、★実データ2本で ★前後を比べた:
   TASK-2DER-51E58279 → ★前後とも ★LOCATED / S14 / last=S17 / candidates[0]=S10 / actor=2DER(確認済)
   TASK-2DER-98D5F072 → ★前後とも ★BOUNDED / None / last=S16 / candidates[0]=S10 / actor=Claude(未確認)
★★戻り値が ★完全一致することを ★機械で確かめた（★`==` で比較）＝ ★★段3 の契約を ★触る必要が無い
★★★理由 = ★`locate_failure` は ★知らない欄を ★読まない（★契約の逐語どおり）
```

## 3. 受入

```
★(1) ★行数が ★★18 のまま（★増えていない）／ ★id の重複 0
★(2) ★`handoff` / `receipt` が ★入っているのは ★★S14 の1行だけ。★他17行は ★どちらも `None`
★(3) ★★`locate_failure` を ★実データ2本で呼び、★§2 の値と ★一致することを ★逐語で示す
     ★★一致しなければ ★★止まる（★段3 に影響が出ている＝★前提が崩れている）
★(4) ★`from twoder.route_table import ROUTE` が ★通る（★論理0行・★import 副作用なし）
★(5) ★Claude の行数（★論理は 0 と書く）／★(6) ★戻せる ／★(7) ★61本を走らせない ／★(8) ★commit しない
★★★(9) ★出せなかったら『出せなかった』と書いて止まる
```

## 4. ★★この後に来るもの（★本件には ★入れない）

```
★MGR が ★`reconcile_handoff` を ★実データで呼ぶ（★受入は ★C1 SPEC §6）
★★★その時 ★S14 は ★`LINKED` と出る見込み（★B' の実測が ★既に両側一致を示している）
★★★★(b)(c)(d) は ★`NOT_WIRED` と出る ＝ ★★『言い当てられなかった』の機械版 ＝ ★次に配線する区間の名指し
★★★★★『経路表が埋まった』とは ★書かない（★配線は ★18分の1 である）
```

## 5. ★★私から1点（★MGR の『成果物の論理不一致』について）

```
★MGR 報告 =「★PHANTOM と出るべき所が ★UNLINKED 期待／優先順 ④⑤ の扱い」
★★私が契約を読み直した = ★★契約は ★曖昧ではない。★逐語:
   ④ 受け取った event の inputs["received_event_id"] が ★無い → "UNLINKED"
   ⑤ その値と等しい "event_id" を持つ event が ★events の中に無い → "PHANTOM"
   ∴ ★★『引用が無い』(④)と ★『引用したが実在しない』(⑤)は ★別物であり、★順序も ★依頼文に書いてある。
★★★∴ ★これは ★契約の欠陥ではなく ★成果物の実装ミスである ＝ ★MGR の裁定（通常の JUDGE/次版）に ★同意する。
★★★★★私が ★契約を直す必要は ★無い（★直すと ★測る強さが ★下がる）
```

## 6. 禁止

```
★行を増やす ／ ★2枚目の経路表を作る ／ ★.jsonl にする（★台帳を作らない）
★S14 以外の行に ★値を入れる（★配線していない区間に ★在るように見せない）
★`route_table.py` に ★関数・分岐・import を書く（★定数だけ）
★段3 の契約を ★触る（★§2 で ★不要と測った）／ ★C1 の契約を ★直す（★§5）
★『経路表が埋まった』『受け渡しが全区間で見える』と書く
★61本を走らせる ／ ★commit する ／ ★`twoder` 配下で python を動かす
```
