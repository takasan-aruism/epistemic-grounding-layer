# 設計/監査 → MGR（写: Taka / IMPL）: **D-57b — `/` 側も数えた。★10件。まとめているのは3件。★これで「数え切った」と言える**

- `BUILD_ROLE: 参照`（**調査＋文言案。★コードを1行も変えていない・投入していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-29_D57_RULING_COUNT_IS_NOT_DONE_YET.md`

## 0. ★結論
> **`/` 側 10件。** **★まとめているのは3件。**
> **∴ 2画面の合計 ★24件 / まとめている ★8件**（`/command` 5 ＋ `/` 3）。
> **★これで「数え切った」と言える。** **★ただし対象は「この2画面の文言」であって、2DER 全体ではない**（§5-1）。

---

## 1. ★私の計器が粗く、危うく取りこぼした（先に書く）
```
最初に打った: 引用符つきパターン '"[^"]*(無い|なし|…)[^"]*"' で抽出 → ★4件
★誤りの機構: / 側は f-string と文字列連結が混在しており、
             '<span class="dim">なし</span>' のような★シングルクォートの断片を取り落とす。
正しい数え方: 引用符に依存せず★文字そのもので数える → ★10件
```
> **★4件のまま進めば、6件を「無い」と扱っていた。**
> **★本日6回目の、私の計器の問題である。** **6回とも自分で見つけている。**
> **★ただし今回は「少なすぎる」で気づいた。** **0 でなくても疑う必要が在る、という形である。**

## 2. ★10件の判定（★まとめていないものも全部書く）
```
再現: awk 'NR>=347 && NR<617' twoder/webui.py | grep -E '(無い|なし|ありません|未完成|未実行|未配線|不明)'
      （347 = render_report_page の開始行 / 617 = PAGE の開始行）
総件数 10 / 確認 10 / 打ち切り無し
```
| # | 場所 | 文言 | 判定 |
|---|---|---|---|
| **1** | `:385` | `⚠ 未完成 — 完成フラグ N/M` | **まとめていない**（数を出している） |
| **2** | `:396` | 直近 DE → `なし` | **★まとめている** — (1)DE が無い (2)★直近の窓（`[-6:]`）に無いだけ |
| **3** | `:397` | 直近 CHG → `なし` | **★まとめている** — 同上 |
| **4** | `:408` | `押しても実装/benchmark は走りません` | **まとめていない**（動作の説明） |
| **5** | `:409` | `今は押す必要はありません` | **まとめていない**（案内） |
| **6** | `:411` | `なし（REQUIRES_APPROVAL の未完了 item はありません）` | **まとめていない** — **★括弧で条件を名指ししている** |
| **7** | `:445` | off-ramp フラグ → `なし` | **★まとめている** — (1)フラグが1つも無い (2)★取得できなかった |
| **8** | `:464` | `トークン発行済（未実行）` | **まとめていない**（状態の明示） |
| **9** | `:465` | `実装/benchmarkは走りません（実行系未配線）` | **まとめていない** — **★理由を名指ししている** |
| **10** | `:465` | `single-use` の説明文 | **まとめていない** |

**★集計: まとめている 3 / まとめていない 7 / 計 10。**

### 2-1. ★#2・#3 は「窓」の問題である（他と種類が違う）
```
再現（実読）: de  = rep.get("recent_de", [])[-6:]
             chg = rep.get("recent_chg", [])[-6:]
∴ ★直近6件だけを見ている。★6件を超える分は最初から入っていない。
```
> **∴ `なし` は「DE が無い」ではなく★「直近6件の窓に無い」である。**
> **★これは `truncated` と同じ形である**（見た範囲の話を、全体の話として出す）。
> **∴ #12（`全 event の result は OK`）と★同じ class である。**

---

## 3. ★直す文言（`/` 側3件）
| # | 現在 | **直した後** |
|---|---|---|
| **2** | `なし` | **「直近6件の中には在りません」** |
| **3** | `なし` | **「直近6件の中には在りません」** |
| **7** | `なし` | **「記録された off-ramp フラグは在りません」** |

**★#2・#3 は「窓」を明示する。** **「無い」と言わず「この窓には無い」と言う。**
**★#7 は `/command` の #7・#10 と同じ形にする**（「記録された◯◯は在りません」）。

## 4. ★2画面まとめて直す対象（MGR「1回で直す」）
| 画面 | # | 種別 |
|---|---|---|
| `/command` | **12** | **★偽になる文（最優先・MGR 裁定）** |
| `/command` | 3 / 4 / 7 / 10 / 11 | まとめている5件 |
| **`/`** | **2 / 3** | **★窓の問題**（#12 と同 class） |
| **`/`** | **7** | まとめている |
> **★合計 9件**（偽になる文1 ＋ 窓3 ＋ まとめ5）。**★1回で直す。**

## 5. ★未確認（「数え切った」の範囲を明示する）
| # | 未確認 | 誰が・いつ |
|---|---|---|
| **1** | **★対象は「この2画面が出す文言」である。** **API の返り値・ログ・文書は数えていない** | **★「2DER 全体を数え切った」ではない。** MGR が要ると言えば広げる |
| 2 | **#7（`/`）の実例**（off-ramp フラグが空になる依頼） | **作らない** |
| 3 | **#2・#3 の「6件を超えて隠れた」実例** | **作らない**（`/api/control` を叩けば分かるが、★叩いていない） |
| 4 | 画面をブラウザで見ていない | D-45 から継続 |

## 6. ★私は直さない
- **文言案のみ。** **★コードを1行も変えていない。**
- **★9件を1回で直すか分けるかは MGR が決める。**

---
*CC-α D-57b。★結論=`/` 側は**10件**で**まとめているのは3件** ∴ 2画面の合計**24件・まとめている8件**（`/command` 5 ＋ `/` 3）で**「数え切った」と言える**——**ただし対象はこの2画面の文言であって 2DER 全体ではない**。★**私の計器が粗く危うく取りこぼした**=引用符つきパターンで抽出して**4件**と出たが、`/` 側は f-string と文字列連結が混在し `'<span class="dim">なし</span>'` のようなシングルクォートの断片を取り落とすため、**引用符に依存せず文字そのもので数えると10件**——**4件のまま進めば6件を「無い」と扱っていた。本日6回目の計器の問題で6回とも自分で見つけているが、今回は「0」ではなく「少なすぎる」で気づいた**。★判定=まとめている3件は #2 直近 DE「なし」／#3 直近 CHG「なし」（**どちらも `[-6:]` の窓の問題**——`なし` は「DE が無い」ではなく「直近6件の窓に無い」で、**`truncated` と同じ形＝#12 と同 class**）／#7 off-ramp フラグ「なし」（フラグが1つも無い／取得できなかった）。まとめていない7件も全部列挙し、#6 と #9 は**括弧で条件・理由を名指ししている**ので問題ない。★直す文言=#2・#3 は「**直近6件の中には在りません**」（窓を明示し「無い」と言わない）、#7 は「**記録された off-ramp フラグは在りません**」（`/command` の #7・#10 と同じ形）。★2画面まとめて直す対象は**合計9件**（偽になる文1＋窓3＋まとめ5）で**1回で直す**。★未確認=**対象はこの2画面が出す文言であり API の返り値・ログ・文書は数えていない（「2DER 全体を数え切った」ではない）**／#7 の実例と #2・#3 の「6件を超えて隠れた」実例は**作らない**（`/api/control` を叩けば分かるが叩いていない）／画面をブラウザで見ていない。★CC-α は文言案のみでコードを1行も変えておらず、9件を1回で直すか分けるかは MGR が決める。*
