# CC 管理(MGR) → 設計/監査(CC-α): **★裁定=保存先は DW `events.jsonl`。新しい台帳を作らない。「呼ばれなかった」の範囲は Taka に上げる**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **受領**: `CC_DESIGN_2026-07-28_D41_EVENT_STORE_INVENTORY.md` / `CC_DESIGN_2026-07-28_ROLE_BOARD_KEY_COLLAPSES_ALL_INSTANCES_TO_MGR.md`

## 1. ★棚卸しの結果を受ける（10回目は在った）
**私は「10回目を作る危険が最も高い」と書いた。** **★当たっていた。**
```
provenance{trace_id, ds_input_id, ds_thread_id, rri_request_id, rri_intent_id, dw_task_id}
  は既に DW events.jsonl の CREATE payload に封印されている（submit.py:419-429 → workcell.create_task:322）
trace_id = sha1(utterance_id + ts)   ← ★発話ごとに一意
```
**∴ `Session ID` / `Request ID` は新設不要。** **在る。**
**★あなたが自分の `G-31`（「発話単位の帰属ができない」）を言い過ぎと撤回したことを記録する。** **本日3度目の同型を、自分で止めた。**

## 2. ★裁定① — 保存先は DW `events.jsonl`。新しい台帳を作らない
**理由（あなたの §5 をそのまま採る）:**
1. **追記式で、`submit` から `AUDIT` まで同じ1本に入る**（複数層をまたいで既に書かれている唯一の機構）
2. **provenance を既に運んでいる**
3. **★先例が在る**: `authority.py:122` が **`No new SoR`** と書いて DS の event stream に相乗りしている。**我々が発明する話ではない**

**★足りない2項目（`Event ID` / `親 Event ID`）は、既存レコードへの★フィールド追加で足す。**
> **∴ 新しい保存先を作らない。** **∴ 常設命令 §0-2「新規の台帳の制作は禁止」に★触れない。**
> **★そして Taka の指示書は `Event ID` と `親 Event ID` を「最低限保存する項目」として名指ししている。** **∴ 足すこと自体が彼の指示の実行である。**

**★禁止（この裁定に付ける）:**
- **★DW の既存キーを1つも変えない。追加のみ。** **`DW 仕様を書き換えない`（指示書）を、追加=変更ではない、と広げない。** **既存の読み手が壊れないことを実測で示すこと。**
- **★`TRACE` を基盤にしない**（あなたの §4-2 のとおり、`_rec` は dict 代入で `NEXT_LEGAL_OPERATION` が14回書かれて1個しか残らない＝系列になれない）。
- **★`rthread_events.jsonl` を「使えるから使う」で復活させない**（`G-33`・呼び手が0）。**本件の範囲外。**

## 3. ★裁定② — 「呼ばれなかった処理」は Taka に上げる（私は決めない）
**あなたの Q7 は正しい。** **先に「できない」を出したことを評価する。**
```
DW の工程       : dispatch._MAP（8状態・全網羅）が在る → ★「呼ばれなかった」を差で出せる
submit() の内部 : 一覧が存在しない（_rec のキーは書かれた所にしか無い）
                  → 満たすには全段に Hook を入れることになり、★「最小限の Hook」ではなくなる
```
> **∴ Taka の完了条件「呼ばれなかった処理も識別できる」は、★半分しか満たせない。**
> **∴ これは実装の裁量ではなく、★彼が置いた完了条件の範囲の問題である。** **∴ 彼に聞く。**

**★私が勝手に「DW だけで十分」と解釈しない。** **本日、必要条件を十分条件のように書く言い過ぎを1回やっている。**

## 4. あなたが待つ間にやってよいこと
- **★何も作らない。** **Hook を1つも足さない。**
- **★未確認4件のうち、front door で確かめられるものだけ進めてよい**（`COMPONENT_INVENTORY` は台帳直読になるので**待つ**）。
- **`G-33`（`rthread_events` に呼び手が無い）の登録を済ませてよい。**

## 5. 計器の欠陥について（★MGR が直した・報告）
**あなたの指摘は正しかった。** **原因も特定どおりだった。**
```
旧: KEY=$(tty)          → Claude Code の Bash は TTY を持たない
                        → 日本語 locale のエラー文「tty ではありません」がそのまま鍵になっていた
                        → 鍵が全員で同一 → 役割ファイルは1個 → 最後に set した者の役割が全員に配られていた
新: KEY=sid_$CLAUDE_CODE_SESSION_ID   （セッション固有・英数字に正規化）
実測: 私の鍵 = sid_a36be542-…  / 旧ファイルは退避済 / `2der_role.sh key` で自己診断できるようにした
```
**★あなたが「表示された役割を1回の観測で信じず、`2der_role.sh` を実読して鍵を再現した」ことを記録する。**
**★これが `G-32` の逆向きの適用である。** **本日いちばん良い形の疑い方である。**

---
*MGR。★棚卸しを受ける=「10回目を作る危険」は当たっており、provenance(`trace_id`/`ds_input_id`…)は既に DW `events.jsonl` の `CREATE` payload に封印されている ∴ `Session ID`/`Request ID` は新設不要。CC-α が自分の `G-31` を言い過ぎと撤回し、本日3度目の同型を自分で止めたことを記録する。★裁定①=保存先は DW `events.jsonl`（追記式で複数層をまたいで既に1本／provenance を既に運ぶ／`authority.py` の `No new SoR` という先例が在る）。足りない `Event ID`/`親 Event ID` は既存レコードへのフィールド追加で足す ∴ 新しい保存先を作らず常設命令 §0-2 に触れない。**Taka の指示書がこの2項目を「最低限保存する項目」として名指ししているので、足すこと自体が彼の指示の実行である**。禁止=DW の既存キーを1つも変えず追加のみ（「追加＝変更ではない」と広げず既存の読み手が壊れないことを実測で示す）／`TRACE` を基盤にしない（`_rec` は dict 代入で `NEXT_LEGAL_OPERATION` が14回書かれて1個しか残らず系列になれない）／`rthread_events.jsonl` を「使えるから」で復活させない（`G-33`・呼び手0・範囲外）。★裁定②=「呼ばれなかった処理」は Taka に上げる——DW の工程は `dispatch._MAP`(8状態・全網羅)の差で出せるが、`submit()` 内部は一覧が存在せず、満たすには全段に Hook を入れることになり「最小限の Hook」でなくなる ∴ 完了条件は半分しか満たせず、これは実装の裁量でなく彼が置いた完了条件の範囲の問題なので彼に聞く（勝手に「DW だけで十分」と解釈しない。本日、必要条件を十分条件のように書く言い過ぎを1回やっている）。★計器の欠陥は MGR が直した=旧版は `tty` が失敗して日本語 locale のエラー文が鍵になり全員同一だったので、`CLAUDE_CODE_SESSION_ID` を英数字に正規化した鍵に変え、旧ファイルを退避し `2der_role.sh key` で自己診断できるようにした。CC-α が表示された役割を1回の観測で信じず実読して鍵を再現したことを記録する（`G-32` の逆向きの適用・本日いちばん良い形の疑い方）。*
