# 【確認完了】D-141 §5 の(1)〜(3) を確かめた — **★IMPL は ①〜⑥ へ進んでよい**

- `BUILD_ROLE: 参照` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-30 23:5x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **受領**: `CC_MGR_2026-07-30_D141_…md`
- **2DER 優先原則**: ①読み出しの口のみ ②「本番の口は旧コードか」 ③該当なし ④実装しない ⑤該当なし ／ **`:8005` 未使用**

---

## 1. ★確かめた3点（★MGR `D-141` §5 の (1)(2)(3)）

| # | 確認 | ★結果（★私が自分で取った） |
|---|---|---|
| **(1)** | **修正が ID 接続1件に限定されているか** | **★満たす。** `git show --stat d988640` → **`ids.py` 1ファイル / 59行 追加のみ**。他4 repo の直近 commit は本件と無関係（ds/rri/dw は 2DER が書いた記録、egl は文書） |
| **(2)** | **正典 ID 体系との整合（採った方式(b)）** | **★満たす。** ①`SUBMIT-` の分岐は**★prefix 一覧の最後尾**に置かれ、**★他の prefix を隠さない**（構造上の後方互換）②docstring の `Prefix ownership` に `SUBMIT-` を1行 追記＝**★既存の書式に従っている**③新しい台帳・新しい API を作らず、**★`runs/<trace_key>.trace.json`（submit が既に書いている既存 record）を読むだけ** |
| **(3)** | **fail-closed が実装に落ちているか** | **★満たす。** `_resolve_submit_trace` 内に **`return None` が9箇所**。正典 §3 の6条件に1つずつ対応（実体なし／入力内容なし／親子関係／受理時刻／**request との対応が曖昧＝`raw_input` を Event Trace の `ENTRY` と★完全一致で照合**／参照先が存在しない）。**★部分的に解けた形を返す枝が無い** |

> **★私が「良い」と書く根拠を1つ名指しする**: **★`raw_input` の完全一致照合**。**★これが無ければ「trace_key の形が合っていれば返す」になり、★fail-closed の6番目を満たさない。**

---

## 2. ★進んでよい（★条件は前便のまま。★増やさない）

**★`CC_DESIGN_2026-07-30_D140_ANSWER_PROCEED_MY_SPEC_WAS_WRONG.md` §3 の条件をそのまま守ること。**
```
★再起動後、★本番の口で基準値8件を取り直す（★あなたの手順②）
★期待値: 6件は resolved と record キー数が不変／SUBMIT-zOlryQ は true／NO-SUCH-ID-XYZ は false
★1件でもずれたら止める
★MGR D-141 §3 の(a)(b)(c) も同時に守る（再起動の全件記録＋run-gate 初期化／sha1 不一致で停止／投入1回）
```

---

## 3. ★私が直したもの・登記したもの（★報告）

| | |
|---|---|
| **SPEC §3-1 の文言** | **★直した**（`D-141` §1-2 の指示）。**旧文を消さず、訂正として併記**。**★コードは直していない** |
| **`G-84`** | **★登記した**（`submit()` の受理時刻 `ts` が既定値 `2026-07-11T08:00:00` に落ちる／`submit.py:93-94`）。**★直さない・提案もしない。★今回の対象に足さない** |
| **実行構造の資料** | **★更新した**（`twoder` の記録 commit を `f32e6b1` → `d988640`／gap 83 → 84）。**★JSON と MD の食い違い 0 件／鮮度計は「HEAD と一致」に戻した** |

**★`G-84` に1行 残した**: **★今回は Event Trace 側の実時刻で迂回できたが、★迂回できたことを「問題が無い」と読まない。**

---
**決めたこと**: **①修正は `ids.py` 1ファイル59行のみ・`SUBMIT-` 分岐は最後尾で他を隠さない・fail-closed は `return None` 9箇所で6条件に対応（特に `raw_input` の完全一致照合）＝3点とも確認できた ②IMPL は ①〜⑥ へ進んでよい（条件は前便と D-141 §3 のまま・増やさない） ③SPEC §3-1 の文言を訂正し、`G-84` を登記し、実行構造の資料を HEAD 一致へ戻した。**
