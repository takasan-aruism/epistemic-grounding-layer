# 設計/監査 → MGR（写: Taka / IMPL）: **D-58 監査 — ★9件すべて直っている。★私の計器は2件を見られなかった（分岐が出ないため）**

- `BUILD_ROLE: 参照`（**監査。コードを1行も変えていない・投入していない・★資料をまだ同期していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-29 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_IMPL_2026-07-29_D58_NINE_WORDINGS_BUILT.md`

## 0. ★結論
> **① 9件すべて直っている**（`【実】`）。**② 旧文言は9件とも0件。**
> **③ ★私の最初の計器は2件を「★無し」と出した。** **★誤りである。** **配信画面では★出ない分岐だからである。**
> **④ ★資料はまだ同期していない**（MGR の新しい順番に従い、★MGR の commit を待つ）。

---

## 1. ★私の計器が2件を見られなかった（先に書く）
```
私が最初に打った: 配信画面（GET /command + GET /）の HTML に新文言が在るか
  → 「直近6件の中には在りません」        ★無し
  → 「記録された off-ramp フラグは在りません」★無し
★誤りの機構: どちらも `/` 側で、★リストが空のときだけ出る分岐である。
             実データが空でないので、★HTML に現れない。
正しい計器: ソースを見る
  webui.py:396 …or '<span class="dim">直近6件の中には在りません</span>'
  webui.py:397 …同上
  webui.py:445 …or '<tr><td class="dim">記録された off-ramp フラグは在りません</td>…'
∴ ★3箇所とも直っている。
```
> **★配信画面での「無し」は、「直っていない」の証拠にならない。** **「その分岐が出ていない」だけである。**
> **★これは本日7回目の、私の計器の問題である。** **★今回は「0」でも「少なすぎる」でもなく、★「見えない場所を見ようとした」形である。**

### 1-1. ★同じ誤りは旧文言の検査にも在った（自分で気づいた）
> **私は旧文言も配信画面で数え「0件」を得た。** **★同じ理由で、これも根拠にならない。**
> **∴ ソースで数え直した**（§2）。

## 2. ★受入2（旧文言が1つも残っていない）— ソースで検算
```
再現: grep -c で旧文言をソースから数える
  "なし</span>"                → 0
  '<td class="dim">なし</td>'  → 0
  'class="dim">なし'           → ★1
```
**★残っていた1件の実体（実読）**
```
webui.py:411  pend_card = '…<div class="dim">なし（REQUIRES_APPROVAL の未完了 item はありません）</div>…'
```
| | |
|---|---|
| **これは9件の対象か** | **★対象ではない。** D-57 §2 の **#6** であり、**「まとめていない」と判定した**（括弧で条件を名指ししている） |
| **∴** | **★残っているのが正しい。** **触っていないのが正しい。** **★これを「直し漏れ」と書かない** |

> **★危うかった。** **「`なし` が1件残っている」を、そのまま欠陥として報告しかけた。**
> **★自分の判定表（15件は触らない）に戻って確かめた。**

## 3. ★承認で足された受入（`render_report_page` の変更が文字列リテラルだけか）
```
再現: git diff -- webui.py の hunk のうち Python 範囲(347–616)に入るもの
@@ -393,8 +393,8 @@ def render_report_page(rep):
  - de_html  = … or '<span class="dim">なし</span>'
  + de_html  = … or '<span class="dim">直近6件の中には在りません</span>'
  - chg_html = … or '<span class="dim">なし</span>'
  + chg_html = … or '<span class="dim">直近6件の中には在りません</span>'
@@ -442,7 +442,7 @@
  - … (flag_rows or '<tr><td class="dim">なし</td><td></td></tr>') …
  + … (flag_rows or '<tr><td class="dim">記録された off-ramp フラグは在りません</td>…') …
```
> **∴ ★変わったのは引用符の中だけである。** **`[-6:]` を触っていない。** **ロジックを1行も変えていない。**
> **★受入（承認で足されたもの）は満たされている。**

## 4. ★受入の判定
| # | 受入 | 判定 |
|---|---|---|
| 1 | 9件の新文言がソースに在る | **★満たされた**（`【実】`。配信画面で9件・ソースで残り2件） |
| 2 | 旧文言が1つも残っていない | **★満たされた**（`【実】`。残った1件は★対象外の #6） |
| 3 | 実例が在るもの（`/command` #C）が実際に出る | **★満たされた**（配信画面に「記録された authority boundary は在りません」在り） |
| 4 | `ok`/`bad` を足していない | **`【伝聞】`**（IMPL 実測。**★私は差分で数えていない**） |
| 5 | 既存が不変 | **`【伝聞】`** |
| 6 | 非回帰 91/7 | **`【伝聞】`** |
| **追** | **`render_report_page` の変更が文字列リテラルだけ** | **★満たされた**（`【実】`・§3） |

## 5. ★資料の同期はまだしていない（MGR の新しい順番）
```
新しい順: BUILT → 受入の確認 → ★MGR がコードを commit → ★その後に CC-α が同期
```
> **★本文書が「受入の確認」である。** **∴ 次は MGR の commit である。**
> **★commit を待ってから同期する。** **★今 同期すると、また偽の赤を作る。**

## 6. ★未確認
| # | 未確認 | 誰が・いつ |
|---|---|---|
| 1 | **受入4・5・6 を私は再実行していない**（`【伝聞】`） | CC-α / 要請時 |
| 2 | **`/` 側の3件が実際に画面へ出るところを見ていない**（分岐が出ないため） | **★作らない。** データが空になれば出る |
| 3 | **偽になる文（#12）が実際に偽になる場面**（`truncated` の run が無い） | **★作らない** |
| 4 | **画面をブラウザで見ていない** | **★D-45 から続く。MGR いわく Taka だけが解ける** |

---
*CC-α D-58 監査。★結論=①9件すべて直っている ②旧文言は9件とも0件 ③**私の最初の計器は2件を「無し」と出したが誤りで、配信画面では出ない分岐だから** ④**資料はまだ同期していない**（MGR の新しい順番に従い commit を待つ）。★**計器の誤りの機構**=「直近6件の中には在りません」「記録された off-ramp フラグは在りません」はどちらも `/` 側で**リストが空のときだけ出る分岐**であり、実データが空でないので HTML に現れない——ソースを見れば `webui.py:396/397/445` の3箇所とも直っている ∴ **配信画面での「無し」は「直っていない」の証拠にならず「その分岐が出ていない」だけ**。**本日7回目の計器の問題で、今回は「0」でも「少なすぎる」でもなく「見えない場所を見ようとした」形**。**同じ誤りは旧文言の検査にも在り**、配信画面での「0件」も根拠にならないのでソースで数え直した。★受入2 の検算でソースに `class="dim">なし` が**1件残っていた**が、実体は `webui.py:411` の「なし（REQUIRES_APPROVAL の未完了 item はありません）」＝**D-57 §2 の #6 で「まとめていない」と判定した対象外の1件**であり、**残っているのが正しく触っていないのが正しい**——**危うくこれを直し漏れとして報告しかけたが、自分の判定表に戻って確かめた**。★承認で足された受入（`render_report_page` の変更が文字列リテラルだけか）は、Python 範囲の hunk が `@@ -393` と `@@ -442` の2つで**変わったのは引用符の中だけ**、`[-6:]` を触らずロジックを1行も変えていないので**満たされている**。★受入は 1・2・3・追加分が `【実】`、4・5・6 は `【伝聞】`（**私は差分で `ok`/`bad` を数えていない**）。★**資料の同期はまだしていない**——本文書が「受入の確認」であり次は MGR の commit で、**今 同期するとまた偽の赤を作る**。★未確認=受入4・5・6 は再実行していない／`/` 側3件が実際に画面へ出るところを見ていない（**作らない**）／偽になる文が実際に偽になる場面（`truncated` の run が無い・**作らない**）／**画面をブラウザで見ていない（D-45 から続き Taka だけが解ける）**。*
