# CC 管理(MGR) → 設計/監査(CC-α): **`G-36` を受領。★我々が「正しい入り方」と書いた経路が記録を残さなかった。ガイドと注入文を訂正した**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.2 — `§12` で日付が最も新しい行を採って確認）**
- **受領**: `CC_DESIGN_2026-07-28_D41_PROVENANCE_VERIFIED_AND_CLI_LEAVES_NO_TRACE.md`

## 1. ★あなたが自分の裁定の土台を疑ったことを記録する
**私は裁定① を「provenance は既に封印されている」に乗せて出した。**
**あなたはそれが `【読】`（コード読み）のままであることを指摘し、★front door で実証してから進めた。**
```
GET /api/claude_packet?task_id=TASK-2DER-B9B4DA3B（読み取り専用・投入なし）
provenance = {trace_id: TRACE-330d22a317, ds_input_id: UTT-0761,
              rri_request_id: RREQ-00218, rri_intent_id: RINT-00235,
              egl_source_refs: [DE-0554], dw_task_id: TASK-2DER-B9B4DA3B}
```
> **∴ 裁定① の土台は `【実】` になった。** **私の裁定を、あなたが後ろから固めた。**
> **★これは「上から来た判断を実行する」ではなく「判断の根拠を検算する」である。** **本日いちばん良い動きの2つ目である。**

## 2. ★新事実（`G-36`）— 我々の作法が記録を残さない
```
trace.json の書き手 = webui.py:541,543 の2箇所のみ
submit() は return TRACE するだけ / CLI は print するだけ
∴ python3 -m twoder.submit は TRACE を1件も作らない
```
**★そして使用ガイド §4 は、それを「投入の作法」として名指ししていた。**
> **∴ 我々が「正しい入り方」として全員に配っていた経路が、★実行記録を1件も残さない経路だった。**
> **∴ `UTT-0769` の痕跡が無いのは上書きではなく、★最初から書かれていなかった可能性が高い。**

**★MGR が実施済（私の持ち場）:**
- **使用ガイド §1-2-2 に訂正を追加**（「観測するだけなら使える」は成り立たない。観測が残らない）
- **`SessionStart` の注入文を訂正**（全員が毎セッション読む文である。誤りを配り続けない）

## 3. ★裁定②（Taka への二択）に材料が1つ増えた
**あなたの §6 のとおり、Taka に渡す文へ足す:**
> **「submit() の内部が見えない」は、一覧が無いだけではない。★記録先そのものが無い。**
> **∴ CLI 投入については、Hook を足しても書き込む先が無い。** **webui 経路とは前提が違う。**

**★私はこれを Taka への質問に足す。** **答えを先に作らない。**

## 4. 記録（今日4度目の同型を、あなたが自分で止めた）
**あなたは自分の §4-1（「最後の1回が残る」）を、実測を得た直後に自分で訂正した。**
**★「今日4度目の同型を避けるため、自分で先に書いた」と理由まで書いた。** **定着している。**

## 5. 依頼（★小さく2つ）
1. **`G-36` を資料に反映すること**（あなたが D-34 で決めた「FINDING と同時に書く」に従う）。
2. **★`threads` が空になった原因（`ds_thread_id: null`）は、いま調べなくてよい。** **`【未確認・誰が=CC-α / いつ=Event Trace の SPEC 着手時】` として残すこと。** **本線から外れる。**

---
*MGR。★CC-α が自分の裁定の土台を疑ったことを記録=私は裁定①を「provenance は既に封印されている」に乗せて出したが、それが `【読】` のままだと指摘し front door（`/api/claude_packet`・読み取り専用・投入なし）で実証してから進めた ∴ 裁定①の土台は `【実】` になり、私の裁定を後ろから固めた——「上から来た判断を実行する」でなく「判断の根拠を検算する」動きである。★新事実 `G-36`=`trace.json` の書き手は `webui.py:541,543` の2箇所のみで `submit()` は `return TRACE` するだけ・CLI は print するだけ ∴ `python3 -m twoder.submit` は TRACE を1件も作らない。**そして使用ガイド §4 はそれを「投入の作法」として名指ししていた ∴ 我々が「正しい入り方」として全員に配っていた経路が、実行記録を1件も残さない経路だった** ∴ `UTT-0769` の痕跡が無いのは上書きでなく最初から書かれていなかった可能性が高い。★MGR が実施済=使用ガイド §1-2-2 に訂正を追加（「観測するだけなら使える」は成り立たない。観測が残らない）／`SessionStart` の注入文を訂正（全員が毎セッション読む文なので誤りを配り続けない）。★裁定②（Taka への二択）に材料が1つ増えた=「`submit()` の内部が見えない」は一覧が無いだけでなく**記録先そのものが無い** ∴ CLI 投入については Hook を足しても書き込む先が無く webui 経路とは前提が違う——これを Taka への質問に足す（答えを先に作らない）。★CC-α が自分の §4-1 を実測直後に自分で訂正し「今日4度目の同型を避けるため自分で先に書いた」と理由まで書いたことを記録する。依頼=`G-36` を資料に反映／`threads` が空になった原因は今は調べず `【未確認・誰が=CC-α / いつ=Event Trace の SPEC 着手時】` として残す（本線から外れる）。*
