# CC 管理(MGR) → 設計/監査(CC-α): **★D-51 — Taka 調査依頼「Embedding 勘定科目システム 現況調査」。★実装変更禁止**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **典拠: Taka（2026-07-29・逐語 §1）**

## 1. Taka の依頼（逐語）
> **件名: Embedding勘定科目システム 現況調査（実装変更禁止）**
> **目的: 2DER初期設計で定義した「Embeddingを利用した勘定科目システム」が、現在の実装・台帳・EGL・RRIに対してどこまで存在しているかを客観的に確認する。今回の作業は調査のみ。Python変更・仕様変更・新規設計・配線変更は禁止。**
>
> **調査項目**
> **1. 初期設計の所在** — Embedding勘定科目に関する設計書・仕様・議論ログを探す。関連文書を列挙し、現在も有効か失効かを判断する。
> **2. 実装状況** — Embeddingを利用するコードが存在するか。存在する場合、現在どこから呼ばれているか。未使用コード・廃止コードならその旨を書く。
> **3. Ledgerとの接続** — Ledger登録時に勘定科目をEmbeddingから決定する経路が存在するか。存在しない場合、どこで止まっているか。
> **4. EGLとの接続** — EGL登録時にEmbedding勘定科目を利用しているか。利用していないなら、その理由を証拠付きで示す。
> **5. RRIとの接続** — RRIがEmbedding勘定科目を参照・更新・利用する設計または実装が存在するか。実際に配線されているか。
> **6. Execution Architecture** — Execution Architecture（または同等資料）上で、この機能がどこへ位置付けられているか。Capability・Edge・Cycleとして定義されているか。
>
> **報告形式**: 各項目について**設計あり／なし・実装あり／なし・配線あり／なし・実運用あり／なし・証拠（ファイル・コミット・DE・実装箇所）**を必ず分けること。
> **禁止事項**: 推測で補完しない／「たぶん」「あるはず」で終わらせない／調査中に修正しない／**配線したくなっても止める**。
> **最終成果物**: 「Embedding勘定科目システム現況報告」——実装・配線・運用の現在地を、証拠付きで一枚で把握できる形で提出すること。

## 2. ★私が渡す材料（★答えではない。手がかりである。必ず自分で確かめること）
**本日〜昨日の調査で、関連しうるものが3つ出ている。** **★私はどれも「勘定科目」として確かめていない。**
| # | 手がかり | 出典（本日の文書） |
|---|---|---|
| **1** | **`rri/rri/request_thread.py` の段2a は「accounts 機械核（chart 検証／suspense 決着／account 保存 load-bearing）」と自称している** | `CC_DESIGN_2026-07-29_D47_PROVENANCE_OF_REQUEST_THREAD.md`（git log の commit 題名） |
| **2** | **`egl/structure/` に `s_account_axes` / `s_embed_axes` / `s_account_axis_names` 等が在り、互いに呼び合う研究スクリプトである。本番コードからの参照は0件** | `CC_DESIGN_2026-07-27_D18_ANSWER_TWO_MECHANISMS.md` §1 |
| **3** | **段2b の作り直しは「勘定科目の軸」が対象で、`stage2b PLAN §0` が「account 次元は soft」と書いている** | `CC_DESIGN_2026-07-29_D47_...` §3 |

> **★これらは私が他人の文書から拾ったものである。** **★私は原典を1つも実読していない。**
> **∴ 材料として渡すが、★これを結論にしないこと。** **本日、私は伝聞をそのまま渡して4回外している。**
> **★もし材料が誤っていたら「誤っていた」と書くこと。** **私の材料に合わせない。**

## 3. ★私が付ける条件（Taka の禁止に加えて）
| # | 条件 |
|---|---|
| **1** | **★走査に `head` / `tail` / `-m` / `limit` を使わない**（v2.6 §3-8） |
| **2** | **★各走査に「総件数・確認件数・打ち切り有無」を書く**（v2.7 §3-9） |
| **3** | **★「無い」と書くときは探索範囲を明示する**（§3-7）。**本日9回「既存の道が既に在った」を数えている** |
| **4** | **★台帳を直読しない。** front door（`/api/resolve`）を使う。**引けないなら「引けない」と書く** |
| **5** | **★Taka の報告形式（設計／実装／配線／実運用／証拠の5分割）を1項目も崩さない** |
| **6** | **★「Embedding」と「勘定科目」を分けて数えること。** **片方だけ在る可能性が在る**（例: 勘定科目は在るが Embedding を使っていない、等） |

## 4. ★順序（中心を止めない）
**D-50（依頼を `run_id` で開く）が実装中である。** **★本件はそれを止めない。**
**★ただし Taka が直接出した調査依頼なので、D-50 の BUILT 待ちの間に進めてよい。**
**★どちらかを選べと言われたら、私に聞くこと。** **あなたが決めない。**

---
*MGR。D-51。★Taka 調査依頼（逐語保存）=「Embedding 勘定科目システム 現況調査（実装変更禁止）」。6項目（初期設計の所在／実装状況／Ledger との接続／EGL との接続／RRI との接続／Execution Architecture 上の位置づけ）を、**設計あり・なし／実装あり・なし／配線あり・なし／実運用あり・なし／証拠**の5分割で報告し、推測で補完せず「たぶん」で終わらせず調査中に修正せず**配線したくなっても止める**。★MGR が渡す材料は**答えではなく手がかり**（`request_thread.py` の段2a が「accounts 機械核」と自称／`egl/structure/` の `s_account_axes`・`s_embed_axes` 等は研究スクリプトで本番参照0件／段2b の作り直しは「勘定科目の軸」が対象で「account 次元は soft」）——**MGR はどれも原典を実読しておらず他人の文書から拾ったものなので、結論にせず自分で確かめ、誤っていたら「誤っていた」と書き MGR の材料に合わせない**（本日、伝聞をそのまま渡して4回外している）。★MGR の追加条件=走査に `head`/`tail`/`-m`/`limit` を使わない／各走査に総件数・確認件数・打ち切り有無を書く／「無い」と書くときは探索範囲を明示する（本日9回「既存の道が既に在った」を数えている）／台帳を直読せず front door を使い引けないなら「引けない」と書く／Taka の報告形式5分割を1項目も崩さない／**「Embedding」と「勘定科目」を分けて数える**（片方だけ在る可能性が在る）。★順序=D-50 が実装中だが本件はそれを止めず、Taka が直接出した調査依頼なので BUILT 待ちの間に進めてよい。どちらかを選べという状況になったら MGR に聞き、CC-α が決めない。*
