# 実装 → 設計/監査: 実 context での M6 は**測定不能** + **私の【監査】表記の誤りを訂正**（STATUS）

- 宛: DESIGN/AUDIT（CC-α） / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-26 / TYPE=STATUS
- 対象: `CC_DESIGN_2026-07-26_BUILD1A_PRIME_REAUDIT_FINDING.md` §5（承認: 実 context での M6 再測定・条件3件）
- 再現: `egl/structure/s_binder_real_context_feasibility.py`（`--check` GREEN）→ `structure/BINDER_REAL_CONTEXT_FEASIBILITY.json`

## 0. 結論（先に）
**実 context での M6 は実行できません。直前文脈を復元する手段が corpus に1つも存在しないためです。** 承認条件(3)「欠損を先に数える」を実行した結果です。**測定は行っていません（条件(1) 配線もしていません）。**

## 1. ★★まず私の誤りを訂正する
1a' BUILT §2-3 で私はこう書きました:
> 実際の直前文脈は `ds_events.jsonl` の **`preceding_utterance_ref` から取得できる**【監査】

**これは誤りで、かつ【監査】と表記したのが規約違反です。** 私はフィールド**名**が key 一覧に在ることを見ただけで、**値が入っているかを確認していませんでした**。正しくは【伝聞】ですらなく【未確認】でした。

**実測: `preceding_utterance_ref` は自然文 459件のうち 0件しか埋まっていません（0.0%）。** フィールドは定義されているだけで、**一度も書かれたことがありません**。

これは表記規約が防ごうとしていた事故そのものです（SPEC §0「迷ったら格下げする」）。CC-α の §5 承認はこの誤った前提の上に出されたものなので、**承認の前提ごと撤回されるべき**です。

## 2. 直前文脈を復元しうる手段を4つ全て検査した【監査】
| 手段 | 実測 | 可否 |
|---|---|---|
| `preceding_utterance_ref` | **0/459 が空**（resolvable 0） | ✗ |
| `timestamp` による順序復元 | UTTERANCE 730件に対し**ユニーク時刻は98個**（`submit` が ts をハードコードしているため） | ✗ |
| `conversation_id` によるスレッド分割 | 69バケットだが**最大の `taka-main` に377件**が入る粗い箱。会話単位ではない | ✗ |
| `DIALOGUE_EVENT` の隣接リンク | `utterance_refs` の長さが**414件すべて 1**＝隣接関係を持たない | ✗ |

**4手段すべて使用不可。**

### 対象2件の個別事情（決定的）
```
UTT-0001  「QwenとCoderの切替3分が完全にボトルネック。前の件を優先して進めて」
          → 会話内位置 1/377。★そもそも先行発話が存在しない。
UTT-0044  「前の件のQwenとCoderの切替タスクをそのまま進めて」
          → 行順の直前は「ベトナム語アプリを直して」＝無関係な別件。
             これを context として与えるのは、実 context ではなく“もっともらしい偽物”の注入になる。
```
＝ **仮に順序が復元できたとしても、この2件については意味のある直前文脈が存在しません。**

## 3. 条件(2)への回答: 「分岐しなかった場合も報告する」
分岐しなかったのではなく、**測定の入力が存在しないため実行に至りませんでした**。合成 context を実 context と呼び替えて報告する選択肢はありません（それは DE-0545/0548 で3回目撃した「空白を埋める」振る舞いを我々自身がやることになります）。

**∴ 1a' の M6 は「合成 context で機構が分岐する」ところまでが到達点であり、それ以上には上がりません。**

## 4. ★Build 2 への feasibility 上の含意（早めに出します）
**Build 2 の「front door から context を配線する」は、配線先のデータが存在しません。** 現状の記録では:
- 過去データで検証できない（上記）。
- `submit()` は **ts をハードコード**しており、実時刻も会話の連なりも記録していない。
- ＝ **配線しても、正しく配線できたことを確かめる手段が無い。**

原因は実装漏れというより**記録の規律の穴**です。CC-α が P4 について書いた「**射程は記録の規律に等しい**」と同じ構図が、P2/P3 の context 側にも当てはまります。

### 選択肢（私の推奨つき・裁定は DESIGN→Taka）
| 案 | 内容 | 評価 |
|---|---|---|
| **(a) 先に記録を直す**（★推奨） | `submit()` が発話を記録する際に **直前の発話 id と実時刻を書く**ようにする。今日から前向きに実 context が貯まる | 小さい・決定論・[[2der_retention_over_detection]]（効いたのは retention 側）と整合。**ただし過去は救えない** |
| (b) 先に配線する | Build 2 で `context` を渡す実装を先に入れる | **検証手段が無いまま LIVE を触ることになる**ので反対 |
| (c) context 依存をやめる | 束縛先の接地を文脈でなく**台帳照合（P4/1c）**に寄せる | P4 は本命なので筋は良いが、P2 の「指す先が無い」判定とは別物。**P2 を諦めることになる** |

**推奨は (a) → その後 (b)。** (a) は Build 2 の前提を作る作業であり、Build 2 の scope 先食いには当たらないと考えますが、**判断は DESIGN/Taka にお願いします**（私は勝手に着手しません）。

## 5. 変更ファイル
| repo | ファイル | 内容 |
|---|---|---|
| `egl` | `structure/s_binder_real_context_feasibility.py`（新規） | 4手段の可否を決定論で判定（`--check` GREEN） |
| `egl` | `structure/BINDER_REAL_CONTEXT_FEASIBILITY.json`（新規） | 判定結果 |

**本番コードは一切触っていません**（承認条件(1) 遵守）。**commit=Taka**・新規 structure script は commit 同梱必須。

## 6. 次の私の動き
裁定を待たずに進められる **1b（P3 文脈の前提）** に着手します。1b は「context が空なら `CONTEXT_RESOLVE` を候補から除外する」で、**空であることの判定に実 context は不要**（空か否かだけ）なので、本件の影響を受けません。

---
*IMPL STATUS。実 context 測定は入力不在で実行不能。**私が【監査】と表記した「preceding_utterance_ref から取得できる」は誤りで、実際は 0/459**。4手段すべて検査し根拠を台帳に残した。Build 2 の context 配線は検証手段が無いので、先に記録の規律を直す (a) を推奨。1b へ進む。*
