# 設計/監査 → MGR（写: Taka / IMPL）: **D-77 — D-76 受入。★答えは1件も変わっていない（私が別経路で確かめた）。★ただし受入5 だけは私に再現できない**

- `BUILD_ROLE: 参照`（**監査のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **受領**: `CC_IMPL_2026-07-30_D76_PASS_EVENTS_BUILT.md` / `CC_MGR_2026-07-30_D76_APPROVED_SAY_WHICH_BARRIER.md` / `CC_MGR_2026-07-30_D76_RECEIVED_BUT_155_VS_160_IS_UNEXPLAINED.md`

## 0. ★結論
> **① 受入1 は満たされている。★私は IMPL の基準を使わなかった。★git HEAD から元のコードを取り出し、自分で基準を作って比べた。★差分0行。**
> **② MGR §2 の 160/155/153 は★不具合ではない。★3つとも正しく、★別々のものを数えている**（§3・1行で答える）。
> **③ ★受入5（非回帰98本）だけ、★私には再現できない。★走らせ方がどこにも書かれていない**（§5・`G-74` を起こす）。
> **④ ★私の SPEC の一文が言い過ぎだった。「変更前に取らないと二度と取れない」は★誤りである**（§6）。

---

## 1. ★受入1 — ★私は IMPL と違う経路で作った（★これが独立検算の中身である）
```
再現（★私が実行・読み取りのみ）:
  git show HEAD:dw/workcell.py / dw/dispatch.py を作業場へ取り出す（★変更前のコード）
  署名を確認 → def derive_state(task_id) / def next_legal_operation(task_id)   ← ★events= が無い
  DW_DATA_DIR=/home/takasan/dev-workcell で走らせ、155件の
    (task_id, state, actor_id, ★nlo["claude_barrier"]) を保存        → 4.748 秒
```
| 比べたもの | 結果 |
|---|---|
| **変更前（★私が HEAD から作った）vs 変更後（events を渡す版）** | **★差分0行**（155件すべて一致） |
| **変更前 vs 変更後（★引数を省略した版）** | **★差分0行**（＝受入3） |
| 所要 | **4.748 秒 → ★0.004 秒**（省略版は 4.825 秒＝★今までどおり読んでいる証拠） |

> **★これは「同じ答えが出たか」ではない。** **★誤り方が違う経路を通ったかである**（D-59）。
> **★IMPL の基準は「変更前に自分で取ったもの」。★私の基準は「git が保存していた元のコード」。★出所が違う。**
> **★もし IMPL の基準取得そのものに誤りが在れば、★私の側とは一致しない。★一致した。**

### 1-1. ★比べた `claude_barrier` は `next_legal_operation` が返す方である
> **★`/api/state` が返す同名の値とは別物である**（あちらは `actor_role in ("MANAGER","CLAUDE_SENIOR")` を計算し直した値）。
> **★D-74 で 36 と 18 を食い違わせた名前が、★受入の中に無注釈で入っていた。★MGR の指摘は正しい。★私の欠陥である。**

## 2. ★受入2 — ★ただし SPEC に無い関数が1つ増えている（★見逃さずに書く）
```
再現: cd /home/takasan/dev-workcell && git diff -- dw/workcell.py dw/dispatch.py
削除された行は★すべて「署名の行」と「docstring の行」だけである。★呼び手の書き換えは0件。
```
| ★私の SPEC が名指しした | 実際に口が付いたもの |
|---|---|
| `derive_state` / `next_legal_operation` | `derive_state` / `next_legal_operation` / **★`completion_blockers`** |

**【設計:CC-α】★これは私の SPEC に無い。★黙って通さない。** **★そのうえで、★受け入れる。理由:**
1. **`completion_blockers` は `next_legal_operation` の中から呼ばれている**（`READY_FOR_UPPER_REVIEW` の枝）。**★口を付けなければ、そこだけ読み直しが残る。**
2. **★その枝は実際に通る**（`READY_FOR_UPPER_REVIEW` が★5件 在る）。**★死んだ枝ではない。**
3. **形が同じである**（省略可能・省略時の振る舞い不変）。**★MGR の裁定 §2 の「許す」の形そのものである。**
> **★ただし「SPEC が2件と書いたのに3件だった」は事実として残す。** **★私の SPEC が呼び先まで見ていなかった。**

## 3. ★MGR §2 への回答（★1行で・★推測で埋めない）
```
再現（★決定論・私が実行）:
  event 総数 734 / task_id 全部 ★160 / TASK-2DER で始まる ★155 / うち COMPLETE・BLOCKED ★2
```
> **★1行: 160 は「全部の task」、155 は「`TASK-2DER` で始まる task」、153 は「155 から COMPLETE と BLOCKED を除いたもの」である。**
> **★3つとも正しい。★不具合ではない。★定義が違うだけである**（∴ MGR §3 の「止まってよい場所7」には当たらない）。
> **★しかし、どれも「件数」としか呼ばれていなかった。** **★今日ずっと潰してきた「1つの名前が2つを指す」の、★集合版である**（MGR の見立てどおり）。

**★D-73 でどれを使うか（★設計として決める・MGR の裁定を仰ぐ）**
| | |
|---|---|
| **★153（`_active_2der_tasks` が返すもの）を使う** | **★理由: 画面は「いま進められる依頼」を出す場所である。★終わったものと詰まったものを数に入れると、「155件 在る」と読まれる** |
| **★そのうえで、★数の横に「何を数えたか」を書く** | **★「進行中の依頼 153件（終了・停止を除く）」。★数だけ置かない** |

## 4. ★受入6 — ★私は配信物を取れない。★代わりに、変わりようが無いことを示す
```
再現: curl http://100.107.6.119:8770/command → ★HTTP 401（★私は token を持っていない）
```
> **★∴ 私は配信物の JS を検査できない。★「通った」と書かない。**
> **★代わりに決定論で示す: `twoder` の変更は `submit.py` の1ファイルだけである**（`git status --porcelain` → ` M submit.py` のみ）。
> **★`webui.py` は1文字も変わっていない。∴ 配信される JS は変わりようが無い。**
> **★これは「検査した」ではない。「変わっていないから検査が要らない」である。★別のことである。**

## 5. ★受入5 — ★私には再現できない（★`G-74` を起こす）
```
再現（私が試したこと・すべて失敗）:
  cd /home/takasan/egl/docs && pytest      → ★collection error（★cwd を間違えた・私の失敗）
  cd /home/takasan/twoder  && pytest       → ★ImportError（operator.py が stdlib を隠す・★既知の罠）
  cd /home/takasan && pytest twoder/tests  → 23 passed（★98本ではない）
  cd /home/takasan/dev-workcell && pytest  → ★INTERNALERROR（test_*.py が script であり test ではない）
```
```
再現（走らせ方が書かれているか・★打ち切り無しで数えた）:
  「98本」を含む文書: ★12件
  そのうち pytest の再現コマンドを含むもの: ★0件
```
> **★「非回帰98本 91 passed / 7 failed」は★12の文書で引用されているが、★どうやって走らせるかがどこにも書かれていない。**
> **★∴ 私は確認できない。★IMPL の申告をそのまま通すことになる。**
> **★これは受入の中で唯一、★私が独立に確かめられないものである。**

**★`G-74`（新規）: 非回帰98本の走らせ方が、どの文書にも記録されていない。**
**★∴ 「91/7」は12の文書で引用されているのに、★実装者以外の誰も再現できない。**
> **★これは「テストが無い」ではない。★「テストは在るが、実装者しか走らせられない」である。**
> **★独立監査の受入に、★独立に確かめられない項目が入り続けていた。**

## 6. ★私の SPEC の一文が誤りだった（★訂正する）
```
私が書いた（§2-1）: 「★①は変更前に取らないと二度と取れない。★先に取ること。」
```
> **★これは言い過ぎである。** **★commit されていなければ、★git HEAD から元のコードを取り出して、★後からでも作れる。★私が実際にそうした。**
> **★正しくは「★commit されると取れなくなる。★commit 前に取ること」である。**
> **★「二度と取れない」と書いたことで、★もし IMPL が取り忘れていたら、★我々は諦めていたかもしれない。**

## 7. ★私の計器の失敗（★自己申告）
| # | |
|---|---|
| **1〜3** | **★cwd を間違えた（本日★3回）**。`egl/docs` で `git diff` / `pytest`、`egl` で `grep` → **★すべてエラーで返った** |
| **4** | **`twoder` を cwd にして `pytest` → ★既知の罠**（使用ガイド §4）。**★D-74 に続き2回目である** |
> **★4回とも「0件」ではなく「エラー」で返った。★だから気付けた。** **★もし「0件」で返っていたら、★私は「無い」と書いていた。**
> **★これは D-72 の「0 が返ったら計器を疑う」が、★たまたま効かなくてもよかった形である。★運が良かっただけである。**

## 8. ★受入の判定
| # | 受入 | 判定 |
|---|---|---|
| **1** | 155件の分類が変更前後で完全一致 | **★満たす（私が別経路で確認・差分0）** |
| 2 | 既存の呼び手が1つも変わっていない | **満たす**（★ただし SPEC に無い関数が1つ増えた・§2） |
| 3 | 省略時の振る舞いが同じ | **★満たす**（差分0・所要も 4.8秒 のまま） |
| 4 | 所要を測った | 満たす（9.880 → 0.102 秒。**★結果であって目的ではない**） |
| **5** | 非回帰 91/7 | **★私には再現できない**（`G-74`）。**★IMPL の申告として記録する** |
| 6 | 配信物の `node --check` | **★私は取れない**（401）。**★`webui.py` 不変により変わりようが無い**（§4） |

**【設計:CC-α】★D-76 を受け入れる。** **★MGR の commit を推す。**
**★ただし受入5 は「確認した」ではなく「★確認できなかった」と書いて残す。** **★通した理由は「IMPL がそう言ったから」である。**

## 9. ★次
```
① MGR が commit（dev-workcell 2ファイル / twoder 1ファイル）
② 私が資料を同期（★G-74 を足す）
③ D-73 を再開 — ★§3 の「どの集合を数えるか」を先に決める
```

---
*CC-α D-77（監査のみ）。★結論=①**受入1 は満たされており、IMPL の基準を使わず git HEAD から元のコードを取り出して自分で基準を作って比べ、差分0行** ②**MGR §2 の 160/155/153 は不具合ではなく3つとも正しく別々のものを数えている** ③**受入5（非回帰98本）だけ再現できず、走らせ方がどこにも書かれていない（`G-74`）** ④**私の SPEC の一文「変更前に取らないと二度と取れない」は誤り**。★受入1=`git show HEAD:` で変更前のコード（`events=` が無い署名）を取り出し `DW_DATA_DIR` を実データに向けて 155件の `(task_id, state, actor_id, nlo["claude_barrier"])` を 4.748秒 で保存 → **渡す版と差分0行・省略版とも差分0行**、所要 **4.748秒 → 0.004秒**（省略版 4.825秒＝今までどおり読んでいる証拠）——**これは「同じ答えが出たか」ではなく「誤り方が違う経路を通ったか」（D-59）であり、IMPL の基準は「変更前に自分で取ったもの」、CC-α の基準は「git が保存していた元のコード」で出所が違う**。比べたのは **`next_legal_operation` が返す方**で `/api/state` の同名とは別物（**D-74 で 36 と 18 を食い違わせた名前が受入に無注釈で入っていた=CC-α の欠陥**）。★受入2=削除行はすべて署名と docstring で**呼び手の書き換えは0件**だが、**SPEC が名指ししたのは2関数なのに `completion_blockers` にも口が付いている**——**黙って通さず書いたうえで受け入れる**（`next_legal_operation` の `READY_FOR_UPPER_REVIEW` 枝から呼ばれ、**その枝は実際に5件通る死んでいない枝**で、形も裁定 §2 の「許す」そのもの）——**ただし「SPEC が2件と書いたのに3件だった」＝SPEC が呼び先まで見ていなかったことは事実として残す**。★MGR §2 への1行回答=**160 は「全部の task」、155 は「`TASK-2DER` で始まる task」、153 は「155 から COMPLETE と BLOCKED を除いたもの」で3つとも正しく不具合ではない**（∴ 止まってよい場所7 に当たらない）が、**どれも「件数」としか呼ばれておらず「1つの名前が2つを指す」の集合版**。**D-73 は 153 を使い、数の横に「進行中の依頼 153件（終了・停止を除く）」と何を数えたかを書くことを推す**（数だけ置かない）。★受入6=**CC-α は 401 で配信物を取れないので「通った」と書かない**代わりに、**`twoder` の変更は `submit.py` 1ファイルのみで `webui.py` は1文字も変わっていないから配信 JS は変わりようが無い**——**これは「検査した」ではなく「変わっていないから検査が要らない」であり別のことである**。★受入5=**4通り試してすべて失敗**（cwd 誤り／`operator.py` の既知の罠／`twoder/tests` は23本で98本ではない／`dev-workcell` の `test_*.py` は script）、**「98本」を含む文書は12件だがそのうち pytest の再現コマンドを含むものは0件** ∴ **`G-74`＝非回帰98本の走らせ方がどの文書にも記録されておらず、「91/7」は12文書で引用されているのに実装者以外の誰も再現できない**——**「テストが無い」ではなく「テストは在るが実装者しか走らせられない」で、独立監査の受入に独立に確かめられない項目が入り続けていた**。★**CC-α の SPEC の一文が誤り**——「変更前に取らないと二度と取れない」は言い過ぎで、**commit されていなければ git HEAD から後でも作れる（実際にそうした）**、正しくは**「commit されると取れなくなる。commit 前に取ること」**——**「二度と取れない」と書いたせいで、もし取り忘れていたら諦めていたかもしれない**。★計器の失敗の自己申告=**cwd 誤り本日3回**＋**`twoder` を cwd にする既知の罠が D-74 に続き2回目**——**4回とも「0件」ではなく「エラー」で返ったから気付けたのであり、「0件」で返っていたら「無い」と書いていた。運が良かっただけである**。★判定=受入1/2/3/4 満たす、**受入5 は再現できず IMPL の申告として記録**、受入6 は変わりようが無いことで代替 ∴ **D-76 を受け入れ MGR の commit を推すが、受入5 は「確認した」ではなく「確認できなかった」と残し、通した理由は「IMPL がそう言ったから」である**。★次=①MGR が commit ②CC-α が資料同期（`G-74` を足す）③**D-73 再開の前に「どの集合を数えるか」を先に決める**。*
