# 【BUILD SPEC / `EVO-0029`】`JUDGE_REQUIRED` に手番を与える — **★意図の記録は無かった（範囲つき）**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-01 08:2x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: ★v1.1 / 2026-08-01）** ／ **裁定**: `D-205` §3・`D-206` §4
- **この .md がまだ .md である理由**: 指示を台帳へ渡す口が無いため（★`EVO-0022` / C-2）
- **★増える管理対象 0** ／ **★私はコードを1行も変えていない**

---

# 0. ★★`EVO-0028` の独立監査（★`D-206` §1「監査が済むまで DONE にしない」への回答）

> ### **★受入①〜④ すべて ○。★私も独立に確かめた。★`DONE` にしてよい。**

```
★差分（★私が読んだ）: ★`submit.py` のみ。★`+33/-11`。★行数が増えて見えるのは
   ★既存の `try/except` を ★`else:` へ入れ直した★字下げのため ＝ ★中身は1文字も変わっていない
★★判定の3条件が ★SPEC §2-1 のとおり入っている（★`_prog` / `contract_none` / `_rest.strip()`）
★★★★壊れた契約は ★`except: _contract_none = False` ＝ ★★「作る側」へ倒している（★私が指定した安全側）
★★マーカーは ★`progress_seal.BEGIN_PR = "<<<2DER:PROGRESS>>>"` ＝ ★私の指定と一致
★★★件数: ★私の実測 ★181 は ★IMPL の「★180→181（受入④の本文つき投入）」と ★一致する
★★★★★★予告を1つ外し、★外れたと書いている ／ ★滞留が 6→7 に増えたことを ★隠さず書いている ＝ ★正しい
```

---

# 1. ★★「口を置かなかったのは意図か」（★`D-205` §3・★1回だけ・★範囲つき）

> ### **★探した範囲に、★意図の記録は ★無かった。**

```
★探した範囲（★全数・打ち切り無し）:
   ① ★`git -C dev-workcell log -S'JUDGE_REQUIRED' -- dw/dispatch.py` → ★★0件
      ＝ ★`dispatch.py` で `JUDGE_REQUIRED` に触れた commit は ★1つも無い
      ∴ ★★`_MAP` から「外した」のではない。★★最初から「入れていない」である
   ② ★`egl/docs/*.md` で `JUDGE_REQUIRED` に触れた文書 ★55件 を走査 → ★「意図」「わざと」に当たる記述 ★0件
★★★★★逆に、★★決定的な記述が ★我々自身の過去文書に在った（★CC-α `D-74`・逐語）:
   「★`nlo["claude_barrier"]` は `_MAP.get(state, (...,True))` の★既定値で `_MAP` に無い状態すべてに True を付ける
    ∴ ★★`JUDGE_REQUIRED` の True は「人を待っている」ではなく「★この状態を知らない」であり、
    ★依存待ちに入れると★知らないものを人待ちと呼ぶことになる」
   ★同文書の末尾（逐語）:「★`JUDGE_REQUIRED` が `_MAP` に無い理由は分からない」
★★★★★★∴ ★(B)「探した範囲に無い」である。★★かつ ★「安全装置だから空けてある」という読みは
   ★我々自身の過去の実読と★食い違う。★★私は「意図ではない」とは断定しない。★記録が無いと書く。
```

---

# 2. ★2案（★最小・可逆。★`D-205` §3 のまま）

| | 変更 | 大きさ | 戻し方 | 既存への影響 |
|---|---|---|---|---|
| **★案A（推す）** | **`dispatch.py:_MAP` に1行 足す**<br>`"JUDGE_REQUIRED": ("UPPER_REVIEW", "CLAUDE_SENIOR", "TASK+RUNS+TEST_RESULT", True)` | **★1行** | **★その1行を消す** | **★表に1行 増えるだけ。★分岐を増やさない**（★既存7行と同じ形） |
| 案B | `next_legal_operation` に `JUDGE_REQUIRED` の分岐を足す | 3〜5行 | その分岐を消す | ★表と分岐の2箇所に知識が散る |

```
★★★私は ★案A を推す。★理由: ★`_MAP` は ★★状態→(操作・actor・入力・barrier) の★表である。
   ★`JUDGE_REQUIRED` は ★状態である ∴ ★表に入るのが★元の設計に沿う。★案B は★同じ知識を2箇所に置く。
★★★★★決めるのは MGR である。
```

## 2-1. ★案A が通ると何が起きるか（★機構から。★1回の観測ではない）
```
★`next_legal_operation` → ★`UPPER_REVIEW` / `CLAUDE_SENIOR` を返す（★既定の `BLOCKED` に落ちなくなる）
★★`webui.ingest`（`webui.py:380-381` 逐語）は ★`UPPER_REVIEW` を★既に受け取れる
   → ★`W.record_upper_review(task_id, result["review"], TS, "claude-senior")`
★★★`record_upper_review` は ★`_require_state(task_id, "UPPER_REVIEW")` を通る ∴ ★案A が要る
★★★★記録後 ★`derive_state` は ★`ph == "UPPER_REVIEW"` → ★`READY_FOR_UPPER_REVIEW`
★★★★★★∴ ★S-3（`EVO-0027`）は ★止まりどころが変わり、★先へ進める。
```

## 2-2. ★★v1.1「作る書く口の、★読む口はどこか」（★`D-206` §4 の要求）
```
★書く口: ★`POST /api/ingest`（`actor_role=CLAUDE_SENIOR` / `result.review`）
★★★読む口:
   ★① ★`GET /api/claude_packet?task_id=…` — ★★既に「Bounded review をしてくれ」と返している（★実測済）
   ★② ★`GET /api/state?task_id=…` の ★`findings`（`webui.py:221` 逐語 `"findings": W._latest_findings(view)`）
   ★★★★★不足（★正直に書く）: ★★`upper_reviews` を ★front door から★読む口は ★見つけていない
      （★`webui.py` に `upper_review` は ★`:83`（説明文）と ★`:381`（書く側）の2箇所のみ）
   ★★★★∴ ★★「書いたら読める」は ★★半分しか成り立たない。★受入⑤ でそこを測る（★§3）
```

---

# 3. ★受入（★v1.1 を当てた形。★緩めない）
```
★① ★`TASK-2DER-B37727E3`（★いま `JUDGE_REQUIRED`）で ★`GET /api/state` の `next_operation` が
     ★`BLOCKED` → ★`UPPER_REVIEW` に変わる
★② ★`POST /api/ingest`（`CLAUDE_SENIOR` / `review`）が ★`ValueError` を返さず ★通る
★③ ★その後 ★`dw_state` が ★`READY_FOR_UPPER_REVIEW` に変わる
★④ ★戻せる（★`_MAP` の1行を消したら ★元の `BLOCKED` に戻る）——★★戻して確かめ、★また足す
★★★⑤ ★★書いた review が ★front door の★どれかの口から★読めるか。★読めなければ
     ★★「★書けたが読めない」と★書く（★★`EVO-0028` の同型を★5回目にしない）
★★★★★投入前に予告を書く: ★①〜⑤ の予想 ／ ★変更行数（★1行の見込み）
```

# 4. ★手順 ／ 5. ★禁止 ／ 6. ★報告
```
【手順】① 予告 → ② `_MAP` に1行 → ③ webui 再起動 → ④ `GET /api/state` で受入① → ⑤ ingest で受入②③
        → ⑥ ★1行を消して受入④（★戻ること）→ ⑦ また足す → ⑧ ★読む口を探して受入⑤
        ★★gate が閉じたら同一依頼文の再投入（★第1章）。★回数と理由を書く
【禁止】★`workcell.py` を変える ／ ★新しい状態語・台帳・マーカーを作る ／ ★`ingest` に分岐を足す
        ★S-3 の依頼文に触る ／ ★commit する ／ ★★review の中身を ★実装が創作する
        （★★処置と同じく ★設計/監査が書き ★MGR が承認する。★受入②は ★通ることだけ見る＝★中身は仮でよい）
【報告】1 ★変更行数 ／ 2 ★受入①〜⑤ ／ 3 ★予告の当否 ／ 4 ★戻して戻ったか ／ 5 ★読む口が在ったか
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---

# 7. ★MGR の計器の欠陥（★`D-206` §5 の1行）— **★設計として形を書く**
```
★★MGR の監視は ★自分が書いた分と ★2DER が動いた分を ★区別できない。
★★★形: ★`status_note` に ★既に `actor=` が入っている（★C-1 の実測・逐語
   `actor=Claude stage=RECORD via=front_door …`）∴ ★★新しい欄も計器も要らない。
★★★★`actor=Claude` / `actor=MGR` を ★「我々が書いた」、★それ以外を ★「2DER が動いた」として★分けて鳴らす。
★★★★★★私の監視（計画8件）も ★同じ条件を入れる（★私が自分で直す。★IMPL の作業に足さない）。
```

---
**決めたこと**: **①`EVO-0028` を独立に監査した——差分は `submit.py` のみで中身は字下げ以外 変わっておらず、判定3条件も安全側の倒し方も指定どおり、件数も私の実測 181 と一致する。予告の外れと滞留 6→7 を隠さず書いている。`DONE` にしてよい ②「口を置かなかったのは意図か」は (B)「探した範囲に無い」——`dispatch.py` で `JUDGE_REQUIRED` に触れた commit は 0件（最初から入れていない）、docs 55件にも意図の記述は 0件。逆に我々自身の過去文書（`D-74`）が「`_MAP` に無い状態の `claude_barrier=True` は『人待ち』ではなく『この状態を知らない』」と実読で書いており、「安全装置だから空けてある」という読みと食い違う。ただし「意図ではない」とは断定せず「記録が無い」と書く ③案A（`_MAP` に1行）を推す。`_MAP` は状態の表であり `JUDGE_REQUIRED` は状態だから。案B は同じ知識を2箇所に置く。決めるのは MGR ④案A が通ると `ingest` が既に受け取れる `UPPER_REVIEW` が次段になり、`record_upper_review` の `_require_state` も通って S-3 が先へ進む ⑤v1.1 を当てた——書く口は `/api/ingest`、読む口は `claude_packet` と `/api/state` の `findings` だが、`upper_reviews` を front door から読む口は見つかっていない。∴「書いたら読める」は半分しか成り立たず、受入⑤ でそこを測る（同型を5回目にしない）⑥受入は5つで、④は「1行を消したら戻ること」を実際に確かめる ⑦review の中身は実装が創作しない。処置と同じく設計/監査が書き MGR が承認する。受入②は通ることだけ見るので中身は仮でよい ⑧MGR の計器は `status_note` に既に入っている `actor=` で分ければ直る。新しい欄も計器も要らない。私の監視にも同じ条件を私が自分で入れる。**
