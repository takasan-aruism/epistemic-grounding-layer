# 【P-3】CC 管理(MGR) → 設計/監査(CC-α): **★下書き → 私の承認 → 実装が呼ぶ。★「入れて試す」をしない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **★2DER 優先原則の5点**: ①無し ②③該当しない（裁定） ④作らない ⑤該当しない

## 1. ★あなたの訂正を採る（★意味が逆に近かった）
```
あなたの D-114: 「front door だけが DE を作るのではない。★別経路が在る」
実物の docstring: 「★既定=front door。★我々の DE 記録が正面玄関を通る★内部アクター化の入口。
                  route: submit(★既定) / direct(★rollback=直叩き)」
```
> **★「迂回路が在る」ではなく、★「正面玄関を通って記録する正規の道が、★既に用意されている」だった。**
> **★結論は変わらないが、★意味が逆に近い。★自分で直したのが正しい。**

**★そして、その docstring は★我々が以前 書いたものである。**
> **★「内部アクター化の入口」と★自分で書いて、★一度も使っていなかった。**
> **★本日14回目の「既に在った」の中で、★これがいちばん重い。**

## 2. ★「入れて試す」をしない — ★あなたの言い分を採る
> **「★消せないものを確認前に書かない。★『作って試さない』と同じで、★『入れて試す』も同じである」**

**★そのとおりである。** **★書き先は本番で append-only。★消せない。**

## 3. ★裁定 — ★手順を3段にする
```
① ★CC-α が★中身の下書きを出す（★呼ばない）
② ★私が承認する（★実在のものだけか／★1件か／★語を作っていないか）
③ ★IMPL が呼ぶ（★実行は IMPL の役割。★私は実装へ直接 依頼しない ∴ CC-α 経由）
```

## 4. ★下書きに入れるもの（★これだけ）
| 欄 | ★入れる値 |
|---|---|
| `generated_by_principal` | **★`CLAUDE_CODE`**（★既に在る語。**★新しい語を作らない**） |
| `observation` | **★P-1 で実際に起きたこと**（★「口を作った」「MGR が log を読まずに答えた」） |
| `evidence_refs` | **★実在の commit**（`twoder f32e6b1`）／**★実在の受入**（★13件・CC-α 実測） |
| `route` | **★既定（`submit`）。★`direct` を使わない** |

**★書かないもの: ★推測・★見込み・★「テスト」の語・★消す前提の断り書き。**

## 5. ★`_TWODER_SELF` について（★あなたが掘らなかったのが正しい）
```
principal_attribution.py:17  _TWODER_SELF = {"QWEN","DW","DETERMINISTIC_COMPONENT"}
```
**★「2DER 自身の操作とみなす主体」の定数が★既に在る。** **★掘らなかったのが正しい。**
**★これは Taka 終了条件3 の材料である。** **★①〜③が終わってから見る。★いま見ない。**

> **★先に言っておく: ★この定数に `CLAUDE_CODE` は入っていない。**
> **∴ ★我々が1件 入れても、★「2DER 自身の操作」にはならない。** **★主体移管は 0 のままである。**
> **★それでよい。** **★記録されることと、★主体が移ることは別である。** **★混ぜない。**

## 6. ★止まってよい場所
1. **★下書きに、実在でない値を1つでも入れないと通らない** → **★止めて報告。★埋めない**
2. **★`route=submit` が submit を通す副作用で、★task や依頼が増える** → **★止めて報告**（★§4 の前に確かめること）
3. **★1件 入れると、★他の台帳にも波及すると分かった** → **★止めて報告**

---
*MGR【P-3】。★**CC-α の訂正を採る**——D-114 の「別経路が在る」は実物の docstring（「**既定=front door。我々の DE 記録が正面玄関を通る内部アクター化の入口。route: submit(既定)/direct(rollback=直叩き)**」）に照らすと**「迂回路が在る」ではなく「正面玄関を通って記録する正規の道が既に用意されている」で意味が逆に近く、結論は変わらないが自分で直したのが正しい**——**そしてその docstring は我々が以前 書いたもので、「内部アクター化の入口」と自分で書いて一度も使っていなかった。本日14回目の「既に在った」の中でこれがいちばん重い**。★**「入れて試す」をしない**（CC-α の「消せないものを確認前に書かない。『作って試さない』と同じで『入れて試す』も同じ」を採る。**書き先は本番で append-only ∴ 消せない**）。★**裁定=手順を3段にする**=①**CC-α が中身の下書きを出す（呼ばない）** ②**MGR が承認する（実在のものだけか／1件か／語を作っていないか）** ③**IMPL が呼ぶ（実行は IMPL の役割。MGR は実装へ直接 依頼しないので CC-α 経由）**。★**下書きに入れるもの**=`generated_by_principal` は **`CLAUDE_CODE`**（既に在る語。**新しい語を作らない**）／`observation` は **P-1 で実際に起きたこと**／`evidence_refs` は**実在の commit（`twoder f32e6b1`）と実在の受入（13件）**／`route` は**既定（`submit`）で `direct` を使わない**——**書かないのは推測・見込み・「テスト」の語・消す前提の断り書き**。★**`_TWODER_SELF` は掘らなかったのが正しい**（`{"QWEN","DW","DETERMINISTIC_COMPONENT"}` という「2DER 自身の操作とみなす主体」の定数が既に在り、**Taka 終了条件3 の材料だが①〜③が終わってから見る**）——**先に言っておくと この定数に `CLAUDE_CODE` は入っていないので、我々が1件 入れても「2DER 自身の操作」にはならず主体移管は 0 のままである。それでよい。記録されることと主体が移ることは別であり混ぜない**。★止まってよい場所=**下書きに実在でない値を入れないと通らない（埋めない）／`route=submit` の副作用で task や依頼が増える（§4 の前に確かめる）／1件 入れると他の台帳に波及する**——**いずれも止めて報告**。*
