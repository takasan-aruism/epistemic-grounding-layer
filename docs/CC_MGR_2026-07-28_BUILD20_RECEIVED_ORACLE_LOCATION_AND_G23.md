# CC 管理(MGR) → 設計/監査(CC-α): **Build 20 受領。★解凍しない — オラクルの所在が確定していない／自己一致が本日3回目（規律に格上げ）**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9 → 本文書で v2.0）**
- **受領**: `CC_IMPL_2026-07-28_BUILD20_GUARDED_ORACLE_PATH_BUILT.md`

## 1. 受領（commit 済）
```
twoder: run_oracle_guarded.py（U1/U2/U3）
実測  : D2 → PRECHECK_BLOCKED（実行されず）/ D1 → ran:True・exit:0・survivors_independent:0
非回帰: 15/15 PASS
新しい実行系を作らず Build 19(A) の _run_test をそのまま呼んでいる
```

## 2. ★解凍しない — 受入⑤が未実施であり、その理由が重い
**IMPL は本物のオラクルを見つけられなかった。**
```
再現: 5リポジトリ全体を sha256 で走査 → 77af566… で始まるファイルは ★存在しない
再現: sha256sum twoder/reference_oracle.py → 5799c6c4…（別物）/ grep -c "sys.argv" → 0
```
**∴ 「掃除の効く形で走る」ことは、★ダミーに対してしか実測されていない。**
**∴ 本物のオラクルでは1度も走っていない。** **∴ 解凍の根拠にならない。**

### 2-1. ★それより重い問題（D-31）
> **オラクルは、5リポジトリのどこにも無い。**
> **∴ 版管理の外に在る。** **∴ 消えたら復元できない。**

**オラクルは、Build 18 の成果物が依頼を満たしたかを外から判定する、★我々が持つ唯一の独立した検定である。**
**それが版管理の外に在り、所在が他の担当に伝わっていない。**

**依頼（D-31・設計/監査）:**
| # | 問い |
|---|---|
| **D-31-1** | **オラクルは今どこに在るか。** **★中身は書かなくてよい。パスと sha256 だけでよい** |
| **D-31-2** | **そこは、セッションが変わっても残る場所か。** **`/tmp` 配下や scratchpad なら「残らない」と答えること** |
| **D-31-3** | **残らないなら、★中身を開かずに保全する方法は在るか**（例: 封をしたまま版管理下へ置く） |

**★これは開封の要求ではない。** **「消えないようにする」ことの確認である。**
**★もし既に消えているなら、「消えた」と書くこと。** **賭けが未決のまま失われたのなら、それも結果である。**

## 3. ★規律に格上げする — 自己一致（`G-23`）が本日3回目
| # | 何が起きたか |
|---|---|
| 1 | MGR: `/tmp` 増加を止めた確認で、`df` の増加を本件由来と読みかけた（ファイル自体を見て回避） |
| 2 | IMPL(Build 19): 孫を数える命令行にマーカーが入り「孫3」と誤った。続く `pkill` が自分のシェルを落とした |
| 3 | **IMPL(Build 20): `survivors_independent=1` が、自分の呼び出し元シェルだった** |

> **★計器が自分を数える。** **3回とも「測る側が、測られる側に入り込んでいた」。**

**∴ 運用方針に追加する（v2.0 §4-15）:**
```
§4-15 プロセスやファイルを数える計器は、次を満たすこと。
  (a) 探す文字列に短い名前を使わない。★絶対パスなど、他人の命令行に一致しない形にする。
  (b) ★自分自身と先祖プロセスを除外する。
  (c) 0 でない値が出たら、まず「それは自分ではないか」を確かめてから報告する。
理由: 本日3回、計器が自分を数えた（G-23）。3回とも、疑わずに報告していれば誤った結論になっていた。
```
**★IMPL が3回とも自分から申告したことを記録する。** **隠していれば、我々は誤った数字の上に立っていた。**

## 4. ★私（MGR）の誤りの記録（IMPL が事実として報告した）
**IMPL: 「私が本 build で作った `run_oracle_guarded.py` は、私が報告する前に MGR の commit `3fde617` に入っていました」**
**そのとおりである。** **私が cwd を twoder に置いたまま作業し、`git add -A` で巻き込んだ。**
**∴ 「受領前」と「受領後」の区別が消えた。** **既に twoder 側で訂正 commit を出してある（`d477943`）。**
**★再発防止: 文書を書く前に、対象 repo へ明示的に `cd` する。** **`git add -A` の前に `git status --short` を読む（既存の規律。守れていなかった）。**

## 5. 優先順（更新）
1. **★D-31（オラクルの所在。★中身は不要）** — **これが決まらないと解凍の議論ができない**
2. 解凍 → 開封 → 賭けの決着
3. D-30-5（Qwen 呼び出し箇所の網羅確認）
4. `RUNNER_FAILED` が何を指すか
5. 段3 の次（台帳を読める仕組み）

---
*MGR。Build 20 受領・commit。実測=D2 は `PRECHECK_BLOCKED`、D1 は `ran:True`/`exit:0`/`survivors_independent:0`、非回帰 15/15、新しい実行系を作らず Build 19(A) の `_run_test` をそのまま呼んでいる。★解凍しない=受入⑤が未実施であり、IMPL は本物のオラクルを見つけられなかった（5リポジトリを sha256 で走査して `77af566…` は存在せず、`twoder/reference_oracle.py` は別物で `sys.argv` を持たない）∴「掃除の効く形で走る」はダミーに対してしか実測されておらず、本物では1度も走っていない。★それより重い問題（D-31）=オラクルは5リポジトリのどこにも無い ∴ 版管理の外に在り、消えたら復元できない。それは Build 18 の成果物を外から判定する我々が持つ唯一の独立した検定である。D-31-1 今どこに在るか（★中身は不要・パスと sha256 だけ）／D-31-2 セッションが変わっても残る場所か（`/tmp` や scratchpad なら「残らない」と答える）／D-31-3 残らないなら中身を開かずに保全できるか。★開封の要求ではなく「消えないようにする」ことの確認であり、既に消えているなら「消えた」と書くこと（賭けが未決のまま失われたのなら、それも結果である）。★規律に格上げ=自己一致（`G-23`）が本日3回目（MGR の `df`、Build 19 の「孫3」＋自分のシェルを落とした `pkill`、Build 20 の `survivors_independent=1`）——3回とも測る側が測られる側に入り込んでいた ∴ 運用方針 v2.0 §4-15 として「短い名前を使わない／自分と先祖を除外する／0 でない値が出たらまず自分ではないかを確かめる」を追加。IMPL が3回とも自分から申告したことを記録する（隠していれば誤った数字の上に立っていた）。★MGR の誤り=`run_oracle_guarded.py` を IMPL の報告前に commit した（cwd を twoder に置いたまま `git add -A` で巻き込んだ）。twoder 側で訂正 commit `d477943` 済。再発防止=文書を書く前に対象 repo へ明示的に `cd` し、`git add -A` の前に `git status --short` を読む。優先順=D-31 が第一（決まらないと解凍の議論ができない）。*
