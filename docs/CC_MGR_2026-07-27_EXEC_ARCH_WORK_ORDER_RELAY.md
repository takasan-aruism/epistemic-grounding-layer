# CC 管理(MGR) → 設計/監査(CC-α): **★Execution Architecture 作業指示書を受領・そのまま渡す + 段取り**（HANDOFF）

- `BUILD_ROLE: 参照`（**作業指示書の本文は `EXEC_ARCH_WORK_ORDER_v0_1.md` に逐語保存した。それが正典**）
- **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**

## 0. 権限と正典
**Taka から作業指示書（GPT 起草）を受領した。** **逐語で `egl/docs/EXEC_ARCH_WORK_ORDER_v0_1.md` に保存した。** **要約で置き換えない。実施の根拠はその全文である。**
**Taka 追記（逐語）**: **「どっかで途中まで作ってるなんかがあるよ。」**

## 1. ★段取り（この順で。理由つき）
### 段0（最優先）— **途中まで作ってあるものを先に見つける**
- **D-23（既に発出済）をそのまま段0とする。** **`EXEC_ARCH` / `execution architecture` / `Task Contract` を名乗る文書・スクリプト・台帳の所在。**
- **指示書 §2.4「新しい正本を無断で作らない」に直結する。** **途中まで在るものを見つけずに書き始めると、8回目の「既存を読まずに作る」になる。**
- **本日確立済の事実**: **`EXEC_ARCH` は本番4リポジトリで参照0件、研究スクリプト3本にのみ残存**（`s_exec_arch_acd.py` / `s_task_contract.py` / `s_llm_invocations.py`）。**そこから始めること。**

### 段1 — **Taka が名指しした2つを先に answer する（早期の出口）**
**指示書は大きい。** **完成を待つと、答えが出るのが遅すぎる。** **∴ 次の2つを、資料の完成前に単独で出すこと:**
1. **4軸7カテゴリは本番の一本の流れに乗っているか。**
2. **★EGL 登録時の勘定科目の自動設定は、登録の経路に繋がっているか。**
**（＝ D-18 の新ゴール。本指示書に吸収するが、答えは先に出す。）**

### 段2 — 指示書の本体（現状記録）
**§5 の構成に従う。** **§9 の禁止をそのまま守る。**

## 2. ★MGR が特に守らせる点（指示書と本日の規律の交点）
| 指示書 | 本日の規律との一致 |
|---|---|
| §2.1 現物優先・「仕様上存在する」と「実行時に呼ばれている」を分離 | **v1.5「ソースに在る≠動く。動作の主張には再現コマンドを併記」** |
| §2.2 推測で状態を昇格させない | **v1.7「失敗を正常に見える結果に置き換えない」** |
| §2.3 UNKNOWN を除外しない | **§6-4「推измеで埋めない」** |
| §2.4 新しい正本を無断で作らない | **v1.9「膨らませない」** |
| §9 切れた経路を補完しない | **本日7回の「作用する場所に無い」の記録法そのもの** |

## 3. ★MGR からの追加条件（3点だけ）
1. **★この資料を孤児にしない。** **指示書 §8（更新方式）は必須で設計すること。** **ただし自動検査の実装は本作業の範囲外**（指示書どおり）。**既存の `--check` 群を使う案にすること。新しい検査機構を作らない。**
2. **★調査中に接続修正を始めない**（指示書 §9）。**見つけた欠陥は登録するだけ。直したくなったら MGR に上げる。**
3. **★commit 前に Taka へ提示して承認を得る**（指示書 §10-14）。**MGR が仲介する。** **設計/監査は「提示できる形」まで作って止めること。**

## 4. 優先度（更新・状況表にも反映する）
| 順 | 件 |
|---|---|
| **1** | **段0（途中まで在るものの発見・D-23）** |
| **2** | **段1（4軸7カテゴリ／勘定科目の自動設定が繋がっているか）** |
| **3** | 段2（Execution Architecture 本体） |
| 4 | 台帳を読める仕組み（`contract` は段0の結果次第で解ける可能性） |

**Build 12 以降の worker 実行は、段0の結果が出るまで止める。** **契約の正体が分かってから再開する。**

---
*MGR。Taka から Execution Architecture 作業指示書（GPT 起草）を受領し、`EXEC_ARCH_WORK_ORDER_v0_1.md` に逐語保存（要約で置き換えない）。段取り=段0 途中まで在るものを先に見つける（D-23。指示書 §2.4 に直結し、見つけずに書き始めると8回目の「既存を読まずに作る」になる）→段1 Taka が名指しした2つ（4軸7カテゴリ／EGL 登録時の勘定科目の自動設定が繋がっているか）を資料完成前に単独で出す→段2 指示書本体。MGR の追加条件=①資料を孤児にしない（更新方式は必須設計・ただし自動検査の実装は範囲外で既存 --check を使う案にする）②調査中に接続修正を始めない③commit 前に Taka へ提示して承認（MGR が仲介、設計は提示できる形まで作って止める）。優先度=段0→段1→段2→台帳読み。Build 12 以降の worker 実行は段0の結果が出るまで止める。*
