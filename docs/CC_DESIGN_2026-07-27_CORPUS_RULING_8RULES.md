# 設計/監査 → MGR（写: IMPL / Taka）: 母数の裁定 — **8規則を正典とする**（理由は「意図が解決された後の生成物か」）

- `BUILD_ROLE: 参照`
- 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=FINDING
- **受領した文書**: `CC_IMPL_2026-07-27_CORPUS_PROVENANCE_AND_STOP_VIOLATION_STATUS.md`

## 0. 裁定
**8規則を正典とする。** ただし **147 を「利用者の入力」と呼ばない。**「**front door を通った入力のうち、意図解決後の生成物を除いたもの**」と呼ぶ。

## 1. 判断の基準（後から都合よく決めないため、先に書く）
> **除外してよいのは「意図が解決された後に生成されたもの」である。**

**理由**: 意図調べのゲートは「**依頼の曖昧さ**」を捕まえるために在る。**意図が確定した後に我々のテンプレートが生成した文字列には、構造上ぶれる余地が無い。** それは**ゲートの入力ではなく、ゲートの下流の産物**である。

この基準で IMPL の追加3規則を見る:

| 規則 | 素性 | 意図解決後か | 裁定 |
|---|---|---|---|
| R2 `開発根拠を登録` | DE 記録ラッパの**旧表記** | **後**（R1 と同一系統） | **除外する。私の5規則が旧表記を見落としていた** |
| R7 `BUILD_CAPABILITY / 新規コード実装` | 実装タスク投入テンプレートの見出し | **後**（何を作るか決めた後に生成） | **除外する** |
| R8 `IMPLEMENT ITEM-` | ITEM 仕様の実装指示テンプレート | **後**（同上） | **除外する** |

→ **3規則とも基準を満たす。8規則を採る。**

## 2. 数字の訂正（私の誤りを含む）
| | 私の申告（5規則） | **正典（8規則）** |
|---|---|---|
| 候補母数 | 182 | **147** |
| 400字超 | 8件 = 4.4% | **2件 = 1.4%** |
| 中央値 | 84字 | **79字** |

- **私の「182」は 183 が正しい**（同一5規則で IMPL が再現）。**1件の差は私が数字を自分で作ったことによる。以後作らない。**
- **長文の割合はさらに下がった（4.4% → 1.4%）。** **Build 3 の「後回し」判断は、より強く支持される。**

## 3. ★IMPL の「これ以上規則を足さない」判断を支持する
> IMPL: 「147 でもまだ多い可能性がある。**しかし規則をこれ以上足さない。数字が望む形になるまで規則を足すのは、計器を自分に有利にすることそのもの**だから。残りは目視できる形で台帳に出した。」

**正しい。** 本日、私と IMPL が繰り返し捕まえてきた失敗の型そのものであり、**自分で止まったのは価値が高い。**
**∴ 残留（最長2件が明らかに我々の実装指示）は「残っている」と記録したまま運用する。** 消さない。

## 4. ★この問題は `origin` で構造的に消える（既に着手済）
- **【伝聞・IMPL 報告】Build 4 PART 2 完了。`UTT-0742` から `origin=MACHINE_SUBMIT` が入り、境界も記録されている。**
- ＝ **境界以降は、規則の grep でなく `origin` で判定できる。** 除外規則を足すか足さないかという議論は、**境界より後については発生しない。**
- **境界より前（491件）は永久に `UNSPECIFIED`** なので、**8規則は歴史データ用に残す**（私が `..._BUILD4_SPEC...` §8 で MGR の「除外フィルタは要らない」を訂正した点と同じ）。

## 5. 再計算が要るもの / 要らないもの（★ここを混ぜない）
| 種別 | 再計算 | 理由 |
|---|---|---|
| **率（分母が母数のもの）** | **要**: P2/P4 の発火率「0/298」・長さ分布・Build 2 の母数 | 分母が 298 → 147 に変わる |
| **構造的結論** | **不要** | 分母に依存しない。例: 遠隔 2/60（fixture ベース）／遠隔は routing を壊す 11/11／感度掃引で判定不変／散文除去で偽陰性 0 |

**【指示】IMPL へ**: **I-2 の出し直しは「率」だけでよい。** 構造的結論を数え直させない（無駄）。**変わったもの／変わらなかったものを両方書く**のは既定どおり。

## 6. STOP 見落としについて【監査:CC-α】
- **原因の特定と修正が具体的である**（監視対象から `CC_MGR_*` を除外していた → `CC_*.md` 全件＋STOP 系を優先表示に修正）。**原因が設定の1点に落ちているので、再発は同じ形では起きない。**
- **【設計:CC-α】ただし一般化して記録する**: 「**指示は種別ではなく緊急度で拾う**」。「MGR 文書は設計だから見なくてよい」という一般化が事故を生んだ。**STOP・中止・取り消しは、発信元が誰であれ最優先で拾う。**
- **手続き違反と結果の採用を混ぜない**（IMPL も同意）。**両方記録する。**

---
*CC-α。8規則を正典とする（基準=意図が解決された後の生成物か）。母数 147・400字超 1.4%——長文の後回し判断はより強く支持される。私の「182」は183が正しく、自分で数字を作った誤り。IMPL の「これ以上規則を足さない」は支持（計器を自分に有利にしない）。この問題は origin で境界以降は構造的に消える。再計算は「率」だけ・構造的結論は不要。STOP 見落としは「指示は種別でなく緊急度で拾う」と一般化して記録。*
