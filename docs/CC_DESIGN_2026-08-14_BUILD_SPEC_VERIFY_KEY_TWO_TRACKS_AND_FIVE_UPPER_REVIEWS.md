開発者規律 確認済(v1.0)

# 【BUILD SPEC】★①`verify` を 名乗れるようにする ／ ★②壊れた走行を **消さずに 2本で 出す** ／ ★③上級監査へ **まず5件**

宛: IMPL ／ 発: DESIGN ／ 2026-08-14 01:1x ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **MGR 裁定 00:58**（受入⑫未達 ／ 2本立て ／ 次の一手の順序）
＋ **正本** §9 ／ §25 段3 ／ **Taka 逐語**「★その問いは 意味を なすか（が先）」／「★帳簿は 試験も 残す 義務が 在る」

**★MGR が 引いた 実測（★私は 引き直さない＝★同じ数を 2度 争わない）**
> `verify` は 0 のまま ／ `machine` が 1→3 ＝ **MGR の検査が『機械が使った』に入った**
> `not_in_list` 5件は **全部 `occurrences=1`** ＝ **2部品で揃った名前は 0件**（★『揺れのせい』は 否定された）
> 3走行のうち `ETR-9b6d70615ffa` は **票が1つも拾えていない** ／ `undecided` 67件は **その30件を含む**

---

## 0. ★★原因（★★行で 確定した・★推測では ない）

```
★★`twoder/webui.py:885` ―― ★★`_kind = "human" if "Mozilla" in _ua else "machine"`
   ★★∴ ★★★`lookup_kind` という 引数を ★★★どこでも 読んでいない
   ★★∴ ★`function_table.py:234` が ★`verify` を 受け取れる形に なっていても ★★★到達しない
   ―― ★★★『受け取る側が 在る』と『★呼び手が 渡している』は ★別（★本日 5回目の 同じ形）
```

---

## 1. ★★第1部 ―― ★★`verify` を 名乗れるようにする（★★LLM 0回）

```
★★① ★`webui.py:885` で ★★`lookup_kind` を ★先に 見る
   ★`_kind = q.get("lookup_kind", [""])[0] or ("human" if "Mozilla" in _ua else "machine")`
   ★★3語（verify / human / machine）の 外は ★★`machine` に 落とす（★fail-closed＝★既存の作法）

★★② ★★★鍵の行に 1行 足す（★★これが 一番 大事）
   ★★★『★`verify` は ★呼び手の 自己申告 ＝ ★★証拠に しない（★v0.3 §13.3）
      ―― ★★★受入の 数から 除く ためだけに 使う ／ ★★『使われた』の 証拠には ★しない』

★★③ ★★★遡って 直さない（★MGR 受入の 逐語）
   ―― ★★既に `machine` に 入った ★2回は ★★★記録を 触らない（★消さない・★書き換えない）
   ―― ★★代わりに ★鍵の行に 1行
      ★『★2026-08-14 の `machine` のうち ★2回は ★MGR の 検査（★遡って 直していない）』

★★④ ★★受入 = ★★★実物 1回（★★MGR が `&lookup_kind=verify` で 引いて ★`verify` が ★0 → 1）
   ―― ★★★私（DESIGN）が 引いた分も ★`verify` と 名乗る（★★これは 私の 規律であって 機構では ない）
```

## 2. ★★第2部 ―― ★★壊れた走行を **消さずに** 2本で 出す（★★LLM 0回）

```
★★出す形 = ★★`funnel` と `undecided` を ★★★2本（★★どちらも 出す＝★片方に しない）
   ★`all_runs`        = ★★3走行 全部（★★試験の走行も 帳簿に 残す＝★Taka 裁定）
   ★`runs_with_votes` = ★★票が 取れた 走行だけ

★★★『汚れ』『捨てる』『除外』と ★書かない ―― ★★正しくは ★★『★鍵が 2つ 在る』

★★★鍵を 2つ 出して ★交差させる（★★1つの鍵で 切ると 後で 揉める＝★本日 7例）
   ★鍵A = ★★`prompt_version` が 違う 走行（★★実装が 直した 時に 変わっている はず）
   ★鍵B = ★★その走行で ★メニューの語（★`NOT_DECIDED` 以外）が ★★1票も 拾えていない
   ★★★受入 = ★★A で 切った 集合と ★B で 切った 集合が ★★★同じ（★★違ったら ★止めて 報告＝
        ★★どちらが 正しいかを 争わず ★★★両方の 走行 id を 名前で 出す）

★★★数を 書く時は ★必ず 鍵を 添える
   ―― ★★『★62%（★票が取れた走行のみ・37/60）』／『★74%（★全走行・67/90）』
   ―― ★★★鍵の 無い 1つの数を ★出さない
```

## 3. ★★第3部 ―― ★★上級監査へ **まず5件**（★★段1『★その問いは 意味を なすか』が 先）

```
★★★新しい機構を 作らない ―― ★★既に 在る `twoder/question_review.py` を ★使い回す
   ★実測(★私が 読んだ) = ★`ANSWERS = ("ANSWERABLE","MATERIAL_MISSING","QUESTION_BROKEN")`(:16)
                       ／ ★`MODEL_CMD = ["claude","-p",None,"--output-format","json"]`(:18)
                       ／ ★`picked()`(:22) ／ `ask_one()`(:55) ／ `run_five()`(:100) ／ `list_reviews()`(:112)
   ★★∴ ★★足すのは ★★★`source` の 1つだけ（★`route_edge` ／ ★`function_vote`）
      ―― ★★★語を 増やさない（★3語は そのまま）／ ★★2本目の モジュールを 作らない

★★選び方（★★決定論・★人が 選ばない）
   ★① ★★`runs_with_votes` の 行だけ（★★壊れた走行から 選ばない＝★★但し 消さない）
   ★② ★`final = NOT_DECIDED`
   ★③ ★★1対1対1 を 優先（★★`disagreement=2`＝★既存 `picked()` と 同じ 優先）
   ★④ ★`component` 昇順 で ★★先頭 5件

★★渡す物（★正本 §8 の 最小 ＋ ★★1つ 足す）
   ★その部品の memo（★`make_memo` が 既に 作る物 ＝ ★★同じ物を 渡す）
   ★★3票と ★★その理由（★★全文・★切り詰めない）
   ★★★渡した メニュー（★★8語）を ★そのまま 見せる
      ―― ★★★理由 = ★★『★問いが 意味を なすか』を 見るなら ★★選択肢を 見せずに 聞けない
      ―― ★★これが 無いと ★★『材料が 足りない』と『★選択肢が 足りない』を ★分けられない

★★出す物
   ★① 5件の 答えの 内訳（★3語ごとの 件数）／ ★★② 理由（★★全文）
   ★③ ★★`MATERIAL_MISSING` の 時は ★★★何が 足りないか（★既存 `MISSING` の 4語から）
   ★④ ★Claude Code の 呼び出し回数（★★5回・★機械で 数える）／ ★所要
   ★⑤ ★読む口 = ★★`GET /api/control?include=question_reviews`（★★既に 在る＝★口 0増）
```

## 4. ★★受入（★★走らせる前に 宣言する）

```
★★① ★`verify` が ★★0 → 1（★★実物1回＝★MGR が 引く）
★★② ★鍵の行に ★★2行 在る（★★★自己申告ゆえ 証拠にしない ／ ★machine の 2回は MGR の検査）
★★③ ★★記録を 遡って 直していない（★★`index_lookup` の 過去行は ★bytes 不変）
★★④ ★★`funnel` ／ `undecided` が ★★2本（`all_runs` ／ `runs_with_votes`）で 出る
★★⑤ ★★鍵A と 鍵B が ★★同じ集合（★★違えば 止めて 報告・★走行 id を 名前で）
★★⑥ ★★上級監査 5件の 答えが ★★3語の 内訳で 出る（★★★1件も 空に しない）
★★⑦ ★★理由が ★★全文（★★切り詰め 0件）
★★⑧ ★★経路表・機能表に ★★★1行も 書かない（★★上級監査は ★門では なく ★情報源）
★★⑨ ★★★答えが 3語の 外だった時 ―― ★記録は 残し ★`answer` を ★★空で 埋めない（★既存 `_parse` の 作法）
★★⑩ ★Claude Code 呼び出し = ★★5回だけ（★★6件目を 聞かない）
★★⑪ ★新台帳 0 ／ ★口 0増 ／ ★★新しい file 0 ／ ★★語彙 0増
★★⑫ ★既定の `/api/control` が ★0.5秒以内
★★⑬ ★走らせた試験の 名前を 書く（★★禁止3本は 走らせない）
```

## 5. ★★★私からの 指摘 ―― ★★索引の 中身が 0 の ★理由（★★裁定を お願いします）

```
★★MGR 逐語 = 「★`登記` で 引いた=`in_list: true` ／ ★★`count: 0`＝★形は在るが 中身が 0」

★★★私が 読んだ 原因（★★行で 確定）
   ★`function_table.py:234 function_index()` は ★★`_rows()`（★★★正式登録された 行）だけを 見る
   ★★∴ ★★★『★確定した 票』（★★2回目の走行=★10件が 一覧の語に 決まった）が ★★索引に 入らない
   ―― ★★これらは ★★`function_vote` の 記録に ★★既に 在る（★`final` が 8語の どれか）
   ―― ★★★また 同じ形 = ★★『★記録に 在るのに ★読み出しが 捨てている』（★本日 何度目か）

★★★なぜ これが 効くか
   ★★Taka 逐語 = 「★機能から 索引できる」＝ ★★★これが 立っていないと ★★機能表は ★名前の 一覧に すぎない
   ★★直す量 = ★★★`hits` に ★`final` が その語の 票行を 足す ＝ ★★1箇所（★LLM 0回）

★★★但し ★私は 順序を 変えない ―― ★★順序は MGR の 担当
   ★★∴ ★★①今すぐ 直す か ★★②上級監査 5件の 後 か を ★★★裁定してください
   ―― ★★私の 推し = ★★★②の 後（★理由=★上級監査の 答えで ★『★問いが 壊れている』が 出たら
      ★★確定した 10件 自体の 意味が 変わりうる＝★★先に 索引へ 入れると ★入れ直しに なる）
```

## 6. ★★やらないこと

```
★★残り3部品（★母数33）は ★★この段では 掛けない（★MGR 裁定＝★上級監査の 後・★上限は 伸ばさない）
★★8語の 一覧を 広げない（★★材料が 揃う前に メニューを 増やさない）
★★走行を 重ねて 揺れを 均さない（★裁定② 却下・★正本 §9）
★★★壊れた走行を ★消さない ／ ★★『汚れ』と 呼ばない（★Taka 裁定）
★★上級監査に ★★『どちらが 正しいか』を 聞かない（★★聞くのは ★★『★その問いは 意味を なすか』）
```

## 7. ★★言い方

```
★★『機能表が できた』と 書かない（★★索引の 中身が 0＝★MGR が 正直に 残した）
★★『上級監査で 決まった』と 書かない（★★門では なく 情報源＝★答えは ★次の設計の 材料）
★★★62% と 74% を ★鍵なしで 並べない（★★どちらも 正しい＝★鍵が 違うだけ）
```
