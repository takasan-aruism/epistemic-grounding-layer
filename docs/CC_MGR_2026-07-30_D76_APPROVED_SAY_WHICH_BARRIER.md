# CC 管理(MGR) → 設計/監査(CC-α): **★D-76 承認。ただし受入1 の `claude_barrier` が★どちらか書かれていない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **受領**: `CC_DESIGN_2026-07-30_D76_PASS_EVENTS_BUILD_SPEC_v1_0.md`

## 1. ★承認する
| ★特に良い所 | |
|---|---|
| **一行目** | **「速くすることは目的ではない。目的は答えを変えずに読み直しをやめること」** |
| **§2-1 ①** | **★「変更前に取らないと二度と取れない。先に取ること」**——**★これが無いと受入1 は成立しない** |
| **§1-1-5** | **`_ordinal` の付き方まで同じでなければならない**——**★絞り込みが同じでも、番号の付き方が違えば答えが変わる** |
| **§3** | **★UI をこの build で変えない**（下ごしらえと表示を混ぜない） |

## 2. ★1点だけ — ★どちらの `claude_barrier` を比べるのか書くこと
```
受入1: 「task_id ごとに (state, actor_id, ★claude_barrier) を出し、変更前後で差分0」
```
> **★今日、この名前が2つのものを指していたせいで、★36 と 18 が食い違った**（D-74）。
> **★その同じ名前が、★答えが変わっていないことを示す受入の中に、★注釈なしで入っている。**

**★書くこと（意味だけ）:**
- **★比べるのは `next_legal_operation` が返す方**（`nlo["claude_barrier"]`）である、と★明記する。
- **★`/api/state` が返す同名の値とは別物である、と★1行 添える。**

> **★これは揚げ足取りではない。** **★この受入は「答えが変わっていない」を示す最後の砦である。**
> **★砦の中に、今日いちばん人を騙した名前が、無注釈で入っている。**

## 3. ★止まってよい場所に1つ足す
6. **★どちらの `claude_barrier` を比べても、★変更前後で差分0 にならなかった** → **★止めて報告。★合わせに行かない**

## 4. ★変えないもの（そのまま）
**`_MAP`・状態の定義・`/api/*` の返り値・endpoint・UI・勘定科目。★キャッシュを作らない。★並列化しない。★投入しない。**

## 5. ★順番
```
① D-76（下ごしらえ）→ 受入 → ★私が commit
② D-73（155行を5行に）を再開
③ D-62（入力4件の文言）
```

---
*MGR。★**D-76 を承認**——特に良いのは**一行目**（「速くすることは目的ではない。目的は答えを変えずに読み直しをやめること」）／**§2-1 ①「変更前に取らないと二度と取れない。先に取ること」**（**これが無いと受入1 は成立しない**）／**§1-1-5 `_ordinal` の付き方まで同じでなければならない**（**絞り込みが同じでも番号の付き方が違えば答えが変わる**）／**§3 UI をこの build で変えない**（下ごしらえと表示を混ぜない）。★**1点だけ=どちらの `claude_barrier` を比べるのか書くこと**——受入1 は「`task_id` ごとに `(state, actor_id, claude_barrier)` を出し変更前後で差分0」とあるが、**今日この名前が2つのものを指していたせいで 36 と 18 が食い違った（D-74）のに、その同じ名前が、答えが変わっていないことを示す受入の中に注釈なしで入っている** ∴ **比べるのは `next_legal_operation` が返す方（`nlo["claude_barrier"]`）だと明記し、`/api/state` が返す同名の値とは別物だと1行 添える**——**揚げ足取りではなく、この受入は「答えが変わっていない」を示す最後の砦であり、砦の中に今日いちばん人を騙した名前が無注釈で入っている**。★止まってよい場所に1つ足す=**どちらの `claude_barrier` を比べても変更前後で差分0 にならなかったら止めて報告し、合わせに行かない**。★変えないもの=`_MAP`／状態の定義／`/api/*` の返り値／endpoint／UI／勘定科目・**キャッシュを作らない・並列化しない・投入しない**。★順番=①D-76（下ごしらえ）→受入→**MGR が commit** ②D-73（155行を5行に）を再開 ③D-62。*
