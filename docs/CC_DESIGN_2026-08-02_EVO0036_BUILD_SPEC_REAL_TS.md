# 【BUILD SPEC】`EVO-0036` — **★呼び手1行。★但し過去は直らない ∴ 突き合わせまでが受入**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-02 23:5x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.6）** ／ **★9項目 確認済（★§6）** ／ **★3値 確認済（★§1）** ／ 親: `ITEM-2DER-EVO-0035`
- **★裁定の在り処**: `ITEM-2DER-EVO-0036` の `status_note`（逐語:「★『先に測る』を独立工程にしない」「★受入は既に在る計器で」「★壊れたらそれは欠陥の発見。直すのは試験側」「★過去2日分を etrace との突き合わせで復元して見せる」）
- **★走行 0・★task 増 0・★commit 0**

---

## 1. ★3値（★受領済・再掲は1行）

```
★『在るが未配線』で確定。★書く口は在る(submit.py:87 の ts= 引数 / :94 の既定値 / set_status(ts=ts) は呼ばれている)。
★★欠けているのは呼び手だけ=webui.py:747 `SUB.submit(b.get("raw",""))` が ★ts を渡していない。
```

## 2. やること（★1箇所・1行）

```python
# twoder/webui.py:747
-                tr = SUB.submit(b.get("raw", ""))
+                tr = SUB.submit(b.get("raw", ""), ★ts=_real_now())   # EVO-0036: caller が実時刻源(submit.py:90-91 の設計どおり)
```
```
★`_real_now()` は ★新しく作らず ★既に在るものを使う:
   ★`ds/ds/etrace.py:40 _now()` が ★実時刻を返している（実測 2026-08-02T20:25:12.819480）
   ★∴ ★`from ds import etrace as _ETRACE` は ★:777 で既に足してある ∴ ★`_ETRACE._now()` を呼ぶ
   ★★私有名(`_now`)を外から呼ぶのが嫌なら ★`etrace.py` に ★1行の公開別名を足してよい（★どちらでも可・行数を書くこと）
★★★★`submit.py` は ★1文字も触らない（★既定値も ts_source も ★そのまま残す＝★既定に落ちた過去を消さない）
★★戻し方: ★引数を消す。★可逆
```

## 3. 受入（★既に在る計器だけで見る）

```
★(1) ★配線後に1件 投入 → ★TRACE の ★`ts_source` が ★★`CALLER` になる（★裁定(2)の指定どおり）
★(2) ★その投入で動いた ITEM の ★`registered_at` が ★★実時刻になる（★`GET /api/resolve` で逐語）
★(3) ★★試験3本を走らせる（★これが「測る」＝★裁定(1)。★別工程にしない）:
     ★`twoder/regression/test_build_planner.py` ／ ★`test_execution_event_log.py` ／ ★`test_runtime_supervisor.py`
     ★★★61本は走らせない。★走らせた名前と ★通/落ちを ★そのまま書く
     ★★落ちた試験は ★★『偽の既定値を正典として固定していた』側の欠陥（★裁定(3)）
     ★★★但し ★直す前に ★落ちた理由を1本ずつ書く（★私が当否を判断する。★実装は直さない）
★★(4) ★★★過去2日分の復元を ★1件で見せる（★裁定(4)・★これが無いと親は未達のまま）:
     ★`ITEM-2DER-EVO-0036` の ★`registered_at` は ★配線後も ★`2026-07-11T08:00:00` のまま（★過去は直らない）
     ★★→ ★同じ item を動かした ★投入の `run_id` を辿り、★`GET /api/etrace?run_id=…` の ★`ts` を突き合わせ、
        ★★「この note は ★実際には いつ 書かれたか」を ★1件 復元して ★逐語で示す
     ★★★出来なければ ★「出来なかった」と書く（★推測で埋めない）
★(5) ★書いていない側が壊れていない（★`?run_id=` `?event_id=` `?task_id=` が従来どおり）
★(6) ★戻せる ／ ★(7) ★Claude が書いた行数（★2DER の実績に数えない）
★★★★★予告を投入前に書く: ★変更行数 ／ ★(3) で落ちると思う試験の本数 ／ ★(4) が出来るかの見込み
```

## 4. ★先に言う（★踏みうるもの）

```
★① ★`_now()` は ★`ds` 側の私有関数 ∴ ★import が通らなければ ★★書き換えず ★そのまま報告すること
★② ★実時刻になると ★★同一文面の再投入でも ts が変わる ＝ ★`task_id` は raw の sha1 なので ★変わらない（★影響なし）
   ★但し ★`ts` を種にしている箇所が在れば ★決定性が変わる【★未確認・★(3) の試験で出る見込み】
★③ ★★過去の記録は ★追記式なので ★遡って直らない。★直すふりをしない（★裁定(4) の受入がそれを担保する）
```

## 5. ★増える代わりに廃止（★規律9）

```
★畳むもの: ★「記録の時刻を ★git log から拾う運用」（★親 EVO-0035 §1 の①）
★★但し ★★受入(4) が通るまで ★畳んだと書かない——★過去2日は ★突き合わせでしか復元できない
```

## 6. ★9項目（私の分）
```
1 置いたなら読めるか＝★`GET /api/resolve` の `registered_at`（★受入(2)）
2 書く口＝★既に在る（`set_status(ts=ts)`）。★足さない
3 理由を捨てない＝★落ちた試験の理由を ★1本ずつ書かせる（★まとめて「壊れた」と書かせない）
4 作っていないのでは＝★★`ts=` 引数も `ts_source` 計器も ★既に在る。★無いのは ★呼び手の1行だけ
5 走ったか＝★受入(1)(2) は ★実投入で測る／6 名前＝★`ts` / `ts_source`（★既存欄。★改名しない）
7 依頼と試験の矛盾＝★★本件そのもの（★試験が偽の既定値を正典にしている）
8 計器が自分を数えないか＝★★受入は ★既存の `ts_source` で見る（★新計器を作らない＝裁定(2)）
★9 増える代わりに廃止＝★§5
```

## 7. 禁止
```
★`submit.py` を触る（★既定値と ts_source を残す）／ ★過去の記録を書き換える
★落ちた試験を ★理由を書かずに直す ／ ★61本を走らせる ／ ★新しい計器・台帳・エンドポイントを作る
★commit する ／ ★`twoder` 配下で python を動かす（★`operator.py` の罠）
```
