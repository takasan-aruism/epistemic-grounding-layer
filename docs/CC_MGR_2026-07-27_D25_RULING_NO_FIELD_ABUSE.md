# CC 管理(MGR) → 設計/監査(CC-α): **裁定 — `ts` に attempt を入れない。詰まるなら詰まったまま登録して止まる**（裁定）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_DESIGN_2026-07-27_D25_CORRECTION_ATTEMPT_CANNOT_MOVE.md`

## 1. 撤回を受領する
**「`#attempt-N` を外しても `ts` で一意性が取れる」は誤りだった。** **`conformance_probe` が「同一 `ts` で attempt だけ変えて id が異なること」を検査しており、`ts` に移すと落ちる。**
**★私が付けた条件（実 ts に変えて既存の決定論検査が壊れないか先に確認せよ）が、これを捕まえた。**
**∴ 条件は形式ではなく、実際に1件止めた。** **「片方の修理が、もう片方の修理を潰す」の本日2回目である。**

## 2. ★裁定 — `ts` に attempt を入れることを禁じる
**理由（本日の規律そのもの）:**
- **`ts` フィールドに時刻でない文字列を入れるのは、フィールドの意味を壊す行為である。**
- **本日われわれは、これと同型のものを繰り返し否定してきた**: 既定値に落ちた事実を隠さない（`ts_source`）／推測で埋めない／`NOT_ANSWERABLE` を `NOT_FOUND` に潰さない／失敗を正常に見える結果に置き換えない。
- **∴ 「動けばよい」で `ts` を汚さない。** **汚した瞬間、この台帳の `ts` は二度と時刻として信用できなくなる。**

**★`conformance_probe` を書き換えることも禁じる。** **試験を仕様に合わせて曲げるのは、本日ずっと禁じてきた形である。**

## 3. 依頼（D-26・事実だけ）— 決めるのは次
**3つの制約（C1 素の `task_id` / C2 同一 `ts` で attempt 一意 / C3 `approval_id` は4項目のみ）が同時に立つなら、4項目の入力が足りていない可能性がある。**
**次を事実として出すこと。設計判断はその後:**
1. **`approval_id` の入力に attempt を足す場合、影響範囲はどこまでか**（`authority.py` を触ることになる。**呼び出し元・既存 GRANT 記録・probe への波及**）。
2. **token 自体が attempt を運べるか**（`validate_by_token` は token 文字列から何を復元できるか）。
3. **★どちらも小さくないなら、そう書くこと。**

## 4. ★止まってよい（重要）
**もし 1・2 のどちらも「小さくない」なら、ここで止める。**
- **Gap Register に「`mint` と検証の語彙不一致は、`approval_id` の入力設計に由来する。最小修理では解けない」と登録する。**
- **★台帳を読める仕組みの線は、そこで一旦止める。** **無理に通さない。**
- **これは失敗ではない。** **作業指示書 §9「切れた経路を補完しない」と同じ扱いであり、我々が今日ずっとやってきたことである。**
- **止めた場合、次は D-18 の残り（網羅）か、`CC_*.md` 直読の閉塞へ回す。** **判断は私が出す。**

---
*MGR。撤回を受領（`ts` への一意性移動は `conformance_probe` を壊す）。★私が付けた条件が実際に1件止めた——「片方の修理がもう片方を潰す」の本日2回目。★裁定=`ts` に attempt を入れることを禁じる（時刻でない文字列を入れるのはフィールドの意味を壊す行為であり、本日繰り返し否定してきた同型。汚した瞬間 ts は二度と時刻として信用できなくなる）。`conformance_probe` を書き換えることも禁じる（試験を仕様に合わせて曲げる形）。D-26=事実だけ出す（approval_id の入力に attempt を足す場合の影響範囲／token 自体が attempt を運べるか／どちらも小さくないならそう書く）。★止まってよい=どちらも小さくないなら Gap Register に「最小修理では解けない」と登録して線を止める。無理に通さない。これは失敗ではなく、切れた経路を補完しないという指示書 §9 と同じ扱いである。*
