# BUILD SPEC — `SENT` を成功時にも残す v1.0（★実装源・小さい）

- `BUILD_ROLE: ★実装源`（**本文書が実装の唯一の典拠**）
- **★宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-29
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-29_D67_TAKA_CANON_AND_SUMMARY_DESIGN.md` §5-①

---

## 0. ★本 build の一行
> **「受けた」は残るが「返した」が残らない。** **∴「処理中に落ちた」と「正常に返した」が区別できない。**
> **★D-64 の目的の、残り半分を埋める。**

## 1. ★現状（実読・打ち切り無し）
```
再現: grep -n "_recv_log" twoder/webui.py
:527 def _recv_log(self, kind, path, status=None)
:545 self._recv_log("RECV", "/api/submit")        ← ★認証の前（401 で黙る場合も見えるように・良い）
:548 self._recv_log("SENT", "/api/submit", 401)   ← ★401 の枝だけ
```
**★`/api/submit` が返る道は3つ在る（実読・行番号つき）**
| # | 道 | 現状 |
|---|---|---|
| **1** | `:548` 認証拒否 → `_deny()` | **★`SENT` 在り（401）** |
| **2** | `:573` 正常応答 → `self._send({...})` | **★`SENT` 無し** |
| **3** | `:632-633` 例外 → `self._send({"error": …})` | **★`SENT` 無し** |

> **∴ 3つのうち2つが残らない。** **★「正常に返した」も「例外で返した」も区別できない。**

## 2. ★直す（2箇所だけ）
| # | 場所 | 足すもの |
|---|---|---|
| **1** | `:573` の `return self._send({...})` の直前 | **`SENT` / `/api/submit` / status=200** |
| **2** | `:632-633` の `except` の中（★`/api/submit` のときだけ） | **`SENT` / `/api/submit` / status=500** ＋ **★例外の型名**（`e` の型のみ。★本文は残さない） |

## 2-1. ★改訂1（2026-07-29・MGR 承認時の指摘。★隠さずに書く）
| # | 変更 | 理由 |
|---|---|---|
| **1** | **★「型名を `path` 欄に入れてよい」を★取り消す** | **★これは「1つの欄が2つのことを指す」形である。** **今日一日、我々がそれを潰してきた**（「取りこぼし 0件」／「取れない」／`欠損・失敗の記録なし`／`【伝聞】`／全24件）。**★`path` に `(TypeError)` が入れば `path` はもはやパスではなく、後で読む者は `/api/submit (TypeError)` をパスとして数えることになる** |
| **2** | **★型名は `path` とは別の欄に置く**（欄の作り方は実装が決めてよい） | **MGR の優先順位を採る: 「欄が1つのことだけを指すこと」＞「引数を増やさないこと」。★「引数を増やさない」は小ささのための方針であって正しさの条件ではない** |
| — | **他は変えていない** | 承認された内容を渡す前に書き換えない |

> **★私は今日の教訓を、その日のうちに自分で破った。** **★MGR が捕まえた。** **★記録に残す。**

---

**★書き方の条件（改訂1 反映後）**
1. **★`path` 欄には★パスだけを入れる。** **★型名を混ぜない。**
   - **★型名は別の欄に置く**（`_recv_log` の引数を1つ増やしてよい。**★増やすのは1つだけ**）。
   - **★型名以外を入れない**（例外の文字列・本文・依頼文は★残さない）。
2. **★`fail-open` のまま**（記録に失敗しても本処理を止めない）。**★書けなければ `stderr`。**
3. **★`/api/submit` 以外のパスでは何も書かない**（`approve`・`run_next` 等は対象外。**★全アクセスログにしない**）。
4. **★本文・依頼文を残さない**（台帳に在る）。

## 3. ★変えないもの
- **成功時の返り値**（`self._send` に渡す dict を1つも変えない）。
- **`RECV` の位置**（★認証の前。**そのままにする**）。
- **`/` 側・JS・`ids.py`・`emit`・Event Trace の record。**
- **勘定科目**（`G-62`/`G-64`〜`G-68` 据え置き）。**★投入しない。**

## 4. 受入
| # | 条件 | 示し方 |
|---|---|---|
| **1** | **3つの道すべてに `SENT` が在る** | **★差分で示す**（`:548` 既存 ＋ 追加2箇所） |
| **2** | **`/api/submit` 以外では書かない** | **★差分で示す**（`_recv_log` の呼び出しが `/api/submit` の条件下のみ） |
| **3** | **★`path` 欄にパス以外が入っていない**（改訂1）。増やした引数は★1つだけ | 差分 |
| **4** | **配信物の JS が構文として通る** | **★`GET /command` → `<script>` 抽出 → `node --check`**（`G-71`。**★ソースではなく配信物**） |
| **5** | 既存が不変 | `/api/state` の sha256・非回帰 91/7 |
| **6** | **★成功時の `SENT` が実際に出たか** | **★投入しないと出ない。** **★「確かめていない」と書く**（§4-1） |

### 4-1. ★受入6 は投入なしでは確かめられない（先に書く）
> **★`/api/submit` を叩かないと `SENT` は出ない。** **★投入しない**（Taka 常設の禁止）。
> **∴ 「コードに在ることは示せるが、出たことは示せない」。** **★`G-61`・(a)・(c2) と同じ扱いで未検証のまま登記する。**
> **★次に Taka が押したときに分かる。** **★それが本 build の目的である。**
> **★ただし今回は前例が在る**——**`RECV` は D-64 で入れ、★Taka の初回操作で実際に出た**（`20:44:33 RECV /api/submit`）。

## 5. ★止まってよい場所
| # | |
|---|---|
| 1 | **`:573` の直前に置くと成功時の返り値が変わってしまう** → **★止めて報告** |
| 2 | ~~例外の型名を `path` に入れる形が読みにくい~~ → **★改訂1 で取り消し**（型名は別の欄） |
| 3 | **`/api/submit` の返る道が3つ以外に在った** → **★私の数え間違い。報告してほしい** |
| 4 | SPEC が2通りに読める |
| **5** | **★欄を分けると `_recv_log` の呼び出し側が広く変わると分かった** → **★止めて報告**（MGR が改めて裁定する） |

## 6. ★資料の同期
**`webui.py` が変わるため資料が古くなる。** **★MGR の commit 後に私が同期する**（D-58 で決めた順番）。**あなたは触らなくてよい。**

---
*CC-α D-68 BUILD SPEC v1.0（実装源・宛 IMPL・小さい）。★一行=**「受けた」は残るが「返した」が残らないので「処理中に落ちた」と「正常に返した」が区別できない ∴ D-64 の目的の残り半分を埋める**。★現状=`_recv_log` は `:527` に在り `:545` で `RECV`（**認証の前に置かれており 401 で黙る場合も見える＝良い**）、`:548` で `SENT`（**401 の枝だけ**）。**`/api/submit` が返る道は3つ**（`:548` 認証拒否＝`SENT` 在り／`:573` 正常応答＝**無し**／`:632-633` 例外＝**無し**）∴ **3つのうち2つが残らない**。★直すのは2箇所だけ（`:573` の `return self._send` の直前に `SENT`/status=200、`:632-633` の `except` の中で `/api/submit` のときだけ `SENT`/status=500 ＋ **例外の型名のみ**）。条件=**`_recv_log` を変えず引数を増やさない**（型名が要るなら `path` 引数に `"/api/submit (TypeError)"` のように入れてよい）／**fail-open のまま**（書けなければ stderr）／**`/api/submit` 以外では何も書かない**（全アクセスログにしない）／**本文・依頼文を残さない**。★変えないもの=成功時の返り値（`_send` に渡す dict を1つも変えない）／`RECV` の位置（認証の前・そのまま）／`/` 側・JS・`ids.py`・`emit`・Event Trace／勘定科目・投入しない。★受入6件=3つの道すべてに `SENT`（差分で示す）／`/api/submit` 以外では書かない／`_recv_log` の引数が増えていない／**配信物の JS が `node --check` を通る（`G-71`。ソースではなく配信物）**／既存不変／**成功時の `SENT` が実際に出たかは投入なしでは確かめられないので「確かめていない」と書く**（`G-61` 等と同じ扱いで未検証のまま登記。**次に Taka が押したときに分かり、それが本 build の目的**。ただし前例が在り **`RECV` は D-64 で入れて Taka の初回操作で実際に出た**）。★止まってよい場所4件（`:573` の直前に置くと返り値が変わる／型名を `path` に入れる形が読みにくい＝別案を出さず報告／**返る道が3つ以外に在った＝CC-α の数え間違い**／2通りに読める）。★資料の同期は MGR の commit 後に CC-α が行う。*
