# 設計/監査 → MGR（写: IMPL / Taka）: D-10 置き場所の裁定 — **役割を切り直す。3d=止まるか / 3e=どう答えるか。事実は 3d の応答生成へ**

- `BUILD_ROLE: 参照`
- 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=FINDING
- **受領した MGR 文書**: `CC_MGR_2026-07-27_BUILD6_RECEIVED_PLACEMENT_QUESTION.md`（D-10 / MGR の問い / 今日3回目のパターン）

## 0. 裁定（3行）
1. **IMPL の3択のうち (i) を採る**（事実を 3d の応答生成へ）。**ただし単なる (i) ではなく、役割の切り直しとして採る。**
2. **3d = 止まるかどうか（決定論）／ 3e = 動くとして、どう答えるか（残り4択）** に分ける。
3. **∴「事実が 3e に届かない」は欠陥ではなく、役割が正しく分かれた結果である。** 置き場所を動かす必要はない。**足りないのは 3d 側の応答生成である。**

## 1. MGR の問いへの回答（現物を読んだ）【監査:CC-α】
> MGR:「3d は止めるが、どう応じるかは決めていないのではないか。止まった入力に返す言葉を組み立てているのは誰か。」

```
再現: sed -n '/def next_legal_operation/,+22p' rri/rri/preflight_gate.py
```
**組み立てているのは `preflight_gate.next_legal_operation()` であり、中身は2通りしかない:**
- **数値ゲート**: 従来の**定型文**を逐語で返す（非回帰のため固定）。
- **それ以外のゲート**: **`clarification_slots` を ` / ` で連結**して `required to proceed: …` に埋める。

**∴ MGR の見立ては正しい。3d は「止める」と「何を出せば進めるか」までは決めているが、「どう応じるか」は決めていない。**
- `INTENT_PROBE`（何を指すか聞く）/ `PREMISE_PROBE`（前提を確かめる）/ `DEFER`（保留）は、**すべて「止まった後の応じ方」**である。
- **∴ 意図調べの STOP 側3戦略が要るのは、まさに 3d の応答生成の場所である。**

## 2. 役割の切り直し（本裁定の中核）
| 段 | 決めること | 手段 | 使う事実 |
|---|---|---|---|
| **3d** | **止まるか / 進むか** | **決定論（ゲート）** | 参照の束縛・存在の接地・出典 |
| **3d の応答生成** | **止まった時、どう応じるか**（聞き返す / 前提を確かめる / 保留） | **決定論 or 最小の LLM** | **★ここに事実が要る** |
| **3e** | **進むとして、どう答えるか**（`DIRECT` / `CONTEXT_RESOLVE` / `CHOICE` / `BMV`） | LLM（4択） | **文脈が「在る」時の正の事実のみ** |

**根拠**:
- 3d を通過した入力は、**定義上「止まる理由が無い」**ものである。**∴ 3e に STOP 側3戦略を出す必要が無い。**
- 3e に必要な事実は **`anchoring in {MEDIUM,HIGH}` の「直前の文脈が記録にある: …」だけ**である（`CONTEXT_RESOLVE` を選ぶために要る）。**「無い」系の事実は 3d 側に属する。**
- **∴ Build 6 の「事実が 3e に届かない」は、設計が正しく機能した証拠である。** 届くべきでない事実が届いていない。

## 3. ★ただし今日3回目のパターンは残る（MGR §4 への回答）
MGR が記録した共通形「**正しく作ったが、作用する場所に無い**」——**本件もこれに該当する。**
- **事実の出し分け機構は、作られたが 3e にある。** **要るのは 3d の応答生成である。**
- **∴ 機構が無駄になったのではなく、置き場所が1段ずれていた。** **移せば作用する。**
- **MGR 提案の「次の SPEC 冒頭に1行の確認項目」に同意する。** 文言案:
> **この機構は何に対して発火するのか。その入力は、実際にこの段へ到達するのか。到達しないなら、どの段へ到達するのか。**

## 4. ★監査で見つけた別件（裁定と分けて報告・要確認）
**【監査:CC-α】本番 front door を私が直接通した結果:**
```
再現: python3 -c "import sys;sys.path.insert(0,'.');import twoder.submit as S;
      t=S.submit('この設計案の得失は？'); print(t.get('INTENT_STRATEGY'))"
結果: {"strategy": null, "candidates": [], "status": "NO_CANDIDATE",
       "facts_emitted": false, "fact_trace": ["SELF_CONTAINED_NO_FACTS"], ...}
```
- **事実が0行なのは仕様どおり（正しい）。**
- **しかし `status=NO_CANDIDATE`・`candidates=[]` は問題である。** **研究スクリプトでは同じ入力（BV3）が 0.93 で `BOUNDED_MULTI_VIEW` に当たっていた。**
- **∴ 本番経路の 3e は、研究スクリプトと同じようには動いていない。** 原因は未確認。
- **【指示】IMPL へ**: **本番 3e が候補ゼロを返す原因を、コードを読んで報告すること**（`:8005` を呼べているか / メニューが空になっていないか / 例外を握り潰していないか）。**LLM 比較は不要。原因の特定のみ。**
- **これは D-10 の裁定とは別件である。** 置き場所を動かす前に、**そもそも 3e が動いているかを確かめる必要がある。**

## 5. 実装の順序（提案・MGR が決める）
1. **§4 の原因特定が先。** **動いていないものの置き場所を議論しても意味がない。**
2. その後、**事実の出し分けを 3d の応答生成へ移す**（§2）。
3. **3e は4択に絞る**（STOP 側3戦略を外す）。**これは「メニューから外す」なので、Build 4 と同じ形＝効果は期待しない。構造の整合のために行う。**
4. **精度の測定を混ぜない**（MGR 規律）。

## 6. 位置づけ（緩めない）
- **配線が通ったことは事実だが、3e は候補ゼロを返している。** **∴「意図調べが本番の経路に入った」とは書けるが、「動いている」とは まだ書けない。**
- **「2DER が良くなった」は引き続き書かない。**

---
*CC-α D-10。裁定=(i) を採るが単なる (i) でなく役割の切り直しとして。3d=止まるか(決定論)/3d の応答生成=止まった時どう応じるか(★事実はここに要る)/3e=進むとしてどう答えるか(4択・要る事実は「文脈が在る」だけ)。MGR の問いには現物が答えていた——next_legal_operation は定型文か clarification_slots の連結だけで「どう応じるか」を決めていない。★別件: 本番 3e が候補ゼロを返す(研究では 0.93 で当たっていた入力)。置き場所より先に、3e が動いているかの原因特定が要る。*
