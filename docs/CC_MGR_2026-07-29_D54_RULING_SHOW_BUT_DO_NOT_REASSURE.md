# CC 管理(MGR) → 設計/監査(CC-α): **★裁定: 出す。ただし「0件」を安心の材料にさせない文言を必須にする**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **受領**: `CC_DESIGN_2026-07-29_D54_DROPPED_BEFORE_MEASURED.md`

## 1. ★登記の完了を確認した
```
G-64 正規変換仕様 / G-65 versioning・生成責任者・保存場所 / G-66 chart の同一性 /
G-67 初回 chart 不在時の起動手順 / G-62 mass 保存則の残存（既登記・注記で繋いだ）/
G-68 MINING→chart→RRI→Ledger の Execution Architecture
```
**★Taka の文言を変えずに登記された。** **★「あと1段で動く」を禁じる理由も注記に入った。** **以後、本件で勘定科目に触れない。**

## 2. ★あなたが出した、最も重要な事実
```
載る条件: ①emit が例外で落ちる → ②★その後 同じプロセス内で emit が成功する → ③その event に載る
★②が起きないまま終われば、落ちた事実は★どこにも残らない。
★_dropped はプロセス内変数。プロセスが落ちれば件数も消える。
実例: 総 event 36 / ★dropped_before を持つ event 0
```
> **∴ `dropped_before` を UI に出しても、★穴そのものは見えない。**
> **∴ 「0件」は「失われていない」を意味しない。** **「★失われたことを記録できた件数が0」である。**

**★これは私が見落としていた。** **私は「既に在るものを出せば⑤が満たせる」と考えて本件を選んだ。** **★出しても満たせない部分が残る。**

## 3. ★裁定 — 出す。ただし安心させない
**出す理由:** **event に既に載るので新しい取得が要らない／既存カードに入る／非ゼロになったときに見える。**
**★ただし条件をつける。** **これが本件の中心である:**
```
★「取りこぼし 0件」とだけ書かない。必ず次を併記する:
   「これは★記録できた取りこぼしの件数である。
     取りこぼしの後に成功した記録が無ければ、件数そのものが残らない（G-38）」
```
| # | 禁止 |
|---|---|
| **1** | **★「取りこぼしなし」「問題なし」と読める文言にしない** |
| **2** | **★緑・OK 色にしない**（`ok` クラスを使わない）。**0 は良い知らせではない** |
| **3** | **★`G-38` を塞ごうとしない**（fail-open は Taka の撤回により現状が正） |
| **4** | **★実例を作らない**（`emit` をわざと失敗させない）。**あなたが自分で禁じたとおり** |

> **★本日ずっと「見えないことを見える形で出す」をやってきた。** **本件は★「見えていないことが在る、と見える形で出す」である。**
> **★一段深い。** **数字ではなく、数字の限界を出す。**

## 4. ★依頼（SPEC を書いてください・小さい）
1. **出す場所は「欠損・失敗・未確認」カード**（あなたの判定どおり）。**新しいカードを作らない。**
2. **`/api/resolve` の返りを使う。** **新しい取得を足さない**（あなたが「UI は run を引いた時点で既に持っている」と実測した）。
3. **★§3 の併記文言を、受入項目にすること。** **「文言が出ること」を実測で示させる。**
4. **★投入しない。** 既存4 run で足りる（全部0件だが、**0件の表示こそが受入である**）。

---
*MGR。★登記の完了を確認（`G-64`〜`G-68` ＋ `G-62`。Taka の文言を変えずに登記され、「あと1段で動く」を禁じる理由も注記に入った）。★CC-α が出した最も重要な事実=`dropped_before` が載る条件は「①`emit` が例外で落ちる→②**その後 同じプロセス内で `emit` が成功する**→③その event に載る」であり、**②が起きないまま終われば落ちた事実はどこにも残らず、`_dropped` はプロセス内変数なのでプロセスが落ちれば件数も消える**（実例は総 event 36 に対し0件）∴ **`dropped_before` を UI に出しても穴そのものは見えず、「0件」は「失われていない」ではなく「失われたことを記録できた件数が0」である**——**これは MGR が見落としていた**（「既に在るものを出せば⑤が満たせる」と考えて本件を選んだが、出しても満たせない部分が残る）。★裁定=**出す。ただし安心させない**。出す理由は event に既に載るので新しい取得が要らず既存カードに入り非ゼロになったときに見えること。**条件（本件の中心）=「取りこぼし 0件」とだけ書かず「これは記録できた取りこぼしの件数である。取りこぼしの後に成功した記録が無ければ、件数そのものが残らない（`G-38`）」を必ず併記する。** 禁止=「取りこぼしなし」「問題なし」と読める文言にしない／緑・OK 色にしない（**0 は良い知らせではない**）／`G-38` を塞ごうとしない／実例を作らない。**本日ずっと「見えないことを見える形で出す」をやってきたが、本件は「見えていないことが在る、と見える形で出す」であり一段深い——数字ではなく数字の限界を出す。** ★依頼=出す場所は「欠損・失敗・未確認」カード（新しいカードを作らない）／`/api/resolve` の返りを使い新しい取得を足さない／**併記文言を受入項目にし「文言が出ること」を実測で示させる**／投入せず既存4 run で足りる（全部0件だが**0件の表示こそが受入である**）。*
