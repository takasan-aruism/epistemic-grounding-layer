# 【報告書】Phase 4 第一弾 — 2DER 単体 End-to-End 性能検査

- **宛: TAKA** / 写: 設計/監査 / IMPL / 発: MGR / 2026-07-30 / TYPE=STATUS
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-30_PHASE4_E2E_INSPECTION_ORDER.md`（★逐語）
- **★検査は終了している。★止まった所で終えた。★直していない。**

---

## ★工程表（★正典の報告形式どおり、説明より先に出す）

| 順番 | 工程 | 判定 | actor | 主な証拠ID | 備考 |
|---|---|---|---|---|---|
| **1** | **受付** | **★FAILED** | 人(IMPL)が投入 → 2DER が受理 | `receipt.last_recv_at=22:30:24.891921`（投入と差 5.9ms） | **★最初の停止点。**受理は成立。**★成立条件(c)「入力内容を取得できる」が未達** |
| **2** | ID 発行 | **PROVEN** | 2DER | `trace_key=SUBMIT-zOlryQ` | **★発行はされた。**解決できないことは工程9 で数える（★欠陥を2箇所に数えない） |
| **3** | RRI | **PROVEN** | 2DER (RRI) | `request_type=OBSERVE_CURRENT_STATE` / `acquisition_method=RUNTIME_INSPECTION` | 呼ばれた証拠＋後工程へ渡った証拠。同区間に `:8005` 4件 |
| **4** | 計画 | **NOT_REACHED** | — | — | **★設計どおり**（`submit.py:377` が意図して DW task を作らない）。`plan_source`・`runtime_recovery` は該当なし |
| **5** | 実行命令生成 | **NOT_REACHED** | — | — | 打たれた4本は **adapter 内蔵の定型**。★planner 生成ではない |
| **6** | 実環境での取得 | **OBSERVED_UNATTRIBUTED** | 記録上 `read-only DW executor` | `ARUN-00954`〜`00957` | 実行主体は特定できる。**★実行された指示に作者が居ない**（定型であって生成物でない）∴ 正典「2DER が生成した指示によって」が不成立 |
| **7** | 出力取得 | **FAILED** | 同上 | `OBS-00955`〜`00958` | **★GPU 使用率=取得不能／プロセスの GPU 紐付け=無し。★空を成功にしない** |
| **8** | 検証 | **NOT_REACHED** | — | — | **★出力がそのまま応答に載った**＝正典 §6 の不成立条件 |
| **9** | 記録 | **FAILED** | 2DER | `ARUN-`/`OBS-`/`SRC-` は resolve 可 | **★1つの ID から辿れない（到達 1/8）** |
| **10** | DW 要約 | **NOT_REACHED** | — | — | 本件に紐づく要約を返す口を引けない |
| **11** | ユーザ返答 | **FAILED** | 2DER | `measured_state`（21行の配列） | **★要約ではなく生の配列。★使用率が欠落したまま** |

**★判定区分は正典の6つだけを使った。★中間の言い方を作っていない。**
**★1点 断っておく**: **工程6 は6区分のどれにも正確には当たらない。**（実行した主体は特定できるが、**実行された指示に作者が居ない**）。**★最も近い区分を選び、理由を備考に書いた。★勝手な区分を足していない。**

---

## 1. 最終結果

```
★一周 不成立
★最後に成立した工程 = 3（RRI）
★最初に成立しなかった工程 = ★1（受付）
★終了条件 = B（最初の停止点を証拠付きで確定）→ ★これで完了
```
**★IMPL は「最初の停止点は工程2」と報告した。★MGR が工程1 に前倒しし、★設計が独立に確かめて一致した。**

---

## 2. ★実際に 2DER が担当した工程（★証拠で主体を示せるものだけ）

| 工程 | 証拠 |
|---|---|
| **3 RRI**（分類と取得方式の決定） | `OBSERVE_CURRENT_STATE` / `RUNTIME_INSPECTION` ＋ 同区間の `:8005` 呼び出し4件 |
| **6 取得の実行** | `ARUN-00954`〜`00957`（command / host / 時刻 / `read-only DW executor`）**★ただし命令の出所は planner ではない** |
| **9 記録の書き込み** | `ds_events.jsonl` / `rri_records.jsonl` / `egl data/events.jsonl` が更新（★中身は読んでいない） |

**★取得は実測である**: `ARUN-00954` の実行は **22:30:30.840**＝**★投入（22:30:24.886〜22:30:31.555）の内側**。**★保存済み情報ではない。**

---

## 3. ★人または Claude が担当した工程（★省略しない）

```
★POST /api/submit を1回 押した（実装(IMPL)）。★これだけである。
★run_next は ★0回（★task_id が無く、押す対象が無かった）
★計画・コマンド選択・コマンド修正・実行・出力整形・検証・台帳登録・DW入力・要約補筆 = ★1件も行っていない
★GPU を自分で測っていない（★nvidia-smi を叩いていない。★3インスタンスとも）
★新しいコード・API・台帳・スクリプト・接続・分岐 = ★0件。★設定変更・再起動 = ★0件
```
| 操作内容 | 操作者 | 理由 | 既存運用か | 主体判定への影響 |
|---|---|---|---|---|
| `POST /api/submit` 1回 | 実装(IMPL) | 検査課題の投入 | **既存運用** | **★2DER の担当工程として数えない** |

---

## 4. ★台帳・Event Trace 評価

**★起点にすべき task_id が無いため、「1つの task_id から辿る」は★入口で成立しない。**
以下は submit 応答を起点にした到達可否（★これ自体が「記録から辿った」ではない）。

| 到達先 | | 根拠 |
|---|---|---|
| 入力 | **×** | 応答が返す手がかり3種（`trace_key` / `OBS-` / `host_ref`）を**★全部 叩いた**。入力へ辿る欄が無い。**★連番は当てていない** |
| RRI | **△** | 値は応答に在るが、**★記録から ID で引く経路が無い** |
| 計画 | **×** | 起きていない（★設計どおり） |
| 実行 | **○** | `ARUN-00954`〜`00957` が引ける |
| 生出力 | **×** | `OBS-` は hash と `blob://…` を返すが、**★blob の中身を引く口が無い** |
| 検証結果 | **×** | 存在しない |
| DW 入力 | **×** | 存在しない |
| 最終結果 | **×** | `trace_key` が `resolved=false` |

```
★到達 1/8（△1・×6）
★actor 欠落: 取得の実行者は記録されているが、★「命令を決めた者」を示す actor が無い
★ID 接続切れ: ★入口の ID（SUBMIT-zOlryQ）が解決できない
★DW の参照元: ★無い（DW は動いていない）
```

**★DW 不作動は3つの独立した出所で一致した**:
```
① IMPL: 応答の task_id = null
② MGR : 検査後 dev-workcell が★無変化（ds/rri/egl は変化あり）
③ 設計: 検査前 tasks 件数 156 → 検査後も 156
```

---

## 5. ★停止原因（★正典の区分から）

> ### **★ID 接続不全**

```
RRI が OBSERVE_CURRENT_STATE と分類 → RUNTIME_INSPECTION 経路へ
→ ★この経路は意図して DW task を作らない（submit.py:377 実読）
→ task_id なし → PLAN / 実行命令生成 / 検証 / DW要約 は★到達しない
→ ★1つの ID から辿れる記録が成立しない
```
- **★工程1 の未達と工程9 の未達は、★同じ1つの欠陥である**（★別々の2件ではない）。
- **★「配線が無い」とは書かない。** PLAN 以降の配線は在る。**★今回の経路が通らなかっただけである。**
- **★併せて（工程7 の別原因）: 環境情報不足。** `--query-gpu` に `utilization.gpu` が無く、プロセスは GPU に紐づかない `ps` 出力である。

### 5-1. ★この検査がいちばんはっきりさせたこと（★MGR の所見）
> **★「要約してください」という依頼は、★2DER では「見に行く依頼」として扱われる。**
> **★そして「見に行く」経路には、★計画・検証・要約・返答の各段が★そもそも含まれていない。**
> **∴ ★この種類の依頼では、★一周は「失敗した」のではなく「★通る道に一周が無い」。**

---

## 6. ★次の最小修正候補（★1件だけ。★検査中に実施していない）

> **`RUNTIME_INSPECTION` 経路でも、★応答に返している `trace_key` を `/api/resolve` で引ける ID として1つ通す。**

```
★新しい台帳を作らない。★新しい口も作らない。★既に応答へ返している ID を、既存の resolve に通すだけ。
★これ1件で、工程1 と工程9 の両方が塞がる（★同じ1つの欠陥のため）。
★GPU 使用率の欠落は別件 ∴ ★ここでは出さない（正典「複数の問題をまとめて修正する提案をしない」）。
★実施していない。★指示を待つ。
```

---

## 7. ★検査前後の状態（★Claude Code の役割 ①⑦）

| | 検査前 22:22 | 検査後 22:40 |
|---|---|---|
| ds / rri / egl(data) | 未commit 0 | **★3つとも更新**（2DER が書いた） |
| **dev-workcell** | 未commit 0 | **★無変化** |
| twoder | 未commit 0 | ★無変化 |
| 稼働プロセス | webui PID 3814663 / vllm PID 700957 | **★同一。★再起動していない** |
| 資料 | component 23 / edge 11 / gap 83 | **★同一。★検査中に資料を触っていない** |

**★正直に1点**: 私の事前保存（22:22）と IMPL の事前保存（22:29）で `egl` の HEAD が違う。**★間に私が commit したためである。★隠さない。**

---
**決めたこと**: **①一周 不成立。最後に成立した工程=3（RRI）、最初に成立しなかった工程=1（受付）。終了条件 B で完了 ②停止原因は ID 接続不全で、工程1 と工程9 は同じ1つの欠陥 ③最小修正候補は1件（`trace_key` を既存 resolve に通す）。実施していない。**
