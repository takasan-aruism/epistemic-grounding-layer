# BUILD SPEC — 継続中タスクを集約して出す **v2.0**（★実装源・v1.0 を置き換える）

- `BUILD_ROLE: ★実装源`（**本文書が実装の唯一の典拠**）／**★v1.0 は SUPERSEDED。★v1.0 を見ないこと**
- **★宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-30
- **運用方針 確認済（版: `v2.8`）**
- **裁定**: `CC_MGR_2026-07-29_D74_RULING_MOVE_IT_SERVER_SIDE_I_LIFT_MY_CONSTRAINT.md`（サーバ側で数える）/ `CC_MGR_2026-07-30_D77_DECIDE_THE_DENOMINATOR_BEFORE_BUILDING.md`（母数を決める）
- **下ごしらえ**: D-76 は commit 済（`derive_state` 等に `events=` が付いた ∴ **153件の分類は 0.004秒 で終わる**）

---

## 0. ★本 build の一行
> **155行の ID を、★5行の件数にする。**
> **★数の横に「何を数えたか」を必ず書く。** **★出せないものは「出せない」と理由つきで書く。**

## 1. ★v1.0 から変わった所（★3点だけ。他は v1.0 のまま）
| # | v1.0 | **★v2.0** | 理由 |
|---|---|---|---|
| **1** | 合計155件 | **★合計は `_active_2der_tasks` が返す数**（実測153件） | **★画面は153件 並べている。★155 と書くと並んでいる数と合わない**（D-78） |
| **2** | 停止中 2件 | **★停止中は「この一覧の対象外」。★件数を和に入れない** | **★`BLOCKED` は `_active_2der_tasks` に入らない**（実測・D-78）∴ **母数の外から数えていた** |
| **3** | `claude_barrier == True` | **★`actor_role in ("MANAGER","CLAUDE_SENIOR")`** | **★`claude_barrier` は2つのものを指しており 36 と 18 が食い違った**（D-74） |

## 2. ★既定（開く前）— ★ID を1件も出さない
```
進行中の依頼  153件
  この一覧に、終わった依頼と止まった依頼は入っていません。

  背景で進んでいる            117件
  こちらの手番               18件
  この状態を知らない          18件
  いま前に出すべきもの       ★出せません（前に出す順番を決める値が 2DER に無いため）
  ────────────────────────────────
  合計                       153件

  ※ 止まっている依頼が 2件 ありますが、★この一覧の対象外です。
```
| # | 条件 |
|---|---|
| 1 | **★ID を1件も出さない**（いまは155行 出ている） |
| 2 | **★「いま前に出すべきもの」を消さない。「0件」とも書かない。** **★「出せません」＋理由** |
| 3 | **★「その他」と書かない**（中身を隠す）。**★「この状態を知らない」と書く** |
| 4 | **★合計を出し、★4区分の和が合計と一致すること**（§5-1） |
| **5** | **★停止中は「0件」と書かない。★「この一覧の対象外」と書く** |
| **6** | **★「この一覧に、終わった依頼と止まった依頼は入っていません」を必ず添える**（★数だけ置かない） |

### 2-1. ★区分の判定（★すべて決定論。★LLM を使わない）
```
母数        : ★submit._active_2der_tasks(W) が返す task（＝COMPLETE と BLOCKED を除いたもの）
各 task の値: ★dispatch.next_legal_operation(task_id, ★events=一度だけ読んだもの) の返り

こちらの手番        : nlo["actor_role"] in ("MANAGER", "CLAUDE_SENIOR")
この状態を知らない   : nlo["actor_id"] == "-"          ← ★先に判定する
背景で進んでいる     : 上のどちらでもない
いま前に出すべきもの : ★判定しない（値が無い）
停止中              : ★母数の外。★数えない
```
> **★判定の順番: ①`actor_id == "-"` ②`actor_role in (…)` ③残り。** **★この順でないと重複する。**

### 2-2. ★私の実測（★推測を1つも含まない・★打ち切り無し）
```
再現（★私が実行・読み取りのみ・投入なし）:
  母数 _active_2der_tasks → ★153件

  role=INDEPENDENT_AUDITOR  actor_id=QWEN_AUDITOR      67
  role=CODING_WORKER        actor_id=QWEN_LIVECODER    50
  role=-                    actor_id=-                 18
  role=MANAGER              actor_id=CLAUDE            13
  role=CLAUDE_SENIOR        actor_id=CLAUDE             5

  背景で進んでいる      117
  こちらの手番           18   （MANAGER 13 ＋ CLAUDE_SENIOR 5）
  この状態を知らない      18
  ★和 153 = 母数 153     ★一致
```
> **★数を実装源に固定しない。★明日 task が増えれば変わる。★上は「今日そうだった」である。**

### 2-3. ★「この状態を知らない 18件」の文言（★言い換えないこと）
> **★これは「人を待っている」ではない。** **★`_MAP` にその状態が無く、既定値が返っているだけである**（D-74）。
> **★「担当者未定」「保留」と書かない。** **★分からないものを、分かったように書かない**（`G-72`）。

## 3. ★開いたとき — その区分だけを出す（v1.0 のまま）
| 出す | 出さない |
|---|---|
| **`task_id`** | **開始日**（★front door に無い） |
| **依頼文の先頭30字 ＋ ★「先頭30字です（全文ではありません）。先頭が同じでも別の依頼です」** | **優先度**（★決める値が無い） |
| **状態**（`dw_state`） | |
| **なぜ表示されたか: 「COMPLETE でも BLOCKED でもないため」** | |

> **★30字の根拠（実測・D-72）: 先頭N字の異なり数は 20字→66種 / ★30字→113種 / ★40字→113種（増えない）/ 120字→124種。**
> **★先頭は「区別」には使えない**——**全文が異なるのは154/155 で重複は1組だけ** ∴ **42件が同じ先頭を持つのは内容が同じだからではない。★併記を必ず書く**（`G-73`）。

## 4. ★出さないものの書き方（★空欄にしない）
| 項目 | **出す文言** |
|---|---|
| **開始日** | **「開始日は出せません（front door から取れないため）」** |
| **優先度** | **「優先度は出せません（前に出す順番を決める値が 2DER に無いため）」** |
| **停止中** | **「止まっている依頼が N件 ありますが、この一覧の対象外です」**（★「0件」と書かない） |

## 5. ★変えないもの
- **★振る舞いを変えない。★色を使わない**（`ok` も `bad` も）。
- **★`/api/state` の返りを1つも変えない。** **★区分を減らさない・増やさない。**
- **★LLM に分類も要約もさせない。★名前を作らない**（`G-73`）。
- **★キャッシュを作らない。★並列化しない**（D-76 の裁定を引き継ぐ。**★もう要らないはずである。0.004秒 で終わる**）。
- **勘定科目**（`G-62`/`G-64`〜`G-68` 据え置き）。**★投入しない。**
- **★新しい endpoint は「必要なら可」**（MGR が D-74 で外した。**★ただし要らないなら作らない**）。

## 6. ★受入
| # | 条件 | 示し方 |
|---|---|---|
| **1a** | **★JS のソースに、全件を並べる箇所が無い** | **★差分で数える**（★ページ全体で数えない） |
| **1b** | **実際の画面から155行が消えたこと** | **★我々には示せない。「確かめていない」と書く**（★Taka にしか出せない） |
| **2** | **★4区分の和が、表示している合計と一致する** | **★数を固定しない**（§6-1） |
| **3** | **★合計が `_active_2der_tasks` が返す数と一致する** | **★別に数えた数を出さない** |
| **4** | **「いま前に出すべきもの」が「出せません」＋理由** | ソース |
| **5** | **★停止中が「0件」ではなく「この一覧の対象外」** | ソース |
| **6** | **★「終わった依頼と止まった依頼は入っていません」が出る** | ソース |
| **7** | **開いたとき、先頭30字＋併記が出る／開始日・優先度が「出せません」＋理由** | ソース |
| **8** | **★開いたときの所要**（★数字だけ出す。「速い」と書かない） | 実測 |
| **9** | **配信物の JS が `node --check` を通る** | **★配信物に対して**（`G-71`） |
| **10** | 既存が不変・非回帰 91/7 | **★走らせ方を BUILT に書くこと**（`G-74`・§8） |

### 6-1. ★受入2 の書き方
> **★153 は今日の値である。★固定すると明日 嘘になる。**
> **∴ 受入は「★各区分の和が、表示している合計と一致すること」とする。**

## 7. ★止まってよい場所
| # | |
|---|---|
| 1 | **区分の判定が §2-1 のとおりに書けない** → **★止めて報告** |
| 2 | **和が合計と一致しない** → **★止めて報告**（★合わせるために区分を動かさない） |
| 3 | **既定で ID を0件にすると、既存の機能が壊れる** → 止めて報告 |
| 4 | SPEC が2通りに読める |
| 5 | **既定の ID が、JS ではなくサーバ側で入っていた** → **★止めて報告** |
| **6** | **★母数が `_active_2der_tasks` では取れないと分かった** → **★止めて報告**（★別の集合に寄せない） |
| **7** | **★重くて出せない** → **★止めて報告**（★区分を減らして軽くしない） |

## 8. ★`G-74` について（★この build で1つだけ埋める）
> **★受入10 の非回帰は、★走らせ方がどの文書にも書かれていない**（12文書が引用し、再現コマンドは0件・D-77）。
> **★∴ 私は確認できない。★IMPL の申告を通すことになる。**
> **★本 build の BUILT には、★実際に走らせたコマンドをそのまま書くこと。**
> **★これは「テストを直す」ではない。★「走らせ方を1行 残す」だけである。** **★`G-74` 本体の解決は別 build。**

## 9. ★資料の同期
**★MGR の commit 後に私が同期する**（①直す ②再起動 ③配信物で検査 ④設計が受入確認 ⑤MGR が commit ⑥設計が同期）。

---
*CC-α D-73 BUILD SPEC **v2.0**（実装源・宛 IMPL・**v1.0 を置き換え、v1.0 は見ないこと**）。★一行=**155行の ID を5行の件数にし、数の横に「何を数えたか」を必ず書き、出せないものは理由つきで「出せない」と書く**。★**v1.0 から変わったのは3点だけ**=①**合計は `_active_2der_tasks` が返す数（実測153件）**で、155 と書くと**画面が並べている数と合わない**（D-78）②**停止中は「この一覧の対象外」とし件数を和に入れない**——**`BLOCKED` は `_active_2der_tasks` に入らないので母数の外から数えていた**③**判定は `claude_barrier` でなく `actor_role in ("MANAGER","CLAUDE_SENIOR")`**——**`claude_barrier` は2つのものを指しており 36 と 18 が食い違った**（D-74）。★既定は **ID を1件も出さず**「進行中の依頼 153件／この一覧に、終わった依頼と止まった依頼は入っていません」＋**背景で進んでいる／こちらの手番／この状態を知らない／いま前に出すべきもの＝出せません＋理由**＋合計＋「※ 止まっている依頼が 2件 ありますが、この一覧の対象外です」——**「その他」と書かない**（中身を隠す）、**停止中を「0件」と書かない**、**数だけ置かない**。★区分の判定は**すべて決定論**で母数＝`_active_2der_tasks`、値は `next_legal_operation(task_id, events=一度だけ読んだもの)` の返り、**判定順は ①`actor_id == "-"`（この状態を知らない）②`actor_role in ("MANAGER","CLAUDE_SENIOR")`（こちらの手番）③残り（背景で進んでいる）でないと重複する**、**いま前に出すべきものは判定しない（値が無い）**、**停止中は母数の外で数えない**。★実測（打ち切り無し）=母数153、`INDEPENDENT_AUDITOR/QWEN_AUDITOR` 67・`CODING_WORKER/QWEN_LIVECODER` 50・`-/-` 18・`MANAGER/CLAUDE` 13・`CLAUDE_SENIOR/CLAUDE` 5 ∴ **背景117／こちらの手番18／この状態を知らない18、和153＝母数153 で一致**——**数を実装源に固定せず「今日そうだった」と書く**。★**「この状態を知らない 18件」は言い換えないこと**——**「人を待っている」ではなく `_MAP` にその状態が無く既定値が返っているだけ**（D-74）で、**「担当者未定」「保留」と書かない。分からないものを分かったように書かない**（`G-72`）。★開いたときは `task_id`・**先頭30字＋「先頭30字です（全文ではありません）。先頭が同じでも別の依頼です」**・状態・「なぜ表示されたか」を出し、開始日と優先度は出さない（**30字の根拠は実測で 30字→113種・40字→113種と増えず、全文が異なるのは154/155 なので先頭は区別に使えない**・`G-73`）。★変えないもの=振る舞い・色・`/api/state` の返り・区分を減らさない増やさない・LLM に分類も要約もさせない・名前を作らない・**キャッシュも並列化もしない（0.004秒 で終わるのでもう要らないはず）**・勘定科目・投入しない、**新しい endpoint は必要なら可だが要らないなら作らない**。★受入10件=1a は差分で数える／**1b は我々には示せず Taka にしか出せない**／**4区分の和が表示している合計と一致（数を固定しない。153 は今日の値で固定すると明日 嘘になる）**／**合計が `_active_2der_tasks` が返す数と一致し別に数えた数を出さない**／「いま前に出すべきもの」は出せません＋理由／**停止中が「0件」でなく「この一覧の対象外」**／「終わった依頼と止まった依頼は入っていません」が出る／先頭30字＋併記と開始日・優先度の文言／**所要は数字だけ出し「速い」と書かない**／**配信物の `node --check`**／**非回帰は走らせ方を BUILT に書くこと**。★止まってよい場所7件（**和が合わないとき区分を動かさない**／**母数が `_active_2der_tasks` で取れないなら別の集合に寄せない**／**重くて出せないとき区分を減らして軽くしない**）。★`G-74` はこの build で1つだけ埋める=**非回帰の走らせ方が12文書に引用されながら再現コマンドは0件で CC-α は確認できず IMPL の申告を通すことになる** ∴ **BUILT に実際に走らせたコマンドをそのまま書く**——**「テストを直す」ではなく「走らせ方を1行 残す」だけで、`G-74` 本体の解決は別 build**。★資料の同期は MGR の commit 後に CC-α が行う。*
