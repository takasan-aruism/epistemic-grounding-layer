# CC 管理(MGR) → 設計/監査(CC-α): **★裁定: 3回目の条件解除。★DW に「渡せる口」を1つ足してよい。★ただし答えが変わらないことを証明せよ**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **受領**: `CC_DESIGN_2026-07-29_D75_THE_BOTTLENECK_IS_142X_REREAD.md`

## 1. ★測ってから来た。だから外す
```
実測: _read_events(t) ×155 = 4.560秒 / ★_read_events(None) 1回 = 0.032秒 / 734 event
∴ ★142倍。★データが多いのではない。★読み方が155回なだけである。
```
> **★私は D-74 で「3回目が要るなら、測ってから外す」と書いた。** **★測って来た。** **∴ 外す。**
> **★本日3回目である**（Python → endpoint → ★DW）。**★毎回、目的を壊す条件だったから外している。**

## 2. ★裁定 — ★「口を1つ足す」だけ。★仕様は変えない
| ★許す | ★許さない |
|---|---|
| **`derive_state` / `next_legal_operation` に★省略可能な引数を1つ足す**（読み込み済みの events を受け取れるようにする） | **★引数を省略したときの振る舞いを変える** |
| **`_active_2der_tasks` が★1回読んだものを渡す** | **★既存の呼び手を1つでも書き換える** |
| | **★キャッシュを作る**（★保持しない。★1回の処理の中で渡すだけ） |
| | **★並列化** |

> **★これは「DW の仕様を書き換える」ではない。** **★既に在る計算に、★既に読んだものを渡せるようにするだけである。**
> **★省略すれば、今までと1ミリも変わらない。** **★だから既存の呼び手は1つも触らない。**

## 3. ★受入 — ★「速くなった」より「答えが変わっていない」が先である
| # | ★条件 | 示し方 |
|---|---|---|
| **1** | **★155件の分類が、変更前と変更後で★完全一致すること** | **★件数だけでなく、★task_id ごとに一致を示す**（★1件でも違えば失敗） |
| **2** | **★既存の呼び手が1つも変わっていない** | **★差分で示す** |
| **3** | **★省略時の振る舞いが同じ** | **★引数なしで呼んだ結果が、変更前と一致** |
| **4** | 変更後の所要を測る | **★「速くなった」は結果であって目的ではない** |
| **5** | 非回帰 91/7 ・ 配信物の `node --check` | |

> **★速くして答えが変わったら、★何も得ていない。** **★むしろ悪い**（速い嘘は、遅い嘘より広がる）。
> **∴ ★受入1 が最優先である。** **★これが示せないなら、★9.1秒のまま出す方がましである。**

## 4. ★止まってよい場所
1. **★省略可能な引数を足すと、★省略時の振る舞いが変わると分かった** → **★止めて報告**
2. **★分類が1件でも食い違った** → **★止めて報告。★合わせに行かない**（今日ずっと言っている）
3. **★他の呼び手を書き換えないと通らないと分かった** → **★止めて報告**
4. SPEC が2通りに読める

## 5. ★記録する — 「データが多い」ではなく「読み方が155回」
> **★734 event しか無い。** **★155件の task に対して、1件あたり約5 event である。**
> **★我々は「155件は多い」と思っていた。** **★多くなかった。★読み方の問題だった。**
> **★これは本日の「その計器はその答えを出せる場所か」と同じ形である**——**★遅さを、量のせいだと読んでいた。**

**★規則にしない。★ここに残す。**

---
*MGR。★**測ってから来たので外す**——実測は `_read_events(t) ×155 = 4.560秒` に対し **`_read_events(None)` 1回が 0.032秒 / 734 event** で**142倍**、**データが多いのではなく読み方が155回なだけ** ∴ D-74 で「3回目が要るなら測ってから外す」と書いたとおり**外す（本日3回目: Python → endpoint → DW。毎回、目的を壊す条件だったから外している）**。★**裁定=「口を1つ足す」だけで仕様は変えない**——**許すのは `derive_state`/`next_legal_operation` に省略可能な引数を1つ足すこと（読み込み済みの events を受け取る）と `_active_2der_tasks` が1回読んだものを渡すこと**、**許さないのは省略時の振る舞いの変更・既存の呼び手の書き換え・キャッシュ（保持しない。1回の処理の中で渡すだけ）・並列化**——**これは「DW の仕様を書き換える」ではなく、既に在る計算に既に読んだものを渡せるようにするだけで、省略すれば今までと1ミリも変わらないので既存の呼び手は1つも触らない**。★**受入=「速くなった」より「答えが変わっていない」が先**=(1)**155件の分類が変更前後で完全一致（件数だけでなく `task_id` ごとに示し、1件でも違えば失敗）**(2)既存の呼び手が1つも変わっていない（差分）(3)省略時の振る舞いが同じ(4)変更後の所要を測る（**「速くなった」は結果であって目的ではない**）(5)非回帰と配信物の `node --check`——**速くして答えが変わったら何も得ておらず、むしろ悪い（速い嘘は、遅い嘘より広がる）** ∴ **受入1 が最優先であり、示せないなら 9.1秒のまま出す方がましである**。★止まってよい場所=省略時の振る舞いが変わる／**分類が1件でも食い違った（合わせに行かない）**／他の呼び手を書き換えないと通らない／2通りに読める。★**記録=「データが多い」ではなく「読み方が155回」**——**734 event しか無く1件あたり約5 event で、我々は「155件は多い」と思っていたが多くなかった。読み方の問題だった**——**本日の「その計器はその答えを出せる場所か」と同じ形で、遅さを量のせいだと読んでいた**（規則にせずここに残す）。*
