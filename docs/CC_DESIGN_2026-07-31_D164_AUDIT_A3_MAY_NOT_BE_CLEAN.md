# 【監査】(a)(b) を取り直した — **★(b) は一致。★(a) は「唯一の成立」が★厳密には崩れる**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 10:3x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **受領**: `CC_MGR_2026-07-31_D164_VARIANCE_SWAMPS_THE_DIFFERENCE.md`
- **★新しい名前で置いた** ／ **★`:8005` を私は叩いていない**（★保存済みの出力を読んだだけ）

---

# 1. ★(b) B・C の②は本当に0か → **★0で一致（6本とも）**

| 出力 | ★私の実測 |
|---|---|
| B-1 / B-3 / C-1 / C-3 | **①に具体値 0件** |
| **B-2**（★英語） | **★全文に具体値が1つも無い**（`rss`/`RSS` は★フィールド名として2〜3回。`703029` 等は0） |
| **C-2**（★英語） | **★`703029` は在るが、★`test_plan` の中である**——逐語: **`"Test with existing PIDs (e.g., 703029) to verify correct RSS/memory parsing."`** ∴ **★①ではない** |

**★IMPL の判定は6本とも正しい。**

## 1-1. ★私の計器の誤り（★本日5回目・★自己申告）
```
★私は最初、★日本語の見出し（「1. 既に確認できていること」）で①を切り出した。
★★B-2 と C-2 は★英語で書かれており、★切り出せず「①の長さ 0字」と出た。
★★これを「①が無い」と読みかけた。★実際は「★私の切り出しが当たらなかった」だけである。
★★英語で切り直し、★さらに全文で具体値の位置を目視して確定した。
★★本日5回目の同型である。★★私自身が SPEC に「部分一致で判定せず実物を目視する」と書いている。
```

---

# 2. ★(a) A-3 は本当に3条件を満たすか → **★厳密に読むと★③を満たさない**

## 2-1. ★満たしているもの（★実物で確認）
```
★①3区分       : ○（「### 1. 既に確認できていること／### 2. 未確認のこと／### 3. 新規実装が本当に必要なこと」）
★②①に観測値   : ○（★逐語: 「GPU 0/1のメモリ使用量（★約30.5GB/31.2GB）とデバイス名（★RTX 5090）」
                      「★`qwen36_vllm`コンテナが5日間稼働していることが確認できている」）
```

## 2-2. ★★満たさない可能性のあるもの（★ここが今回の要）
**★③の1件目（逐語）**:
> **「`/proc`ベースのメモリ詳細取得ツール: … `/proc/<pid>/status`、`/proc/<pid>/fd`、`/proc/<pid>/cgroup`、`/proc/<pid>/smaps`を安全に読み取り、★RSS/VMS/オープンFD数/cgroup制限を構造化JSONで出力するスクリプトの実装」**

**★①の逐語**:
> 「`ps`により、PID、★RSS（実メモリ使用量）、コマンド名が★取得できている」

```
★★∴ ★③の出力に ★RSS が含まれている。★①では「RSS は取得できている」と書いてある。
★★基準③の逐語は「★③が空か、★①に無いものだけになっている」である。
★★厳密に読む → ★満たさない（★①に在るものが③に入っている）
★★緩く読む   → ★満たす（★新規性は VMS/FD/cgroup/smaps に在り、★RSS は出力に同梱されるだけ）
```

## 2-3. ★私の判定案と、その理由
> ### **★私は「厳密に読む」を採る ∴ ★A-3 は 2/3。★9本すべてで「分けられた」は★0本になる。**

```
★理由1: ★本日ずっと守ってきた線がこれである——「★1つでも欠けたら成立と書かない」「★概ねと書かない」
★理由2: ★受入3（既に観測済みの能力を新規作成対象として重複計画しない）と★同じ問いである。
        ★★本番で我々が × にしたのは、★まさに「既に取れているものを作り直す計画」だった。
        ★★ここで緩く読むと、★本番と別の物差しを使うことになる。
★★ただし ★これは判定であって事実ではない ∴ ★確定は MGR。★私は自分の判定を自分で承認しない。
★★緩い読みを採るなら、★「なぜ本番では厳しく、ここでは緩いのか」を★1行 書く必要が在る。
```

---

# 3. ★MGR §3 の指摘に同意する（★これが今回いちばん重い）
```
★A・B・C の3条件とも ★Taka の問い（出力契約）が入っている ∴ ★曖昧さ③は★振っていない。
★★本番（契約なし）は受入3・4 に4回とも届かず、★契約ありの A は★1回だけ 3/3 に届いた（★私の判定では0回）。
★★∴ ★交絡している。★「入力の選別」と「出力契約」を★同時に変えたまま比べている。
★★私も同じ穴に落ちていた: ★私は SPEC で A に「問いを末尾に足す」と書いた ＝ ★契約を3条件に固定した。
```

---

# 4. ★登記した（★MGR `D-164` §5 の指示。★id は設計が採った）
```
★G-88: ★`finish_reason=length` が2本（A-2 は completion_tokens=8192 を使い切り／C-3 は末尾切断）
       ★max_tokens 不足に見えるが、★A は「PLAN を JSON で出せ」と「3つに分けよ」が★1つの prompt に同居している
       ★`G-83` と同じ型の2例目 ∴ ★budget を上げる前に prompt を目視する
       ★★測定設計上の含意も note に書いた: ★A は「分けられるか」だけを測っていない＝★私の SPEC の欠陥
★資料: gap 87 → 88。★JSON と MD の食い違い 0 件。
```

---

# 5. ★私は次の一手を出さない（★理由は前回と同じ・★変えない）
```
★★交絡（§3）が残ったままでは、★次に何を振っても解釈が割れる。
★★「契約を外した条件」を足すのは★Taka の指示の範囲（「実際のplanner入力をそのまま渡して」）を出る。
★★∴ ★これは測定設計の変更であり、★MGR の裁定または Taka の判断である。★私は材料までを出す。
```

---
**決めたこと**: **①(b) は6本とも一致（B-2 は全文に具体値0、C-2 の `703029` は `test_plan` の中）——ただし私は最初 日本語の見出しで切り出して「①が無い」と読みかけた（本日5回目の計器の誤り・自己申告） ②★(a) A-3 は③の1件目の出力に `RSS` を含み、①では「RSS は取得できている」と書かれている ∴ 厳密に読むと③を満たさず、9本すべてで「分けられた」は0本になる——本番で我々が × にしたのと同じ問いなので、ここで緩く読むと別の物差しを使うことになる（確定は MGR） ③A・B・C の3条件とも出力契約が入っており曖昧さ③を振っていない＝交絡している（私の SPEC がそう固定した） ④`G-88` を登記（`finish_reason=length` は budget でなく prompt を見る・`G-83` と同型の2例目）。**
