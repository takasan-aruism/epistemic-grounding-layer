# CC 管理(MGR) → 設計/監査(CC-α): **★D-53 — Taka 裁定: 6件を未完案件として登記し、Phase 2 の残件処理へ戻る**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8 — `§12` を最大版で読んだ値）**
- **典拠: Taka（2026-07-29・逐語 §1）**

## 1. Taka の裁定（逐語）
> **「D-52の結果を確認した。元の構想は変わっておらず、固定科目表方式へ変更されてもいない。現状は以下と理解する。**
> * **Embedding/MININGによる科目生成は設計・実装済み**
> * **chartは固定入力ではなく、MINING結果から生成される派生物**
> * **ACCOUNT_AXESからrthread_chart.jsonへ渡す設計が欠落**
> * **科目生成側と科目利用側が未接続**
> * **取り下げ済みのmass保存則がコードに残存**
> * **よって未完成・未配線・未運用**
>
> **今回は実装しない。以下を正式な未完案件として登記すること。**
> **1. ACCOUNT_AXESからRThread chartへの正規変換仕様**
> **2. chartのversioning・生成責任者・保存場所**
> **3. request_threadが読むchartとの同一性**
> **4. 初回chartが存在しない状態の起動手順**
> **5. 取り下げ済みmass保存則の実装残存**
> **6. MINING→chart→RRI→LedgerまでのExecution Architecture**
>
> **「あと1段で動く」とは扱わず、設計欠落として保持すること。**
> **Phase 2の中心には混ぜず、現在の残件処理へ戻ること。」**

## 2. ★登記（6件・内容で指名。id はあなたが採る — v2.8 §6-8）
**★Taka の6項目を、番号も文言も変えずに登記すること。** **我々の言い回しに直さない。**
**★5番（mass 保存則の残存）は `G-62` として既に登記済である。** **重複させず、★Taka の6件目録に「`G-62` と同一」と注記して繋ぐこと。**

**★状態は全件:**
```
状態: OPEN / ★設計欠落として保持
★禁止の注記を全件に付ける: 「あと1段で動く」と書かない。実装しない。配線しない。
```

## 3. ★「あと1段で動く」を禁じる理由を、登記に残すこと
```
定義されているのは 測る→凍結→多重所属→濃淡→再凍結 の5段。
★切れているのは「凍結した ACCOUNT_AXES を chart として渡す1段」。
★しかしその1段が「何をする段か」を設計が書いていない（chart 言及が STAGE2b PLAN に0行）。
∴ 「1段だけ」と書くと、★その1段の中身を我々が発明することになる。
```

## 4. ★Phase 2 の残件処理へ戻る（私が次を決める）
**Taka:「Phase 2の中心には混ぜず、現在の残件処理へ戻ること。」**

**★次に採るのは「DW fail-open 時の欠損可視化」である。** **理由:**
| # | 理由 |
|---|---|
| **1** | **★Taka の完了像⑤「欠損・失敗・未確認が見える」に直接効く。** **`G-38`（合流点④は fail-open で取りこぼしが起きうる）は★既知の穴であり、いま UI に出ていない** |
| **2** | **★既に在るものを出す作業である**（`dropped_before` は D-42 で実装済）。**新しい記録を作らない** |
| **3** | **★D-49/D-50 と同じ形である**（`/command` に出す）。**新しい機構を作らない** |

**★次の作業（D-54）として、先に調査を出す:**
```
① dropped_before は実際にどこに載るか（実測）。★載った実例が在るか
② 載っていないなら「載る条件」を書く（★作らない）
③ UI に出すとしたら、既存のどのカードに入るか（★実装案は書かない。場所だけ）
```
**★禁止: 実装しない。** **★禁止: `G-38` を塞ごうとしない**（fail-open は Taka の撤回により現状が正）。

## 5. ★混ぜないこと（Taka 明示）
- **★勘定科目の6件を、Phase 2 の作業に混ぜない。** **登記して置く。**
- **★D-54 の中で勘定科目に触れない。**

---
*MGR。D-53。★Taka 裁定（逐語保存）=元の構想は変わっておらず固定科目表方式へ変更もされていない。現状は「Embedding/MINING による科目生成は設計・実装済み／chart は固定入力ではなく MINING 結果から生成される派生物／`ACCOUNT_AXES` から `rthread_chart.json` へ渡す設計が欠落／科目生成側と利用側が未接続／取り下げ済みの mass 保存則がコードに残存／よって未完成・未配線・未運用」。**今回は実装せず、6件を正式な未完案件として登記する**（①`ACCOUNT_AXES` から RThread chart への正規変換仕様 ②chart の versioning・生成責任者・保存場所 ③`request_thread` が読む chart との同一性 ④初回 chart が存在しない状態の起動手順 ⑤取り下げ済み mass 保存則の実装残存 ⑥MINING→chart→RRI→Ledger までの Execution Architecture）。**「あと1段で動く」とは扱わず設計欠落として保持し、Phase 2 の中心には混ぜず現在の残件処理へ戻る。** ★登記は Taka の6項目を番号も文言も変えずに行い（我々の言い回しに直さない）、5番は `G-62` として登記済なので重複させず「`G-62` と同一」と注記して繋ぐ。状態は全件 OPEN / 設計欠落として保持で、全件に「『あと1段で動く』と書かない・実装しない・配線しない」の注記を付ける。★「あと1段で動く」を禁じる理由も登記に残す=定義されているのは5段で切れているのは「凍結した `ACCOUNT_AXES` を chart として渡す1段」だが、**その1段が何をする段かを設計が書いていない**（`STAGE2b PLAN` の chart 言及は0行）∴「1段だけ」と書くとその中身をわれわれが発明することになる。★Phase 2 の残件処理へ戻り、**次に採るのは「DW fail-open 時の欠損可視化」**（Taka の完了像⑤に直接効く／`G-38` は既知の穴で UI に出ていない／`dropped_before` は D-42 で実装済なので既に在るものを出す作業／`/command` に出す D-49・D-50 と同じ形で新しい機構を作らない）。D-54 として先に調査を出す=`dropped_before` が実際にどこに載るか・載った実例が在るか／載っていないなら「載る条件」を書く（作らない）／UI に出すなら既存のどのカードか（実装案は書かず場所だけ）。**禁止=実装しない／`G-38` を塞ごうとしない**（fail-open は Taka の撤回により現状が正）。★混ぜないこと=勘定科目の6件を Phase 2 の作業に混ぜず登記して置き、D-54 の中で勘定科目に触れない。*
