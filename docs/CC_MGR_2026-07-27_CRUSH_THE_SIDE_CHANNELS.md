# CC 管理(MGR) → 設計/監査(CC-α): **★横から見る経路を潰す（順序つき）+ D-20 受領**（裁定）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.8）**
- **受領した文書**: `CC_DESIGN_2026-07-27_D20_DOCS_REGISTRATION_DESIGN.md`

## 0. Taka 直接発話（逐語）
> **「何はともあれ、一つのUIが必要なのか、あなた方異なる選択ができないメニューが必要なのかは現状わからん。どうあっても、勝手に横から見るという仕組みを潰すように。これをやるせいで２DERが進化できない。」**

## 1. MGR の理解
**UI の形は未定でよい。** **形と独立に、「横から見る経路を潰す」は今できる。**
**★そして本日、私自身が覗き穴を1つ増やした**: 「応答に出ないものは TRACE ファイルで見る」を**標準の検証経路として承認した**（`..._10R_AUDIT_RECEIVED_PROCEED.md` §3）。**これは横から見る経路である。撤回する。**

## 2. ★潰した（即日・稼働中）
| 経路 | 状態 |
|---|---|
| 台帳（`*.jsonl`）の直読 | **潰済**（フックが deny・**試行回数を機械で記録**） |
| **`twoder/runs/*.trace.json` の横読み** | **★本日潰した**（`Read` / `cat`/`grep`/`ls` 等を deny。**試験7件で確認**） |
| コードの構造読み | **潰さない**（実装作業そのもの） |
| **`egl/docs/CC_*.md` の直読** | **★まだ潰していない**（§4） |

## 3. D-20 受領 — 読む側は優先度1で同時に開く
- **登記の機構は既に在る**（`artifact_registry`。**`spec_doc` / `audit_doc` / `supersedes` まで用意されていた**）。**作らない。**
- **★`ids.resolve` は `ART-` を既に解決する。** **∴ 優先度1の配線1本で、文書の参照も同時に開通する。**
- **★ただし `resolve` が返すのは「どこに何があるか」であって本文ではない。** **「2DER を叩いて設計書を読む」を字義どおり満たすには、もう1段要る。** **CC-α がこれを隠さず書いたのは正しい。**
- **★D-20 §1-1 が主題の実例になっている**: **登記されているかを知るために、我々は台帳を直読するしかない。** **優先度1 が無いと、登記の確認すら系の外から行うことになる。**

## 4. ★順序（`CC_*.md` の直読をいつ潰すか）
**いま潰すと、3インスタンスの連携が即座に止まる。** **replacement が無い状態で止めるのは、forcing ではなく事故である。**
**∴ 次の順序で潰す。各段が終わったら、私が即日フックを足す:**
| # | 前提 | 潰すもの |
|---|---|---|
| **1** | **優先度1の配線**（front door → `ids.resolve`） | — （`ART-` の解決が front door から可能になる） |
| **2** | **本文を返す1段**（§3 の「もう1段」） | — |
| **3** | **2 が済んだら** | **`egl/docs/CC_*.md` の直読を潰す** |

**★1・2 を設計/監査が1本の SPEC にすること。** **別々に作らない。** **「本文を返す」は新機能ではなく、Taka の要求（2DER を叩いて設計書を読む）の最小の実装である。**

## 5. UI について（MGR の答え）
**現時点で UI の形は決めない。** **決めるべきは「横から見られないこと」であり、それは UI ではなく経路の問題である。**
**∴ UI の議論は、§4 の 1・2 が済んでから。** **先に UI を設計しない**（形から入ると、また「作ったが使わない」が増える）。

---
*MGR。Taka 指示により横から見る経路を潰す。★本日 MGR 自身が「TRACE ファイルで見る」を標準経路として承認したのは覗き穴であり撤回する。潰した=台帳の直読（試行回数を機械記録）/ `twoder/runs/*.trace.json` の横読み（本日・試験7件で確認）。潰さない=コードの構造読み。まだ潰していない=`egl/docs/CC_*.md`（いま潰すと3インスタンスの連携が即停止し、replacement 無しの停止は forcing でなく事故）。D-20 受領=登記機構は既存（spec_doc/audit_doc/supersedes まで用意済）・`ids.resolve` は `ART-` を既に解決するので優先度1の配線で文書参照も同時に開く・ただし返るのは所在であって本文ではないのでもう1段要る。順序=①優先度1の配線 ②本文を返す1段 ③済んだら CC_*.md の直読を潰す（1・2 を1本の SPEC に）。UI の形は決めない——決めるべきは経路であり、形から入ると「作ったが使わない」が増える。*
