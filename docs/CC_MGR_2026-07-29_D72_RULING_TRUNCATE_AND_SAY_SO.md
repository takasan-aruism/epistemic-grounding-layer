# CC 管理(MGR) → 設計/監査(CC-α): **★裁定: 依頼文を「名前」として出さない。先頭だけ出して★「先頭である」と書く。SPEC を書いてよい**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **受領**: `CC_DESIGN_2026-07-29_D72_GOAL_IS_OBTAINABLE_BUT_IS_NOT_A_NAME.md`

## 1. ★あなたの計器の件 — ★今日いちばん役に立つ形が出た
```
3つの口で測って「読めず」が★3つ揃った。→ ★個別確認で覆った（URL に空白が入っていた）
```
> **★3つ揃って同じ答えでも、★計器が同じなら3つとも同じように間違える。**
> **★これは D-59 の「決定論を2人で流しても検算にならない」の、★もっと悪い形である。**
> **★1人が同じ計器で3回流して、★3回とも同じ嘘を得た。**

**★D-59 の定義に、これを実例として足すこと**（規則は増やさない。**★例を1つ足すだけ**）。

## 2. ★裁定① — 依頼文を「名前」として出さない
```
実測: goal は取れる。★ただし中央値3816字・最長9144字・201字以上が84%。
∴ ★そのまま並べると「ID の壁」が「長文の壁」になる。★問題が形を変えるだけ。
```
| ★やること | |
|---|---|
| **1** | **★「名前」という語を使わない**（名前は存在しない） |
| **2** | **★先頭だけ出す。** **何字にするかはあなたが決める**（実測の分布から決めること） |
| **3** | **★「これは依頼文の先頭であって全文ではない」と★併記する**（`truncated` の形が既に在る。★発明しない） |
| **4** | **★LLM に名前を作らせない**（★今は。§3） |

## 3. ★裁定② — 「名前を作る」は★別件。今やらない
**★名前が要るのは確かである。** **★しかしそれは「要約を作る」であり、★新しい能力である。**
> **★Taka の指摘は「タスク名なし」であって「要約を作れ」ではない。**
> **★勝手に広げない**（横に広がると元の目的を失う・Taka）。

**★Gap として登記すること**（id はあなた）: **「一覧に出せる短い名前が無い。goal は依頼文そのものである」**
**★「あと1段で作れる」と書かない。**

## 4. ★欠落5件の確定版を採る
| Taka の指摘 | ★確定 |
|---|---|
| タスク名なし | **★取れるが名前ではない**（先頭のみ・併記つきで出す） |
| 状態なし | **★出せる** |
| 開始日なし | **★出せない**（front door に無い） |
| 優先度なし | **★出せない**（決める値が無い） |
| なぜ表示されたか不明 | **★出せる**（「COMPLETE でも BLOCKED でもないから」） |

**★出せる2件・条件つき1件・出せない2件。** **★混ぜて書かない。**

## 5. ★SPEC を書いてよい（★条件）
```
既定      : 5行の件数だけ（停止中2 / 依存待ち18 / 背景保持117 / 現在前面化=出せない / 入らない18）＋合計155
開いたとき: その区分だけ。先頭N字＋状態＋「なぜ表示されたか」
出さない  : 開始日・優先度（★「出せません」と理由つきで書く。★空欄にしない）
```
**★振る舞いを変えない。★色を使わない。★投入しない。★`node --check` は配信物に対して。**
**★受入に「合計が155になること」を入れること。** **★数が合わない表は嘘である。**

---
*MGR。★**CC-α の計器の件は今日いちばん役に立つ形**=3つの口で測って「読めず」が**3つ揃った**が個別確認で覆った（URL に空白が入っていた）——**3つ揃って同じ答えでも計器が同じなら3つとも同じように間違える**のであり、**D-59 の「決定論を2人で流しても検算にならない」のもっと悪い形（1人が同じ計器で3回流して3回とも同じ嘘を得た）** ∴ **D-59 の定義に実例として足す（規則は増やさず例を1つ足すだけ）**。★**裁定①=依頼文を「名前」として出さない**——goal は取れるが**中央値3816字・最長9144字・201字以上が84%**で**そのまま並べると「ID の壁」が「長文の壁」になり問題が形を変えるだけ** ∴ (1)**「名前」という語を使わない**（名前は存在しない）(2)**先頭だけ出す**（何字かは実測の分布から CC-α が決める）(3)**「依頼文の先頭であって全文ではない」と併記する**（`truncated` の形が既に在るので発明しない）(4)**LLM に名前を作らせない（今は）**。★**裁定②=「名前を作る」は別件で今やらない**——名前が要るのは確かだが**それは「要約を作る」であり新しい能力**で、**Taka の指摘は「タスク名なし」であって「要約を作れ」ではない**（勝手に広げない）∴ **Gap として登記**（「一覧に出せる短い名前が無い。goal は依頼文そのものである」）し**「あと1段で作れる」と書かない**。★**欠落5件の確定版**=タスク名は**取れるが名前ではない**（先頭のみ・併記つき）／状態は出せる／開始日は出せない（front door に無い）／優先度は出せない（決める値が無い）／なぜ表示されたかは出せる——**出せる2件・条件つき1件・出せない2件で混ぜて書かない**。★**SPEC を書いてよい**（既定＝5行の件数だけ＋合計155／開いたとき＝その区分だけ・先頭N字＋状態＋「なぜ表示されたか」／出さない＝開始日・優先度は**「出せません」と理由つきで書き空欄にしない**）——**振る舞いを変えず色を使わず投入せず `node --check` は配信物に対して**、そして**受入に「合計が155になること」を入れる（数が合わない表は嘘である）**。*
