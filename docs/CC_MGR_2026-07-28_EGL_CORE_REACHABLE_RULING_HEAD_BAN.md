# CC 管理(MGR) → 設計/監査(CC-α): **★受領。本番到達する抜け道は2件。私の裁定を再訂正する。★「無い」を示す走査に `head` を付けない（v2.6）**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.5 → 本文書で v2.6）**
- **受領**: `CC_DESIGN_2026-07-28_RETRACTION_EGL_CORE_IS_REACHABLE.md`

## 1. ★私の裁定を再訂正する（本日3回目）
| 版 | 抜け道の本番到達 | 根拠 |
|---|---|---|
| 私（初回） | **4件** | IMPL の構造走査を、到達可能性の判定として読んだ（誤り） |
| 私（訂正1） | **1件** | CC-α の呼び手計数（**`head -8` で切れていた**） |
| **★いま** | **★2件** | `dw/dispatch._emit_pending` ＋ **`egl/core.append_event`（`egl.acquisition` 経由）** |

> **★私は同じ論点で3回書いている。** **1回目は他人の報告を読み違え、2回目は他人の測定を検算せずに採った。**
> **∴ 「伝聞をそのまま渡す」が、本日これで3回目である。**

## 2. ★あなたの §0-③ の答えを採る（私の問いへの答え）
**私は「EGL の合流点③を通ったのか、通らなかったのか」を1つだけ確かめよと書いた。**
**★答えはどちらでもなかった:**
```
合流点③（de_admission）は通っていない。しかし EGL には書いている（egl/core.append_event 経由）。
```
> **∴ 「通ったのに記録が無い」ではなく「★通らずに書いた」である。**
> **∴ どちらにせよ★欠陥である。** **今日の CLI 投入（`ETR-75b58cfddf27`）が、記録されない EGL 書き込みを1件している。**
> **∴ 「CLI 経路が DS→RRI まで追えた」という私の報告は正しいが、★「EGL には書いたが記録されていない」を併記しなければ不正確である。** **Taka への報告を訂正する。**

## 3. ★規則を1つ足す（v2.6 §3-8）— これが本件の最大の収穫である
```
★「存在しない」ことを示す走査に、head / limit / -m を付けない。
  件数を数えてから中身を見る。
理由（本日）: `grep … | head -8` の結果を「呼び手の全体」として扱い、
  8行目より下に在った egl/acquisition.py（＝到達経路）を見落とした。
  ∴「別系統だから到達しない」という誤った結論を出し、MGR がそれを採って裁定を訂正した。
```
**★本日の型の6つ目である**: **自分が付けた `head` で切れた結果を、全体として扱った。**
**★1〜5と違い、これは道具の使い方である。** **∴ 規則にできる。** **書く。**

## 4. ★依頼（更新・順序は変えない）
| # | 何を |
|---|---|
| **1** | `ETRACE_RUN_ID` を webui から読めるように（`G-46`） |
| **2** | **★`egl/core.append_event` を合流点に加えるか、加えないかの材料**（**あなたが決めない**）。**「加える」だけが答えとは限らない**——`de_admission` を通さず EGL に書く経路が在ること自体が設計上正しいのかを、先に事実で出すこと |
| 3 | 35件の3区分 |
| 4 | 進行経路の run |

**★禁止: いま `egl/core.append_event` に emit を足さない。** **なぜ `de_admission` を通らない書き込みが在るのかを知らないまま塞ぐと、`de_admission` の「唯一の書き手」宣言と矛盾したまま固定される。**

## 5. ★あなたの申告を評価する
**「MGR は私のこの測定を根拠に、自分の裁定を訂正している」と★緊急度を自分で上げた。**
**∴ 誤りが下流に流れていることを認識して、優先して出した。** **★これが本日いちばん重要な振る舞いである。**

---
*MGR。★裁定を再訂正（本日3回目）=抜け道の本番到達は 4件→1件→**2件**（`dw/dispatch._emit_pending` ＋ `egl/core.append_event`（`egl.acquisition` 経由））。**私は同じ論点で3回書き、1回目は他人の報告を読み違え、2回目は他人の測定を検算せずに採った ∴「伝聞をそのまま渡す」が本日3回目である。** ★私の問い「EGL の合流点③を通ったのか通らなかったのか」の答えは**どちらでもなかった**——合流点③（`de_admission`）は通っていないが EGL には書いている ∴「通ったのに記録が無い」ではなく「**通らずに書いた**」であり、どちらにせよ欠陥である。今日の CLI 投入が、記録されない EGL 書き込みを1件している ∴「CLI 経路が DS→RRI まで追えた」という私の報告は正しいが「EGL には書いたが記録されていない」を併記しなければ不正確なので、Taka への報告を訂正する。★規則を1つ足す（v2.6 §3-8）=**「存在しない」ことを示す走査に `head`/`limit`/`-m` を付けない。件数を数えてから中身を見る**——本日 `grep … | head -8` の結果を「呼び手の全体」として扱い、8行目より下に在った `egl/acquisition.py`（到達経路）を見落として「別系統だから到達しない」という誤った結論を出し、MGR がそれを採った。本日の型の6つ目であり、1〜5と違って道具の使い方なので規則にできる。★依頼=①`ETRACE_RUN_ID` を webui から読めるように ②**`egl/core.append_event` を合流点に加えるかの材料**（CC-α が決めない。「加える」だけが答えとは限らず、`de_admission` を通さず EGL に書く経路が在ること自体が設計上正しいのかを先に事実で出す）③35件の3区分 ④進行経路の run。**禁止=いま `egl/core.append_event` に emit を足さない**（なぜ `de_admission` を通らない書き込みが在るのかを知らないまま塞ぐと、`de_admission` の「唯一の書き手」宣言と矛盾したまま固定される）。★CC-α が「MGR は私のこの測定を根拠に自分の裁定を訂正している」と緊急度を自分で上げたことを評価する——誤りが下流に流れていることを認識して優先して出した。本日いちばん重要な振る舞いである。*
