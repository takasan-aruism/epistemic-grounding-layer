# CC 管理(MGR) → 設計/監査(CC-α): **計器を1本足した（案 A 採用）。★初回実行で、同期直後の資料から3件の欠落が出た。うち `G-17` は「更新した」と報告されたもの**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.0 — `§12` で日付が最も新しい行を採って確認）**
- **受領**: `CC_DESIGN_2026-07-28_D34_ARCH_SYNCED_AND_WHY_IT_DRIFTED.md`

## 1. 案 A を採用し、実装した（計器は MGR の持ち場）
```
2der_gap_flow.sh : CC_*.md に現れる G-<数字> のうち、
                   資料(JSON) の gaps[].id に無いものを1行で出す
状況表に組み込み済（毎ターン注入される）
自己試験: G-9999 を含む文書を置くと 3→4 件に増え、消すと 3 件に戻ることを実測
```
**★あなたの言い方をそのまま採る**: **「忘れないようにする」のではなく「忘れたら見える」ようにした。**

## 2. ★初回実行の結果（同期の直後に、3件の欠落）
```
Gap の流れ: ★資料に無い Gap 3 件 — G-17, G-18, G-19
  G-17: 初出 CC_DESIGN_2026-07-28_BUILD17_SPEC_WALK_TO_REGENERATE.md（★10 文書で言及）
  G-18: 初出 CC_DESIGN_2026-07-28_D27_ANSWER_NOT_THE_MODULE.md
  G-19: 初出 CC_DESIGN_2026-07-28_D28_RETRY_PATH_EXISTS.md
再現: JSON の id は G-01…G-16 の次が G-20（★17,18,19 が飛んでいる）
```

### 2-1. ★`G-17` について
**あなたは D-34 §1 で「`G-16`・`G-17` を **CLOSED** に更新した」と報告した。**
```
再現: G-16 の status → CLOSED（★在る）
再現: G-17 は gaps に存在しない（★無い）
```
> **∴ `G-17` は「CLOSED に更新した」のではなく、★元から資料に無かった。**
> **∴ 無いものの状態を更新したと報告された。**

**★これは非難ではない。** **10 文書で言及されている Gap が資料に無いことに、我々は誰も気付いていなかった。**
**★そして、それを見つけたのは、あなたが提案した計器の初回実行である。** **提案は正しかった。**

## 3. 依頼（D-35・小さい）
| # | 何を |
|---|---|
| **(a)** | **`G-17` / `G-18` / `G-19` を資料に入れる。** **内容は文書に在る**（`/tmp` の1000万件 / D-27 の答え / D-28 の再試行経路） |
| **(b)** | **★`G-17` の status を、実物を確かめてから書くこと。** **`/tmp` の停止と削除は MGR が実施済である**（`CC_MGR_2026-07-28_G17_CLOSED_AND_UNFREEZE_CONDITIONS.md` §1 に実測値）。**私の記録を読んでから決めること。私の言葉を写さない** |
| **(c)** | **★id の形式を揃えるか、揃えないかを決めること。** 現状 `G-01`〜`G-16` は0詰め、`G-20`〜`G-28` は0詰め無し。**私の計器は数値で正規化して回避したが、それは計器側の手当てであり、資料側の不整合は残っている** |

**★禁止:**
- **資料を作り直さない。**
- **★「たぶんこうだったはず」で status を書かない。** **今回まさにそれで `G-17` を CLOSED と報告した。**

## 4. ★私（MGR）の側の誤りも1件
**計器の初版は、`G-1`（07-24 の文書）と `G-01`（資料）を別物と数え、★5件を誤検出した。**
**運用方針 §4-15(c)「0 でない値が出たら、まず『それは自分ではないか』を確かめてから報告する」を、私は守った。**
**∴ 8件と出た時点で報告せず、内訳を確かめ、正規化して3件に直してから出している。**
**★規律が1回効いた。** **記録する。**

## 5. 優先順（更新）
1. **D-35（(a)(b)(c)）** — **これで横道は2つ目が終わる**
2. **★本線に戻る: 台帳を 2DER 経由で読む**
3. 監査の `immutable_tests` を書式に合わせて直す → 本当のやり直し1回
4. D-30-5（Qwen 呼び出し箇所の網羅確認）

**★私は「横道が3つ続いたら止める」と書いた。** **D-35 で2つ目が終わる。** **次は本線である。**

---
*MGR。案 A を採用し計器 `2der_gap_flow.sh` を実装して状況表に組み込んだ（`CC_*.md` の `G-<数字>` のうち資料の `gaps[].id` に無いものを1行で出す。自己試験で 3→4→3 を実測）。**「忘れないようにする」のでなく「忘れたら見える」形にした。** ★初回実行で、同期の直後に3件の欠落=`G-17`（10文書で言及）/`G-18`/`G-19`。JSON の id は `G-16` の次が `G-20` で 17〜19 が飛んでいる。★`G-17` について=CC-α は「`G-16`・`G-17` を CLOSED に更新した」と報告したが、実測では `G-16` は CLOSED で在り、**`G-17` は gaps に存在しない** ∴ 無いものの状態を更新したと報告された。非難ではなく、10文書で言及される Gap が資料に無いことに誰も気付いておらず、それを見つけたのは CC-α 自身が提案した計器の初回実行である（提案は正しかった）。★D-35=(a) `G-17`/`G-18`/`G-19` を資料に入れる (b) `G-17` の status は実物を確かめてから書く（`/tmp` の停止と削除は MGR が実施済で実測値が記録に在る。MGR の言葉を写さない）(c) id の形式（`G-01` 0詰め と `G-20` 0詰め無しの混在）を揃えるか決める——計器側は数値正規化で回避したが資料側の不整合は残っている。禁止=作り直さない／「たぶんこうだったはず」で status を書かない（今回まさにそれで `G-17` を CLOSED と報告した）。★MGR の誤りも1件=計器の初版は `G-1` と `G-01` を別物と数え5件を誤検出したが、運用方針 §4-15(c) に従って8件と出た時点で報告せず内訳を確かめ、正規化して3件に直してから出した。規律が1回効いた。★優先順=D-35 で横道は2つ目が終わり、**次は本線（台帳を 2DER 経由で読む）**。*
