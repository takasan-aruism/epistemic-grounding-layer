# 宛: Taka / 設計 / 監査 ―― 本線の仕切り直し：全件分類 と GM v1 の完了条件

**新しい実装をしていない。台帳・正本・今夜の記録から引いた。**

## 1. 全件分類（台帳の `status` と `note=next=` をそのまま写した）

```
全 item 134 ／ ★DONE 106 ／ DROPPED 1 ／ ★未了 27
```

| 分類 | 件数 | 中身 |
|---|---|---|
| **① 完了済み** | **106** | `status=DONE` |
| **② 進行中** | **1** | `EVO-0019`（`next=MGR`・dep 2） |
| **③ 発見済み未着手** | **14** | `PROPOSED` ―― EVO-0044/0055/0056/0057/0060/0061/**0075**/**0076** ／ PARALLEL-OPS ／ IMPL-PLATFORM 系4 ／ OFFRAMP |
| **④ 後回し** | **12** | `DEFERRED` 9 ＋ `PLANNED` 3（EVO-0010/0018/0069 ほか） |
| **⑤ Taka 裁定待ち** | **★0** | `next=TAKA` の item は**1つも無い** |

### ★`next` 欄の実態

```
next=MGR      1件
next=NONE    11件
★next 欄なし  14件   ← ★待ち相手が「分からない」。「待っていない」ではない
next=TAKA     0件
```

**14件は観測欠落。** ここを `UNKNOWN` と `NONE` に分けるのが `item_waiting_on` の目的だが、**部品は未完**（`TASK-2DER-5C8E13D7` が `JUDGE_REQUIRED`）。

## 2. GM v1 の完了条件（Taka 定義を既存設計に1本化）

> **1案件（＝台帳の ITEM 1件）について、Claude MGR が進行判断をせずに次の6つを通せること。**

| # | 要件 | 既存資産 | 状態 |
|---|---|---|---|
| 1 | **状態取得** | ITEM `status` ／ TASK `/api/state`・`_MAP` | **○ 在る** |
| 2 | **待ち相手取得** | TASK: `whose_turn`（2026-08-18 実装・LLM 0回） ／ **ITEM: 無い** | **✗ 未実装** |
| 3 | **実行可否判定** | `decide_rearm_v2` ＋ 門（2026-08-18 実装・境界10ケース一致） | **○ 在る** |
| 4 | **Domain への委譲** | `to_domain` ＋ `DOMAIN_OPERATIONS`（2026-08-18） | **○ 在る** |
| 5 | **結果回収** | `receive_finished`（機械が置いて commit まで） | **○ 在る** |
| 6 | **完了/継続/上申 判定** | 完了: TASK `completion_blockers` ○ ／ ITEM `acceptance` **散文** ✗ ／ 上申8条件 **文書のみ** ✗ | **✗ 未実装** |

**∴ 6要件中 4つは既に在る。** 足りないのは **2・6** の3点。

## 3. GM v1 の残件 ―― **3件だけ**

| # | 残件 | 依存 | 実行者 | GM v1 を物理的に止めるか |
|---|---|---|---|---|
| **G1** | **ITEM の待ち相手を返す純関数**（`item_waiting_on`） | なし（材料は `status`/`depends_on`/`authority`/`status_note` に在る。`is_blocked` も既存） | **DESIGN が契約 → 2DER が実装** | **★止める**（要件2） |
| **G2** | **acceptance を機械照合できる形にする** | なし（`record.acceptance` 欄は在る・中身が散文） | **DESIGN が契約 → 2DER** | **★止める**（要件6の完了判定） |
| **G3** | **上申8条件の機械判定** | なし（8条件は文書のみ） | **DESIGN が契約 → 2DER** | **★止める**（要件6の上申判定） |

**3件とも相互依存なし。並列に出せるが、今夜の実測（同時走行で状態を見失う）から★1本ずつ。**

### 実行者の根拠（実測）

```
契約の通過率  ★DESIGN 6/6 ／ ★MGR 4/9
差            DESIGN の契約には「★出してはいけない結果」の欄が在る
∴ ★G1〜G3 の契約は DESIGN が書く。MGR は 投入・駆動・受領のみ。
```

## 4. 本線に残さないもの（「見つけたから直す」の禁止）

| 発見 | 扱い | 理由 |
|---|---|---|
| `item_turn`（MGR が書いた契約・`JUDGE_REQUIRED`） | **★破棄して G1 へ寄せる** | MGR の契約は 4/9。**作りかけだから続ける、は理由にならない** |
| 監査が自分の記録を増やす（`found_size` 12→16） | **後回し** | 判定は不変。GM v1 を止めない |
| EVO-0075（PLAN の供給元が引けない） | **後回し** | 偽陰性率の実測には要るが、**GM v1 の6要件には無い** |
| EVO-0076（既存機能を使わず自作するのを止める） | **後回し** | 予防策。GM v1 を止めない |
| 経路表に Domain 区間が0 ／ 機能表の収穫0 ／ RRI ／ 並列 | **★着手しない** | Taka 明示 |
| A〜H の欠陥群（task_id 未付与ほか） | **後回し** | inventory の精度に効くが、**1案件を通すことは止めない** |

## 5. Claude MGR にまだ残る進行責務

**G1〜G3 が入っても残るもの（今夜の代行11件から）:**

```
★残る  ・front door を叩いてループを回す者（投入 → 駆動 → 受領）
        ・契約を DESIGN に依頼する判断（誰に書かせるか）
        ・発見事項を「今／別item／後回し」に振る判断  ← ★機械化候補だが GM v1 の外
★消える G1 で「誰を待っているか」の推測（今夜2回誤った）
        G2 で「完成と呼んでよいか」の材料集め
        G3 で「Taka へ上げるべきか」の判断（今夜1回誤って上げた）
```

**∴ GM v1 完了後も、MGR は「ループを回す者」として残る。** そこを消すのは v1 の範囲外。

## 6. 次の1本

**G1（`item_waiting_on`）を DESIGN に契約させる。**
返す語は Taka 指定の6つ ―― `TAKA` / `DESIGN` / `DEPENDENCY` / `EXTERNAL` / **`NONE`** / **`UNKNOWN`**。
**`NONE`（調べた結果 待ち相手なし）と `UNKNOWN`（判定不能）を必ず別語にする。**
**「手番」と名乗らない**（次操作を決めない）。
