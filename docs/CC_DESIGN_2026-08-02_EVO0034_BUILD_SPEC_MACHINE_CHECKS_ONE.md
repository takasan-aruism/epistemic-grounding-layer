# 【BUILD SPEC】`EVO-0034` — **★機械が新しく見るのは #6 だけ。★#3 と #5 は もう見ている**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-02 12:2x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.3）** ／ **★9項目 確認済（★§5）** ／ **★3値 確認済（★§1）**
- **★裁定の在り処**: `ITEM-2DER-EVO-0034` の `status_note`（逐語:「★機械が実際に検査できる項目だけを検査し、残りは『機械では見ない』と★明示する」「★チェック欄を並べて人に押させる形にしない=それは自己申告の再生産」）
- **★走行 0・★task 増 0・★commit 0**

---

## 1. ★3値（★9項目を1つずつ・★MGR の見立てを実測で確定）

| # | 問い | ★3値 | ★逐語・実測 |
|---|---|---|---|
| 1 | 置いたなら どこから読めるか | **★一部 もう見ている** | `submit.py:232` 逐語 `"set_status returned None (unknown id or status not in STATUSES)"` ＝ ★書いた item が解決しないと ★理由つきで返る |
| 2 | 読めるなら どこから書けるか | **★見えない** | ★「書く口が在るか」は ★投入文から導けない |
| 3 | 失敗の理由は作られているか | **★★もう見ている** | ★C-2(c) で ★雛形が `reason`/`runner_exit`/`skeleton_missing_segment` を ★出す（★本日 実測・7 task で発火） |
| 4 | その理由は「作っていない」のでは | **★見えない** | ★`reason` の有無では判定できない（★実物 `verify_skeleton_preserved` は ★欄ごと無かった）。★★MGR は #3 と束ねたが ★別物である |
| 5 | 走ったのか | **★★もう見ている** | ★(a) 状況表が ★プロセス起動時刻 と ★ソース mtime を比べている（★本日 実測）／★(b) `worker_run_ref` は ★`claude_packet` に在る |
| **6** | **名前は 系が実際に作る名前か** | **★★★見える。★★まだ誰も見ていない** | ★`generate_via_runner.py:115,122` 逐語 `"target_file": "impl.py"` `"allowed_files": ["impl.py","test_impl.py"]` ＝ ★固定／★`contract_seal.py` の検査は ★3つだけ（★マーカー / ★契約2組 / ★`^def test_` の有無）＝ **★import 名を見ていない** |
| 7 | 依頼文と試験が矛盾していないか | **★見えない** | ★自然言語 と コード の照合。★特定の型に限れば書けるが ★一般化できない |
| 8 | その計器は自分を数えていないか | **★見えない** | ★本日の実物（監視が自分の書き込みで鳴る）は ★機械が自分で気づけない型 |
| 9 | 増えるなら 何を廃止するか | **★見えない**／★書かせてはならない | ★宣言を促す形は ★★裁定逐語「★自己申告の再生産」に当たる |

```
★★★∴ ★★新しく作るのは ★#6 だけ。★#3 と #5 は ★★もう見ている ∴ ★作らない（★規律9）。
★★★★MGR の見立てとの差 ★2件:
   ★① 「#3/#4」を束ねていたが ★★#4 は見えない（★#3 だけ）
   ★② 「#5 #6 #3 を作る」ではなく ★★#5 と #3 は ★既に在る ＝ ★★作る物は 1つに減る
```

## 2. やること（★1箇所・★既存の検査の並びに1つ足す）

```python
# twoder/contract_seal.py — ★既に在る3検査の ★直後（★新しい関数・新しい口を作らない）
import sys as _sys
_mods = set()
for ln in immutable_tests.splitlines():
    s = ln.strip()
    if s.startswith("import "):
        _mods.add(s[7:].split()[0].split(".")[0].split(",")[0])
    elif s.startswith("from ") and " import " in s:
        _mods.add(s[5:].split()[0].split(".")[0])
_bad = sorted(m for m in _mods if m != "impl" and m not in _sys.stdlib_module_names)
if _bad:
    raise ValueError("immutable_tests imports %s; 2DER が作るのは impl.py です。試験は `import impl` と書いてください"
                     % ", ".join(_bad))
```
```
★許すもの: ★`impl` ／ ★標準ライブラリ（★`sys.stdlib_module_names`・★290件・★python3.13 で実測）
★弾くもの: ★それ以外すべて（★fail-closed。★既存3検査と ★同じ `ValueError` の流儀）
★★本日の事故が ★これで ★投入時に止まる: ★`import human_view` → ★`ModuleNotFoundError`・`runner_exit: 2`（★実測済）
★★★戻し方: ★この塊を消す。★可逆
```

## 3. ★「機械では見ない」を明示する（★裁定の要）

```
★弾いた時の文言に ★1行 足す（★チェック欄を作らない・★人に押させない）:
   「★機械が見たのは ★#6(名前) だけです。★#2 #4 #7 #8 #9 は ★機械では見ていません」
★★★一覧・チェックボックス・確認欄を ★★作らない（★裁定逐語「★自己申告の再生産」）
★★★★★#1 #3 #5 は ★既に機械が見ている ∴ ★この文言に ★入れない（★二重に言わない）
```

## 4. 受入

```
★(1) ★`import human_view` を含む契約の投入が ★★投入時に弾かれる（★本日の事故が ★再現しない）
     ★front door の応答に ★理由が ★逐語で出ることを ★確かめる
★(2) ★★偽陽性が無い: ★`import impl` ＋ ★標準ライブラリ（★`json` 等）だけの契約は ★通る
     ★★実物で確かめる: ★`TASK-2DER-A64D0C6D` の契約（★10本・`import impl`）が ★通ること
★(3) ★既存3検査が壊れない ＝ ★名指し2本だけ走らせる:
     ★`twoder/test_contract_read_spec.py` ／ ★`twoder/test_generate_via_runner_spec.py`（★61本は走らせない）
★(4) ★§3 の1行が ★出る（★一覧・チェック欄が ★出ない ことも確かめる）
★(5) ★戻せる ／ ★(6) ★Claude が書いた行数を書く（★2DER の実績に数えない）
★★★★★予告を投入前に書く: ★変更行数 ／ ★(2) で通す契約の task_id
```

## 5. ★9項目（私の分）
```
1 置いたなら読めるか＝★front door の応答（★受入(1)）／2 書く口＝★本件は検査 ∴ 該当なし
3 理由を捨てない＝★★弾く理由を ★逐語で返す（★「不正」とだけ言わない）
4 作っていないのでは＝★★`contract_seal` は import 名を ★最初から見ていない ＝ ★本件そのもの
5 走ったか＝★実装が webui 再起動を確かめる／6 名前＝★★本件そのもの
7 依頼と試験の矛盾＝★成果物を作らない ∴ 該当なし／8 計器＝★★私の検査は ★自分の投入を数えない
★9 増える代わりに廃止＝★★C-2(b) の雛形注意書き「※ 試験は `import impl` と書いてください」が ★★人の注意頼みだったのを畳む。
   ★★★★但し ★`<<<FILL` の注意書きは ★残す（★そちらは ★まだ機械が見ていない）——★★畳んだと書きすぎない
```

## 6. 禁止
```
★9項目のチェック欄・確認一覧を作る（★裁定逐語で禁止）／ ★#2 #4 #7 #8 #9 を「見た」ことにする
★新しい関数・エンドポイント・台帳・状態語を作る ／ ★`contract_seal` の既存3検査を緩める
★標準ライブラリの import を弾く（★偽陽性）／ ★61本を走らせる ／ ★commit する
★`twoder` 配下で python を動かす（★`operator.py` の罠）
```
