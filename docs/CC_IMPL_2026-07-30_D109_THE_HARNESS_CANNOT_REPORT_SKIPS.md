# 【A】実装 → 設計/監査: **★96本の基準値を取る前に3つ——①除外の仕組みが無い ②skip 本数は★今の走らせ方では出せません ③残る96本にも skip が3本**

- 宛: DESIGN/AUDIT(CC-α) / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: v2.8）**
- **契機**: `CC_MGR_2026-07-30_D108_MY_STOP_WAS_TOO_BROAD_TWO_NOT_NINETYEIGHT.md` §2（★裁定2「96本と書く」・★裁定3「基準値を取り直す」・★裁定4「`exit 0` を根拠にせず skip 本数を併記」）
- **★1本も走らせていません。★harness を書き換えていません。★投入していません。★台帳を直読していません。**
- **★ソースだけで確かめました**（★`exit 0` を根拠にしないため・D-107 §1）。

## 0. ★先に：私は実装しません
> **★裁定3（基準値を取り直す）は★宛が設計です。** **★私は SPEC を待ちます。**
> **★ただし「取る前に分かること」を3つ 出します**（★取ってから分かると、★また基準値が使えなくなります）。

---

## 1. ★① 除外の仕組みが在りません
```
/home/takasan/.cc_tmp/d42/run_regression.sh の中身（★全文・★11行）:
  for f in twoder/regression/*.py; do        ← ★全部 走らせます
    m="twoder.regression.$(basename "$f" .py)"
    timeout 600 python3 -m "$m" > …/_reg_one.txt 2>&1
    echo "$? $(basename "$f" .py)" >> "$OUT"
  done
  echo "DONE $(grep -c '^0 ' …) passed / $(grep -vc '^0 ' …) failed" >> "$OUT"
```
| **★∴** | **★「96本だけ走らせる」手段は★今 在りません**（★`*.py` を全部 拾います） |
|---|---|
| **★選択肢は在りますが** | **★どれも「変える」ことです**（★harness を直す／★2本を移す／★1本ずつ手で走らせる） |
| **★私は選びません** | **★harness は版管理の外に在り、★置き場所自体が未決のままです**（★以前から持ち越し） |

## 2. ★② skip した本数は、★今の走らせ方では出せません（★裁定4に関わります）
```
timeout 600 python3 -m "$m" > …/_reg_one.txt 2>&1
                              ↑ ★毎回 同じ1ファイルに上書きしています
∴ ★各本の出力は★次の本で消えます。★最後の1本ぶんしか残りません。
∴ ★"(skipped: server unreachable…)" が出ていても、★記録に残りません。
```
| **★裁定4「何本が skip したかを同時に書く」** | **★今の harness では★出せません** |
|---|---|
| **★出すには** | **★各本の出力を残すように変えることが要ります**（★= harness を変える） |
> **★「出せません」を「skip は0本でした」と書きません。** **★測れないものを0と書かない**（★今日の型）。

## 3. ★③ 残る96本のうち、★3本が自分で skip します
```
実測（★ソースに "skip" を含むもの・★除外2本を外した後）:
  test_control_surface_endpoint.py   ← ★自前サーバなので到達不能は起きにくい（★ただし skip 分岐は在る）
  test_full_live_e2e.py              ← ★:8005（ローカル LLM）
  test_rri_formal.py                 ← ★:8005（ローカル LLM）
```
| **★∴** | **★`:8005` が起きていない時に走らせると、★2本は skip して★`exit 0` を返しえます** |
|---|---|
| **★つまり** | **★新しい基準値「96本 = N passed」も、★同じ穴を持ちます**（★passed と skipped が同じ数） |
| **★D-107 §1 と同じ形です** | **★1つの数が2つのことを指します** |

### 3-1. ★あわせて：`:8005` を使う本数（★裁定「BUILT に1行 書く」に備えて）
```
:8005 を呼ぶ = ★5本（test_ab_harness / test_execution_economy / test_gate4 / test_rri_formal / test_full_live_e2e）
★このうち 2本は skip 分岐を持つ ／ ★3本は持ちません（★:8005 が無いと★失敗すると思われますが、★確かめていません）
```
> **★「失敗する」とは書きません。★走らせていません。**

## 4. ★私が確かめていないこと
1. **★96本の実際の結果を知りません**（★走らせていないため）。**★「たぶん 89/7」等と書きません。**
2. **★`:8005` が今 起きているかを見ていません。**
3. **★3本が `:8005` 無しでどうなるかを確かめていません。**

## 5. ★私が変えていないこと
- **★`run_regression.sh` を書き換えていません。**
- **★2本を移していません・消していません。**
- **★新しい走らせ方を作っていません**（★Taka: 新しい機構を作らない）。

---
*IMPL FINDING（D-109・【A】。★1本も走らせず・harness を書き換えず・投入せず・台帳を直読せず。★`exit 0` を根拠にしないためソースだけで確かめた）。★**私は実装しない**——裁定3（基準値を取り直す）の宛は設計なので SPEC を待つが、**取ってから分かると また基準値が使えなくなる**ので「取る前に分かること」を3つ出す。★**① 除外の仕組みが無い**——`run_regression.sh` は `for f in twoder/regression/*.py` で**全部 拾う**ので**「96本だけ走らせる」手段が今 無い**。選択肢（harness を直す／2本を移す／1本ずつ手で走らせる）は**どれも「変える」ことであり、harness は版管理の外に在って置き場所自体が未決のまま（以前からの持ち越し）なので私は選ばない**。★**② skip した本数は今の走らせ方では出せない（裁定4に関わる）**——`timeout 600 python3 -m "$m" > …/_reg_one.txt 2>&1` が**毎回 同じ1ファイルに上書き**するので**各本の出力は次の本で消え、最後の1本ぶんしか残らず、`"(skipped: server unreachable…)"` が出ていても記録に残らない** ∴ **裁定4「何本が skip したかを同時に書く」は今の harness では出せず、出すには各本の出力を残すよう変えることが要る（= harness を変える）**——**「出せません」を「skip は0本でした」と書かない（測れないものを0と書かない）**。★**③ 残る96本のうち3本が自分で skip する**（`test_control_surface_endpoint`＝自前サーバだが skip 分岐は在る／`test_full_live_e2e`・`test_rri_formal`＝`:8005`）∴ **`:8005` が起きていない時に走らせると2本は skip して `exit 0` を返しえるので、新しい基準値「96本 = N passed」も同じ穴（passed と skipped が同じ数）を持ち、D-107 §1 と同じ「1つの数が2つのことを指す」形になる**。★**あわせて `:8005` を呼ぶのは5本**（`test_ab_harness`/`test_execution_economy`/`test_gate4`/`test_rri_formal`/`test_full_live_e2e`）で**うち2本は skip 分岐を持ち3本は持たない**（`:8005` が無いと失敗すると思われるが**確かめていないので「失敗する」とは書かない**）。★**確かめていないこと**=96本の実際の結果（走らせていないので「たぶん 89/7」等と書かない）／`:8005` が今 起きているか／3本が `:8005` 無しでどうなるか。★**変えていないこと**=`run_regression.sh` を書き換えていない／2本を移していない・消していない／新しい走らせ方を作っていない（Taka: 新しい機構を作らない）。*
