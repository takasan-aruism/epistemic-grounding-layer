開発者規律 確認済(v1.0)

# 【BUILD SPEC】★受入②の 未達を 潰す ―― ★候補・漏斗・理由を **記録から** 読む（★LLM 0回）

宛: IMPL ／ 発: DESIGN ／ 2026-08-14 00:0x ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **MGR 裁定 23:47**（①条件つき ／ ②却下 ／ ③本命・但し推測で直さない ／ **受入② 未達**）
＋ **正本** §9（票の扱い）／ §25 段3 ／ **Taka 逐語**「★その問いは 意味を なすか（が先）」

**★MGR が front door から 引いた 食い違い（★逐語）**
> 「`not_in_list` = **`{}`**（空）／ `funnel` = **`null`** ／ あなたは台帳に『候補=通信／送信／研究（held=溜めた・消していない）』と書いた ＝ **私はそれを front door から1件も見られない**」

---

## 0. ★★私が 引いた 事実（★★推測と 分けて 書く）

```
★★【★★コードで 確定した（★★これは 断定してよい）】
   ★`function_table.py:245 function_table_view(last_run=None)`
   ★`not_in_list` = ★`(last_run or {}).get("candidates") or {}`
   ★`funnel`      = ★`(last_run or {}).get("funnel")`
   ★★front door の 呼び手 = ★`webui.py:881 rep["function_table"] = _FTV()` = ★★★引数なし
   ★★∴ ★★★この2欄は ★走行の 有無に かかわらず ★★必ず `{}` と `null` に なる
      ―― ★★これは『消えた』では なく ★★★『★走行中の 記憶からしか 読んでいない』

★★【★★引けなかった（★★★これが 結果＝規律 §3）】
   ★★`function_vote` の 記録を ★★front door の どの口でも 引けなかった
   ★試した口 = ★`/api/etrace`(★引数は run_id / event_id / task_id ★だけ＝★走行 id を 知らないと 引けない)
             ／ ★`/api/control?include=observed_edges`(★`top_rows` 30件に ★ANATOMIST 無し＝★上位30だけ)
             ／ ★`include=anatomist`
   ★★∴ ★★★『記録に 在るか 無いか』を ★私は まだ 知らない

★★【★★★2つの 可能性（★★★どちらかを 私が 選ばない）】
   ★(a) ★記録は 在る ／ ★★読み出す口が 無い（★`function_table.py:202` の emit は ★ループの中で 無条件）
   ★(b) ★`fail_open=True` で ★emit が 黙って 落ちた
   ★★★分ける物 = ★★★`function_vote` の 行数 ★1つ（★§3 受入① ＝ ★これを 最初に 出す）
```

---

## 1. ★★場合の 列挙（★★走らせる前に 出す）

```
★① `function_vote` の 行数が ★★0 → ★★★原因は emit 側＝★★(a) は 外れ＝★★★そう 書く（★私の 見立ての 誤りとして 記帳）
★② 行数が ★60 前後（★2走行 × 30部品）→ ★(a) ＝ ★★読み出す口を 作れば 直る
★★③ 行数が ★30（★1走行分だけ）→ ★★★1回目の 走行が 記録に 入っていない＝★★別の 欠陥＝★★数を 出して 止める
★★④ 記録に 在るが ★`new_name` が どの行にも 無い → ★★候補は 元から 0件＝★★★『溜めた』が 誤り＝★そう 書く
★★⑤ 同じ名前が ★★2つの 相異なる 部品で 出ている → ★★★`MIN_OCCURRENCES=2` を ★★LLM 0回で 満たす
★★⑥ 同じ名前だが ★★同じ部品で 2回（★走行違い）→ ★★★揃っていない（★★★2部品であって 2回ではない）
     ―― ★★★ここを 取り違えると『★揃った』を 誤って 報告する（★★数える鍵を 答えに 1行 書く）
★★⑦ 理由が 空の票が 在る → ★★`reason=None` として 数える（★★0件に しない）
★★⑧ 再起動を またいで `index_lookups` が 0 に 戻る → ★★★記憶から 読んでいる印（★★同じ病）
```

---

## 2. ★★直す物（★★1本の 中だけ・★★LLM 0回・★★新台帳 0）

```
★★`twoder/function_table.py` に ★★読み出しを 1つ 足す（★★新しい file を 作らない）

★★① `_votes_from_records()`
   ―― ★`ETRACE` から ★`("ANATOMIST", "function_vote")` の 行を 読む
   ―― ★★読み方は ★★`route_edge_vote.list_votes()` と ★同じ形を 使う（★★写経ではなく ★同じ作法）
   ―― ★★返す物 = ★1行ずつ = ★`run_id` ／ `component` ／ `final` ／ `disagreement` ／
        ★`votes[].seed` ／ `votes[].answer` ／ ★★`votes[].reason`（★★全文・★切り詰めない）／
        ★`new_name` ／ `not_in_list_names` ／ `duplicate_of_existing` ／ `model` ／ `input_hash`

★★② `candidates_from_records()`
   ―― ★名前ごとに ★★★『出た部品の 名前』を 集める（★★★数ではなく 名前）
   ―― ★★`occurrences` = ★★★相異なる 部品の 数（★★走行の 数では ない＝★列挙⑥）
   ―― ★`runs` = ★その名前が 出た 走行 id（★★走行を またいだ ことが 読める）
   ―― ★★`held` = ★`occurrences < MIN_OCCURRENCES`（★★溜まっている ことが 外から 見える）
   ―― ★★★綴りが 近い物を 束ねない（★★`_near_names` で 併記するだけ＝★既に 在る）

★★③ `funnel_from_records()`
   ―― ★★§0 の 6段を ★★記録から 数え直す（★★走行ごと ＋ ★通算の 2本）
   ―― ★★★走行中の 記憶を 使わない（★★これが 未達の 原因）

★★④ `function_table_view()` の ★`last_run` 依存を ★★外す
   ―― ★`not_in_list` ／ `funnel` ／ ★★`undecided`（★新）を ★★①〜③から 作る
   ―― ★★`last_run` 引数は ★★消す（★★呼び手 0件の 引数を 残さない）

★★⑤ ★`index_lookups` を ★★記録から 数える
   ―― ★★引かれた 時に ★1行 emit（★`COMPONENT="FUNCTION_TABLE", FUNCTION="index_lookup"`）
   ―― ★★`kind`(verify / human / machine) を ★行に 持つ ／ ★★読む側が 数える
   ―― ★★理由 = ★★★いまは module の 変数 ＝ ★★★入口が 再起動すると 0 に 戻る
      （★★入口は 本日 11回 落ちている ∴ ★★『0回』が ★使われていない のか ★消えた のか 分けられない）
```

## 3. ★★出す物（★`?include=function_table` に 足す）

```
★★① ★★`records` = ★`function_vote` の ★★行数 ／ ★走行ごとの 行数 ／ ★走行 id の 一覧
     ―― ★★★これを 一番 先に 出す（★★★(a) と (b) を 分ける 1つの 数）
★★② ★`not_in_list` = ★名前 ／ ★★出た部品の 名前（★一覧）／ ★`occurrences`（★相異なる部品）／
     ★`runs` ／ ★`held`
★★③ ★`funnel` = ★走行ごと ＋ ★通算
★★④ ★★`undecided` = ★★`final=NOT_DECIDED` の 件（★部品名 ／ ★票 ／ ★★理由 全文）
     ―― ★★★MGR 裁定③の 順序の ★2番目 ＝ ★★『★決められない 16件の 理由を 読める形に する』
★★⑤ ★`index_lookups` = ★記録から の 3値 ＋ ★★鍵の 1行（★既に 在る文を そのまま）
★★⑥ ★`counted_by` = ★★★『★相異なる 部品の 数で 数える（★走行の 数では ない）』を ★1行
```

## 4. ★★受入（★★走らせる前に 宣言する・★数で）

```
★★① ★★`records.count` が ★出る ―― ★★★0 なら ★★★『原因は emit 側／★私の見立ては 外れ』と ★書いて 止める
★★② ★★`not_in_list` が ★★front door から 引ける（★★★空なら『0件』と ★★明記＝★★`{}` を 残さない）
★★③ ★★`funnel` が ★★`null` で ない
★★④ ★★`undecided` の 理由が ★★全文で 読める（★★切り詰め 0件＝★本日 廃止した 物が 戻っていない）
★★⑤ ★★★同じ名前が ★2つの 相異なる 部品で 出ているか / いないか が ★★★LLM 0回で 分かる
     ―― ★★在れば = ★★★`MIN_OCCURRENCES=2` を ★満たす候補が 在る（★★★登録は まだ しない＝★次の手番）
     ―― ★★無ければ = ★★★『揺れのせい』では なく『★元から 揃っていない』が ★確定する
★★⑥ ★★`index_lookups` が ★★入口を 再起動しても ★★★0 に 戻らない（★★1回 引いて → ★再起動 → ★引き直す）
★★⑦ ★★LLM 呼び出し = ★★★0回（★★機械で 数える＝★★読み出しだけ）
★★⑧ ★既定の `/api/control` が ★0.5秒以内 ／ ★`?include=function_table` の 所要も 出す
★★⑨ ★★新台帳 0 ／ ★口 0増 ／ ★`route_table.py` bytes 不変 ／ ★`authority.POLICY` bytes 不変
★★⑩ ★★`last_run` 引数が ★★消えている（★呼び手 0件の 引数を 残さない）
★★⑪ ★走らせた試験の 名前を 書く（★★禁止3本は 走らせない）
```

## 5. ★★やらないこと（★★MGR 裁定を そのまま）

```
★★★同じ走行を 数回 まわして 揺れを 均さない（★★裁定② 却下・★正本 §9
   『★同一モデル・同一入力の 票は 独立した 証拠では ない』＝★★多数決で 真実を 作る 形）
★★★8語の 一覧を 広げない（★★材料が 揃う前に メニューを 増やさない＝★V1 の 失敗＝★母数で 殴る）
★★残り3部品（★母数33）は ★★★この段では 掛けない
   ―― ★理由 = ★★★受入⑤が『在り』なら ★LLM 0回で 済む（★掛ける前に 分かる）
   ―― ★★★戻す条件 = ★受入⑤が『★無し』だった時 ＝ ★★その時に 33 で 打ち止めで 掛ける（★上限は 伸ばさない）
★★上級監査（★まず5件・★段1『★その問いは 意味を なすか』）は ★★④が 出てから（★裁定③の 順序）
★★『候補が 消えていた』と 書かない（★★★まだ 分かっていない＝★受入① が 答える）
```

## 6. ★★言い方

```
★★『直った』と 書かない ―― ★★正しくは = ★★『★記録に N 行 在り ／ ★候補 c 件 ／ ★2部品で 揃った物 k 件』
★★★私（DESIGN）の 見立て（★(a) 読み出しが 捨てている）は ★★★受入①の 数で ★覆りうる
   ―― ★★覆ったら ★★『★私が 外した』と ★★先に 書く（★★実装の 責任に しない）
```
