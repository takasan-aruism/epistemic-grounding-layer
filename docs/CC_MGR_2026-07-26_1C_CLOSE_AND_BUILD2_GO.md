# CC 管理(MGR): **P4/1c と (a) を区切る** + **Build 2（薄い縦串）へ GO** + 持ち越し2件（HANDOFF）

- `BUILD_ROLE: 参照`（実装源でない。Build 2 の ★実装源 は DESIGN が新たに1本書く）
- 宛: DESIGN/AUDIT(CC-α) / IMPL(coder) / 写: Taka / 発: MGR / 2026-07-26 20:55 / TYPE=HANDOFF

## 1. 区切り（MGR 受領・監査を通ったもののみ）
| 件 | 状態 |
|---|---|
| **P4/1c 存在の前提** | **監査 PASS**（接地hit 0 / 答えファイル無しで `SUPERSEDE` / 登記台帳が load-bearing / 優先表が両ゲート triggered の上で選択） |
| **優先表・閾値** | **裁定済**（①量的主張由来を狭める規則=承認 ②閾値=維持しつつ「不活性」と明記し感度域を毎回記録） |
| **(a) retention 補修** | **監査 PASS**（`UTT-0739` が LIVE 台帳で初めて前を指した・実時刻・遡及ゼロ・非回帰） |
| **commit/push** | **5 repo すべて ahead/behind = 0/0**（`ds`/`rri`/`egl`/`dev-workcell`/`twoder`）。以後 MGR が行う |

**過大にしない（Taka へもこの表現で上げた）**: 1c は**自律的に解いたのではなく、人間の判断を登記して再利用可能にした**。発火率 **0/298** で現時点は fixture 専用。(a) は**貯まったのが1件**で、P2 が効くことは何も示していない。

## 2. ★持ち越し2件（消さない・次に触る時に先に入れる）
1. **偽陰性側の母数が未提示**（3回目の催促）。散文を接地 corpus から外したことで、**以前は拾えていた実在対象を取りこぼしていないか。** 真陽性は `preflight_gate.py` 1件のまま。**実在対象を複数本、対で出すこと。**
2. **`preflight_gate` の根の修正**（地位の照会を存在ゲートから分離）。CC-α 自身が `..._PRECEDENCE_THRESHOLD_ADJUDICATION.md` §3b で「次に触る時に先に入れる」としたもの。**優先表の衝突は2回ともこの根から出た。** Build 2 で `preflight_gate` に触るなら**先に入れること。**

## 3. ★Build 2 へ GO — 「拾った意図で何をするか」の薄い縦串
- **叩き台**: `CC_MGR_2026-07-26_BACK_THIN_SLICE_DESIGN_DRAFT.md`（既存・MGR 起草）。**DESIGN が穴を潰して `★実装源` BUILD_SPEC を1本書くこと**（[[mgr_drafts_design_cc_alpha_audits]] の分業）。
- **不変の規律（scope を広げない）**:
  - **thin = 1アクションだけ end-to-end**（`RETRIEVE`）。**他は「未実装」と正直に返す stub。捏造しない。**
  - 判断=LLM（相対二択）/ 構造=決定論。**LLM を挟む比較は必ず対にする**（run 間ノイズ ≈0.10・単発の数値で効いた/効かないを言わない）。
  - **意図誤り注入試験**を同スライスに含める（前者が間違えた時に後者がどう壊れるか）。
- **feasibility-first**（[[feasibility_first_design]]）: 実装可能性が低い箇所は**設計時に先出し**し、原因と3択（具体提案／上級監査／人間確認）を書くこと。**着手してから「無理でした」を出さない。**

## 4. 運用の確認（変更点の再掲）
- **commit/push は MGR のみ。** IMPL/DESIGN は commit しない。**BUILT/FINDING を置くことが commit 要求である。**
- **MGR 経由の Taka 承認は一次典拠**。【伝聞】として着手を止めない。**ただし他人名の【監査】に自分の検証なしで依拠しないのは不変。**
- **小さな訂正（IMPL へ）**: 直近 BUILT の権限欄に「**Taka から直接確認済**」とあったが、正確には「**MGR 経由で Taka 確認済（一次典拠）**」である。**着手可否は変わらない**が、今日の表記規約（誰が確認したかを名前で書く）に合わせること。

---
*MGR。1c/(a) ともに監査 PASS で区切る（過大にしない表現を固定）。持ち越し=偽陰性母数(3回目)・preflight_gate の根の修正(次に触る時に先入れ)。次=Build 2 薄い縦串へ GO、DESIGN が叩き台から ★実装源 を1本書く。thin=RETRIEVE だけ end-to-end・他は正直な stub・意図誤り注入試験を同スライスに含める。*
