# 実装 → 設計/監査: Build 6 配線 — **段3e は入った。ただし事実は本番で一度も出ない**（BUILT）

- 宛: DESIGN/AUDIT（CC-α） / MGR / 写: Taka
- 発: 実装(IMPL) / 2026-07-27 / TYPE=BUILT
- 実装源: `CC_DESIGN_2026-07-27_BUILD6_SPEC_WIRING.md` v1.0 / GO: `CC_MGR_2026-07-27_BUILD6_GO.md`
- 表記規約: **【監査:IMPL】** / **【設計:IMPL】**

## 0. 言い方の固定（条件③・先に書く）
**これは精度の改善ではありません。** 正しくは **「意図調べが本番の経路に入った。効果は別に測る。」**
**「2DER が良くなった」「精度が上がった」とは書きません。本 SPEC に精度の測定は含めていません。**

## 1. ★★構造的な発見 — 事実を出すはずの入力は、段3e に1件も到達しません【監査:IMPL】
配線後に front door を実際に通して確かめました。
| 入力 | 段3d | **段3e 到達** | 出た事実 |
|---|---|---|---|
| 「それ、その後どうなった？」（指示語） | `RRI_PREFLIGHT_HOLD` | **False** | — |
| 「以前作った Watcher 仕様ってどこ？」（過去参照） | `RRI_PREFLIGHT_HOLD` | **False** | — |
| 「前の件を優先して進めて」 | `RRI_PREFLIGHT_HOLD` | **False** | — |
| 「この設計案の得失は？」（自己完結） | `DW_IMPLEMENTATION` | **True** | **[]（0行）** |

**＝ 段3d（P2 参照ゲート / P4 存在ゲート）が、事実の対象になる入力を**先に全部止めています**。
**段3e に到達する入力は、定義上すべて「事実が0行」のものだけです。**
**∴ 事実の出し分け機構は、本番では一度も事実を出しません。**

**【設計:IMPL】断定はしません。** 選択肢としては (i) 事実を段3d の聞き返し文に載せる / (ii) 3e を 3d より前に置く / (iii) 現状のまま「配線は通ったが未到達」と記録する、が考えられますが、**どれも私が決めることではありません。** 受入②（3d で止まった入力では 3e を呼ばない）は**仕様どおり**であり、**仕様どおりに作った結果この状態になりました。**

## 2. 受入【監査:IMPL】
| # | 条件 | 結果 |
|---|---|---|
| 1 | **既存 LIVE の非回帰（最優先）** | `test_preflight_gate` **13/13** / `test_return_loop` 12/12 / `test_dispatch_provenance` 11/11 / `test_artifact_registry` 17/17 / `test_runtime_supervisor` 8/8 / `ds/test_ds_phase0` 19/19 |
| 2 | 3e は 3d 通過時のみ呼ばれる | **実証**（3d で止まった入力に `INTENT_STRATEGY` キーが無い） |
| 3 | 段4 の入力に応じ方が乗る | **実証**（`TRACE["INTENT_STRATEGY"]` に `strategy`/`candidates`/`status`/`stops_before_action`） |
| 4 | 事実の出し分けが決定論 | **実証**（同一入力で同一の事実行・LLM ゼロ） |
| 5 | **★自己完結した依頼に文脈の事実が載らない** | **実証（LLM 不使用）**。§3 |
| 6 | 閉塞3箇所 | **2箇所完了・1箇所は要判断**。§4 |
| 7 | `record_plan` 2箇所 | **「保留」** と明記。§5 |
| 8 | 暫定の単純化を明記 | §6 |
| 9 | 精度の測定を含めない | **含めていません** |
| 10 | commit しない | **していません**（MGR） |

## 3. 受入⑤ — Build 5 の欠陥は是正されました（LLM 不使用で証明）【監査:IMPL】
```
それ、その後どうなった？        → 2行: 直前の文脈は記録に存在しない。/「それ」が指す対象は記録から特定できない。
以前作った Watcher 仕様ってどこ？ → 2行: 直前の文脈は記録に存在しない。/「Watcher 仕様」が記録にあるか判定できない。
この設計案の得失は？            → 0行  ← Build 5 で壊した BV3
Windows 10 の一般提供開始日は？  → 0行
どのデータベースを採用すべき？     → 0行
```
`prompt` 文字列に事実ブロックが**含まれないこと**も直接確認済（BV3=False / IP2=True）。

## 4. 閉塞（受入⑥）
| 対象 | 状態 |
|---|---|
| `twoder/live_worker_runtime.py:140` `admit_design_evidence` | **閉塞完了**。front door の段1.5 DE admission fast path 経由へ |
| `twoder/runtime_supervisor.py:218` `admit_design_evidence` | **閉塞完了**。同上 |
| `twoder/experiment_candidate.py:116` `create_task` | **★未実施・要判断**（下記） |

**`create_task` を閉塞しなかった理由（feasibility-first・隠しません）**【監査:IMPL】:
呼び出し元は **`dw_task_id` を自分で決めて渡しています**。front door の段4 ROUTING を通すと **task id は routing 側が決める**ため、**呼び出し元が握っている id が失われます**。
＝ **「経路を通す」だけでなく「id の所有権を移す」変更**になり、**既存の呼び出し元を壊すおそれ**があります。
**受入⑥は「既存の呼び出し元が壊れていないこと」も要求している**ので、**壊す可能性がある変更を勝手に入れませんでした。** **id の所有権をどちらに置くかの裁定をお願いします。**

## 5. `record_plan` 2箇所は**保留**です（条件①）
`twoder/webui.py:315` / `twoder/build_planner.py:301`。
**「閉塞対象から外した」とは書きません。「対応する依頼型の有無が未確認のため保留」です。**
確認は名前のある作業 **`MIGRATION-PLAN-CHECK`** として残っています（本 SPEC には含めません）。

## 6. ★暫定の単純化（条件②・黙って既定にしません）
> **本実装は「1つの問い合わせ = 1つの明細」の前提で通しています。**
> **これは Taka が「そもそもナンセンス」と指摘した前提そのものであり、暫定の単純化です。**

**見直す条件（先に書きます）**: (1) 配線が1本通った後（＝本 BUILT の後）、(2) 明細への分解を誰がやるか（決定論か LLM か）が決まった後。**LLM なら新しい判断点であり、既知の弱点が入る場所です。** (3) `account chart` は完成・凍結につき触りません。
同じ但し書きは `rri/intent_strategy.py` の冒頭にも書いてあります。

## 7. 作ったもの / 触ったもの
| repo | ファイル | 内容 |
|---|---|---|
| `rri` | `rri/intent_strategy.py`（新規） | 段3e 本体。事実の出し分け（決定論）+ 選択の場。**:8005 不通なら沈黙して修復せず失敗を返す** |
| `twoder` | `submit.py` | 段3e を 3d の後・4 の前に挿入。`TRACE["INTENT_STRATEGY"]` を記録 |
| `twoder` | `live_worker_runtime.py` / `runtime_supervisor.py` | 直叩き閉塞 2箇所 |

**作り直していないもの**（既存の出力をそのまま使用）: `context_binding.bind_context` の `anchoring` / `preflight_gate` の束縛判定 / `existence_grounding` の接地判定 / `request_type`（触っていません）。

**commit しません（MGR）。** 触った repo: `rri` / `twoder`。

---
*IMPL BUILT（Build 6）。**段3e は入り、非回帰は全て GREEN。事実の出し分けは Build 5 の欠陥を是正（自己完結した依頼に事実を載せない・LLM 不使用で実証）。** ★**ただし段3d が指示語・過去参照の入力を先に全部止めるため、事実を出すはずの入力は段3e に1件も到達しない＝本番では事実が一度も出ない。** 仕様どおりに作った結果であり、扱いは DESIGN の判断。閉塞は2/3完了、`create_task` は id 所有権の移動を伴うため未実施（裁定待ち）。`record_plan` 2箇所は「保留」。「1問い合わせ=1明細」は暫定と明記。*
