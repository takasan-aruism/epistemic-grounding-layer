# 【BUILT】D-153 — **★観測を見せたら計画が変わった（受入3 が立った）。★受入4 はまだ立たない**／**§0: 監視が鳴らなかった理由**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: MGR / Taka / 発: 実装(IMPL) / 2026-07-31 09:1x / TYPE=BUILT
- **運用方針 確認済（版: v2.8）** ／ **実装源**: `CC_DESIGN_2026-07-31_D153_BUILD_SPEC_SHOW_THE_OBSERVATION.md`（★03:24:19 版）
- **`:8005` が呼ばれた**（★私は0件。**★投入で4件・`run_next` で2件**。§5 に内訳）

---

# 0. ★最初に — **★03:24 の文書で監視が鳴らなかった理由（★MGR の問い）**

> ## **★原因は私の監視の作りである。★「ファイル名」しか見ていなかった。**

```
★私の監視: ls で CC_*.md の★名前の一覧を取り、★前回に無い名前だけを新着として鳴らす。
★実測(いま確認):
   D-153 の SPEC は ★03:17 頃に一度 出現し、★そのとき鳴っている（★私はそれを読んだ）。
     → その版には ★依頼文が無く、「依頼文は MGR が固定する」と書かれていた（★113行前後）。
   同じ名前で ★03:24:19 に ★中身の違うものが置かれた（★birth=mtime=03:24:19 / 127行 / 8752 bytes）。
     → ★名前は既に一覧に在る ∴ ★「新着」に当たらない ∴ ★鳴らない。
★∴ ★3つの可能性のうち「条件が合わなかった」に当たる。★正確には「私の条件が名前だけだった」。
```
**★併せて確認した2点（★隠さない）**
| | |
|---|---|
| **監視の生死** | **★いま探すと、その監視のループは★プロセスとして存在しない。★いつ落ちたかは分からない**（記録を取っていない）。**★ただし本件の説明には要らない**——**★03:17 に鳴っている＝その時点では生きていた**し、名前は既に見た後だからである |
| **取りこぼしの有無** | **★03:15 以降に出た CC_*.md 7件のうち、★宛が IMPL なのは D-153 の1件だけ**（他は 宛: 設計 / 宛: MGR）。**★他に取りこぼした文書は無い** |

**★直した**: 監視を **名前＋更新時刻＋サイズ** で見る形に張り直した（**★同名の置き換え・追記も鳴る**）。**★30分ごとに生存の1行を出す**ようにしたので、**★次からは「静かなのか死んだのか」が区別できる**。

**★私の落ち度である。** **★「新しい file を置けば届く」という我々の前提は、★私の側で成立していなかった。**

---

# 1. ★変更した1箇所
```
twoder/build_planner.py  ／ 3 hunk ／ 31挿入・1削除
内容: _plan_prompt に ★_observation_facts() の出力を差し込む（★引用した観測の中身を見せる）
```
**★どこから中身を取ったか（★選んだ理由を1行）**: **★値そのものは Observation 記録に入っていない**（実測: `OBS-00975` は hash と参照のみ）。**★生出力は保留中の Ledger にしか無い** ∴ **★触ってよい範囲で最も中身に近い「どの命令で・どこから・いつ取得され、結果はどうだったか」を、既存の `resolve` 2段（OBS→ARUN）で取って渡した。**
**★指示は足していない**（「重複して作るな」とは書いていない）。**★事実だけ渡した。**

| 禁止語 | 実測 |
|---|---|
| `gpu` / `nvidia` | **0件** |
| `port` | **部分一致で1件。★中身は Python の `import`**（単語境界 `\bport\b` では **0件**）。★両方の数字を出す |

**★後方互換**: 基準値**11件すべて不変**。`egl_source_refs` が無い／解決しない場合は**空文字＝従来と同じ prompt**（★`DE-` を渡して確認済）。

---

# 2. ★受入（★1条件に1つの印。★「概ね」と書かない）

| # | 受入（Taka 逐語） | 印 | 実測 |
|---|---|---|---|
| **1** | `cites_source_ids` が空でない | **○** | `['OBS-00979','OBS-00980','OBS-00981','OBS-00982']` |
| **2** | 対象 Observation の ID を引用する | **○** | **★この投入の `egl.source_refs` と完全一致** |
| **3** | **既に観測済みの能力を、新規作成対象として重複計画しない** | **★○（前回の × から変わった）** | **前回**: `steps[0]="…ss/netstatコマンド実行、socket接続テストの3段階アプローチ"`＝**取得能力を作り直す計画**<br>**今回**: `steps[0]="Design a Python script that reads container observation data from a JSON input file."`＝**★取得は済んでいる前提で、読んで不足を判定する計画**。**★取得コマンドを実装する step は無い** |
| **4** | 証拠から不足が判定された場合だけ改修 Task を作る | **★×** | `steps[2]="Define a set of expected metrics (CPU, memory, network I/O, logs, exec access, labels)"`＝**★planner が想定した一般的な一覧**。**★実際の観測（`docker ps --format {{.Names}}::{{.Status}}::{{.Image}}` で name/status/image が取れた）に基づいて「何が取れていないか」を述べていない** |
| **★補** | 観測が本文に届いたか | **★×（★設計の予告が外れた）** | `objective`/`scope`/`steps`/`completion_criteria`/`requirement` に **`OBS-` 0回**（前回も0）／「ALREADY」0／「acquired」0／「観測」0 |
| 補 | 後方互換 | **○** | 11件 不変 |
| 補 | 1箇所・語の禁止 | **○** | `build_planner.py` のみ／3 hunk／31挿入1削除／`gpu`・`nvidia` 0件 |

## 2-1. ★「見せていない」ではなく「見せても本文に出なかった」（★区別した）
```
★prompt に入っていたことを確かめた:
   provenance = {… egl_source_refs: [OBS-00979…00982] …}   ← ★載っている
   _observation_facts(provenance) → ★4行が生成される（★空ではない）
   _plan_prompt(...) に "ALREADY ACQUIRED" が★含まれる      ← ★実行して確認
★∴ ★「見せても、本文には出なかった」である。★言い換えない。
★★ただし ★受入3 は変わった ∴ ★「効かなかった」とも書かない。★本文に id は出ないが、★計画の形は変わった。
```

---

# 3. Last PASS
> **★受入3。** **前回の First FAIL が解消した**（`plan_source=QWEN_BUILD_PLANNER` ／ `runtime_recovery={attempts:2, 4096, RECOVERED}` ／ `CREATED → READY_FOR_IMPLEMENTATION`）。

# 4. First FAIL
> **★受入4。** **★計画は「何が取れていないか」を、★観測ではなく★一般論から並べている。**
> **★∴ 前回から First FAIL が 3 → 4 へ動いた。**

---

# 5. ★予告の当否
| 予告 | 結果 |
|---|---|
| 設計「**補（本文に観測が届く）は立つ**」（★唯一の予告） | **★外れた。** 本文の `OBS-` は 0回のまま |
| 設計「受入3 が立つとは予告しない」 | **★立った**（★予告していない側が立った） |
| 設計「分類は `OBSERVE_CURRENT_STATE`」 | **★当たり** |
| 私の予告 `TASK-2DER-9A6415BE` | **★当たり**（依頼文 57字 / sha1 `9a6415be…`・機械抽出） |

**★`:8005` の内訳（★窓が投入と重なるので分けた）**: 投入区間 **4件**（うち `400` 1件・いつもの形）／`run_next` 区間 **2件**（**★`attempts=2` と一致**）。

# 6. ★2DER 担当工程数の前回差分
```
★0（★今回の修正は IMPL が書いた ∴ 2DER の担当に数えない）
```

# 7. ★次に直す1件（★実施しない・★これは候補である）
> **★観測の「値」を渡せるようにする＝保留中の `D-151`（Ledger）の再開。**
> **根拠**: 今 渡せているのは**取得記録（命令・出所・時刻・結果）だけ**である。**★値は Ledger にしか無い。**
> **★受入4 は「観測の結果 何が取れていないか」を求めている ∴ ★値が無いまま届く見込みは低い**（★`docker ps --format` が name/status/image しか出していないことは、**★値を見なければ言えない**）。
> **★順番は Taka と MGR が決める。★私は決めない。**

---
*IMPL → 設計/監査（写: MGR / Taka）。**§0 = 03:24 の文書で監視が鳴らなかった理由は「私の監視がファイル名しか見ていなかった」ため——D-153 の SPEC は 03:17 に一度 出現して鳴っており（依頼文の無い版）、03:24:19 に同名で中身の違うものが置かれたので新着に当たらなかった。監視ループはいま存在せず落ちた時刻は不明だが本件の説明には要らない（03:17 に鳴っている）。03:15 以降の CC_*.md 7件のうち宛が IMPL なのは D-153 の1件だけで他の取りこぼしは無い。名前＋更新時刻＋サイズで見る形に張り直し、30分ごとの生存行も出すようにした。私の落ち度である。** 本題は D-153＝`build_planner.py` の `_plan_prompt` に観測を見せる修正（3 hunk・31挿入1削除・`gpu`/`nvidia` 0件・`port` は `import` の部分一致1件で単語境界0件・後方互換11件不変）。**受入は 1○ 2○ ★3○（前回×から変化：取得コマンドを実装する step が消え、「観測データを読んで不足を判定する」計画になった）／★4×（不足の一覧が観測ではなく一般論）／★補×（本文に `OBS-` は0回＝設計の唯一の予告が外れた）。** **prompt に観測が入っていたことは実行して確認済み ∴「見せても本文には出なかった」であり「見せていない」でも「効かなかった」でもない（形は変わった）。** **Last PASS=受入3、First FAIL=受入4 ∴ First FAIL が 3→4 へ動いた。** `:8005` は投入4件／`run_next` 2件（`attempts=2` と一致）。**2DER 担当工程数の差分は 0。GENERATE は押していない。commit していない。** 次の1件の候補は**保留中の `D-151`（Ledger）再開＝観測の「値」を渡せるようにすること**（順番は Taka と MGR が決める）。*
