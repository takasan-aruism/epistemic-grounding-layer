# 【BUILD SPEC v2】`EVO-0052` — **★契約だけ直す。★成果物は作り直させない**

- `BUILD_ROLE: ★実装源` / **宛: IMPL**（★封入は MGR） / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-03 11:2x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.8）** ／ **★9項目 確認済（★§6）** ／ 親: `ITEM-2DER-EVO-0035`
- **★v1 との関係**: `CC_DESIGN_2026-08-03_EVO0052_BUILD_SPEC_EFFECTIVE_STATE.md` を **★差し替えない・追記しない**（規律）。**★本書が実装源**。**★1件1本は維持**＝v1 は**役目を終えた**と MGR が受領時に記す。
- **★走行 0・★task 増 0・★commit 0**

---

## 1. ★何を直すのか（★成果物ではない）

```
★worker の成果物は ★試験どおりに正しい（★23行・sha256 677c3f59… 一致・PASSED）。
★★欠陥は ★★私の契約に在る。★2件とも ★私が書いた試験が ★現実を写していなかった。
★★★∴ ★直すのは ★封印試験だけ。★依頼文・骨格は ★1文字も変えない（★同一性を保つ）。
```

| # | 欠陥 | ★実測 | ★原因（★私の側） |
|---|---|---|---|
| 1 | 実データで `next` が取れない | `EVO-0052` の history 9件 → `status=IN_PROGRESS(idx8)` は正・**`next=None`** | 私の試験4本目の fixture が `"note=next=MGR x"` ＝ **先頭から `note=`** の**非現実的な文字列**。実物は **`actor=…` で始まる** |
| 2 | 値を検査していない | `next=FOO` → **`'FOO'` を返す** | 依頼文には5語限定と書いたが、**試験で縛っていない** |

## 2. ★★足す封印試験（★2本だけ。★既存7本は1文字も変えない）

**★fixture は ★台帳の実物から取った（★作文していない・`GET /api/resolve?id=ITEM-2DER-EVO-0052` の history）**

```python
def test_real_note_format_yields_next():
    """★実物の note 形式(先頭は actor=)から next が取れること。
    ★2026-08-03 実測の逐語を fixture にしている(作文でない)。"""
    real = ("actor=Claude stage=IMPLEMENT via=front_door run=ETR-b672a77a2bb9 "
            "note=next=DESIGN 配線した。実測を書く。")
    v = impl.effective_state([{"status": "IN_PROGRESS", "status_note": real}])
    assert v["next"] == "DESIGN", v

def test_unknown_next_value_is_rejected():
    """★5語以外は手番ではない ∴ None(空文字にしない・推測しない)"""
    v = impl.effective_state([{"status": None, "status_note": "next=FOO 何か"}])
    assert v["next"] is None
```

```
★★★これで ★9本になる（★7 + 2）。★既存7本は ★そのまま封入する（★緩めない・並べ替えない）。
```

## 3. ★依頼文に足す1行（★規則を明確にするだけ・★他は不変）

```
・next の取り出しは、★`next=` が ★文字列の先頭に在るか、★`note=` の★直後に在る時だけ採る。
  ★★「直後」は ★文字列全体の先頭という意味ではない ―― ★note の途中に在る `note=next=…` も採る。
  ★取れた値が DESIGN / IMPL / MGR / TAKA / NONE の★いずれでもなければ ★None にする。
```
```
★★v1 の依頼文の該当2行を ★この1行で ★置き換える（★他の行は ★1文字も変えない）
★★★骨格は ★不変（`def effective_state(entries):`）
```

## 4. 受入

```
★(1) ★★同じ依頼文＋9本の試験で ★1回 走らせる。★worker が書く（★Claude は本文を書かない）
★(2) ★★9本 全通（★特に新2本）
★(3) ★★★実データで確かめる: ★`ITEM-2DER-EVO-0052` の history を入れて
     ★`next` が ★`None` でない値になること（★前回は None だった＝★これが直った証拠）
     ★★私の監視の正規表現 `(?:^|note=)next=(DESIGN|IMPL|MGR|TAKA|NONE)\b` と ★同じ答えになること
★(4) ★置いた本文の sha256 が `artifact_sha256` と一致（★Claude が書き直していない）
★(5) ★★行数を分けて書く: worker N行 ／ Claude の配線 M行（★配線は v1 で済んでいるなら 0行と書く）
★(6) ★戻せる ／ ★(7) ★61本を走らせない
★★★★(8) ★出せなかったら『出せなかった』と書いて止まる（★v1 と同じ）
★★★★★予告を投入前に書く: ★worker の行数の見込み ／ ★(3) で出ると思う値
```

## 5. ★★同型の再発（★私の側・★数える）

```
★1度目 2026-08-02 S-3: ★依頼文が書式を1文字ずつ指定したのに ★試験が書式を検査していなかった
   → ★成果物は通ったが ★依頼を満たしていなかった（★14件が日本語にならない 等）
★2度目 2026-08-03 本件: ★依頼文が5語限定と書いたのに ★試験が縛らず、★fixture が現実の形でなかった
★★★共通の形 = ★★「★依頼文には書いた。★試験には書かなかった」
★★★★★∴ ★次から: ★依頼文に「★…の時だけ」と書いた条件は ★★必ず ★否定側の試験を1本 対で置く
   （★通る例だけでなく ★通らない例）。★★これは ★9項目 #7 の具体化であり ★規則は増やさない
★★★★★★★fixture は ★台帳の実物から取る（★作文しない）。★本件で ★実際にそうした
```

## 6. ★9項目（私の分）
```
1 置いたなら読めるか＝★受入(3) は ★front door の history を入力にする
2 読めるなら書けるか＝★書く側は ★本件でも触らない（★v1 §6 のまま）
3 理由を捨てない＝★★§1 の表で ★欠陥2件を ★原因つきで残した（★「直した」で消さない）
4 作っていないのでは＝★★成果物は ★既に在る。★無いのは ★試験2本だけ
5 走ったか＝★受入(3) が ★実データでの確認／6 名前＝★不変（`next` / `status`）
★7 依頼と試験の矛盾＝★★本件そのもの。★§5 で ★否定側の試験を対で置く形にした
8 計器が自分を数えないか＝★受入(3) は ★私の監視の正規表現と ★突き合わせる（★独立な2経路）
★9 増える代わりに廃止＝★★v1 の試験7本のうち ★4本目の fixture は ★現実を写していなかったが
   ★★消さない（★通る例として残す）。★廃止するのは ★★「作文の fixture で受入を測る運用」
```

## 7. 禁止
```
★成果物(effective_state の本文)を Claude が書く・直す ／ ★既存7本の試験を変える・消す
★依頼文の他の行を触る ／ ★骨格を変える ／ ★`set_status` を触る（★範囲外）
★v1 の .md を差し替える・追記する ／ ★新しい台帳・エンドポイントを作る
★61本を走らせる ／ ★commit する ／ ★`twoder` 配下で python を動かす
```
