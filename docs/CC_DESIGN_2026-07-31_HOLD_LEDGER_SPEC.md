# 【保留通知】`Ledger` の SPEC を**★保留する** — **★Taka が順番を変えた。★着手しないこと**

- `BUILD_ROLE: 参照` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 03:0x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **典拠**: `TAKA_2026-07-31_HOLD_THE_DIAGRAM_PROVE_PLAN_USES_OBSERVATION.md`（★逐語・02:59 受領）

---

## 1. ★止めるもの

> ### **★`CC_DESIGN_2026-07-31_D151_BUILD_SPEC_LEDGER_RAW_OUTPUT.md`（`ART-6b7ba8a77f`）は★保留。**
> ### **★着手していないなら着手しない。★着手していたら、★そこで手を止めて報告する（★戻せる範囲で戻す）。**

**★取り下げではない。★順番が後ろになっただけである。** **★調査結果（`blob://` は保管庫でない／生出力は `runs/<RIREQ>.snapshot.json` に在る／つなぎ目はハッシュ）は★そのまま生きている。**

---

## 2. ★理由（★Taka 逐語）

> **「図の修正は保留。まず『Observation を使って PLAN が立つ』ことを証明してください。そこができて初めて、図と実装のどちらを修正すべきか判断します。」**

```
★前回の順番: 1. Ledger → 2. E2E → 3. PLAN 証拠接続（別件）
★★新しい順番: ★PLAN 証拠接続が先。★Ledger は待つ。
★かつ「図を実装に合わせる」という前回の裁定そのものが★保留になった（★確定ではない）。
```

---

## 3. ★変わっていないもの（★私が勝手に緩めない）
```
★1件ずつ（同時実装しない）        ★観測を Task の下流へ移す設計変更は禁止
★GENERATE を押さない              ★新しい台帳・API・ID 族を作らない
★別の欠陥を見つけたことを理由に止まらない（★2通りに読めたら狭い方を採って進み、迷いは1行 書く）
★受入条件（★`G-87` に登記済・★Taka の逐語のまま）:
   cites_source_ids が空でない ／ 対象 Observation または Ledger 記録の ID を引用する ／
   既に観測済みの能力を新規作成対象として重複計画しない ／
   証拠から不足が判定された場合だけ改修 Task を作る
```

---

## 4. ★次に来るもの
```
★新しい SPEC を私が1本 書いて出す（★対象＝`G-87` の受入条件を満たすこと）。
★★それが届くまで、★あなたは何も始めない。
★★私は MGR の実施文書（D-152）を待って、★手番と範囲を合わせてから出す。
```

---
**決めたこと**: **①`D-151` の Ledger SPEC は保留（取り下げではなく順番が後ろ）。着手していたら手を止めて報告する ②理由は Taka が「まず『Observation を使って PLAN が立つ』ことを証明せよ」と順番を変えたため（図を実装に合わせる裁定自体も保留） ③変わっていない縛り（1件ずつ・観測を task 下流へ移さない・GENERATE を押さない・新しい台帳/API/ID 族なし・止まらない・`G-87` の受入条件）は緩めない。**
