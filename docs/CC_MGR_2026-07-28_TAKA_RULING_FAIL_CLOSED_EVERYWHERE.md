# CC 管理(MGR) → 設計/監査(CC-α): **★Taka 裁定: 記録失敗時は fail-closed。私の ④ 例外を撤回する**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.5 — `§12` を最大版で読んだ値）**
- **典拠: Taka（2026-07-28・逐語）「記録失敗時は fail-closed で」**

## 1. ★Taka の裁定（逐語）
> **「記録失敗時は fail-closed で」**

**★これは全合流点に及ぶ。** **「⓪①②③ は fail-closed、④ だけ fail-open」という私の裁定は★撤回する。**

## 2. ★私が付けていた例外と、その撤回
| | |
|---|---|
| **私の裁定（撤回）** | 合流点④（DW `_append_event`）だけ fail-open。**理由: worker 実行中に止めると成果物が失われる** |
| **★Taka の裁定** | **全て fail-closed** |
| **私が Taka に報告済** | **「DW だけ穴が残ります」と明記して報告した。** **∴ 彼はその代償を知ったうえで決めている** |

**★帰結（隠さず書く）:**
> **記録に失敗した瞬間に、worker の実行も止まる。** **∴ そのとき sandbox に在った成果物は失われる。**
> **★本日 Build 18 で、成果物が消える経路を実際に見ている。** **その代償を受け入れる、という決定である。**

**★私は再度の異議を出さない。** **一度述べ、決定が出た。** **実装する。**

## 3. 依頼（★小さく1つ）
**合流点④を fail-closed にすること。** **形は他の4つと同じ**（`_fail` 相当で「記録できなかった」を結果として返す）。
| # | 条件 |
|---|---|
| **1** | **★他の4つと同じ形にする。** **④専用の新しい形を作らない** |
| **2** | **★`G-38`（「④は fail-open。取りこぼしが起きうる」）を `CLOSED` にする。** **穴が塞がるため** |
| **3** | **★`dropped_before` / `stderr` の仕組みは残してよいが、必須条件から外す。** **fail-closed なら取りこぼしは起きない** |
| **4** | **★「成果物が失われうる」ことを、実装源とコードのコメントに1行残す。** **後から読む者が、代償を知らずに「なぜここで止まるのか」と外さないように** |
| **5** | **★止まったときに何が失われるかを、止まった時点で分かる形にする**（可能なら sandbox のパスを返り値か記録に残す）。**★できないなら「できない」と書く。無理に作らない** |

## 4. ★順序（変えない）
**★これは次段に入れる。** **いま走っている CLI 投入の後始末より先にしない。**
1. `ETRACE_RUN_ID` を webui から読めるように（`G-46`）
2. **★合流点④の fail-closed 化（本件）**
3. 35件の3区分
4. 進行経路の run
5. EGL 合流点③を通ったかの確認

**★1と2は同じ build にしてよい。** **どちらも「記録の完全性」であり、混ざっても切り分けられる**（片方は読み出し、片方は失敗時の挙動）。
**★3以降とは混ぜない。**

---
*MGR。★Taka 裁定（逐語）「記録失敗時は fail-closed で」——これは全合流点に及び、「ⓠ①②③ は fail-closed、④ だけ fail-open」という私の裁定を**撤回する**。★私が付けていた例外は「worker 実行中に止めると成果物が失われる」を理由とした ④ の fail-open だったが、その代償は Taka に「DW だけ穴が残ります」と明記して報告済であり、**彼はそれを知ったうえで決めている**。帰結を隠さず書く=記録に失敗した瞬間に worker の実行も止まり、そのとき sandbox に在った成果物は失われる（本日 Build 18 で成果物が消える経路を実際に見ている）——**その代償を受け入れるという決定である。私は再度の異議を出さない。一度述べ、決定が出た。実装する。** ★依頼=合流点④を他の4つと同じ形で fail-closed にする（④専用の新しい形を作らない）／`G-38`（④は fail-open で取りこぼしが起きうる）を `CLOSED` にする（穴が塞がるため）／`dropped_before`・`stderr` の仕組みは残してよいが必須条件から外す（fail-closed なら取りこぼしは起きない）／**「成果物が失われうる」ことを実装源とコードのコメントに1行残す**（後から読む者が代償を知らずに「なぜここで止まるのか」と外さないように）／止まったときに何が失われるかを止まった時点で分かる形にする（可能なら sandbox のパスを残す。できないなら「できない」と書き無理に作らない）。★順序=次段に入れ、いま走っている CLI 投入の後始末より先にしない。①`ETRACE_RUN_ID` を webui から読めるように ②本件 ③35件の3区分 ④進行経路の run ⑤EGL 合流点③の確認。**①と②は同じ build にしてよい**（どちらも「記録の完全性」で、片方は読み出し・片方は失敗時の挙動なので混ざっても切り分けられる）が、③以降とは混ぜない。*

---

# ★★ 本文書は撤回する（2026-07-28・Taka「誤送信」） ★★

**Taka から「記録失敗時は fail-closed で　誤送信」と訂正があった。**
> **∴ §1 の「裁定」は★発せられていない。** **私が指示として受け取ったのが誤りである。**

## 撤回の内容
| | |
|---|---|
| **無効になるもの** | 本文書 §1〜§4 のすべて（④ を fail-closed にする依頼・`G-38` の `CLOSED` 化・順序への割り込み） |
| **★戻る先** | **`CC_MGR_2026-07-28_D42_RULINGS_A_B_AND_TEXT_APPROVED.md` §2 の裁定**<br>= **合流点 ⓪①②③ は fail-closed / ★合流点④（DW `_append_event`）は fail-open。穴を見える場所に置く4条件つき** |
| **`G-38`** | **`OPEN` のまま。** **閉じない** |
| **実装** | **★何も変えない。** **現状（④ が fail-open）が正である** |

## ★本文書を消さない理由
**私が「指示でないもの」を裁定として登録し、設計へ流した事実は残す。**
**★記録を書き換えない**（追記式台帳を遡らない原則を、我々自身の文書にも適用する）。

## ★私の落ち度
**★私は確かめずに実行した。** **「誤送信ではないか」を一度も疑っていない。**
**本日、私は「伝聞をそのまま渡す」で2回外している。** **★今回は Taka の1行を、確かめずに全合流点へ広げた。** **3回目である。**
**∴ 短い指示ほど、範囲を自分で広げていないかを確かめる。**

**★もし fail-closed への変更を本当に望むなら、一言で戻せる。** **その場合は本文書の §1〜§4 を再有効化する。**
