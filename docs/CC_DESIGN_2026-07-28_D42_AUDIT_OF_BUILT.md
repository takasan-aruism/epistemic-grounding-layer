# 設計/監査 → MGR（写: Taka / IMPL）: **D-42 BUILT の監査 — ★判定は不合格。ただし理由は「抜け道4件」ではない。本番で到達するのは1件で、根は別に在る**

- `BUILD_ROLE: 参照`（**監査。コードを1行も変えていない・投入していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: `v2.2` — `§12` で日付が最も新しい行を採って確認）**
- **受領**: `CC_IMPL_2026-07-28_D42_EVENT_TRACE_PHASE1_BUILT.md`

## 0. ★判定（先に）
> **不合格。** **ただし IMPL が挙げた「抜け道4件」が理由ではない。**
> **★実測: 4件のうち本番経路から到達するのは★1件だけである。**
> **★そしてその1件の根は「台帳に emit が無い」ではなく、★進行経路が合流点⓪を通らないことである。**
> **★もう1つの不合格理由は、IMPL の落ち度ではない: 受入⑥（実データを貼る）が★構造上満たせない。**

---

## 1. ★抜け道4件の到達可能性【監査:CC-α】（IMPL が「調べていない」と明記した箇所）
```
再現: 各関数の呼び手を全repo で計数（test / audit スクリプトを除外）
```
| # | 抜け道 | **呼び手** | **本番到達** |
|---|---|---|---|
| 1 | `rri/rri/request_thread.py:73 _append` | **★0**（`egl/docs/audit_rthread_stage1.py` = 我々の監査スクリプトのみ） | **★到達しない**（`G-33` の再確認） |
| 2 | `egl/egl/core.py:119 append_event` | `egl/curator.py` / `egl/pipeline.py` | **★到達しない**（`grep`: `twoder`/`ds`/`rri`/`dw` は `egl.pipeline`・`egl.curator` を1つも import しない＝別系統の EGL パイプライン） |
| 3 | `dev-workcell/dw/authorization.py:46 _append` | **★0**（1件も出ない） | **★到達しない** |
| **4** | **`dev-workcell/dw/dispatch.py:162 _emit_pending`** | **`dispatch.py:132`（`dispatch_once` の内側・Claude barrier 時）** | **★到達する。本番で頻繁に通る** |

> **∴ 「抜け道4件」は★本番では1件である。**
> **★IMPL は誤っていない。** **「構造として書けること」だけを示すと明記し、到達可能性は調べていないと書いた。** **その境界を守った報告である。**
> **★調べるのは監査の担当であり、私がやった。** **これが分業の正しい形である。**

## 2. ★残った1件の根は、そこではない
```
再現: grep -n "run_next\|dispatch_once" twoder/webui.py
  webui.py:591-592   if u.path == "/api/run_next":  step = D.dispatch_once(tid, _machine_registry(), TS)
```
> **`/api/run_next` は `dispatch_once` を★直接呼ぶ。** **`submit()` を通らない。**
> **∴ 合流点⓪を通らない。** **∴ `run_id` が無い。**
> **∴ `_emit_pending` に emit を足しても、★`run_id` の無い event が増えるだけである。**

**★これは IMPL の12入口表の ⑨〜⑫ と★同じ根である**（`create_task` 直呼び3件・`record_utterance` 直呼び1件も `run_id=None`）。
| | |
|---|---|
| **本当の欠落** | **合流点⓪は「投入」の経路にしか無い。** **★「進行」の経路（`run_next` / `run_until_barrier` / `operator`）には無い** |
| **∴** | **1件の依頼を DS→RRI→EGL→DW と追うには、★投入だけでなく進行にも run を開く必要が在る** |
| **★私は作らない** | **これは第一段階の範囲を超える**（実装源 §2-7-2「第二段階を先取りしない」）。**裁定事項として上げる** |

## 3. ★もう1つの不合格理由（IMPL の落ち度ではない）
```
IMPL 実測: grep -n "event_trace\|etrace" twoder/webui.py twoder/ids.py → 0件
           GET /api/resolve?id=RUN-ee28ab4e9438 → resolved:false
           台帳直読 → PreToolUse フックが拒否
           LEDGER_REGISTRY 判定 → ★ORPHAN（読み手が無い）
```
> **∴ Taka の完了条件⑥「実データを貼って証明する」は、★構造上満たせない。**
> **∴ 記録は作られたが、★読む口が無い。** **本日9回数えた型の10回目を、我々自身が作った。**

**★ただし IMPL は正しく振る舞った**: **迂回していない。** **拒否された操作を別の書き方で通そうとしていない。** **「答えられなかった」を結果として出した**（使用ガイド §0-3）。

## 4. ★私（設計）の欠陥3件（自己申告。IMPL に帰さない）
| # | 欠陥 | 実害 |
|---|---|---|
| **1** | **受入⑦(c) の区分が「入口」と「台帳への書き込み」を★混同していた** | **IMPL は私の基準どおりに分類し、到達しない3件を「抜け道」に入れざるを得なかった。** **判定を4倍に膨らませたのは私の基準である** |
| **2** | **4区分に `twoder` 自身の台帳の枠が無かった** | **IMPL が「区分の不足」として自分で申告した。** **★申告できるように書いていなかったのは私である** |
| **3** | **§3-4 の予想が、routing を確かめずに前提を置いた** | **実際は `OBSERVE_CURRENT_STATE` → 取得経路で、`task_id` は2件とも `None`。** **★DW task が作られる前提が最初から成立していなかった** |

**★特に3について**: **依頼文は私が書いた。** **「現状で追跡が途切れる箇所を挙げてください」は★観測の依頼であって実装の依頼ではない。**
> **∴ `classify_request_type` が `OBSERVE_CURRENT_STATE` を返すことは、★投入前に決定論で確かめられた。**
> **★私は確かめずに予想を固定した。** **「予想を固定する前に決定論で確定する」に反した。**
> **∴ 受入③が満たせなかったのは、★依頼文の選定ミスであって実装の問題ではない。**

## 5. ★IMPL の報告で評価する点（記録する）
1. **★自分が増やした失敗を消していない**（`test_fi_min` が新規に落ちた事実と原因と修正を書いた）。**「増えていない」と書くために都合の悪い事実を消さなかった。**
2. **★区分の不足を自分から申告した**（私の基準の欠陥を、埋めずに報告した）。
3. **★「実際に呼ばれるかは調べていない」と境界を明記した**（構造と到達可能性を混ぜなかった）。
4. **★webui を投入前に再起動し、pid と時刻を実値で示した**（本日 Build 10 の型を踏まなかった）。
5. **★スナップショットを実装前に取り、順序を守った。**

## 6. 監査で確認した「変えていないこと」（受入④）
```
IMPL 実測: /api/claude_packet before 109f58874740 / after 109f58874740  差分ゼロ
           /api/state        before f1c971e61fbe / after f1c971e61fbe  差分ゼロ
           非回帰 98本: 前 91/7 → 後 91/7、失敗の顔ぶれ diff 空
```
**【監査:CC-α】★受入④は満たされている。** **★ただし私は再実行していない**（`【伝聞】`）。**再実行するなら誰が=CC-α / いつ=MGR が要ると言えば。**

## 7. ★MGR に上げる（私は決めない）
| # | 論点 | 選択肢 |
|---|---|---|
| **A** | **本 build を「不合格」とするか「条件付き受領」とするか** | (a) 不合格として差し戻す (b) **★配線は受領し、残り2点（進行経路の run／読み出し口）を別の段として立てる**。**【設計:CC-α】(b) を推す** — 配線と受入④は実測で満たされており、残り2点はいずれも★第一段階の範囲外だから |
| **B** | **進行経路（`run_next` 等）にも run を開くか** | **第二段階の先取りに当たりうる。** **★Taka の裁定②（「呼ばれなかった処理」の範囲）と同じ場所に属する** |
| **C** | **読み出し口をどうするか** | **★既に Taka に上がっている「2行の表示追加」と同じ性質である。** **こちらに合流させるべきか、別に立てるか** |

**★私は (b) を推すが決めない。** **★特に C は、`ORPHAN` を放置すると「作ったが読めない台帳」が1つ増えたまま固定される。**

## 8. ★未確認（「全部見た」と書かない）
| # | 未確認 | 誰が・いつ |
|---|---|---|
| 1 | **受入④を私は再実行していない**（IMPL の実測を読んだだけ） | CC-α / MGR が要ると言えば |
| 2 | **`event_trace.jsonl` の中身を私も見ていない**（読み出し口が無いため）。**∴ 本番で親子が付いたかは私も未確認** | CC-α / 読み出し口ができた時 |
| 3 | **抜け道 #2（`egl/core.append_event`）は「`twoder` が `egl.pipeline` を import しない」で到達しないと書いた。** **間接呼び出し（動的 import 等）は見ていない** | CC-α / 疑義が出れば |
| 4 | 入口③④⑤⑥は実行未確認（`grep` のみ・IMPL から引き継ぎ） | 据え置き |

---
*CC-α による D-42 BUILT の監査。★判定=不合格。ただし IMPL が挙げた「抜け道4件」が理由ではない——**呼び手を実測した結果、本番経路から到達するのは1件だけ**（`rri/request_thread._append` は呼び手0＝`G-33` 再確認／`egl/core.append_event` は `egl/curator`・`egl/pipeline` からのみで `twoder`/`ds`/`rri`/`dw` はそれらを1つも import しない別系統／`dw/authorization._append` は呼び手0／**`dw/dispatch._emit_pending` のみ `dispatch_once` の Claude barrier 経路で本番到達**）。**IMPL は誤っていない**——「構造として書けることのみ示し到達可能性は調べていない」と境界を明記した報告であり、調べるのは監査の担当で私がやった（分業の正しい形）。★残った1件の根はそこではない=`webui.py:591-592` の `/api/run_next` は `dispatch_once` を直接呼び `submit()` を通らない ∴ 合流点ⓠを通らず `run_id` が無く、`_emit_pending` に emit を足しても `run_id` の無い event が増えるだけ。**これは IMPL の12入口表の ⑨〜⑫（`create_task` 直呼び3件・`record_utterance` 直呼び1件が `run_id=None`）と同じ根で、本当の欠落は「合流点ⓠが投入の経路にしか無く、進行の経路（`run_next`/`run_until_barrier`/`operator`）に無い」ことである**——第一段階の範囲を超えるので私は作らず裁定事項として上げる。★もう1つの不合格理由（IMPL の落ち度ではない）=読み出し経路が無く（`webui`/`ids` に該当0件・`/api/resolve` は `resolved:false`・直読はフックが拒否・`LEDGER_REGISTRY` 判定は **ORPHAN**）**Taka の完了条件⑥「実データを貼って証明する」が構造上満たせない**——本日9回数えた型の10回目を我々自身が作った。ただし IMPL は迂回せず「答えられなかった」を結果として出しており正しく振る舞った。★私（設計）の欠陥3件を自己申告=①受入⑦(c) の区分が「入口」と「台帳への書き込み」を混同しており、判定を4倍に膨らませたのは私の基準である ②4区分に `twoder` 自身の台帳の枠が無く、IMPL が「区分の不足」を自分から申告する形になった ③**§3-4 の予想が routing を確かめずに前提を置いた**——依頼文「現状で追跡が途切れる箇所を挙げてください」は観測の依頼であり `classify_request_type` が `OBSERVE_CURRENT_STATE` を返すことは投入前に決定論で確かめられたのに確かめずに予想を固定した（「予想を固定する前に決定論で確定する」に反した）∴ 受入③が満たせなかったのは**依頼文の選定ミスであって実装の問題ではない**。★IMPL の報告で評価する点5件（自分が増やした `test_fi_min` の失敗を消さず原因と修正を書いた／区分の不足を自分から申告した／「実際に呼ばれるかは調べていない」と境界を明記した／webui を投入前に再起動し pid と時刻を実値で示した／スナップショットを実装前に取り順序を守った）。★受入④は満たされている（`/api/claude_packet`・`/api/state` とも sha256 一致、非回帰 91/7 で顔ぶれ diff 空）が**私は再実行していない**（`【伝聞】`）。★MGR に上げる=A 本 build を不合格として差し戻すか条件付き受領とするか（**CC-α は「配線は受領し、残り2点を別の段として立てる」を推す**——配線と受入④は実測で満たされ、残り2点はいずれも第一段階の範囲外だから）／B 進行経路にも run を開くか（第二段階の先取りに当たりうる。Taka の裁定②と同じ場所に属する）／C 読み出し口をどうするか（**既に Taka に上がっている「2行の表示追加」と同じ性質**で、合流させるか別に立てるか。`ORPHAN` を放置すると「作ったが読めない台帳」が1つ増えたまま固定される）。★未確認4件（受入④を私は再実行していない／`event_trace.jsonl` の中身は私も見ていない ∴ 本番の親子は私も未確認／抜け道 #2 の間接呼び出しは見ていない／入口③④⑤⑥は実行未確認）。*
