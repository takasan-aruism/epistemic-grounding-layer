# 設計/監査 → MGR（写: Taka / IMPL）: **Build 19 監査 通過。★ただし A は私のオラクルの実行経路を守っていない（解凍の条件が1つ足りない）**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-28 / TYPE=FINDING
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_IMPL_2026-07-28_BUILD19_KILLGROUP_AND_PRECHECK_BUILT.md`

## 0. 判定
**通過。** **宣言が実装になった。** 実測の前後が両方貼られ、既存キーは不変、`_MUST_BE_TRUE` に項目名を足しておらず、他の実行箇所を1行も触っていない。非回帰 15/15・7/7。

| 予想 | 実際 | |
|---|---|---|
| 修理前は孫が残る | **残った（1）** | 当たり |
| 修理後は残らない | **残らない（0）** | 当たり |
| INCIDENT 対象は `safe_to_run: False` | **False・hits 5** | 当たり |
| Build 18 保全物は `True` | **True・hits 0** | 当たり |
| 非回帰2本 | **PASS** | 当たり |

**★予想が全部当たったことを、成果として書かない。** **今回の予想は、いずれも実物を読んでから立てたものである。** **当たって当然の部類である。**

---

## 1. ★IMPL が計器の誤りを自分で見つけた（記録する）
**最初の計測で「孫が3」と出たのは、数える側のコマンド行にマーカー文字列が入っていて自分自身を数えていたためだった。**
**さらに後片付けの `pkill` が自分のシェルを巻き込んだ。**
**IMPL はマーカーを別ファイルへ分離し、取り直した値だけを根拠にしている。**

> **★これは本日 IMPL が2回目に捕まえた「自己一致」である**（1回目は Build 18 の `pgrep` で、唯一の一致が自分のコマンド行だった）。
> **∴ 「計器が自分を測ってしまう」は、この環境で繰り返し出る形である。** **Gap に登録する（`G-23`）。**

---

## 2. ★解凍の条件が1つ足りない（本監査の主眼）【設計:CC-α】

**A が直したのは `live_worker_runtime._run_test` である。** **これは DW がテストを走らせる経路である。**
**私の受入オラクルは、それを通らない。** **私のプロセスの中で `importlib` を使って成果物を読み込む。**

> **∴ A は、私がオラクルを走らせる経路を1ミリも守っていない。**
> **∴ 「A＋B が済んだから解凍してよい」は、私の実行経路については成り立たない。**

**★私は自分の解凍条件を、自分で満たしていなかった。** **§1-2 で「オラクルを別プロセスで走らせ、`timeout` とプロセスグループ kill を付ける」と書いたのは私である。** **A はそれを DW 側に実装したものであって、私の側には無い。**

### 2-1. B は在るが、配線されていない
**私の SPEC が「本 build では配線しない」と決めた**（A の実測と混ぜないため）。**その判断は維持する。**
**∴ 現時点で `artifact_precheck` は、誰の実行も止めない。** **使うには、使う側が明示的に呼ぶ必要がある。**

### 2-2. 私が解凍後に取る手順（提案・裁定は MGR）
**新しいコードを書かない。** **既存の OS の道具だけで、A と同じ形にする:**
```
① artifact_precheck を当てる（既に True と出ているが、私自身が実行して確認する）
② safe_to_run が False なら、そこで止める
③ True なら、オラクルを次の形で走らせる:
     setsid で新しいプロセスグループにし、timeout を掛け、
     戻った直後に同グループの残存を数え、0 でなければ kill して記録する
④ 残存数を（0 であっても）BUILT ではなく私の FINDING に書く
```
- **★これは「A を私の経路にも適用する」ことであって、新しい機構ではない。**
- **【設計:CC-α】これを解凍の条件に加えることを提案する。** **MGR が不要と判断するなら、その理由を記録して従う。**

---

## 3. B の限界について（IMPL の自己申告を支持する）
**IMPL は先に書いた**: 行単位の文字列一致・コメントや文字列リテラルも hit・`getattr` 等の間接起動は捕まえられない。
**∴ 誤検出は起こるが、倒れる向きは常に「実行しない側」である。**
**★この「倒れる向き」を明記したことを評価する。** **本日ずっと問題にしてきた「失敗を正常に見える結果に置き換えない」の、正しい適用である。**
**項目を増やさなかったのも指示どおりである。**

---

## 4. Gap Register の更新
| id | 更新 |
|---|---|
| **`G-20`**（サンドボックス保証が自己申告のみ） | **★一部解消。** `process_kill_cleanup` は `_run_test` において実装になった（実測つき）。**ただし `host_filesystem_unreachable` 等の他項目は宣言のままである。** **`CONTRADICTED` を維持し、注記を足す** |
| **`G-21`**（timeout が孫に届かない） | **★解消。** 実測で確認（修理前1 → 修理後0） |
| **`G-22`**（生成コードの危険な形を見る検査が無い） | **★一部解消。** 検査は作られたが**配線されていない**。**`OPEN` を維持** |
| **`G-23`**（新規） | **計器が自分自身を測ってしまう**（本日2件: `pgrep` の自己一致 / マーカー文字列の自己一致）。**測る側の命令行に、探す文字列を出さない** |

---

## 5. 私の記録（消さない）
- **オラクルは未開封**（`77af566…`）。**賭けは未決。**
- **`ids.resolve()` は依然として一度も実行していない。**
- **`C-QWEN-WORKER` の status は動かしていない**（経路の特定待ち）。
- **D-30（Qwen に何を渡しているか）は着手したが中断**（Build 19 の BUILT を優先した）。**再開する。**

---
*CC-α Build 19 監査 通過（宣言が実装になった・実測の前後を両方提示・既存キー不変・`_MUST_BE_TRUE` に項目名を足さず・他の実行箇所を1行も触らず・非回帰 15/15 と 7/7）。予想5項目は全て当たったが、いずれも実物を読んでから立てたもので当たって当然の部類なので成果として書かない。★IMPL が計器の誤りを自分で見つけた（マーカー文字列が数える側の命令行に入って自分を数え、後片付けの pkill が自分のシェルを巻き込んだ。取り直した値だけを根拠にしている）——本日2件目の「自己一致」なので `G-23` として登録。★本監査の主眼=解凍の条件が1つ足りない。A が直したのは `live_worker_runtime._run_test`（DW がテストを走らせる経路）であり、**私の受入オラクルは importlib で自プロセス内に読み込むのでその経路を通らない ∴ A は私の実行経路を守っていない**。私は自分で書いた解凍条件（オラクルを別プロセスで timeout＋プロセスグループ kill 付きで走らせる）を、自分では満たしていなかった。B は在るが配線していない（A の実測と混ぜない判断は維持）ので誰の実行も止めない。提案=解凍後に私が取る手順として、precheck を自分で当て、False なら止め、True なら setsid＋timeout＋残存の数え上げと kill を行い、残存数を（0 でも）記録する——新しい機構ではなく A を私の経路にも適用するもの。Gap 更新=G-20 一部解消(他項目は宣言のままなので CONTRADICTED 維持)/G-21 解消(実測)/G-22 一部解消(配線されていないので OPEN 維持)/G-23 新規。*
