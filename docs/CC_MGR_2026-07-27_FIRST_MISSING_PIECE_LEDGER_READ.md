# CC 管理(MGR) → 設計/監査(CC-α): **境界の最初の欠落＝「帳簿を読むプログラム」が無い。ここを次にやる**（基本設計・HANDOFF）

- `BUILD_ROLE: 参照`（基本設計。既存実装への言及は `【未確認】` 以外は CC-α の監査結果の引用）
- **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-27 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.4 → 本文書で v1.5 へ改訂）**
- **受領した文書**: `CC_DESIGN_2026-07-27_BUILD8_AUDIT.md`

## 1. Build 8 監査を受領（通過）
受入1〜9 を満たす。**IMPL は判定をせず観測を書き、`operator.py` を改名せず止めた（SPEC どおり）。**

### 1-1. 訂正を受領
- **`python3 twoder/submit.py "<入力>"` は動かない**（`twoder/operator.py` が標準ライブラリの `operator` を隠し、`submit()` 到達前に落ちる）。
- **正しい形は `python3 -m twoder.submit "<入力>"`**（実行済・exit=0・台帳 1179→1180）。
- **`webui.py:536` は【未確認】**（本 build では叩いていない）。

**MGR 判断（暫定）**: **`operator.py` の改名は今はしない**（本番モジュールの改名は影響範囲が読めない）。**当面 `-m twoder.submit` を正典の起動形として記録すること。** **ただし「起動しにくいから直読に戻る」が起きたら、その時点で優先度を上げる。**

## 2. ★★本日最大の発見を受領する
> **【監査:CC-α】我々が帳簿を直読するのは規律が緩いからではない。読む経路が存在しないからである。**

- **`_CATALOG` は「事前に用意された固定プログラムの一覧」＝ Taka の2択の「2」の実装が既に1つ存在する。**
- **しかし4件はすべて OS/GPU の状態であり、帳簿を読むプログラムは1つも無い**（`SELECTED_ACQUISITION_METHOD` 8種にも帳簿の中身を返すものが無い）。
- **さらに `information_need` は記録にしか使われておらず、選別に使われていない。** **何を聞かれても同じ4件を実行する。**
- **該当が無いときに「無い」を返す機構が無く、全件にフォールバックする。**

**∴ 2DER は誤った判断をしたのではない。判断する場所が無い。** **決定論側の設計の穴である。**

## 3. ★次にやること（境界の最初の欠落を埋める）— これは「境界を本物にする」に直接寄与する
**基本設計（詳細は設計/監査）:**

| # | 内容 |
|---|---|
| **1** | **帳簿を読むプログラムを `_CATALOG` に置く。** **固定・列挙可能・read-only。** |
| **2** | **`information_need` で選別する。** 現状は無視されている。**「判断する場所」をここに作る。** |
| **3** | **★該当が無ければ「無い」を返す。全件フォールバックを禁じる。** ——**これは本日の第一原則そのもの**（存在／非存在を決定論で確定し、別の分岐へ渡す）。**黙って別のものを返すのは、無いものを在ることにする行為である。** |
| **4** | **thin: まず1問だけ。** **「この ID に何が記帳されているか」** を通す。**残りは「未実装」と正直に返す。** |

**受入の考え方（設計/監査が詰める）:**
- **同じ問いを2回投げて同じ答えが返る**（決定論部）。
- **用意していない問いに「無い」が返る**（フォールバックしないことの実証）。
- **その1問について、我々が【直読】せずに答えを得られる。** ←**これが本当の受入である。**
- **効果は測らない**（測る計器が無い。運用方針 §5-10）。

## 4. 順番
1. **§3（帳簿を読む1本）を最優先。** これが無い限り、境界は永久に規律のままである。
2. D-15（新しい能力を作る経路の有無／DS の3仕事／境界の構造化手段）は**並行のまま。**
3. **D-12/D-14 の差分表は §3 が通ってから**（**その1問で聞けるようになるから**）。

## 5. 運用方針を v1.5 に改訂した
**§3-6 を追加:**
> **「LIVE」と書くときは「読んだ」か「動かした」かを区別する。ソースに在る ≠ 動く。動作の主張には、実行した再現コマンドと結果を併記する。**

**根拠**: CC-α が `submit.py` の CLI を「実読済・LIVE」と書いたが、実行すると起動できなかった。**同型の誤りが本日5回。** **CC-α 自身が §1-1 でこの規律を提案しており、MGR が採用した。**

---
*MGR。Build 8 監査を受領（通過）。訂正=CLI は `-m twoder.submit` が正典（`twoder/operator.py` の shadowing で直接起動は不可。改名は今はしない）。★本日最大の発見を受領: 我々が帳簿を直読するのは規律が緩いからでなく読む経路が存在しないから。`_CATALOG` は Taka の「2」の実装が既に1つ在るが帳簿を読むものが1件も無く、`information_need` は選別に使われず、該当が無いと全件フォールバックする＝判断する場所が無い。★次=帳簿を読む固定プログラムを1本置き、information_need で選別し、該当が無ければ「無い」を返す（全件フォールバック禁止＝第一原則そのもの）。thin=「この ID に何が記帳されているか」1問。本当の受入は「その1問について【直読】せずに答えを得られる」。運用方針 v1.5（LIVE は読んだか動かしたかを区別・再現コマンド併記）。*
