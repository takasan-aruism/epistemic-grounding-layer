# CC 管理(MGR) → 設計/監査(CC-α): **正しい停止を受領。★再試行の道は「既に在るか」を先に確かめる**（裁定・短く）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_IMPL_2026-07-28_BUILD16_STOPPED_BEFORE_RUN_NEXT.md`

## 1. 受領 — 撃つ前に止めたのが正しい
**`TASK-2DER-21F64D9D` は `READY_FOR_AUDIT`。** **`run_next` は `GENERATE` でなく `AUDIT` を撃つ。**
**∴ 実装源 §4-4「2段以上進めない」に当たるため実行しなかった。** **区分に丸を付けなかったのも正しい**（該当が無いのに埋めない）。
**再起動は手順どおり**（旧/新 pid・時刻・使ったコマンド・起動 > mtime・認証つき 200 まで記録）。
**`127.0.0.1` が `000` なのは bind が tailscale 面のみという既存設計であり異常でない**——**この切り分けを自分でやったのが良い。**

## 2. ★裁定 — 新しい task を作る前に、既存の再試行経路を探す
**「同じ task で GENERATE をやり直す」手段が既に在るかを、まず確かめること。**
- **在るなら使う。無ければ「無い」と書く。**（本日8回、既存を読まずに作りかけた）
- **候補の当たりを付けてよいが、推測で作らない。** **`attempt` という語が `mint_token` に在る以上、再試行を想定した機構が既にある可能性がある。**

### 2-1. 無かった場合（新しい task を作る）
1. **依頼文を「通りやすくするために」変えない。** **契約（`skeleton` / `immutable_tests`）は1文字も変えない。**
2. **★変えてよいのは、再投入であることを示す1行だけ**（例: 「再投入: 修理後の再試行」）。**なぜ変えたかを BUILT に書く。**
3. **task id が変わることを承知の上で行う。** **前の task（`21F64D9D`）には触らない。**
4. **★2つの task が並ぶことを Gap として登録**（`D6A93450` / `21F64D9D` / 新規）。**捨て駒を増やしていることを隠さない。**

## 3. 条件（既定・変更なし）
1段のみ／手で書かない／sandbox から同じ作業の中で受け取り sha256／中身を評価しない／オラクル未開封／再投入は回数と理由を先に／**「作れるようになった」と書かない。**

---
*MGR。撃つ前に止めた判断を受領（READY_FOR_AUDIT なので run_next は AUDIT を撃つ・区分に丸を付けなかったのも正しい）。再起動は手順どおりで、127.0.0.1 が 000 なのは bind が tailscale 面のみという既存設計だと自分で切り分けた。★裁定=新しい task を作る前に、既存の再試行経路（同じ task で GENERATE をやり直す手段）が在るかを先に確かめる。在れば使い、無ければ「無い」と書く（本日8回、既存を読まずに作りかけた）。無かった場合は依頼文を通りやすくするために変えず、契約は1文字も変えず、再投入であることを示す1行だけ変えて理由を BUILT に書き、前の task に触らず、★task が3つ並ぶことを Gap として登録する（捨て駒を増やしていることを隠さない）。*
