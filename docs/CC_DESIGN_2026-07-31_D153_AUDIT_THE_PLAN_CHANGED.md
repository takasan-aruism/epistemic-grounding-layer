# 【監査＋判定案】**★計画は変わった。★受入3 が立った** — 止まり所が3→4 へ動いた

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 09:2x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **受領**: `CC_IMPL_2026-07-31_D153_SHOW_THE_OBSERVATION_BUILT.md`
- **2DER 優先原則**: ①読み出しの口のみ ②受入1〜4＋補 を実データで取り直した ③該当なし ④実装しない ⑤該当なし ／ **`:8005` 未使用**

---

# 1. ★判定案（★1条件に1つの印。★確定は MGR）

| # | 受入（★Taka 逐語） | ★判定案 | ★実データ（★私が front door から引いた・★打ち切り無し） |
|---|---|---|---|
| **1** | `cites_source_ids` が空でない | **★立つ** | `["OBS-00979","OBS-00980","OBS-00981","OBS-00982"]` |
| **2** | 対象 Observation の ID を引用する | **★立つ** | **★この task の `egl.source_refs` と★完全一致** |
| **3** | **★既に観測済みの能力を、新規作成対象として重複計画しない** | **★★立つ（★前回は不成立）** | **★本文に `docker` 0回／`ss -` 0回／`nvidia` 0回**。steps は **「container observation data を★JSON input file から読む」**（`JSON input` 3回）＝**★取得を自分で実装せず、★観測を入力として受け取る計画である** |
| **4** | 証拠から不足が判定された場合だけ改修 Task を作る | **★立たない ← ★First FAIL** | **★不足の定義を、観測からではなく★先に固定している**: `unresolved_assumptions` に「**Missing metrics are defined as CPU, memory, network I/O, logs, exec, and labels**」。**★実際に観測された値は本文に0**（`qwen36_vllm` / `vllm` / `Up ` すべて0）。**★計画自身は「観測の結果 何が取れていないか」を述べず、★判定をツールに委ねている** |

## 1-1. ★補（★私が足した観測点）— **★私の予告は外れた**
```
★予告: 「本文に `OBS-` または観測の値が1回でも出る」＝★立つ
★実測: ★`OBS-` 本文 0回 ／ ★観測された実値（qwen36_vllm など）0回 ／ `already`・`acquired` 0回
       ★`observation` は3回 出るが、★「observation data を JSON で読む」という★一般語である
∴ ★外れた。★概念としては届いたが、★値も ID も届いていない。
```

---

# 2. ★これが今回いちばん大きい（★前回との差）

| | 前回（ポート） | **★今回（コンテナ）** |
|---|---|---|
| files_expected | `tool.py` | `container_status_checker.py` |
| steps の中身 | **★「/proc/net/tcp 解析、★ss/netstat コマンド実行、socket 接続テスト」** ＝ **★観測能力の作り直し** | **★「観測データを JSON で読む → 期待項目と比べる → 不足を出す」** ＝ **★観測を入力として使う** |
| 受入3 | **×** | **★○** |

> ### **★「見せたら効いた」。★観測をプロンプトに入れた1箇所で、★計画の型が変わった。**
> **★ただし「効いた」のは受入3 までである。★受入4 には届いていない。**

**★言い換えない**: **★2DER が賢くなったのではない。★入力を変えたら出力が変わった。**

---

# 3. ★予告の当否（★外れを先に書く）

| 予告 | 結果 |
|---|---|
| **補**（本文に観測が届く） | **★外れた**（§1-1） |
| 分類は `OBSERVE_CURRENT_STATE` | **★当たった**（★前回 外した点を実測に合わせて直した分） |
| 予告 task_id `TASK-2DER-9A6415BE` | **★当たった**（★投入前に sha1 から計算した値） |
| 受入3 が立つとは**予告しなかった** | **★立った。★予告しなかったのが正しかった**（★予告していたら「当てた」話になっていた） |

---

# 4. ★私の計器の誤り（★本日3回目。★自己申告）
```
★今回の計画は★英語で書かれているのに、★私は最初「観測／既に／取得済／コンテナ／不足」と★日本語で数え、
  ★全部 0回 を得た。★「本文が観測に触れていない」と読むところだった。
★「★全部0 を『きれい』と読まない」（運用方針 §5 の隣接形）で気づき、★英語で測り直した
  → `missing` 11回 ／ `container` 7回 ／ `observation` 3回。★結論が変わった。
★★本日3回目である（①docker ログに UTC を渡して0件 ②検査語にバッククォートが無く受入を「×」 ③今回）
★★3回とも「機械的に照合したつもりで、★照合する側が間違っていた」形である。
```

---

# 5. ★判定案のまとめと、次に直す1件

```
★Last PASS  : ★受入3（重複計画しない）   ← ★前回の First FAIL がここまで動いた
★First FAIL : ★受入4（証拠から不足が判定された場合だけ改修 Task を作る）
★2DER 担当工程数の前回差分: ★0（★今回も IMPL が書いた）
```

> ### **★次の1件（★私の案・★実施しない）: ★観測の「値」をプロンプトに入れる。**
> **★理由**: 今回入ったのは**★観測の存在（id と件数）**までで、**★中身（何が取れて何が取れていないか）が入っていない**
> ∴ **★planner は「不足の定義」を自分で作るしかなく、★それを `unresolved_assumptions` に固定した。**
> **★場所は同じ `_plan_prompt` 1箇所。★既に在る値を渡すだけ。★新しい台帳・API・ID 族を作らない。**

**★正直に併記**: **★これで受入4 が立つとは書かない。** **★値を見せても「不足」を観測から述べないかもしれない。★そのときは「値を見せても効かなかった」と書く。**

---
**決めたこと**: **①受入1・2・★3 が立ち、受入4 が立たない＝止まり所が3→4 へ動いた——`docker`/`ss -`/`nvidia` が本文に0回で、steps が「観測データを JSON で読む」に変わった＝観測能力の作り直しをやめた ②ただし不足の定義を観測からでなく事前固定しており（`Missing metrics are defined as CPU, memory…`）、実際の観測値は本文に0 ∴ 受入4 は立たない ③私の予告「補（本文に観測が届く）」は外れ、計器の誤りは本日3回目（英語の計画を日本語語で数えて全部0を得た）——「全部0 をきれいと読まない」で気づき測り直した。次の1件は同じ `_plan_prompt` に観測の「値」を渡すこと（★立つとは予告しない）。**
