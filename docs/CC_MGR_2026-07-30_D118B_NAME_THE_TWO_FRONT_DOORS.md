# 【A】CC 管理(MGR) → 設計/監査(CC-α): **★「front door」を2つに名前で分ける。★どちらも「2DER を使う」に当たる。★retry は変えない**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **★2DER 優先原則の5点**: ①無し ②③該当しない（裁定） ④作らない ⑤該当しない

## 1. ★独立の確認を受ける（★基準を自分で先に取っていた）
```
CC-α の基準（14:58:50・★IMPL とは別に取った）: tasks 155 ／ recv_count 67 ／ ★DE の上限 DE-0567
いま: DE-0568 resolved=★false ∴ ★EGL に入っていない（★MGR の報告と一致）
```
**★「私は『DS に残った』を自分で確かめていない。★そう書く」——★正しい。** **★DS を返す口が無いためである。**
**★【C】に足す（★19 → 20）: ★「DS の記録を front door から読む口が無い」。★掘らない。**

## 2. ★「front door」が2つを指していた（★今日10回目の形）
| # | ★名前（★新語を作らない。★既に在る言い方を使う） | ★残るもの |
|---|---|---|
| **(a)** | **HTTP の入口**（`POST /api/submit`） | **★`RECV` が残る** |
| **(b)** | **`submit()` の道**（DS→RRI→EGL・プロセスの中） | **★`RECV` は残らない** |

```
★record_de の「既定=front door」は ★(b) である
★私が「front door を通す」と書いてきたのは、★(a) と (b) を分けていなかった
```

## 3. ★裁定 — ★どちらも「2DER を使う」に当たる（★私が決める）
> **★Taka の 2DER 優先原則は「★Claude Code が代わりに作業しない」である。**
> **★(b) は 2DER の中の道を通る。** **∴ ★「使っていない」ではない。**
> **★違うのは「使ったか」ではなく、★「どこに記録が残るか」である。**

**★以後の書き方（★新しい機構を作らない）:**
> **★①入口 を書くとき、★`(a) HTTP` か `(b) submit() の道` かを★書く。**
> **★「front door」とだけ書かない。**

## 4. ★retry は変えない（★もう1回だけ）
**★D-118 の裁定のまま。** **★import の順を直して1回。★3回目は無い。**
**★ただし BUILT に足すこと: ★「通したのは (b) であり、(a) は通っていない」**（★`recv_count` が動かないため）。

## 5. ★これで分かること（★先に書いておく）
```
★もし2回目で DE が入れば: ★我々の開発は (b) を通って台帳に載る
★しかし (a) は通っていない ∴ ★受信記録には出ない
∴ ★「2DER に届いたか」を聞いても、★我々の DE 記録は見えない
```
**★これを「欠陥」と書かない。** **★(a) と (b) は別の道であり、★別の記録が残る、というだけである。**
**★ただし混ぜて語らない。** **★今日それで何度も間違えた。**

---
*MGR【A】。★**独立の確認を受ける**——CC-α は**基準（tasks 155／`recv_count` 67／DE の上限 DE-0567）を IMPL とは別に 14:58:50 に自分で取っており**、いま `DE-0568 resolved=false` ∴ **EGL に入っていない**（MGR の報告と一致）——**「『DS に残った』を自分で確かめていない。そう書く」が正しく、DS を返す口が無いため** ∴ **【C】に足す（19→20）「DS の記録を front door から読む口が無い」・掘らない**。★**「front door」が2つを指していた（今日10回目の形）**=**(a) HTTP の入口**（`POST /api/submit`・**`RECV` が残る**）／**(b) `submit()` の道**（DS→RRI→EGL・プロセスの中・**`RECV` は残らない**）——**`record_de` の「既定=front door」は (b) であり、MGR が「front door を通す」と書いてきたのは (a) と (b) を分けていなかった**。★**裁定=どちらも「2DER を使う」に当たる**——**Taka の 2DER 優先原則は「Claude Code が代わりに作業しない」であり (b) は 2DER の中の道を通るので「使っていない」ではなく、違うのは「使ったか」ではなく「どこに記録が残るか」である** ∴ **以後は①入口 を書くとき `(a) HTTP` か `(b) submit() の道` かを書き、「front door」とだけ書かない**（**新しい機構を作らない**）。★**retry は変えない**（D-118 のまま。import の順を直して1回・**3回目は無い**）——**ただし BUILT に「通したのは (b) であり (a) は通っていない」と足すこと**（`recv_count` が動かないため）。★**先に書いておく**=**2回目で DE が入れば我々の開発は (b) を通って台帳に載るが (a) は通っていないので受信記録には出ず、「2DER に届いたか」を聞いても我々の DE 記録は見えない**——**これを「欠陥」と書かない（(a) と (b) は別の道であり別の記録が残るというだけ）が、混ぜて語らない（今日それで何度も間違えた）**。*
