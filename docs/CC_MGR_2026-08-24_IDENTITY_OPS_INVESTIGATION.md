# 同一性操作32件と新規候補36件の全件調査（★適用0・★名称/意味/authority を自動確定していない）

作成: Claude Code (MGR) / 2026-08-24 / ITEM: `ITEM-2DER-EVO-0100`
上申: `HESC-441033d3d50b` / 親仕様: `ART-dd54fb656c` / 生成器: `ART-b5e6863323`

Taka 裁定 逐語:
「同一性操作32件は自動適用しない。RETIRE / MERGE / SPLIT を別々に扱う。」
「RETIRE候補20件については、現在memberが0または実質消滅しているか、既存割当・履歴参照が残っているかを
 機械で全件調査し、『廃止しても参照が切れない候補』だけを一括裁定可能な一覧にする。」
「MERGE 10件、SPLIT 2件は個別裁定とする。既存axis_id、関係するmembers、既存割当数、統合/分割後の
 参照影響を提示し、Ledger Domain Managerは適用しない。」
「36件の新規科目候補も一括採用しない。既存科目への吸収可能性を先に調査し、
 真に新規科目が必要な候補だけへ減らす。」
「いずれも名称・意味・authorityを自動確定しない。」

## 0. 調査の口（★私の直読ではない）

`twoder/domain_ledger.ledger_identity_report()` を足した。★読むだけ / 適用0 / LLM 0 / 名前を付けない。
General から `to_domain("ledger_identity_report")` で実走した。

**分母**: 同一性操作 32 / tree 詳細 88 / tree カテゴリ 7 / chart 63 / 明細 1,071

「参照が切れない」の定義（★決定論）:
`assigned_ever == 0` **かつ** `effective_now == 0`。
`effective_now` は `request_thread.effective_account_of` と同じ作法（起票時の値に割当を上書き・最後の行が効く）。

## 1. ★上申の件数を訂正する

上申 `HESC-441033d3d50b` に書いた **RETIRE 20 / MERGE 10 / SPLIT 2** は
**遷移中（52→88 の回）の値**だった。★不動点（88→88）で数え直すと:

| op | 上申時 | **★不動点での実測** |
|---|---|---|
| RETIRE_CANDIDATE | 20 | **20** |
| MERGE_ABSORBED_CANDIDATE | 10 | **12**（LCAT 3 / LDET 9） |
| SPLIT_CANDIDATE | 2 | **0** |
| 計 | 32 | **32** |

★SPLIT は不動点では **0件**。★MERGE が 12件（カテゴリ層3件を含む）。

## 2. ★RETIRE 20件 ―― 一括裁定できる候補は **0件**

| | 件数 |
|---|---|
| ★廃止しても参照が切れない | **★0** |
| 参照が残る | **20** |
| members が 0件のもの | **0** |

いま載っている明細の合計 **285件**。最小 2件・最大 33件。全件が `in_chart=True`。

```
LDET-68ad5c78  members 33  いま 33  過去 33  処分 4
LDET-f71e5f63  members 27  いま 25  過去 27  処分 0
LDET-4e3c2ee6  members 26  いま 26  過去 26  処分 0
LDET-dc8bc11f  members 24  いま 25  過去 25  処分 0
LDET-04574589  members 21  いま 21  過去 21  処分 0
…（20件すべて いま≥2）
```

★★**「RETIRE_CANDIDATE」という札が誤解を招く**。これは「使われていない科目」ではない。
★再クラスタで members の過半数が別の塊へ動いた **＝ いま現役で使われている科目** である。
★∴ **一括裁定可能な一覧は空**。廃止すると 285件の割当が tree から引けなくなる。

## 3. ★MERGE 12件 ―― 個別裁定（★適用していない）

| 吸収される axis_id | 吸収先 | この科目 いま/過去/members | 吸収先 いま/members |
|---|---|---|---|
| `LCAT-6417f122` | `LCAT-d23a39ff` | 0 / 0 / 40 | 0 / 214 |
| `LCAT-755e4c72` | `LCAT-9d8487ed` | 0 / 0 / 83 | 0 / 377 |
| `LCAT-a04a25a1` | `LCAT-9d8487ed` | 0 / 0 / 110 | 0 / 377 |
| `LDET-0f65a4f9` | `LDET-d7526ed4` | 1 / 1 / 1 | 26 / 43 |
| `LDET-128c0964` | `LDET-54662029` | 11 / 11 / 11 | 18 / 53 |
| `LDET-669435b8` | `LDET-9a67ce29` | 1 / 1 / 1 | 19 / 28 |
| `LDET-8f887782` | `LDET-54662029` | 13 / 13 / 13 | 18 / 53 |
| `LDET-9fcf3111` | `LDET-be8953eb` | 1 / 1 / 1 | 3 / 27 |
| `LDET-a779c996` | `LDET-54662029` | 4 / 4 / 4 | 18 / 53 |
| `LDET-ab132d04` | `LDET-54662029` | 7 / 7 / 7 | 18 / 53 |
| `LDET-b0150279` | `LDET-e2940095` | 3 / 3 / 3 | 8 / 14 |
| `LDET-d153d542` | `LDET-aeb46ac5` | 4 / 4 / 4 | 14 / 18 |

- ★**参照が切れないもの 3件**（LCAT の3件・`いま 0 / 過去 0`）＝カテゴリ層は割当に使われていない。
- ★**参照が残るもの 9件**（LDET・計 45件の割当）。
- ★`LDET-54662029` が **4件を吸収する側**になっている（11+13+4+7 = 35件の割当が移る）。
- 統合しても**台帳の履歴は消えない**（追記式）＝ **読む側の名寄せが要る**。

## 4. ★SPLIT ―― 不動点では **0件**

遷移中に出た2件は、不動点では消えた（members の散らばりが `SPLIT_MIN_SHARE=0.25` を下回った）。

## 5. ★新規科目候補 36件 → 真に必要なのは **5件**

| 判定 | 件数 | 未計上明細 |
|---|---|---|
| **NEW_LIKELY_NEEDED** | **★5** | **51** |
| PARTIAL_MOSTLY_EXISTING | 3 | 8 |
| **ABSORBABLE_NO_NEW_NEEDED** | **28** | **★0** |

★**28件（77.8%）は1件も新規計上を増やさない** ―― members が全て既存科目に載っている。

**真に必要な5件**:

| axis_id | members | 既に計上 | ★未計上 | 最大既存科目（share） |
|---|---|---|---|---|
| `LDET-5d6ea6cf` | 18 | 1 | **17** | `LDET-a5e121fc` (0.06) |
| `LDET-68d67c42` | 18 | 3 | **15** | `LDET-b654430e` (0.11) |
| `LDET-dd12b59f` | 16 | 3 | **13** | `LDET-2d01f311` (0.19) |
| `LDET-1e6a6ce0` | 3 | 0 | **3** | なし (0.00) |
| `LDET-60107ae8` | 4 | 1 | **3** | `LDET-1213247d` (0.25) |

**PARTIAL 3件**（過半数が既存科目に載っている ∴ 吸収の余地が大きい）:

| axis_id | members | 既に計上 | 未計上 | 最大既存科目（share） |
|---|---|---|---|---|
| `LDET-014d9b4d` | 12 | 11 | 1 | `LDET-e9a6c50a` (**0.92**) |
| `LDET-9445a9c5` | 22 | 18 | 4 | `LDET-f71e5f63` (0.50) |
| `LDET-92ff6ce4` | 6 | 3 | 3 | `LDET-dc8bc11f` (0.50) |

## 6. ★この調査で分かった一番大きなこと

★**再クラスタは新しい領域を発見していない ―― 同じ母集団を切り直しているだけ**。

- RETIRE 20件は「死んだ科目」ではなく **現役の20科目**（285件の割当）
- 新規候補36件のうち **28件は既に計上済みの明細だけを再び切っている**
- 新たに計上できる明細は **59件**（5件 + PARTIAL 3件）

★未割当 **422件** に対して、**36件を全部採用しても 59件（14.0%）しか動かない**。
∴ ★**未割当率を大きく下げる鍵は「科目を増やすこと」ではない**。
★残り 363件が **なぜ tree の corpus に入らないか**（tree の corpus は逐語
「台帳の明細(`list_account_proposals`)」＝**提案がある明細だけ**）が次の本丸である。
★★但し ★これは本調査の範囲外 ∴ **測っていない**。

## 7. ★やっていないこと（隠さない）

- 同一性操作 32件を **1件も適用していない**
- 新規候補 36件を **1件も採用していない**
- **名称・意味・authority を自動確定していない**
- RETIRE の「一括裁定可能な一覧」は **空である**（作れなかったのではなく、★該当が0件だった）
- 未割当 422件の内訳（tree corpus に入らない 363件の理由）は **未測**
