# 【監査＋判定案】**★引用はした。★だが引用は効いていない** — 受入1・2 立つ／3・4 立たない

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 03:2x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **受領**: `CC_IMPL_2026-07-31_D152_PLAN_CITES_OBSERVATION_BUILT.md`
- **2DER 優先原則**: ①読み出しの口のみ ②受入1〜4 を実データで取り直した ③該当なし ④実装しない ⑤該当なし ／ **`:8005` 未使用**

---

# 1. ★受入の判定案（★1条件に1つの印。★まとめない）

| # | 受入（★Taka 逐語） | ★判定案 | ★実データ（★私が front door から引いた） |
|---|---|---|---|
| **1** | `cites_source_ids` が空でない | **★立つ** | **`["OBS-00975","OBS-00976","OBS-00977","OBS-00978"]`** |
| **2** | 対象 Observation の ID を引用する | **★立つ** | **★この task の `egl.source_refs` と★完全一致**（＝★今回の投入で生まれた観測である。★別の投入の id ではない） |
| **3** | **★既に観測済みの能力を、新規作成対象として重複計画しない** | **★立たない** | `files_expected` = **`["tool.py","test_tool.py"]`** ／ `steps[1]` = **「ポート状況取得の基盤ロジック設計（/proc/net/tcp解析、★ss/netstatコマンド実行、socket接続テストの3段階アプローチ）」** ＝ **★たった今 `ss` で観測した能力を、★作り直す計画である** |
| **4** | 証拠から不足が判定された場合だけ改修 Task を作る | **★立たない** | `completion_criteria` は「tool.py がエラーなく実行され…」「設定ファイル欠落時にフォールバック…」、`unresolved_assumptions` は「Linux/Unix 系である」「Python 3.8 以降」——**★観測から「何が取れていないか」を1つも述べていない**（★一般的なツール実装の話に終始） |

> ### **★∴「PLAN が Observation を使って立つ」ことは、★まだ証明されていない。**
> **★引用は★機械的に付いただけである。★計画の中身は前回と同じ型（「ツールを新しく作れ」）のままである。**

**★MGR が「1〜2 は引用したか、3〜4 は引用が効いたか。分けて判定する」と先に決めていたのが効いた。**
**★まとめて1つの印にしていたら、「引用できた」で通っていた。**

---

# 2. ★なぜこうなるか（★構造。★私が SPEC に予告として書いた通り）

```
★`build_planner.py:203`  plan["cites_source_ids"] = provenance.get("egl_source_refs") or []
   → ★provenance に id を入れれば、★中身を見ないまま★欄は埋まる
★`build_planner.py:99-126` _plan_prompt(goal, provenance)
   → ★planner が実際に読むのは ★goal と ★trace_id だけ
   → ★OBS の id も、★観測の中身も、★プロンプトに入っていない
∴ ★「引用した」と「見た」は別である。★今回それが実データで出た。
```

---

# 3. ★予告の当否（★5件中4件 当たり・★1件 外れ。★外れを先に書く）

| # | 予告 | 結果 |
|---|---|---|
| **P-5** | 依頼文は `MODIFY_EXISTING`／`BUILD_CAPABILITY` に分類される。**`OBSERVE_CURRENT_STATE` なら「外れた」** | **★外れた。★実際は `OBSERVE_CURRENT_STATE`** |
| | **★しかも理由も誤っていた** | **★私は「その場合 task が作られず PLAN に届かない」と書いた。★誤り。** `D-144` で★観測経路も task を作るようにした ∴ **★`OBSERVE_CURRENT_STATE` でも task は作られ、PLAN は動いた**。**★自分たちが2時間前に変えた仕様を、★予告に反映していなかった** |
| P-1 | 受入1 は立つ | ★当たった |
| P-2 | 受入2 は立つ | ★当たった |
| **P-3** | **受入3 は立たない見込み**（プロンプトに観測が入らないため） | **★当たった。★外れてほしかった方である** |
| P-4 | 受入4 は判定できない見込み | ★当たった（★判定材料が無く不成立） |

**★task_id の予告 `TASK-2DER-444E7599` も当たった**（★私が投入前に sha1 から計算した値）。

---

# 4. ★判定案（★確定は MGR）

```
★Last PASS  : ★受入2（Observation の ID を引用する）
★First FAIL : ★受入3（既に観測済みの能力を重複計画しない）
★2DER 担当工程数の前回差分: ★0（★この修正は IMPL が書いた）
```
**★「概ね引用できた」と書かない。** **★1・2 が立っても、★3 が立たない以上、★証明は出ていない。**

---

# 5. ★次に直す1件（★私の案。★1件だけ。★実施しない）

> ### **★planner のプロンプトに、★引用した Observation の中身を渡す（`_plan_prompt` 1箇所）。**

| | |
|---|---|
| **★場所** | **`twoder/build_planner.py:99-126`**（★`_plan_prompt`）。**★1箇所** |
| **★渡すもの** | **★既に provenance に在る `egl_source_refs` と、★その観測の中身**（★`MEASURED_STATE` 等・★既に記録に在る値） |
| **★作らないもの** | **新しい ID 族／新しい台帳／新しい API**（★渡すだけ） |
| **★線を越えない** | **★「観測を Task の下流へ移す」ではない**（★観測は今までどおり submit で走る。★その結果を planner に見せるだけ） |
| **★受入は変えない** | **★Taka の4条件のまま。★特に3（重複計画しない）が★本番である** |

**★正直に併記**: **★これで受入3 が立つとは書かない。** **★見せても同じ計画を書くかもしれない。★そのときは「見せても効かない」が結果である。**

---

# 6. ★IMPL の作法で良かった点（★成果判定に効くものだけ）
```
★保留を受けて手を止め、★コード変更0・5 repo 未変更で「戻すものが無い」状態にした
★★私の SPEC の前提（つなぎ目はハッシュ）を弱める点（★16桁 vs 64桁）を自分から出した
   → ★私が確かめて潰せた（★突き合わせ可能）。★捨てられていたら Ledger 再開時に踏んでいた
```

---
**決めたこと**: **①受入1・2 は立ち（`cites_source_ids` に今回の投入で生まれた `OBS-00975`〜`00978` が入り、task の `egl.source_refs` と完全一致）、★受入3・4 は立たない（`files_expected` は `tool.py` で、steps は `ss/netstat` を実行する能力を作り直す計画。観測から「何が取れていないか」を1つも述べていない）∴ 「PLAN が Observation を使って立つ」ことは まだ証明されていない ②理由は構造——`cites_source_ids` は provenance から機械的に埋まるが、planner が読むプロンプトには goal と `trace_id` しか入らない ③予告は5件中4件 当たり、★P-5 は外れ、しかも理由も誤っていた（`OBSERVE_CURRENT_STATE` でも task は作られる＝自分たちが2時間前に変えた仕様を予告に反映していなかった）。次の1件は `_plan_prompt` に観測の中身を渡すこと（★それで立つとは書かない）。**
