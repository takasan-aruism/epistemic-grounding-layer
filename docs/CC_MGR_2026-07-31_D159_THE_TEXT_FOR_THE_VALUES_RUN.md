# 【D-159】次の確認に使う依頼文 — **★これで進め**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / 発: MGR / 2026-07-31 09:1x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** ／ **★新しい名前で置いた**（D-158 §2-2。★既存文書を差し替えない）

## 1. ★依頼文（★1文字も変えずに投入する）

```
現在メモリを多く使っているプロセスの状況を確認し、確認できない項目があれば、それを取得できるようにする方法を検討してください。
```

## 2. ★根拠
```
★(1) ★前2回と★同じ形（「現在の◯◯の状況を確認し、確認できない項目があれば、…検討してください。」）
      ∴ ★受入4 の結果を「文の形が違ったから」で説明できない
★(2) ★前2回と違う内容 ∴ ★新しい task_id になる（★同じ文は届かない・実証済）
★(3) ★`top_memory_processes`（`ps -eo pid,rss,comm`）は ★_CATALOG に既に在る ∴ ★能力を足していない
★(4) ★観測が先に走る ∴ ★引用すべき値が★同じ依頼の中で生まれる
★(5) ★「確認できない項目があれば」＝ ★受入4 を直接 試す
★(6) ★架空ではない（★このマシンのメモリは実際に我々の関心事である）
★(7) ★GPU / port / container の語を含まない ／ ★裸の指示語なし ／ ★1行・改行なし
```

## 3. ★今回いちばん見たい1点
```
★`measured_state` の★値がプロンプトに入ったとき、★計画は「不足」を★観測から述べるか。
★前回は「CPU・メモリ・ネットワーク…」と★一般論から先に決めていた。
★★今回それが「★ps -eo pid,rss,comm では ◯◯ が取れていない」の形に変われば★受入4 が立つ。
★★変わらなければ「★値を見せても効かなかった」が結果である。★それも答えである。
```

## 4. ★変わらない
```
★手順: ①修正 → ②再起動（全件記録）→ ③機械抽出（字数・sha1）→ ④task_id を投入前に予告
       → ⑤投入1回 → ⑥run_next 1回 → ⑦PLAN で停止
★受入は Taka の4条件のまま ／ ★GENERATE を押さない ／ ★担当工程数の差分は 0 の見込み
```

---
**決めたこと**: **①依頼文を固定した（前2回と同じ形・違う内容・既存 `_CATALOG` の項目・観測が先に走る） ②今回の焦点は「値を見せたとき、不足を観測から述べるか」——変わらなければ「値を見せても効かなかった」が結果である。**
