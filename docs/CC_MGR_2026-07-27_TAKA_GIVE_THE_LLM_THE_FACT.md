# CC 管理(MGR): **★Taka 指摘 — 選択のヒントになる事実を LLM に渡していない。渡して試せ**（HANDOFF）

- `BUILD_ROLE: 参照`（実装源でない。**DESIGN が最小の SPEC を1本書く**）
- 宛: DESIGN/AUDIT(CC-α) / IMPL(coder) / 写: Taka / 発: MGR / 2026-07-27 04:20 / TYPE=HANDOFF

## 0. Taka 直接発話（逐語）
> **「おや、私が説明したHBB30系というのは、そもそも問いそのものを正しいか疑う系。問い自体が怪しいことをLLMが情報として持っていれば間違えないし、問い自体を疑うことも問題なくできるはずなんだがな。取り込んだと思ったけど取り込んでないの？ つまり、LLMが選択をする際に選択のヒントになる情報を渡せば、LLMはそのどれかを選ぶこと自体は問題ない、はずなんだけど？ 試しに設計と実装に頼んで簡単なテストをさせてみればわかるよ」**

## 1. MGR の理解（DESIGN が検証すること）
**我々がやったのは「選択肢を取り上げる」ことだけで、「なぜそれが成立しないのか」という事実を LLM に一度も渡していない。**
- **【監査:MGR】私が読んだ範囲では** `s_intent_role_split_d2p2.py::_rel_prompt` / `_sel_prompt` が prompt に入れているのは **`fx["request"]` と `fx["context"]` だけ**。`context` が空なら**その行を出さない**——**「文脈は存在しない」という事実を明示的に伝えてはいない。**
- **∴ モデルから見えているのは「文脈の話が書かれていない」だけであって、「文脈が無い」ではない。** 情報が無いまま選ばされている。
- **D-5 の結果はこれで説明がつく**: 「文脈で絞る」を消したら「観点を並べる」へ移った＝**止まる理由を持っていないから、別の走り出す道を選んだ。**

## 2. ★取り込めていない疑い（確認すること・私は断定しない）
**P4（存在の接地）と P2（束縛先）は既に決定論で判定を出している。しかしその判定結果が意図調べの prompt に渡っているかを、私は確認していない。**
- 例: `rri/rri/context_binding.py::bind_context` は **実在 ref のみ**で `UNRESOLVED / LOW / MEDIUM / HIGH` を返す。**この結果が選択側に渡っているか。**
- 例: `CLAIM_STATUS` 登記・存在照合（`GROUNDED / NOT_FOUND / UNKNOWN / DECLARED_UNVERIFIED`）は **preflight gate 側で出口を決めるのに使っている**が、**意図調べの選択のヒントとしては渡していないのではないか。**
- **＝ HBB-30 系（問いそのものを疑う）を作ったのに、疑うための材料を選択の場に届けていない可能性。** **これを最初に確認すること。**

## 3. 依頼（★最小のテスト。Taka「簡単なテストをさせてみればわかる」）
### D-7（DESIGN）— 最小 SPEC を1本
**アームは3つだけ。fixture は既存。新しい台帳を作らない。**
| arm | prompt に渡すもの |
|---|---|
| **A（現行）** | 依頼のみ（`context` 空なら何も書かない） |
| **B（Build 4）** | 同上 + **成立しない戦略をメニューから外す** |
| **★C（本命）** | **メニューは7つのまま。代わりに決定論が出した事実を明示的に渡す。** 例: **「直前の文脈は存在しない（記録に無い）」「『それ』の指す先は記録から特定できない」** |

- **C の文言は決定論の判定結果をそのまま書く。** **我々が誘導する言葉を足さない**（「だから聞き返せ」等は書かない。**書いたら答えを教えたことになる**）。
- **可能なら D（B+C 併用）も。** ただし **C 単独の効果が分かる形にすること**（まとめて出すと帰属できない）。
- **測るもの**: **止まる/動くの取り違え率**（主）+ 別解も可の一致率 + 厳密一致率 + **per-fixture**。**IP1/IP2 が `INTENT_PROBE` に来るか**が核心。
- **計器規律**: paired・分布・run 間ノイズ ≈0.10・**上がらなければ上がらなかったと書く。**

### I-4（IMPL）
**D-7 の SPEC 1本から作る。** その前に **§2 の確認（決定論の判定が意図調べに渡っているか）を事実として報告すること**（コードを読むだけ・LLM 不使用）。

## 4. 位置づけ
- **これは新しい機構ではない。既に作った判定の出力を、既にある選択の場に渡すだけ。** 「精度が動くまで新しい門を作らない」に反しない。
- **Taka の原則の続き**: 存在/非存在を決定論で確定する（済）→ **その結果を分岐と生成に渡す（ここが抜けていた）。**

---
*MGR。Taka 指摘=選択のヒントになる事実を LLM に渡していない。我々は選択肢を取り上げただけで「なぜ成立しないか」を一度も伝えていない(prompt に入るのは依頼と context だけ・空なら行を出さないだけ)。D-5 の結果はこれで説明がつく(止まる理由を持たないから別の走り出す道へ移った)。★確認事項=P4/P2 の決定論判定が意図調べの prompt に渡っているか(preflight gate 側でしか使っていない疑い)。依頼=arm C(メニューは7つのまま・決定論の事実だけを渡す・誘導語を足さない)を最小で試す。核心は IP1/IP2 が INTENT_PROBE に来るか。*
