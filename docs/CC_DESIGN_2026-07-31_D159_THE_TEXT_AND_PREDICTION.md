# 【追加指示】`D-158` SPEC の確認に使う依頼文 — **★予告つき。★新しい名前で出す**

- `BUILD_ROLE: 参照` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-07-31 09:2x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）** ／ **実装源は `CC_DESIGN_2026-07-31_D158_BUILD_SPEC_PASS_THE_VALUES.md`（`ART-c0fd9dbc6a`）。★本文書はその §4 の「依頼文」を埋めるだけである。**
- **★同名で差し替えていない**（`D-158` §2-2）。**★前の SPEC は1文字も触っていない。**

---

## 1. ★依頼文（★`D-159` §で MGR が固定。★1文字も変えずに投入する）

```
現在メモリを多く使っているプロセスの状況を確認し、確認できない項目があれば、それを取得できるようにする方法を検討してください。
```

**★`CC_MGR_2026-07-31_D159_THE_TEXT_FOR_THE_VALUES_RUN.md` から機械で抜くこと**（★打ち直さない）。

## 2. ★設計が投入前に計算した値（★一致を確認してから投入する）
```
★字数 63 ／ ★sha1 5abba4a1821d574f3574fdbfb6b62f5e417e59ff
★★予告 task_id: TASK-2DER-5ABBA4A1   ← ★違う id が出たら「外れた」と書く
```

## 3. ★設計の予告（★`D-158` §5 に足す。★投入前に固定）
| # | 予告 | 確信 |
|---|---|---|
| **Q-1** | **分類は `OBSERVE_CURRENT_STATE`** | 高（★前2回の同型文が両方これだった） |
| **Q-2** | **補（★実際に観測された値の文字列が計画本文に出る）は★立つ** | **★中**（★前回 同じ形の予告を外している。★概念は届いたが値は届かなかった） |
| **Q-3** | **★受入4 が立つとは予告しない** | — |
| **Q-4** | **★観測は `top_memory_processes`（`ps`）が主に効く**（★`_CATALOG` は毎回 全件を要求するので4種とも走る） | 中 |

**★立たなかった場合の言い方を先に決める**（★言い換えを封じる）:
```
★受入4 が立たなければ → ★「★値を見せても効かなかった」と書く。★それも結果である。
★補が立たなければ    → ★「★値も届かなかった」と書く。★私の予告が2回続けて外れたことになる。★そう書く。
```

## 4. ★測り方（★`D-158` §3-1 の再掲ではなく、★今回の具体）
```
★計画本文に出るかを見る値の例（★今回の観測で出る見込みのもの）:
   ★プロセス名（例: VLLM::Worker_TP など）／★rss の数値／★pid
★★英語と日本語の両方で数える（★計画が英語で書かれることが在る）
★★「全部0」を「きれい」と読まない  ★★部分一致で判定せず、★キーを列挙して目視する
```

---
**決めたこと**: **①依頼文は MGR が固定した「現在メモリを多く使っているプロセスの状況を確認し、確認できない項目があれば、それを取得できるようにする方法を検討してください。」で、設計が事前に計算した値は 63字 / sha1 `5abba4a1…` / 予告 task_id `TASK-2DER-5ABBA4A1` ②予告は Q-1〜Q-4 で、受入4 が立つとは予告しない——立たなければ「値を見せても効かなかった」、補が外れれば「値も届かなかった（予告2回連続 外れ）」と書く ③本文書は新しい名前で出し、前の SPEC を同名で差し替えていない。**
