# 【調査回答 / D-196】**★保留の状態は在る。★前で弾く仕組みも在る** — ★足りないのは★条件1つ

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-08-01 00:4x / TYPE=FINDING
- **開発者規律 確認済（版: v1.0 / 2026-07-31）** ／ **受領**: `D-196`（Taka 問い・3値で返す・コード0行）
- **この .md がまだ .md である理由**: 回答を台帳へ書く口が front door に無いため（C-1）
- **★コードを1行も変えていない ／ ★増やした管理対象 0 ／ ★新しい状態語を作っていない**

---

# 1. ★(a)「作業を保持したまま、足りないものを名指しして待つ」→ **★在るが配線されていない**

**★3つの部品に分けて測った（★総件数・確認件数・打ち切り無し）**

| 部品 | 3値 | 逐語の出所 |
|---|---|---|
| **①作業を保持したまま止まる状態** | **★在って動く** | `dev-workcell/dw/workcell.py:41` 逐語 `REWORK_ESCALATION_THRESHOLD = 2 # rework がこの回数を超えたら JUDGE_REQUIRED へ強制昇格` ／ **実測**: REGENERATE が2回 走り `JUDGE_REQUIRED` で停止・**同一 task・events は保持** |
| **②足りないものを名指しする欄** | **★在るが、task を作る経路では★書かれない** | 書き手は **1箇所のみ**（`twoder/submit.py:484` `_rec("NEXT_INFORMATION_NEED", ans.get("open_gaps") or [])`）＝ **`DECIDE / OTHER` 分岐＝★task を作らない側**。読み手も **1箇所のみ**（`twoder/webui.py:135` の表示） |
| **③そこから再開する経路** | **★在るが、別入口である** | `submit.py:422-424` 逐語 `elif rt.get("request_type") == "RESUME_PRIOR" and resume_target:` → `_rec("SELECTED_ACQUISITION_METHOD", "RESUME")`。**＝新しい依頼文を投げて過去の仕事を指す形**。**`JUDGE_REQUIRED` から `run_next` で再開する経路ではない**（**実測**: `next_legal_operation` は `BLOCKED` を返す） |

## 1-1. ★★∴ 何と何が繋がっていないか（★1行）
> ### **★`JUDGE_REQUIRED`（止まった状態）と `next_information_need`（何が足りないかを書く欄）が★繋がっていない。**
```
★止まる仕組みは在る。★足りない物を書く欄も在る。★★止まった時にその欄へ書く手が★無い。
∴ ★★「作り直し」ではなく「保留」にする道具は ★既に揃っている。★配線が1本 欠けているだけである。
★★★`completion_blockers`（`workcell.py:282` 逐語）は
   `if state in ("COMPLETE","BLOCKED","JUDGE_REQUIRED"): b.append(f"state={state} は COMPLETE 遷移不可")`
   ＝ ★`JUDGE_REQUIRED` は ★★「終わった」ではなく「★COMPLETE させない」状態である。★保留そのものである。
```

## 1-2. ★Taka の問いへの直接の答え
```
★「そういうステータスにできんの？」→ ★★もう在る（`JUDGE_REQUIRED`）。★作り直しは要らない。
★「仕組み作り直し必要だろうけど」→ ★★不要である。★欄も状態も在る。
★★★ただし ★今のままだと ★止まった理由が★画面に出ない（②が書かれないため）
   ∴ ★人から見ると「★黙って死んだ」ように見える。★これが「馬鹿すぎる」の実体だと思われる。
```

---

# 2. ★(b) 契約検査を生成の前に出せるか → **★もう前に在る。★条件が1つ足りないだけ**

```
★★`twoder/submit.py:470` 逐語（★投入時に走っている）:
   `_contract = contract_seal.extract_contract(raw_input)   # 契約封印(決定論・LLM 非経由)。壊れた契約は ValueError で fail-closed`
★★★∴ ★契約検査は ★★生成の前どころか ★★投入の時点で走っている。★作らない。
```

| 依頼文の状態 | いま起きること | 判定 |
|---|---|---|
| 契約が**壊れている**（片方のマーカーだけ／`END` 欠落／`def test_` が0行） | **★投入時に `ValueError`** で弾かれる（`contract_seal.py:44,50,56,66-67`） | **★在って動く** |
| 契約が**そもそも無い**（マーカー0件） | **★`None` を返して★通る**（同 `:41-42` 逐語 `if not has_sk and not has_it: return None`）→ `create_task(..., contract=None)` → **★3周してから** `generate_via_runner.py:180/185` が `SPEC_INCOMPLETE_NO_CONTRACT` | **★素通り** |

## 2-1. ★★足りない条件は1つ（★ただし一律にしてはいけない）
```
★足りないの: 「★task を作る分岐で ★contract が None なら、★その場で止めて理由を返す」——★これだけ。
★★★★一律に None を拒んではいけない:
   ★`OBSERVE_CURRENT_STATE`（★観測の依頼）に契約は要らない。★一律必須にすると ★観測が全部 弾かれる。
   ∴ ★必須にしてよいのは ★`BUILD_CAPABILITY` / `MODIFY_EXISTING`（★`submit.py:410` の task を作る分岐）だけ。
★★∴ ★これは ★バグではなく ★★「契約を要する種別」と「要らない種別」を分ける条件が★書かれていない、である。
```

## 2-2. ★MGR の読み（`D-196` §2「順序の問題」）— **★半分 当たり、半分 違う**
```
★当たり: ★「足りない物を先に言わずに作らせている」は ★そのとおり（★3周してから出た）。
★★違う: ★「先に言う仕組みが無い」のではない。★★投入時に走っている。★条件が1つ抜けているだけ。
★★★∴ ★「順序を作り直す」ではなく「★既に在る検査に条件を1つ足す」である。★規模が2桁 違う。
```

---

# 3. ★次の1件（★私は決めない。★材料を出す。★ACTIVE は S-3 のまま）

| | 案 | 効くもの | 大きさ |
|---|---|---|---|
| **(1)** | **`submit.py` の task を作る分岐で `contract is None` なら止めて理由を返す** | **★3周の空回りが消える**（★S-3 が同じ穴で回るのを防ぐ） | **★条件1つ** |
| **(2)** | **`JUDGE_REQUIRED` へ入る時に `next_information_need` を書く** | **★止まった理由が画面に出る**（★Taka の「馬鹿すぎる」に直接 当たる） | **★配線1本** |
| (3) | 両方 | 上記の両方 | ★規律9 に触れる（★一度に2つ） |

**★私の見立て**: **★(1) を先に。** 理由: **★`D-196` §4 が「S-3 がまた同じ穴で3周したら (b) を前倒しする」と決めている** ∴ **★S-3 の結果を待たずに (1) を入れれば、★その3周そのものが起きない。**
**★(2) は (1) の直後。** **★どちらも「作る」ではなく「繋ぐ」である。★新しい状態語・新しい台帳・新しい計器は 0。**
**★決めるのは あなたです。**

---

# 4. ★やっていないこと
```
★コードを1行も変えていない ／ ★新しい状態語を作っていない ／ ★台帳・計器を作っていない
★S-3 に手を出していない（★ACTIVE は S-3 のまま。★IMPL の手番）
★`D-191`（gate の意図調査）は止めたまま ／ ★gate にパッチを当てていない（★`D-195` で取り下げ済）
★★止めたものはそのまま: 案C の測定 ／ 受入3 の採点 ／ Ledger ／ 図 ／ (c) patch
```

---
**決めたこと**: **①(a) は「在るが配線されていない」——止まった状態 `JUDGE_REQUIRED` は在って動き（rework 2回超で強制昇格・同一 task・events 保持）、足りない物を書く欄 `next_information_need` も在るが、書き手は `submit.py` の task を作らない分岐1箇所だけで、読み手は表示1箇所だけ。再開の入口 `RESUME_PRIOR` も在るが、それは新しい依頼文であって `JUDGE_REQUIRED` からの再開ではない（実測で `BLOCKED`）②繋がっていないのは「止まった状態」と「何が足りないかを書く欄」の1本。∴ Taka の言う保留は作り直し不要で、既に在る。ただし止まった理由が画面に出ないので黙って死んだように見える ③(b) は「もう前に在る」——`submit.py:470` が投入時に `extract_contract` を呼んでおり、壊れた契約は `ValueError` で弾かれる。素通りするのは「契約がそもそも無い」場合だけ ④足りないのは条件1つだが、一律に必須化してはいけない（観測の依頼が全部 弾かれる）。必須にしてよいのは task を作る分岐だけ ⑤MGR の「順序の問題」は半分 当たり——先に言う仕組みは在り、条件が1つ抜けているだけなので、規模が2桁 違う ⑥次の1件は (1) 契約 None を task 分岐で止める、を推す。S-3 の結果を待たずに入れれば3周が起きない。(2) は直後。どちらも「作る」ではなく「繋ぐ」で、新しい状態語・台帳・計器は 0。決めるのは MGR。**
