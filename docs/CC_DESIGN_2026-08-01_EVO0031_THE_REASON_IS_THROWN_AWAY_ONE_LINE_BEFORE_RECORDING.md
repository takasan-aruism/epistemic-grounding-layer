# 【調査回答 / `EVO-0031`】**★理由は作られている。★記録の1行手前で★捨てられている**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-08-01 19:4x / TYPE=FINDING
- **開発者規律 確認済（版: v1.1 / 2026-08-01）** ／ **受領**: `D-212`（3値で返す・直し方を今は書かない）
- **★コードを1行も変えていない** ／ **★front door へ投入していない** ／ **★台帳を直読していない**

---

# 1. ★★3値の答え — **★「在るが未配線」ではない。★★「作って、捨てている」**

```
★★実行器の理由は ★★★作られている（`twoder/generate_via_runner.py:213-216` 逐語）:
   # G-25: reason の文字列は変えない。隣に exit と stdout 末尾を載せ、0件(5)と失敗(1)を読む側が区別できるようにする。
   return {"ok": False, "run_id": run_id, "artifact_sha256": artifact_sha256, "reason": reason,
           "contract_source": contract_source,
           ★"runner_exit": res.get("runner_exit"), ★"runner_stdout_tail": res.get("runner_stdout_tail")}

★★★★しかし ★★受け取る側が ★捨てている（`twoder/webui.py:327-329` 逐語）:
   "test_result": {"status": "PASSED" if gr.get("ok") else "FAILED",
                   "ok": bool(gr.get("ok")), "reason": gr.get("reason"),
                   "artifact_sha256": gr.get("artifact_sha256")},
   ★★★＝ ★★4欄しか採らない。★`runner_exit` も `runner_stdout_tail` も ★★★1文字も渡していない。
★★★★★★∴ ★★★この直後の `W.record_generate(tid, wr, TS)`（★同 `:331`）に ★理由は★入らない。
   ★★★★★★★★台帳に ★最初から ★書かれていない。★★front door の問題ではない。
```

## 1-1. ★★∴ 正確な言い方
```
★「★front door から読めない」ではない（★私が `EVO-0031` の前回で書いた言い方は ★★不正確だった。★訂正する）
★★正しくは: ★★「★実行器は理由を返しているが、★記録する1行手前で ★4欄に絞られ、★捨てられている」
★★★★∴ ★front door に欄を足しても ★★出ない。★★★捨てているのは ★もっと手前である。
```

---

# 2. ★★これは規律 第5章に直接 当たる（★MGR の `D-212` §2(b) の読みは正しい）

```
★開発者規律 第5章（★MGR が引いた逐語）:「★失敗を正常に見える別の結果に置き換えない・★理由を捨てない」
★★★本件は ★★まさに ★「★理由を捨てた」である。
★★★★しかも ★★過去に一度 直された跡が在る: ★`G-25` の注記（逐語）
   「★正規化がここで `res["test"]` を捨てていたため、★『テストが落ちた(exit 1)』と
     『1件も集まらなかった(exit 5)』が★区別できず、★どちらも RUNNER_FAILED になっていた」
★★★★★★∴ ★★★同じ形が ★★1つ下流で ★もう一度 起きている。
   ★`G-25` は ★`generate_via_runner` の中を直した。★★`webui.py` の受け取り側は ★直っていない。
```

---

# 3. ★同型の数え直し（★`D-212` §2(b) は「7件目」と書いたが ★形が違う）

| # | 形 |
|---|---|
| ①〜⑥ | **★片側だけ在る**（読めるが書けない／書けるが読めない） |
| **⑦（本件）** | **★★両側 在る。★★渡す所で ★捨てている** |

```
★★∴ ★私は「★同型7件目」とは書かない。★★別の形である。
★★★★★★そして ★★この形の方が ★見つけにくい: ★★どちらの側を見ても「★在る」ので、
   ★★★★★繋いで初めて ★★「★間で消えている」と分かる。
```

---

# 4. ★直し方は書かない（★`D-212` §2 を守る）— **★ただし1つだけ測れることを書く**

```
★★`D-212` は「★読めたら逐語で持ち帰り、★そこで初めて直す対象が決まる」と定めている。
★★★いま ★読めない ∴ ★★私は ★`cwd` の件（★絶対パス／ファイル名だけ）を ★★候補のまま置く。★断定しない。
★★★★★ただし ★★次に ★理由を通す変更を入れたら、★★★過去の task は★測り直せない
   （★台帳に書かれていないので ★遡って読めない）。★★∴ ★測るのは ★新しい走行になる。
   ★★★★★★これは ★先に書いておく（★「前の task で確かめる」と言えなくなるため）。
```

---

# 5. ★私が前回 書いた言い方の訂正（★自分から）
```
★前回（`EVO-0031` 調査）で私は「★front door から読めない」「★繋がっていないのは
   ★`generate_via_runner` の `runner_exit` ↔ ★front door」と書いた。
★★★実際は ★★`generate_via_runner` → ★`webui.py` の ★受け取りで ★捨てられている。
   ＝ ★★繋がっていない区間が ★★1つ手前である。
★★★★私は ★返り値が在ることを確かめて ★★「front door に無い」を見て ★★間を見なかった。
   ★★★★★これは ★本日 私が繰り返した「★片側だけ見て全体を言う」と ★同じ形である。★★5回目。
```

---

# 6. ★やっていないこと
```
★コードを1行も変えていない ／ ★front door へ投入していない ／ ★直し方を書いていない
★`cwd` の件を ★断定していない（★候補のまま）／ ★commit していない
★★`run_runner` の中（★実行器そのもの）は ★読んでいない【★未確認】——★捨てている場所が
   ★手前で見つかったので ★そこで止めた（★★「★見つかったら止まる」を守った）
```

---
**決めたこと**: **①3値の答えは「在るが未配線」ではない。実行器の理由（`runner_exit` / `runner_stdout_tail`）は `generate_via_runner.py:213-216` で確かに作られて返っているが、受け取る `webui.py:327-329` が4欄しか採らず1文字も渡していない ∴ その直後の `record_generate` に理由は入らず、台帳に最初から書かれていない。front door の問題ではない ②∴ 正確には「front door から読めない」ではなく「記録する1行手前で捨てられている」。front door に欄を足しても出ない ③これは開発者規律 第5章「理由を捨てない」に直接 当たる。しかも `G-25` として過去に一度 直された跡があり、同じ形が1つ下流でもう一度 起きている——`G-25` は `generate_via_runner` の中を直したが、`webui.py` の受け取り側は直っていない ④同型7件目とは書かない。①〜⑥は「片側だけ在る」だが、本件は「両側 在るのに渡す所で捨てている」で形が違い、どちらの側を見ても「在る」ので繋いで初めて分かる分 見つけにくい ⑤直し方は書かない（`D-212` を守る）。`cwd` の件は候補のまま。ただし理由を通す変更を入れても過去の task は台帳に書かれていないので遡って測れず、測るのは新しい走行になることを先に書く ⑥私が前回「front door から読めない」「繋がっていないのは `generate_via_runner` ↔ front door」と書いたのは不正確で、実際は1つ手前で捨てられている。返り値が在ることと front door に無いことを見て、間を見なかった——本日5回目の「片側だけ見て全体を言う」である ⑦`run_runner` の中は読んでいない【未確認】。捨てている場所が手前で見つかったのでそこで止めた。**
