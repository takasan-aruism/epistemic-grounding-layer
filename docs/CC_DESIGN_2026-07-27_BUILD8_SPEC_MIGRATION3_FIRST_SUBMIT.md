# BUILD SPEC — Build 8: MIGRATION-3 第1号 — 指示を front door に投入し、返った routing に従う

- **`BUILD_ROLE: ★実装源`**（本 build task の唯一の実装源。**IMPL はこの1本だけから作る**）
- **宛: IMPL（coder）** / 写: MGR / Taka
- 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=BUILD_SPEC / v1.0
- **運用方針 確認済（版: v1.1）**
- 権限: `CC_MGR_2026-07-27_MIGRATION3_DECISIONS.md`（裁定①②③ + 追加B-4）
- 設計本体: `CC_DESIGN_2026-07-27_MIGRATION3_DESIGN_v1.md`（`BUILD_ROLE: 参照`）

---

## 0. ★これは何に対して発火するのか。その入力は実際にここへ来るのか（運用方針 §5-5）
| | |
|---|---|
| **発火対象** | **我々（Claude 群）の作業指示**——今回は `MIGRATION-PLAN-CHECK` の1件のみ |
| **到達するか** | **【監査:CC-α】来る。** `twoder/submit.py:489` に CLI エントリが LIVE で在る（実読済）。**投入口は既に在り、我々が使っていなかっただけ** |

## 0-1. ★これは精度の改善ではない。機能追加でもない
**運用の変更である。** **新しいコードをほぼ書かない。** **「2DER が良くなった」と書かない。**

---

## 1. 何をするか（thin・1件だけ）
**`MIGRATION-PLAN-CHECK` の作業指示を、file に直接書く代わりに front door へ投入し、返ってきた routing に従う。**

### 1-1. 投入する指示文（**DESIGN が確定。IMPL は変えない**）
```
宛: 設計/監査(CC-α)
MIGRATION-PLAN-CHECK: twoder/webui.py:315 と twoder/build_planner.py:301 の record_plan について、
その書き込みの上流に依頼が存在するかをコードを読んで確認せよ。LLM は使わない。
出すもの: 各呼出の上流を辿った結果（file:line の連なり）と、依頼に相当する入力が在るか無いか。
判定・提案はしない。事実のみ。
```
- **★「宛: 設計/監査」を先頭に含める**（MGR 追加 B-4: front door は運び方であって宛先ではない）。
- **文面はこのまま投入する。** 通りやすくするための書き換えをしない（**書き換えたら「front door が我々の指示を扱えるか」の観測にならない**）。

### 1-2. 投入方法（裁定③）
```
cd /home/takasan && python3 twoder/submit.py "<上記の指示文>"
```
- **CLI を使う**（auth 不要・webui と同じ `submit()` を通る）。**webui のトークンを用意しない。**
- **1回だけ投入する。** 複数回投げない（台帳を汚さない）。

---

## 2. ★予想を先に書く（裁定② — 外れたら外れたと書く）
**DESIGN の予想（実測前に固定・後から変えない）:**
| 項目 | 予想 |
|---|---|
| `RRI_REQUEST_TYPE.request_type` | **`OBSERVE_CURRENT_STATE`**（「確認せよ」「読んで」なので観測系と読まれる可能性が高い。`BUILD_CAPABILITY` ではないと予想する） |
| `RRI_PREFLIGHT.triggered` | **False**（指示語も未接地の過去参照も無い。`record_plan` はファイル名として実在する） |
| `DW_TASK_ID` | **返らない可能性が高い**（`OBSERVE_CURRENT_STATE` は ACQUISITION へ行くため） |
| `INTENT_STRATEGY.strategy` | **`DIRECT`** |

**★予想が外れることは失敗ではない。** **外れた事実こそが「front door は我々の指示を想定していない」ことの証拠になる。**

---

## 3. ★「従う」— 何をして何をしないか（設計 §2-2・MGR 支持済）
| 返ってきたもの | すること |
|---|---|
| `DW_TASK_ID` が返った | **その task を作業単位とする。** 自分で別の単位を切らない |
| `RRI_PREFLIGHT` が `blocks_dw_escalation=True` | **進まない。** Claude の判断で押し通さない |
| `DISPATCH_RESULT` が示す先 | **それと違うことをしない** |
| **routing が我々の意図と食い違った** | **★書き換えない。「食い違った」と記録して設計/監査へ上げる。** |
| **`DW_TASK_ID` が返らなかった** | **強制的に task を作らない。**「返らなかった」と記録して上げる |

**上書きするなら、回数と理由を残す。** **上書きが多いこと自体は失敗ではない。隠すことが失敗である。**

---

## 4. 受入
1. **投入した指示文（逐語）と、返ってきた `TRACE` を全文記録する。**
2. **§2 の予想と実際を並べた表を出す。** **外れた項目に「外れた」と書く。**
3. **`RRI_REQUEST_TYPE` / `RRI_PREFLIGHT` / `INTENT_STRATEGY` / `DISPATCH_RESULT` / `DW_TASK_ID` を全部書く。**
4. **`origin=MACHINE_SUBMIT` が記録されていること**（corpus 汚染の処理・新しい除外規則を作らない）。
5. **本番コードを変更しないこと。** 投入するだけ。**変更が必要になったら止めて設計へ上げる。**
6. **1回だけ投入する。** 繰り返さない（台帳を汚さない）。
7. **★運用方針 §4-13**: 本件は**1回しか投入しない**ので、**「1回しか見ていない」と BUILT に明記する。** **1回の結果で「front door は我々の指示を扱える／扱えない」と断定しない。**
8. **観測を書き、判定・評価・提案をしない**（責務区分）。**判定は設計/監査。**
9. **commit しない**（MGR）。
10. **BUILT 冒頭に「運用方針 確認済（版: v1.1）」と受領文書一覧。**

---

## 4-1. ★追加条件（MGR・v1.1 追記）— **届いたのか、自分で読みに行っただけなのか**
**BUILT に、次を事実として1行書くこと（判定はしない・判定は設計/監査）:**

> **投入した指示は、front door を経て設計/監査に「届いた」のか。**
> **それとも、投入した後に我々が自分で `TRACE` を読みに行っただけなのか。**

- **★ここが移行条件2（2DER 側が routing を決めている）の分かれ目である。**
- **「投入した」だけで、その後の伝達が従来どおり我々の手読みなら、運び方は変わっていない。**
- **それは失敗ではない。** ただし **そう書かないと「移行が進んだ」と誤読される。**
- **書き方の例（どちらかを事実として選ぶ）**:
  - 「投入後、`TRACE` を IMPL が自分で読み、本 BUILT に転記した。設計/監査へ自動で届く経路は通っていない。」
  - 「投入後、〇〇（経路名）を経て設計/監査へ届いた。」
- **【設計:CC-α】現時点の私の予想は前者である**（自動で届く経路は存在しないと見ている）。**予想であり、事実は IMPL が書く。**

## 5. 完了条件（移行条件3・先に固定）
**「本線作業が1つ完走した」と言ってよいのは、次が全部そろった時のみ:**
1. 指示文が `submit()` を通った（`TRACE` が記録に残っている）
2. **返された routing に従って作業した**（従った証跡が BUILT/FINDING に書かれている）
3. 設計→実装→監査→commit が終わっている
4. **食い違いがあった場合、それが潰されずに記録されている**

**1つでも欠けたら「完走した」と言わない。** **特に4を欠くと「従った」の証拠にならない。**

**本 SPEC は 1〜2 までを担当する。** 3〜4 はこの後の作業で満たす。

---

## 6. 位置づけ（緩めない）
- **1件通っただけでは「移行した」と言わない。** **移行3条件のうち条件1（直叩きが閉じている）と条件2（2DER が routing を決めている）は依然として未達。**
- **本 SPEC が示せるのは「我々の指示が front door を通った」ことと「2DER が何と判定したか」だけである。**

---
*BUILD SPEC v1.0（★実装源）。MIGRATION-3 第1号=MIGRATION-PLAN-CHECK の指示を CLI から front door に投入し、返った routing に従う。★投入口は既存（作らない・我々が使っていなかっただけ）。指示文は DESIGN が確定し「宛: 設計/監査」を含める（front door は運び方であって宛先ではない）。★予想を先に固定（OBSERVE_CURRENT_STATE / preflight 不発火 / DW_TASK_ID は返らない / DIRECT）——外れたら外れたと書く。従う=食い違いを潰さず記録して上げる。1回だけ投入し「1回しか見ていない」と明記（運用方針 v1.1 §4-13）。1件通っただけでは「移行した」と言わない。*
