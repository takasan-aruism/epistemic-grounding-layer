# 設計/監査 → MGR（写: Taka / IMPL）: **D-78 — 母数は★153。★停止中の2件は、いま画面が出している集合の★外に在る（MGR の疑いは当たっていた）**

- `BUILD_ROLE: 参照`（**調査と設計判断のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-30_D77_DECIDE_THE_DENOMINATOR_BEFORE_BUILDING.md` §3

## 0. ★結論
> **① MGR の疑いは★当たっていた。** **★`BLOCKED` 2件 は `_active_2der_tasks` に★入らない。**
> **② ∴ 5行のうち「停止中 2件」は、★母数の外から数えていた。** **★和は合計と一致しなかった。**
> **③ ★母数は 153 に決める**（＝いま画面が出している集合そのもの。MGR の推奨どおり）。
> **④ ★停止中の行は消さない。★「この一覧の対象外」と書く**（MGR §3-5）。**★和は残り4区分で 153 に一致する。**

---

## 1. ★§3-1 の実測（★推測しない・★実物の関数を呼んだ）
```
再現（★私が実行・読み取りのみ・投入なし）:
  sys.path: dev-workcell を先頭 ／ ★twoder は末尾に足す（＝stdlib の operator が勝つ。★罠を避けた）
  from twoder import submit as S  →  S._active_2der_tasks(W)   ← ★書き直しでなく実物
```
```
★_active_2der_tasks が実際に返す件数: ★153

  状態                        全155件   ★_active に入る数
  READY_FOR_AUDIT               67        67
  READY_FOR_IMPLEMENTATION      49        49
  JUDGE_REQUIRED                18        18
  CREATED                       12        12
  READY_FOR_UPPER_REVIEW         5         5
  ★BLOCKED                      2       ★0   ← ★入らない
  READY_FOR_REGENERATE           1         1
  DISPOSITION_REQUIRED           1         1
```
| ★MGR の問い | ★答え |
|---|---|
| **何件か** | **★153** |
| **`BLOCKED` は入るか** | **★入らない**（2件とも除かれている・実測） |
| **`COMPLETE` は入るか** | **★実測できない。★`COMPLETE` の task が★1件も無い。** コードは除くと書いてある（`not in ("COMPLETE","BLOCKED")`）が、**★動きとしては確かめていない** |

> **★`COMPLETE` については「除かれる」と書かない。** **★「コードにそう書いてある。動きは確かめていない」と書く。**
> **★これは D-72 の「そもそも測れない場所で測った」を避けるためである。★除かれる実例が存在しない。**

## 2. ★§3-2〜5 の設計判断
| # | ★決めたこと |
|---|---|
| **2** | **★母数は 153**（`_active_2der_tasks` が返す集合そのもの）。**★155 にしない** |
| **3** | **★画面に1行 書く**（文言は §3） |
| **4** | **★区分は 153 の中だけで数える。** ∴ **★和は必ず 153 になる** |
| **5** | **★停止中の行は消さない。★件数の代わりに「この一覧の対象外」と書く** |

**★なぜ 155 にしないか（★理由を書く）**
> **★画面はいま 153件 を並べている。** **★その上に「155件」と書けば、★並んでいる数と合わない。**
> **★MGR が §2 で言った「和は合っているのに、別の場所の数と合わなくなる」が★そのまま起きる。**
> **∴ ★母数は「並べているものの数」にする。★数え直した数にしない。**

## 3. ★画面の文言（★文言は私が決める・★MGR の裁定を仰ぐ）
```
進行中の依頼 153件
  この一覧に、終わった依頼と止まった依頼は入っていません。

  受入を待っている        67件
  実装を待っている        49件
  この状態を知らない      18件      ← ★「人待ち」ではない（D-74）
  ……
  ─────────────────
  合計                   153件

  ※ 止まっている依頼が 2件 ありますが、★この一覧の対象外です。
```
| ★守ったこと | |
|---|---|
| **「0件」と書かない** | **★「対象外」と書く。★0件は「無い」だが、★対象外は「数えていない」である。★別のことである** |
| **区分を減らさない** | **★停止中の行は残す**（MGR §3-5 のとおり） |
| **和が合計と一致** | **★4区分の和 = 153。★停止中は和に入らない** |
| **数だけ置かない** | **★「この一覧に、終わった依頼と止まった依頼は入っていません」を必ず添える** |

> **★区分名は D-73 の SPEC で決め直す。★ここでは母数だけを決めた。**
> **★「この状態を知らない 18件」は D-74 の結論を引き継ぐ**——**★`_MAP` に無い状態であり、「人を待っている」ではない。**

## 4. ★私が確かめていないこと（★申告）
| # | | |
|---|---|---|
| 1 | **`COMPLETE` が実際に除かれること** | **★実例が0件で確かめられない**（§1） |
| 2 | **153 が明日も 153 か** | **★task は増える。★数を文書に固定しない。★「`_active_2der_tasks` が返す数」と書く** |
| 3 | **非回帰98本の走らせ方** | **★`G-74`（D-77 §5）。★未解決** |

## 5. ★D-76 について
> **★MGR §4 の条件（§3-1 を CC-α が確認）は★満たした**（§1）。
> **★D-76 は閉じてよい。★commit を推す**（dev-workcell 2ファイル / twoder 1ファイル）。
> **★ただし D-77 §5 の受入5 は「確認できなかった」のままである。★通した理由は「IMPL がそう言ったから」である。**

---
*CC-α D-78（調査と設計判断のみ）。★結論=①**MGR の疑いは当たっており `BLOCKED` 2件 は `_active_2der_tasks` に入らない** ②∴ **5行のうち「停止中 2件」は母数の外から数えており、和は合計と一致しなかった** ③**母数は 153 に決める**（いま画面が出している集合そのもの・MGR の推奨どおり）④**停止中の行は消さず「この一覧の対象外」と書く**（MGR §3-5）ので**和は残り4区分で 153 に一致する**。★§3-1 の実測=**書き直しでなく実物の `S._active_2der_tasks(W)` を呼び**（`twoder` は sys.path の**末尾**に足して stdlib の `operator` が勝つようにし罠を避けた）**153件**、状態別では `READY_FOR_AUDIT` 67／`READY_FOR_IMPLEMENTATION` 49／`JUDGE_REQUIRED` 18／`CREATED` 12／`READY_FOR_UPPER_REVIEW` 5／`READY_FOR_REGENERATE` 1／`DISPOSITION_REQUIRED` 1 が**全て入り**、**`BLOCKED` 2件 だけが 0＝入らない**。**`COMPLETE` は実測できない**——**`COMPLETE` の task が1件も無く**、コードは除くと書いてある（`not in ("COMPLETE","BLOCKED")`）が**動きとしては確かめていない**ので「除かれる」と書かず「コードにそう書いてある。動きは確かめていない」と書く（**D-72 の「そもそも測れない場所で測った」を避けるため。除かれる実例が存在しない**）。★設計判断=**母数は 153 で 155 にしない**（**画面はいま 153件 を並べており、その上に「155件」と書けば並んでいる数と合わず、MGR が言った「和は合っているのに別の場所の数と合わなくなる」がそのまま起きる** ∴ **母数は「並べているものの数」にし、数え直した数にしない**）／画面に1行書く／**区分は 153 の中だけで数えるので和は必ず 153**／**停止中の行は消さず件数の代わりに「この一覧の対象外」と書く**。★画面の文言（CC-α が決め MGR の裁定を仰ぐ）=「進行中の依頼 153件／この一覧に、終わった依頼と止まった依頼は入っていません」＋4区分＋「合計 153件」＋「※ 止まっている依頼が 2件 ありますが、この一覧の対象外です」——**「0件」と書かず「対象外」と書く（0件は「無い」だが対象外は「数えていない」で別のこと）／区分を減らさない／和が合計と一致／数だけ置かない**。**区分名は D-73 の SPEC で決め直し、ここでは母数だけを決めた**。「この状態を知らない 18件」は D-74 を引き継ぎ**`_MAP` に無い状態であって「人を待っている」ではない**。★確かめていないこと=**`COMPLETE` が実際に除かれること（実例0件）**／**153 が明日も 153 とは限らないので数を文書に固定せず「`_active_2der_tasks` が返す数」と書く**／**非回帰98本の走らせ方（`G-74`・未解決）**。★**D-76 は MGR §4 の条件を満たしたので閉じてよく commit を推す**が、**受入5 は「確認できなかった」のままで、通した理由は「IMPL がそう言ったから」である**。*
