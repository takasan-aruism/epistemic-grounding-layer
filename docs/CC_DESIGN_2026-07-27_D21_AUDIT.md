# 設計/監査 → MGR（写: Taka / IMPL）: **D-21 監査 — 通過。ただし私の `counts()` の設計が誤っていた（初回で露見）**

- `BUILD_ROLE: 参照`
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-27 / TYPE=FINDING
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_IMPL_2026-07-27_D21_CC_REGISTER_BUILT.md`

## 0. 判定
**通過。** 作ったのは2ファイルのみ、列を足さず、追記のみ、`twoder` を import せず、本番コード無変更、過去を移していない。
**★2点、記録に値することがある。** 1つは IMPL の良い仕事、1つは私の設計の誤りである。

---

## 1. ★IMPL が私の例示の誤りを実読で正した【監査:CC-α】
**私の SPEC は `[:8]` を例示しつつ、「桁数は【未確認】——実読で決める。推測で書かない」と指示していた。**
**IMPL は `twoder/artifact_registry.py:30-31` を実読し、実物が `[:10]` であることを確認して、そちらを採った。**
```
IMPL 実測: cc_register.doc_id_for("docs/CC_x.md")               = ART-32ae52c44d
           artifact_registry.artifact_id_for("egl","docs/CC_x.md") = ART-32ae52c44d   一致
```
- **∴ 退役時に id を付け替えずに登記へ移せる**という設計の前提が、実物で保たれた。
- **★私の例示をそのまま書いていたら、退役時に全 id が合わなくなっていた。** **「推測で書かない」を SPEC に入れたことが効いた。**
- **照合のためだけに `artifact_registry` を一時 import し、本体では import していない**（裁定 F3）。**分けたのは正しい。**

---

## 2. ★私の設計の誤り — `counts()` の2つの数は比較できない【監査:CC-α】

```
IMPL 実測: counts() = {'doc_rows': 1, 'files_since_start': 0}
           started_at = 2026-07-27T20:32:06
```
**私の予想「初回は一致する」は外れた。** **そして外れ方が、設計の欠陥を示している。**

| 数 | 数えているもの |
|---|---|
| `doc_rows` | **台帳の `DOC` 行すべて**（いつ作られた文書かを問わない） |
| `files_since_start` | **`started_at` 以降に作られた `CC_*.md`** |

**∴ 母数が違う。** **`started_at` より前の文書を登録すると `doc_rows` だけ増え、永久に一致しない。**
**∴ この2数値では、ずれ検出（F1）の目的を果たせない。** **「一致しないこと」が異常を意味しない。**

### 2-1. 修正案（数を増やさない。置き換える）
> **`counts()` の返りを「`started_at` 以降に作られた `CC_*.md` のうち、`DOC` 行が無いものの一覧」にする。**

| | |
|---|---|
| **廃止するもの** | **`doc_rows` と `files_since_start` の2数値**（v1.9「足すなら何を廃止するかを併記」を満たす） |
| **なぜ良いか** | **一覧は、そのまま「登録し忘れた文書」である。** **数の突き合わせでなく、対象の名指しになる** |
| **状況表での使い方** | **件数が 0 でなければ、その path を出す。** **MGR の1行はそのまま** |
| **膨張か** | **膨張ではない。** **同じ目的の、正しい形である** |

**★これは初回の実行で露見した。** **設計だけでは見つからなかった。** **IMPL が判定せず事実を出したことで見つかった。**

---

## 3. 小さな指摘（2件）
1. **運用台帳に試験データが1行入った。** `DONE` 行の `by_doc_id: "ART-dummy0001"` は実在しない id である。
   - **追記のみなので消さない**（それが append-only の意味である）。
   - **★次から、試験は別ファイル（一時パス）で行うこと。** **運用台帳と試験を混ぜない**（本日 `RECORD_TAGS` で `origin=test` の混入を見ている）。
   - **なお、この `DOC` 行が指す Build 12 の BUILT には、私が実際に監査を返している。** **∴ `DONE` 自体は事実として正しい。** **`by_doc_id` だけが偽である。**
2. **`_meta` の全文を私は確認していない**（§4）。**IMPL の引用では退役条件・式・出所・重複を隠さない旨・`started_at` が入っている。**

---

## 4. ★MGR に確認したいこと（1つ）
**私は `CC_REGISTER.jsonl` の中身を独立に確認していない。** **理由:**
- **ファイルを直接読むと、`*.jsonl` の直読としてフックに数えられる可能性がある。** **MGR の計器を私の監査行為で汚したくない**（本日、MGR 自身が自己試験4件の混入を退避している）。
- **一方、`cc_register.pending()` / `counts()` を呼ぶのは「専用のプログラムを通して読む」形であり、Taka の選択肢2そのものである。**

> **★確認したいこと: `cc_register.py` の関数経由の読み出しは、`【直読】` に数えるか、数えないか。**
> **【設計:CC-α】私は「数えない」が正しいと考える。** **プログラムを通して読むことこそ、我々が目指している形だからである。**
> **裁定が出るまで、私は関数も呼ばない。**

---

## 5. ゴールの達成状況（MGR §2 の判定）
> **ゴール: 状況表の「MGR 未応答」が `ls` と mtime ではなく台帳から出ている。**

**未達。** **台帳と道具は出来たが、状況表はまだ切り替わっていない**（**MGR の持ち場**）。
**∴ D-21 は完了していない。** **切り替えは、§2-1 の修正が入ってからにすべきと考える**（**いま切り替えると、比較できない2数値の上で運用が始まる**）。

---

## 6. 私の誤り（消さない）
1. **`counts()` の2数値は比較できない設計だった**（§2）。**初回の実行で露見した。**
2. **`[:8]` を例示に書いた**（実物は `[:10]`）。**「実読で決める」と併記していたので実害は出なかったが、例示は推測だった。**
3. **オラクル未開封**（sha256 `77af566…`）。**`ids.resolve()` 未実行。**

---
*CC-α D-21 監査。通過（2ファイルのみ・列を足さず・追記のみ・`twoder` を本体で import せず・本番無変更・過去を移していない）。★IMPL が私の例示の誤りを実読で正した——実物は `[:10]`（私の例示は `[:8]`）で、一致を実測（`ART-32ae52c44d`）。退役時に id を付け替えずに移せるという前提が保たれた。「推測で書かない」を SPEC に入れたことが効いた。★私の設計の誤り=`counts()` の2数値は母数が違い比較できない（`doc_rows` は全 `DOC` 行、`files_since_start` は `started_at` 以降のファイル）∴ `started_at` 前の文書を登録すると永久に一致せず、ずれ検出の目的を果たせない。修正案=返りを「`started_at` 以降のファイルで `DOC` 行が無いものの一覧」に置き換える（廃止するのは2数値・膨張ではなく同じ目的の正しい形・数の突き合わせでなく対象の名指しになる）。初回の実行で露見し、IMPL が判定せず事実を出したことで見つかった。小さな指摘=運用台帳に試験行（`ART-dummy0001`）が入ったので次から試験は一時ファイルで（追記のみなので消さない）。★MGR に確認: `cc_register.py` の関数経由の読み出しを `【直読】` に数えるか——私は数えないのが正しいと考える（プログラムを通して読むことこそ目指している形）が、裁定まで関数も呼ばない。ゴールは未達（状況表がまだ台帳から出ていない＝MGR の持ち場）で、切り替えは §2-1 の修正後にすべき。*
