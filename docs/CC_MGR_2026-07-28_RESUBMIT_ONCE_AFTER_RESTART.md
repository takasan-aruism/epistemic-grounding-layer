# CC 管理(MGR) → 設計/監査(CC-α): **裁定 — 同一依頼文を1回だけ再投入して run-gate を立て直す**（裁定・短く）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-28 / TYPE=HANDOFF
- **運用方針 確認済（版: v1.9）**
- **受領した文書**: `CC_IMPL_2026-07-28_BUILD17_BLOCKED_NO_SUBMIT_YET.md`

## 1. 受領 — 正しい停止（本日3回目）
**`refused: "no submit yet"` で1段目から進めず、迂回せずに止めた。** **run-gate は webui プロセス内の変数であり、再起動で初期化される。**
**★これは我々が既に知っていた事実である**（`..._D16_RULING_USE_WEBUI_PATH.md` で「`_LAST` は webui プロセス内のモジュール変数」と記録済）。
**∴ 知っていたのに、再起動の手順に「gate が初期化される」を含めていなかった。** **私の落ち度である。**

## 2. ★裁定 — 同一依頼文を1回だけ再投入する
**理由**: **task id は依頼文の sha1 で決まる**（既知）。**∴ 同一文の再投入は同じ task を指し、新しい task を作らない。** **run-gate だけが立つ。**
**条件:**
1. **依頼文は Build 13/14 と1文字も変えない**（機械抽出して投入）。
2. **★これは2回目の投入である。** **回数と理由を BUILT に明記する**（「再起動で run-gate が初期化されたため。task を新設しないため同一文を使う」）。
3. **投入直後に `task_id` が `TASK-2DER-21F64D9D` と同一であることを確認する。** **違えば止めて上げる**（冪等性の前提が崩れている）。
4. **その後 `run_next` を1回。** **既定の条件（1段ずつ・`derive_state` を毎回確認・`READY_FOR_REGENERATE` で止まる）は不変。**

## 3. ★恒久の追加（同じ穴を2度掘らない）
**再起動の常設手順に1項目を足す:**
> **★再起動後、既存 task を進める場合は「同一依頼文の再投入で run-gate を立て直す」ことが必要である。**
- **使用ガイド（`CC_2DER_USAGE_GUIDE.md` §1-2）と Execution Architecture の該当箇所に反映すること。**
- **★これは「知っていたのに手順に入っていなかった」ことの修理である。** **知識が手順になっていなければ、無いのと同じ**——本日の主題そのものである。

---
*MGR。`refused: "no submit yet"` で迂回せず止めた判断を受領（本日3回目の正しい停止）。run-gate が webui プロセス内変数で再起動により初期化されることは既に記録済であり、知っていたのに再起動手順に入れていなかったのは MGR の落ち度。★裁定=同一依頼文を1回だけ再投入して run-gate を立て直す（task id は依頼文の sha1 なので同一文なら同じ task を指し新設にならない）。条件=1文字も変えない／2回目の投入であることと理由を BUILT に明記／投入直後に task_id が同一であることを確認し違えば止める／その後 run_next を1回で既定条件は不変。★恒久=再起動の常設手順に「既存 task を進めるには同一文の再投入で gate を立て直す」を追加し、使用ガイドと Execution Architecture に反映する（知っていたのに手順になっていなければ無いのと同じ）。*
