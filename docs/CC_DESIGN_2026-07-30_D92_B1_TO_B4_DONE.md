# 設計/監査 → MGR（写: Taka / IMPL）: **D-92 — B-1〜B-4 実行。★未実行の裁定は3件（1件は私が見落としていた）／★35件は34件だった／★入口の総数は88件（本番16件）**

- `BUILD_ROLE: 参照`（**測定と登記訂正のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-30_D91_B_MEASURED_ZERO_DONE_AND_MY_RULINGS_SAT_UNEXECUTED.md` §5

## 0. ★2DER 優先原則の5点
| ① 入口 | **`GET /api/tasks` / `/api/state` / `/api/claude_packet` / `/api/resolve`**（★B-2 の別経路判定に使った） |
|---|---|
| ② 何を試したか | **34件それぞれについて、front door の別の口から同じものが取れるかを★8状態 × 4口で引いた** |
| ③ **できなかったこと** | **★値の同一性は確かめられない**（名前が一致しても同じ値とは限らない）／**★`RAW_INPUT` など25件はどの口にも出ない** |
| ④ 何を実装するか | **★何も実装しない** |
| ⑤ | **★該当しない**（実装していない） |

---

## 1. 【B-1】★裁定済みで未実行 — ★3件（★件数と id だけ）
| # | 裁定 | 対象 |
|---|---|---|
| 1 | **D-60 裁定①** | `G-50` |
| 2 | **D-60 裁定②** | `G-46` |
| **3** | **★D-61 §4** | **`G-31` `G-37` `G-41` `G-42` `G-44` `G-45` `G-46` `G-48` `G-50` `G-55` `G-57` `G-58` `G-61` `G-69`（14件の選別）** |

```
★走査: CC_MGR_2026-07-29_*.md ＋ CC_MGR_2026-07-30_*.md = ★75件・打ち切り無し
★指示らしい行に Gap id が出た文書から候補を機械的に抽出 → 23 gap
★そのうち実行文書が見つからないもの = ★3件の裁定
```
> **★3件目（D-61 §4）は、★MGR が挙げた2件に入っていなかった。** **★私も気づいていなかった。**
> **★内容は「14件のうち本当に MGR の裁定を待っているものはどれかを、総数・確認件数・打ち切り無しで書き、機械が読める印を付ける」。**

## 2. 【B-2】★`G-46` の「35件」— ★いまは34件。★1件は読めるようになっていた
```
★同じ方法で数え直した（打ち切り無し）:
  placed  = submit.py の _rec("KEY", …) 全キー ＋ ETRACE_RUN_ID  → 57件
  exposed = webui.py の tr.get("KEY") / tr["KEY"]                → うち23件
  ★読めない = ★34件
★2026-07-28 の35件との差 = ★ETRACE_RUN_ID 1件だけ（★読めるようになった）
```
> **★数が減った理由を「たぶん直ったから」と書かない。★差分を取って1件を名指しした。**
> **★それは Taka の残件②そのものである。**

### 2-1. ★別経路の有無（★34件・D-60 裁定②）
```
★測り方: 8つの状態それぞれ1件ずつ task を選び、
        /api/state ・ /api/claude_packet ・ /api/resolve から取れる欄名を★全部 集めて和集合を作り、
        34件のキー名と照合した（★打ち切り無し）
```
| | 件数 | 内訳 |
|---|---|---|
| **★別経路が在る** | **★9件** | `ACTOR_ROLE` `DS_THREAD_ID` `RRI_INPUT_REF` `RRI_INTENT_ID` `RRI_REQUEST_ID` `RRI_RESEARCH_SIGNAL_ID` `RRI_RESIDUAL` `SENIOR_REVIEW_CANDIDATE` `TRACE_ID` |
| **★別経路が見つからない** | **★25件** | `ADMISSION_LOOP_TRACE` `CANDIDATE_DW_TASK_ID` `CANDIDATE_VALIDATION` `DISPATCH_PROVENANCE` `DISPATCH_RESULT` `DS_EVENT_ID` `DS_THREAD_UPDATE` `DW_EXPERIMENT_GATE` `EGL_ADMISSION_RESULT` `EGL_FORWARD_ADMISSION` `EGL_FORWARD_REJECTED_CLAIMS` `EGL_OBSERVATION_INGEST` `EGL_QUERY` `EXPERIMENT_CANDIDATE` `GUARD_OPEN_GAPS` `INTENT_STRATEGY` `LEDGER_ENTRY_ID` `RAW_INPUT` `RESEARCH_ACQUISITION_RESULT` `RRI_ADMISSION_CLASSIFICATION` `RRI_FORMAL_VALIDATION` `RRI_PREFLIGHT` `RRI_PREFLIGHT_DECISION_ID` `RRI_RESEARCH_SIGNAL` `RUNTIME_INSPECTION_REQUEST_REF` |

**★最初 私は「別経路が在る＝4件」と出した。★測り直して9件になった。**
```
1回目: TASK-2DER-C032596E（★CREATED）1件だけで測った → 4件
2回目: ★8状態それぞれで測った                        → ★9件
```
> **★`CREATED` の task では、下流の欄は空である。** **★出せない場所で測っていた。**
> **★今日ずっと言ってきた「その計器はその答えを出せる場所か」を、★私がまた踏んだ。**
> **★1回目の4件を消さずに残す。**

### 2-2. ★この判定の限界（★先に書く）
| # | |
|---|---|
| 1 | **★名前一致で判定した。★値が同じとは確かめていない**（`TRACE_ID` が両方に在っても同じ値とは言えない） |
| 2 | **★8状態 × 各1件しか見ていない。** 同じ状態の別の task では違うかもしれない |
| 3 | **★`/api/control` `/api/pending_approvals` は見ていない**（★口を全部 試していない） |

> **∴ ★「25件は取れない」と書かない。★「見つからない」と書く**（`G-32`）。

## 3. 【B-3】★`G-50` の述語を1つ書いて1回走査
```
★述語（★1つ・決定論）:
  「.py を★構文木で解析し、関数呼び出しの名前が submit または create_task である Call ノード」
  → ★コメント・文字列・埋め込み JS・def 行は原理的に入らない
★走査: 全5repo・.py 全件・打ち切り無し・構文解析できなかったファイル ★0件
★総数: ★88件
```
| 置き場所（★同じ述語・同じ1回の走査を分けただけ） | 件数 |
|---|---|
| テスト（`test_*` / `regression/`） | 59 |
| **★本番コード** | **★16** |
| 実験・資料（`experiments/` / `docs/`） | 13 |

**★本番コード16件（全件）**
```
dev-workcell/dw/workflow.py:37          create_task   ← ★13番目の入口として登記されたもの
dev-workcell/run_esde_task.py:167       create_task
dev-workcell/run_rri_task.py:167        create_task
egl/structure/de_submit_route.py:46     submit
egl/structure/s2_extract.py:144         submit
egl/structure/s_de_route_equiv.py:107   submit
twoder/counterfactual_runner.py:48      submit
twoder/experiment_candidate.py:116      create_task
twoder/live_worker_runtime.py:197       submit
twoder/runtime_supervisor.py:222        submit
twoder/select_and_create.py:80          create_task
twoder/select_and_create.py:150         create_task
twoder/submit.py:452                    create_task
twoder/submit.py:505                    submit
twoder/tools/codegen_run_fn.py:70       submit
twoder/webui.py:601                     submit
```
> **★最初 私は grep で書いて21件を得た。★コメントと埋め込み JS を数えていた。** **★構文木で書き直した。**
> **★述語を1回だけ変えた。★理由: 最初の述語は「呼び出し」を数えていなかった**（D-60 の「述語を変えたときだけ数え直し、理由を書く」に従う）。
> **★14番目を探しに行っていない。** **★これで `G-50` は「総数を記録した」状態である。★CLOSED にするかは MGR。**

## 4. 【B-4】★`G-76` の登記を D-60 に合わせた（★訂正済）
| | |
|---|---|
| **私が付けていた** | `DEFERRED`（根拠: D-45 §1-3「作らない」） |
| **★訂正後** | **`CLOSED`**（根拠: **D-60「⑦独立検算＝済（D-59）」**。★D-59 は D-45 の翌日であり、後の判定を採る） |
| **★新しい判断をしていない** | **★既に在る裁定に合わせただけである** |
| **★note に残したこと** | 定義の中身は D-59「検算とは『同じ答えが出るか』ではない。**誤り方が違う経路を通ったか**である」／**★『正式な定義文書』という体裁の文書は無く、1本の裁定文書である ∴「文書が在る」と書かない** |

```
★資料: JSON 76 / MD 76 / ★食い違い なし
```

## 5. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **B-1 の走査の取りこぼし** | **★「指示らしい行」の判定に私の言葉選び（6語）が入っている。★語を変えれば件数が変わりうる** |
| 2 | **B-2 の値の同一性** | **★名前一致だけ。★25件を「取れない」と書いていない** |
| 3 | **B-3 の述語の妥当性** | **★「呼び出し」を入口とみなす述語である。★別の述語なら総数は変わる** |
| 4 | **B-3 の16件が全部 入口か** | **★1件も中身を読んでいない。★数えただけである** |

> **★4が重い。** **★私は「本番16件」と出したが、★それが16個の入口だとは言っていない。**
> **★D-60 が求めたのは「述語で1回走査して総数を書く」であり、★中身の判定は求められていない。**

---
*CC-α D-92（測定と登記訂正のみ）。★**【B-1】裁定済みで未実行は3件**=D-60 裁定①(`G-50`)／D-60 裁定②(`G-46`)／**★D-61 §4（`G-31` `G-37` `G-41` `G-42` `G-44` `G-45` `G-46` `G-48` `G-50` `G-55` `G-57` `G-58` `G-61` `G-69` の14件の選別＋機械が読める印）**——**走査は本日分 MGR 文書75件・打ち切り無しで、指示らしい行に Gap id が出た文書から23 gap を機械抽出し実行文書が無いものを取った**。**3件目は MGR が挙げた2件に入っておらず CC-α も気づいていなかった**。★**【B-2】`G-46` の「35件」はいま34件**——同じ方法で数え直すと placed 57／exposed 23／**読めない34**で、**2026-07-28 の35件との差は `ETRACE_RUN_ID` 1件だけ（★読めるようになった＝Taka の残件②そのもの）**——**「たぶん直ったから」と書かず差分を取って1件を名指しした**。★**別経路の有無（34件）**=**8つの状態それぞれ1件ずつ task を選び `/api/state`・`/api/claude_packet`・`/api/resolve` から取れる欄名の和集合と照合** → **別経路が在る9件**（`ACTOR_ROLE` `DS_THREAD_ID` `RRI_INPUT_REF` `RRI_INTENT_ID` `RRI_REQUEST_ID` `RRI_RESEARCH_SIGNAL_ID` `RRI_RESIDUAL` `SENIOR_REVIEW_CANDIDATE` `TRACE_ID`）／**見つからない25件**（`RAW_INPUT` `INTENT_STRATEGY` `RRI_PREFLIGHT` `EGL_FORWARD_ADMISSION` ほか）——**★最初 CREATED の task 1件だけで測って「4件」と出し、8状態で測り直して9件になった。CREATED では下流の欄が空であり、出せない場所で測っていた＝今日ずっと言ってきた「その計器はその答えを出せる場所か」を CC-α がまた踏んだ（1回目の4件を消さずに残す）**。**限界=名前一致で判定し値の同一性は確かめていない／8状態×各1件しか見ていない／`/api/control` `/api/pending_approvals` は見ていない** ∴ **「25件は取れない」と書かず「見つからない」と書く**。★**【B-3】`G-50` の述語**=**「.py を構文木で解析し、呼び出し名が `submit` / `create_task` である Call ノード」**（コメント・文字列・埋め込み JS・def 行は原理的に入らない）で**全5repo・.py 全件・打ち切り無し・構文解析失敗0件・総数88件**、**同じ走査を置き場所で分けるとテスト59／★本番16／実験・資料13**で本番16件を全件 列挙（`dw/workflow.py:37` は13番目の入口として登記されたもの）——**最初 grep で21件を得たがコメントと埋め込み JS を数えていたので構文木に書き直し、述語を1回だけ変えた（理由: 最初の述語は「呼び出し」を数えていなかった）**。**14番目を探しに行っていない**ので `G-50` は「総数を記録した」状態であり **CLOSED にするかは MGR**。★**【B-4】`G-76` を D-60 に合わせて `DEFERRED` → `CLOSED` に訂正**（**新しい判断をせず既に在る裁定に合わせただけ**）、note に**定義の中身は D-59「検算とは『同じ答えが出るか』ではなく『誤り方が違う経路を通ったか』」**と**「正式な定義文書」という体裁の文書は無く1本の裁定文書である ∴「文書が在る」と書かない**を残した。**資料は JSON 76 / MD 76 / 食い違いなし**。★確かめていないこと=**B-1 の走査は「指示らしい行」の判定に CC-α の語選び6語が入っており語を変えれば件数が変わりうる**／B-2 は名前一致だけ／**B-3 の述語は「呼び出し」を入口とみなすもので別の述語なら総数が変わる**／**B-3 の16件は1件も中身を読んでおらず数えただけで、「本番16件」を「16個の入口」とは言っていない（D-60 が求めたのは述語で1回走査して総数を書くことであり中身の判定は求められていない）**。*
