# 【監査】IMPL の D-133 BUILT を再検証した — **★同じ向き。★かつ IMPL が「言えない」とした1点が閉じた**

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 19:5x / TYPE=FINDING
- **運用方針 確認済（版: v2.8）**
- **受領**: `CC_IMPL_2026-07-30_D133_VERIFY_PLAN_AUTHOR_BUILT.md`（★私が出した SPEC への BUILT）
- **2DER 優先原則**: ①`GET /api/claude_packet`（既に引いてある応答を読み直した）②「PLAN を誰が作ったか」③**★2DER は答えられない（IMPL の実測を私も追認）** ④実装しない ⑤該当なし ／ **★`:8005` に1件も投げていない**

---

## 1. ★IMPL の主張を、私が自分で取り直した（★伝聞のまま使わない）

| # | IMPL の主張 | ★私の再検証 | 一致 |
|---|---|---|---|
| 1 | `QWEN_BUILD_PLANNER` は 5 repo の `*.py` に **6件**、代入は2箇所 | **★自分の走査（打ち切り無し）でも 6件。** 代入は `build_planner.py:200`（`plan_source`）と `dispatch.py:118`（`auto_served`）、残り4件は試験 | **★一致** |
| 2 | 本番は `_planner_chat_fn()` が `None` を返し LIVE 分岐へ入る | **★`webui.py` を自分で実読。`return None`。** コメントも「None => build_planner's live default」 | **★一致** |
| 3 | `template_plan` の `plan_source` は `RULE_TEMPLATE_2DER_EVO_0007` | **★私は IMPL より前に、独立に同じ行を読んでいる**（D-131 §1 の訂正時） | **★一致（★出所が違う）** |

---

## 2. ★IMPL が「言えない」と書いた点を、私が閉じた

**IMPL の記述（正しい慎重さ）:**
> 「窓の2件のうちどちらが当該 PLAN かは名指しできない（★要求 id が記録に無い）」

**★別の出所から閉じられる。**
```
★計画そのものが、自分の実行回数を持っている:
   implementation_packet.runtime_recovery = {"attempts": 2, "final_max_tokens": 4096, "outcome": "RECOVERED"}
★取得は front door: GET /api/claude_packet?task_id=TASK-2DER-B11764B3（★既存の口）
★コード実読: build_plan は run_with_recovery(call=lambda mt: _qwen_call(...)) を1つ持ち、
             validate_plan は決定論（LLM を呼ばない）
∴ ★planner の LLM 呼び出しは attempts と同数 ＝ ★2回
```
| | |
|---|---|
| **モデルサーバ側（IMPL・docker ログ）** | 窓内の completion **★2件** |
| **計画側（設計・front door）** | planner の試行 **★2回** |
| **★一致** | **★出所が違う2つが、同じ数を指した ＝ 検算になった** |

> **★これで「窓の2件は planner の2回である」が最も強い説明になる。**
> **★ただし言い過ぎない**: **★「他の呼び手が同時に居なかった」を直接示してはいない。** **★傍証は対照10分が0件であること。**

### 2-1. ★同時に分かった、都合の良くない事実（★自分から出す）
```
★attempts: 2 / outcome: "RECOVERED" ＝ ★1回目は失敗している（★max_tokens を上げて2回目で通った）
★∴「Qwen が一発で計画を作った」ではない。★2回目で作った。
★これは印2 を弱めない（★作ったのは Qwen である）が、★「うまくいった」と書かない材料である。
★★私はこれを直しに行かない・提案もしない（★成果判定の対象外）。
```

---

## 3. ★V-1/V-2/V-3 の受入（★1条件に1つの印）

| 受入 | ★判定 | 根拠 |
|---|---|---|
| **V-1-a** 見た先の名指しと形式 | **★満たす** | `qwen36_vllm` コンテナ自身の json-file ログ。**★2DER の台帳ではない**＝別出所 |
| **V-1-b** 窓内の件数（打ち切り無し） | **★満たす** | 2件（200 OK）。**★「測って2件」と書いている** |
| **V-1-c** 対照 | **★満たす** | 19:20〜19:30 は **0件**。**★「その窓だけとも書けない」と自分で限界を書いた**（19:31 に別のかたまり） |
| **V-2-a** 代入箇所の全件 | **★満たす** | 総6・確認6・打ち切り無し。**★私の走査と一致** |
| **V-2-b** 到達経路 | **★満たす** | 本番は HTTP を必ず通る。**★迂回の継ぎ目2つを「在る」と正直に書き、本番からの設定が0件であることまで示した** |
| **V-2-c** テンプレートの値 | **★満たす** | `RULE_TEMPLATE_2DER_EVO_0007` ∴ **★テンプレート説は消える** |
| **V-3** | **★「分からない」で満たす** | **★SPEC が「分からなければ分からないと書く。追加で掘らない」と定めた通り** |

## 3-1. ★IMPL の作法で良かった点（★成果判定に効くものだけ）
- **★窓の1分前に別のかたまりが在ることを、自分から書いた。** **★書かなければ「その窓だけ」と読めた。**
- **★自分が計器に入っていないことを4点で確かめた**（投げていない／作業開始は19:38／直近2分0行／自分の `ugrep` を除外）＝ **§4-15**。

---

## 4. ★私の結論と、私がしないこと

```
★V-1 と V-2 は同じ向きに出た。★加えて、出所の違う2つ（docker ログ／計画の runtime_recovery）が
  ★同じ「2」を指した。
★∴ 印2【生成】の材料は揃った。
★★しかし私は印2 を「立つ」と書かない。★判定の承認はあなたである（運用方針 §1「自分の監査を自分で承認しない」）。
★私は GENERATE を叩いていない。★成果物を production に入れていない。★何も実装していない。
```

---
**決めたこと**: **①IMPL の主張3点を自分で取り直し、3点とも一致した ②IMPL が「言えない」とした「窓の2件のどちらが PLAN か」を、計画自身の `runtime_recovery.attempts=2` と突き合わせて閉じた（出所が違う2つが同じ数を指した＝検算） ③ただし1回目は失敗しており「一発で作った」ではない。印2 の承認は MGR。**
