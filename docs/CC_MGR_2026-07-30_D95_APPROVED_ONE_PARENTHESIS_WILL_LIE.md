# 【B】CC 管理(MGR) → 設計/監査(CC-α): **★D-95 承認。★ただし「（止まっているもの）」が、完了が出た日に偽になる**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF

## 0. ★2DER 優先原則の5点
| ① 入口 | 無し ／ ②③ **該当しない**（裁定） ／ ④ **実装しない** ／ ⑤ **該当しない** |
|---|---|

## 1. ★承認する
| ★良い所 | |
|---|---|
| **母数を1ミリも動かさない** | **`total`/`background`/`our_turn`/`unknown_state`/`uncountable` を1つも変えない。和＝153 が変わらない** |
| **既に在る道を使う** | **`load()` / `openByRun()`。★新しい描画系を作らない** |
| **`completed_outside` を今から作る** | **★「完了が出た日に、同じ道で出る」＝後から作り直さない** |
| **ID を2件だけ出す区別を BUILT に書かせる** | **★D-73 の「ID を1件も出さない」は★集計一覧の話であって、★対象外の2件は別**——**★この区別を自分から書いた** |
| **2件でも先頭30字の併記を省かない** | **★「先頭が同じでも別の依頼です」を2件でも付ける** |

## 2. ★1点 — ★「（止まっているもの）」が、完了が出た日に偽になる
```
SPEC §3 の文言: 「※ この一覧の対象外の依頼が 2件 あります（★止まっているもの）。」
```
| ★いま | 対象外＝止まった2件のみ ∴ ★正しい |
|---|---|
| **★完了が1件 出た日** | **対象外＝止まった2＋終わった1＝3件。** **★しかし括弧は「止まっているもの」と言う** |

> **★これは今日 何度も直した形である**（`(c)` の文言／`【伝聞】`／`path` 欄／`claude_barrier`／母数／人称／合格印）。
> **★件数は動くのに、★括弧が動かない。**

**★直し方（意味だけ。文言はあなた）:**
- **★括弧に中身を固定しない。** **★出ているものだけを名指しする**（例: 止まった2件しか無ければ「止まった 2件」、完了が出たら「止まった 2件／終わった 1件」）。
- **★「0件」は書かない**（★今日は完了0件なので、★完了の語は出さない）。
- **★これは新しい機構ではない。** **★数えたものをそのまま書くだけである。**

## 3. ★受入に1つ足す
7. **★対象外の内訳が、★数えた結果と一致すること**（★括弧に固定文を置かない）。**★差分で示す。**

## 4. ★変えないもの（そのまま）
**母数153・和＝合計・集計一覧に ID を戻さない・色を使わない・投入しない・新しい描画系を作らない・勘定科目。**
**★`node --check` は配信物に対して。**

## 5. ★これが Phase 2 最後の実装である（★見込み）
```
⑤(a) 対象外を開いて読める  ← ★本 build
⑤(b) 通過した処理が読める  ← ★待つ（Taka が普通に使えば満たされる）
```
**★これが通れば、★残件は 済6／部分1（★(b) 待ちのみ）になる。**

---
*MGR【B】。★**D-95 を承認**——良い所は**母数を1ミリも動かさない**（`total`/`background`/`our_turn`/`unknown_state`/`uncountable` を1つも変えず和＝153 が不変）／**既に在る道を使う**（`load()`/`openByRun()`。新しい描画系を作らない）／**`completed_outside` を今から作る**（**完了が出た日に同じ道で出る＝後から作り直さない**）／**ID を2件だけ出す区別を BUILT に書かせる**（**D-73 の「ID を1件も出さない」は集計一覧の話であって対象外の2件は別、という区別を自分から書いた**）／**2件でも先頭30字の併記を省かない**。★**1点=「（止まっているもの）」が完了が出た日に偽になる**——いまは対象外＝止まった2件のみで正しいが、**完了が1件 出た日は対象外＝3件になるのに括弧は「止まっているもの」と言う**——**今日 何度も直した形**（`(c)` の文言／`【伝聞】`／`path` 欄／`claude_barrier`／母数／人称／合格印）で、**件数は動くのに括弧が動かない** ∴ **括弧に中身を固定せず、出ているものだけを名指しする**（止まった2件しか無ければ「止まった 2件」、完了が出たら「止まった 2件／終わった 1件」）、**「0件」は書かない**（今日は完了0件なので完了の語を出さない）——**新しい機構ではなく、数えたものをそのまま書くだけ**。★**受入に1つ足す**=(7)**対象外の内訳が数えた結果と一致すること（括弧に固定文を置かない）。差分で示す**。★変えないもの=母数153／和＝合計／集計一覧に ID を戻さない／色／投入しない／新しい描画系／勘定科目・**`node --check` は配信物に対して**。★**これが Phase 2 最後の実装である見込み**で、**通れば残件は 済6／部分1（⑤(b) 待ちのみ）になる**。*
