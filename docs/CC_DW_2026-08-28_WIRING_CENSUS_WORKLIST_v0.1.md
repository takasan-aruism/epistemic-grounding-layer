# DW 配線 全件調査 — 作業リスト 第1版（★各担当に確認してもらう物）

- 親: `ITEM-2DER-EVO-0149` / 担当: DW（`DW_CLAUDE`） / 日付: 2026-08-28
- 測ったHEAD: twoder `e27ebe5` / dev-workcell `b003368` / egl `febee25`
- 指示（Taka 逐語）: 「現状の配線を全て確認する／つながっているのが全て正しいわけではない／
  接続の期待が想定より古い場合もある／全件検索をしながら一つずつ／最終的にまとめた作業リストを出し、
  各担当に確認させて現実的な配線を構築する」
- **★これは調査結果であって修理案の確定ではない。★DW のコードは1行も変えていない。**

---

## 0. 調査の分母（★手で並べていない＝ AST と全件走査で作った）

| 軸 | 何を数えたか | 分母 |
|---|---|---|
| A | import 配線（両方向） | DW→外 11 module / 26箇所 ／ 外→DW 41 file / 63箇所 |
| B | CREATE payload の欄 | 708 CREATE |
| C | provenance の欄 | 697 packet |
| D | DW 側が叩く front door の口 | 8種 |
| E | 相手系の語が DW 側に在るか | dev-workcell 全 .py |
| F | 経路表 S09〜S18 | 10区間 |
| G | 外が実際に呼ぶ DW の口 | 公開関数 23 / 消費 file 40 |
| H | DW が空を監視している欄 | 709 TASK |
| I | ESDE の対象選別が DW state を見るか | `esde_backlog` の入力 |

★未測定は 0件（★第1回で 9軸すべて測った。★item 起票時は 6/9 だったので **ここで更新する**）。

---

## 1. 作業リスト（★ずれが在った軸だけ）

| # | 軸 | 実測（分母つき） | 確認してほしい担当 | 既存 item | 私の見立て（★決めていない） |
|---|---|---|---|---|---|
| **W1** | H/B | `related_failure_patterns` が **EMPTY 665 / 709 = 93.8%**。★DW が空を監視している欄は**この1つだけ**で、その1つが94%空 | **EVO-0124 の担当** | **在る**（EVO-0068 / 0123 / 0124） | ★新規起票しない。**この数字を 0124 へ渡すだけ** |
| **W2** | C | `ds_thread_id` が **3 / 697 = 0.4%**。★DW から明細へ戻る鍵がほぼ無い | **明細 / Ledger 担当** | **無い**（全件検索で 0149 以外に0件） | ★起票候補。**但し「鍵を足す」のか「明細側が task_id で引く」のかは 明細担当の判断** |
| **W3** | E | dev-workcell 全 .py に **明細 0回 / ESDE 0回 / RRI 0回 / 勘定科目 0回**。かつ `provenance` を**読む行が 0**（docstring と comment だけ） | **MGR（どこが持つか）** | 無い | ★「入るが読まれない」。**読む必要が本当に在るのかを先に決める**（読み手を作るのが正しいとは限らない） |
| **W4** | F | S09/S12/S13/S16/S17/S18 の **6区間が同一の受け手 `DW._append_event` を宣言**し、記録上 区別できない（6区間とも 45964回・08-28 は 6966回）。**S10 は receipt の宣言が無い** | **Route / Topology 担当** | route 系は45件あり特定できず | ★**鍵が区間を分けていない**。★DW 側で分けるのか経路表側で分けるのかは Route 担当の判断 |
| **W5** | I | `esde_backlog` は `/api/tasks?limit=5000` の**平坦な一覧**から選び、**DW state を1度も見ない**。★一方 `domain_esde` 自身が逐語で「入力が変わった/PLAN・contract が変化した は**前回評価時の指紋を持たないと判定できない**」と書いている | **ESDE 担当** | 無い | ★**DW はその指紋（events / plans / contract）を持っている**。★ESDE が「無い」と言っている物を DW が持っている ―― **口が無いだけ** |
| **W6** | B | `contract` を持つ CREATE が **217 / 708 = 30.6%** | **MGR** | 無い | ★契約なしの TASK が7割。**それが正常なのか（調査系は契約を持たない）を先に決める** |

### ★ずれが無かった軸（★item を作らない＝番号を増やさない）
- **G**: DW の公開関数 23 のうち **21 が外から呼ばれている**。呼ばれていないのは `block_task` / `data_dir` の2つだけ。
  → **DW の出口は使われている。** ★但し `block_task` に呼び手が無いのに `BLOCKED` が4件在るのは別途確認（★小さいので W リストに載せない）。
- **A**: import は両方向で成立。★`rri` から `dw` を import する **.py は 0本**（当たったのは `.jsonl` 2本＝データ）。
  → **RRI→DW はコードの依存ではなく payload だけ**。これは W3 と同じ話なので独立させない。
- **D**: front door の口 8種。★`dev-workcell` 本体は front door を**1つも叩かない**（叩くのは `domain_dw` だけ）＝**層が守られている**。

---

## 2. ★この調査で分かった形（★1行）

> **DW への入力は 99.7% 揃っているのに、DW はそれを読む口を持っていない。**
> **DW の出力は 21/23 が使われている。**
> ∴ **ずれは「出す側」ではなく「受け取った物を使う側」に偏っている。**

（provenance: `rri_request_id` 99.7% / `rri_intent_id` 99.7% / `trace_id` 99.7% / `egl_source_refs` 94.7%
　―― すべて記録されるが、`dev-workcell` の中に読む行が1つも無い）

---

## 3. ★各担当への依頼（★私は相手の領分を決めない）

各行について **①この配線は今も必要か ②期待している形は今の形か ③誰が直すか** の3つだけ返してほしい。
★「不要」と返ってきた行は **消す**（配線を増やすのが目的ではない）。

---

## 4. していないこと

- DW のコードを 1行も変えていない ／ TASK を 1件も進めていない
- ずれが在った軸を **まだ item に割っていない**（★各担当の確認前に番号を増やさない）
- 相手 Domain のコードを 1文字も読んで直していない
- 既存 item が在る軸（W1）を **新規起票していない**
