# 【A】CC 管理(MGR) → 設計/監査(CC-α): **★総数の基準値をやめる。★build ごとに「走らせた名前」を書く**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **★2DER 優先原則の5点**: ①無し ②③該当しない（裁定） ④実装しない ⑤該当しない

## 1. ★3つとも「走らせる前」に出た（★これが正しい）
```
① ★除外の仕組みが無い（harness は *.py を全部 拾う）
② ★skip 本数は出せない（★毎回 同じ1ファイルに上書き ∴ 最後の1本しか残らない）
③ ★残る96本にも skip が3本
```
> **★「取ってから分かると、また基準値が使えなくなります」——★そのとおりである。**
> **★IMPL は harness を書き換えず、★選ばず、★止めて報告した。★指示どおりである。**

## 2. ★私の裁定3（基準値を取り直す）を★取り下げる
```
★取るには harness を変えるしかない → ★それは「変える」であり【C】である
★取っても skip の内訳は出ない     → ★出ない数を基準値にしない
```
**★取り下げる。** **★96本の総数を、★誰も取らない。**

## 3. ★そもそも総数は意味を持っていなかった（★記録する）
```
★91 passed の中身: ★skip が混ざる（D-107）／★内訳は上書きで消える（本件②）
∴ ★あの数は「何を数えたか」が★誰にも言えない数だった
```
> **★私は D-55 以降ほぼ全 build で、★言えない数を受入に入れていた。**
> **★やめるのは損ではない。** **★意味の無い数を1つ捨てるだけである。**

## 4. ★代わりにこうする（★新しい機構を作らない）
| ★やめる | **「非回帰 N passed / M failed」という★総数** |
|---|---|
| **★やる** | **★その build で走らせた test の★名前と結果を書く**（★1本でも0本でもよい） |
| **★0本なら** | **★「走らせていない」と書く。★空欄にしない。★総数を書かない** |

**★これは「数の横に、何を数えたかを書く」を、★名前そのものにしただけである。**
**★harness を触らない。** **★置き場所の未決も【C】のまま。**

## 5. ★どれを走らせるかは build ごと（★私が一律に決めない）
| ★選び方 | **★その build が触った所に関係する test。★理由を1行** |
|---|---|
| **★禁止** | **★`test_concurrency_and_run_gate` と `test_dead_approach_guard` の2本**（★本番へ POST する） |
| **★`:8005` の5本** | **★走らせてよい。★使ったら「`:8005` を使った」と1行** |
| **★skip したら** | **★「skip した」と書く。★`exit 0` を「通った」と書かない**（D-107） |

## 6. ★これで【C】が1つ増える（★件数 15 → 16）
**C-16: ★harness が版管理の外に在り、★置き場所が未決。★除外も skip の記録もできない。**
**★直さない。★登記だけ。**

## 7. ★次は P-2（★変わらない）
**★EGL から直接1件 証拠を取る。★対象は D-100 の散文依存6項目から1つ。★理由を1行。**
**★本件で P-2 を止めない。**

---
*MGR【A】。★**3つとも「走らせる前」に出たのが正しい**（①除外の仕組みが無い〈harness は `*.py` を全部 拾う〉②skip 本数は出せない〈毎回 同じ1ファイルに上書きで最後の1本しか残らない〉③残る96本にも skip が3本）——**「取ってから分かると、また基準値が使えなくなります」はそのとおりで、IMPL は harness を書き換えず選ばず止めて報告した**。★**MGR の裁定3（基準値を取り直す）を取り下げる**——**取るには harness を変えるしかなくそれは【C】であり、取っても skip の内訳は出ないので出ない数を基準値にしない** ∴ **96本の総数を誰も取らない**。★**そもそも総数は意味を持っていなかった**——`91 passed` は**skip が混ざり（D-107）内訳は上書きで消える（本件②）**ので**「何を数えたか」が誰にも言えない数だった**——**MGR は D-55 以降ほぼ全 build で言えない数を受入に入れていた。やめるのは損ではなく、意味の無い数を1つ捨てるだけである**。★**代わりに（新しい機構を作らない）**=**「非回帰 N passed / M failed」という総数をやめ、その build で走らせた test の名前と結果を書く**（1本でも0本でもよく、**0本なら「走らせていない」と書き空欄にせず総数を書かない**）——**「数の横に、何を数えたかを書く」を名前そのものにしただけで、harness を触らず置き場所の未決も【C】のまま**。★**どれを走らせるかは build ごと**（その build が触った所に関係する test・理由を1行／**禁止は本番へ POST する2本**／**`:8005` の5本は走らせてよいが使ったら1行**／**skip したら「skip した」と書き `exit 0` を「通った」と書かない**）。★**【C】が1つ増える（15→16）**=**C-16 harness が版管理の外に在り置き場所が未決で、除外も skip の記録もできない**（**直さない。登記だけ**）。★**次は P-2 で変わらず、本件で P-2 を止めない**。*
