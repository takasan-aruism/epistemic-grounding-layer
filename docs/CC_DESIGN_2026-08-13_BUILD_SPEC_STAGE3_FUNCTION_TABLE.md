開発者規律 確認済(v1.0)

# 【BUILD SPEC】★段3 ―― ★機能表（★一覧＋「一覧に無い」＋★正式登録が **1件** 通る）

宛: IMPL ／ 発: DESIGN ／ 2026-08-13 22:3x ／ 台帳: `ITEM-2DER-EVO-0058`
出所: **正本** `CC_MGR_2026-08-12_TAKA_ROUTE_AND_FUNCTION_SPEC_v0.1.md` §4.1 / §4.3 / §7.2 / §10 / §16 段3 / §22 / §25
＋ **MGR 受領 22:19**（受入5点）／ **Taka 逐語**「★人体図鑑のような物であると同時に ★機能表でもある。つまり ★機能から索引できる」

**★正本 逐語（★段3 の 終了条件）**
> 「Function一覧が実際に1件増え、★追加主体・承認根拠をfront doorから確認できる。★ここで一度停止する。」

**★いま 埋まっているのは 人体図鑑の側だけ ＝ ★機能表は 0件。★片方だけ 作って『できた』と 書かない。**

---

## 0. ★★既存を 読んでから 作る（★私が 22:2x に 実測・★名前を 出す）

```
★★探した = ★5 repo の `*.py` に ★`FUNCTION_LIST` / `function_list` / `FUNCTIONS =` → ★★0件
   ―― ★∴ ★機能表は ★本当に 無い（★『在るのに 作った』を 避けるため 先に 引いた）

★★使い回す 既存（★★3つとも 既に 動いている・★写さない＝★呼ぶ）
   ★① `twoder/account_gate.py`      = ★★一覧＋『一覧にない』＋『決められない』の ★既存の形
       ―― ★`_menu()`(:57) ／ `_prompt()`(:62) ／ `check()`(:105 ＝★メニュー外を 通さない)
   ★② `twoder/approve_account.py`   = ★★正式登録の 既存の形（★正本 §10.3『新しい承認システムは作らない』）
       ―― ★`MIN_OCCURRENCES=2`(:13 ＝★1回きりは 溜める) ／ ★承認者は ★呼び手の 認証済み識別(:3)
       ―― ★★『chart を書くのは この関数だけ＝★機械で数えられる』(:4) を ★そのまま 効かせる
   ★③ `twoder/route_adopt.py`       = ★★append-only の 採用／取り消し（★新台帳0＝★`event_trace` へ emit）
       ―― ★`origin` は ★`machine` / `hand` の ★★2語だけ(:12) ＝ ★★語彙を 増やさない
   ★④ `twoder/menu_vote.py`         = ★3-seed 合議（★段2 で 括った物）

★★∴ ★★新規は ★`twoder/function_table.py` ★1本だけ（★規律 §9 = ★増やす管理対象を 数える）
```

---

## 1. ★★場合の 列挙（★★走らせる前に 出す・★Taka 常設）

```
★★【★問い2 の 票（★1部品あたり 3票）】
★① 3票 全会一致で 一覧の 語        → ★確定 ／ final=その語 ／ ★候補は 出ない
★② 3票 全会一致で ★`NOT_IN_LIST`  → ★確定 ／ ★★新規候補として 名前を 溜める
★★③ 2対1（★片方が `NOT_IN_LIST`）→ ★★final=`NOT_DECIDED` ／ ★★★但し ★`NOT_IN_LIST` の 票と 名前は ★残す
     ―― ★理由 = ★★受入②『★一覧に無いが ★別の値で 残る（★0に 押し込まない）』
★④ 1対1対1                        → ★final=`NOT_DECIDED` ／ ★票は 残る
★★⑤ ★`NOT_IN_LIST` だが ★名前が 拾えない → ★★`name=None` で 1件 数える（★★0件に しない）

★★【★新規候補 → ★正式登録】
★★⑥ 同じ名前が ★1部品でしか 出ない      → ★★溜める（★登録しない・★消さない）＝ `held`
★★⑦ 同じ名前が ★★2部品以上で 出た       → ★★★承認できる（★`MIN_OCCURRENCES=2` を そのまま）
★★⑧ ★綴りが 違う 近い名前（例『登記』『登記処理』）→ ★★★別物として 数える（★★綴りで 潰さない）
     ―― ★★★出す時に『★近い名前が N 組 在る』を ★★併記する（★人が 見て 決める材料・★機械は 潰さない）
★★⑨ ★既に 一覧に 在る名前が `NOT_IN_LIST` として 出た → ★★登録しない ／ ★★『重複』として 数える
★★⑩ ★承認が 下りない                    → ★★一覧は 増えない ＝ ★★★これも 正常な 結果（★理由を 返す）
★★⑪ ★登録した後で 間違いと 分かった      → ★★★取り消しも 1行 足す（★消さない＝`route_adopt` と 同じ）

★★【★索引（★機能から 引く）】
★★⑫ ★その機能を 持つ部品が 0件          → ★★『0件』を 返す（★★空欄に しない）
★★⑬ ★一覧に 無い機能名で 引かれた        → ★★★エラーにせず『★一覧に無い』を 返す ＋ ★★引かれた回数に 数える
```

---

## 2. ★★作る物（★1本・★★台帳 0増・★口 0増）

```
★★`twoder/function_table.py`（★★新規 1本＝★これだけ）

★★【★一覧の 出どころ】
   ★★種（seed）= ★★正本 §4.3 の ★★8語を そのまま = ★受信 ／ 検証 ／ 分類 ／ 登記 ／ 配送 ／ 監査 ／ 実行 ／ rollback
      ―― ★★これは ★★★人（Claude）が 書いた物 ∴ ★`origin="hand"` ＝ ★★★受入④の Claude 側は ★最低 8
      ―― ★★『2DER が 作った』の 数を ★濁らせない ため ★種は ★★module の 定数に 置く（★記録に 書かない）
   ★★育った分 = ★★`event_trace` へ emit（★★新台帳 0 ／ `route_adopt` と ★同じ器）
      ★`COMPONENT, FUNCTION = "FUNCTION_TABLE", "register"`
      ★★同じ `function_id` は ★★最後の行が 効く（★取り消しが 効く＝`adopted_rows()` と 同じ読み方）

★★【★id】★決定論 = ★`"FN-" + sha1(承認された名前)[:8]`（★`account_id_for` と 同じ作法）
   ―― ★★★候補の うちは id を 振らない（★`account_gate` 逐語:4『承認された時に 振る』）

★★【★front door】★口 0増 = ★`GET /api/control?include=function_table`
   ―― ★★既定（`include` なし）では ★計算しない（★正本 §15）
   ★★索引 = ★`GET /api/control?include=function_index&function=登記`
```

## 3. ★★問い2 を 掛ける 相手（★★決定論・★人が 選ばない）

```
★★母数 = ★★経路表に 在る ★`component`.`function` の ★相異なる ペア
   ―― ★出どころ = ★`route_table.ROUTE`(★手書き18) ＋ ★`route_adopt.adopted_rows()`(★採用行)
   ―― ★★★数を この文書に 書かない（★★段2b の 教訓＝★『644』を 定数で 書いて 外した）
      ★∴ ★★走り始めに 1回 引き、★`total_components` ／ `counted_by` ／ `as_of` を ★記録の 先頭に 置く

★★順番 = ★★`component`.`function` の ★昇順（★★人が 並べ替えない）

★★★止まる 条件（★★2つ・★どちらか 早い方）
   ★★① ★同じ名前の 新規候補が ★★2部品で 揃った → ★★★そこで 止める（★正本『ここで一度停止する』）
   ★★② ★★30 部品を 掛けても 揃わない        → ★★★止めて 報告する（★★止まりも 結果）
      ―― ★★この 30 は ★★私が 置いた 上限（★根拠=★段2b の 並列30 と 同じ枠・★:8005 を 独占しない）
      ―― ★★★超えた時に 勝手に 伸ばさない = ★★MGR へ 上げる（★『どこで 死んだか』を 漏斗で 出す）

★★★漏斗を 出す（★★どこで 減ったかが 読める形・★数だけ）
   ★掛けた部品 → ★`NOT_IN_LIST` の票が 1票以上 → ★全会一致 `NOT_IN_LIST` → ★名前が 拾えた
   → ★同じ名前 2部品 → ★承認できる
```

## 4. ★★Worker へ 渡す物（★正本 §8・★★repo 全体を 読ませない）

```
★★その部品の ★module docstring（★在れば）＋ ★★先頭 40行（★`route_edge_vote._head` を そのまま 呼ぶ）
★★その部品が ★経路表で ★受け取る／送る 物 = ★`sends` ／ `returns` だけ
★★一覧（★8語＋育った分）＝ ★★★これが『★Worker から 参照可能』の 実体（★正本 §16 段3 の ①）
★★★これ以外を 渡さない（★正本 §21.4）
```

## 5. ★★問い2（★メニュー型・★自由記述させない）

```
★★「★この部品は 何をする所か」
   ★① 一覧の 語（★8語＋育った分）から ★1つ
   ★② ★★`NOT_IN_LIST` … ★どれにも 当てはまらない
       ―― ★★この時だけ ★新しい名前を 1つ 書かせる（★★日本語 2〜6文字・体言止め）
       ―― ★★★id は 振らない ／ ★★綴りを 直さない（★★受け取った まま 数える）
   ★③ ★`NOT_DECIDED` … ★決められない（★推測で 埋めない）

★★合議 = ★`menu_vote.ask(..., require_unanimous=True)`（★★経路の分類と 同じ厳しさ）
★★メニュー外の 語 = ★`NOT_DECIDED` に 落とす（★`account_gate.check` と 同じ fail-closed）
★★★票は どの場合も 全部 残す（★★『割れた』は ★失敗では ない）
```

## 6. ★★正式登録（★★既存の承認経路を 使う＝★新しい承認工程を 作らない）

```
★★① ★権限の規則に 掛ける = ★`authority.POLICY` に ★★1行 足す
   ★`"REGISTER_FUNCTION": (REQUIRES_APPROVAL, "adding one name to the function list
      (revocable by appending one row; local to the function table)", REVERSIBLE)`
   ―― ★★根拠 = ★`ADOPT_ROUTE_EDGE`(:47) と ★同じ形（★★取り消しの1行で 戻る）
   ―― ★★★`CHANGE_AUTHORITY_CEILING` は 触らない（★自己昇格の 錠）

★★② ★承認者 = ★★呼び手の 認証済み識別（★★body から 詐称できない＝`approve_account` 逐語:3）
★★③ ★★書くのは この関数 1つだけ（★`approve_account` 逐語:4 ＝ ★★機械で 数えられる）
★★④ ★★2部品で 揃うまで 承認できない（★`MIN_OCCURRENCES=2` ＝ ★★人の注意力ではなく 数で 止める）
   ―― ★★足りない時は ★`{"approved": false, "held": true, "occurrences": n}` を 返す（★★黙って 落とさない）
```

## 7. ★★出す物（★★`?include=function_table` の 中身）

```
★① ★一覧（★語 ／ `function_id` ／ `origin` ／ ★`registered_by` ／ `registered_at` ／ `from_component`）
★★② ★★`by_origin` = ★★`{"hand": N, "machine": M}` ＝ ★★★Claude が 書いた 件数 ／ 2DER が 作った 件数
     ―― ★★Taka 明示（★★片方だけ 出さない）
★★③ ★★`not_in_list` = ★★候補の 一覧（★名前 ／ ★出た部品の 名前 ／ ★出た回数 ／ ★`held` か 否か）
     ―― ★★★0に 押し込まない（★受入②）
★★④ ★★`near_names` = ★★綴りが 近い 組（★★潰さずに 併記するだけ）
★⑤ ★漏斗（★§3 の 6段）＋ ★`total_components` ／ `counted_by` ／ `as_of`
★★⑥ ★★`index_lookups` = ★★索引が 引かれた 回数 ＋ ★★★その 鍵を 1行
     ―― ★★鍵 = ★`User-Agent` に `Mozilla` を 含めば ★`human`、含まなければ ★`machine`
     ―― ★★★この鍵は ★呼び手側が 変えられる ∴ ★★『人が set する値』に 近い = ★鍵を 添えて 出す
     ―― ★★★0 なら 0 と 書く（★★『誰も 使わない物に なる』への 受入）
★⑦ ★LLM 呼び出し回数（★実測＝★掛けた部品 × 3）／ ★所要
★★⑧ ★新しい名前（定数・関数・欄）の 数
```

## 8. ★★受入（★★走らせる前に 宣言する・★数で）

```
★★① ★★機能一覧が ★★実際に 1件 増える（★★8 → 9）
     ―― ★★★増えなかった場合は ★★§3 の 漏斗を 出して 止める（★★★止まりを 失敗と 書かない）
★★② ★★`not_in_list` の 候補が ★★別の値として 残る（★★`NOT_DECIDED` に 混ぜない・★0に しない）
★★③ ★★追加主体と ★承認根拠が ★★front door から 読める
     ―― ★`registered_by` ／ ★`authority_decision.decision` ／ ★`.rule` ／ ★`from_component` ／ ★票
★★④ ★★`by_origin` の 2つが 併記される（★hand ≧ 8 ／ machine ≧ 1）
★★⑤ ★★`index_lookups` が ★★0 でない（★★★私が 引く＋★機械が 引く の ★2通りで 数が 動く）
★★⑥ ★★同じ問いを 2回 引いて ★同じ（★時刻の欄を 除く）
★★⑦ ★★既定の `/api/control` が ★0.5秒以内（★★段0 を 壊さない）
★★⑧ ★★`route_table.ROUTE` が ★bytes 不変 ／ ★★新台帳 0 ／ ★口 0増
★★⑨ ★★`authority.POLICY` の 行数が ★★23 → 24（★★1行だけ・★★他の行は bytes 不変）
★★⑩ ★★既存試験が 通る（★★走らせた名前を 書く／★★禁止3本は 走らせない）
★★⑪ ★★取り消しが 1行で 効く（★★1件 登録 → ★取り消し → ★★一覧が 8 に 戻る → ★★★記録は 2行 残る）
```

## 9. ★★言い方（★★書いてはいけない文）

```
★★『機能表が 完成した』と 書かない（★★1件 増えた だけ＝★正本『ここで一度停止する』）
★★『Worker が 部品の 機能を 判定できるように なった』と 書かない
★★正しくは = ★★『★掛けた部品 N のうち ★確定 a ／ ★決められない b ／ ★一覧に無い c ／ ★登録 1件』
★★『2DER が 機能表を 作った』と 書かない = ★★★hand 8 ／ machine 1 の ★両方を 書く
★★★私（Claude）が 本番 repo へ 配線した行は ★行数を 報告し ★2DER の実績に 数えない
```

## 10. ★★やらないこと（★★一度に 広げない）

```
★★問い3（★『この機能は 既に在るか』＝正本 §7.3）を ★この段で 作らない
   ―― ★★★戻す条件 = ★★段3 が 通り ★一覧が 1件 増えた 後（★★畳めない物に 畳む条件を 併記＝★規律 §0）
★★母数 全部（★経路表の 全部品）を 掛けない ―― ★★止まる条件は §3 の 2つ
★★`WIRE_CANDIDATE` 11件を ★繋がない（★MGR が 後回しに した・★戻す条件つき）
★★★綴りの 近い名前を ★機械で 束ねない（★★正規化は 綴り一致で auto-collapse しない）
★★数えていない 9種 を この段で 数えない ／ ★★新しい承認システムを 作らない（★正本 §10.3）
```
