# 【D-172】PLAN の4入力を突き合わせ、**★衝突を対応づけて露出する**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 14:5x / TYPE=HANDOFF
- **正典**: `TAKA_2026-07-31_EXPOSE_THE_REJECTION_REASON_PLAN_INPUTS.md`（★逐語・全文＋★出力様式）
- **運用方針 確認済（版: v2.9）** ／ **★新しい名前で置いた**

## 1. ★取り下げる
```
★`D-171` の「★GENERATE の禁止を解くか」は ★Taka が却下した ∴ ★取り下げる。
★★「PLAN 監査を新しく追加する」も ★不要と明示された ∴ ★作らない。
```

## 2. ★1点だけ前提を正確にする（★指示を狭くするためではない。★誤った物を露出させないため）

**★Taka 逐語**: 「**Auditは既にPLANを却下できているため、まずその却下理由を完全に露出し**」

```
★実測（設計の調査・`D-170` 回答）: ★DW の `AUDIT` 段は ★本日1度も到達していない。
   ★4件の task はすべて ★`READY_FOR_IMPLEMENTATION`（★PLAN 完了）で停止している。
∴ ★★`QwenAuditor` が出した却下理由は ★存在しない。★露出させる対象が無い。
★★★却下したのは ★我々（設計/監査 CC-α）である。★受入3 を × と判定したのは我々の監査である。
```

> ### **★∴ 露出させるのは「★我々の却下理由」である。★Taka の様式で書く。**
> **★`QwenAuditor` に判定させるのは★別件であり、★今はやらない**（★正典「新規監査は要らない」に触れないため）。
> **★もし Taka が「DW の auditor に判定させろ」という意味だったなら、★そう言われた時点でやる。★先回りしない。**

## 3. ★やること（★1本で返す。★増やさない）

### 3-1. ★PLAN に渡された4つを、★現物で並べる
| # | 何を | ★どこから取るか |
|---|---|---|
| 1 | **system prompt** | **★`_plan_prompt` が実際に組み立てた全文**（★保存済み `abc40_A_full.txt` が実物である） |
| 2 | **依頼** | ★`goal`（★`D-159` の依頼文） |
| 3 | **Observation** | ★観測ブロック（★取得記録4行＋値21行） |
| 4 | **制約** | ★`prohibited_actions` / ★JSON の欄指定 / ★その他 prompt 内の禁止・要求 |

### 3-2. ★論理的に両立しているかを見る（★これが本題）
```
★★「どの入力」と「どの入力」が★衝突しているかを★対応づける。★片方だけ書かない。
★★逐語で引く（★要約しない）。★引用元（行）を書く。
★★衝突が無いなら「★無い」と書く（★探して無いなら、それが結果である）
```

### 3-3. ★出力は Taka の様式で（★1文字も足さない・引かない）
```
AUDIT_RESULT:
FAILED_CLAIM:
CONFLICTING_INPUTS:
REJECTION_REASON:
LIKELY_CAUSE:
```
**★`LIKELY_CAUSE` は★仮説である ∴ ★「仮説である」と分かる書き方にする**（★Taka の例も「作用している」と書いている）。
**★PASS/FAIL だけにしない**（★正典が明示的に禁じている）。

## 4. ★やらないこと
```
★PLAN の修正 ／ ★新規監査の追加 ／ ★GENERATE 実行 ／ ★prompt の変更
★★すべて「★この確認の後」である（★正典）。★1件ずつ。
```

## 5. ★Taka の仮説を、★検証も否定もせずに扱う
```
★Taka の `LIKELY_CAUSE`: 「★『不足情報を取得せよ』が『既存観測を優先せよ』より強く作用している」
★★これは★現物で確かめられる（★prompt に両方が在るか、★どちらが何回・どの位置に在るか）。
★★∴ ★確かめて書く。★「Taka がそう言ったから」と書かない。
★★★もし「既存観測を優先せよ」に当たる文が ★prompt に1つも無いなら、★それが答えである。
```

---
**決めたこと**: **①`D-171`（GENERATE の禁止を解くか）は Taka が却下したので取り下げ、新規の PLAN 監査も作らない ②DW の `AUDIT` 段は1度も到達しておらず `QwenAuditor` の却下理由は存在しない——却下したのは我々（設計/監査）なので、露出させるのは我々の理由である（DW の auditor に判定させるのは別件で、言われた時点でやる） ③PLAN に渡された4つ（system prompt・依頼・Observation・制約）を現物で並べ、どの入力とどの入力が衝突しているかを逐語で対応づけ、Taka の様式で出す ④Taka の仮説（「不足情報を取得せよ」が「既存観測を優先せよ」より強い）は現物で確かめて書く——「Taka がそう言ったから」とは書かない。**
