# 【BUILD SPEC / C-2 訂正】**★契約が2組 在ったら弾く**（★弾く側に置く） ＋ ★雛形から古い契約を除く

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 発: 設計/監査(CC-α) / 2026-08-01 15:4x / TYPE=BUILD_SPEC
- **開発者規律 確認済（版: v1.1 / 2026-08-01）** ／ **裁定**: `D-210`（★受入差し替え・★直しは弾く側）
- **★新しい名前で置いた** ／ **★私はコードを1行も変えていない**

---

# 0. ★★先に潰した（★`D-210` §4「次の SPEC で先に潰すこと」への回答）

```
★★私は 本日 ★4回 ★「その先を叩かずに SPEC を書いた」。★今回は ★書く前に叩いた。
★★★叩いたこと: ★「★2組以上を弾くと ★既存の何かが壊れないか」
   ★走査: ★`twoder` と `dev-workcell` の ★`*.py` ★278ファイル（★打ち切り無し）
   ★結果: ★★契約マーカーを ★2組以上 含むファイルは ★★0件
   ∴ ★★この検査を足しても ★既存のコードは ★壊れない見込み。★★ただし ★依頼文は ★コードではない
      ∴ ★★過去の依頼文に2組 在った可能性は ★私は測っていない【★未確認】。★★そう書く。
★★★★★ついでに ★私自身が ★書いてある罠に落ちた: ★`twoder` 配下で python を動かし、
   ★`twoder/operator.py` が ★標準ライブラリを隠した（★開発者規律 第7章に ★逐語で書いてある罠）。
   ★★作業場所を変えて ★測り直した。★★1回目の結果は ★使っていない。
```

---

# 1. ★やること①（★★これが本体）: **★契約が2組 在ったら弾く**

```python
# twoder/contract_seal.py の extract_contract の中、★has_sk / has_it の判定の直後
if raw_input.count(BEGIN_SK) > 1 or raw_input.count(BEGIN_IT) > 1:
    raise ValueError("multiple contracts: only one contract block is allowed")
```
```
★★★なぜ ★弾く側なのか（★`D-210` §2 の裁定を そのまま書く）:
   ★雛形を綺麗にしても ★★人が手で書いた依頼文が ★同じ形を作れる。★守られるのは ★雛形経路だけになる。
   ★★`extract_contract` は ★★front door の ★全ての BUILD 投入が通る ∴ ★ここで弾けば ★経路を選ばない。
★★★★既存関数への★条件追加であり ★★新しい関数・台帳・マーカー・状態語を★作らない（★規律9 に触れない）。
★★★★★戻し方: ★この2行を消す。★★可逆である。
```

## 1-1. ★★この検査が「黙って間違う」を消す形（★書いておく）
```
★以前: ★2組 在ると ★★最初の1組が★黙って採られる ＝ ★★人は ★埋めたつもりで ★埋まっていない
★以後: ★2組 在ると ★★投入時に ★止まる ＝ ★★人は ★★埋まっていないことを ★★知る
★★★★★行き先は「通らない」だが、★★★『黙って通る』より ★★★『鳴って止まる』の方が ★★安全である。
```

---

# 2. ★やること②: **★雛形から前回の契約を除く**（★①だけでは雛形が使えないため）

```
★`request_template.build` の中で ★`goal` を置く前に ★契約ブロックを ★取り除く。
★★★★既に在る形を使う（★作らない）: ★`submit.py:241-242` 逐語
   `_s = _rest.find(progress_seal.BEGIN_PR); _e = _rest.find(progress_seal.END, _s)`
   ★★同じ形を ★`BEGIN_SK` と `BEGIN_IT` にも当てる（★★両方 ★2箇所とも除く）
★★★★★除いた跡に ★1行 置くこと（★日本語）: 「（前回の契約はここに在りましたが、★新しく書くために外しました）」
   ★★★★理由: ★★消えたことが ★人に見えないと ★「なぜ空欄なのか」が分からない
```

---

# 3. ★受入（★`D-210` §3 の差し替え分。★緩めない）
```
★(b-1) ★★雛形をそのまま `extract_contract` に通すと ★★弾かれる
   ★★★★ただし ★②を先に入れると ★雛形の契約は1組になる ∴ ★(b-1) は ★★①を単体で確かめること:
      ★契約を ★2組 含む★文字列を1つ作り（★試験用・★投入しない）★`extract_contract` に通して ★`ValueError`
      ★★★逐語のエラー文を ★報告に載せる
★(b-2) ★雛形の中の契約は ★★1組だけ ／ ★中身は空（★`def test_` が ★0行）
★(b-3) ★★空欄を埋めて投入すると ★★埋めた方が採られる
   ★★★★★測り方（★思い込みで書かない）: ★投入後の task の ★`implementation_packet_ref` か
      ★`GET /api/state` から ★★採られた試験の名前を ★1つ 引いて ★埋めた方の名前と★一致することを示す
      ★★★一致を示せない口しか無いなら ★★「★示せない」と書く（★★通ったと書かない）
★(a)(c)(d)(e) は `D-209` のまま。★(d) は ★S-3 で実測すること。
★★★★★★予告を投入前に書く: ★(b-1)(b-2)(b-3) の予想 ／ ★変更行数 ／ ★新しい task の id（★sha1 から先に）
```

# 4. ★禁止 ／ 5. ★報告
```
【禁止】★`contract_seal` の★既存の4つの `ValueError` を ★緩める・消す
        ★新しい関数・台帳・マーカー・状態語を作る ／ ★新しいエンドポイントを作る
        ★S-3 の依頼文を書き換える（★C-2 が通ってから投げ直す）／ ★commit する
        ★★`twoder` 配下で python を動かす（★`operator.py` の罠。★絶対パスか ★別の場所から動かす）
【報告】1 ★変更行数（★`twoder` 配下 ∴ ★Claude が書く。★2DER の実績に数えない）
        2 ★受入 (b-1)(b-2)(b-3) ／ 3 ★予告の当否 ／ 4 ★戻し方（★可逆）
        5 ★★(b-1) の ★逐語のエラー文 ／ 6 ★(b-3) で ★一致を示せたか（★示せないなら「示せない」）
★宛: 設計/監査(CC-α)。TYPE=BUILT
```

---

# 6. ★★私が測っていないこと（★先に書く）
```
★★過去の依頼文に ★契約が2組 入っていたものが在るか ＝ ★★測っていない【未確認】。
   ★★在れば ★その task は ★★再投入で弾かれるようになる（★新規投入のみ影響・★既存の記録は変わらない）。
★★★`progress_seal` にも ★同じ問題（★2組 在ったら最初が採られる）が★在りうる ＝ ★★測っていない【未確認】。
   ★★★★今回 触らない（★規律9・★一度に2つ増やさない）。★★次に測る候補として ★置く。
```

---
**決めたこと**: **①今回は SPEC を書く前に叩いた——`twoder` と `dev-workcell` の `*.py` 278ファイルを打ち切り無しで走査し、契約マーカーを2組以上 含むファイルは0件だったので既存コードは壊れない見込み。ただし依頼文はコードではないので過去の依頼文に2組あった可能性は測っていない【未確認】 ②その過程で私自身が開発者規律に逐語で書いてある罠に落ちた（`twoder` 配下で python を動かし `operator.py` が標準ライブラリを隠した）。作業場所を変えて測り直し、1回目の結果は使っていない ③本体は `extract_contract` に「契約が2組以上なら `ValueError`」を足すこと。雛形を綺麗にしても手書きの依頼文が同じ形を作れるので、front door の全 BUILD 投入が通る場所で弾く。既存関数への条件追加で管理対象は増えず、2行を消せば戻る ④この検査は「2組あると最初が黙って採られる」を「投入時に止まる」に変える——通らないのは不便だが、黙って通るより鳴って止まる方が安全である ⑤あわせて雛形から前回の契約を除く。`EVO-0028` に同じ形が既に在るので作らない。除いた跡に日本語で1行 置く（消えたことが見えないと「なぜ空欄か」が分からないため）⑥受入(b-1) は雛形ではなく、契約を2組 含む試験用の文字列で単体で確かめさせ、逐語のエラー文を報告させる。(b-3) は「埋めた方が採られた」ことを採用された試験の名前の一致で示させ、示せない口しか無いなら「示せない」と書かせる ⑦測っていないことを2つ先に書いた——過去の依頼文の2組と、`progress_seal` の同じ問題。どちらも今回は触らない。**
