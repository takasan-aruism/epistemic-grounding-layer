# 設計/監査 → MGR（写: Taka / IMPL）: **D-111 — P-2: ★EGL からの証拠取得は★既にできる（実装不要）。★束縛できない理由は「読む口」ではなく「★我々の開発が EGL に記録されていない」ことである**

- `BUILD_ROLE: 参照`（**測定のみ。★実装していない・★投入していない・★台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **裁定**: `CC_MGR_2026-07-30_D104_RULING_WATCH_DONT_CHASE_NEXT_IS_P2.md` §5

## 0. ★2DER 優先原則の5点
| # | |
|---|---|
| ① 入口 | **`GET /api/resolve?id=DE-…`** |
| ② 何を試したか | **★EGL の記録を front door から引けるか／★今日の作業の DE が在るか** |
| ③ **できなかったこと** | **★今日の作業に対応する DE が見つからない**／**★DE に時刻の欄が無く、front door から日付で絞れない** |
| ④ 何を実装するか | **★何も実装しない**（★§2 の理由） |
| ⑤ | **★該当しない** |

## 1. ★選んだ対象と理由（★MGR の条件）
| ★選んだ | **⑭「Claude・MGR・人間が担当し続ける部分は何か」** |
|---|---|
| **★理由（1行）** | **★EGL に `generated_by_principal` という★機械の欄が既に在り、★D-100 の実行主体表（2DER 0/8）が散文依存だった原因を★直接つぶせるから** |

## 2. ★結論 — ★P-2 は実装を要しない（★既に取れる）
```
再現（★front door だけ・★台帳を読んでいない）:
  GET /api/resolve?id=DE-0567 → resolved:true
    evidence_refs: ["SLICE-TASK-2DER-21F64D9D", "workspace:/tmp/2der_runner_ghaiakgs/ws-8cff562b1d"]
```
> **★Taka の Phase 3 終了条件2「EGL から直接1件以上の証拠を取得できる」は、★今日 すでに満たせる。**
> **★私は実装案を出さない。** **★「既存の道が既に在った」の、★今日 数えて13回目である。**

### 2-1. ★`generated_by_principal` は実際に値を持っている（★標本28件）
```
再現: DE-0001〜DE-0525 を25飛ばし＋末尾6件で標本 → ★28件 引けた
  CLAUDE_CODE        ★10
  UNKNOWN_PRINCIPAL   4
  ★欄そのものが無い  14
```
> **★「誰が作ったか」は、★散文ではなく★機械の欄で持っている。** **★10件は実際に `CLAUDE_CODE` と入っている。**

## 3. ★しかし ⑭ を束縛できない（★理由は読む口ではない）
```
再現（★末尾10件 DE-0558〜0567 の observation を読んだ）:
  Build 2/3/4 の実装・retention 補修・front door 直叩きの列挙・runtime supervisor・live-worker slice
  ★今日の作業を指す語（受信 / RECV / receipt / 集約 / 153 / Phase 2 / P-1 / 届いた）を含む DE: ★0件
```
| ★分かること | **★今日の Phase 2 / P-1 の作業は、★EGL に記録が見当たらない** |
|---|---|
| **★∴ 束縛できない理由** | **★「EGL を読めない」からではない。★読める。** **★束縛する先（今日の DE）が★無いからである** |
| **★言い過ぎない** | **★DE に時刻の欄が無い**（front door から確認）∴ **★「今日は1件も作られていない」とは書かない。★「今日の語を含む DE は0件」である** |

## 4. ★∴ 足りないのは何か（★私は作らない）
```
★読む口   : 在る（/api/resolve）
★機械の欄 : 在る（generated_by_principal）
★足りない : ★我々の開発が EGL に記録される道
```
> **★これは D-100 ⑨（本日の commit 8件すべてに案件ID が無い）と★同じ1つのことである。**
> **★我々は 2DER の外で開発している ∴ ★2DER の帳簿に載らない ∴ ★束縛する相手が無い。**
> **★「口を作る」では埋まらない。** **★これは P-2 の範囲を超える ∴ ★MGR と Taka へ返す。**

## 5. ★在るものを1つ書いておく（★掘らない）
```
DE-0565: 「依頼 D4-I: front door を経由しない直叩き経路を★機械的に列挙
          (egl/structure/s_front_door_bypa…)」
```
> **★今日 見つけた `G-77`（テストが front door を通らずに本番へ書く）と★同じ主題の資産が、★既に EGL に在る。**
> **★私は開かない**（★C を掘らない・D-83）。**★在ることだけ書く。**

## 6. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **DE の総数** | **★上限は `DE-0567`（★二分探索で確認）。★欠番が在るかは見ていない** |
| 2 | **標本28件の代表性** | **★25飛ばしの標本である。★全数ではない** |
| 3 | **今日 DE が作られたか** | **★時刻の欄が無い ∴ 出せない。★「語を含むものが0件」までである** |
| 4 | **`generated_by_principal` が何を基準に入るか** | **★実装を読んでいない**（★`UNKNOWN_PRINCIPAL` と欄なしの違いも分からない） |

---
*CC-α D-111（測定のみ・実装なし）。★**選んだ対象は ⑭「Claude・MGR・人間が担当し続ける部分は何か」**で、**理由は EGL に `generated_by_principal` という機械の欄が既に在り D-100 の実行主体表（2DER 0/8）が散文依存だった原因を直接つぶせるから**。★**結論=P-2 は実装を要しない（既に取れる）**——`GET /api/resolve?id=DE-0567` が `resolved:true` で **`evidence_refs: ["SLICE-TASK-2DER-21F64D9D", "workspace:/tmp/…"]`** を返す ∴ **Taka の Phase 3 終了条件2「EGL から直接1件以上の証拠を取得できる」は今日すでに満たせる**——**CC-α は実装案を出さない。「既存の道が既に在った」の今日13回目である**。★**`generated_by_principal` は実際に値を持っている**（標本28件で **`CLAUDE_CODE` 10／`UNKNOWN_PRINCIPAL` 4／欄そのものが無い 14**）∴ **「誰が作ったか」は散文ではなく機械の欄で持っており10件は実際に `CLAUDE_CODE` と入っている**。★**しかし ⑭ を束縛できず、理由は読む口ではない**——末尾10件（DE-0558〜0567）の observation は Build 2/3/4 の実装・retention 補修・front door 直叩きの列挙・runtime supervisor・live-worker slice であり、**今日の作業を指す語（受信/RECV/receipt/集約/153/Phase 2/P-1/届いた）を含む DE は0件** ∴ **今日の Phase 2/P-1 の作業は EGL に記録が見当たらず、束縛できないのは「EGL を読めない」からではなく「束縛する先（今日の DE）が無い」から**——**ただし DE に時刻の欄が無いので「今日は1件も作られていない」とは書かず「今日の語を含む DE は0件」と書く**。★**∴ 足りないのは「我々の開発が EGL に記録される道」**（読む口も機械の欄も在る）——**これは D-100 ⑨（本日の commit 8件すべてに案件ID が無い）と同じ1つのことであり、我々は 2DER の外で開発しているので 2DER の帳簿に載らず束縛する相手が無い。「口を作る」では埋まらず P-2 の範囲を超えるので MGR と Taka へ返す**。★**在るものを1つ書いておく（掘らない）**=**`DE-0565`「front door を経由しない直叩き経路を機械的に列挙(egl/structure/s_front_door_bypa…)」**——**今日 見つけた `G-77`（テストが front door を通らずに本番へ書く）と同じ主題の資産が既に EGL に在るが、CC-α は開かず在ることだけ書く**。★確かめていないこと=**DE の上限は `DE-0567`（二分探索）だが欠番は見ていない**／**標本28件は25飛ばしで全数ではない**／**今日 DE が作られたかは時刻の欄が無いので出せない**／**`generated_by_principal` が何を基準に入るかは実装を読んでおらず `UNKNOWN_PRINCIPAL` と欄なしの違いも分からない**。*
