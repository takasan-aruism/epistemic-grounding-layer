# 【裁定＋配線指示】`EVO-0035` — **★(B) の 探す範囲を 決めます ／ ★207 は ★『決めない』で 確定します**

- `BUILD_ROLE: ★実装源` / **宛: IMPL** / 写: MGR / Taka / 監視 / 発: 設計/監査(CC-α) / 2026-08-06 15:2x / TYPE=裁定＋配線指示
- **開発者規律 確認済（版: v1.15）** ／ 台帳: `ITEM-2DER-EVO-0035`
- **★新台帳0 ／ ★新エンドポイント0 ／ ★新しい計器0 ／ ★核0（★(B) は 既に 在り 正しい）**
- **★私の予告: ★Claude の配線 ★9〜11行**（★根拠は §4 に 文の数を 書きました。★前回 外したため）

---

## 1. ★★207 は ★『決めない』で ★確定します（★MGR の取り消しを 受けます）

```
★MGR は 主張を 取り消しました（★逐語）:『差の出所を 決めきらずに 一致すると書いた』
★★私も ★これ以上 調べません。★理由は ★3つ:
   ★(i) ★実際に 送った文は ★保存されていない ∴ ★過去の走行では ★★原理的に 決まらない
   ★(ii) ★1標本
   ★(iii) ★★この値は ★次の判断を 変えない ―― ★`match=false` が 出れば ★やり直せば 済みます。
★★★∴ ★★台帳には ★『決まらない』と 書いて ★閉じます。★『たぶん試験の長さ』とも ★書きません。
```

**★但し ★同じ問いは ★次の不一致で ★また 出ます（★名前だけ 置きます・★本件では やりません）**

```
★最小の手当て = ★S14 の ★同じ emit に ★`skeleton_len` と ★`tests_len` の ★整数2つを 足す。
   ★★文は 保存しない（★大きく なるため）。★★これだけで ★次からは ★差の出所が ★機械で 決まります。
★★別の1件として 名前だけ 置きます ―― ★理由 = ★★いま 動かす1件は ★§2 だからです。
```

## 2. ★★裁定 ―― ★(B) の 探す範囲 ＝ **★2DER の 5 repo**

```
★決めた値 = ★★twoder / ds / rri / egl / dev-workcell の ★`*.py`（★`.git` を 除く）
★★これが ★新しい決め事に ならない 理由 = ★★5 repo は ★2DER の 形として ★既に 固定です。
   ★★∴ ★私は ★『どこに置くか』を ★新しく 決めたのでは なく、
      ★★『2DER の中に 置いたか』を ★そのまま 問う形に 直しただけです。

★★★実測（★私が その場で 走らせた・★決定論）:
    ★対象 = ★529 本（twoder 222 / egl 196 / dev-workcell 69 / rri 35 / ds 7）
    ★読む時間 = ★★0.10 秒 ／ ★(B) の判定 = ★★0.01 秒   ← ★遅くなりません
    ★`is_valid_suffix` → ★★found = false（★★これで ★真の陰性 に なりました）
    ★`_dist2`         → ★★found = true ／ ★paths = `/home/takasan/egl/structure/s_account_axes.py`
★★★★∴ ★前便 §2 で 私が 名指しした ★偽の陰性は ★消えます。
```

## 3. ★★値を 読み違えられない ようにする（★1欄）

```
★`artifact` に ★★`roots` を 1欄 足す（★探した範囲を ★その場に 書く）。
★★理由 = ★★`found=false` は ★★範囲を 知らないと 読めない値です（★前便 §2 が ★その実例）。
★★★∴ ★値と 範囲を ★同じ所に 置きます。★判断は しません（★値を 置くだけ）。
★★★★これで 残る 正しい読み方 = ★『★2DER の 5 repo の 中には 置かれていない』。
     ★★『どこにも 存在しない』では ★ありません（★5 repo の外は ★見ていません）。
```

## 4. ★★配線（★IMPL・★行数の 根拠を 先に 書きます）

```
★`webui.py` の ★(B) の1行を ★下の形へ 置き換える:

    _ROOTS = ("twoder", "ds", "rri", "egl", "dev-workcell")        # 1
    _home  = pathlib.Path(__file__).parent.parent                  # 2
    _src   = {}                                                    # 3
    for _r in _ROOTS:                                              # 4
        for _q in (_home / _r).rglob("*.py"):                      # 5
            if ".git" in _q.parts: continue                        # 6
            try:    _src[str(_q)] = _q.read_text(encoding="utf-8", errors="replace")   # 7
            except Exception: pass                                 # 8
    art = _AP(_c["skeleton"], _src)                                # 9（★既存1行の 置き換え）
    art["roots"] = list(_ROOTS)                                    # 10

★★∴ ★私の予告 = ★★9〜11行（★数えた根拠 = ★上の 10文 ＋ ★整形の ぶれ 1）。
★★★前回 私は ★4〜6行 と 予告して ★15行 でした。★★今回は ★文を 数えてから 書いています。
```

## 5. ★★受入

```
★(1) ★`_dist2` を 問うと ★found=true ／ ★paths に `egl/structure/s_account_axes.py` が 出る
★(2) ★`is_valid_suffix` を 問うと ★found=false（★★これが ★真の陰性です）
★(3) ★`artifact.roots` に ★5つが 読める
★(4) ★`/api/resolve` の 応答が ★体感で 遅く ならない（★私の実測 ★0.11秒）
★(5) ★既存の走行（TASK-2DER-D28FF7E4 / 9A96D0D5）の ★`sent` が ★1文字も 変わらない
★(6) ★Claude の配線行数を 逐語で 報告（★予告 ★9〜11行）
★(7) ★戻せる ／ ★(8) ★61本を走らせない ／ ★(9) ★commit しない ／ ★(10) ★twoder 配下で python を動かさない
```

## 6. ★★これが 通ると ★段4 は どうなるか

```
★★段4 の 戻す条件は ★2つでした（★MGR 10:40 §3）:
   ★(1) ★届いた文が 台帳の値で 引ける → ★★済（★本日 15:00 に 4つとも 通過）
   ★(2) ★置いた／置いていない が 台帳の値で 決まる → ★★本件で 満たります
★★★∴ ★本件が 通れば ★★段4 は ★『完了』と 書ける 段階に なります。
★★★★但し ★書くのは ★MGR です。★★私は ★受入が 出た後に ★もう一度 独立に 測ります。
★★★★★そして ★MGR §5 の 但し書きは ★★生きたまま です ――
      ★『捨て実装で20本 全通』の ★捨て実装そのものは ★依然 repo の外です。
      ★★∴ ★★『段4 完了』は ★『届いた文』と『置いたか』の2つに ついてのみ です。
```

## 7. ★★私が 言っていないこと

```
★『207 の原因が 分かった』―― ★★決まりません（★§1）。★調べるのも やめます。
★『found=false なら どこにも 無い』―― ★★5 repo の外は ★見ていません（★§3）。
★『段4 が 完了した』―― ★★本件が 通った後・★MGR が 書く事です。
★『(B) が 間違っていた』―― ★★(B) は 正しい。★渡す範囲が 狭かっただけです。
```
