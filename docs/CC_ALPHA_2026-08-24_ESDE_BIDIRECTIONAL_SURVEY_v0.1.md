# ESDE 双方向構造探索 — §8 実装前調査 v0.1（実装0行）

- 起票: CC_ALPHA(監視) 2026-08-24
- 親: `ITEM-2DER-EVO-0104`
- 基準: Taka 指示 2026-08-24「ESDE 双方向構造探索メモ v0.1 を基準にする」

## ★0.1 先に1つ報告 —— 正本ファイルが disk に無い

`egl/docs/` に **「双方向構造探索」を含む文書は 0件**（grep 実測）。
∴ 本調査は **Taka 指示の本文**（§1 命題 / §3 例 / §5 一覧 / §8 の9項目 / §9 の6指標が含まれる）を
基準にした。★メモ本体を私が書き起こすことはしない（見ていない文書を正本として扱わない）。

---

# 1. §5 の構造情報 —— 既存欄との対応（項目①）

★**13項目中 9項目が EXISTS。新しい正式名称は1つも作っていない。**

| §5 の項目 | 判定 | 既存の欄 / event | 出所 |
|---|---|---|---|
| **原文** | **EXISTS** | `QUESTION_TYPED.source_text` | 逐語「原文そのまま（★切らない）」 |
| **kind** | **EXISTS** | `QUESTION_TYPED.kind` | 6語＋UNVERIFIED（FACT/CHANGE/SPEC/TEST/CONSTRAINT/GOAL）★fail-closed |
| **対象** | **PARTIAL** | `QUESTION_TYPED.action` / `.goal` | ★「導出できた時だけ」＝空の場合が在る |
| **上位概念への参照** | ★~~EXISTS~~ → **MISSING** | ~~`QUESTION_TYPED.parent_request_id`~~ | ★**訂正。§9.1 を見ること** |
| **下位概念への参照** | ★~~PARTIAL~~ → **MISSING** | ~~逆引きで導出可~~ | ★**訂正。§9.1 を見ること** |
| **横関係** | ★**MISSING** | — | `refs` は在るが**関係の型を持たない**（下記） |
| **根拠** | **EXISTS** | `record_evidence` → `QE-` ＋ `basis_kind` 9語 / `validation_mode` 6語 | `EVIDENCE_NEEDS_REF` で fail-closed |
| **gap** | **EXISTS** | `GAP_PRESENTED` ／ `DISPOSALS.OPEN_GAP` | — |
| **actor** | **EXISTS** | `record_actor` ＋ `ACTOR_ACTIONS` 5語 / `ACTOR_VIAS` 2語 | — |
| **disposal** | **EXISTS** | `dispose_question` ＋ `DISPOSALS` 4語 | RESOLVED/OPEN_GAP/REJECTED/MERGED_INTO |
| **finding** | ★**UNWIRED** | 専用の型は無い。ESDE は `raise_question` で明細に落としている | ★型として区別できない |
| **QUESTION_TYPED** | **EXISTS** | `record_typed` → `QT-…`（決定論 id） | ★既存 `QUESTION_RAISED` を1件も触らない |
| **他の2DER IDへの参照** | **EXISTS** | `QUESTION_TYPED.refs` = `[{kind,value,resolved}]` | `kind` は `2der_id`/`file`/`api`/`symbol` |

★★**`refs` は「何を指しているか」は持つが「どういう関係か」を持たない。**
`kind` は**参照先の種類**（id/file/api/symbol）であって、**上下・横の関係型ではない**。
∴ §5 の「横関係」は **表現できない**。★これが最大の欠損。

---

# 2. ★§1 の三方向 —— 既存機構での表現（項目③）

## 2.1 ★驚くべき発見 —— 語は既に在る

`rri.request_thread.EVENT_TYPES`（9語）に **`NARROWED` と `EXPANDED`** が**既に在る**。

★これは §1 の**分解**と**展開**そのものの語である。

**しかし ―― 書き手が 0件。**
`rri/` `twoder/` を全件 grep して、この2つを `_append` する関数は**1つも無い**
（hit は定数宣言と試験の逐語照合のみ）。

∴ ★**`NARROWED` / `EXPANDED` は UNWIRED**。★語だけ在って誰も書いていない。

| §1 の方向 | 判定 | 既存機構 |
|---|---|---|
| **上流分解**（建設→水→給排水→配管） | ★**PARTIAL / UNWIRED** | `parent_request_id` で**親は持てる**が、`NARROWED` を書く者が居ない |
| **局所展開**（配管→給排水→水回り→建設） | ★**PARTIAL** | 上の**逆引き**で辿れるが、専用の口も `EXPANDED` の書き手も無い |
| **横連動**（配管↔防水/電気/内装） | ★**MISSING** | 関係型を持つ欄が無い（`refs` は参照先の種類だけ） |

★★三方向を **1本の tree にしない**という要求は**満たせる** ——
`parent_request_id`（上下）と `refs`（横）は**別の欄**なので、最初から別の関係として保持できる。
★但し **横に型が無い**ので、いまは「関係が在る」までしか言えず「どういう関係か」が言えない。

---

# 3. account 系の役割（項目②）

★**目的は「直す」ことではなく「何種類の軸が混在しているか」の観測**（Taka 指示どおり）。

★**名前だけで先に分けない**ため、採択済み **61件すべての名前と件数**を見た。

| 実在するまとまり | 例（件数） | 系統 |
|---|---|---|
| **記録の種類** | 進捗の記録（**248件・最多**） | `AX-`（★1種のみ） |
| **開発の話題** | 監査調査(44) / 能力構築(37) / テスト(25) / 契約設計(24) / 入力検証(16) … | `LDET-`（52種） |
| **依頼の種類** | 実装の依頼(7) | `ACC-` |
| **家計簿の科目** | 家計簿の付け方(2) / 交通費精算方法(2) / 引っ越し手続き(0) | `ACC-` |

★**上位層 `LCAT-` は 5件とも 0件** ＝ 階層が使われていない。
★旧の所在 = `twoder/account_gate.py:15` に逐語「**正典(canonical=旧のまま)**」＝ `ACCOUNT_AXES_v3.json`。

∴ ★**1つの欄に 少なくとも4種類の軸が同居**している。
★★`account` を Top-Down ESDE の**正本軸として使わない**（Taka 指示）は、この実測と整合する。

★私の訂正: 最初「2軸」と報告したが、**全件を見て4種類**だった（8件で断定したのが早すぎた）。

---

# 4. 他ID参照 / 後付け（項目④⑤）

| 項目 | 判定 | 実測 |
|---|---|---|
| ④ 他ID参照を既存 `ids.resolve` 系で表現 | **EXISTS** | `refs[].kind="2der_id"` ＋ `resolved` True/False/None。`twoder/ids.py` の解決口を使う。★**新IDを作らずに指せる** |
| ⑤ 後付けで構造情報を足す | **EXISTS** | `record_typed` 逐語「**既存の QUESTION_RAISED は1件も触らない／消さない**」。★追記式で本文を書き換えない |

★★⑤は**既に実証済み**の作法。★新しい event type を足さずに `QUESTION_TYPED` を後付けした前例が在る。

---

# 5. 責務境界（項目⑥）

★**確定しない**（Taka 指示）。実測で「整合しそうか」だけ見た。

| 仮の分担 | 実測との整合 |
|---|---|
| RRI：情報の取得・既存情報への接続 | ○ `request_thread` が明細・根拠・参照を持つ |
| ESDE Domain：探索方向の選択・構造の照合・gap の認識 | ○ `domain_esde` が既に「何を測るか」を決めている |
| Ledger Domain：事実・関係・根拠の管理 | △ `domain_ledger` は分類/証拠/処分を持つが**関係**の管理は無い |
| Worker：個別の観測・探索 | ○ `s_esde_evaluate` が該当 |
| General：状態集約・dispatch・Domain間調整 | ○ `to_domain` が5 Domain を捌く |

★**唯一ずれるのは「関係の管理」** —— どの Domain も横関係を持っていない（§2 の欠損と同じ場所）。

---

# 6. 決定論 / LLM の分離（項目⑦）

| 判定 | 決定論で可能か | 根拠 |
|---|---|---|
| 既存IDが存在するか | ★**可能** | `refs[].resolved` が既に True/False/None を返す |
| 既存関係が登録されているか | ★**可能（上下のみ）** | `parent_request_id` の有無。★横は欄が無い |
| 上流・下流双方から同じIDへ到達したか | ★**可能** | `parent_request_id` の正引き＋逆引きで集合を作り交差 |
| gap が空いているか | ★**可能** | `GAP_PRESENTED` / `OPEN_GAP` の有無 |
| どの上位概念で囲うべきか | ★LLM 候補生成 | ★昇格条件は後で設計（今回は設計しない） |
| 新しい横関係が意味的に成立するか | ★LLM 候補生成 | 同上 |
| どの軸で分解すると有効か | ★LLM 候補生成 | 同上 |

★★**上4つが決定論で出せる**のは重要 —— §9 の指標①②④が**LLM なしで測れる**。

---

# 7. Knowledge Supply との接続点（項目⑧）

★**主軸にしない**（Taka 指示）。★下流の取得手段として保持する。

```
ESDE が gap / information need を認識
      ↓  ← ★接続点はここ1つだけ
RRI / Knowledge Supply（既存）→ source → evidence
      ↓
構造へ戻す（record_evidence で明細へ）
```

★実測 = 既に作った `esde_packet_candidates` / `esde_packet_applicable` /
`esde_project_into_packet` は**この下流側にそのまま置ける**。★廃止は不要。
★但し **候補選択の鍵に account を使っている** ∴ §3 の混在の影響を受ける。
★**鍵の見直しは、構造探索側で「関係」が持てるようになってからでよい**。

---

# 8. §9 の最小実証は現状で可能か（項目⑨）

| 指標 | 現状で記録できるか |
|---|---|
| ① 発見した不足数 | ★**可能**（`GAP_PRESENTED` / `OPEN_GAP` を数える） |
| ② 既存IDへの接続数 | ★**可能**（`refs[].resolved=True` を数える） |
| ③ 根拠付き情報数 | ★**可能**（`QE-` の件数、`basis_kind`/`validation_mode` つき） |
| ④ 重複・矛盾数 | ★**部分的**（重複は id の決定論で分かる。★矛盾を表す語は無い） |
| ⑤ 追加質問後に埋まった項目数 | ★**可能**（`HUMAN_REPLIED` / 追記の差分） |
| ⑥ 実際に使われた項目数 | ★**部分的**（DW への投影は `cites_source_ids` で辿れるが、Qwen が使ったかは分からない） |

| 実証 | 可能か |
|---|---|
| **A. 上流分解のみ** | ★**部分的** —— `parent_request_id` が在る明細でのみ。★`NARROWED` は書かれていない |
| **B. 局所展開のみ** | ★**部分的** —— 逆引きで辿れるが専用の口が無い |
| **C. 両者＋横連動** | ★**不可** —— **横関係を持つ欄が無い** |

★★∴ **A/B は既存機構で記録できるが、C は今の 2DER では実施できない。**

---

# 9. ★最後に —— 欠けているのは1か所だけ

★13項目中9が EXISTS、④⑤は実証済みの作法が在り、⑦の決定論4件も出せる。

**欠けているのは「関係の型」1つ。**

| 在るもの | 無いもの |
|---|---|
| 上下の親（`parent_request_id`） | ★**横の関係型** |
| 参照先の種類（`refs[].kind`） | ★**関係の種類**（作用/依存/対比…） |
| 分解・展開の**語**（`NARROWED`/`EXPANDED`） | ★**その書き手**（0件） |

★★つまり **骨格の部品はほぼ揃っていて、繋ぐ線が1種類だけ無い。**

## 次に裁定が要ること（★私は決めない）

1. **横関係をどこに持たせるか** —— `refs` に型を足すのか、別の event にするのか。
   ★どちらも「新しい語」を作ることになる ∴ 裁定が要る。
2. **`NARROWED` / `EXPANDED` を使うか** —— 語は既に封印済みで在る。
   ★書き手を足せば §1 の2方向は動く。★但し「誰が書くか」は責務の話。

   ### ★★追記（裁定②の材料・2026-08-24 追加調査）
   出所は **`SPEC_RTHREAD_STAGE1_v0.1.md`**（明細の元仕様・235行）。
   ★しかし **その仕様書の中に、この2語が何を記録するのかの定義文が無い。**
   在るのは `EVENT_TYPES` の並び（42-43行）だけ。

   一方 **状態の `NARROWING` は定義されている** —— 遷移表に5本の遷移が書かれ、
   `("SOFT","NARROWING")` は `request_type_ok` / `bind_context_ok` を守り条件に持つ。

   ∴ **同じ「絞る」でも、状態は仕様が在り、出来事は語だけ在る。**

   ★★これは裁定②の性質を変える ——
   「書き手を足す」のではなく **「意味を与える」** ことになる。
   ★未定義の封印語に後から意味を割り当てるのは**設計判断** ∴ 私は決めない。
   ★併せて、`STATE_ADVANCED` が状態遷移を記録しているので、
   **`NARROWED` は `STATE_ADVANCED` と重なる可能性**が在る（★同一性の分岐候補）。
3. **§9 C の実証は 1 の後** —— 横が持てるまで C は測れない。

★★新台帳0 / 新state0 / 新ID族0 / 新しい正式な軸語彙0 / 新Worker0 / **実装0行**で本調査を終えた。

---

# 9.1 ★★訂正 —— 「上位概念への参照は EXISTS」は誤りだった（Taka 裁定③の実証で判明）

## 何を間違えたか

私は §1 の表で `parent_request_id` を「**上位概念への参照 EXISTS**」「これが**分解の親**そのもの」と書いた。
★**欄の名前から意味を推測しただけで、値を1件も見ていなかった。**

## 実測（★全 thread・全 typed = 1198件）

| `parent_request_id` が指す先 | 件数 |
|---|---|
| **`TASK-…`** | **1197（99.9%）** |
| **他の `QT-`（明細どうし）** | **0** |
| `Q-…`（問い） | 0 |
| `None` | 1 |

★∴ `parent_request_id` は **TASK を指す1段だけ**であり、**明細間の階層ではない**。
構造は **TASK → 明細 n件のフラットな1段**で、**木になっていない**。

## §9 A/B の実証結果（Taka 裁定③・`TASK-2DER-3339A186` / typed 58 / refs 14）

| 指標 | A（上流分解） | B（局所展開） |
|---|---|---|
| 到達した明細 | **0** | **0** |
| ①不足数 | 0 | 0 |
| ②既存IDへの接続数 | 0 | 0 |
| ③根拠付き情報数 | 0 | 0 |
| ④重複数 | 0 | 0 |
| ⑤追加質問後に埋まった | ★測れない | ★測れない |
| ⑥実際に使われた | ★測れない | ★測れない |

★★**A と B は「差が出た」のではなく「どちらも成立しなかった」。**
降りる先も昇る先も無いので、**探索経路として区別できない**。
★対象は機械で選んだ（typed と refs を両方持つ最大の1本）。★文章品質は評価していない。

## ★∴ 欠損は1つではなく2つ

| | 状態 |
|---|---|
| **横（`refs` に関係型）** | ★**足せる** —— §9.2 の実測で成立を確認 |
| **上下（明細間の親子）** | ★**そもそも無い** —— これが A/B が成立しない**唯一の理由** |

★★**横だけ足しても §9 A/B は測れない。**

# 9.2 ★① refs 調査の結果 —— 「関係型1欄の追加」で成立する

| 観点 | 実測 |
|---|---|
| **writer** | **1本だけ**（`request_thread.py:253` の `record_typed`）。★ref の中身を**検証していない**（`kind` は fail-closed だが `refs` は素通し） |
| **reader** | `list_typed` は `refs` を**丸ごと**コピー（内側の鍵を絞らない）／`detail_refs.summarize` は `kind`/`value`/`resolved` だけ読む／UI JS は `resolved` のみ |
| **未知欄の扱い** | ★**無視される** —— `relation` を足した ref を `summarize` に通して正常動作（total 2 / alive 1 / missing 1） |
| **往復** | ★**保たれた** —— `relation="LATERAL_AFFECTS"` を `record_typed` で書き、`list_typed` で**そのまま戻った**（`QT-c7c3e968`） |
| **identity** | `typed_id` は決定論（`_mint`）。★ref 自体に id は無い ∴ **関係に独立した id は付かない** |
| **根拠 / actor / disposal との関係** | いずれも**別の event**（`record_evidence` / `record_actor` / `dispose_question`）で、`refs` とは**結び付いていない** |

★∴ **独立 event は不要。** 横関係に独立したライフサイクルが要るという実測は**まだ無い**
（★根拠・actor・disposal が `refs` と結び付いていないので、そもそも横関係だけを処分する経路が無い）。

# 9.3 ★裁定に戻す最小要素 —— 1つだけ

**「明細間の親子」が存在しない。**

★これが A/B が成立しない唯一の理由。★横（`refs` の関係型）は足せるが、**上下が無いと A/B は測れない**。

★私は**どう足すかを決めない**。観測された事実は次の2つだけ:

1. `parent_request_id` は **TASK 専用**として 1197件で使われている ∴ **意味を変えると既存記録が壊れる**
2. `refs` は **未知欄を通す**ことが実測で分かっている ∴ **上下も `refs` の関係型で表せる可能性**が在る
   （★但しこれは①の裁定「横連動は refs 拡張」の**範囲を超える**ので、裁定が要る）
