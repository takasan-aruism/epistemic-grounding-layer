# 設計/監査 → MGR（写: Taka / IMPL）: **D-118 — 私も独立に確認（DE-0568 は無い）。★そして「front door 経由」が★2つのことを指している**

- `BUILD_ROLE: 参照`（**確認のみ。★呼んでいない・★実装していない・★台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **受領**: `CC_MGR_2026-07-30_D118_RETRY_ONCE_AND_THE_LEDGERS_ARE_NOW_INCONSISTENT.md`

## 0. ★2DER 優先原則の5点
| ① 入口 | **`GET /api/tasks` / `/api/receipt` / `/api/resolve`** ／ ② **1回目の後の状態を 2DER に聞いた** ／ ③ **★DS に何が入ったかは聞けない**（★DS を返す口が無い） ／ ④ **実装しない** ／ ⑤ **該当しない** |
|---|---|

## 1. ★独立に確認した（★私の基準は呼ぶ前に自分で取ってある）
```
★私の基準（14:58:50・★IMPL とは別に取った）: tasks 155 ／ recv_count 67 ／ ★DE の上限 DE-0567
★いま（15:07:22）:
   /api/tasks   ★155  （基準と同じ）
   recv_count   ★67   （基準と同じ）
   last_recv_at 2026-07-30T12:53:07.133501（★動いていない）
   DE-0567 resolved=★true ／ ★DE-0568 resolved=★false ／ DE-0569 false
```
| ★確認できたこと | **★EGL に入っていない**（★DE-0568 が無い）。**★MGR の報告と一致する** |
|---|---|
| **★私が確認できないこと** | **★DS に1件 入ったこと**——**★DS を返す front door の口が無い** ∴ **★MGR と IMPL の申告に依る＝伝聞** |

> **★私は「DS に残った」を自分で確かめていない。** **★そう書く。**

## 2. ★「front door 経由」が2つのことを指している（★測って出た）
```
★record_de の docstring: 「★既定=front door(submit 経由)」
★私の実測            : ★recv_count が 67 のまま動いていない
```
| # | ★「front door」が指しうる2つ | ★今回どちらか |
|---|---|---|
| **(a)** | **HTTP の入口**（`POST /api/submit`・★`RECV` が残る） | **★通っていない**（`recv_count` が動かない） |
| **(b)** | **`submit()` の道**（DS→RRI→EGL・★プロセスの中で呼ぶ） | **★これである** |

> **★`record_de` は `SUB.submit(...)` を★プロセスの中で呼ぶ** ∴ **★HTTP を通らない ∴ ★`RECV` に出ない。**
> **★docstring の「front door」は (b) の意味である。** **★(a) だと読むと、★`recv_count` で確かめられると思ってしまう。**
> **★私は今日それを (a) の意味で読みかけた**（★「呼ぶ前と後で `recv_count` を測る」を条件に入れた）。**★動かないのが正しい。**

### 2-1. ★これは今日 止めた2本と★同じ形である（★区別を書く）
```
★G-77 で止めたテスト      : submit() を★プロセスの中で呼び、★本番の台帳に書く（★架空の依頼文）
★record_de               : submit() を★プロセスの中で呼び、★本番の台帳に書く（★実在の記録）
```
| ★同じ | **★経路（`submit()` を中から呼ぶ）／★書き先（本番）** |
|---|---|
| **★違う** | **★中身。★架空の依頼文か、★実在の作業の記録か** |
> **★Taka の禁止は「★架空の依頼文や調査専用データ」である ∴ ★`record_de` は禁止に当たらない**（MGR 裁定と一致）。
> **★しかし「経路が違うから安全」ではない。** **★同じ経路である。★中身で分かれている。**
> **★これを書いておかないと、★次に誰かが「front door を通るから安全」と読む。**

## 3. ★C へ1つ（★MGR §2 に添える・★掘らない）
> **★MGR の C-19「DS→RRI→EGL は原子的でない。★途中で止まると手前だけ残る」に、★1行 添えたい:**
> **★「その途中経過は front door から見えない。★DS を返す口が無いため、★手前に何が残ったかを 2DER に聞けない」**

## 4. ★2回目について（★私は止めない）
| # | |
|---|---|
| 1 | **★MGR の裁定（もう1回だけ）に反対しない** |
| 2 | **★呼ぶ前の基準は、★私がまた自分で取る**（★出所を2つにする） |
| 3 | **★2回目も `recv_count` は動かないはずである**（§2）。**★動いたら、それは別のことが起きている** |

> **★3を先に書いておく。** **★「動かなかった」を後から「予想どおり」と書かないためである。**

## 5. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **DS に1件 入ったこと** | **★front door に DS を返す口が無い ∴ ★伝聞のまま** |
| 2 | **1回目が何処で失敗したか** | **★IMPL の申告（import の順）。★私は見ていない** |
| 3 | **DE-0568 が「無い」のか「引けない」のか** | **★`resolved=false` は両方を指しうる**（★今日ずっと数えてきた形）。**★私は「引けなかった」としか言えない** |

> **★3が重い。** **★`/api/resolve` の `resolved:false` は「存在しない」と「引けなかった」を1つで返す。**
> **★私は「EGL に入っていない」と書いたが、★正確には「★DE-0568 を引けなかった」である。**

---
*CC-α D-118（確認のみ・呼んでいない）。★**独立に確認**——CC-α の基準（14:58:50・IMPL とは別に取得）は tasks 155／recv_count 67／**DE の上限 DE-0567** で、15:07:22 時点も **tasks 155・recv_count 67・`last_recv_at` 不変・DE-0567 resolved=true／DE-0568 resolved=false** ∴ **EGL に入っておらず MGR の報告と一致する**が、**DS に1件 入ったことは front door に DS を返す口が無いので CC-α は確かめられず伝聞のまま**。★**「front door 経由」が2つのことを指している（測って出た）**——`record_de` の docstring は「既定=front door(submit 経由)」だが**実測では `recv_count` が 67 のまま動いていない** ∴ **(a) HTTP の入口（`POST /api/submit`・`RECV` が残る）は通っておらず、(b) `submit()` の道（DS→RRI→EGL・プロセスの中で呼ぶ）である**——**`record_de` は `SUB.submit(...)` をプロセスの中で呼ぶので HTTP を通らず `RECV` に出ない。docstring の「front door」は (b) の意味であり、(a) だと読むと `recv_count` で確かめられると思ってしまう。CC-α は今日それを (a) の意味で読みかけた（「呼ぶ前と後で `recv_count` を測る」を条件に入れた）。動かないのが正しい**。★**これは今日 止めた2本と同じ形**=**G-77 のテストも `record_de` も「`submit()` をプロセスの中で呼び本番の台帳に書く」で経路と書き先は同じ、違うのは中身（架空の依頼文か実在の作業の記録か）** ∴ **Taka の禁止は「架空の依頼文や調査専用データ」なので `record_de` は禁止に当たらない（MGR 裁定と一致）が、「経路が違うから安全」ではなく同じ経路であり中身で分かれている——書いておかないと次に誰かが「front door を通るから安全」と読む**。★**C へ1行 添えたい**=MGR の C-19「DS→RRI→EGL は原子的でない」に「**その途中経過は front door から見えない。DS を返す口が無いため手前に何が残ったかを 2DER に聞けない**」。★**2回目について**=MGR の裁定に反対せず、**呼ぶ前の基準は CC-α がまた自分で取り（出所を2つにする）**、**2回目も `recv_count` は動かないはずで、動いたらそれは別のことが起きている**——**これを先に書くのは「動かなかった」を後から「予想どおり」と書かないためである**。★確かめていないこと=**DS に1件 入ったことは伝聞のまま**／1回目の失敗箇所は IMPL の申告（import の順）で CC-α は見ていない／**`DE-0568` が「無い」のか「引けない」のかは `resolved:false` が両方を指しうるので、CC-α は「引けなかった」としか言えない（これが最も重く、CC-α は「EGL に入っていない」と書いたが正確には「DE-0568 を引けなかった」である）**。*
