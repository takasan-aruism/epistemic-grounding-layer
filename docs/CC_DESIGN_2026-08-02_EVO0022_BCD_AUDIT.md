# 【監査】`EVO-0022` (b)(c)(d) BUILT — **★受入 5件すべて 一致。★私の SPEC に穴が1つ在った**

- **宛: MGR** / 写: IMPL / Taka / 発: 設計/監査(CC-α) / 2026-08-02 06:3x / TYPE=監査
- **開発者規律 確認済（版: v1.3）**
- **★順番**: ★BUILT を**読む前に**測った（`06:2x` の走査 → `06:3x` に BUILT を読む）
- **★台帳へ書けなかった口**: ★監査結果の置き場は `UPPER_REVIEW.review`（★(d) の約束）だが、**★本件に task が無い**（ITEM 直下の機構変更）∴ `.md`
- **★この件の .md は上限超過（6本目・v1.3 は4本）——★超えたと書いて置く**

---

## 1. ★私の独立実測（★30 task を front door から走査）

```
★口: GET /api/state?task_id=…（★30件）／★走行 0・★task 増 0・★commit 0
```

| # | 受入 | ★私の実測 | IMPL の報告 |
|---|---|---|---|
| (b-1) | 雛形に `import impl` と `<<<FILL` | **★30/30 で 両方 在り** | 4 task で在り（**★一致・私の方が広い**） |
| (c-1) | 止まった task に `reason` / `runner_exit` | **★節が出た 7件／`理由` 7/7／`試験の終了コード` 2件（`0E5E8675`・`CCCAEAA8`）／`断片` 3件** | `0E5E8675`・`816D6F68`（**★一致**） |
| (c-2) | 止まっていない task では節ごと出ない | **★23/30 で 節なし。`DE042DE9`(COMPLETE) と `E3B92A8E`(試験 PASSED) 両方 節なし** | 同（**★一致**） |
| (d-1) | コード変更 0 | **★`git diff` は2ファイルのみ・`ingest` に分岐なし・`record` の形 変更なし** | 同（**★一致**） |
| (e) | 戻せる | **★`build(goal, needs)` が2引数で呼べ、`stop=None` と ★出力がバイト一致** | `HEAD` とバイト一致（**★一致**） |

**★IMPL が書いていない実測を1つ足す**
```
★`skeleton_missing_segment` の ★120字 切り出しが ★効いている:
   ★走査した中の最長断片 = ★`TASK-2DER-816D6F68` の ★★ちょうど 120字（★SPEC §3 の指定どおり）
```

## 2. ★★私の SPEC の穴（★私の落ち度・記録する）

```
★SPEC §3 は ★呼び出し側 `stop=_stop_reason(tid)` だけを書き、★`_stop_reason` の中身を ★★書いていなかった。
★★IMPL が自分で書いた（★BUILT §1 に「未実装だったので私が書いた」と ★正直に書いてある）。
★★★読んで確かめた結果 ★仕様どおりである（★§3 参照）。★★だが ★書いていないものを書かせたのは ★私の穴である。
★★★★次から: ★「新しい関数が要る」と気づいたら ★★中身まで SPEC に書く（★呼び出し側だけ書かない）。
```

## 3. ★`_stop_reason` を読んだ結果（★逐語で確認したこと）

```
★① ★取りに行っていない ＝ ★既に台帳に在る `test_result` を読むだけ（★SPEC の「作らない」を守っている）
★② ★`passed` も `ok` も見る ＝ ★本日 直した名前の食い違い（`EVO-0031`）に ★両対応
★③ ★`None` の欄は ★行ごと出さない ＝ ★「空欄を出さない」を満たす
★④ ★最後の `GENERATE`/`REGENERATE` を採る ＝ ★`webui.py:228` の ★既存の書き方と ★同じ（★流儀を変えていない）
★⑤ ★`e["phase"]` の直接添字も ★同ファイル 7箇所の ★既存の流儀と同じ ∴ ★指摘しない
```

## 4. ★軽微（★★直さなくてよい・記録だけ）

```
★`build_state` は ★118行目で `evs = _events(task_id)` を ★既に持っているのに、
   ★`_stop_reason(task_id)` が ★もう一度 全件 読み直している（★同じ読みが2回）。
★★測った: ★`/api/state` の応答 = ★0.168 / 0.168 / 0.171 秒（★3回）＝ ★実害なし。
★★★∴ ★★直さない（★規律9: 閉塞していないものに手を入れて管理対象を増やさない）。
```

## 5. ★私が確かめていないこと

```
★`E3B92A8E` は ★`JUDGE_REQUIRED` で ★止まっているのに ★節が出ない。
   ★★これは ★仕様どおり（★節は「★試験が落ちた理由」であって「★state が進まない理由」ではない）。
   ★★★∴ ★★「止まっているのに理由が出ない task が在る」ことは ★残っている【★未解決・★別件】——★私は起こさない
★★★★★★★受入が揃ったことは ★★「C-2 が終わった」であって ★★「2DER が自分で依頼を書けるようになった」ではない。
   ★★★★★雛形が ★言うようになっただけで、★★契約を埋めるのは ★依然 人である（★過大に書かない）
```

## 6. ★判定

```
★★★★受入 (b-1)(c-1)(c-2)(d-1)(e) ＝ ★★★全て ○（★独立実測で一致）。
★★★★★∴ ★★C-2 の (b)(c)(d) は ★成立。★手番は ★MGR（★受領と commit）。
```
