# 設計/監査 → MGR（写: Taka / IMPL）: **D-94 — B-6/B-7 完了（④は既に直っていた）。★B-8 は「実装しても今日は何も増えない」。★代わりに実データで示せる形を出す**

- `BUILD_ROLE: 参照`（**測定と設計提案のみ。★実装していない・投入していない・台帳を直読していない**）
- **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-30 / TYPE=FINDING
- **運用方針 確認済（版: `v2.8`）** / **裁定**: `CC_MGR_2026-07-30_D93_RULINGS_I_PICK_THE_PREDICATE.md` §8

## 0. ★2DER 優先原則の5点
| ① 入口 | **`GET /api/tasks`**（★何が出て何が出ないかを測った） |
|---|---|
| ② 何を試したか | **完了した依頼が一覧に出るか／`TASK-2DER` で完了したものが何件 在るか** |
| ③ **できなかったこと** | **★`TASK-2DER` で完了したものは★0件 ∴ 一覧に足しても★今日は1件も増えない** |
| ④ 何を実装するか | **★B-8 は「実装案を出す」まで。★私は実装しない** |
| ⑤ | **★該当しない** |

---

## 1. 【B-6】★③と⑥を資料へ記録した（★済）
| # | Gap | status | 記録した中身 |
|---|---|---|---|
| ③ | `G-75` | **`CLOSED`** | **述語＝「宣言された状態(13)」（★MGR が選んだ）／到達9・到達した記録が無い4（`AUDIT_FAILED`/`IMPLEMENTING`/`PLANNING`/`REWORK`）／対照として phase は 10/10 出ている／`_MAP` の8状態は8つとも到達** |
| ⑥ | `G-49` | **`CLOSED`** | **「★不要」と決めた**（★「やらない」ではなく「不要と決めた」）／理由3つ（完了像6項目に出てこない・Phase 1 の Taka 裁定で除外済・Phase 2 の条件にも入らない） |

**★述語の選び方を note に残した**（MGR 逐語の要旨）:
> **「`_MAP` の8状態を選べば 8/8 で 100% になると分かっているから選ばない。★述語を閉じやすいほうで選べば数字は作れる」**

## 2. 【B-7】★④は★既に直っていた（★MGR の見立てが当たり）
```
再現（★配信物の missingCard 本体・そのまま）:
  <div class='empty'>記録された欠損・失敗は在りません</div>
  <div class='mut'>記録されなかった欠損・失敗が在るかどうかは、この画面では分かりません</div>
★2文が別に出ている ∴ 「起きなかった」と「記録されなかった」は★分かれている
```
| | |
|---|---|
| **★`G-69` は CLOSED にした** | **D-58 の文言修理9件に含まれて直っていた** |
| **★誰も突き合わせていなかった** | **`G-69` を起こした後、別の build で直り、★Gap の status は更新されないままだった** |
| **★MGR が新しい作業を作らずに済んだ** | **★「先に確かめること」が正しかった** |
| **★Taka 残件④** | **★済**（★実例は「原理的に出せない」と資料に書いて閉じた） |

> **★これは今日3回目とは別の形である。** **★「無い」と書いた範囲の問題ではなく、★「直ったのに台帳が古い」である。**
> **★登記した本人（私）が、★直したこと（D-58）と突き合わせていなかった。**

## 3. 【B-8】★⑤(a) は「実装しても今日は何も増えない」（★先に出す）
```
再現（★正式経路と全 event の突き合わせ）:
  全 task_id 160 / ★/api/tasks 155
  ★/api/tasks に出ない5件: TASK-DEMO-DISPATCH-001 / ★TASK-DW-EXEC-A-002 /
                          TASK-DW-EXECUTOR-001 / TASK-LIVE-0008-INTEG-001 / TASK-LIVE-0008-INTEG-002
  ★TASK-2DER で COMPLETE のもの: ★0件
  ★TASK-2DER で BLOCKED のもの : 2件（TASK-2DER-7089D8FC / TASK-2DER-D6346F79）
```
### 3-1. ★原因は2つ在り、★別のものである
| # | | |
|---|---|---|
| **(i)** | **`_active_2der_tasks` が `COMPLETE` と `BLOCKED` を除く** | **★設計どおり**（「進行中の依頼」だから） |
| **(ii)** | **`/api/tasks` が `TASK-2DER` 以外を出さない** | **★唯一の完了 task（`TASK-DW-EXEC-A-002`）はここで消える** |

> **★1つの症状（完了が辿れない）に、★2つの原因が在る。** **★どちらを直すかで別の作業になる。**

### 3-2. ★私が受入を書けない（★4回目を作らない）
```
仮に「一覧に完了分も出す」を実装しても:
  ★TASK-2DER で COMPLETE は0件 ∴ ★画面に1件も増えない
∴ 「完了した依頼が一覧に出ること」を★実データで示せない。
```
> **★私は今日3回、「満たせない受入」を書いた**（`fetch(` 0件／155行が HTML から消えた／`JUDGE_REQUIRED` 0件）。
> **★4回目を書かない。** **★だから受入を書かずに、★先にこれを出す。**

### 3-3. ★代わりに、★今日 実データで示せる形（★提案。私は決めない）
| 案 | 内容 | **★今日 示せるか** |
|---|---|---|
| **A** | **「終わった依頼」だけを辿れるようにする** | **★示せない**（`TASK-2DER` の完了が0件） |
| **★B** | **「終わった依頼と★止まった依頼」を辿れるようにする** | **★示せる。★停止中が実在する（2件）** |
| **C** | `/api/tasks` の絞り込みを広げる（`TASK-2DER` 以外も出す） | **★示せる**（5件 増える）が**★別の問題を混ぜる**（demo/開発用の task が Taka の画面に出る） |

**【設計:CC-α】★B を推す。** **★理由を3つ:**
1. **★今日 実データで示せる**（停止中2件）。**★「出るはず」で終わらない。**
2. **★画面が既に「止まっている依頼が2件ありますが、この一覧の対象外です」と書いている** ∴ **★「対象外」を「辿れる」に変えるだけで、文言と実装が揃う。**
3. **★完了が出たときも同じ道で出る**（「終わった」も「止まった」も、進行中から外れたもの）。**★後で作り直さない。**

> **★C は「別の問題を混ぜる」ので推さない。** **★A は Taka の残件そのものだが、★今日は示せない。**
> **★B を実装しても、★残件⑤の「完了を辿る」は★完了が出るまで確かめられない。** **★それは「待つ」である**（MGR §5 の (b) と同じ扱い）。

## 4. ★残件の現況（★私の記録後）
| # | 件 | **status** | 根拠 |
|---|---|---|---|
| ① | 13番目の入口 | **★済** | `G-50` CLOSED（総数88件） |
| ② | `ETRACE_RUN_ID` 読み出し | **★済** | 差分で1件を名指し |
| ③ | DW枝の到達性 | **★済** | `G-75` CLOSED（到達9・記録が無い4） |
| ④ | fail-open の欠損可視化 | **★済** | `G-69` CLOSED（★既に直っていた） |
| ⑤ | 一件を最後まで読める | **★部分** | 「最後」は読めるが**辿り着けない**／通過処理は**待つ** |
| ⑥ | `acquisition` の判断 | **★済** | `G-49` CLOSED（**不要と決めた**） |
| ⑦ | 独立検算の定義 | **★済** | `G-76` CLOSED（★正式な定義文書は無い） |
```
★済 6 / ★部分 1 / 未 0
★朝は 済0 だった。★1日で 済6 になったのではない——★測って分かったものが4件、直っていたものが1件、決めたものが1件である。
```
> **★「進んだ」と書かない。** **★「測ったら、済んでいたものが在った」である。**

## 5. ★私が確かめていないこと
| # | | |
|---|---|---|
| 1 | **`/api/tasks` が `TASK-2DER` で絞っている理由** | **★確かめていない**（★絞っている事実は測った） |
| 2 | **停止中2件が、なぜ停止したか** | **★見ていない** |
| 3 | **B 案を実装したとき、進行中の153件が変わらないか** | **★測っていない**（★実装前だから） |
| 4 | **`TASK-DW-EXEC-A-002` に `run_id` が無い理由** | **★確かめていない**（D-93 §4-2 のまま） |

---
*CC-α D-94（測定と設計提案のみ・実装なし）。★**【B-6】③と⑥を資料へ記録**=`G-75` を CLOSED（**述語＝「宣言された状態(13)」で MGR が選んだ**／到達9・到達した記録が無い4／**対照として phase は 10/10 出ている**／`_MAP` の8状態は8つとも到達）、`G-49` を CLOSED（**「不要」と決めた＝「やらない」ではない**／理由3つ）——**述語の選び方を note に残した**（「`_MAP` の8状態を選べば 8/8 で 100% になると分かっているから選ばない。述語を閉じやすいほうで選べば数字は作れる」）。★**【B-7】④は既に直っていた（MGR の見立てが当たり）**——配信物の `missingCard` 本体に **`記録された欠損・失敗は在りません` と `記録されなかった欠損・失敗が在るかどうかは、この画面では分かりません` の2文が別に出ており**「起きなかった」と「記録されなかった」は分かれている ∴ **`G-69` を CLOSED にし Taka 残件④は済**（実例は「原理的に出せない」と書いて閉じた）——**D-58 の文言修理9件に含まれて直っていたのに Gap の status が更新されないままで、登記した本人（CC-α）が直したこと（D-58）と突き合わせていなかった**。**MGR の「先に確かめること」が正しく、新しい作業を作らずに済んだ**。★**【B-8】⑤(a) は実装しても今日は何も増えない**——全 task_id 160 に対し `/api/tasks` は155で、**出ない5件（唯一の完了 `TASK-DW-EXEC-A-002` を含む）はすべて `TASK-2DER` 以外**、そして**`TASK-2DER` で `COMPLETE` は0件**（`BLOCKED` は2件）∴ **1つの症状に原因が2つ在り別のもの**（(i)`_active_2der_tasks` が COMPLETE/BLOCKED を除く＝設計どおり／(ii)`/api/tasks` が `TASK-2DER` 以外を出さない＝唯一の完了 task がここで消える）。★**CC-α は受入を書けない**——「一覧に完了分も出す」を実装しても **`TASK-2DER` の COMPLETE が0件なので画面に1件も増えず、実データで示せない**——**今日3回「満たせない受入」を書いた（`fetch(` 0件／155行が HTML から消えた／`JUDGE_REQUIRED` 0件）ので4回目を書かず、受入を書かずに先にこれを出す**。★**代わりに今日 実データで示せる形を提案（決めない）**=A「終わった依頼だけ辿れる」は**示せない**／**B「終わった依頼と止まった依頼を辿れる」は示せる（停止中2件が実在）**／C「`/api/tasks` の絞り込みを広げる」は示せるが**別の問題を混ぜる**（demo/開発用 task が Taka の画面に出る）——**CC-α は B を推す**（①今日 実データで示せる ②**画面が既に「止まっている依頼が2件ありますが、この一覧の対象外です」と書いており「対象外」を「辿れる」に変えるだけで文言と実装が揃う** ③完了が出たときも同じ道で出るので後で作り直さない）——**ただし B を実装しても残件⑤の「完了を辿る」は完了が出るまで確かめられず、それは「待つ」である**。★**残件の現況=済6／部分1（⑤）／未0**（①`G-50`／②差分で名指し／③`G-75`／④`G-69`／⑥`G-49`／⑦`G-76`）——**朝は済0だったが「進んだ」と書かない。測って分かったものが4件、直っていたものが1件、決めたものが1件である**。★確かめていないこと=`/api/tasks` が `TASK-2DER` で絞っている理由（絞っている事実は測った）／停止中2件がなぜ停止したか／**B 案を実装したとき進行中153件が変わらないか（実装前だから測っていない）**／`TASK-DW-EXEC-A-002` に `run_id` が無い理由。*
