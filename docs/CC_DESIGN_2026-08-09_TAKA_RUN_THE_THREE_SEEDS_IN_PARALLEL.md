開発者規律 確認済(v1.0)

# 【指示】★3-seed 合議を ★並列で引く（★Taka 指摘）／ ★併せて Taka の設計原則を1つ登記

宛: MGR ／ 発: DESIGN（監視兼務）／ 2026-08-09 07:33 ／ 台帳: `ITEM-2DER-EVO-0049`

## 1. ★Taka 逐語

> LLM ってコンテキストが生きてれば同じ質問繰り返すと答えが変わるしね。**決定論が重要な場面で LLM を用いるという場合はどうしてもメニューを用意して選んでね、とする方法が一番安定する**とは思う。**ただこれは機械的な決定論とは違う。その意味でほぼ揺れがなくなる、ということを前提におく必要がある**。**設計センスが問われるとすれば、決定論で解決できない問題か？ があって、それが NO であるならば、であれば時点として揺れを抑えた LLM を使うのがセンスいい回答、で問題ない。3回という点に関しては並列処理すればタイムロスも軽減される。**

## 2. ★直すこと（★1点・★小さい）

```
★実測（逐語）: rri/rri/intent_strategy.py:175
   runs = [resolve(raw_input, anchoring, supporting_refs, context_text, seed=s) ★for s in seeds]
   ＝ ★★3回を 直列に引いている ∴ ★所要も約3倍
★★入口で 毎回 走る場所（submit の段3e）∴ ★Taka の体感に直接 効く
```

**★3つを同時に投げ、揃ってから多数決する。**
- **★判定の規則は1文字も変えない**（★3のうち2以上が止まる側なら止める）。
- **★票数の記録も変えない**（★CHG-0144 で入った欄をそのまま使う）。
- **★新台帳0 ／ ★新エンドポイント0 ／ ★:8005 の直叩きを増やさない**（★既存 `_llm` をそのまま3本 呼ぶ）。

**★受入**
```
★(1) ★同じ入力で ★結果（strategy・票数）が ★直列版と一致すること（★規則を変えていない証拠）
★(2) ★所要時間を ★直列版と並べて書くこと（★何秒→何秒）
★(3) ★1本が失敗した時の扱いを1行 書くこと（★2票で決めるのか・★止めるのか）
      ―― ★「答えられなかった」を「答え」に混ぜない（既存規律）
```

## 3. ★併せて登記（★Taka の設計原則・★今後の判断基準）

```
★順序を固定する:
  ★① ★「これは 決定論で解けるか？」を ★先に問う
  ★② ★解ける → ★決定論で書く（★LLM を使わない）
  ★③ ★解けない → ★★揺れを抑えた LLM を使う（★これがセンスのいい回答であり、妥協ではない）

★★LLM を使う時の安定化の第一手 = ★★メニューを用意して選ばせる（自由記述にしない）
★★ただし これは ★機械的な決定論ではない ∴ ★★「ほぼ揺れない」を前提に置く
   → ★票数・seed・合議の結果を ★必ず記録に残す（★「ほぼ」の中身を後から読めるようにする）
```

**★既に家の作法になっている（★確認済・変えないこと）**
```
応じ方=7択 ／ 依頼の種類=6択 ／ 監査の指摘=13種 ／ 勘定科目=凍結軸から選ぶ（新規はその場で作らない）
```

## 4. ★注意

**本件は ★速さの改善であって ★正しさの改善ではない。★(1) が満たせない実装なら 出さないこと。**
