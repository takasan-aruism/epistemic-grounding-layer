# 【受領＋順序の確定】**★10本を回すのは私（設計/監査）である** — ★本番1回の★後でないと回せない

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-07-31 19:3x / TYPE=FINDING
- **運用方針 確認済（版: v2.9）** ／ **受領**: `D-182`（★母数は「対」／★本番1回では判定しない）
- **★新しい名前で置いた** ／ **★短く書く。★手番の取り違えを潰すだけである**

## 1. ★裁定を受ける（★両方とも採る）
```
★母数の単位 = ★「対」。★以後 統一する。★報告に両方 書かせるのはやめる（★私の §4-2 を撤回する）
★本番1回は ★判定に使わない。★決定論の確認（★P-1〜P-3）だけに使う
```

## 2. ★★手番が未確定だった1点（★これを潰す）
```
★`D-182` §2 の表: 「★:8005 単体で10本」——★誰が回すかが書かれていない。
★★私が IMPL へ出した SPEC（`ART-7f814715c1` §8）には★逐語でこう書いてある:
   「★`:8005` は 2DER が内部で呼ぶ。★あなたは直接 叩かない」
∴ ★★IMPL は回せない（★回したら私の SPEC 違反になる）。
★★★∴ ★回すのは ★私（設計/監査）である。★前回の38本も私が回した（★2DER の外・本番経路ではない）。
★★これは★実装ではなく★監査の測定である ∴ ★私の担当で運用方針に反しない。
```

## 3. ★★順序（★これが逆だと回せない）
```
★① IMPL: ★webui 再起動 → ★依頼文1回 投入 → ★run_next 1回 → ★PLAN で停止 → ★BUILT を出す
★② 私: ★BUILT を読む前に ★自分で本番の結果を測る（★P-1〜P-3 の当否）
★③ 私: ★★その task の provenance を `/api/resolve` で取り、★規則入りの最終 prompt を★描画する
★④ 私: ★その prompt を ★:8005 単体へ ★10本（★並列・★同時実行数を記録）→ ★母数（対）と ×
★⑤ 私: ★規則の前（★109対中 ×14）と★並べる。★動かなければ「★動かなかった」と書く
★★★③は ★①の後でしか作れない（★新しい task は ★新しい観測 id と ★新しい値を持つ）
   ∴ ★★私は ①より前に回せない。★先回りしない。
```

## 4. ★測る前に固定してあるもの（★動かさない）
```
★語彙表: `ART-7f814715c1` §3-2（★英＋日。★主題語を入れない）★← ★足さない
★物差し: ★母数＝対 ／ ★成果物側で道具名の前後160字に語彙表の語が★1つも無ければ ×
★前の値: ★109対中 ×14（★:8005 単体・A/B/C1/C2 合算38本）★← ★経路は同じ、★母数は違う ∴ ★率で並べる
★★成功の定義（★`D-182` §3・★あなたが固定した）: ★「作らなくなった」も「×0件」も★成功にしない
```

## 5. ★やっていないこと
```
★10本を★まだ回していない（★①待ち）／ ★本番へ投入していない ／ ★コードを1行も変えていない
★★実装の差分は★確認だけした（★1ファイル・挿入1行・★文言は SPEC の逐語・★禁止語0件・
   ★`STRUCTURED_KEYS`/`EXECUTABLE_KEYS`/`validate_plan` の出現0件）。★私は触っていない
★★保留はそのまま: Ledger ／ 図 ／ (c) の patch ／ GENERATE
```

---
**決めたこと**: **①`D-182` の裁定を両方 採る（母数は「対」に統一し、報告に両方 書かせる私の §4-2 は撤回。本番1回は決定論の確認だけに使う） ②10本を誰が回すかが書かれていなかったが、私が IMPL へ出した SPEC が「`:8005` を直接 叩かない」と明示している以上 IMPL は回せない ∴ 回すのは私（設計/監査）で、これは実装ではなく監査の測定である ③順序を固定した——本番1回が先で、その task の provenance から規則入りの最終 prompt を描画しないと10本は作れないので、私は先回りしない ④語彙表・物差し・前の値（109対中 ×14）・成功の定義はすべて固定済で動かさない ⑤実装の差分は確認だけした（SPEC どおり）。私は触っていない。**
