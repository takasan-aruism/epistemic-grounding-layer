# 宛: Taka / 設計 / 監査 ―― EVO-0077 `task_ids` を誰がいつ埋めるか（設計のみ・実装 0）

**新 ID・新台帳・新しい対応表を作っていない。実装していない。**

## 0. 結論を先に

```
★`task_ids` を 書ける 関数は `register_item` ★1つだけ（★item を 作る時のみ）
★`register_item` を 呼ぶ 本線は `submit.py` ★1つだけ
★その 呼び出しは `task_ids` を ★渡していない
★既存 item の task_ids を ★更新する 口は 無い（`set_status` は status と note だけ）
```

**∴ 27/27 が空なのは★構造上必然。** 誰かが埋め忘れたのではない。

## 1. 六問への回答（すべて実装を読んで確定）

### ① ITEM から TASK が最初に生成・投入される瞬間はどこか

**`/api/submit`（front door）。** task はここで作られる。
ただし **契約文書には item への参照が無い** ―― 「この契約は EVO-0019 のもの」を知っているのは
**投入した MGR だけ**（今夜の全走行で、私が `status_note` に手で書いていた）。

### ② item_id と task_id の両方を同時に持つ既存処理はどこか

**`twoder/submit.py` の中。1箇所だけ。**

```python
_prog = progress_seal.extract_progress(raw_input)      # ★item_id は ここに 在る
if _prog:
    if _RM.resolve(_prog["item"]) is None:
        _RM.register_item(_prog["item"], _prog["phase"], ..., ts=ts)   # ★task_ids 無し
```

**進捗マーカー（`progress_seal`）が在る投入だけが item_id を持つ。**
**マーカーが無い投入（＝今夜の契約投入すべて）は item_id を持たない。**

### ③ 投入時／受領時／完了時のどこで記録するのが最小で一意か

**★投入時（submit）。** 理由は実測:

```
受領時・完了時は ★task から item を 引く 手段が 無い
  （TASK の state に item への 参照欄が 無い＝2026-08-19 実測）
∴ 後段では ★誰かが 記憶で 補うしかない = ★今夜 私が やっていたこと
```

**投入時なら両方がその場に在る。後段は逆引きが不可能。**

### ④ 複数 TASK を持つ ITEM は append か replace か

**append。** 実測: `EVO-0019` の履歴には **8個**の task id が現れる（v1〜v4 の作り直しを含む）。
**replace にすると、直前の1本しか残らず、失敗の系列が消える。**

### ⑤ 再試行・失敗 task・破棄 task を残すか

**残す。** 今夜の実測が根拠:

```
v2 は PLAN を抜けて test_failure ／ v3 は PLAN barrier ／ v4 は COMPLETE
→ ★3本を 並べたから 対照実験が 成立し「骨格 +232B」が 特定できた
→ ★失敗を 消していたら 原因は 出なかった
```

**削除経路を作らない**（今夜「削除経路が無い」を負債として記録済みだが、ここでは**長所**として働く）。

### ⑥ 散文履歴2件を構造化へ移すか

**★過去は触らない。今後だけ埋める。**

```
理由① 台帳は append-only（★過去の行は 書き換えない＝正本の作法）
理由② 散文2件は ★一意に対応できない（投入・受領・失敗が混在）
       → 移すには ★私が 文を読んで 選ぶ ＝ ★新判断（Taka が 禁じた）
```

## 2. 最小経路 ―― **既存の書き込み口では実現できない**

Taka の条件「既存の書き込み口で実現できるならその口だけ使う」を検査した結果:

| 既存の口 | task_ids を書けるか |
|---|---|
| `register_item` | **★新規作成時のみ**（既存 item には使えない） |
| `set_status` | **✗**（status と note だけ） |
| `set_authority` | **✗** |
| front door の口 18本 | **✗**（item への追記口は無い＝2026-08-18 実測） |

**∴ 既存では実現できない。** 足りないのは **「既存 item の `task_ids` に1件 append する追記」** 1つだけ。

```
★これは 判断では ない（★どの task を 足すかは submit が 既に 知っている）
★新台帳 0 ／ 新 ID 0 ／ 新しい 対応表 0
★台帳の append-only の 作法に 沿う（★既存行を 書き換えない）
```

## 3. 通ったら何が変わるか（受入）

```
① item_state() の `task_ids` が ★空でなくなる
② `task_turns`（whose_turn・TASK 層）が ★UNKNOWN でなくなる
③ TASK の state に `rthread_id` が 在るので ★ITEM→TASK→RRI が 一度に 引ける
   ＝ Taka の 前提「ITEM から TASK/RRI まで 一度に 見える」が ★満たされる
④ その後に acceptance 照合へ 進める
```

**★①〜③は「1件でも自動で埋まれば」測れる。** 過去27件を遡らなくてよい。

## 4. ★残る問い（私は決めない）

**進捗マーカーが無い投入は、いまも item_id を持たない。**

```
今夜の契約投入（★9本すべて）は ★マーカー無し = ★item と 結び付かない
→ 追記の口を 作っても ★契約経路は 埋まらない
```

**∴ 二択。裁定は Taka。**

```
α  契約の投入にも 進捗マーカーを 付ける（★MGR が 投入時に item を 名乗る）
   → 追記の口 ＋ 名乗りの 規約
β  追記の口だけ 作り、まず ★マーカー付き投入 で 1件 埋まることを 実測する
   → 契約経路は 別件のまま
```

**私の見立ては β** ―― 「1件でも自動で埋まる最小経路」を先に実測でき、αの是非はその後に決められる。

## 5. していないこと

実装 0 ／ 新 ID 0 ／ 新台帳 0 ／ 過去の散文2件に触っていない ／ acceptance 照合に入っていない。
