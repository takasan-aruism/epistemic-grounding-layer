# CC 管理(MGR): **★Build 3 を止める（STOP・着手前）** — 計器が自分の活動を食っていた3例目（HANDOFF）

- `BUILD_ROLE: 参照` / 宛: IMPL(coder) / DESIGN/AUDIT(CC-α) / 写: Taka / 発: MGR / 2026-07-26 22:40 / TYPE=HANDOFF
- 直前: `CC_MGR_2026-07-26_BUILD3_GO.md` を**本文書が取り消す**

## 0. ★STOP（最優先・呼出が焼ける前に）
**IMPL: Build 3 の LLM 呼出を開始しない。開始済みなら止める。** 840 呼出は撤回する。
- 本 STOP は **CC-α の申告**（`P-ASSERTS-CONTEXT` は実入力に現れないパターン／除外フィルタが codegen プロンプト `# gen-nonce` を素通ししていた／回帰セットの正典は 298 でなく 182 の可能性）に基づく。
- **CC-α が IMPL へ直接 STATUS を先に置く**判断は**支持する**。通信規約上 設計→実装は隣接であり、**焼ける呼出を止めるのに MGR の往復を待つ必要はない。** 規約より実害の回避が上。**MGR の正式な停止は本文書。**

## 1. 何が起きたか（MGR の理解）
**計器が自分の活動を食う、の3例目。** 1例目=DE 散文が接地 corpus を汚染／2例目=DE を書くと母数が増える／**3例目=今回**。
- **今回が最も悪い**: 前2件は**量**の汚染だったが、今回は **corpus の「入力の性格そのもの」**が変わっていた。**我々が機械生成した codegen プロンプト（長大・自己言及的）が実発話として数えられていた。**
- ＝ **「実発話の 42% が400字超・平均3446字」は、我々自身のプロンプトを実発話と誤認した結果**である可能性が高い。

## 2. ★私（MGR）の責任範囲を先に書く
- **私は「42%・平均3446字」を Taka に2回報告した。** 実装の報告を受け、監査が通ったものとして上げた。**中身（ヒットの実物）を見ていない。**
- **CC-α が「率を出したらヒットを目視する規律を、他人の数字に適用しなかった」と自己申告したのと同じ誤りを、私も上流でしている。**
- **Taka への訂正は私が出す。** 訂正の材料が揃うまで、**この数字を使った主張を新たに出さない。**

## 3. 依頼（順番どおり・急がない。焼く前に正す）
### 依頼A（IMPL・決定論のみ・LLM 呼出なし）
1. **除外フィルタに codegen プロンプトを追加**（`# gen-nonce` 等）。**除外規則は列挙して記録**し、**negative control（除外すべきでないものを混ぜると RED）を付ける。**
2. **母数の系列を全部出し直す**: 生 → 除外（**規則ごとの件数**）→ dedup → 正典。**298 → 182 が正しいかを機械で確定する。**
3. **★今日出した「298件中」の数字を全件洗い出し、新母数で出し直す。** 対象は少なくとも: **P4 発火率 0/298**／**長文分布（42%・平均3446字）**／Build 2 の母数。**変わったものは「変わった」と書く。変わらなかったものも「変わらなかった」と書く。**
4. **【重要】これは訂正であって隠蔽ではない。** 数字が悪くなっても、良くなっても、そのまま出すこと。

### 依頼B（CC-α・設計判断）
1. **長文の実運用像を測り直した上で、Build 3 の優先順位を再評価する。** **400字超が 4% なら、長文は主たるリスクではない**——**優先順位そのものが変わる。** その場合 Build 3 は**やらない／後回し**が正しい結論であり得る。**SPEC を守るために実験を残さない。**
2. **`P-ASSERTS-CONTEXT` の妥当性を再検討する。** CC-α の整理（**「将来こう使うから」なら残す・「実運用の再現」としては使えない**）は正しい切り分けである。**どちらで残すかを明記すること。** 実データに無いパターンを「実運用の弱点」と呼ばない。
3. **既知の最大弱点（主張された文脈を検証せず受け入れる）そのものは消えていない**（独立3回の目撃は実在する）。**長文という入口が実運用的でないだけである。** **ではどの入口が実運用的か**を、**実データを見てから**決めること。

## 4. ★恒久規律に格上げする（3例目なので）
> **【設計:MGR】率・母数・分布を出したら、その母集団の実物を目視する。自分の数字にも、他人の数字にも、同じ規律を適用する。**
- CC-α は「率を出したがヒットを見なかった」を自分の誤りとして既に類型化していた。**その規律を"他人の数字"に適用していなかった**のが今回の穴であり、**私も同じ穴を上流で開けた。**
- **さらに**: **corpus を作る時は「我々が生成したもの」を除外する規則を先に書き、negative control を付ける。** 今日3回同じ形で刺されている。**次は機構で止める。**

---
*MGR。Build 3 を着手前に STOP（840 呼出を撤回）。CC-α の直接 STATUS 先行は支持。原因=除外フィルタが codegen プロンプトを素通しし、我々自身の生成物を実発話として数えていた（計器が自分の活動を食う3例目・今回は量でなく入力の性格が変わっていた）。依頼A=除外規則の修正と母数の全面出し直し（「298件中」の全数字）。依頼B=長文の優先順位そのものの再評価と P-ASSERTS-CONTEXT の位置づけ。Taka への訂正は MGR が出す。恒久規律=率を出したら母集団の実物を目視する（他人の数字にも適用する）。*
