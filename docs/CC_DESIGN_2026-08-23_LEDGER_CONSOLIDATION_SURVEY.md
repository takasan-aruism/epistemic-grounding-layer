# 台帳56本の棚卸し ―― 何が生きていて、何を統廃合できるか v0.1

発: ESDE Evaluation 専任監査（Claude）／ 宛: MGR・Taka
根拠: Taka 指示 2026-08-23「使ってない、そもそも書き手がいない台帳も多いのでその扱いには一段と検討が必要。車輪の再発明にならないように、あるもので使えるものは使う。いらないんじゃね？ または同じものだなこれ、というのは統廃合する流れでいい」
台帳: `ITEM-2DER-EVO-0090`

**★実装 0 行。★1本も消していない。★調査と候補の提示のみ。**

---

## 0. 判定の軸（★「読み手0」だけでは決めない）

読み手の検出は本日 MGR が直したばかりで、私の再測でも **`rthread_events` が W0/R0 と出た**（実際は reader 9件）。
∴ **検出器の値だけを根拠にしない。** 動かぬ事実で切る:

| 軸 | 出所 |
|---|---|
| **行数** | ファイルの実行数 |
| **最終更新** | `stat` |
| 読み手 | 参考にとどめる（検出器が直った直後） |

---

## 1. 全56本の分類（★分母つき）

```
A. 空（0行）                                3本
B. 7月で止まっている（8月に1バイトも書かれていない）  39本   ← ★70%
C. 本日も書かれている（生きている）           11本
   （A/B/C の重なりを除いた残り 3本は 8月に動いたが本日は静か）
```

**56本中39本（70%）が7月で止まっています。**

## 2. C ―― 生きている11本（★これが本体）

```
ds/data/event_trace.jsonl              1,946,049行   ← 実行の痕跡（本日 私が読み方を直した）
egl/data/events.jsonl                     28,081行
rri/rri_records.jsonl                      8,665行
ds/ds_events.jsonl                         6,935行
dev-workcell/events.jsonl                  4,749行   ← DW の本体
rri/rri/rthread_events.jsonl               3,361行   ← ★明細（いま改修中）
twoder/audit/ROADMAP_REGISTRY.jsonl        2,912行
egl/docs/CC_REGISTER.jsonl                 1,038行
egl/DESIGN_EVIDENCE_LEDGER.jsonl             939行
dev-workcell/data/pending_actor.jsonl        777行
twoder/failure_recurrence.jsonl              634行
```

**★この11本だけが「使われている台帳」である。** 残り45本は在庫。

## 3. ★統廃合の第一候補 ―― 同じ名前が repo ごとに散っている

```
DESIGN_EVIDENCE_LEDGER.jsonl   4本 / 8月も動いているのは ★1本だけ
    egl  939行 2026-08-23   ← ★生きている
    rri   12行 2026-07-07
    dw     7行 2026-07-07
    ds     3行 2026-07-06

REVIEW_LEDGER.jsonl            4本 / 8月に動いたもの ★0本
    egl   11行 2026-07-21
    dw     2行 2026-07-06
    ds     0行 ★空
    rri    0行 ★空

audit_backlog.jsonl            4本 / 8月に動いたもの ★0本
    egl   36行 2026-07-26
    ds     2行 2026-07-06
    rri    1行 2026-07-06
    dw     0行 ★空
```

**12本が3つの名前に重複しています。** そのうち **生きているのは `egl/DESIGN_EVIDENCE_LEDGER.jsonl` の1本だけ。**

`REVIEW_LEDGER` と `audit_backlog` は **4本とも 8月に1バイトも書かれておらず、合計 13行 と 39行。**

## 4. ★重複の変種 ―― 同じ内容の再実行が3本並んでいる

```
egl/docs/BREAKAGE_LIST_2026-07-23.jsonl          9行
egl/docs/BREAKAGE_LIST_2026-07-23_rerun.jsonl    9行
egl/docs/BREAKAGE_LIST_2026-07-23_rerun2.jsonl   9行
```

**同じ日・同じ行数・`_rerun` / `_rerun2` という名前。** 実験の再実行がそのまま登記簿に残っている。

同様に `egl/data_*/events.jsonl` が **6本**（`data_acq_live` / `data_acq_task` / `data_gate4` / `data_jrev0003` / `data_sleepmode_acq` / `data_sleepmode_claim`、合計 135行・全部7月）。
**実験ごとの作業ディレクトリが、登記簿では台帳として並んでいる。**

## 5. 扱いの候補（★私は決めない。★1本も消していない）

**★消す前に必ず確認すべきこと** ―― 本日の私の実測では、`artifact_registry.supersedes` のように
**「書き手だけ在って読み手0」でも、後から読み手を付ける価値がある欄**があった。
∴ **「読み手0だから消す」は使わない。** 判定は次の3つに分ける:

| 区分 | 該当 | 扱いの候補 |
|---|---|---|
| **①統合できる** | `DESIGN_EVIDENCE_LEDGER` 4→1 / `REVIEW_LEDGER` 4→1 / `audit_backlog` 4→1 | **9本減**。★中身は合計でも 60行程度 ∴ 移送は軽い |
| **②登記簿から外す（消さない）** | `BREAKAGE_LIST_*rerun*` 2本 / `egl/data_*/events.jsonl` 6本 / `SUBMIT_2026-07-21/**` 2本 | **10本減**。★実験の作業物であって台帳ではない。★ファイルは残す |
| **③保留（判断材料が足りない）** | 上記以外の7月停止分 20本 | ★何のために作ったかが分かるまで触らない |

**①+② で 56本 → 37本。★3分の1が消える。**

## 6. ★車輪の再発明を避ける（Taka 指示）

**新しい統廃合の仕組みを作らない。** 既にある物で足りる:

- `s10_ledger_registry.build()` が **登記簿を再生成できる**（本日 MGR が実行して 1 mismatch を解消した）
- ∴ **②「登記簿から外す」は `_is_nonprod` の条件に足すだけ**で、新しい台帳も新しい state も要らない
- ①「統合」は **移送先が既に在る**（`egl` 版が唯一生きている）

## 7. ★私が確認した本日の事実（判断の前提）

- MGR の reader 検出器の修理は **私の独立実測で支持**（登記器の自己参照 **0件**、明細の reader **9件**を目視で確認）
- ただし **私自身が一度「56台帳すべて reader 0件」と誤報**した ―― `path_owner()` の辞書を
  文字列リストとして渡した私の呼び出しバグ。**検出器は正常だった。**
  ∴ **本調査では読み手の数を主判定に使っていない。**

## 8. 私がしていないこと

★1本も消していない ／ 登記簿を書き換えていない ／ `_is_nonprod` を触っていない ／
★実装 0 行 ／ `EF6826DC` `ED65242E` に触れていない。
