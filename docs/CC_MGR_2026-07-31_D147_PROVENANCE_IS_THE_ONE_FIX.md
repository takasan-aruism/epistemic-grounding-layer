# 【D-147】確定 — **★押しても流れない。原因は PLAN 手前の provenance 不足。★次の1件を承認する**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 01:5x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-31_RUNTIME_INSPECTION_COMMON_BASE_ORDER.md`

## 1. ★D-146 の問いに答えが出た
```
★問い: 「押していないだけ」なのか「流れない」のか
★答え: ★押しても流れない。★ただし切れているのは Task→Runtime ではなく、★その手前である。
★PLAN は★実際に呼ばれ、★provenance 検査で fail-closed した（★:8005 は0件＝LLM の前で止まった）
★不足: trace_id / rri_request_id / rri_intent_id ★の3件
```
**★IMPL が「配線が壊れていることは示していない。示したのは到達しないことだ」と区別して書いたのが正しい。**

## 2. ★今回いちばんの収穫（★機構が良くなっていることの実測）
```
★planner_outcome が null ではなく、★失敗理由が3件 そのまま応答に載った。
★以前は同じ場所で「CLAUDE_BARRIER」の一語に潰れていた（★07-27 に我々が診断した形）。
∴ ★「失敗を正常に見える別の結果に置き換えない／理由を捨てない」が★実際に効いている。
★これは我々の主張ではなく、★今回の実測である。
```

## 3. ★次の1件を承認する（★正典「一度に一箇所」）

> ### **★観測経路の packet に、★既存の `rri.intent_record.mint` で `trace_id` / `rri_request_id` / `rri_intent_id` を入れる。**

| 条件 | |
|---|---|
| **1** | **★新しい ID 族を作らない。★既存の mint を使う**（BUILD 経路が既にやっていることを、観測経路にも通すだけ） |
| **2** | **★GPU 固有の語を入れない**（`gpu`/`nvidia` を diff に0件） |
| **3** | **★1箇所・最小差分。★他を直さない**（A-3・生出力・GPU 取得・選別は触らない） |
| **4** | **★後方互換**: `BUILD_CAPABILITY`/`MODIFY_EXISTING` 経路の provenance を変えない。★基準値は取る前に固定 |
| **5** | **★修正後、同じ検査を再実行**（★同一依頼文で投入1回 → `run_next` 1回） |
| **6** | **★PLAN が動いたらそこで止めて返す。★GENERATE へ進まない** |

## 4. ★次に分かること（★先に書いておく。★予想ではなく観測点）
```
★provenance が満たされたとき、PLAN は
   (a) planner で通るのか
   (b) それとも actor_id=CLAUDE / claude_barrier=true で★我々に止まるのか
★今回の応答には両方が出ている（planner が呼ばれた／barrier も立っている）。
★∴ どちらが本体かは、★今は決められない。★次に分かる。
★★(b) だった場合、★それは「2DER 単体で一周できるか」に直接 効く。★そのときは私が Taka へ上げる。
```
**★いま「Claude が要る経路だ」と書かない。★1回の観測で断定しない。**

## 5. ★変わらないこと
```
★この修正は IMPL が書く ∴ ★2DER の担当工程に数えない。★1/8 は動かない
★Ledger（生出力）／A-3（trace_key の prefix）／GPU 取得／選別 は★今回も触らない
★止まったらそこで終了。★直さない・迂回しない
```

## 6. 手番
```
★設計: §3 を SPEC にして IMPL へ
★私  : 結果を確定 → Taka へ5行
★Taka: 手番なし
```

---
**決めたこと**: **①押しても流れない。切れているのは Task→Runtime ではなく手前の PLAN で、原因は provenance 3件の不足（`:8005` は0件＝LLM の前で止まった） ②失敗理由が `CLAUDE_BARRIER` の一語に潰れず3件そのまま返った＝07-27 に診断した欠陥が実際に直っていることの実測 ③次の1件は「観測経路の packet に既存の mint で3つを入れる」。新しい ID 族を作らず、GPU 固有語を入れず、修正後は同じ検査を再実行する。**
