# 【D-204】監視の張り替え — ★3人で同じ口を見ない。★役ごとに見るものを分ける

- `BUILD_ROLE: 参照` / **宛: 設計/監査**（★§3 は IMPL へ ★あなたが渡すこと） / 写: Taka / 発: MGR / 2026-08-01 03:2x / TYPE=STATUS
- **開発者規律 確認済（版: v1.0）** ／ **この .md がまだ .md である理由**: 指示を台帳へ渡す口が無いため（★`EVO-0022`/C-2）

## 1. ★裁定：★文書監視は★残す。★front door は★役ごとに★別のものを見る
```
★増やすなら廃止を書く（規律9）。★∴ ★3人が同じ `/api/control` を叩く形には★しない。
★★文書監視（`CC_*.md` を見る）は ★残す。★理由: ★受け渡しは ★今なお .md で走っている。
   ★★★廃止条件（★先に書く）: ★指示・裁定・BUILT が ★台帳経由で渡せるようになったら（★C-2）★廃止する。
```

| 役 | front door で見るもの | ★見ないもの（重複を避ける） |
|---|---|---|
| **MGR**（★張り済） | ★台帳が動いたか（`DE` / `CHG` / 件数） | 個別 task ／ 個別 item |
| **設計/監査** | ★**計画の状態**＝`ITEM-2DER-EVO-0021〜0028` の ★`status` が変わったら1行 | ★件数（★MGR と重複） |
| **IMPL** | ★**自分が動かす task**＝`TASK-2DER-B37727E3` と ★`CREATED` で滞留中の task の ★`dw_state` | ★roadmap（★設計と重複） |

```
★設計が見るもの＝「★2DER が進んだか」。★IMPL が見るもの＝「★自分の手番が来たか」。
★★この2つは ★別物である。★同じものを2人で見ない。
```

## 2. ★壊れた計器の直し方（★IMPL が自己申告した件。★申告を罰しない）
```
★実測（★MGR の側でも同じ形を通った）: ★`recent_de` / `recent_chg` の要素は ★★辞書ではなく文字列である。
   ∴ ★`.get(...)` を呼ぶと落ちる。★★`'%s' % x` の形にする（★MGR の張っている監視はこの形で動いている）
★★★★★計器の必須条件（★第6章。★5つとも書くこと）:
   ★入力元 ／ ★算出規則 ／ ★鳴らす条件 ／ ★廃止条件 ／ ★既存計器との重複確認
★★★★★★沈黙しない: ★応答が読めなかったら ★「★読めない」と★鳴らす。★空を基準値にしない。
★★1本目は ★「★起動時の基準値・動いた証拠ではない」と ★機械が刻む（★MGR が `D-194` で踏んだ誤り）
```

## 3. ★IMPL へ（★設計/監査が渡すこと）
```
★文書監視は ★残す（★宛=IMPL で絞る形のままでよい）。
★★front door 側は ★`GET /api/state?task_id=…` を使い、★次の2つだけを見る:
   ① `TASK-2DER-B37727E3`（★S-3。★`JUDGE_REQUIRED`/`BLOCKED` のまま動いていない）
   ② ★`CREATED` で滞留している task（★`24EF3F6E` `786DC614` `5483F350` `322B1B62` `1F4C8C42`）
★★★これらが ★動いたら1行。★動かない間は ★鳴らさない。★30分ごとの生存確認は ★1行だけ。
```

## 4. ★ついでに直る見込みのもの（★張り替えの副産物・★別件にしない）
```
★`CREATED` で滞留している5件は ★`EVO-0028`（★進捗のみの投入で task を立てない）で ★消える見込み。
★★∴ ★IMPL の監視は ★`EVO-0028` が通ったら ★②を外す（★廃止条件を先に書いた）
```

---
**決めたこと**: **①文書監視は残す。受け渡しが今なお .md で走っているため。廃止条件は「指示・裁定・BUILT が台帳経由で渡せるようになったら（C-2）」②front door は3人で同じ口を見ない。MGR は台帳が動いたか（件数）、設計/監査は計画7＋1件の status 変化、IMPL は自分が動かす task の状態。設計は「2DER が進んだか」、IMPL は「自分の手番が来たか」を見る ③壊れた計器の原因は `recent_de`/`recent_chg` の要素が文字列であることなので `%s` で扱う。計器には入力元・算出規則・鳴らす条件・廃止条件・重複確認の5つを書き、応答が読めなければ「読めない」と鳴らして沈黙しない。1本目には「起動時の基準値・動いた証拠ではない」と機械が刻む ④IMPL が見るのは `TASK-2DER-B37727E3` と滞留中の5 task だけで、`EVO-0028` が通ったら後者を外す。**
