# 【A・訂正】CC 管理(MGR) → 設計/監査(CC-α): **★私の停止は広すぎた。★止めるのは2本。★96本は走らせてよい**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-30 / TYPE=HANDOFF
- **★2DER 優先原則の5点**: ①無し ②③該当しない（裁定） ④実装しない ⑤該当しない

## 1. ★私の裁定を訂正する（★先に書く）
```
私が D-106 §4-1 で書いた: 「★以後、非回帰98本を走らせない」
IMPL の実測          : ★本番の 2DER へ POST するのは ★2 / 98 本
∴ ★その2本を走らせなければ、★残り96本は本番へ1回も POST しない
```
> **★私は「全部やめる」と裁定した。** **★2本で足りた。**
> **★違反を止めるために、★96本の確かめを一緒に捨てていた。**
> **★これは「安全側」ではない。★粗いだけである。** **★訂正する。**

## 2. ★訂正後の裁定
| # | ★裁定（★D-106 §4 を置き換える） |
|---|---|
| **1** | **★走らせない: `test_concurrency_and_run_gate.py` と `test_dead_approach_guard.py` の★2本だけ** |
| **2** | **★96本は走らせてよい。** **★ただし「98本」と書かない。★「96本」と書き、★除いた2本の名前を書く** |
| **3** | **★基準値を取り直す**（★91/7 は98本のもの。★96本の数は★まだ誰も知らない） |
| **4** | **★`exit 0` を根拠に使わない**（D-107）。**★何本が skip したかを★同時に書く** |
| **5** | **★`G-74` の取り消しは★維持**（★走らせ方を版管理に残さない） |

**★受入から「非回帰」を外す、は★撤回する。** **★96本で戻す。** **★弱くならない。**

## 3. ★`:8005` の5本について（★私が決める）
```
IMPL 報告: 5本は :8005 のローカル LLM を使う（★2DER ではない。★GPU を実際に使う）
```
**★2DER の本番経路ではない ∴ ★Taka 常設命令の対象外。★走らせてよい。**
**★ただし BUILT に「★`:8005` を使った」と1行 書くこと**（★GPU を使うため。★黙って使わない）。

## 4. ★IMPL の自己申告を記録する（★2件とも良い）
| # | |
|---|---|
| 1 | **★「live を叩くのは2本だけ」は★探した範囲が狭かった**（★`100.107.6.119:8770` だけを探し、`localhost`/`127.0.0.1` を探していなかった） |
| 2 | **★「結論は変わらない。★しかし変わらなかったことを理由に、★狭かったことを消さない」** |

> **★2 が特に良い。** **★結果が同じでも、★測り方の欠けを消さない。**
> **★今日ずっと言ってきた「当たったが理由が違う」と同じ形である。**

## 5. ★「既に在った」をまた1つ見つけた（★記録する）
**`test_control_surface_endpoint.py` は★自前の `127.0.0.1:0` にサーバを立て、★書き先を scratch へ隔離している。**
> **★本番へ出さない型は、★既にこの中に在った。** **★本日 12回目の「既存の道が既に在った」である。**
> **★ただし今回は使わない。** **★2本を走らせなければ足りるからである**（★型を当てるのは「直す」であり、★いま要らない）。

## 6. ★Taka の3択を書き換える（★別紙を更新する）
```
★A（止めたまま進む）→ ★「2本だけ止める。96本は走らせる」に変わった ∴ ★損がほぼ無い
★B（向き先を直す）  → ★いま要らない（★2本を走らせなければ足りる）
★C（承知で続ける）  → ★変わらず不要
```
**★私は A（訂正後）を推す。** **★Taka の答えを待たずに §2 を実行してよい。**

---
*MGR【A・訂正】。★**MGR の裁定を訂正する**——D-106 で「以後 非回帰98本を走らせない」と書いたが、IMPL の実測では**本番の 2DER へ POST するのは 2/98 本**で**その2本を走らせなければ残り96本は本番へ1回も POST しない** ∴ **MGR は「全部やめる」と裁定したが2本で足り、違反を止めるために96本の確かめを一緒に捨てていた。これは「安全側」ではなく粗いだけなので訂正する**。★**訂正後の裁定**（D-106 §4 を置き換える）=(1)**走らせないのは `test_concurrency_and_run_gate.py` と `test_dead_approach_guard.py` の2本だけ** (2)**96本は走らせてよいが「98本」と書かず「96本」と書き除いた2本の名前を書く** (3)**基準値を取り直す**（91/7 は98本のもので96本の数はまだ誰も知らない）(4)**`exit 0` を根拠に使わず何本が skip したかを同時に書く** (5)**`G-74` の取り消しは維持** ∴ **「受入から非回帰を外す」は撤回し96本で戻すので弱くならない**。★**`:8005` の5本は MGR が決める**——**2DER の本番経路ではないので Taka 常設命令の対象外で走らせてよいが、GPU を使うため BUILT に「`:8005` を使った」と1行 書き黙って使わない**。★**IMPL の自己申告2件とも良い**=(1)「live を叩くのは2本だけ」は**探した範囲が狭かった**（`100.107.6.119:8770` だけを探し `localhost`/`127.0.0.1` を探していなかった）(2)**「結論は変わらない。しかし変わらなかったことを理由に、狭かったことを消さない」**——**2 が特に良く、結果が同じでも測り方の欠けを消さないのは今日ずっと言ってきた「当たったが理由が違う」と同じ形**。★**「既に在った」をまた1つ**=`test_control_surface_endpoint.py` が**自前の `127.0.0.1:0` にサーバを立て書き先を scratch へ隔離しており、本番へ出さない型は既にこの中に在った（本日12回目）**——**ただし今回は使わない（2本を走らせなければ足りるので、型を当てるのは「直す」でありいま要らない）**。★**Taka の3択を書き換える**=**A は「2本だけ止める。96本は走らせる」に変わり損がほぼ無い**／**B はいま要らない**／C は変わらず不要 ∴ **MGR は訂正後の A を推し、Taka の答えを待たずに §2 を実行してよい**。*
