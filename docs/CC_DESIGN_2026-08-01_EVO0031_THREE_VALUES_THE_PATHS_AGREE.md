# 【調査回答 / `EVO-0031`】**★(1)(2)(3) すべて一致している** — ★食い違っているのは★名前だけ

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-08-01 20:5x / TYPE=FINDING
- **開発者規律 確認済（版: v1.1 / 2026-08-01）**
- **★裁定の在り処**: `ITEM-2DER-EVO-0031` の `status_note`（★`.md` 無し伝達の ★★2回目・★`D-213` §4 の廃止条件が★満たされた）
- **★コードを1行も変えていない** ／ **★直し方を書かない**（★裁定の手番）

---

# 1. ★3値で答える（★逐語で辿った。★推測していない）

| 問い | 答え | 逐語の出所 |
|---|---|---|
| **(1) 成果物はどこに書かれるはずか** | **★`ws/impl.py`**（★`ws` ＝ `sandbox_root/ws-<sha1>` の隔離作業域） | `generate_via_runner.py:99` `"target_file": "impl.py"` ／ `run_runner` が `sandbox_root = tempfile.mkdtemp(prefix="2der_runner_")`（同 `:90`）／ `live_worker_runtime.py:153` `ws = os.path.join(sandbox_root, "ws-" + …)` |
| **(2) 実際に書かれているか** | **★書かれる**（★worker が `ws` に1ファイル変更を作る） | 同 `:161` 逐語 `result = (worker or LocalWorker()).run(tp, ws)` ／ ★試験ファイルは先に置かれる（同 `:158-159` 逐語 `open(os.path.join(ws, tp["test_file"]), "w")`） |
| **(3) 試験の cwd と一致しているか** | **★★一致している** | 同 `:169` 逐語 `test = _run_test(ws, result.get("test_command"))` ／ `:59-71` 逐語 `def _run_test(workspace, …)` … `subprocess.Popen(test_command, ★cwd=workspace, …)` |

```
★★★★∴ ★★場所は ★食い違っていない。★★`impl.py` も `test_impl.py` も ★同じ `ws` に置かれ、
   ★試験は ★その `ws` を ★cwd にして走る。
★★★★★★∴ ★★MGR の手がかり（★`test_command` が絶対パスとファイル名だけで違っていた）は
   ★★★原因ではない。★★どちらでも ★cwd が `ws` なので ★ファイル名だけで届く。★★候補から落としてよい。
```

---

# 2. ★★∴ 食い違っているのは ★★名前だけである

```
★`ws` に在るのは ★★`impl.py`。★★契約の試験が import しているのは ★★`human_view`。
★★★実測のエラー（★MGR が note に逐語で書いた）: ★`ModuleNotFoundError: No module named human_view`
★★★★★∴ ★★★場所の問題ではなく ★★名前の問題である。★★これで原因は ★確定した。
★★★★★★（★私が前回 書いた「★契約が存在しない名前を import している」は ★★これで裏づけられた）
```

---

# 3. ★★webui の再起動（★MGR の追加依頼）— **★要る**

```
★状況表 逐語: 「実行中プロセス: twoder.webui(pid 853291, 0h前起動: ★ソースが新しい=再起動が要る)」
★★私の実測: ★`GET /api/claude_packet` に ★`runner_exit` が★出ている ∴ ★★`webui.py` の変更は ★効いている
   ＝ ★★`EVO-0031` の2欄については ★再起動は ★済んでいる（★出ているのが証拠）
★★★★∴ ★★「要る」のは ★その後に入った変更のためである【★★どの変更かは ★私は測っていない・未確認】
★★★★★★∴ ★★次に何かを測る前に ★1回 再起動すること。★★測ってから「効かない」と言わないため。
```

---

# 4. ★直し方は書かない（★裁定の手番）— **★材料の更新だけ**

```
★前回 私が出した2案は ★そのまま残る:
   ★(A) ★契約を ★`impl.py` に合わせて書く ＝ ★依頼文の作法
   ★(B) ★runner が ★契約の `target_file` を尊重する ＝ ★機構
★★★★今回 分かったことで ★1つ 材料が増えた:
   ★★`generate_via_runner.py:99` は ★`target_file` を ★★固定で上書きしている（★契約に在っても使わない）
   ∴ ★★(B) は ★「★尊重するようにする」＝ ★★その1行を ★契約の値に差し替える形になる【★未確認: 影響範囲】
★★★★★★どちらを選ぶかは ★あなたです。★★私は選びません。
```

---

# 5. ★やっていないこと
```
★コードを1行も変えていない ／ ★webui を再起動していない ／ ★front door へ投入していない
★契約を書き直していない ／ ★commit していない ／ ★(B) の影響範囲を測っていない【未確認】
★★★`LocalWorker().run(tp, ws)` の中（★worker が実際に何というファイル名で書くか）は ★読んでいない【★未確認】
   ★★理由: ★★`allowed_files` が `["impl.py","test_impl.py"]` に固定されている（`:106` 逐語）ので
      ★それ以外の名前で書けば ★安全検査で落ちるはずである ∴ ★★`impl.py` で書かれると読んだ。
   ★★★★★これは ★推論であって ★実読ではない。★★そう書く。
```

---
**決めたこと**: **①3値で答えた——(1) 成果物は `sandbox_root/ws-<sha1>/impl.py` に書かれるはずで、(2) worker が実際にその `ws` に書き（試験ファイルも先に同じ `ws` へ置かれる）、(3) 試験は `subprocess.Popen(test_command, cwd=workspace)` で同じ `ws` を cwd にして走る ②∴ 場所は食い違っていない。MGR の手がかり（`test_command` の絶対パスとファイル名だけの違い）は原因ではなく、cwd が `ws` なのでどちらでも届く。候補から落としてよい ③食い違っているのは名前だけである——`ws` に在るのは `impl.py`、契約が import しているのは `human_view`。実測のエラーは `ModuleNotFoundError: No module named human_view` で、これで原因は確定した ④webui の再起動は要る。ただし `runner_exit` が出ている以上 `EVO-0031` の2欄については再起動済みであり、要るのはその後に入った変更のため（どの変更かは測っていない）。次に何かを測る前に1回 再起動すること ⑤直し方は書かない。前回の2案（契約を `impl.py` に合わせる／runner が契約の `target_file` を尊重する）はそのまま残り、材料が1つ増えた——`generate_via_runner.py:99` は `target_file` を固定で上書きしているので、(B) はその1行を契約の値に差し替える形になる（影響範囲は未確認）⑥`LocalWorker().run` の中は読んでいない。`allowed_files` が固定なので `impl.py` で書かれると読んだが、これは推論であって実読ではない。**
