# 【調査回答】**★再起動は要らない** — ★状況表の「要る」は★1秒差の見かけである（★実測で判定）

- `BUILD_ROLE: 参照` / **宛: MGR** / 写: Taka / IMPL / 発: 設計/監査(CC-α) / 2026-08-01 21:0x / TYPE=FINDING
- **開発者規律 確認済（版: v1.1 / 2026-08-01）**
- **★裁定の在り処**: `ITEM-2DER-EVO-0031` の `status_note`（逐語:「★webui がソースより古い件(再起動の要否)は未回答なので設計/監査が併せて確認すること」）
- **★コードを1行も変えていない** ／ **★webui を再起動していない**

---

# 0. ★★先に、私の前回の答えを訂正する

```
★私は前回 §3 で「★要る」と書いた。★★根拠は ★状況表の記述（★伝聞）だった。
★★★MGR が「★未回答」と書いたのは ★正しい。★★私は ★測っていなかった。
★★★★★今回 ★測った。★★答えは ★★★「要らない」である。
```

---

# 1. ★測ったこと（★全数・逐語）

| | 時刻 |
|---|---|
| **webui プロセスの起動** | **`08-01 20:19:01`**（pid 853291） |
| `twoder/webui.py` の最終更新 | **`08-01 20:19:02`** ← **★1秒 後** |
| `twoder/submit.py` | `08-01 07:47:46`（★起動より古い） |
| `twoder/contract_seal.py` | `08-01 15:28:07`（★古い） |
| `twoder/request_template.py` | `08-01 15:28:07`（★古い） |
| `twoder/progress_seal.py` | `08-01 02:45:05`（★古い） |
| `twoder/generate_via_runner.py` | `07-28 04:24:37`（★古い） |
| `dev-workcell/dw/workcell.py` | `08-01 13:03:53`（★古い） |
| `dev-workcell/dw/dispatch.py` | `08-01 12:00:50`（★古い） |

```
★★★∴ ★★「ソースが新しい」と出ているのは ★★`webui.py` の ★★1ファイルだけ、★★差は ★1秒 である。
```

## 1-1. ★★その1秒が何だったか（★git で確かめた・★推測していない）
```
★`git log -- webui.py` 逐語: ★`e3a7827 ★20:25:09 First MGR instruction to arrive without a .md: …`
★★未 commit の差分: ★★0件（★`git diff --stat -- webui.py` が空）
★★★∴ ★★`20:19:02` の更新は ★★commit（20:25:09）に含まれた内容と★同じであり、
   ★★★★いま作業ツリーに ★未反映の変更は ★★1つも無い。
```

## 1-2. ★★★決め手（★mtime ではなく ★★挙動で判定した）
```
★★走っている webui に ★何が効いているかを ★front door で ★直接 確かめた:
   ★`runner_exit` 欄 ＝ ★★在る（値 2）      ← ★`EVO-0031`（★20:1x の変更）が★効いている
   ★`upper_reviews` 欄 ＝ ★★在る            ← ★`EVO-0030`（★12:4x の変更）が★効いている
   ★`next_request_template` 欄 ＝ ★★在る    ← ★C-2（★14:4x の変更）が★効いている
★★★★∴ ★★本日 入れた ★webui 側の変更は ★★3つとも ★走っている。
★★★★★★＝ ★★「ソースが新しい」は ★★1秒の書き込み順の見かけであって、★★未反映の機能は ★★無い。
```

---

# 2. ★答え

> ### **★再起動は ★要らない。**

```
★★根拠は ★mtime ではなく ★★挙動である（★★3つの欄が ★実際に出ている）。
★★★★状況表の「★ソースが新しい＝再起動が要る」は ★★mtime の比較であり、★★1秒差でも「新しい」と出る。
   ★★★★★計器としては ★正しい（★安全側に鳴る）。★★ただし ★★「鳴った＝要る」ではない。
★★★★★★∴ ★★次に何かを測る前に ★★挙動で確かめること（★mtime だけで判断しない）。★これが私の答えである。
```

---

# 3. ★★私が今回やった順序（★書いておく・★本日の反省の適用）

```
★★本日 私は ★6回 ★「片側だけ見て全体を言った」。
★★★今回は ★★3つ 重ねた:
   ★① ★mtime を全ファイル 比べた（★1ファイル・1秒差と分かった）
   ★② ★git で ★その1秒が commit 済みかを確かめた（★未 commit 0件）
   ★③ ★★front door で ★挙動を確かめた（★3欄とも在る）
★★★★★①だけなら「要る」、②だけなら「要らないはず」、★★③で ★確定した。
★★★★★★★「★ソースに在る≠動く」の ★逆（★「mtime が新しい≠効いていない」）も ★同じ形である。
```

---

# 4. ★やっていないこと
```
★webui を再起動していない ／ ★コードを1行も変えていない ／ ★front door へ投入していない
★commit していない ／ ★台帳を直読していない
★★`20:19:02` に ★★何の文字が変わったかは ★見ていない【★未確認】——★★見る必要が無かった
   （★★commit 済みで ★未反映の差分が0件 ∴ ★★内容を見なくても「未反映は無い」が言える）
★★★`EVO-0031` の直し（★契約を `import impl` に直して投げ直す）は ★★MGR の手番（★裁定の逐語どおり）
```

---
**決めたこと**: **①前回 私が「要る」と書いた根拠は状況表の伝聞であり、MGR が「未回答」としたのは正しい。今回 測った ②測定——webui の起動は `20:19:01`、`webui.py` の更新は `20:19:02` で差は1秒。他の7ファイルはすべて起動より古い ③その1秒は commit（`e3a7827` 20:25:09）に含まれており、未 commit の差分は0件なので、作業ツリーに未反映の変更は1つも無い ④決め手は mtime ではなく挙動である——front door で確かめると `runner_exit`（`EVO-0031`）・`upper_reviews`（`EVO-0030`）・`next_request_template`（C-2）の3欄がすべて出ており、本日入れた webui 側の変更は3つとも走っている ⑤∴ 再起動は要らない。状況表の「ソースが新しい」は mtime 比較で1秒差でも鳴る安全側の計器であり、「鳴った＝要る」ではない。次に何かを測る前に挙動で確かめること ⑥今回は①mtime 全比較 ②git で commit 済みか ③front door で挙動、の3つを重ねた。①だけなら「要る」、②だけなら「要らないはず」、③で確定した。「ソースに在る≠動く」の逆（「mtime が新しい≠効いていない」）も同じ形である ⑦`20:19:02` に何の文字が変わったかは見ていない。commit 済みで未反映が0件なので、内容を見なくても「未反映は無い」が言える。**
