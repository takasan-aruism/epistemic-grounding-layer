# 【D-149】★観測は task の下流に無い — **★Taka の図と実装が違う。★GENERATE へ進まない**

- `BUILD_ROLE: 参照` / **宛: 設計/監査(CC-α)** / 写: Taka / IMPL / 発: MGR / 2026-07-31 02:5x / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** ／ **正典**: `TAKA_2026-07-31_RUNTIME_INSPECTION_COMMON_BASE_ORDER.md`

## 1. ★監査を依頼する（★私はまだ Taka へ成果として上げない）
```
★IMPL の BUILT は未監査である（運用方針 §3-1）。★あなたが取り直すまで、私は確定しない。
★特に取り直してほしいもの:
   (a) B の provenance 3件（trace_id / rri_request_id / rri_intent_id）が実際に載っているか
   (b) plan_source=QWEN_BUILD_PLANNER と runtime_recovery.attempts=2 が、★:8005 の2件と一致するか
   (c) ★B-4「PLAN 由来の新規観測は0」（★これが §2 の根拠である）
```

## 2. ★私が読み取った構造（★これが今回いちばん重い）

> ### **★Taka が描いた一本の線は、★実装には存在しない。**

```
★Taka の図        : Request → RRI → Task → ★Runtime → Observation → Ledger → DW → Response
★実測で見えた形   :
     submit ─┬→ RRI → ★runtime inspection → observation → 応答（★ここで観測は完結している）
             └→ Task → PLAN → GENERATE →（★worker が「物を作る」道）
```
**★根拠（すべて今回の実測）:**
```
★観測は task が無かった第1・第2試行でも走った ＝ ★task を必要としない
★A では run_next を押す前の submit 応答に OBS-00967〜970 が既に載っていた ＝ ★観測は submit 内で完結
★B で PLAN が動いたが、★PLAN 由来の新規観測は0（ARUN-00974/OBS-00975 は resolved=false）
★B の PLAN の中身は「port_checker.py を実装せよ」＝ ★物を作る計画であって、観測を実行する計画ではない
★PLAN の cites_source_ids は★空 ＝ ★取った観測を引用してもいない
```
> **∴ ★`Task → Runtime` の矢印は「まだ繋がっていない」のではなく、★実装ではそもそも別の道である。**
> **∴ ★これを繋ぐのは「配線の修理」ではなく「★経路をどう設計するか」の話になる。**
> **∴ ★私は裁定しない。★Taka の案件である**（運用方針 §7-1 の1＝目的・優先順位そのもの）。

**★いま「図が間違っている」とは書かない。** **★「実装が図と違う」だけを書く。** **★どちらを直すかは Taka が決める。**

## 3. ★GENERATE へ進まない（★裁定）
```
★IMPL は次を「GENERATE を1回押すか」とした。★押さない。
★理由: GENERATE は「★port_checker.py を作る」道であり、★正典の目的（Runtime Inspection の共通基盤）
       とは★別の道である ∴ ★押しても Taka の8点は進まない。
★かつ 2DER が書いたコードを production に入れるかは★Taka の案件（D-129 §3-3・据え置き）。
★★「せっかく動いたから次も押す」をしない。
```

## 4. ★8点の現状（★私の確定案。★監査後に固める）

| 配線 | 判定 | 根拠 |
|---|---|---|
| Request / RRI / Task | **PASS** | B で3点とも通った |
| **Runtime / Observation** | **★PASS（★但し「task の下流としてではない」）** | 走って引ける。**★ただし §2** |
| **Ledger** | **★FAIL ← First FAIL** | ★生出力（`blob://…`）に到達できない（★今回も触っていない） |
| DW / Response | **FAIL** | 要約が無い／生の配列 |

## 5. ★次に直す1件（★変えない）
> **★`Ledger` — 生出力に既存の口から到達できるようにする。** **★D-145 から順番を待っていたものである。**
> **★§2 は「修正」ではなく「裁定待ち」∴ 1件の枠を使わない。**

## 6. ★共通基盤か（★正典の最終報告 4項目め・★これは答えが出た）
```
★出た: ★同じ入口・同じ _CATALOG で、★GPU 以外（待ち受けポート）が通った。
★PLAN の中身も port の話になった ＝ ★GPU 専用ではない。
★我々が足したコードに `gpu`/`nvidia` は0件（★2回とも diff を走査）。
★★ただし「選別が効いていない」は残る（build_request が毎回 全件を要求＝既存 G-06）。
```

## 7. 手番
```
★あなた: §1 の監査 → 判定案を1本
★私    : 確定 → Taka へ最終報告（4項目）＋ §2 を裁定要求として上げる
★Taka  : ★§2 に答える（★それまで我々は先へ進まない）
```

---
**決めたこと**: **①IMPL の BUILT は未監査 ∴ 設計が取り直すまで Taka へ成果として上げない ②観測は task の下流に無く、実装は「submit→観測→応答」と「Task→PLAN→GENERATE（物を作る）」の2本である——これは配線の修理ではなく経路設計の話なので Taka の案件 ③GENERATE へ進まない（別の道であり、押しても8点は進まない） ④次に直す1件は `Ledger`（生出力への到達）のまま ⑤共通基盤かは答えが出た＝GPU 以外が同じ入口で通った。**
