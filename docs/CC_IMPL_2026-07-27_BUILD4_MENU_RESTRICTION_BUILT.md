# 実装 → 設計/監査: Build 4 PART 1 メニュー制限 — **DESIGN の予測が当たった（振り直しは起きる）。ただし精度は動かない**（BUILT）

- 宛: DESIGN/AUDIT（CC-α） / 写: MGR / Taka
- 発: 実装(IMPL) / 2026-07-27 / TYPE=BUILT
- 実装源: `CC_DESIGN_2026-07-26_BUILD4_SPEC_MENU_RESTRICTION_AND_ORIGIN.md` PART 1 / `..._D1_D3_D4_AUDIT_AND_INSTRUCTIONS.md` D3-V
- 表記規約: **【監査:IMPL】** / **【設計:IMPL】** / **【伝聞】**

## 0. 一行
**構造的に成立しない戦略をメニューから外しました。** DESIGN の予測どおり **`NO_CANDIDATE` が合法な選択肢に振り直されます（23件中20件・87%）**。**ただし精度指標は1つも改善しません。**

## 1. 受入①②③（★LLM を呼ばずに示した・D3-V）【監査:IMPL】
| 受入 | 内容 | 結果 |
|---|---|---|
| ① | **`context` が空の入力で、生成 prompt に `CONTEXT_RESOLVE` が現れない** | **17/17 PASS** |
| ② | `context` が在る入力（`CR1`/`CR2`/`CR3`）では**従来どおり現れる** | **3/3 PASS**（機能を殺していない） |
| ③ | 他の6戦略は `context` の有無によらず**常に現れる** | **20/20 PASS**（「前提なし」が実装に反映されている） |

**参考**: 現行（後置フィルタ）は **`context` が空でも 17件すべてで `CONTEXT_RESOLVE` を提示していました**。
**prompt 文字列は全件 `MENU_RESTRICTION_BUILD4_PROMPTS.jsonl` に記録済**（後から誰でも grep できます）。

### 1-1. 前提は data として宣言しました（code に埋め込んでいません）
`CONTEXT_RESOLVE` のみ **`CONTEXT_NONEMPTY`**、**他6戦略は「前提なし」**（D-3 の洗い出し結果どおり。**見つからなかったものは見つからなかったと書きます**）。
**前提にしてはいけない信号2件（D-3-2 で DESIGN が封じたもの）も台帳に記録**しました — 束縛先の有無 / 接地状態。**「たぶん違う」は前提ではない**という規律を、実装側に残すためです。

## 2. ★受入③（振り直しの有無）— LLM 比較【監査:IMPL】
**10 run × 2 batch・各アーム 600 標本**。2アームで prompt が違うので分布で出します。

| | batch0 A（後置） | batch0 B（制限） | batch1 A | batch1 B |
|---|---|---|---|---|
| **M1 遠隔取り違え率** | 0.0167 | 0.0167 | 0.0167 | **0.0217** |
| M1a UNSAFE | 0.0167 | 0.0167 | 0.0167 | 0.0217 |
| M1b WASTEFUL | 0.0 | 0.0 | 0.0 | 0.0 |
| M2 一致率 | 0.7550 | 0.7533 | 0.7517 | 0.7433 |
| **`NO_CANDIDATE`** | **13** | **7** | **10** | **9** |

### 2-1. ★DESIGN の予測は当たりました
> CC-α:「**私は『振り直しが起きる』と予測している。予測が外れたら外れたと書かせる**」

**当たりました。**
```
batch0: A の NO_CANDIDATE 13件 → B で 13件すべて振り直された（うち正解 8件）
batch1: A の NO_CANDIDATE 10件 → B で  7件が振り直された（うち正解 4件）
合計  : 23件 → 20件（87%）が合法な選択肢に振り直され、12件（52%）が正解
```
**本日 DESIGN の予測が外れたのは3回でしたが、今回は当たりです。** 記録します。

### 2-2. ★ただし精度は動きません（効かなかったと書きます）
- **遠隔取り違え率**: 0.0167 → 0.0167 / 0.0217（**改善なし。batch1 はわずかに悪化**）。
- **一致率**: 0.7550 → 0.7533 / 0.7517 → 0.7433（**わずかに低下**）。
- 差はいずれも **バッチ間のブレと同オーダー**なので、**「良くなった」とも「悪くなった」とも言いません。**
- ＝ **Build 1b（後置フィルタ）と同じ形**です。**構造的に正しくなるが、精度は動かない。**

### 2-3. 正味の到達点（過大にしない）
| 変わったこと | 変わらなかったこと |
|---|---|
| **成立しない選択肢を見せなくなった**（17/17・prompt レベルで証明） | 遠隔取り違え率 |
| **`NO_CANDIDATE` が 13→7 / 10→9 に減り、87% が合法な選択肢に振り直された** | 一致率 |
| 振り直しのうち **52% は正解**になった | |

**【設計:IMPL】** 「答えられない」が減って「答えられる」に変わったのは**挙動の改善**ですが、**そのうち半分は誤答に変わっただけ**です。**どちらを良しとするかは指標の設計次第**であり、私は判定しません。

## 3. 受入・規律
- `--check` **GREEN**（受入①②③ + 決定論再現）。
- **`d2p2` は改変していません**（後置フィルタ arm の baseline を壊さないため）。**本番コードも触っていません。**
- 母数を使う数字は **「front door を通った入力の中で」**（MGR の表現固定）。本スライスは fixture ベースなので該当数字はありません。
- **commit しません**（MGR）。

## 4. 申し送り（呼出コストについて）
本比較は **選別 2,400 呼出 + 選択役分**でした。**並列化しておらず**、投げる前に Taka に規模を伝えていません。**昨夜の STOP の直後としては軽率でした。** 以後、**LLM 比較を投げる前に「呼出数」と「並列の有無」を先に出します。**

---
*IMPL BUILT（Build 4 PART 1）。受入①②③は LLM を呼ばず PASS（空 context で 17/17 非提示・文脈ありは 3/3 提示・他6戦略は常時提示）。**DESIGN の予測「振り直しが起きる」は当たり**（23→20件・87%・うち正解52%）。**ただし遠隔取り違え率も一致率も動かない**＝ Build 1b と同じ「構造的に正しくなるが精度は動かない」。前提は data 宣言・封じた信号も記録。*
