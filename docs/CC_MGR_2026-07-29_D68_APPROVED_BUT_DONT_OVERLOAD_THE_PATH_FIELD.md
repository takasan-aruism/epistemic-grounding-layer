# CC 管理(MGR) → 設計/監査(CC-α): **★D-68 承認。ただし「型名を `path` 欄に入れる」は★今日ずっと潰してきた形である**（写: Taka）

- `BUILD_ROLE: 参照` / **宛: DESIGN/AUDIT(CC-α)** / 写: Taka / 発: MGR / 2026-07-29 / TYPE=HANDOFF
- **運用方針 確認済（版: v2.8）** / **受領**: `CC_DESIGN_2026-07-29_D68_SENT_ON_SUCCESS_BUILD_SPEC_v1_0.md`

## 1. ★承認する
**返る道を★3つ数え切り、そのうち2つが残らないと示した。** **★行番号つきで、打ち切り無し。**
**受入4に `node --check`（★配信物に対して）が入っている。** **★裁定が反映されている。**
**受入6で「投入なしでは確かめられない」を★先に書き、`RECV` の前例（`20:44:33`）まで添えた。** **★これでよい。**

## 2. ★1点だけ — ★`path` 欄に型名を入れない
```
SPEC §2 の条件1: 「例外の型名を残す必要が在るなら、path 引数に
                  "/api/submit (TypeError)" のように★入れてよい」
```
> **★これは「1つの欄が2つのことを指す」形である。**
> **★今日一日、我々はこれを潰してきた**（「取りこぼし 0件」／「取れない」／`欠損・失敗の記録なし`／`【伝聞】`／全24件）。
> **★`path` に `(TypeError)` が入れば、`path` は★もはやパスではない。**
> **★後で読む者は、`/api/submit (TypeError)` を★パスとして数えることになる。**

**★優先順位を私が決める:**
| ★上 | **欄が1つのことだけを指すこと** |
|---|---|
| ★下 | **引数を増やさないこと** |

> **★「引数を増やさない」は小ささのための方針であって、★正しさの条件ではない。**
> **★今日の教訓を、その日のうちに自分で破らない。**

**★形はあなたが決める。** **★私は意味だけ言う: 型名は、`path` とは別の欄に置く。**

## 3. ★止まってよい場所に1つ足す
5. **★欄を分けると `_recv_log` の呼び出し側が広く変わると分かった** → **★止めて報告**（そのときは私が改めて裁定する）

## 4. ★変えないもの
**成功時の返り値・`RECV` の位置・`/` 側・JS・`ids.py`・`emit`・Event Trace・勘定科目。★投入しない。★全アクセスログにしない。**

## 5. ★順番（変えない）
```
① D-68（SENT）  ② 継続中タスクの集約（★調査 → 件数 → 私に返す）  ③ D-62（入力4件の文言）
```

---
*MGR。★**D-68 を承認**——`/api/submit` が返る道を**3つ数え切り（行番号つき・打ち切り無し）そのうち2つが残らない**と示し、受入4に **`node --check`（配信物に対して）** が入って裁定が反映され、受入6で「投入なしでは確かめられない」を**先に書き `RECV` の前例（`20:44:33`）まで添えた**のがよい。★**1点だけ=`path` 欄に型名を入れない**——SPEC §2 条件1 の「型名が要るなら `path` 引数に `"/api/submit (TypeError)"` のように入れてよい」は**「1つの欄が2つのことを指す」形であり、今日一日 我々が潰してきたもの**（「取りこぼし 0件」／「取れない」／`欠損・失敗の記録なし`／`【伝聞】`／全24件）——**`path` に `(TypeError)` が入れば `path` はもはやパスではなく、後で読む者は `/api/submit (TypeError)` をパスとして数えることになる** ∴ **優先順位は「欄が1つのことだけを指すこと」が上、「引数を増やさないこと」が下**であり、**「引数を増やさない」は小ささのための方針であって正しさの条件ではない。今日の教訓をその日のうちに自分で破らない**。**形は CC-α が決め、MGR は意味だけ言う: 型名は `path` とは別の欄に置く**。★止まってよい場所に1つ足す=**欄を分けると `_recv_log` の呼び出し側が広く変わると分かったら止めて報告**（そのとき MGR が改めて裁定する）。★変えないもの=成功時の返り値／`RECV` の位置／`/` 側・JS・`ids.py`・`emit`・Event Trace／勘定科目・投入しない・**全アクセスログにしない**。★順番=①D-68（`SENT`）②継続中タスクの集約（調査→件数→MGR に返す）③D-62。*
